# Task Map: 184-classic-perimeter-flow-parity

| docs/07 task ID | Packet step | Primary docs | Expected code surface | OrcaSlicer refs | Context cost | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| `TASK-303` | `Step 1` | none required | `crates/slicer-runtime/tests/integration/classic_wall_width_resolution_tdd.rs` (new), `crates/slicer-runtime/tests/integration/main.rs` | `Flow.cpp::auto_extrusion_width` | `S` | RED: proves classic resolves the canonical `0` auto sentinel to a literal `0.0` today instead of `1.125 × nozzle_diameter`. Second test locks the absent-key `0.4` fallback that must **not** move. |
| `TASK-303` | `Step 2` | `docs/15_config_keys_reference.md` (delegated `rg` only) | `modules/core-modules/classic-perimeters/classic-perimeters.toml`, `ClassicPerimeters::run_perimeters` R2 block (`modules/core-modules/classic-perimeters/src/lib.rs`), `crates/slicer-runtime/tests/integration/manifest_default_reconcile_tdd.rs` | `PrintConfig.cpp::PrintConfigDef::init_fff_params`, `Flow.cpp::new_from_config_width`, `Flow.cpp::auto_extrusion_width` | `S` | **D-164.** Retype both wall-width keys to `float_or_percent` / `min = 0.0`, hold `default = 0.4`; resolve via `ConfigView::get_abs_value` with the `1.125 × nozzle_diameter` auto arm. Requires reordering the `nozzle_diameter` read above the widths to break the read cycle. |
| `TASK-303` | `Step 3` | none required | `crates/slicer-runtime/tests/integration/outer_inner_width_and_spacing_tdd.rs` | `PerimeterGenerator.cpp::process_classic` | `S` | RED: expectations re-derived by **calling** `slicer_core::flow::line_width_to_spacing`, never transcribed. `expected_outer_right` (4.75) is unchanged — canonical's first-loop inset is `ext_perimeter_width / 2`, which PnP already matches. Adds the negative-spacing fatal-error test. |
| `TASK-303` | `Step 4` | `docs/08_coordinate_system.md` (delegated SUMMARY only if needed) | `ClassicPerimeters::emit_walls` inset loop + gap-fill block, new `ERR_NEGATIVE_SPACING` (`modules/core-modules/classic-perimeters/src/lib.rs`) | `PerimeterGenerator.cpp::process_classic` | `M` | **D-105 / `T-052-WIRE-FLOW`.** Non-precise `ext_perimeter_spacing2` becomes the mean of *spacings*; `i >= 2` inset becomes `perimeter_spacing`; gap-fill `min` uses `min(perimeter_width, ext_perimeter_width)` and `max` uses the true spacing. The **precise** branch's mean-of-widths is already canonical and stays. No `emit_walls` signature change — all five call sites untouched. |
| `TASK-303` | `Step 5` | none required | `crates/slicer-runtime/tests/fixtures/golden/precision_legacy_20mmbox.gcode` | none | `S` | Recorded-output fallout of Step 4. This golden is **classic-produced** (`DEFAULT_WALL_GENERATOR = "classic"`; the file records `; wall_generator = Classic`), so this packet moves it and the arachne sibling does not. Requires `cargo build --workspace` first — the test shells out to a `pnp_cli` binary on disk. Also re-verifies the two-sided `arachne_structural_invariants` ratio. |
| `TASK-303` | `Step 6` | `docs/15_config_keys_reference.md` (delegated `rg` only) | `crates/slicer-runtime/tests/integration/classic_min_width_top_surface_tdd.rs` (new), `crates/slicer-runtime/tests/integration/main.rs` | `PerimeterGenerator.cpp::split_top_surfaces` | `S` | RED: proves every top sub-area collapses to one wall today because `min_width_top_surface` is read and discarded (`let _ = min_width_top_surface;`). |
| `TASK-303` | `Step 7` | `docs/15_config_keys_reference.md` (delegated `rg` only) | `ClassicPerimeters::run_perimeters` `only_one_wall_top` sites + new bbox-extent helper (`modules/core-modules/classic-perimeters/src/lib.rs`), `modules/core-modules/classic-perimeters/classic-perimeters.toml`, `crates/slicer-runtime/tests/integration/manifest_default_reconcile_tdd.rs` | `PerimeterGenerator.cpp::split_top_surfaces` | `S` | **D-152.** Mirrors the already-landed `ArachnePerimeters::emit_only_one_wall_top_second_pass` (`retain` + `offset2_ex`) at the **call site** — `slicer_core::top_surface_split::split_top_surfaces` keeps its signature because classic reuses it for a second, overhang-masked purpose. Converges the manifest entry with arachne (`float_or_percent` / `"0.0"`), resolving the classic-vs-arachne divergence recorded in docs/15. |
| `TASK-303` | `Step 8` | `docs/15_config_keys_reference.md`, `docs/DEVIATION_LOG.md`, `docs/07_implementation_status.md` | none (docs only) | none | `S` | `cargo xtask gen-config-docs` + `cargo xtask check-deviations`; hand edits only outside `BEGIN GENERATED` markers; three rows flipped to `Closed`; one **new** residual-gap row filed under an ID derived at the moment of writing. |

Backlog anchors: deviations `D-164-WALL-WIDTH-KEYS-NOT-FLOAT-OR-PERCENT` (classic half), `D-105-FLOW-NOT-WIRED` (classic half; roadmap task `T-052-WIRE-FLOW`), and `D-152-CLASSIC-MIN-WIDTH-TOP-SURFACE-REMAINDER` in `docs/DEVIATION_LOG.md`. All three are also listed in the generated open-deviations block of `docs/07_implementation_status.md`, which is regenerated via `cargo xtask check-deviations` and never hand-edited. Batch home: `docs/specs/deviation-backlog-remediation-plan.md` §Packet Queue row 4 — **owned by the batch orchestrator; this packet does not edit it.**

**Ledger-fact warning (re-derive, never quote).** `TASK-303` was free against **both** `docs/07_implementation_status.md` and `.ralph/specs/**` **at authoring time only**. No "highest present" figure is recorded here on purpose — it is mutable shared state that rots while you work (an earlier draft of this note quoted a `.ralph/specs/**` high-water mark that sibling packets had already overtaken). Re-derive from both sources with `rg -o 'TASK-[0-9]{3}' docs/07_implementation_status.md | sort -u | tail -5` and `rg -o 'TASK-[0-9]{3}' .ralph/specs/ --no-filename | sort -u | tail -5` before trusting it — packet 181's first allocation collided precisely because only `docs/07` was checked. The same applies to the new deviation ID in Step 8: derive it with `rg -o '^\| DEV-[0-9]{3}' docs/DEVIATION_LOG.md | sort -u | tail -1` at the moment you write the row, never from a number quoted in this packet.

**Cross-packet collision.** `crates/slicer-runtime/tests/arachne_structural_invariants.rs` asserts `min(a,c)/max(a,c) >= COVERAGE_THRESHOLD` (`0.99`) with **classic on both sides of the ratio**. This packet moves it and so does the arachne T2 sibling packet in the same batch. Whichever lands second must re-run it; neither may lower the threshold.
