# Task Map: 193-overhang-distance-prepass-carrier

This packet groups a single task ID. The crosswalk is carried anyway because that ID is **newly allocated** and has zero hits in `docs/07_implementation_status.md` at authoring time — registering it there is packet work (Step 11, `packet.spec.md` §Doc Impact Statement), and this table is the mapping that registration must reproduce.

Its backlog authority is not `docs/07` but `docs/specs/deviation-backlog-remediation-plan.md` §Packet Queue **row 9a2**, created by that plan's `Queue amendment (2026-07-25d)` recording the maintainer's **option (C)** ruling on packet 190's `[BLOCK-1]`/`[BLOCK-1b]`/`[BLOCK-2]`/`[BLOCK-3]` set. Re-derive the row at the moment of use with `rg -n '^\| 9a2 ' docs/specs/deviation-backlog-remediation-plan.md` — the rows begin at column 1, so `^` must anchor directly on the leading `|`.

**The TASK id is the one ledger fact this packet cannot avoid freezing, and it must be re-derived before Step 11 writes the row.** Row 9a2 was added *without* a TASK id, unlike every other row covered by that amendment's central-allocation paragraph (186→305 … 192→311). Two sources disagree and both must be consulted:

```
rg -o 'TASK-[0-9]{3}' docs/07_implementation_status.md | sort -u | tail -1
rg -o 'TASK-[0-9]{3}' .ralph/specs --no-filename | sort -u | tail -1
```

The specs tree runs **ahead** of `docs/07`, because several packets in this batch allocated ids they have not registered yet. Take the next free number above the higher of the two. If that is no longer the id in `packet.spec.md`'s frontmatter, change it in all five files of this packet before Step 1.

| docs/07 task ID | Packet step | Primary docs | Expected code surface | OrcaSlicer refs | Context cost | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| (re-derive) | `Step 0` | none | none (writes `target/pin-*-schema-before.txt` scratch only) | none | `S` | Pins both pre-packet schema versions so `AC-2` can assert a **relative** additive bump instead of a hardcoded SemVer, and re-derives the struct-literal sweep sizing that Steps 3-9b are graded against. Both outputs are ledger facts: the pins live in `target/`, the sizing lives in the swarm log, neither in a tracked file. |
| (re-derive) | `Step 1` | none | `crates/slicer-core/tests/overhang_distance_carrier_tdd.rs`, `crates/slicer-ir/tests/point3_overhang_distance_roundtrip.rs`, `modules/core-modules/arachne-perimeters/tests/overhang_distance_tdd.rs` (all new) | `ExtrusionProcessor.hpp` — `estimate_points_properties` (the `distance + boundary_offset` assignments only) | `M` | Red-first drivers for every change-proving AC. `AC-4`'s test must be written from the verbatim canonical quote, not from memory — signedness and the offset are the two things this packet exists to pin. |
| (re-derive) | `Step 2` | `docs/02_ir_schemas.md` §"IR 7 — PerimeterIR" | `crates/slicer-ir/src/slice_ir.rs` (`Point3WithWidth`, `CURRENT_PERIMETER_IR_SCHEMA_VERSION`), `crates/slicer-schema/wit/deps/types.wit` (`record point3-with-width`) | none | `S` | The carrier itself. `record seam-point3-with-width` two lines away gets nothing. Ends with the workspace failing **only** on `E0063`; that failure list is the sweep worklist. |
| (re-derive) | `Steps 3, 6, 7, 8, 9a, 9b` | none | the exhaustive `Point3WithWidth` struct-literal sweep, split by crate group | none | `M` each | **Six blind mechanical steps.** Split this way — rather than left as one `L` — because the blast radius is several times packet 189's `LayerCollectionIR` sweep. Three sites are **not** blind and are excluded: `interpolate_point` (`crates/slicer-core/src/lib.rs`) must inherit from `start`, the three `crates/slicer-wasm-host/src/marshal/` files get real conversions in Step 5, and `crates/slicer-runtime/src/layer_executor.rs`'s synthetic points need per-site judgement. `AC-8` (workspace-wide zero `E0063`) is Step 9b's exit and the sweep's completion signal. |
| (re-derive) | `Step 4` | `docs/adr/0031-overhang-classification-in-prepass.md` | `crates/slicer-core/src/algos/overhang_annotation.rs`, `crates/slicer-core/src/perimeter_utils.rs`, `crates/slicer-runtime/src/builtins/overhang_annotation_producer.rs`, `crates/slicer-ir/src/slice_ir.rs` (`SurfaceClassificationIR`) | `ExtrusionProcessor.hpp` — `ExtrusionQualityEstimator::estimate_extrusion_quality` (that `unscaled_prev_layer` is the previous layer's **slice boundary**) | `M` | The design core: the previous-layer boundary reaches the stamping site, and the signed offset distance is computed. ADR-0031's in-body amendment explicitly preserves "the `SurfaceClassificationIR` extension shape", which is the clause this step extends rather than contests. |
| (re-derive) | `Step 5` | `docs/03_wit_and_manifest.md` | `crates/slicer-schema/wit/deps/ir-types.wit` (`resource slice-region-view`), `crates/slicer-wasm-host/src/marshal/{in_,out,leaf}.rs` | none | `M` | **Real edits, never blind ones.** A mechanical `None` inserted in a marshal conversion compiles and silently drops the field across the guest boundary — invisible to `E0063` and to every test in this packet. Full guest rebuild; `AC-9` gates it. |
| (re-derive) | `Step 10` | `CLAUDE.md` §"Guest WASM Staleness" | `modules/core-modules/classic-perimeters/src/lib.rs`, `modules/core-modules/arachne-perimeters/src/lib.rs` | none | `M` | Both stamping sites. The arachne assignment must sit **outside** `if !overhang_bands.is_empty()`; `AC-6`'s structural half measures byte offsets against that guard's matched braces because a test cannot see the nesting. |
| (re-derive) | `Step 11` | `docs/02_ir_schemas.md`, `docs/03_wit_and_manifest.md`, `docs/05_module_sdk.md`, `docs/DEVIATION_LOG.md`, `docs/07_implementation_status.md` | none | none | `S` | Doc anchors, the new `DEV-###` row, and the TASK registration **outside** the generated open-deviations block. The overhang-speed parity record stays `Open` — packets 190 and 191 own its paragraph and its closure. |

Costs are copied from `implementation-plan.md` §Per-Step Budget Roll-Up. Aggregate `M`, **at the top of the band**; no step is `L`, so no pre-activation split is required — but `design.md` §Open Questions records the 193a/193b split seam as the correct escalation if a swarm run hits the context band before Step 9b.

## Packet crosswalk

| Packet | Relationship | Where discharged |
| --- | --- | --- |
| `190-smoothed-overhang-speed` (TASK-309) | **This packet is 190's hard prerequisite** under the maintainer's option (C) ruling. 190 reads `overhang_distance_mm` instead of computing a cross-layer wall distance in-module, which dissolves its `[BLOCK-2]`, its `+0.5 × outer_wall_line_width` compensation `[FWD]`, its `OuterWall`-only-vs-all-roles `[FWD]`, and the whole `OuterWall`-centerline proxy-bias `DEV-###` row it planned to file | `design.md` §Data and Contract Notes (the carrier definition 190 cites); `AC-4` |
| `191-overhang-add-intersections` (TASK-310) | Transitive prerequisite. **Every predicate 191 ports reads this field** — the XOR crossing test against `boundary_offset + EPSILON`, the outer proximity test `> -boundary_offset && < boundary_offset + 2.0`, and `a0`/`a1`'s `+ 3 × boundary_offset`. All three require the value to be **signed and offset-normalised**; against an unsigned un-offset carrier the proximity test's negative half is unreachable and the XOR degenerates | `design.md` §Data and Contract Notes §"The signedness contract"; `AC-4`; `AC-N1`'s forbidden-substitute list, which is normative for 191's unwrap rule |
| `189-per-point-speed-factor-carrier` (TASK-308) | **Independent, not ordered.** 189 adds a per-point *speed* carrier on `LayerCollectionIR`; this adds a per-point *distance* carrier on `Point3WithWidth`. Disjoint types, disjoint schema constants, either order. The one shared hazard is `CURRENT_LAYER_COLLECTION_IR_SCHEMA_VERSION`, which is **189's line and not this packet's** — `Point3WithWidth` reaches `LayerCollectionIR` transitively, which is a real reason someone might reach for it | `design.md` §Architecture Constraints; `requirements.md` §Out of Scope |

## Deviation crosswalk

| Deviation | Relationship | Where discharged |
| --- | --- | --- |
| `overhang-speed parity` | **Not touched.** Row stays `Open`; this packet ships a carrier and closes neither sub-item. 190 appends the smoothed-speed progress paragraph, 191 closes the row | `packet.spec.md` §Doc Impact Statement (the "no edit" bullet and its `Open` probe); Step 11 |
| new `DEV-###` (re-derive) | Residual: canonical's `boundary_offset` is `0.5 × path.width`, a **per-path constant**; PnP stamps per point and resolves it from the point's own `width`. Identical for classic perimeters (one width per loop); varies along **arachne variable-width** loops | Step 11 — `packet.spec.md` §Doc Impact Statement |
| `DEV-093` | **Constrains the verification strategy, is not addressed.** It records that two runs of the same unmodified release binary already differ by ~100-160 lines, so whole-output G-code byte comparison cannot attribute anything. Every criterion in this packet is therefore structural or unit-level, and the "no G-code moves" claim rests on `golden_emit_tdd`, not on a byte diff | `packet.spec.md` `AC-N3`; `requirements.md` §Out of Scope |

Every `DEV-###` above must have its number **re-derived at the moment the row is written** (`rg -o '^\| DEV-[0-9]{3}' docs/DEVIATION_LOG.md | sort -u | tail -1`, take the next). Sibling packets in this queue file rows concurrently; a number captured earlier in the session will collide.

## ADR crosswalk

| ADR | Relationship | Where discharged |
| --- | --- | --- |
| `ADR-0031` (overhang classification in prepass) | **Honoured, not contested.** This packet keeps classification upstream and adds a second upstream-produced signal beside the quartile. Its in-body `### Amendment (overhang-after-Slice inversion)` explicitly lists "the `SurfaceClassificationIR` extension shape" among the clauses that stand unchanged — which is precisely the shape Step 4 extends | `design.md` §Architecture Constraints, §Code Change Surface |
| `ADR-0053` (**forward reference** — being authored in a parallel workstream; not on the tree at authoring time) | The decision record for the maintainer's option (C) ruling, amending ADR-0031, ADR-0032 and ADR-0008 for this three-packet set. **This packet files no amendment row**: it supersedes nothing on its own. The amendment obligations belong to 190 and 191, whose `AC-6`-equivalent removes `EntityMutation::SetSpeedFactor` and whose geometry mutation the ADR is written to cover | `implementation-plan.md` §Packet Completion Gate, which states the absence explicitly so it is not read as the supersession set being empty |
