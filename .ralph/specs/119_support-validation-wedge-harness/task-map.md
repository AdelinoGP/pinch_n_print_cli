# Task Map: support-validation-wedge-harness

No task IDs are retained in this packet's frontmatter. Source-plan `TASK-260` is an explicit unresolved mapping because the live `docs/07_implementation_status.md` row assigns it to gyroid-infill work; no support replacement ID is invented.

| docs/07 task ID | Packet step | Primary docs | Expected code surface | OrcaSlicer refs | Context cost | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| `- (unresolved source-plan TASK-260)` | Step 1 | `docs/specs/support-modules-orca-port.md` C1 and Validation Strategy; `docs/02_ir_schemas.md` IR 9b; `docs/07_implementation_status.md` | Integration target registration, fixture helpers, `prepare_prepass_context`, `SupportPlanIR`, and `SupportGeometryIR` shape inventory | none | S | Confirm the current driver and record the absent `dist_to_top`, parent-link, and `raft_plan` surfaces before test authoring. |
| `- (unresolved source-plan TASK-260)` | Step 2 | `docs/01_system_architecture.md` `PrePass::SupportGeometry`; `docs/02_ir_schemas.md` IR 9b | `crates/slicer-runtime/tests/common/support_wedge.rs` and its registration in `common/mod.rs` | none | M | Shared helper must run the real wedge prepass and reject an enabled empty plan. |
| `- (unresolved source-plan TASK-260)` | Step 3 | `docs/02_ir_schemas.md` IR 9b; `docs/08_coordinate_system.md`; `docs/specs/support-modules-orca-port.md` C1 and Validation Strategy | Integration aggregate registration and `support_invariants_wedge_tdd.rs` finite-path, endpoint, layer-Z, overhang-contact, radius, and raft-prefix tests | none | M | Assertions use public committed IR and canonical unit conversion only. |
| `- (unresolved source-plan TASK-260)` | Step 4 | `docs/01_system_architecture.md` `PrePass::SupportGeometry`; `docs/07_implementation_status.md` | Wedge helper config override and disabled-support test in `support_invariants_wedge_tdd.rs` | none | S | `support_enabled = false` must produce an explicitly empty plan without weakening the enabled-path check. |
| `- (unresolved source-plan TASK-260)` | Step 5 | `docs/specs/support-modules-orca-port.md` Validation Strategy; `docs/02_ir_schemas.md` IR 9b | `support_golden_regression_wedge_tdd.rs` golden parser, endpoint extraction, Hausdorff comparison, and guarded regeneration path | none | M | Normal tests compare the committed branch-count and endpoint self-captures without writing them. |
| `- (unresolved source-plan TASK-260)` | Step 6 | `docs/specs/support-modules-orca-port.md` Validation Strategy | `resources/golden/support_regression_wedge_branch_count.txt` and `resources/golden/support_regression_wedge_endpoints.txt` | none | M | Capture is allowed only after prerequisite packets, freshness, and non-empty enabled output are confirmed. |
| `- (unresolved source-plan TASK-260)` | Step 7 | `docs/specs/support-modules-orca-port.md` Validation Strategy | In-memory branch-count drift test in `support_golden_regression_wedge_tdd.rs` | none | S | The negative case must detect drift above 10 percent without mutating either committed golden. |
| `- (unresolved source-plan TASK-260)` | Step 8 | `docs/07_implementation_status.md`; packet 119 verification matrix | Final integration, freshness, check, and clippy evidence for the test harness and committed goldens | none | S | Harness closure remains draft while the source-plan mapping and current-IR owner decisions are unresolved. |

Aggregate context cost: `M`. No step is `L`; the source-plan mapping remains unresolved.
