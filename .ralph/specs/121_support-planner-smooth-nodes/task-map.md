# Task Map: support-planner-smooth-nodes

| docs/07 task ID | Packet step | Primary docs | Expected code surface | OrcaSlicer refs | Context cost | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| `TASK-286` (renumbered from source-plan `TASK-262`; collision: `TASK-262` is now LightningTreeGen per `docs/07_implementation_status.md:227`, packet 137) | Step 1, Step 2, Step 3, Step 4, Step 5 | `docs/specs/support-modules-orca-port.md` §C3 + §Validation Strategy | `modules/core-modules/support-planner/src/lib.rs` (new `smooth_branches(entries: &mut Vec<SupportPlanEntry>, iterations: usize)` at the tail of `plan_for_object` between line 742 and line 750; new `group_branches_into_columns` helper; `MAX_BRANCH_RADIUS_MM = 6.0` width clamp per iteration); `modules/core-modules/support-planner/tests/smooth_nodes_tdd.rs` (NEW; 4 unit tests); `crates/slicer-runtime/tests/integration/support_invariants_wedge_tdd.rs` (extended with `branch_curvature_below_threshold` — the 8th invariant); `resources/golden/support_regression_wedge_branch_count.txt` and `..._endpoints.txt` (regenerated via `SUPPORT_WEDGE_REGEN_GOLDEN=1`); `docs/specs/support-modules-orca-port.md` §Validation Strategy (one-line invariant list extension). | `OrcaSlicerDocumented/src/libslic3r/Support/TreeSupport.cpp::TreeSupport::smooth_nodes` (~line 3153; delegate SUMMARY) | M | The data shape (per `crates/slicer-ir/src/slice_ir.rs:1113-1126` and `:1780-1788`) is `SupportPlanEntry.branch_segments: Vec<ExtrusionPath3D>` where each `ExtrusionPath3D` is typically a 2-point MST-edge segment. The "chain" the smoother operates on is across layers, not within a single path — the `group_branches_into_columns` helper groups by `(object_id, region_id)` and orders by `global_layer_index` descending. Smoothing only mutates per-layer (x, y) and width; z, role, speed_factor are NOT smoothed. Endpoint (highest z and lowest z) are held fixed. |

Aggregate context cost across rows: `M`. No row is L.
