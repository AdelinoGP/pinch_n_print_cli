# Task Map: 148-arachne-per-vertex-parity

This packet does NOT correspond to a `docs/07_implementation_status.md` task ID — the audit (`tmp/arachne_parity_audit_20260709.md`) surfaced the gaps, and the 7 red tests in `crates/slicer-runtime/tests/arachne_parity.rs` are the runnable fingerprint. The packet spans multiple `docs/07` workstreams by intent (D-104 + the per-vertex propagation that P104/P107 left as T-024-WIRE-VIEW-CONSUMER follow-up). The task-map table below is constructed to help a future reviewer map the packet back to its doctrinal anchors, not to assert `docs/07` ownership.

| Audit gap | Packet step | Primary docs | Expected code surface | OrcaSlicer refs | Context cost | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| G18 (boundary_type hardcoded Interior) | Step 2 + Step 7 | `docs/02_ir_schemas.md` §1418-1428 | `arachne-perimeters/src/lib.rs:302` | `PerimeterGenerator.cpp:383` | S | AC-1; rewritten test drives `run_perimeters` natively (was substring match) |
| G10 (ThinWall loop type never emitted) | Step 3 + Step 7 | `docs/02_ir_schemas.md` §1505-1516 | `arachne-perimeters/src/lib.rs:206-214` | `WideningBeadingStrategy.cpp:27-77`, `WallToolPaths.hpp:18` | S | AC-2; rewritten test drives `run_perimeters` natively |
| G20 (is_thin_wall flag never set) | Step 3 + Step 7 | `docs/02_ir_schemas.md` §1528 | `arachne-perimeters/src/lib.rs:296-303` | (same as G10) | S | AC-3; rewritten test drives `run_perimeters` natively |
| G19 (is_bridge flag never set) | Step 4 + Step 7 | `docs/02_ir_schemas.md` §1526 | `arachne-perimeters/src/lib.rs:301` | `PerimeterGenerator.cpp:675-678` | M | AC-4; per-vertex is_bridge from `region.bridge_areas()`; rewritten test drives `run_perimeters` natively |
| G7 (overhang_quartile hardcoded None) | Step 4 + Step 7 | `docs/02_ir_schemas.md` §1556-1557 | `arachne-perimeters/src/lib.rs:301` (path-points construction) | `PerimeterGenerator.cpp:2113-2119` | M | AC-5; per-vertex band lookup from `region.overhang_quartile_polygons()` (mirrors `expolygon_to_path3d:316-331`); rewritten test drives `run_perimeters` natively. Note: the host pipeline (`generate_toolpaths.rs:184,471,866`) hardcodes `None` upstream; the guest module overrides per-vertex from the region's bands AFTER `extrusion_line_to_extrusion_path3d` copies the default. |
| G21 (seam_candidate producer missing) | Step 5 | `docs/05_module_sdk.md` §"SliceRegionView accessors" | `arachne-perimeters/src/lib.rs` (post-loop, after wall push) | `PerimeterGenerator.cpp:2093-2535` | M | AC-6; call shape is `&region.polygons()[0].contour` (units-space input polygon, NOT `&wall.path`); mirrors classic's `lib.rs:889-900`; rewritten test drives `run_perimeters` natively |
| G12 (precise_outer_wall not registered) | Step 1 + Step 6 | `docs/15_config_keys_reference.md` §Walls | `arachne-perimeters.toml` + `arachne_params_from_config` (gates `ArachneParams.outer_wall_offset` on `precise_outer_wall && wall_sequence==InnerOuter`) | `PerimeterGenerator.cpp:2146-2158`, `PrintConfig.cpp:1484-1489`, `Arachne/BeadingStrategy/OuterWallInsetBeadingStrategy.cpp:44-60` | M | AC-7 + AC-8; manifest registration is Step 1, beading-stack mechanism is Step 6 (NOT post-hoc path mutation). Unit test asserts outer wall distance-to-boundary via `output.wall_loops()[0].path.points` geometry. OrcaSlicer has no unit test for the offset magnitude; PnP's new test is canonical. |

The packet also lands the test rewrite (Step 8) which replaces the 7 substring-matching red tests in `arachne_parity.rs` with thin delegators to the new `arachne-perimeters/tests/*_tdd.rs` unit tests (or runs the same harness inline). The deviation log + audit history + config-keys reference updates land in Step 9. These are necessary closures, not optional polish — the deviation log is the canonical source per the grilling-session decision, so the D-104 row must reflect the arachne-path-only scope at packet close.

The Context cost column copies the per-step estimate from `implementation-plan.md`. The aggregate is M (sum of S+S+S+M+M+M+M+M+S = M-equivalent). No single step is L. The packet does not need to split before activation.
