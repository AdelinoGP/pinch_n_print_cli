# Task Map: support-planner-to-buildplate-pruning

| docs/07 task ID | Packet step | Primary docs | Expected code surface | OrcaSlicer refs | Context cost | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| `TASK-288` (renumbered from source-plan `TASK-264`; collision: `TASK-264` is now Lightning Layer per `docs/07_implementation_status.md:229`, packet 139) | Step 1, Step 2, Step 3, Step 4, Step 5 | `docs/specs/support-modules-orca-port.md` §C5 + §Validation Strategy | `modules/core-modules/support-planner/src/lib.rs::PlannedSupportNode` (line 92; new `to_buildplate: bool` field); `SupportPlanner` struct (lines 68-90; new `support_on_build_plate_only: bool` field); `on_print_start` (line 114; config parse); `plan_for_object` contact creation (lines 380-416; set `to_buildplate` per contact + reject-at-creation when `support_on_build_plate_only = true`); `plan_for_object` propagation prune (lines 711-723; additional `to_buildplate = true` prune trigger alongside the existing collision-prune); `support-planner.toml` (new `[config.schema].support_on_build_plate_only` entry, default `false`); `modules/core-modules/support-planner/tests/to_buildplate_tdd.rs` (NEW; 5 unit tests); `crates/slicer-runtime/tests/integration/support_invariants_wedge_tdd.rs` (extended with `build_plate_only_emits_no_to_model_branches` — the 10th invariant); `resources/golden/support_regression_wedge_branch_count.txt` and `..._endpoints.txt` (regenerated via `SUPPORT_WEDGE_REGEN_GOLDEN=1`; small shift since default config doesn't change behavior); `docs/specs/support-modules-orca-port.md` §Validation Strategy (one-line invariant list extension). | `OrcaSlicerDocumented/src/libslic3r/Support/TreeSupport.cpp::drop_nodes` (~line 2752; `unsupported_branch_leaves`; delegate SUMMARY) and `TreeSupport.cpp::generate_contact_points` (~line 3486; `to_buildplate` init; delegate SUMMARY) | M | The `to_buildplate` flag is internal to the planner (does NOT cross the IR/WIT boundary). The new `to_buildplate = true` prune is ADDITIVE to the existing collision-prune — it's a tightening, not a relaxation. The `PlannedSupportNode` field addition requires updating every `PlannedSupportNode { ... }` struct-literal site in the planner; the implementer audits these with `rg 'PlannedSupportNode \{' modules/core-modules/support-planner/src/lib.rs` and updates each. |

Aggregate context cost across rows: `M`. No row is L.
