# Task Map: 149-pipeline-config-wiring

This packet does NOT correspond to a `docs/07_implementation_status.md` task ID — the audit (`tmp/arachne_parity_audit_20260709.md`) surfaced the gaps, and the 4 red tests in `crates/slicer-runtime/tests/arachne_parity.rs` are the runnable fingerprint. The packet spans the existing D-104 family of deviations (D-104b, D-104c, D-104d, D-104e, D-104f, D-104g are all new) and the bridging_flow concern that `crates/slicer-core/src/flow.rs` was set up to handle (D-104b's "flow_factor is never reduced for overhang" gap maps to a new `bridging_flow()` helper in the same file; D-104g documents the per-vertex `flow_factor` vs OrcaSlicer's per-path `Flow` model divergence). The task-map table below helps a future reviewer map the packet back to its doctrinal anchors, not to assert `docs/07` ownership.

| Audit gap | Packet step | Primary docs | Expected code surface | OrcaSlicer refs | Context cost | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| G9 (overhang_reverse config keys) | Step 1 + Step 2 | `docs/ORCA_CONFIG_REFERENCE.md:165-167`, `docs/15_config_keys_reference.md` §Overhangs | `arachne-perimeters.toml` (4 new sections), `classic-perimeters.toml` (3 new sections, 1 re-publication) | `PrintConfig.cpp:5003-5066, 5059-5066` | S | AC-1; manifest-only (D1 has no behavior in this packet) |
| G14 (min_width_top_surface) | Step 1 + Step 2 | `docs/ORCA_CONFIG_REFERENCE.md:135` (sub-agent dispatch verifies default — 300%, coFloatOrPercent) | `arachne-perimeters.toml` (2 new sections incl. `only_one_wall_top`), `classic-perimeters.toml` (1 new section), read-and-validate config reads in BOTH modules' `lib.rs` (AC-2's predicate is a SOURCE grep, not a manifest grep) | `PrintConfig.cpp:1491-1511` | S | AC-2; keys + reads (D2 behavior deferred per D-104d); default verified BEFORE commit |
| G16 (alternate_extra_wall) | Step 1 + Step 3 | `docs/ORCA_CONFIG_REFERENCE.md:178` | `arachne-perimeters.toml` (3 new sections: `alternate_extra_wall` + gate keys `spiral_vase`/`sparse_infill_density`, previously unreadable by the module), `arachne-perimeters/src/lib.rs` (`max_bead_count` bump on odd layers), `arachne-perimeters/tests/alternate_extra_wall_tdd.rs` (NEW) | `PerimeterGenerator.cpp:1227` (classic) + `:2133` (arachne) — `loop_number++` → `max_bead_count` bump | S | AC-3; manifest + behavior (beading-stack mechanism, NOT wall-count mutation) |
| G8 (bridge flow) | Step 4 | `docs/ORCA_CONFIG_REFERENCE.md:150` (`thick_bridges`), `:146` (`bridge_flow`), `docs/02_ir_schemas.md` §1542-1558 | `crates/slicer-core/src/flow.rs` (NEW `bridging_flow(bridge_flow_ratio, thick_bridges)` helper), `arachne-perimeters/src/lib.rs` (apply), `classic-perimeters/src/lib.rs` (apply for parity), `arachne-perimeters/tests/bridge_flow_factor_tdd.rs` (NEW), `crates/slicer-runtime/tests/arachne_parity.rs:232-250` (AC-4 red test REWRITTEN — host-junction predicate on a bridgeless square cannot observe the guest-side fix) | `LayerRegion.cpp:135` — real ratio-based formula (NOT constant 0.85) | M | AC-4; behavior change, both perimeter modules; the per-vertex `flow_factor` model diverges from OrcaSlicer's per-path `Flow` model — D-104g documents this |
| G23 / D-104f (concentric infill) | Step 5 | `docs/DEVIATION_LOG.md` (D-104f row), `docs/14_deviation_audit_history.md` (1 row), `docs/15_config_keys_reference.md` (no key — the gap is a missing module wiring) | `docs/DEVIATION_LOG.md` (1 new row, Status: Open — deferred) | `FillConcentric.cpp:80-118`, `FillConcentricInternal.cpp:29-55` | S | D-104f registration only; no code; the `arachne_parity_pipeline_concentric_infill_uses_arachne` test stays red |
| D-104g (per-vertex `flow_factor` divergence) | Step 4 + Step 5 | `docs/DEVIATION_LOG.md` (D-104g row) | `docs/DEVIATION_LOG.md` (1 new row, Status: Open), `crates/slicer-core/src/flow.rs` (the `thick_bridges` branch is the canonical realization site) | `LayerRegion.cpp:135` (OrcaSlicer's per-path `Flow` model) | S | D-104g registration only; the `bridge_flow` ratio is correctly modelable per-vertex; the divergence is limited to the `thick_bridges` branch |
| (Doc Impact gate) | Step 5 | `docs/DEVIATION_LOG.md`, `docs/14_deviation_audit_history.md`, `docs/15_config_keys_reference.md` | 6 + 6 + 11 new rows | none | S | 6 deviation log rows (D-104b/c/d/e/f/g), 6 audit-history rows, 11 config-keys rows |

The Context cost column copies the per-step estimate from `implementation-plan.md`. The aggregate is M (sum of S+S+S+M+S = M-equivalent). No single step is L. The packet does not need to split before activation.

**Cross-packet dependencies:**
- Packet 148 (arachne per-vertex parity) lands `is_bridge` per-vertex, which packet 149's D4 (bridge flow) reads.
- D-104f (concentric infill Arachne) is a follow-up workstream; the red test for it stays red at packet 149 close. The follow-up workstream will likely be a multi-packet design effort (per the grilling-session decision: the largest of the 15 gaps).
- D1's behavior (overhang_reverse logic) and D2's behavior (min-width-top-surface threshold for `only_one_wall_top`) are deferred to a separate follow-up packet. This packet registers the keys; the behavior is a separate concern.
- D-104g documents the per-vertex `flow_factor` vs OrcaSlicer's per-path `Flow` model divergence; the `bridge_flow` ratio is correctly modelable per-vertex, so the divergence is limited to the `thick_bridges` branch (PnP doesn't model Flow height/width/thread_diameter the way OrcaSlicer does).
