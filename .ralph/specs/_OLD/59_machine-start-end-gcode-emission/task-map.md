# Task Map: 59_machine-start-end-gcode-emission

Three task IDs span six implementation steps. The map below shows the bridge from `docs/07_implementation_status.md` back into this packet's steps, with primary docs, expected code surface, OrcaSlicer references, and per-step context cost.

| docs/07 task ID | Packet step | Primary docs | Expected code surface | OrcaSlicer refs | Context cost | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| `TASK-194` + `TASK-194a` + `TASK-194b` | Step 1 | `docs/07_implementation_status.md` (delegate; never load full) | `docs/07_implementation_status.md` (worker dispatch edit only) | — | `S` | Backlog rows must exist before any code change so cross-references resolve. No source code in this step. Three rows: TASK-194, TASK-194a, TASK-194b. |
| `TASK-194` + `TASK-194a` + `TASK-194b` | Step 2 | `docs/02_ir_schemas.md` `ConfigValue` + `GCodeCommand` (range) | NEW `crates/slicer-host/tests/machine_start_end_gcode_emission_tdd.rs` (≤ 400 LOC, 13 tests = 10 positive + 3 negative). Sibling reads: `gcode_header_thumbnail_config_blocks_tdd.rs`, `gcode_emit_tdd.rs:1-120`, `postpass_gcode_emit_contract_tdd.rs:1-80`. End-to-end: slicer-cli → ResolvedConfig → emitter (ExtrusionMode head) → GCodePostProcess module (Raw start/end) → serializer → file scan. | — | `M` | TDD-first: lock all 13 ACs as failing tests. Production code untouched. Unknown-placeholder AC asserts verbatim passthrough only (substitution runs in WASM guest; cross-component log forwarding is out of scope). |
| `TASK-194b` | Step 3 | `docs/02_ir_schemas.md` `GCodeCommand` (range); `docs/03_wit_and_manifest.md` (WIT extension protocol, only if `wit/deps/ir-types.wit` is touched) | `crates/slicer-ir/src/slice_ir.rs` (1 new `GCodeCommand::ExtrusionMode { absolute: bool }` variant at `:1697`, ≈ 5 LOC); `crates/slicer-host/src/gcode_emit.rs` (push `ExtrusionMode` head in `DefaultGCodeEmitter::emit_gcode` at `:304`, ≈ 6 LOC; remove hard-coded M82/M83 at `:1154-1156`, ≈ -8 LOC; add `ExtrusionMode` arm in per-command renderer near `:1280-1281`, ≈ 6 LOC); `wit/deps/ir-types.wit` (conditional: 1 new variant if `gcode-command` is mirrored, ≈ 0-4 lines). | — | `M` | Critical falsifying check: `cargo test -p slicer-host --test gcode_emit_tdd` must remain GREEN — the packet-54 regression suite asserts M82/M83 presence and position. After promotion the line appears in the same byte position by construction. AC `extrusion_mode_still_emitted_after_promotion` turns green here. The remaining 12 ACs stay red until Step 4. |
| `TASK-194a` + `TASK-194` (emission) | Step 4 | `docs/03_wit_and_manifest.md` (`[config.schema.<key>]` section); `docs/05_module_sdk.md` (GCodePostProcess module trait shape) | NEW `modules/core-modules/machine-gcode-emit/{machine-gcode-emit.toml, Cargo.toml, src/lib.rs}` (≤ 70 + ≤ 25 + ~120-150 LOC respectively). `[stage] id = "PostPass::GCodePostProcess"` — NOT `LayerFinalization`. Templates copied from `modules/core-modules/part-cooling/{part-cooling.toml, Cargo.toml, src/lib.rs}` (full reads OK; each is small). String-type precedent from `modules/core-modules/seam-placer/seam-placer.toml`. `src/lib.rs` REAL `run_gcode_postprocess` body: reads 4 keys → builds HashMap lookup → calls private `substitute_placeholders(template, &lookup) -> String` (≤ 60 LOC, scoped to module file) → pushes `Raw(start)` (skip if empty) → re-emits each input command → pushes `Raw(end)` (skip if empty). After creation: `./modules/core-modules/build-core-modules.sh` (build) + `--check` (freshness gate) + `./test-guests/build-test-guests.sh --check` (Step 3 slicer-ir variant change invalidates test-guest bindgen). | `OrcaSlicerDocumented/src/libslic3r/PrintConfig.hpp` (key-name spelling, FACT ≤ 6 lines); `OrcaSlicerDocumented/src/libslic3r/PlaceholderParser.cpp` (`apply_config()` only, FACT ≤ 10 lines). NEVER read `PlaceholderParser.cpp` body. | `M` | All remaining 12 ACs turn green here. Guest WASM Staleness checks (`--check` clean) mandatory per CLAUDE.md. The SDK trait exact name + signature is identified via a single ranged read of `crates/slicer-sdk/src/traits.rs`. |
| `TASK-194` + `TASK-194a` + `TASK-194b` | Step 5 | — | None edited (or ≤ 5 LOC clippy fix in one file) | — | `S` | Regression sweep: `postpass_gcode_emit_contract_tdd`, `gcode_header_thumbnail_config_blocks_tdd`, both `--check` commands, `cargo check --workspace`, `cargo clippy --workspace -- -D warnings`. Six FACT dispatches. |
| `TASK-194` + `TASK-194a` + `TASK-194b` | Step 6 | `docs/11_operational_governance_and_acceptance_gate.md`; `docs/12_architecture_gate_metrics.md` (only on fail) | `docs/07_implementation_status.md` rows → `[x]` (worker dispatch); `.ralph/specs/59_machine-start-end-gcode-emission/packet.spec.md` status flip ONLY after explicit user OK | — | `S` | Closure ceremony: re-dispatch all 13 pipe-suffixed AC commands + the once-per-packet `cargo test --workspace` (per CLAUDE.md test discipline). 15 FACT dispatches total. |

**Aggregate context cost:** 3×S + 3×M = **`M`**. No single step is `L`. Within the cap.

## Why this packet covers TASK-194 evidence

Step 4's 12 newly-green ACs (start/end emission, position assertions, full and partial user override, empty-end-block, unknown-placeholder verbatim, unclosed-bracket literal, position-regression sentry, four-key manifest registration, four-key CONFIG_BLOCK presence) collectively demonstrate that `machine_start_gcode` and `machine_end_gcode` are emitted at the correct byte positions with correct minimal `[key]` substitution by a `PostPass::GCodePostProcess` module. TASK-194 is satisfied.

## Why this packet covers TASK-194a evidence

Step 4 creates `modules/core-modules/machine-gcode-emit/` with three files. The manifest declares four `[config.schema.<key>]` blocks with the correct types and defaults. The `run_gcode_postprocess` body runs real `[key]` substitution against `ResolvedConfig` (via `ConfigView`) and rebuilds `GCodeIR.commands` as `[Raw(resolved_start), ...existing..., Raw(resolved_end)]`. The new module's `.wasm` is built by `./modules/core-modules/build-core-modules.sh` and `--check` returns clean. The two ACs `module_manifest_registers_four_keys_with_expected_types_and_defaults` and `new_keys_appear_in_config_block` (the latter via packet-55's automatic CONFIG_BLOCK propagation) directly attest. TASK-194a is satisfied.

*(Note: the previously-included `rejects_temp_out_of_registered_range` AC was SCOPE-CUT in the refinement — declared `min`/`max` ranges are declarative-only as of this packet; tracked as a separate future TASK-### packet.)*

## Why this packet covers TASK-194b evidence

Step 3 promotes `M82`/`M83` from the hard-coded `DefaultGCodeSerializer` preamble at `:1154-1156` to a new `GCodeCommand::ExtrusionMode { absolute: bool }` variant pushed by `DefaultGCodeEmitter::emit_gcode` at `:304`. The serializer renders the new variant in its per-command loop (sibling to the `Temperature` arm at `:1280-1281`). The packet-54 regression suite (`cargo test -p slicer-host --test gcode_emit_tdd`) remains GREEN — confirming that the M82/M83 effect is preserved bit-identically for default configs. The packet's own AC `extrusion_mode_still_emitted_after_promotion` provides an explicit regression sentry. The structural value of this promotion is enabling Step 4's GCodePostProcess module to prepend `Raw(machine_start_gcode)` BEFORE the M82/M83 line, matching OrcaSlicer ordering — verified by AC `start_block_position_before_extrusion_mode_and_first_g1`. TASK-194b is satisfied.

## Refinement Note

This packet replaces the original packet-59 draft (commit 700ca98 "docs(spec-59): refine packet against module-owned print-boundary gcode") which routed substituted strings through new `FinalizationBuilderPush` variants + new `Option<String>` fields on `GCodeIR` + new `dispatch.rs` match arms + byte-offset placement in the host serializer. The refined design uses the existing `PostPass::GCodePostProcess` scheduler stage, the existing `gcode-output-builder.push-raw` WIT method, and the existing `GCodeCommand::Raw` IR variant — with a small companion refactor (M82/M83 promotion) to preserve OrcaSlicer ordering. Net surface reduction: removes the WIT extension, two `FinalizationBuilderPush` variants, two `GCodeIR` fields, and two dispatch match arms; adds one `GCodeCommand` variant.
