# Task Map: 162_wit-lifecycle-export-removal

Emitted because this packet **reopens prior work**: TASK-146 (`docs/07_implementation_status.md:39`) is marked `[x]` but closed on `validate_wit_world`, which ADR-0044 showed compares one hand-written string to another with no artifact to check against, and which ADR-0045 retires outright. This crosswalk owns the `docs/07` reconciliation.

| docs/07 task ID | Packet step | Primary docs | Expected code surface | OrcaSlicer refs | Context cost | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| `TASK-146a` | `Step 1` | `docs/specs/adr-0045-per-stage-wit-packages-plan.md` §"Out of scope" item 3 (history only — its "keep known-red" instruction is obsolete) | `crates/slicer-runtime/tests/contract/wit_drift_detection_tdd.rs` | None — no canonical behavior borrowed | `S` | Proves the deletion is enforceable: the RED guard walks the tree instead of asserting against its own table. Also pins the **green** parity baseline (12/12 + 1/1) the packet must preserve — the `object_id` fix is committed at `ff21378e`, so it reproduces from HEAD and green-before/green-after is now the behavior-neutrality signal. |
| `TASK-146a` | `Step 2` | `docs/adr/0044-wit-world-version-is-not-an-identity-token.md` (delegated SUMMARY) | `crates/slicer-schema/wit/deps/world-layer/world-layer.wit`, `crates/slicer-schema/src/lib.rs` | None | `S` | The reopened slice's core: the declaration (10 → 8 exports) and the self-certifying `WORLD_LIFECYCLE_EXPORTS` table whose guard test asserted against itself — the same pathology ADR-0044 found in `wit_world_major_version_mismatch_rejects_future_major`. |
| `TASK-146a` | `Step 3` | `docs/05_module_sdk.md` `:165-200`, `:726-736` | `crates/slicer-sdk/src/traits.rs`, `crates/slicer-sdk/tests/*_module_tdd.rs` | None | `S` | Renames the constructor to the honest name (`from_config`) across all four traits and deletes the `on_print_end` the macro never dispatched to. |
| `TASK-146a` | `Step 4` | `docs/specs/adr-0045-per-stage-wit-packages-plan.md` §"The lifecycle finding" | `crates/slicer-macros/src/lib.rs` | None | `M` | Removes both independent lifecycle surfaces — the `WORLD_LIFECYCLE`-driven metadata (`:148`) and the hardcoded `impl Guest` glue (`:2764-2771`) — plus the fake `#[export_name]` shims the macro emitted for worlds whose `.wit` declares no such export. |
| `TASK-146a` | `Step 5` | None (mechanical) | `modules/core-modules/**`, `crates/slicer-wasm-host/test-guests/**`, `crates/slicer-runtime/tests/**` | None | `M` | 535 occurrences / 110 files. Scripted; completeness is proved by Step 8's `cargo check --workspace --all-targets`, not by reading. |
| `TASK-146a` | `Step 6` | None | `crates/slicer-macros/tests/{binding_surface,all_worlds_glue,slicer_module}_tdd.rs`, `smoke.rs` | None | `S` | Retargets the macro's own assertions at the stage-only surface; a stageless impl now reports an empty export list rather than two lifecycle entries. |
| `TASK-146a` | `Step 7` | None | `crates/pnp-cli/src/module_new.rs` | None | `S` | Stops scaffolding the hook and drops the vacuous `on_print_start_succeeds()` test the scaffold generated for every new module. |
| `TASK-146a` | `Step 8` | `CLAUDE.md` §"Test Discipline" | Compile-error sites only, within Steps 2-7's surface | None | `S` | The type system is the sweep's completeness proof; also the one place the real `-D warnings` forcing function bites (`SUPPORTED_WIT_WORLDS`' broken intra-doc link). |
| `TASK-146a` | `Step 9` | `CLAUDE.md` §"Guest WASM Staleness" | Build outputs: 20 × `modules/core-modules/*/*.wasm` + test guests | None | `S` | The empirical payoff: every guest's decoded `world root` loses `on-print-start` / `on-print-end` (present today at `gyroid-infill.wasm` lines 12-13), shrinking `world-layer` 10 → 8 before packets #2/#3 split it. |
| `TASK-146a` | `Step 10` | `docs/specs/adr-0045-per-stage-wit-packages-plan.md` §"Grounding corrections" item 1; `CLAUDE.md` §"`cargo xtask test`" | `crates/slicer-runtime/tests/common/slicer_cache.rs`, `xtask/src/test.rs`, `crates/slicer-runtime/tests/integration/pnp_cli_freshness_tdd.rs` (+ its mandatory `mod` registration) | None | `M` | Folded in because this packet's blast radius is measured by exactly the tests a stale `pnp_cli` corrupts. The plan's `env!("CARGO_BIN_EXE_pnp_cli")` fix was falsified during grounding; the approved replacement gates at both `xtask test` and the spawn site. Two consecutive sessions were burned by this trap; the second nearly committed a wrong golden (`9dda3c89` vs the correct `da3bd96b`). |
| `TASK-146a` | `Step 10b` | `docs/DEVIATION_LOG.md` DEV-026 row (delegated SUMMARY) | `crates/slicer-runtime/benches/gate_evidence.rs`, `crates/slicer-scheduler/tests/integration/dag_cli_integration.rs` | None | `S` | Closes the remaining two copies. `gate_evidence` is the **sole source of DEV-026's `~438ms` 50-layer time evidence** — a stale binary there yields a plausible number attributed to the wrong code, silently invalidating governance evidence rather than failing. Triplication is deliberate; the shared-helper extraction needs its own ADR (`[FWD]`). |
| `TASK-146a` | `Step 11` | `docs/03_wit_and_manifest.md` `:555-565`; `docs/04_host_scheduler.md` `:1444-1455`; `docs/05_module_sdk.md` (ranged) | `docs/03_wit_and_manifest.md`, `docs/04_host_scheduler.md`, `docs/05_module_sdk.md`, `docs/07_implementation_status.md` | None | `S` | Retires the fiction at its source: `docs/03:559`'s `// Lifecycle — optional` is where "optional export" was first written down, in the initial commit, before a host existed — and the component model has no optional exports. Downstream, the host never called `on-print-start` (`docs/04:1449`), and "initialize expensive resources once per print" (`docs/05:184`) was inverted — it ran once per layer, per stage. |

## Reopened-status reconciliation (Step 11 dispatch)

- `TASK-146` (`docs/07_implementation_status.md:39`) stays `[x]` for the work it actually did but must be annotated **reopened**: its `validate_wit_world` check is retired by ADR-0045, and `wit_world_major_version_mismatch` is the vacuous guard ADR-0044 documented.
- `TASK-146a` is added as this packet's ID, following the existing `TASK-119a/b/c`, `TASK-120a-d`, `TASK-194a/b` sub-lettering convention.
- `TASK-146b` (packet `163_per-stage-wit-packages-pilot`) and `TASK-146c` (packet `164_per-stage-wit-packages-bulk`) are **not** this packet's to add or close.
- `TASK-144` / `TASK-145` (`:37-38`) are untouched: the canonical single WIT source and the normalized identifiers both survive this packet intact.

Costs are copied from `implementation-plan.md`. Aggregate `M`; no row is `L`.
