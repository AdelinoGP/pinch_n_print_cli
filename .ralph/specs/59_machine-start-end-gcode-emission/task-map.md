# Task Map: 59_machine-start-end-gcode-emission

Three task IDs span seven implementation steps. The map below shows the bridge from `docs/07_implementation_status.md` back into this packet's steps, with primary docs, expected code surface, OrcaSlicer references, and per-step context cost.

| docs/07 task ID | Packet step | Primary docs | Expected code surface | OrcaSlicer refs | Context cost | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| `TASK-193` + `TASK-193a` + `TASK-193b` | Step 1 | `docs/07_implementation_status.md` (delegate; never load full) | `docs/07_implementation_status.md` (worker dispatch edit only) | — | `S` | Backlog rows must exist before any code change so cross-references resolve. No source code in this step. Three rows: TASK-193, TASK-193a, TASK-193b. |
| `TASK-193` + `TASK-193a` + `TASK-193b` | Step 2 | `docs/02_ir_schemas.md` `ConfigValue` (range) | NEW `crates/slicer-host/tests/machine_start_end_gcode_emission_tdd.rs` (≤ 400 LOC, 12 tests = 9 positive + 3 negative). Sibling reads: `gcode_header_thumbnail_config_blocks_tdd.rs`, `gcode_emit_tdd.rs:1-120`, `postpass_gcode_emit_contract_tdd.rs:1-80`. End-to-end: slicer-cli → ResolvedConfig → module → dispatch → GCodeIR → serializer → file scan. | — | `M` | TDD-first: lock all 12 ACs as failing tests. Production code untouched. Unknown-placeholder AC is renamed `unknown_placeholder_passes_through_verbatim` and drops the WARN-log clause — substitution runs in WASM guest, cross-component log forwarding is out of scope. |
| `TASK-193b` (primary) + `TASK-193` (plumbing) | Step 3 | `docs/03_wit_and_manifest.md` (WIT extension protocol); `docs/05_module_sdk.md` (`FinalizationOutputBuilder` API patterns) | Additive across 5 files: `wit/world-finalization.wit` (2 new methods on `finalization-output-builder` at `:62-104`); `crates/slicer-sdk/src/traits.rs` (2 new `FinalizationOutputBuilder` methods at impl `:717` + 2 `Option<String>` struct fields at `:704-:715`); `crates/slicer-host/src/wit_host.rs` (2 new `FinalizationBuilderPush` variants at enum `:837` — currently 6 variants → 8 — + 2 new bridge push sites near `:4895-:5020`); `crates/slicer-host/src/dispatch.rs` (2 new match arms in apply-site loop at `:2892-:2978`); `crates/slicer-ir/src/slice_ir.rs` (2 new `Option<String>` fields on `GCodeIR` at `:1781` + `Default` extension at `:1790`). Total ~50 LOC. CLAUDE.md WIT/Type Changes Checklist mandatory. | — | `M` | No AC turns green in this step; this is the typed-contract foundation. `cargo build --tests` clean is the exit condition. |
| `TASK-193a` | Step 4 | `docs/03_wit_and_manifest.md` (`[config.schema.<key>]` section); `docs/05_module_sdk.md` (`FinalizationModule` trait, non-no-op example) | NEW `modules/core-modules/machine-gcode-emit/{machine-gcode-emit.toml, Cargo.toml, src/lib.rs}` (≤ 70 + ≤ 25 + ~120-150 LOC respectively). Templates copied from `modules/core-modules/part-cooling/{part-cooling.toml, Cargo.toml, src/lib.rs}` (full reads OK; each is small). String-type precedent from `modules/core-modules/seam-placer/seam-placer.toml`. `src/lib.rs` REAL `run_finalization` body: reads 4 keys → builds HashMap lookup → calls private `substitute_placeholders(template, &lookup) -> String` (≤ 60 LOC, scoped to module file) → calls `output.push_print_start_gcode(resolved)` + `output.push_print_end_gcode(resolved)`. After creation: `./modules/core-modules/build-core-modules.sh` (build) + `--check` (freshness gate) + `./test-guests/build-test-guests.sh --check` (Step 3 WIT change invalidates test-guest bindgen). | `OrcaSlicerDocumented/src/libslic3r/PrintConfig.hpp` (key-name spelling, FACT ≤ 6 lines). NEVER read `PlaceholderParser.cpp` body. | `M` | Two ACs turn green: `module_manifest_registers_four_keys_with_expected_types_and_defaults` and `new_keys_appear_in_config_block` (the latter via packet-55's automatic CONFIG_BLOCK propagation). Guest WASM Staleness checks (`--check` clean) mandatory per CLAUDE.md. |
| `TASK-193` | Step 5 | `docs/01_system_architecture.md` (finalization / serializer SUMMARY) | `crates/slicer-host/src/gcode_emit.rs` (~20-25 LOC added: read `gcode_ir.print_start_gcode` / `gcode_ir.print_end_gcode` + insert at byte positions. NO `substitute_placeholders` helper added — it lives in the module). Ranged reads: `:667-740`, `:928-1020`, `:1021-1166` (start-block site after `serialize_header_block`/`serialize_width_comments`, before M83 at `:1067` / M82 at `:1069`; end-block site before `ThumbnailAwareSerializer` at `:973` appends THUMBNAIL/CONFIG_BLOCK). | `OrcaSlicerDocumented/src/libslic3r/GCode.cpp:3181` / `:3200` / `:3258` (start-before-preamble ordering, FACT ≤ 12 lines); `OrcaSlicerDocumented/src/libslic3r/PlaceholderParser.cpp` `apply_config()` (FACT ≤ 10 lines). NEVER read `PlaceholderParser.cpp` body. | `S` | The remaining 10 ACs turn green here. The two insertion points are byte-offset-contractual relative to packet 54 (preamble) and packet 55 (HEADER / CONFIG_BLOCK) sites. |
| `TASK-193` + `TASK-193a` + `TASK-193b` | Step 6 | — | None edited (or ≤ 5 LOC clippy fix in one file) | — | `S` | Regression sweep: `postpass_gcode_emit_contract_tdd`, `./modules/core-modules/build-core-modules.sh --check`, `./test-guests/build-test-guests.sh --check`, `cargo check --workspace`, `cargo clippy --workspace -- -D warnings`. Five FACT dispatches. |
| `TASK-193` + `TASK-193a` + `TASK-193b` | Step 7 | `docs/11_operational_governance_and_acceptance_gate.md` (acceptance-gate section); `docs/12_architecture_gate_metrics.md` (consulted only on workspace-gate fail) | `docs/07_implementation_status.md` rows → `[x]` (worker dispatch); `.ralph/specs/59_machine-start-end-gcode-emission/packet.spec.md` status flip ONLY after explicit user OK | — | `S` | Closure ceremony: re-dispatch all 12 pipe-suffixed AC commands + the once-per-packet `cargo test --workspace` (per CLAUDE.md test discipline). 14 FACT dispatches total. |

**Aggregate context cost:** 4×S + 3×M = **`M`**. No single step is `L`. Within the cap.

**Why this packet covers TASK-193 evidence:** Step 5's 10 newly-green ACs (start/end emission, position assertions, full and partial user override, empty-end-block, unknown-placeholder verbatim, unclosed-bracket literal, position-regression sentry) collectively demonstrate that `machine_start_gcode` and `machine_end_gcode` are emitted at the correct byte positions with correct minimal `[key]` substitution. TASK-193 is satisfied.

**Why this packet covers TASK-193a evidence:** Step 4's 2 newly-green ACs demonstrate that all four keys are declared in `modules/core-modules/machine-gcode-emit/machine-gcode-emit.toml` with the correct types and defaults AND flow through into the CONFIG_BLOCK emitted by packet 55. The new module's `run_finalization` body runs real `[key]` substitution against `ResolvedConfig` and pushes the resolved start/end strings through the new `FinalizationOutputBuilder` print-boundary methods. The new module's `.wasm` is built by `./modules/core-modules/build-core-modules.sh` and `--check` returns clean. TASK-193a is satisfied. (Note: the previously-included `rejects_temp_out_of_registered_range` AC is SCOPE-CUT — declared `min`/`max` ranges are declarative-only as of this packet; tracked as a separate future TASK-### packet.)

**Why this packet covers TASK-193b evidence:** Step 3's additive WIT/SDK/dispatch/IR extension establishes the typed channel for print-boundary pushes: `wit/world-finalization.wit`'s `finalization-output-builder` resource gains `push-print-start-gcode` and `push-print-end-gcode`; `FinalizationBuilderPush` grows from 6 to 8 variants; `FinalizationOutputBuilder` impl exposes two new methods; the dispatch apply-loop gains two new match arms; `GCodeIR` gains two `Option<String>` fields. `cargo build --tests` clean is the falsifying check. The CLAUDE.md WIT/Type Changes Checklist sub-steps (grep all `wit_host.rs` / `dispatch.rs` / `wit_guest` references, verify type identity, run `cargo build --tests`, update inline + external WIT consistently) are explicit step exit-conditions. TASK-193b is satisfied.
