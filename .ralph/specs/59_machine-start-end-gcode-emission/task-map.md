# Task Map: 59_machine-start-end-gcode-emission

Two task IDs span six implementation steps. The map below shows the bridge from `docs/07_implementation_status.md` back into this packet's steps, with primary docs, expected code surface, OrcaSlicer references, and per-step context cost.

| docs/07 task ID | Packet step | Primary docs | Expected code surface | OrcaSlicer refs | Context cost | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| `TASK-193` + `TASK-193a` | Step 1 | `docs/07_implementation_status.md` (delegate; never load full) | `docs/07_implementation_status.md` (worker dispatch edit only) | — | `S` | Backlog rows must exist before any code change so cross-references resolve. No source code in this step. |
| `TASK-193` + `TASK-193a` | Step 2 | `docs/02_ir_schemas.md:433-444` (`ConfigValue`), `:618-730` (`ResolvedConfig`) | NEW `crates/slicer-host/tests/machine_start_end_gcode_emission_tdd.rs` (≤ 400 LOC, 13 tests). Sibling reads: `gcode_header_thumbnail_config_blocks_tdd.rs`, `gcode_emit_tdd.rs:1-120`, `postpass_gcode_emit_contract_tdd.rs:1-80` | — | `M` | TDD-first: lock all 13 ACs as failing tests. Production code untouched. `log` capture mechanism (custom `log::Log` impl, ~30 LOC, no new dep) confirmed via FACT dispatch — `log = "0.4"` is already in `crates/slicer-host/Cargo.toml:20`. |
| `TASK-193a` | Step 3 | `docs/03_wit_and_manifest.md` (schema-validation section only) | `crates/slicer-host/src/config_schema.rs` (≤ 40 LOC added; 4 keys registered following `thumbnail_path` precedent at `:367-:387`) | `OrcaSlicerDocumented/src/libslic3r/PrintConfig.hpp:1243` / `:1288` (key-name spelling, FACT ≤ 6 lines) | `S` | Two ACs turn green: `schema_registers_four_keys_with_expected_types_and_defaults` and `new_keys_appear_in_config_block` (the latter via packet-55's automatic CONFIG_BLOCK propagation). |
| `TASK-193` | Step 4 | `docs/01_system_architecture.md` (finalization / serializer SUMMARY) | `crates/slicer-host/src/gcode_emit.rs` (≤ 100 LOC added: ≤ 60-LOC `substitute_placeholders` helper + two ≤ 10-LOC insertion blocks in `DefaultGCodeSerializer::serialize_gcode()`). Ranged reads: `:626-740`, `:887-977`, `:979-1166`. Ranged reads in `crates/slicer-ir/src/slice_ir.rs:433-444` and `:618-730`. | `OrcaSlicerDocumented/src/libslic3r/GCode.cpp:3181` / `:3200` / `:3258` (start-before-preamble ordering, FACT ≤ 12 lines); `OrcaSlicerDocumented/src/libslic3r/PlaceholderParser.cpp:164` (`apply_config()` symbol-table contract, FACT ≤ 10 lines). NEVER read `PlaceholderParser.cpp` body (`:2433` grammar). | `M` | The remaining 11 ACs turn green here. The two insertion points are byte-offset-contractual relative to packet 54 (preamble) and packet 55 (HEADER / CONFIG_BLOCK) sites. |
| `TASK-193` + `TASK-193a` | Step 5 | — | None edited (or ≤ 5 LOC clippy fix in one file) | — | `S` | Regression sweep: `postpass_gcode_emit_contract_tdd`, `cargo check --workspace`, `cargo clippy --workspace -- -D warnings`. Three FACT dispatches. |
| `TASK-193` + `TASK-193a` | Step 6 | `docs/11_operational_governance_and_acceptance_gate.md` (acceptance-gate section); `docs/12_architecture_gate_metrics.md` (consulted only on workspace-gate fail) | `docs/07_implementation_status.md` rows → `[x]` (worker dispatch); `.ralph/specs/59_machine-start-end-gcode-emission/packet.spec.md` status flip ONLY after explicit user OK | — | `S` | Closure ceremony: re-dispatch all 13 pipe-suffixed AC commands + the once-per-packet `cargo test --workspace` (per CLAUDE.md test discipline). 15 FACT dispatches total. |

**Aggregate context cost:** 5×S + 1×M = **`M`**. No single step is `L`. Within the cap.

**Why this packet covers TASK-193 evidence:** Step 4's 11 newly-green ACs (start/end emission, position assertions, full and partial user override, empty-end-block, unknown-placeholder warning, unclosed-bracket literal, position-regression sentry) collectively demonstrate that `machine_start_gcode` and `machine_end_gcode` are emitted at the correct byte positions with correct minimal `[key]` substitution. TASK-193 is satisfied.

**Why this packet covers TASK-193a evidence:** Step 3's 2 newly-green ACs demonstrate that all four keys are registered with the correct types and defaults AND flow through into the CONFIG_BLOCK emitted by packet 55. The negative case `rejects_temp_out_of_registered_range` (turned green during Step 4 along with the rest of the runtime path) demonstrates that the registered `0..=300` range validator is enforced end-to-end. TASK-193a is satisfied.
