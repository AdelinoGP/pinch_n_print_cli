# Task Map: 140_lightning-module-rewrite

Single-task packet; this crosswalk maps the one task ID to its step + cost.

| docs/07 task ID | Packet step | Primary docs | Expected code surface | OrcaSlicer refs | Context cost | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| `TASK-265` | `Step 0` (WIT + trait + macro + dispatch + 4-module + test-guest + drift-re-baseline) + `Step 1` (Orca FACT + RED) + `Step 2` (the rewrite) + `Step 3` (pipeline + guard) + `Step 4` (closure) | `docs/specs/lightning-infill-parity.md` §L4; `docs/adr/0029-lightning-prepass-tree-generator.md` (SUMMARY); `docs/adr/0025-infill-linker-as-raw-emit-post-pass.md` §Amendment point 2; `docs/DEVIATION_LOG.md` (DEV-081 + `D-137-WIT-RUN-INFILL-NO-PAINT-VIEW`) | `modules/core-modules/lightning-infill/src/lib.rs` (the rewrite) + `modules/core-modules/lightning-infill/tests/lightning_infill_tdd.rs` (rewritten) + `crates/slicer-schema/wit/deps/world-layer/world-layer.wit` (signature + version) + `crates/slicer-sdk/src/traits.rs` (trait extension) + `crates/slicer-macros/src/lib.rs` (macro glue) + `crates/slicer-wasm-host/src/dispatch.rs` (Infill arm) + four `run_infill`-implementing core modules (rectilinear/gyroid/lightning/top-surface-ironing) + `crates/slicer-wasm-host/test-guests/layer-infill-guest/src/lib.rs` (test-guest extension) + `crates/slicer-runtime/tests/contract/wit_drift_detection_tdd.rs` (drift re-baseline) + `crates/slicer-runtime/tests/executor/lightning_pipeline_linked_tdd.rs` (new) + `docs/DEVIATION_LOG.md` (DEV-081 + D-137-WIT-RUN-INFILL-NO-PAINT-VIEW closure) + `docs/07_implementation_status.md` (TASK-262…265 sweep) | `OrcaSlicerDocumented/src/libslic3r/Fill/FillLightning.cpp` (one SUMMARY only) | Step 0 = L (justified: atomic coupled bundle); Steps 1-4 = M, M, S, S | Closes `D-137-WIT-RUN-INFILL-NO-PAINT-VIEW` (Step 0) and DEV-081 (Step 4); consumes 139's per-region keying contract via the new `_paint: &PaintRegionLayerView` argument. |
