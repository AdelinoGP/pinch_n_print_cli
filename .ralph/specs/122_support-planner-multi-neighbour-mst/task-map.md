# Task Map: support-planner-multi-neighbour-mst

| docs/07 task ID | Packet step | Primary docs | Expected code surface | OrcaSlicer refs | Context cost | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| `TASK-287` (renumbered from source-plan `TASK-263`; collision: `TASK-263` is now Lightning DistanceField per `docs/07_implementation_status.md:228`, packet 138) | Step 1, Step 2, Step 3, Step 4, Step 5 | `docs/specs/support-modules-orca-port.md` §C4 + §Validation Strategy | `modules/core-modules/support-planner/src/lib.rs` (new `aggregate_neighbour_targets` helper; lines 671-682 `nearest_neighbour`/`nearest_distance` lookup replaced with all-neighbours scan; lines 688-704 move-target synthesis updated to call the new aggregate); `modules/core-modules/support-planner/tests/multi_neighbour_mst_tdd.rs` (NEW; 4 unit tests); `crates/slicer-runtime/tests/integration/support_invariants_wedge_tdd.rs` (extended with `merge_geometry_symmetric_for_n_branches` — the 9th invariant); `resources/golden/support_regression_wedge_branch_count.txt` and `..._endpoints.txt` (regenerated via `SUPPORT_WEDGE_REGEN_GOLDEN=1`); `docs/specs/support-modules-orca-port.md` §Validation Strategy (one-line invariant list extension). | `OrcaSlicerDocumented/src/libslic3r/Support/TreeSupport.cpp::TreeSupport::drop_nodes` (~line 2625; delegate SUMMARY) | M | The reciprocal-distance aggregate replaces the single-neighbour lookup. Existing `max_move_xy` cap (lines 695-704) and `clamp_to_avoidance` enforcement (line 707) are preserved; only the move *direction* changes. Degenerate `D_j = 0` collapses to that neighbour's position (no division-by-zero). |

Aggregate context cost across rows: `M`. No row is L.
