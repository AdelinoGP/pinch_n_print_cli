# Task Map: 190-smoothed-overhang-speed

This packet groups a single task ID (`TASK-309`, per `packet.spec.md`'s `task_ids` frontmatter). The crosswalk is carried anyway because that ID is **newly allocated** and registering it in `docs/07_implementation_status.md` is packet work (Step 6, `packet.spec.md` §Doc Impact Statement) — this table is the mapping that registration must reproduce.

Its backlog authority is not `docs/07` but `docs/specs/deviation-backlog-remediation-plan.md` §Packet Queue **row 9b**, the middle of the 9a/9b/9c split the orchestrator made of `DEV-009` in tranche T3. That row also records the **maintainer's option (C) ruling**, in the plan's `Queue amendment (2026-07-25d)`. **Do not quote the row's text, its rendering, or any TASK-ID hit count into this or any other artifact** — the queue is mutable shared state amended while packets are in flight, and an earlier revision of this packet's `requirements.md` froze a row rendering that is now 0 hits in the plan file. Re-derive at the moment of use:

```
rg -n '^\| 9b ' docs/specs/deviation-backlog-remediation-plan.md
```

The rows begin at column 1, so `^` must anchor directly on the leading `|`; a pattern written `^.\|` consumes that `|` and then demands a second one — measured in this packet set at 0 hits and exit 1, a silently-empty re-derivation.

**The TASK id is the one ledger fact this packet cannot avoid freezing, and it must be re-derived before Step 6 writes the row.** Two sources disagree and both must be consulted:

```
rg -o 'TASK-[0-9]{3}' docs/07_implementation_status.md | sort -u | tail -1
rg -o 'TASK-[0-9]{3}' .ralph/specs --no-filename | sort -u | tail -1
```

The specs tree runs **ahead** of `docs/07`. Take the next free number above the higher of the two; if that is no longer `TASK-309`, change it in all five files of this packet before Step 0.

| docs/07 task ID | Packet step | Primary docs | Expected code surface | OrcaSlicer refs | Context cost | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| (re-derive) | `Step 0` | none | none (read-only; dependency probes plus four baselines, all delegated) | none | `S` | **`AC-18` — the `[BLOCK]` activation gate — now PASSES**: `design.md` §Open Questions carries the line-anchored `RESOLVED [BLOCK-1/1b/2/3]: option C` marker. What is **not** cleared is the ADR obligation option (C) still carries, which `AC-19` gates and which *is* implementer work; `AC-19` is expected to FAIL here and clear in Step 6. The 193 dependency probe asserts the **field and the stamping site**, not a helper symbol — under (C) 193 delivers data, not an API. |
| (re-derive) | `Step 1` | none | `modules/core-modules/overhang-classifier-default/tests/basic_tdd.rs` | `ExtrusionProcessor.hpp` — `ExtrusionQualityEstimator::estimate_extrusion_quality`'s `calculate_speed` lambda | `M` | Red-first drivers. The single whole read of the module source happens here. **Four pre-existing tests assert the behaviour being changed** (two on the `SetSpeedFactor` mutation, two on the `max(overhang_q, curl_q)` merge) — ADR-0053 §Consequences requires them **migrated, not deleted**; a genuinely obsolete one is recorded as deliberately retired in `DEV-009`'s progress paragraph, never silently dropped. |
| (re-derive) | `Step 2` | `docs/15_config_keys_reference.md`, `docs/03_wit_and_manifest.md` | `modules/core-modules/overhang-classifier-default/overhang-classifier-default.toml` | `PrintConfig.cpp` — `PrintConfigDef::init_fff_params` | `S` | The three config keys, including `slowdown_for_curled_perimeters` — the key that retires ADR-0032's "**No new config keys.**" consequence. ADR-0032 named its own reversal condition ("If that independent control is ever needed, it is new scope"); this is that scope, taken deliberately, and `AC-19` files the amendment row. |
| (re-derive) | `Step 3` | none | `modules/core-modules/overhang-classifier-default/src/lib.rs` (`OVERHANG_OVERLAP_LEVELS`, `build_speed_sections`, `calculate_speed`) | `ExtrusionProcessor.hpp` — `estimate_extrusion_quality`'s section-table construction and `calculate_speed` | `M` | The six-entry `{90, 75, 50, 25, 13, 0}` table restored as **emission-time speed sections**. Sort ascending by distance with ties broken higher-speed-first, then **flatten, do not de-duplicate** — canonical has no `std::unique` and a dedup changes which pair the interpolation brackets (ADR-0053 §"What option (C) does NOT change"). **No boundary-scan function is written here** — option (C) deleted it. |
| (re-derive) | `Step 4` | `docs/03_wit_and_manifest.md` | `modules/core-modules/overhang-classifier-default/src/lib.rs` (`run_finalization`), its `.toml` `[ir-access] reads`, `crates/slicer-runtime/tests/integration/overhang_classifier_refactor_regression_tdd.rs` (the TRIPWIRE mirror, rewritten in the **same** step) | `ExtrusionProcessor.hpp` — `estimate_extrusion_quality`'s per-point loop | `M` | **Reads** each point's `overhang_distance_mm`; adds no scan and grows no produce phase. Two consumption rules, both from 193's contract and both easy to get wrong: the value is **signed** (feed it to `calculate_speed` as-is — never `.abs()`, never add or subtract an offset), and `None` means "not measured" ⇒ factor `1.0`, never `0.0`/`-1.0`/`f32::MAX` (193's `AC-N1` enumerates all three). Emits one `SetPointSpeedFactors`; deletes `SetSpeedFactor` and the callerless `quartile_for_distance`. |
| (re-derive) | `Step 5` | `CLAUDE.md` §"Guest WASM Staleness" | none (guest rebuild plus four delegated cargo runs) | none | `M` | Full guest rebuild and the regression wall. `cargo xtask build-guests --check` must be clean before any component or dispatch failure is attributed to this packet. |
| (re-derive) | `Step 6` | `docs/DEVIATION_LOG.md`, `docs/15_config_keys_reference.md`, `docs/07_implementation_status.md`, `docs/03_wit_and_manifest.md` | `crates/slicer-core/src/algos/overhang_annotation.rs` — **module doc-comment ONLY** | none | `S` | The `DEV-009` progress paragraph (naming `ADR-0053`), the three `D-<n>-ADR-####-AMENDED` rows `AC-19` requires, the new `DEV-###` row, and the `TASK-309` registration **outside** the generated open-deviations block. **`const BAND_BOUNDARY_MULTIPLIERS` and every band-geometry expression stay byte-identical** — 193's `AC-N2` compares that declaration against `HEAD`. ADR-0053 §"Consequent obligation" is why the doc-comment correction lands in this packet and not a later one. |

Costs are copied from `implementation-plan.md` §Per-Step Budget Roll-Up. Aggregate `M`; no step is `L`, so no pre-activation split is required.

## Packet crosswalk

| Packet | Relationship | Where discharged |
| --- | --- | --- |
| `193-overhang-distance-prepass-carrier` | **Hard prerequisite.** Under option (C) this packet *reads* `Point3WithWidth.overhang_distance_mm` rather than computing a cross-layer wall distance in-module. Without 193 there is no continuous distance to interpolate over, and the only alternative is the in-module scan option (C) exists to avoid. Do not start until 193's `AC-1`, `AC-4` and `AC-6` are green — the field, the signedness contract, and the stamping reaching regions with no overhang bands. **193's task id is re-derived, not frozen** | `packet.spec.md` §Prerequisites; `implementation-plan.md` §Execution Rules and Step 0's 193 probe |
| `189-per-point-speed-factor-carrier` (TASK-308) | **Hard prerequisite.** This packet's output is `EntityMutation::SetPointSpeedFactors(Vec<f32>)` into `LayerCollectionIR.speed_profiles`, read per point by `crates/slicer-gcode/src/emit.rs`. None of it exists today. Do not start until 189's `AC-3`, `AC-5` and `AC-6` are green. **189 and 193 are independent of each other** — disjoint types, disjoint schema constants — so either order, but both before this packet | `packet.spec.md` §Prerequisites |
| `191-overhang-add-intersections` (TASK-310) | **Unblocked by this packet.** 191 reuses this packet's `speed_sections` table (canonical derives `estimate_points_properties`' `min_distance` from it). **It does NOT reuse a previous-layer distance *function*** — under (C) no such function exists in this module; 191 reads the same 193 carrier this packet reads. An earlier revision of this packet promised 191 a `distance_to_prev_boundary` to consume, and 191's Step 0 probe was written against that promise; both are corrected | `packet.spec.md` §Prerequisites ("Unblocks"); 191's `implementation-plan.md` Step 0 |

## Deviation crosswalk

| Deviation | Relationship | Where discharged |
| --- | --- | --- |
| `DEV-009` | **Advanced, not closed.** This packet appends the smoothed-speed progress paragraph and leaves the row `Open`; 191 flips it. The paragraph must name `ADR-0053`, state that `{90, 75, 50, 25, 13, 0}` returns as emission-time **speed sections** while `annotate_overhangs`' four quartile bands are unchanged, and record the three canonical simplifications (`ext_perimeter_speed`/`original_speed` collapse, unborrowed `object_layer_over_raft()`, `ThinWall`'s `thin_wall_speed` reference). **Its probe is anchored on `TASK-309` and `ext_perimeter_speed`, not on the phrase "smoothed-speed"** — that phrase is the sub-item's own name and already occurs many times in the row, so a phrase probe passes on the unfixed tree. An earlier revision froze an occurrence count for it; the count is deleted rather than corrected, because nothing in the criterion depends on the number, only on the phrase being non-discriminating at any count above zero | `packet.spec.md` §Doc Impact Statement; Step 6 |
| **~~previous-layer boundary proxy row~~ — NOT FILED** | Struck rather than deleted so its removal is auditable. Under option (B) this packet measured from the previous layer's `OuterWall` **centerline** and owed a row for the bias. **Under (C) there is no proxy** — 193 measures against the previous layer's real slice boundary — so filing it would describe a mechanism the tree does not contain. 193 files its own, narrower row for the per-point-vs-per-path `boundary_offset` resolution | `packet.spec.md` §Doc Impact Statement (the struck bullet and its inverted probe) |
| `DEV-093` | **Constrains the verification strategy, is not addressed.** Two runs of the same unmodified release binary already differ by ~100-160 lines, so whole-output G-code byte comparison cannot attribute anything here. Every criterion is structural or unit-level | `packet.spec.md`; `implementation-plan.md` §Acceptance Ceremony |

Every `DEV-###` and `D-###` above must have its number **re-derived at the moment the row is written** — `rg -o '^\| DEV-[0-9]{3}' docs/DEVIATION_LOG.md | sort -u | tail -1` and `rg -o '^\| D-[0-9]{3}' docs/DEVIATION_LOG.md | sort -u | tail -1` respectively. **The two series have separate counters.** Sibling packets in this queue file rows concurrently; ADR-0053 §Consequences says in terms to trust no `D-` number quoted in any packet or ADR, including itself.

## ADR crosswalk

| ADR | Relationship | Where discharged |
| --- | --- | --- |
| `ADR-0053` (overhang emission-time speed sections) | **This packet's decision record.** It states the maintainer's option (C) ruling, quotes the retired clauses of ADR-0031, ADR-0032 and ADR-0008 verbatim with the sources' own emphasis, and names the amendment rows `AC-19` requires. **Read it whole and reuse its quotations rather than re-extracting them.** One thing it deliberately does **not** define: the carrier's signedness and `+ boundary_offset` normalisation — that is 193's, at `.ralph/specs/193-overhang-distance-prepass-carrier/design.md` §Data and Contract Notes §"The signedness contract" | `packet.spec.md` §Authoritative Docs; `AC-19` |
| `ADR-0031` (overhang classification at prepass) | **Amended a second time, by ADR-0053.** Its Decision clause "shrinks to a pure finalization-tier consumer that reads `Point3WithWidth.overhang_quartile` (now populated upstream) and applies `EntityMutation::SetSpeedFactor`" is retired as to the mutation mechanism: `AC-6` removes `SetSpeedFactor` under **every** option including (C). **The clause's architecture survives** — classification stays upstream, the module stays a consumer — which is the trade (C) was chosen to make. **Note the narrow scope of the pre-existing in-body amendment:** `### Amendment (overhang-after-Slice inversion)`'s "stands unchanged" list names *the finalization-consumer clause* but **not** the Consequences bullet about dropping `lines_distancer.rs`; do not read it as covering both. ADR-0031 now carries **two** amendments and anyone reading it must read both | `design.md` §Open Questions `[BLOCK-2]`; `AC-19` |
| `ADR-0032` (curled-edge slowdown) | **Two clauses retired by ADR-0053.** Its "**No new config keys.**" consequence dies with `slowdown_for_curled_perimeters` (ADR-0032 named its own reversal condition), and its `max(overhang_quartile, curl_quartile)` merge plus `quartile_for_distance` are replaced by canonical's `min(curled_speed, extrusion_speed)` after the `original_speed` clamp. **One thing that does not survive automatically:** ADR-0032's equivalence argument rests on the shared table being *monotonic*; a continuous interpolation must **re-establish** that, not inherit it. Do not carry the equivalence claim forward unexamined | `AC-19`; `implementation-plan.md` §Acceptance Ceremony |
| `ADR-0008` (overhang as a `FinalizationModule`) | **Its `set-speed-factor` mutation-kind decision is retired by ADR-0053**; its *placement* decision stands — still a `FinalizationModule`, still through `FinalizationOutputBuilder`, still no new stage. ADR-0031's cross-reference that ADR-0008's decision "stands" should be read with that substitution. ADR-0008's separate "No WIT contract change" consequence is addressed by **ADR-0052**, not here | `AC-19` |
| `ADR-0052` (per-point speed-factor contract) | **The channel this packet's continuous speeds travel through**, established by packet 189. Normative here in two respects: `factors` are **factors** on the same scale as `path.speed_factor`, not absolute mm/s (a producer holding an absolute speed divides by the same role base speed `resolve_feedrate` uses), and `factors.len()` MUST equal the target entity's `path.points.len()` at mutation time | `packet.spec.md` §Prerequisites (the 189 dependency) |
| `ADR-0049` (batched host services) | Source of the fifth `*-batch` function in the fifteen-function `host-services` count that settles `[BLOCK-3]`. **The list is five, not four** — `offset-polygons-batch`, `clip-polygons-batch`, `simplify-polygon-batch`, `raycast-z-down-batch`, `surface-normal-at-batch` — an earlier revision said four and made the enumeration sum to fourteen against its own stated total of fifteen | `design.md` §Open Questions `[BLOCK-3]` |
