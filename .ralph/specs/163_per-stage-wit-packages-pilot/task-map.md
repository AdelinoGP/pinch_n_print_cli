# Task Map: 163_per-stage-wit-packages-pilot

Emitted because this packet carries a **reopened** backlog task. `docs/07_implementation_status.md:39` records TASK-146 as `[x]` closed — "Add host-side `wit_world` allowlist validation using the canonical identifiers and reject mismatched manifests at startup" — but ADR-0044 (accepted) showed `validate_wit_world` compares one hand-written string (`manifest.wit-world`) to another (`SUPPORTED_WIT_WORLDS`) with **no artifact to check against**, and ADR-0045 (accepted) retires the check outright. `docs/specs/adr-0045-per-stage-wit-packages-plan.md` §"Task mapping" therefore reopens TASK-146 and sub-letters it across four packets, following the existing `TASK-119a/b/c`, `TASK-120a-d`, `TASK-194a/b` convention. This packet owns exactly one letter, `TASK-146b`.

## Crosswalk

| docs/07 task ID | Packet step | Primary docs | Expected code surface | OrcaSlicer refs | Context cost | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| `TASK-146b` | `Step 1` | `docs/adr/0045-…md` §"Decision", §"Versions reset to 1.0.0", §"Verified empirically" | `crates/slicer-schema/wit/deps/{postpass-gcode-postprocess,postpass-text-postprocess,finalization-layer-finalization}/` (new); `deps/world-{postpass,finalization}/` (deleted) | none | `S` | The contract itself. AC-1 pins the imported-`-types` / exported-`run` split and zero resources in exported interfaces; AC-1b pins `major >= 1`. |
| `TASK-146b` | `Step 2` | `docs/adr/0006-export-for-stage-id-sole-lookup.md` | `crates/slicer-schema/src/lib.rs` (`StageSpec` + 5 lookups), `crates/slicer-schema/tests/export_for_stage_id_tdd.rs` | none | `S` | ADR-0006 makes `export_for_stage_id` the sole stage→export lookup; this extends its table rather than paralleling it. AC-3 asserts exactly one `STAGES`. |
| `TASK-146b` | `Step 3` | `docs/adr/0002-wit-marshalling-type-unification.md`, `docs/adr/0003-macro-per-world-wit-conversions.md` | `crates/slicer-wasm-host/src/host.rs` (2 tier `bindgen!` mods → 3 per-stage; `postpass_impls`/`finalization_impls` repointed) | none | `M` | ADR-0002's `with:` remap is what must survive 4→5 `bindgen!` calls — the packet's headline risk. AC-4. |
| `TASK-146b` | `Step 4` | `docs/adr/0045-…md` §"Decision", §"Verified empirically"; `docs/adr/0015-prepass-export-normalization.md` | `crates/slicer-wasm-host/src/dispatch.rs` (3 pilot runners; `TypedInstantiation` reason) | none | `M` | This is the TASK-146 slice the reopen actually redeems: enforcement moves from an unfalsifiable string compare to wasmtime's typed instantiation. AC-5, AC-N1. |
| `TASK-146b` | `Step 5` | `docs/adr/0045-…md` §"Verified empirically"; `docs/05_module_sdk.md` §"Module Entry Point" | `crates/slicer-macros/src/lib.rs` (`StageGlueKind`, `resolve_stage_glue`, postpass glue split, interface-grouped `Guest`) | none | `M` | Deletes the benign-`Ok` padding arm — the ADR-0015 "do not swallow" violation that made the miss silent. AC-6. |
| `TASK-146b` | `Step 6` | `docs/adr/0045-…md` §"Verified empirically"; `CLAUDE.md` §"Guest WASM Staleness", §"WIT/Type Changes Checklist" | `crates/slicer-wasm-host/test-guests/{postpass-guest,finalization-guest,finalization-mutation-roundtrip-guest}/src/lib.rs` | none | `M` | First green `cargo check --workspace --all-targets`. Nothing before this point produces a meaningful test result. |
| `TASK-146b` | `Step 7` | `docs/adr/0045-…md` §"Verified empirically"; `docs/adr/0014-xtask-guest-discovery-via-validated-filesystem-walk.md`; `CLAUDE.md` §"Guest WASM Staleness" | `xtask/src/build_guests.rs`, `xtask/Cargo.toml` | none | `M` | Not in the approved plan — added because grounding falsified the premise that packaging alone delivers "doesn't even rebuild". AC-9, AC-N2. |
| `TASK-146b` | `Step 8` | `docs/adr/0045-…md` §"Verified empirically"; `CLAUDE.md` §"Test Discipline" | `crates/slicer-runtime/tests/contract/{wit_single_source_tdd.rs,wit_drift_detection_tdd.rs,dispatch_protocol_tdd.rs}`; `crates/slicer-macros/tests/*`; 5 pilot `modules/core-modules/*/tests/slicer_module_binding_tdd.rs` | none | `M` | All three contract files are already registered in `crates/slicer-runtime/tests/contract/main.rs:19,39,40` (verified), so a new test in them cannot silently report `0 passed`. Sub-batched to honor the 3-edit rule. |
| `TASK-146b` | `Step 9` | `docs/adr/0002-wit-marshalling-type-unification.md`; `CLAUDE.md` §"Test Discipline" | verification only — no new surface | none | `M` | AC-12 is the packet's reason to exist; AC-N3 is the `ff21378e` behavior-neutrality baseline. A failure here stops the queue, it does not get worked around. AC-12 rests on **four** suites, not five — `postpass_gcode_boundary` never instantiates a component (already tracked as `DEV-087`) and is excluded by name. |
| `TASK-146b` | `Step 10` | `docs/specs/adr-0045-per-stage-wit-packages-plan.md` §"Packet Queue", §"Status since approval" | `crates/slicer-schema/wit/README.md`, `docs/03_wit_and_manifest.md`, `docs/DEVIATION_LOG.md`, `docs/07_implementation_status.md` | none | `S` | Records TASK-146b; files **one** row — `DEV-086`, the accepted two-mechanism intermediate, owner `164_per-stage-wit-packages-bulk`/TASK-146c. References the already-committed `DEV-087` (`57ceae39`) for the `postpass_gcode_boundary_tdd.rs` defect; files nothing for it. Re-confirm `DEV-086` is free at implementation time. |

## Sub-lettering of the reopened TASK-146

| Task ID | Packet | Status | Owns |
| --- | --- | --- | --- |
| `TASK-146a` | `162_wit-lifecycle-export-removal` | generated (PREFLIGHT PASS); **must be implemented before this packet starts** | Lifecycle export deletion, `from_config`, `WORLD_LIFECYCLE_EXPORTS`, CLI-binary staleness at all three spawn sites. |
| `TASK-146b` | `163_per-stage-wit-packages-pilot` (**this packet**) | draft | The per-stage package mechanism + the postpass (2) and finalization (1) stages + per-stage `xtask` staleness. |
| `TASK-146c` | `164_per-stage-wit-packages-bulk` | pending | Prepass (4) and layer (10); retirement of `wit-world` / `SUPPORTED_WIT_WORLDS` / `validate_wit_world`; `docs/03` and `CONTEXT.md` corrected to the delivered contract. Consumes `design.md` §"Exports handed to packet #3". |
| `TASK-146d` | `165_cli-binary-locator-extraction` | pending | Collapsing the three `pnp_cli` binary locators into one home, with an ADR deciding that home. |

Copy costs from `implementation-plan.md`. Split before activation if any row is L or aggregate exceeds M — aggregate is `M`, no row is L.
