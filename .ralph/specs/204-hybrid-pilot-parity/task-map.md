# Task Map: 204-hybrid-pilot-parity

This packet spans two governing decisions and nine steps, so the crosswalk is emitted.

**Backlog note:** `docs/07_implementation_status.md` contains **no** TASK row for the distribution/editions workstream (verified 2026-08-07 by `docs/specs/multi-edition-distribution-plan.md` §"Backlog anchoring [FWD]", which also forbids editing that file while the parallel 194–199 session is active). This packet's `task_ids` are therefore the two governing ADR slots, matching the convention packets 200–203 already use. The rows below map each ADR clause to the step that discharges it.

| Governing clause (task ID) | Packet step | Primary docs | Expected code surface | OrcaSlicer refs | Context cost | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| `ADR-0056` Decision items 1–2 (one model, tier 5) | `Step 1` | `docs/adr/0056-integrated-modules-native-dispatch.md` | none (read-only reconciliation) | none | `S` | Proves the 201/202 FORWARD-DEP symbols exist with the assumed shapes before any code is written against them |
| `ADR-0056` Decision item 1 (embedded manifest, one ingestion path) | `Step 2` | `docs/adr/0056-...md`, `docs/adr/0033-host-service-bridge-for-host-only-algorithms.md` | `crates/slicer-integrated-modules/{Cargo.toml,src/lib.rs}` | none | `S` | AC-1, AC-2, AC-6 static half; per-module features named after module directories |
| `ADR-0056` Decision item 4 + `ADR-0042` §Decision (parity gate is structural, not byte-equal) | `Step 3` | `docs/adr/0042-arachne-parity-structural-invariants-over-fixtures.md` | `crates/slicer-runtime/tests/common/parity_invariants.rs`, `tests/common/mod.rs`, `tests/contract/parity_invariants_selftest_tdd.rs` | none | `M` | AC-N2, AC-N3 (layer family) and AC-N6 (prepass family) — both comparators' non-vacuity proofs land before any subject |
| `ADR-0056` Decision item 4 (per-module parity contract test) | `Step 4` | `docs/adr/0056-...md` Decision item 4 | `crates/slicer-runtime/tests/contract/integrated_parity_classic_perimeters_tdd.rs` + aggregator | none | `M` | AC-3; establishes the dual-`CompiledModuleLive` template Steps 5–6 copy |
| `ADR-0056` Decision item 4 + `ADR-0033` (cfg-split native arm) | `Step 5` | `docs/adr/0042-...md`, `docs/adr/0033-...md` | `crates/slicer-runtime/tests/contract/integrated_parity_arachne_perimeters_tdd.rs` + aggregator; conditional `docs/DEVIATION_LOG.md` row | none | `M` | AC-4, AC-6 runtime half; `host-algos` reaches the native arm through `slicer-sdk`'s non-wasm32 dependency |
| `ADR-0056` Decision item 4 (prepass family) | `Step 6` | `docs/adr/0056-...md` Decision items 4–5 | `crates/slicer-runtime/tests/contract/integrated_parity_support_planner_tdd.rs` + aggregator | none | `M` | AC-5; compares the `SupportPlanIR` inside `PrepassStageOutput::SupportPlan`, keyed on the full `(global_layer_index, object_id, region_id)` triple — `support-planner` reaches geometry only via `slicer_sdk::host_batch`, so polygons are not the comparable surface |
| `ADR-0056` Decision item 2 + Decision item 5 (override wins; single-threaded module logic) | `Step 7` | `docs/adr/0056-...md` Decision item 2, Decision item 5 | `crates/slicer-runtime/tests/integration/hybrid_pilot_external_override_tdd.rs` + aggregator | none | `S` | AC-N4, AC-9 — the packet's contract-boundary negative and the no-rayon floor |
| `ADR-0057` (Hybrid set is a dist-config list, finalized by profiling) + `ADR-0055` (evidence methodology) | `Step 8` | `docs/adr/0057-three-editions-and-integrated-tier.md`, `docs/adr/0055-fuel-based-module-profiling.md` | `dist/editions.toml`, `xtask/src/editions.rs`, `xtask/src/main.rs` | none | `M` | AC-7, AC-8 (both ADR-0055 signals: guest fuel primary + profiling-off wall-clock secondary), AC-N1 — the sole export packet 205 consumes |
| `ADR-0057` (edition vocabulary surfaced in docs) | `Step 9` | `docs/01_system_architecture.md` §"Producing the tier-4 layout: `cargo xtask dist`" | `docs/01_system_architecture.md`; conditional `docs/DEVIATION_LOG.md` | none | `S` | AC-N5 plus the three workspace gate commands |

Costs are copied from `implementation-plan.md` §Per-Step Budget Roll-Up. No row is `L`; the aggregate is `M`.
