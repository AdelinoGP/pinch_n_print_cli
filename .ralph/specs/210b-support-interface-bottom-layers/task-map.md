# Task Map: 210b-support-interface-bottom-layers

Single-task packet, but the crosswalk is emitted for three reasons: **`TASK-327` is revived** (it was folded into `TASK-326` by the 2026-08-07 merge and restored by the same day's re-split — re-derive that the slot is still free at the moment you register it, because the highest `TASK-###` moves while parallel packets land); the paired `TASK-326` belongs to `210a` and must not be re-registered here; and this packet **retires** a capability recorded as shipped under the existing `TASK-163b-diagnostic` row, so that row needs an amendment note rather than a new closure.

| docs/07 task ID | Packet step | Primary docs | Expected code surface | OrcaSlicer refs | Context cost | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| `TASK-327` | `Step 0` | none | none — a delegated signature check against `modules/core-modules/support-planner/src/lib.rs` | none | `S` | The `210a`-merged gate. `split_column_into_chains`, `point_in_any_expoly(&[ExPolygon], Point2)` (the model-landing helper — `LayerCollisionCache.collision_polys` is `Vec<ExPolygon>`, so the ring-level `point_in_polygon_units` is *not* what this packet consumes), `first_point_xyw -> Option<(Point2, f32)>` and the unit-typed `push_interface_scan_lines` must all exist. A mismatch stops the packet — adapting to whatever is on disk is the exact defect that made the original packet 211 unmergeable. |
| `TASK-327` | `Step 1` | `docs/15_config_keys_reference.md` (`support_interface_bottom_layers` note only) | `modules/core-modules/support-planner/src/lib.rs` — `resolve_interface_bottom_layers` (net-new `pub`), `should_densify_bottom_interface` (net-new `pub`), `SupportPlanner.support_interface_bottom_layers`, `from_config` parse | `OrcaSlicerDocumented/src/libslic3r/Support/SupportParameters.hpp` (`number_of_support_interface_bottom_layers`) | `S` | Ports the `< 0 ⇒ use the top count` fallback `DEV-129` names as canonical, and lifts the call-site guard into a pure predicate so it is falsifiable at all. Owns the two-site `SupportPlanner` struct-literal blast radius (`Ok(Self { … })` in `from_config`; `default_planner()` in the in-file test module). |
| `TASK-327` | `Step 2` | `docs/08_coordinate_system.md` §"SDK Helpers" | `modules/core-modules/support-planner/src/lib.rs` — `densify_bottom_interface` (net-new private, second caller of `split_column_into_chains`) and its guarded call after `smooth_branches` in `plan_for_object`; `modules/core-modules/support-planner/tests/interface_bottom_layers_tdd.rs` (net-new, four cases); two in-file skip-path unit tests | `OrcaSlicerDocumented/src/libslic3r/Support/TreeSupport.cpp` (`draw_circles` floor-area block), `.../TreeSupportCommon.hpp` (`support_floor_layers`) | `M` | The answer to `DEV-129`'s design blocker: the landing is detected from the emitted plan plus the per-layer `LayerCollisionCache`, not from a `PlannedSupportNode` field that cannot be filled during a single top-down walk. Three details are load-bearing — each band layer's `z` comes from **that layer's own entry**; `split_column_into_chains` is called with no length filter so short chains still get bands; and the two skip paths (`L_end == 0`, `found_contact == false`) get **separate** in-file unit tests because the first masks the second in any planner fixture. |
| `TASK-327` | `Step 3` | `docs/adr/0010-typed-diagnostic-channel.md` §Status and §Context | `modules/core-modules/support-planner/src/lib.rs` (delete the code-1003 block), `.../support-planner.toml` (delete the "Not yet implemented" comment), `.../tests/diagnostics_tdd.rs` (rewrite the two bottom-layers cases) | none | `S` | Retires the stub only after Step 2 makes the warning false. The two pinned tests are rewritten to "exactly 0 code-1003 records" under their original names, never weakened, deleted, renamed or `#[ignore]`d — and the now-unreachable `ibl_diags[0]` binding is deleted while the comment gains the word `retired`, because those are the only clauses of AC-N4 that discriminate the rewrite. |
| `TASK-327` | `Step 4` | `CLAUDE.md` §Test Discipline, §"Guest WASM Staleness" | guest rebuild; `support_invariants_wedge_tdd` and both golden comparators as read-only oracles; `resources/golden/**` regeneration if attributable to bands-on-by-default | none | `S` | Proves bands do not violate invariant 2 and reconciles the band-attributable golden drift, which is **expected** here because `-1` resolves to `support_interface_top_layers = 2`. Clippy `too_many_arguments` on `densify_bottom_interface` is the anticipated lint. |
| `TASK-327` | `Step 5` | `docs/DEVIATION_LOG.md` (`DEV-129`, next free row), `docs/07_implementation_status.md` (Workstream 3), `docs/15_config_keys_reference.md`, `docs/adr/0010-typed-diagnostic-channel.md` §Status **and** §Context | four ledger files; split 5a/5b | none | `S` | Closes `DEV-129` **and files one new `Open` row** for the two permanent divergences the bottom band introduces, so they are not buried in a closure cell. Every ID and status is re-derived at edit time; nothing is quoted from this packet. |
| `TASK-163b-diagnostic` (existing, closed) | `Step 5` | `docs/07_implementation_status.md`, `docs/adr/0010-typed-diagnostic-channel.md` §Status and §Context | no code surface — amendment notes only | none | `S` | Amendment, not reopening. That row records codes 1001/1002/1003 as shipped by packet 118; code 1003 is now retired because its underlying gap is closed. ADR-0010's §Context sentence "All three shipped via packet 118 with the codes `1001` / `1002` / `1003`" goes stale at the same moment and is corrected in the same step — a §Status-only edit leaves §Context wrong. Codes 1001 and 1002 are unaffected and the normative §Decision is unchanged, so no ADR-amendment deviation is required. |

Costs are copied from `implementation-plan.md` §Per-Step Budget Roll-Up. No row is `L`; the aggregate is `M`.

## Sibling and superseded packets

- `.ralph/specs/210a-support-planner-coord-t/` carries `TASK-326` and `DEV-128`. **Do not register `TASK-326` from this packet.** This packet depends on `210a` being implemented and merged, not merely generated.
- `.ralph/specs/211-support-interface-bottom-layers/` is `status: superseded` (by the merged `210`, which was subsequently re-split). `TASK-327` originated there, was folded into `TASK-326` by the merge, and is revived here. Its directory is retained for provenance: do not revive it, do not edit it, do not delete it. The full chain is in `requirements.md` §Provenance.

## Upstream / downstream

Upstream: `210a-support-planner-coord-t`, **implemented and merged**. Consumed symbols and their expected shapes are tabulated in `design.md` §Prerequisites and gated by Step 0.

Downstream: nothing in the 206–212 queue consumes any symbol added here. `resolve_interface_bottom_layers`, `should_densify_bottom_interface` and `densify_bottom_interface` have no call sites outside `modules/core-modules/support-planner/`, and no crate outside that directory depends on this module's library surface.
