# Task Map: 189-per-point-speed-factor-carrier

This packet groups a single task ID (`TASK-308`, per `packet.spec.md`'s `task_ids` frontmatter). The crosswalk is carried anyway because that ID is **newly allocated** and registering it in `docs/07_implementation_status.md` is packet work (Step 8, `packet.spec.md` §Doc Impact Statement) — this table is the mapping that registration must reproduce.

Its backlog authority is not `docs/07` but `docs/specs/deviation-backlog-remediation-plan.md` §Packet Queue **row 9a**, the first of the 9a/9b/9c split the orchestrator made of `DEV-009` in tranche T3. **Do not quote that row's text, its rendering, or any TASK-ID hit count into this or any other artifact** — the queue is mutable shared state the orchestrator amends while packets are in flight, and an earlier revision of this packet's `backlog_source` froze both a row rendering and a "zero hits" count that were true when written and false within the hour. Re-derive at the moment of use:

```
rg -n '^\| 9a ' docs/specs/deviation-backlog-remediation-plan.md
```

The rows begin at column 1, so `^` must anchor directly on the leading `|`; a pattern written `^.\|` consumes that `|` and then demands a second one — measured elsewhere in this packet set at 0 hits and exit 1, a silently-empty re-derivation.

**The TASK id is the one ledger fact this packet cannot avoid freezing, and it must be re-derived before Step 8 writes the row.** Two sources disagree and both must be consulted:

```
rg -o 'TASK-[0-9]{3}' docs/07_implementation_status.md | sort -u | tail -1
rg -o 'TASK-[0-9]{3}' .ralph/specs --no-filename | sort -u | tail -1
```

The specs tree runs **ahead** of `docs/07`, because several packets in this batch allocated ids they have not registered yet. If `TASK-308` is no longer the correct allocation, change it in all five files of this packet before Step 1.

| docs/07 task ID | Packet step | Primary docs | Expected code surface | OrcaSlicer refs | Context cost | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| (re-derive) | `Step 0` | none | none (writes `target/pin-layer-collection-schema-before.txt` scratch only) | none | `S` | Pins the pre-packet `CURRENT_LAYER_COLLECTION_IR_SCHEMA_VERSION` triple so `AC-2` can assert a **relative** additive minor bump instead of a hardcoded SemVer, and re-derives the `LayerCollectionIR` literal-site census Steps 3-4 are graded against. Both outputs are ledger facts: the pin lives in untracked `target/`, the census in the swarm log, neither in a tracked file. |
| (re-derive) | `Step 1` | `docs/05_module_sdk.md` (delegated grep — the `modify_entity` variant table only) | `crates/slicer-sdk/tests/finalization_builder_tdd.rs`, `crates/slicer-gcode/tests/gcode_feedrate_emission_tdd.rs` | `GCode.cpp` — `GCode::_extrude`'s per-segment `F` emission (delegate; never load) | `S` | Six red-first drivers. **Red-by-non-compilation is the correct state** — do not stub the new types to make the binaries link. |
| (re-derive) | `Step 2` | `docs/02_ir_schemas.md` §"IR 10 — LayerCollectionIR" | `crates/slicer-ir/src/slice_ir.rs` (`EntitySpeedProfile`, `LayerCollectionIR.speed_profiles`, its `Default` impl, `CURRENT_LAYER_COLLECTION_IR_SCHEMA_VERSION`), `crates/slicer-ir/src/lib.rs` | none | `S` | The carrier itself, and the schema bump. **The bump target is computed from Step 0's pin (minor + 1, patch 0) and appears as a literal in no packet file.** ADR-0052 §Decision 2 fixes the shape: an `entity_id`-keyed side table, **not** a per-point field on `Point3WithWidth`. |
| (re-derive) | `Steps 3, 4` | none | the exhaustive `LayerCollectionIR` struct-literal sweep, split by crate group (`slicer-ir`/`slicer-macros`/`slicer-gcode`, then `slicer-runtime`/`modules/core-modules`) | none | `S` then `M` | Two mechanical steps, split so neither rates `L`. **One site in `crates/slicer-macros/src/lib.rs` is NOT blind:** its `LayerCollectionIR` literal hardcodes a `schema_version` that is already stale against the live constant and will be further behind after Step 2 — switch it to `::slicer_ir::CURRENT_LAYER_COLLECTION_IR_SCHEMA_VERSION` rather than propagating the staleness. File counts in `implementation-plan.md` are ledger facts; the **file list** is the authority, not the numbers. |
| (re-derive) | `Step 5` | `docs/03_wit_and_manifest.md`, `docs/05_module_sdk.md` (both delegated grep) | `crates/slicer-schema/wit/deps/world-finalization/world-finalization.wit` (`variant entity-mutation`), `crates/slicer-sdk/src/traits.rs` (`EntityMutation`, `FinalizationOutputBuilder::apply_to`), `crates/slicer-wasm-host/src/host.rs`, `crates/slicer-wasm-host/src/dispatch.rs`, `crates/slicer-macros/src/lib.rs` | none | `M` | The five-file mutation chain plus the applier plus a full guest rebuild. **Two non-obvious compile hazards, both written out in the step:** `apply_to`'s new arm cannot copy the existing arms' `iter_mut().and_then(…)` shape (the entity borrow outlives the block and there is no `layer` binding left for `speed_profiles`), and `slicer-macros`' arm needs `v.clone()` where its two neighbours deref `*v`. This is the step that invalidates **every** guest artifact — ADR-0052 records that as the honest cost of the WIT change. |
| (re-derive) | `Step 6` | none | `crates/slicer-gcode/src/emit.rs` (`emit_gcode`'s per-entity loop, `resolve_feedrate`, the simplification `kept` remap) | `GCode.cpp` — `GCode::_extrude` | `S` | The only consumer that turns a factor into an `F` token. `resolve_feedrate`'s signature and body stay **unchanged** (ADR-0052 §Decision 1); the `kept` remap must carry the **original** index, or every factor silently shifts wherever simplification drops a vertex. |
| (re-derive) | `Step 7` | none | none (four delegated cargo runs; no source edits) | none | `M` | The blast-radius regression sweep. `cargo xtask build-guests --check` must be clean before any component or dispatch failure is attributed to this packet. |
| (re-derive) | `Step 8` | `docs/02_ir_schemas.md`, `docs/03_wit_and_manifest.md`, `docs/05_module_sdk.md`, `docs/07_implementation_status.md` | none | none | `S` | Doc anchors and the TASK registration **outside** the generated open-deviations block. **`DEV-009` stays `Open` and this packet must not flip it** — 190 appends its progress paragraph, 191 closes the row. |

Costs are copied from `implementation-plan.md` §Per-Step Budget Roll-Up. Aggregate `M`; no step is `L`, so no pre-activation split is required.

## Packet crosswalk

| Packet | Relationship | Where discharged |
| --- | --- | --- |
| `190-smoothed-overhang-speed` (TASK-309) | **This packet is 190's hard prerequisite.** 190's output is `EntityMutation::SetPointSpeedFactors(Vec<f32>)` written into `LayerCollectionIR.speed_profiles` and read per point by `crates/slicer-gcode/src/emit.rs`. None of that exists today; without this packet 190's per-point interpolation is discarded at the entity boundary | `packet.spec.md` §Prerequisites; `AC-3`, `AC-5`, `AC-6` (the three 190 gates on) |
| `191-overhang-add-intersections` (TASK-310) | Transitive prerequisite, plus **one constraint 191 inherits from here specifically**: `layer-collection-view` exposes no `travel-moves` accessor and hence none for `speed_profiles`, so **a guest can never read back a profile it or a sibling wrote**. 191 submits `SetPathPoints` then a resized `SetPointSpeedFactors` for the same entity and cannot verify the first landed; ordering is a producer obligation, enforced only by `apply_to`'s length check, and `crates/slicer-wasm-host/src/dispatch.rs` swallows `modify_entity`'s `Err` with `log::warn!` | `design.md` §"The guest cannot read back what it writes — and packet 191 depends on that being true"; `packet.spec.md` §Prerequisites |
| `193-overhang-distance-prepass-carrier` | **Independent, not ordered.** 193 adds a per-point *distance* carrier on `Point3WithWidth`; this adds a per-point *speed* carrier in a side table on `LayerCollectionIR`. Disjoint types, disjoint schema constants, either order. The one shared hazard is `CURRENT_LAYER_COLLECTION_IR_SCHEMA_VERSION`, which is **this packet's line and not 193's** — `Point3WithWidth` reaches `LayerCollectionIR` transitively, which is a real reason someone might reach for it. ADR-0052 §"ADR-0048 chose a per-point field …" gives the two-rule test that makes the opposite shape choices correct for the two packets | `design.md` §Reconciliation; `requirements.md` §Out of Scope |

## Deviation crosswalk

| Deviation | Relationship | Where discharged |
| --- | --- | --- |
| `DEV-009` | **Not touched.** Row stays `Open`; this packet ships a carrier and closes neither sub-item. 190 appends the smoothed-speed progress paragraph, 191 closes the row | `packet.spec.md` §Authoritative Docs and §Doc Impact Statement |
| `DEV-093` | **Constrains the verification strategy, is not addressed.** It records that two runs of the same unmodified release binary already differ by ~100-160 lines, so whole-output G-code byte comparison cannot attribute anything. The "absent profile ⇒ byte-identical output" claim therefore rests on `AC-N2`, the feedrate suite and `golden_emit_tdd` — including a **mixed** layer holding both profiled and un-profiled entities, because an all-absent-profiles test cannot see a lookup miss (ADR-0052 §Consequences) | `packet.spec.md` `AC-N2`; `requirements.md` §Out of Scope |

Any `DEV-###` this packet ends up filing must have its number **re-derived at the moment the row is written** (`rg -o '^\| DEV-[0-9]{3}' docs/DEVIATION_LOG.md | sort -u | tail -1`, take the next). Sibling packets in this queue file rows concurrently; a number captured earlier in the session will collide.

## ADR crosswalk

| ADR | Relationship | Where discharged |
| --- | --- | --- |
| `ADR-0052` (per-point speed-factor contract) | **This packet's decision record**, written for it. Fixes both contract questions — `factors` is a `Vec<f32>` of *factors* on the same scale as `path.speed_factor`, not absolute mm/s; the carrier is an `entity_id`-keyed side table on `LayerCollectionIR`, not a field on `Point3WithWidth` — and carries the reconciliations below. **Read it whole and reuse its quotations rather than re-extracting them** | `packet.spec.md` §Authoritative Docs; `design.md` §Reconciliation |
| `ADR-0032` (curled-edge slowdown) | **Applied, not overturned — and no amendment row is owed for it here.** Its §Decision 2 rejected "adding a WIT field, an IR schema-version bump, and marshal fan-out …" as speculative, which is on its face what this packet does. But ADR-0032's own criterion is the *boundary*: curl was computed **and** consumed inside one `run_finalization` call, so nothing crossed a WIT-enforced tier. Per-point speed is produced in a WASM guest and consumed by `DefaultGCodeEmitter` in native code. ADR-0032's word for the qualifying case is "a genuine cross-module IR/WIT field". (Packet 190 separately retires two *other* ADR-0032 clauses under `ADR-0053`; unrelated to Decision 2) | `design.md` §"ADR-0032 §Decision 2 rejected, in terms, the churn this packet incurs"; `packet.spec.md` §Prerequisites |
| `ADR-0008` (overhang as a `FinalizationModule`) | **Addressed by ADR-0052, not amended here.** Its Consequences open "**No WIT contract change**: existing 20 core-modules are unaffected (no rebuild churn)" and this packet adds one additive `variant` case. That clause is a *consequence* recording what that packet cost, not a prohibition; ADR-0008's decision — no new stage, no new WIT *export*, speed application at finalization through `FinalizationOutputBuilder` — stands. **The churn half does now apply and is this packet's honest cost:** bindgen re-reads every `.wit`, so every guest artifact is invalidated. Read the bullet as historical from ADR-0052 forward. `ADR-0053` separately retires ADR-0008's `set-speed-factor` *mutation-kind* decision — 190's amendment, not this packet's | `design.md` §"ADR-0008 §Consequences recorded 'No WIT contract change'"; `AC-10` (the `build-guests --check` gate) |
| `ADR-0048` (`dist_to_top_mm` per-point) | **Divergent shape for a different kind of value; not superseded, and its "Do not re-suggest per-entry `dist_to_top_mm`" is not violated.** ADR-0052 gives the two-rule test: per-point field for geometry-intrinsic data every point has and that must survive interpolation; `entity_id`-keyed side table for sparse, entity-scoped, optional data. **State which rule you are applying before choosing a shape** | `design.md` §Reconciliation (the ADR-0048 paragraph) |
| `ADR-0044` (WIT world version is not an identity token) | Honoured. The `package slicer:world-finalization@1.0.0;` declaration is deliberately **not** bumped: no host or guest resolves a component by that version. The IR schema constant *is* bumped in the same packet because `LayerCollectionIR` is a live `serde` surface with a `Default` contract. The asymmetry is intentional | `design.md` §Data and Contract Notes |
