# Task Map: 191-overhang-add-intersections

This packet groups a single task ID (`TASK-315`, per `packet.spec.md`'s `task_ids` frontmatter). The crosswalk is carried anyway because that ID is **newly allocated** and registering it in `docs/07_implementation_status.md` is packet work (Step 5, `packet.spec.md` §Doc Impact Statement) — this table is the mapping that registration must reproduce.

Its backlog authority is not `docs/07` but `docs/specs/deviation-backlog-remediation-plan.md` §Packet Queue **row 9c**, the last of the 9a/9b/9c split the orchestrator made of `overhang-speed parity` in tranche T3. **Do not quote that row's text, its rendering, or any TASK-ID hit count into this or any other artifact** — the queue is mutable shared state amended while packets are in flight. Re-derive at the moment of use:

```
rg -n '^\| 9c ' docs/specs/deviation-backlog-remediation-plan.md
```

The rows begin at column 1, so `^` must anchor directly on the leading `|`; a pattern written `^.\|` consumes that `|` and then demands a second one — measured in this packet set at 0 hits and exit 1, a silently-empty re-derivation.

**The TASK id is the one ledger fact this packet cannot avoid freezing, and it must be re-derived before Step 5 writes the row.** Two sources disagree and both must be consulted:

```
rg -o 'TASK-[0-9]{3}' docs/07_implementation_status.md | sort -u | tail -1
rg -o 'TASK-[0-9]{3}' .ralph/specs --no-filename | sort -u | tail -1
```

The specs tree runs **ahead** of `docs/07`. Take the next free number above the higher of the two; if that is no longer `TASK-315`, change it in all five files of this packet before Step 0.

| docs/07 task ID | Packet step | Primary docs | Expected code surface | OrcaSlicer refs | Context cost | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| (re-derive) | `Step 0` | none | none (read-only; **two** dependency probes plus five baselines, all delegated) | none | `S` | The single whole read of the module source happens here. **The old `rg -q "fn distance_to_prev_boundary"` conjunct is deleted, not weakened:** under the maintainer's option (C) ruling packet 190 never writes that function, so leaving it in would have printed FAIL and halted this packet on a genuinely-satisfied prerequisite. The replacement probe asserts 193's **carrier** — the `Point3WithWidth` field, the `point3-with-width` WIT record field, the prepass stamping site, and 190's consumption of it — because under (C) the prerequisite delivers **data, not an API**. |
| (re-derive) | `Step 1` | none | `modules/core-modules/overhang-classifier-default/tests/basic_tdd.rs`, `crates/slicer-sdk/tests/finalization_builder_tdd.rs` | `ExtrusionProcessor.hpp` — `estimate_points_properties`, the threshold-crossing block (bounded `SNIPPETS`, ≤ 1 of ≤ 25 lines) | `M` | Ten red-first drivers. **Two fixtures are constrained and the constraints are what make them discriminate.** `AC-5`'s must satisfy `(next.distance + 3 × boundary_offset) / line_len != 0.5` — **not** `a0 != 1.0 - a1`, which reduces to `curr.distance != next.distance` and passes under both the correct and the symmetric-guess implementations. `AC-N3`'s must sit in **a region whose previous-layer slice boundary is empty** — the reachable one of the two `None` triggers packet 193's `AC-N1` defines — with at least one segment of `line_len > 4.0` mm and `min_distance <= 0`. `AC-N4`'s (new under (C)) must have **distinct endpoint distances** and produce **at least two** synthetic vertices on one segment, so a constant or a defaulted field cannot pass as an interpolation. |
| (re-derive) | `Step 2` | `docs/05_module_sdk.md`, `docs/03_wit_and_manifest.md` | `crates/slicer-schema/wit/deps/finalization-layer-finalization/finalization-layer-finalization.wit` (the `variant entity-mutation` case **and** a one-name addition to the existing `use slicer:types/geometry.{…}`), `crates/slicer-sdk/src/traits.rs`, `crates/slicer-wasm-host/src/host.rs`, `crates/slicer-wasm-host/src/dispatch.rs`, `crates/slicer-macros/src/lib.rs` | none | `M` | The `SetPathPoints` geometry channel plus its `apply_to` guards, plus a full guest rebuild. `point3-with-width` is **not currently imported** by `finalization-layer-finalization`; both edits are required in the same change or the WIT does not parse. `AC-N1`'s empty/unclosed-loop rejection lives in `apply_to`, not in the module, because the channel becomes generally available to any future finalization module. |
| (re-derive) | `Step 3` | none | `modules/core-modules/overhang-classifier-default/src/lib.rs` (`segment_intersections`, `min_distance_from_sections`, `insert_extended_points`) | `ExtrusionProcessor.hpp` — `estimate_points_properties`, and `ExtrusionQualityEstimator::estimate_extrusion_quality` for `max_line_length = -1` (three bounded `SNIPPETS` dispatches; never let a sub-agent return the whole function) | `M` | The densest algorithm in the four-packet set. **Three option-(C) shape changes are already worked into the step:** segmentation candidates **interpolate** rather than re-query (no boundary geometry exists in this module) — a real divergence from canonical, which re-measures, filed as a `DEV-###` in Step 5; the synthetic-vertex field list **includes `overhang_distance_mm`**, interpolated (ADR-0053 §Decision item 3; a defaulted value reads as "exactly on the boundary" on every inserted point); the **crossing** branch is untouched, because canonical assigns the intersection vertex `boundary_offset` verbatim there. `a1` carries a leading `1.0 -` and `a0` does not — do not "tidy" it into symmetry. |
| (re-derive) | `Step 4` | none | `modules/core-modules/overhang-classifier-default/src/lib.rs` (`run_finalization`), `crates/slicer-runtime/tests/integration/overhang_classifier_refactor_regression_tdd.rs` (the TRIPWIRE mirror, rewritten in the **same** step) | none | `M` | Wiring plus the mandatory mirror update. **Submission order is the contract:** `SetPathPoints` **then** `SetPointSpeedFactors` for the same entity, because 189's profile branch length-checks against the entity's *current* point count. It is a producer obligation only — a guest cannot read back what it wrote, and `crates/slicer-wasm-host/src/dispatch.rs` swallows `modify_entity`'s `Err` with `log::warn!`. Emit **no** `SetPathPoints` when the list is unchanged (`AC-N2`). |
| (re-derive) | `Step 5` | `docs/05_module_sdk.md`, `docs/03_wit_and_manifest.md`, `docs/02_ir_schemas.md` §IR-10, `docs/DEVIATION_LOG.md`, `docs/07_implementation_status.md` | none (guest rebuild plus six delegated cargo runs) | `M` | Guest rebuild, the regression sweep, and every doc edit: the overhang-speed parity **closure** (naming `ADR-0053`), the `TASK-315` registration **outside** the generated open-deviations block, **two** residual `DEV-###` rows (the `p0`/`p1` `min_spacing` decision from Step 3, and the option-(C) interpolated-rather-than-re-measured distance), and the **`D-<n>-ADR-0031-AMENDED`** clause `AC-N5` requires. Then `cargo xtask check-deviations`. |

Costs are copied from `implementation-plan.md` §Per-Step Budget Roll-Up. Aggregate `M`; no step is `L`, so no pre-activation split is required.

## Packet crosswalk

| Packet | Relationship | Where discharged |
| --- | --- | --- |
| `193-overhang-distance-prepass-carrier` | **Hard prerequisite, and the reason this packet's predicates work at all.** Every predicate ported here reads `Point3WithWidth.overhang_distance_mm`: the XOR crossing test against `boundary_offset + EPSILON`, the outer proximity test `> -boundary_offset && < boundary_offset + 2.0`, and `a0`/`a1`'s `+ 3 × boundary_offset`. All three require the value to be **signed and `boundary_offset`-normalised** — against an unsigned un-offset carrier the proximity test's negative half is unreachable and the XOR degenerates. **193 defines that contract once and this packet cites it; neither restates it**, because a paraphrase is exactly how 190's original unsigned declaration and this packet's signed predicates came to disagree for a round. Do not start until 193's `AC-1`, `AC-4` and `AC-6` are green | `design.md` §Architecture Constraints and §Data and Contract Notes; `packet.spec.md` §Prerequisites; `implementation-plan.md` Step 0's second probe |
| `190-smoothed-overhang-speed` (TASK-309) | **Hard prerequisite** for `build_speed_sections` and `calculate_speed` — canonical derives `min_distance` from `speed_sections`' `smallest_distance_with_lower_speed`, so the segmentation branch's primary gate does not exist until 190 builds that table; and an inserted vertex is only observable if adjacent vertices can carry different speeds. **`build_speed_sections` is pinned by 190's `AC-14`, so probing it is meaningful.** **`distance_to_prev_boundary` is NOT a dependency** — 190 never writes it under option (C). **190 carries zero unresolved `[BLOCK]` items**; an earlier revision of this packet inherited "four" transitively and that is no longer true | `packet.spec.md` §Prerequisites; `implementation-plan.md` §Execution Rules and Step 0 |
| `189-per-point-speed-factor-carrier` (TASK-308) | Transitive prerequisite, via 190. **One constraint inherited from it directly:** `layer-collection-view` exposes no `travel-moves` accessor and hence none for `speed_profiles`, so a guest **cannot read back** a profile it or a sibling wrote. This packet submits a geometry mutation and then a resized profile for the same entity and cannot verify the first landed — hence the submission-order obligation in Step 4. ADR-0052 §Decision 1 supplies the guarantee this packet needs from the other side: a second `SetPointSpeedFactors` for the same `entity_id` **replaces** the row rather than appending | 189's `design.md` §"The guest cannot read back what it writes"; this packet's `design.md` §Architecture Constraints |

## Deviation crosswalk

| Deviation | Relationship | Where discharged |
| --- | --- | --- |
| `overhang-speed parity` | **Closed by this packet** — the `ADD_INTERSECTIONS` mid-segment-splitting sub-item is the last one open. The closing paragraph must use the settled framing (`{90, 75, 50, 25, 13, 0}` returns as emission-time **speed sections** in packet 190, while `annotate_overhangs`' four concentric quartile bands are unchanged by 189/190/191) and must name **`ADR-0053`**. `AC-12` checks for the literal phrase `speed sections`, **not** for a restatement about quartile bands | `packet.spec.md` §Doc Impact Statement; Step 5 — `AC-12` |
| new `DEV-###` (re-derive) | Residual: the `p1` candidate's `min_spacing` test compares against `curr.position` rather than against `p0`, so `p0` and `p1` can land closer to each other than `min_spacing`. **A latent upstream quirk, measured against the checkout.** Reproduce verbatim (recommended — canonical parity is this repo's stated highest priority and the quirk only admits a slightly tighter vertex pair) or fix to a true pairwise test; **one row either way, silence is not an option** | `design.md` §Open Questions (the retained `[FWD]`); Step 3 records the decision, Step 5 files the row |
| new `DEV-###` (re-derive) | Residual, **mandatory under option (C)**: inserted vertices **interpolate** `overhang_distance_mm` between the segment endpoints where canonical genuinely re-measures each new point against `unscaled_prev_layer`. Scoped to the **segmentation** branch only — the crossing branch assigns `boundary_offset` verbatim in canonical too, so it is unaffected. ADR-0053 §Decision item 3 names it "a different algorithm with a different error profile … an accepted divergence [that] belongs in a deviation row" | `design.md` §"Under option (C): interpolate, do not re-measure"; Step 5 — `AC-N4` |
| `infill-linker containment contract` | **Landed and must stay landed** where it touches this module's neighbours; not re-litigated here | `implementation-plan.md` Step 5's regression sweep |
| `DEV-093` | **Constrains the verification strategy, is not addressed.** Two runs of the same unmodified binary already differ by ~100-160 lines of exactly the "loops appear/disappear" shape, so byte comparison cannot attribute this packet's point-count change to it. Every criterion here is structural or unit-level, and `segment_intersections` must break ties deterministically (`total_cmp`) so the packet adds nothing to the existing non-determinism | `design.md` §Risks and Tradeoffs, §Data and Contract Notes; `implementation-plan.md` §Acceptance Ceremony |

Every `DEV-###` and `D-###` above must have its number **re-derived at the moment the row is written** — `rg -o '^\| DEV-[0-9]{3}' docs/DEVIATION_LOG.md | sort -u | tail -1` and `rg -o '^\| D-[0-9]{3}' docs/DEVIATION_LOG.md | sort -u | tail -1` respectively. **The two series have separate counters.** Packets 190 and 193 file rows into both concurrently; ADR-0053 §Consequences says in terms to trust no `D-` number quoted in any packet or ADR, including itself.

## ADR crosswalk

| ADR | Relationship | Where discharged |
| --- | --- | --- |
| `ADR-0053` (overhang emission-time speed sections) | **This packet's decision record, and it covers this packet by name** — verified: its Scope paragraph states "Scope covers packets 190 *and* 191 … both are therefore in scope here", written precisely because this packet warned that an ADR scoped to 190 alone would not cover its geometry mutation. It also supplies the interpolation obligation (§Decision item 3) and the `overhang_distance_mm` addition to the synthetic-vertex field list (§Consequences) | `design.md` §"Option (C) is the ruling"; `AC-N4`, `AC-N5` |
| `ADR-0031` (overhang classification at prepass) | **Departed from, and now amended rather than merely disclosed.** Its Decision says the module "shrinks to a pure finalization-tier consumer …"; this packet makes it a **geometry mutator** rewriting `path.points`. For a round the packet flagged that and filed nothing — **disclosure is not an amendment.** ADR-0053 §Amendments retires the clause "in two respects", the second being in terms that "packet 191 makes the module a **geometry mutator** that rewrites `path.points`". **Division of labour:** 190 files the `D-<n>-ADR-0031-AMENDED` row (its `AC-19`); this packet **appends** its geometry-mutation clause rather than filing a second ADR-0031 row, and files the row outright if 190's is absent. ADR-0031 now carries **two** amendments — the in-body `### Amendment (overhang-after-Slice inversion)` and ADR-0053 — and anyone reading it must read both | `design.md` §"ADR-0031 departure: the amendment this packet owes"; `packet.spec.md` §Prerequisites; `AC-N5` |
| `ADR-0052` (per-point speed-factor contract) | The channel `SetPointSpeedFactors` travels through, and the source of the two guarantees this packet relies on: a repeat mutation for the same `entity_id` **replaces** the row, and `factors.len()` must equal the entity's current `path.points.len()` at mutation time — which is why `SetPathPoints` must be submitted **first**. Also the record that the guest side of the channel is **write-only** | `design.md` §Data and Contract Notes §Ordering; `AC-2` |
