# Task Map: support-plan-raft-plan-and-raftinfill-role

| docs/07 task ID | Packet step | Primary docs | Expected code surface | OrcaSlicer refs | Context cost | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| `TASK-289` (renumbered; replaces source-plan `TASK-265` AND `TASK-266` — both are reused by unrelated work in `docs/07_implementation_status.md:230` for lightning-infill; `TASK-266` is absent from the ledger entirely) | Step 1, Step 2, Step 3, Step 4, Step 5, Step 6, Step 7, Step 8 | `docs/specs/support-modules-orca-port.md` §C6, §D5, §D6 + `docs/adr/0009-raft-as-layer-infill-role.md` + `docs/specs/raft-default-module.md` | `crates/slicer-ir/src/slice_ir.rs` (line 1138 `SupportPlanIR.raft_plan`, new `RaftPlan` + `RaftLayerSpec` structs, line 1639 `ExtrusionRole::RaftInfill` variant, schema_version minor bump); `crates/slicer-sdk/src/views.rs::should_emit` (line 497 new `claim:raft-fill` arm); `crates/slicer-schema/wit/deps/types.wit` (interface `geometry`, lines 12-17; new `raft-infill` variant in `extrusion-role`); `modules/core-modules/support-planner/src/lib.rs` (lines 442-491 degenerate raft block replaced with `RaftPlan` emission; ≈50 lines net); `modules/core-modules/traditional-support/src/lib.rs` (one sentence added to lead `//!`); new tests in `crates/slicer-ir`, `crates/slicer-sdk`, `crates/slicer-runtime`; `docs/02_ir_schemas.md` IR 9b extended. | `OrcaSlicerDocumented/src/libslic3r/Support/SupportCommon.cpp::generate_raft_base` (delegate) | M | The load-bearing item. Step 1 discovery confirms `should_emit` location at `views.rs:497-513` and that the WIT mirror lives in `types.wit` (NOT `ir-types.wit` as the original spec claimed). Step 3 WIT change triggers 20-guest rebuild. The `_ => true` fallback at `views.rs:503` means a missing `RaftInfill` arm would silently emit `true` — AC-3 (structural) and AC-N2 (behavioral) both gate against this. |

Aggregate context cost across rows: `M`. No row is L. The packet collapses the two source-plan TASK rows (`TASK-265` and `TASK-266`) into a single `TASK-289` because the renumbering is necessary (both source-plan IDs are taken) and the work is small enough that the C6 + C7 separation in the source plan is not a meaningful unit-of-work boundary — they're both "complete the raft IR seam" review surface.
