# Task Map: support-plan-raft-plan-and-raftinfill-role

| docs/07 task ID | Packet step | Primary docs | Expected code surface | OrcaSlicer refs | Context cost | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| `TASK-289` | Steps 1-7 | `docs/adr/0009-raft-as-layer-infill-role.md` + `docs/specs/support-modules-orca-port.md` §C6, §C7 | `crates/slicer-ir/src/slice_ir.rs` (`ExtrusionRole` enum at line 1659, new `RaftInfill` variant; `CURRENT_SUPPORT_PLAN_IR_SCHEMA_VERSION` at lines 251-255, bump 1.2.0→1.3.0); `crates/slicer-sdk/src/views.rs::should_emit` (line 498, new `RaftInfill => "claim:raft-fill"` arm); `crates/slicer-schema/wit/deps/types.wit:13-18` (new `raft-infill` member); 14 workspace `match role` sites audited (5-6 non-wildcard sites get the new arm); `crates/slicer-sdk/tests/should_emit_raft_fill_claim_tdd.rs` (new behavioral test for AC-4, AC-N1, AC-N3) | None for this packet (geometry comparison surface is owned by `raft-default-module`) | M | The load-bearing item. The `match role` audit (Step 1) MUST run before the variant is added (Step 2); the audit's `[explicit]` list determines Step 4. The WIT enum addition (Step 3) triggers 20-guest rebuild. The `_ => return true` fallback at `views.rs:504` means a missing `should_emit` arm would silently emit `true`; AC-2 (structural) and AC-4/AC-N1/AC-N3 (behavioral) both gate against this. The schema bump to 1.3.0 is semver-minor per ADR-0009 §Consequences; every test that hard-asserts on the old 1.2.0 value is updated in Step 2. |

Aggregate context cost across rows: `M`. No row is L. The packet collapses the original source-plan `TASK-265` and `TASK-266` (both reused by unrelated work in the ledger) into a single `TASK-289` because the C6 IR-side and C7 doc-side work merged into one additive role/claim extension after the original draft's `Vec<RaftPlan>` geometry claim was found to be already-implemented and §C6-forbidden.
