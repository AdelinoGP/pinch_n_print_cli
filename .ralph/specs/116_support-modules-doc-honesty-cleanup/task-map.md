# Task Map: support-modules-doc-honesty-cleanup

Status: implemented (2026-07-19). All three source-plan rows completed; the test fixture at `modules/core-modules/support-planner/tests/orca_parity_tdd.rs:166,288` (which previously passed the now-removed config key) was also cleaned up as a follow-up to the spec-review finding.

The batch anchor names B1/B2/B3 as `TASK-250`/`TASK-251`/`TASK-252`, but those IDs are not current canonical ownership for this support slice: `TASK-250` and `TASK-252` are unrelated current work, and no support row for `TASK-251` exists. The crosswalk therefore uses source-plan labels only and intentionally assigns no replacement ID. The closest current support ownership is `TASK-163`/`TASK-163b-diagnostic` in `docs/07_implementation_status.md`; the maintainer crosswalk is the source-plan label, not a canonical backlog ID.

| docs/07 task ID | Source-plan work item | Packet step | Primary docs | Expected code surface | Verification | OrcaSlicer refs | Context cost | Notes |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `— (unmapped B1)` | B1 | Steps 1-3, 5 | `docs/specs/support-modules-orca-port.md` §B1 | Three support-module contiguous leading `//!` blocks | AC-1, AC-2, and AC-3 use bounded prefix extraction and assert each required opening line inside the block. | none | S | **Done.** Tree-support, traditional-support, and support-planner leading blocks rewritten to current grid-MST/scan-line/`TreeSupport::drop_nodes`-inspired wording. |
| `— (unmapped B2)` | B2 / D8 | Steps 1, 4-5 | `docs/specs/support-modules-orca-port.md` §B2, §D8 | `SupportPlanner`, `PrepassModule::on_print_start`, `support-planner.toml` | AC-4 rejects all field/struct-literal assignment forms and the parse lookup; AC-7 proves the snake_case schema entry and adjacent deferred-status comment in a bounded excerpt. | none | S | **Done.** Dead field and parse branch removed from `support-planner/src/lib.rs`; `support-planner.toml` `support_interface_bottom_layers` entry preserved with adjacent `Not yet implemented` comment. Packet 118 owns D11 typed warning emission. |
| `— (unmapped B3)` | B3 | Steps 1-3, 5 | `docs/specs/support-modules-orca-port.md` §B3, §D9 | `tree-support`, `traditional-support`, `rectilinear-infill` contiguous leading blocks | AC-6 checks `# Speed normalization`, the formula, and `BASE_SPEED = 50.0` in each current consumer's bounded leading block. | none | S | **Done.** All three current consumers document the normalization; `support-planner` correctly has no speed section. |

Aggregate context cost across rows: `S`; no row exceeded `S` and no row was L. Packet is now `status: implemented`; the maintainer crosswalk remains the source-plan label, not a canonical `TASK-###` ID.
