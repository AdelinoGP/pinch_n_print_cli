# Task Map: 185-arachne-width-bridge-parity

This packet owns two open `docs/07_implementation_status.md` tasks and absorbs the residuals left by packet 184. Packet 184 is a predecessor dependency for the residual behavior, not a second implementation owner.

| docs/07 task ID | Packet step | Primary docs | Expected code surface | OrcaSlicer refs | Context cost | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| `TASK-303` | Steps 1-9, 11-13 | `docs/07_implementation_status.md`; `docs/DEVIATION_LOG.md` rows `DEV-100`, `D-164-WALL-WIDTH-KEYS-NOT-FLOAT-OR-PERCENT`, `D-185-ADR-0043-AMENDED`, `D-185-ADR-0014-AMENDED`, `DEV-102`; `docs/adr/0043-derive-arachne-bead-widths-from-wall-flows.md`; `docs/adr/0014-xtask-guest-discovery-via-validated-filesystem-walk.md` | `crates/slicer-scheduler/src/manifest.rs`; `crates/slicer-scheduler/src/config_resolution.rs`; `crates/slicer-ir/src/resolved_config.rs`; `crates/slicer-core/src/flow.rs`; five module consumers and manifests; targeted transport and precedence tests | `Flow.cpp::new_from_config_width`; `Flow.cpp::auto_extrusion_width`; `PrintConfig.cpp::PrintConfigDef::init_fff_params` | `M` | Retains `Percent`/`FloatOrPercent`, renames the canonical first-layer key with a rejecting legacy-alias collision, and wires geometric role-aware resolution. Flow-ratio controls remain the new `DEV-102` follow-up. |
| `TASK-212b` | Steps 10-13 | `docs/07_implementation_status.md`; `docs/DEVIATION_LOG.md` rows `D-105-FLOW-NOT-WIRED`, `D-185-ADR-0043-AMENDED`, `D-185-ADR-0014-AMENDED`, and `DEV-102`; the two amended ADR files | `object_metadata_to_config_data` in `crates/slicer-model-io/src/loader.rs`, `threemf_sidecar_classification_tdd` regression, the width-resolution matrix, and the final task/deviation updates | `PerimeterGenerator.cpp::process_classic`; `PrintRegion.cpp`; `LayerRegion.cpp`; `PrintConfig.cpp::PrintConfigDef::init_fff_params` | `M` | The loader allowlist is an explicit part of this packet because it is the verified TASK-212b requirement. The task row is checked only after the part-level metadata regression, width-resolution matrix, and amendment records pass. |

Packet 184's residuals are absorbed here: the D-105-FLOW-NOT-WIRED final-infill-boundary residual and D-164-WALL-WIDTH-KEYS-NOT-FLOAT-OR-PERCENT/DEV-100 percent/default residuals are closed or reclassified by the implementation and documentation steps above. `D-152-TOP-AREA-SOURCE` remains open. The packet creates the next free deviation ID only after re-deriving the live ledger at implementation time.

Costs are copied from `implementation-plan.md` §Per-Step Budget Roll-Up. No step is rated `L`; Steps 6-8 remain separate `S` steps to preserve the three-edit cap.
