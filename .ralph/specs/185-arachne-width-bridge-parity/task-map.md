# Task Map: 185-arachne-width-bridge-parity

This packet carries a single `docs/07_implementation_status.md` task ID but closes three independently-logged deviations across two functions and one manifest, so the crosswalk is kept to make the deviation → step → surface mapping explicit and to record the packet-184 collision per row.

**Backlog source.** `docs/specs/deviation-backlog-remediation-plan.md` `## Packet Queue` **row 5** (`<tbd>-arachne-width-bridge-parity`; D-164-arachne, D-168, D-163; tranche T2, no dependencies) — not `docs/07_implementation_status.md`.

**`TASK-304` is newly allocated and must be registered.** It has **zero** hits in `docs/07_implementation_status.md` today; the file's current maximum is `TASK-302` (a ledger fact — re-derive it at the moment of writing rather than trusting this sentence). This is the same registration obligation packet 181 states for `TASK-297`. Step 5b hand-adds the row **outside** the generated block and then runs `cargo xtask check-deviations`; verification `rg -q 'TASK-304' docs/07_implementation_status.md`.

**ADR amendment.** This packet contradicts `docs/adr/0043-derive-arachne-bead-widths-from-wall-flows.md` §Decision item 2, which specifies the two wall-width keys as "plain mm floats, default 0.4, range [0.1, 2.0], group Walls" — AC-1 retypes them to `float_or_percent` and lowers `min` to `0.0`. It therefore amends the ADR **as packet work in Step 5b** and files `D-185-ADR-0043-AMENDED` in Step 5a (which owns every `docs/DEVIATION_LOG.md` edit; 5b's exit verifies the two together), per the `D-161-ADR-0037-AMENDED` / `D-283-ADR-0046-AMENDED` convention (both verified present in `docs/DEVIATION_LOG.md`). The design is **not** re-scoped to conform: ADR-0043's own §Consequences already anticipates this direction ("Upstream models both wall-width keys as `coFloatOrPercent` with default `0` = 'auto from nozzle'; PnP's plain float `min 0.1` cannot express auto — logged as its own deviation row rather than silently narrowed" — that row is D-164). Verification `rg -q 'D-185-ADR-0043-AMENDED' docs/DEVIATION_LOG.md`. Items 1 and 3 of the ADR's Decision are untouched, and no other ADR is engaged (see `design.md` §ADR Alignment).

**Packet 182 is `draft`, not landed.** `crates/slicer-gcode/src/serialize.rs` still holds `outer_wall_line_width: 0.42`. AC-3's `0.4` fallback is locked on **fixture-stability** grounds; the 182 header alignment is a conditional secondary reason only. Do not restate 182 as landed in any row below.

| docs/07 task ID | Packet step | Primary docs | Expected code surface | OrcaSlicer refs | Context cost | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| `TASK-304` (D-164, arachne half) | `Step 1` | `docs/15_config_keys_reference.md` (delegated SUMMARY of the `inner_wall_line_width` prose) | `modules/core-modules/arachne-perimeters/arachne-perimeters.toml` `[config.schema.outer_wall_line_width]` + `[config.schema.inner_wall_line_width]`; `arachne_params_from_config` (`modules/core-modules/arachne-perimeters/src/lib.rs`) | `Flow.cpp` — `Flow::new_from_config_width`, `Flow::auto_extrusion_width`; `PrintConfig.cpp` — `PrintConfigDef::init_fff_params` | `S` | Proves the task by AC-1 (manifest is `float_or_percent`, `min = 0.0`, numeric `default = 0.4`), AC-2 (explicit `0` → `1.125 × nozzle`), AC-3 (absent key still `0.4`, so default geometry is unmoved), AC-4 (`get_abs_value` at both read sites) and AC-N3 (the lowered `min` admits the auto sentinel). Capability-only by design: no live-path code **produces** a `Percent`/`FloatOrPercent` (the sole constructor `parse_percent_default` has its return discarded), filed as a residual in Step 5a and worded about the parser — **not** about `ResolvedConfig::to_config_map`, which merges `extensions` verbatim and already transports any variant. Also amends ADR-0043 §Decision item 2 (see the ADR amendment note above); that edit lands in Step 5b. |
| `TASK-304` (D-168) | `Step 2a` + `Step 2b` | `docs/DEVIATION_LOG.md` (D-168 row, delegated — its stated provenance is false and must be corrected, not repeated) | `impl Default for ArachneParams` (`crates/slicer-core/src/arachne/pipeline.rs`); the duplicate default literal in `crates/slicer-sdk/src/host.rs`; `[config.schema.wall_maximum_resolution]` + `[config.schema.wall_maximum_deviation]`; `ARACHNE_FALLBACKS` (`crates/slicer-runtime/tests/integration/manifest_default_reconcile_tdd.rs`) | `Arachne/WallToolPaths.cpp` — `simplifyToolPaths`; `Arachne/WallToolPaths.hpp` — `WallToolPathsParams`, `meshfix_maximum_resolution`, `meshfix_maximum_deviation`, `meshfix_maximum_extrusion_area_deviation`; `PrintConfig.cpp` — `PrintConfigDef::init_fff_params` | `S` | Proves the task by AC-5 (`0.25` / `0.000625` in `pipeline.rs`, old literals and false `meshfix_*` comments gone), AC-6 (the `slicer-sdk` copy stays in sync), AC-7 (exhaustive reconcile green with `Float(0.5)` / `Float(0.025)`) and AC-8 (a **new** default-asserting unit test; the pre-existing `wall_maximum_resolution_wired` supplies both keys explicitly and proves nothing about the fallbacks). `crates/slicer-core/src/arachne/simplify.rs`'s `ultra_short_threshold` is a distinct `ExtrusionLine.cpp` ~5 µm epsilon that merely coincides with the old value — verified, and out of bounds. |
| `TASK-304` (D-168 blast radius) | `Step 3` | `docs/17_agent_debugging.md`, `docs/19_visual_debug.md` (delegated SUMMARY, only on failure) | Read-only. Ten `ArachneParams::default()`-driven `crates/slicer-core/tests/arachne_*.rs` binaries; `crates/slicer-runtime/tests/arachne_structural_invariants.rs`; `crates/slicer-runtime/tests/integration/perimeter_parity.rs`; `crates/slicer-runtime/tests/executor/arachne_*.rs` | `Arachne/WallToolPaths.cpp` — `simplifyToolPaths` (only if a failure suggests a structural, not tolerance, divergence) | `S` | Proves the task by AC-9 and AC-10. Raising the segment gate `0.05 mm → 0.5 mm` at default config is the packet's largest behavioural risk. **`--features host-algos` is mandatory on every `slicer-core` command** — without it each `arachne_*` binary is `#![cfg(feature = "host-algos")]`-compiled to a no-op. The resulting run is only **partially** vacuous — unrelated `slicer-core` binaries still pass — so neither a non-emptiness nor a line-count check detects the omission; AC-9's guard pins the gated `arachne_pipeline` binary for exactly that reason. `COVERAGE_THRESHOLD = 0.99` must never be lowered to obtain a pass. **Collision:** packet 184 moves the classic side of the same ratio; whichever lands second re-validates it. |
| `TASK-304` (D-163) | `Step 4` | `docs/08_coordinate_system.md`, `docs/02_ir_schemas.md` (both delegated SUMMARY) | `build_walls` (`modules/core-modules/arachne-perimeters/src/lib.rs`) — the `ring_pts_units` hoist, the gated back-conversion loop, and the reuse of `bridge_vertex` in the `feature_flags[i].is_bridge` marking block; `modules/core-modules/arachne-perimeters/tests/bridge_flow_factor_tdd.rs` | `VariableWidth.cpp` — `thick_polyline_to_multi_path`; `Flow.hpp` / `Flow.cpp` — `Flow::bridging_flow`, `Flow::with_width`; `LayerRegion.cpp` — `LayerRegion::bridging_flow` | `M` | Proves the task by AC-11 (hoist + `thick_bridges` gate), AC-12 (`pt.width == 0.4 × √0.7 ≈ 0.334664` and volumetric `≈ 0.0879646 mm³/mm` = canonical `π·dmr²/4`), AC-13 (exemption inert when `thick_bridges` is off) and AC-N2 (non-bridge vertices of the same loop keep the normal back-conversion). The exemption is per **vertex**, not per path, because PnP loops are partially bridgeable. Inert at default config (`thick_bridges` defaults to `false`). The existing `bridge_vertices_get_round_section_factor_when_thick_bridges_on` derives its expectation from `pt.width` itself and is therefore **not** valid proof. |
| `TASK-304` (doc + ledger closure) | `Step 5a` + `Step 5b` | `docs/15_config_keys_reference.md` (ranged; generated block via `cargo xtask gen-config-docs`), `docs/DEVIATION_LOG.md` (delegated rows), `docs/adr/0043-derive-arachne-bead-widths-from-wall-flows.md` (§Decision item 2 + §Consequences, read in full — short), `docs/07_implementation_status.md` (worker-dispatched row insert) | No code surface. Doc 15's generated `module-config-keys` block, its hand-written arachne six-key table, its two wall-width prose paragraphs and its `wall_maximum_resolution` / `wall_maximum_deviation` prose paragraph; the D-163 / D-164 / D-168 rows plus one new `DEV-###` **and** `D-185-ADR-0043-AMENDED`; ADR-0043 §Decision item 2's type/range clause; the `TASK-304` backlog row outside doc 07's generated block | `Arachne/WallToolPaths.hpp` — `meshfix_maximum_resolution`, `meshfix_maximum_deviation` (to restate the provenance correction accurately; **cite by function name, never line number**) | `S` | Proves the task by AC-15 (both the generated and hand-written `wall_maximum_resolution` rows read `0.5`; the `^2$` count catches a partial update; a `! rg -q "state the CODE fallbacks"` clause additionally covers the tolerance-key **prose paragraph**, which is invisible to both row-shaped clauses and would otherwise be left asserting the deviation this packet closes) and AC-16 (all three rows `Closed`, D-168's false `meshfix_*` provenance corrected, one new residual row) plus the three Doc Impact greps for `D-185-ADR-0043-AMENDED`, the ADR text, and `TASK-304`. AC-16's row regex is narrowed to `D-16[348]-(ARACHNE|WALL)` because the bare form also matches the unrelated, already-`Closed` `D-168-SEAM-PREPASS-SOURCE` row (measured: 4 rows bare, 3 narrowed). The new `DEV-###` **must** be re-derived in this step with `rg -o '^\| DEV-[0-9]{3}' docs/DEVIATION_LOG.md \| sort -u \| tail -1` — packet 184 files a same-shaped row and neither packet may assume the other's number. `docs/ORCA_CONFIG_REFERENCE.md` and `docs/specs/deviation-backlog-remediation-plan.md` stay untouched. |

Costs are copied from `implementation-plan.md` §Per-Step Budget Roll-Up. Aggregate is `M`; no row is `L`; splitting before activation is not required.
