# Task Map: 299-object-level-shell-infill-planning

Queue packet from the wayfinder map "Close the OrcaSlicer FFF feature gap"; authored under map ticket 80 (P73). Single-task packet (no backlog slice); this crosswalk exists for the S0 structural gate and records the map provenance.

| docs/07 task ID | Packet step | Primary docs | Expected code surface | OrcaSlicer refs | Context cost | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| `TASK-000` (queue packet, `task_ids: []`) | Steps 1–6 | `docs/01_system_architecture.md` (prepass vs module seam; rule-4 trigger test does not fire); `docs/02_ir_schemas.md` (SlicedRegion field + version bump); `docs/03_wit_and_manifest.md` (additive slice-region-view accessor); `docs/04_host_scheduler.md` (stage ordering); `docs/08_coordinate_system.md` (mm↔unit at shell margins); `docs/DEVIATION_LOG.md` (DEV-190 minted) | `crates/slicer-ir/src/resolved_config.rs` (4 rows) + `slice_ir.rs` (`combined_infill_height`) + `crates/slicer-schema/wit/deps/ir-types.wit` (1 accessor) + `crates/slicer-sdk/src/views.rs` (field + getter) + `crates/slicer-runtime/src/slice_postprocess_prepass.rs` (3 stages + 5 tests) + `modules/core-modules/rectilinear-infill/src/lib.rs` (sparse-height arm + test) + `docs/DEVIATION_LOG.md` + `docs/15_config_keys_reference.md` (regen) | `OrcaSlicerDocumented/src/libslic3r/PrintObject.cpp` (discover_vertical_shells / discover_horizontal_shells / combine_infill) + `OrcaSlicerDocumented/src/libslic3r/utils.cpp` (check_layer_id_pattern) + `OrcaSlicerDocumented/src/libslic3r/PrintConfig.cpp` (defaults) | M | P73: Tier B held, owner narrowed to the host prepass seam (`commit_shell_classification_builtin`) + rectilinear sparse emitter (ticket-36 precedent); membership 4/4, all wired, declaration-only 0; one intended default output change (vertical shells active at canonical `ensure_all`, AC-2); DEV-190 records the Surface-type-zoo simplification, the `interface_shells` non-borrow (P76), and the reslice-invalidation non-borrow (ticket 124); canonical scalarity held via existing per-object overlay (explicitly not ticket 125's tool axis); no numeric range rejection (ticket-113 rule) with strict-parse mode rejection only; locked-path ADR-0062/0063 conformance pinned (AC-N2); CONFIG_BLOCK honest absence (rides 132, no padding edits); gyroid/lightning sparse arms out of scope; no queue-count change; no new fog, nothing out of scope. |

Copy costs from `implementation-plan.md`. Split before activation if any row is L or aggregate exceeds M.