# Task Map: 301-interface-shells-classic-perimeters

Single-task queue packet: P76 maps to one implementation slice. No multi-task crosswalk is needed — every step below serves the same one key.

| docs/07 task ID | Packet step | Primary docs | Expected code surface | OrcaSlicer refs | Context cost | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| `TASK-000` (queue packet, `task_ids: []`) | Steps 1–3 | `docs/01_system_architecture.md` (prepass vs module seam; rule-4 trigger test does not fire); `docs/04_host_scheduler.md` (BASE timelines); `docs/DEVIATION_LOG.md` (DEV-192 minted) | `crates/slicer-ir/src/resolved_config.rs` (1 row + map arm) + `crates/slicer-runtime/src/slice_postprocess_prepass.rs` (resolver + gate + 4 tests) + lock arm + TOML row + `docs/DEVIATION_LOG.md` + `docs/15_config_keys_reference.md` (regen) | `OrcaSlicerDocumented/src/libslic3r/PrintObject.cpp` (`detect_surfaces_type` arms borrowed; `discover_vertical_shells` merge + `PrintObject::infill` scatter as named non-borrows) + `OrcaSlicerDocumented/src/libslic3r/PerimeterGenerator.cpp` (perimeter masks inherited, not re-armed) + `OrcaSlicerDocumented/src/libslic3r/PrintConfig.cpp` (coBool default `false`) | `M` | P76: Tier B held, owner narrowed to the host prepass seam (`commit_shell_classification_builtin`'s Pass-1 neighbour source, ticket-36 precedent); membership 1/1, wired, declaration-only 0; defaults ARE identity on single-body prints (AC-4); DEV-192 (spiral-conjunct + vertical-merge + perimeter-mask + reslice non-borrows); canonical scalarity held via existing per-object overlay (explicitly not ticket 125's tool axis); no numeric range rejection (ticket-113 rule; wrong-variant spelling is the typed `extract_bool` mismatch); no IR/WIT/emitter change; locked-path ADR-0062/0063 conformance pinned (AC-N1); padding twin untouched, shadowed via the map arm (rides 132); no queue-count change; no new fog, nothing out of scope. |
