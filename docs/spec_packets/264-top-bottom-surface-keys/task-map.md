# Task Map: top-bottom-surface-keys

Use this crosswalk when a packet spans more than one task ID, reopens prior work, or supersedes an earlier packet. **This packet emits the template's own skip clause:** it is a single-coherent-slice queue packet with `task_ids: []` (queue precedent — packets 234a, 253–263), so the `docs/07` crosswalk is N-A. Implementation is recorded against wayfinder ticket 17 (`docs/specs/orca-feature-gap/issues/17-author-packet-p10-strength-top-bottom-shells-infill-modules.md`).

This revision **overwrites** the packet's prior Tier A draft in place (number 264 and slug `top-bottom-surface-keys` retained, with explicit user approval) under the map's Authoring rules 1–6. The prior draft wired two density keys and declared the two pattern keys with-gap; rule 1 prohibits that disposition, and rule 4 identifies the claim-holder mechanism as the correct home for a pattern enum. No predecessor packet is superseded.

Note for the closing agent: this packet adds six core modules, so `docs/07_implementation_status.md`'s module inventory does change. Re-derive at completion time whether that inventory is generated or hand-maintained before editing it.

| docs/07 task ID | Packet step | Primary docs | Expected code surface | OrcaSlicer refs | Context cost | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| — (queue packet, `task_ids: []`) | Steps 1–11 | `docs/01_system_architecture.md` §Claim System, `docs/04_host_scheduler.md` §Claim Resolution, `docs/03_wit_and_manifest.md` §Known claim IDs, `docs/adr/0027-gyroid-multi-role-fill-holder.md`, `docs/adr/0056-integrated-modules-native-dispatch.md`, `docs/08_coordinate_system.md`, `docs/21_data_defaults_and_fixtures.md`, `docs/15_config_keys_reference.md` (generated) | six new `modules/core-modules/{monotonicline,alignedrectilinear,concentric,hilbert-curve,archimedean-chords,octagram-spiral}-infill/**` trees + `modules/core-modules/rectilinear-infill/{src/lib.rs,rectilinear-infill.toml,Cargo.toml,tests/**}` + `modules/core-modules/monotonic-infill/{monotonic-infill.toml,src/lib.rs}` + `crates/slicer-scheduler/src/config_resolution.rs` + root `Cargo.toml` + `crates/slicer-integrated-modules/{Cargo.toml,src/lib.rs}` + `crates/pnp-cli/Cargo.toml` + scheduler/runtime test arms + two hand-maintained docs | `Fill/Fill.cpp` (`group_fills`, `Layer::make_fills`), `Fill/FillBase.cpp` (`Fill::new_from_type`), `Fill/FillRectilinear.cpp` (`FillMonotonic::fill_surface`, `FillMonotonicLines::fill_surface`, `connect_segment_intersections_by_contours`, `fill_surface_by_lines`, `FillAlignedRectilinear`), `Fill/FillConcentric.cpp` (`FillConcentric::_fill_surface_single`), `Fill/FillPlanePath.cpp` (`FillPlanePath::fill_surface`, `FillHilbertCurve`, `FillArchimedeanChords`, `FillOctagramSpiral`), `PrintConfig.cpp` (`PrintConfigDef::init_fff_params`), `PerimeterGenerator.cpp` (`top_fill_replaces_inner_walls`), `PrintObject.cpp` (`detect_surfaces_type`) | L (aggregate; no single step above M) | Re-authored under the map's Authoring rules 1–6; **Tier C** (was Tier A). Hard dependency on packet 262b. Re-derive the module-count and deviation-row ledger facts at point of use per the ledger-fact rule |
