# Task Map: fuzzy-skin-gate-and-mode-keys

Use this crosswalk when a packet spans more than one task ID, reopens prior work, or supersedes an earlier packet. **This packet emits the template's own skip clause:** it is a single-coherent-slice queue packet with `task_ids: []` (queue precedent — packets 234a, 253–263), so the `docs/07` crosswalk is N-A. Implementation is recorded against wayfinder ticket 14 (`docs/specs/orca-feature-gap/issues/14-author-packet-p07-others-fuzzy-skin-fuzzy-skin.md`).

This packet and `docs/spec_packets/259b-fuzzy-skin-noise-modules/` together **replace** the single `259-fuzzy-skin-keys` draft, re-authored under the map's Authoring rules 1–6 with explicit user approval for the split (210a/210b, 262a/262b precedent). The prior draft declared five of seven keys with-gap; rule 1 prohibits that disposition. 259a takes the three control keys; 259b takes the four noise keys. No other packet is superseded.

Note for the closing agent: this packet adds no module, so `docs/07_implementation_status.md`'s module inventory does not change. It does change `crates/slicer-ir`'s public `LoopType` surface — re-derive at completion time whether `docs/07` or `docs/03_wit_and_manifest.md` enumerates that enum before editing either.

**This packet carries an unresolved `[BLOCK]` (BLOCK-1, `design.md` § Open Questions): the contour/hole distinction is both an IR schema change and a WIT interface change.** It stays `draft` and must not be activated until an architecture owner resolves it.

| docs/07 task ID | Packet step | Primary docs | Expected code surface | OrcaSlicer refs | Context cost | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| — (queue packet, `task_ids: []`) | Steps 1–5 | `docs/03_wit_and_manifest.md`, `docs/04_host_scheduler.md`, `docs/08_coordinate_system.md`, `docs/21_data_defaults_and_fixtures.md`, `docs/DEVIATION_LOG.md`, `docs/15_config_keys_reference.md` (generated) | `crates/slicer-ir/src/slice_ir.rs` (`LoopType`, `WallLoop`) + `crates/slicer-ir/tests/loop_type_hole_tdd.rs` + `modules/core-modules/fuzzy-skin/{fuzzy-skin.toml,Cargo.toml,src/lib.rs,tests/**}` + `modules/core-modules/{classic,arachne}-perimeters/src/lib.rs` + scheduler/runtime test arms + all guest `.wasm` | `Feature/FuzzySkin/FuzzySkin.cpp` (`should_fuzzify`, `fuzzy_extrusion_line`, `fuzzy_polyline`, `group_region_by_fuzzify`), `PerimeterGenerator.hpp` (`FuzzySkinConfig`), `PerimeterGenerator.cpp` (`traverse_loops`, `traverse_extrusions`), `PrintConfig.cpp` (`PrintConfigDef::init_fff_params`, `s_keys_map_FuzzySkinType`, `s_keys_map_FuzzySkinMode`) | M (aggregate; no step above M) | Re-authored under Authoring rules 1–6; **Tier B** (was Tier A). **Open [BLOCK]: BLOCK-1.** Unblocks 259b. Re-derive schema-version and deviation-ID ledger facts at point of use |
