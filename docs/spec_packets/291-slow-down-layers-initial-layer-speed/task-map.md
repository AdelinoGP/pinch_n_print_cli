# Task Map: 291-slow-down-layers-initial-layer-speed

Queue packet from the wayfinder map "Close the OrcaSlicer FFF feature gap"; authored under map ticket 65. Single-task packet (no backlog slice); this crosswalk exists for the S0 structural gate and records the map provenance.

| docs/07 task ID | Packet step | Primary docs | Expected code surface | OrcaSlicer refs | Context cost | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| `TASK-000` (queue packet, `task_ids: []`) | Steps 1–1b–4 | `docs/config/host-keys.toml`; `docs/DEVIATION_LOG.md` (DEV-183); `docs/15_config_keys_reference.md` (generated via `cargo xtask gen-config-docs`, Step 1b); `docs/01_system_architecture.md` (claim-system trigger test) | `crates/slicer-ir/src/resolved_config.rs` + `crates/slicer-gcode/src/emit.rs` (print-path blend arm + helper + emitter-side bounds) + `crates/slicer-gcode/tests/speed_p58_slow_down_layers_emission_tdd.rs` (new) + `docs/config/host-keys.toml` | `OrcaSlicerDocumented/src/libslic3r/PrintConfig.cpp` (`slow_down_layers` coInt `0` min `0`, scalar) + `OrcaSlicerDocumented/src/libslic3r/GCode.cpp` (`GCode::_extrude` gate + `is_perimeter` selection + never-slow guard + lerp shape + raft-offset arm + `speed_for_path` call-site position; bottom/skirt/brim site rows differ — DEV-183(b)) | M | P58: Tier B held, owner `crates/slicer-gcode` stands (all canonical reads emission-side in `_extrude`; machine-gcode-emit sweep is the wrong seam; no blend exists anywhere in tree — reuse nothing, fork nothing); membership held 1-in, none shed, none returned (live in canonical, zero-occurrence here); canonical scalarity IS held (coInt scalar — no ticket-125 vector arm); defaults ARE identity (`0`/`1` inert by the `> 1` gate, AC-2 pins it); `#if 0` over-raft arm not borrowed; raft offset is a comment (no raft prefix layers emit); bottom/skirt/brim held flat as port divergences (role-speed table differs at those rows). |

Copy costs from `implementation-plan.md`. Split before activation if any row is L or aggregate exceeds M.
