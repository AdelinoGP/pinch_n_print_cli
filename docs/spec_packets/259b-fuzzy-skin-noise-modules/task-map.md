# Task Map: fuzzy-skin-noise-modules

Use this crosswalk when a packet spans more than one task ID, reopens prior work, or supersedes an earlier packet. **This packet emits the template's own skip clause:** it is a single-coherent-slice queue packet with `task_ids: []` (queue precedent — packets 234a, 253–263), so the `docs/07` crosswalk is N-A. Implementation is recorded against wayfinder ticket 14 (`docs/specs/orca-feature-gap/issues/14-author-packet-p07-others-fuzzy-skin-fuzzy-skin.md`).

This packet and `docs/spec_packets/259a-fuzzy-skin-gate-and-mode-keys/` together **replace** the single `259-fuzzy-skin-keys` draft, re-authored under the map's Authoring rules 1–6 with explicit user approval for the split (210a/210b, 262a/262b precedent). 259a takes the three control keys; this packet takes the four noise keys. No other packet is superseded. **259a is a hard dependency and carries its own open `[BLOCK]`; this packet has none of its own.**

**Ticket update required before this packet can close ticket 14:** shipping the `ripple` value pulls in three keys outside ticket 14's list — `fuzzy_skin_ripples_per_layer`, `fuzzy_skin_ripple_offset`, `fuzzy_skin_layers_between_ripple_offset`. Reported in the session handoff; a packet may not edit the ticket.

Note for the closing agent: this packet adds five core modules **and** one library crate (`fuzzy-skin-core`), so `docs/07_implementation_status.md`'s module inventory does change. Re-derive at completion time whether that inventory is generated or hand-maintained before editing it.

| docs/07 task ID | Packet step | Primary docs | Expected code surface | OrcaSlicer refs | Context cost | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| — (queue packet, `task_ids: []`) | Steps 1–8 | `docs/01_system_architecture.md` §Claim System, `docs/04_host_scheduler.md` §Claim Resolution (the `wall_generator` subsection), `docs/03_wit_and_manifest.md` §Known claim IDs, `docs/adr/0056-integrated-modules-native-dispatch.md`, `docs/08_coordinate_system.md`, `docs/DEVIATION_LOG.md`, `docs/15_config_keys_reference.md` (generated) | net-new `crates/fuzzy-skin-core/**` + five new `modules/core-modules/{perlin,billow,ridged-multi,voronoi,ripple}-fuzzy-skin/**` trees + `modules/core-modules/fuzzy-skin/{fuzzy-skin.toml,Cargo.toml,src/lib.rs,tests/**}` + `crates/slicer-scheduler/src/execution_plan.rs` + `crates/slicer-runtime/src/run.rs` + root `Cargo.toml` + `crates/slicer-integrated-modules/{Cargo.toml,src/lib.rs}` + `crates/pnp-cli/Cargo.toml` + scheduler integration test arms and one net-new aggregated test file with its `mod` registration | `Feature/FuzzySkin/FuzzySkin.cpp` (`get_noise_module`, `UniformNoise::GetValue`, `random_value`, `fuzzy_polyline`, `fuzzy_extrusion_line`, `fuzzy_polyline_ripple`, `fuzzy_extrusion_line_ripple`, `ripple_phase_shift_rad`, `ripple_anchor_arc_mm`, `collect_merged_fuzzy_regions`, `same_fuzzy_effect`), `PerimeterGenerator.hpp` (`FuzzySkinConfig`), `PrintConfig.cpp` (`PrintConfigDef::init_fff_params`, `s_keys_map_NoiseType`), `LayerRegion.cpp` (`LayerRegion::make_perimeters`) | L (aggregate; no single step above M) | Re-authored under Authoring rules 1–6; **Tier C** (was Tier A). No `[BLOCK]` of its own; gated by 259a's BLOCK-1. libnoise is an external CMake dep, not vendored — the four generators are Rust ports (DIV-2). Re-derive module-count and deviation-ID ledger facts at point of use |
