# Task Map: 292-speed-jerk-xy-emitter

Queue packet from the wayfinder map "Close the OrcaSlicer FFF feature gap"; authored under map ticket 66. Single-task packet (no backlog slice); this crosswalk exists for the S0 structural gate and records the map provenance.

| docs/07 task ID | Packet step | Primary docs | Expected code surface | OrcaSlicer refs | Context cost | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| `TASK-000` (queue packet, `task_ids: []`) | Steps 1–1b–4 | `docs/config/host-keys.toml`; `docs/DEVIATION_LOG.md` (DEV-184); `docs/15_config_keys_reference.md` (generated via `cargo xtask gen-config-docs`, Step 1b); `docs/01_system_architecture.md` (claim-system trigger test) | `crates/slicer-ir/src/resolved_config.rs` + `crates/slicer-gcode/src/emit.rs` (print/travel/JD selection stage + existing-flavor rendering + emitter-side bounds) + `crates/slicer-gcode/tests/speed_p59_jerk_emission_tdd.rs` (new) + `docs/config/host-keys.toml` | `OrcaSlicerDocumented/src/libslic3r/PrintConfig.cpp` (all eight `coFloats` per-nozzle vectors: `default_jerk` 0 / `default_junction_deviation` 0 max 0.3 / five role jerks 9 / `travel_jerk` 12) + `OrcaSlicerDocumented/src/libslic3r/GCode.cpp` (`GCode::_extrude` chain + `GCode::process_layer` first-layer header + `GCode::travel_to` default; two travel overrides + Calib arm differ — DEV-184(d)) + `OrcaSlicerDocumented/src/libslic3r/GCodeWriter.cpp` (`GCodeWriter::set_jerk_xy` + `set_junction_deviation` forms, reused verbatim) + `OrcaSlicerDocumented/src/libslic3r/Print.cpp` (warn-only evidence for DEV-184(a)) | M | P59: Tier B held, owner `crates/slicer-gcode` stands (all canonical reads emission-side in `_extrude`/`travel_to`/`process_layer`; machine-gcode-emit sweep is the wrong seam; no jerk command emits anywhere in tree — reuse the two flavor arms, fork nothing); membership held 8-in, none shed, none returned (all live in canonical, zero-occurrence here); canonical scalarity NOT held (all eight per-nozzle vectors — DEV-184(b) scalar-global, ticket-125 future); defaults ARE identity (`default_jerk` 0 master gate, AC-2 pins byte-identical — inverse of 289's emitting default); Marlin2-only JD + stateless restore (DEV-184(c)); `initial_layer_travel_jerk` not in P59 scope (DEV-184(d) non-borrow). |

Copy costs from `implementation-plan.md`. Split before activation if any row is L or aggregate exceeds M.
