---
status: draft
packet: core-brittle
task_ids:
  - 'core/BRITTLE'
backlog_source: docs/specs/test-quality-remediation-plan.md
depends_on:
  - core-paint
context_cost_estimate: S
copy_note: Generated from the approved core-wave queue row #12; plan wave/item IDs are used under the standing mapping exemption, not TASK-### IDs.
---

# Packet Contract: core-brittle

## Goal

Resolve the three `slicer-core` timing guards through earn-their-keep review, keeping every guard with its correctness witness and recorded Benchy regression rationale unless migration to the existing criterion bench wins on the evidence.

## Scope Boundaries

The test-only edit is limited to the timing tests in `crates/slicer-core/src/algos/mesh_analysis.rs` (`compute_xy_footprint_is_fast_for_thousands_of_disjoint_facets`, absolute 3-second gate plus 1200-facet equality), `crates/slicer-core/src/algos/overhang_annotation.rs` (`annotate_overhangs_is_fast_for_many_stacked_layers`, absolute 1-second gate plus empty-result witness), and `crates/slicer-core/tests/algo_prepass_slice_tdd.rs` (`prepass_slice_caches_bottom_surface_footprint_across_layers`, relative 1.5x cached-vs-uncached ratio gate). A separate implementation step updates only the `core` row in §7 of `docs/specs/test-quality-remediation-plan.md`; production code, thresholds, other core tests, fixtures on disk, registrations, contracts, bench targets, and the census manifest are out of scope.

## Prerequisites and Blockers

- Depends on: `core-paint` (row #11, `generated`; test-only and exports no symbols, APIs, or files consumed here).
- Forward dependency: `core-retire` (`status: draft`) plans `prepass_slice_closing_radius_gate_applies_only_when_positive` in `algo_prepass_slice_tdd.rs` with that exact name and shape; AC-N1 accepts the roster with or without that 7th name (name reconciled with the producer plan).
- Unblocks: `core-cross` (row #13) packet generation only.
- Activation blockers: none for draft generation; parent owns the independent preflight.

## Acceptance Criteria

- **AC-1. Given** `compute_xy_footprint_is_fast_for_thousands_of_disjoint_facets` in `crates/slicer-core/src/algos/mesh_analysis.rs` driving production `compute_xy_footprint` over 1200 disjoint facets, **when** the feature-correct lib substring filter runs, **then** exactly one test executes and passes with the exact input const `FACET_COUNT: usize = 1200`, a line-anchored live `assert_eq!(footprint.len(), FACET_COUNT, ...)` (not a commented-out copy), the `Duration::from_secs(3)` wall-clock guard, and the exact marker line `// KEEP-review (core-brittle): 118s Benchy regression guard; batched union measured 61ms vs 20.6s incremental (338x); 3s threshold keeps ~50x headroom.` all present in the file. Substring filters are used instead of `--exact` because libtest `--exact` requires the full module path and would pin a brittle path. | `set -euo pipefail; mkdir -p target; cargo test -p slicer-core --features host-algos --lib compute_xy_footprint_is_fast_for_thousands_of_disjoint_facets -- --nocapture 2>&1 | tee target/test-output.log >/dev/null; grep -Eq '^test result: ok\. 1 passed; 0 failed;' target/test-output.log; rg -q 'FACET_COUNT: usize = 1200' crates/slicer-core/src/algos/mesh_analysis.rs || { echo 'FAIL: 1200-facet const missing'; exit 1; }; rg -q -U --multiline '^[ \t]*assert_eq!\(\s*footprint\.len\(\),\s*FACET_COUNT' crates/slicer-core/src/algos/mesh_analysis.rs || { echo 'FAIL: live equality assert missing'; exit 1; }; rg -q 'from_secs\(3\)' crates/slicer-core/src/algos/mesh_analysis.rs || { echo 'FAIL: 3s guard missing'; exit 1; }; rg -qF '// KEEP-review (core-brittle): 118s Benchy regression guard; batched union measured 61ms vs 20.6s incremental (338x); 3s threshold keeps ~50x headroom.' crates/slicer-core/src/algos/mesh_analysis.rs || { echo 'FAIL: keep-review marker missing'; exit 1; }; echo 'AC-1 PASS'`
- **AC-2. Given** `annotate_overhangs_is_fast_for_many_stacked_layers` in `crates/slicer-core/src/algos/overhang_annotation.rs` driving production `annotate_overhangs` over 1200 pre-sliced stacked-cube layers, **when** the feature-correct lib substring filter runs, **then** exactly one test executes and passes with the exact input const `CUBE_COUNT: usize = 1200`, a line-anchored live `assert!(result.is_empty(), ...)` (not a commented-out copy), the `Duration::from_secs(1)` wall-clock guard, and the exact marker line `// KEEP-review (core-brittle): O(layers) sweep guard; timed region excludes one-time slicing setup; 1s threshold on 1200 pre-sliced layers.` all present in the file. | `set -euo pipefail; mkdir -p target; cargo test -p slicer-core --features host-algos --lib annotate_overhangs_is_fast_for_many_stacked_layers -- --nocapture 2>&1 | tee target/test-output.log >/dev/null; grep -Eq '^test result: ok\. 1 passed; 0 failed;' target/test-output.log; rg -q 'CUBE_COUNT: usize = 1200' crates/slicer-core/src/algos/overhang_annotation.rs || { echo 'FAIL: 1200-layer const missing'; exit 1; }; rg -q -U --multiline '^[ \t]*assert!\(\s*result\.is_empty\(\)' crates/slicer-core/src/algos/overhang_annotation.rs || { echo 'FAIL: live empty-result assert missing'; exit 1; }; rg -q 'from_secs\(1\)' crates/slicer-core/src/algos/overhang_annotation.rs || { echo 'FAIL: 1s guard missing'; exit 1; }; rg -qF '// KEEP-review (core-brittle): O(layers) sweep guard; timed region excludes one-time slicing setup; 1s threshold on 1200 pre-sliced layers.' crates/slicer-core/src/algos/overhang_annotation.rs || { echo 'FAIL: keep-review marker missing'; exit 1; }; echo 'AC-2 PASS'`
- **AC-3. Given** `prepass_slice_caches_bottom_surface_footprint_across_layers` in the `host-algos`-gated target `algo_prepass_slice_tdd` (`required-features = ["host-algos"]` in `crates/slicer-core/Cargo.toml`, no inner cfg header, so the feature flag is mandatory), **when** the exact feature-correct filter runs, **then** exactly one test executes and passes with a line-anchored live `assert!(cached_elapsed * 3 < uncached_elapsed * 2, ...)` (not a commented-out copy) and the exact marker line `// KEEP-review (core-brittle): per-layer footprint-recomputation guard; cached-vs-uncached ratio self-normalizes across machines; 1.5x minimum (measured ~3.5x debug / ~2.3x release).` both present in the file. | `set -euo pipefail; mkdir -p target; cargo test -p slicer-core --features host-algos --test algo_prepass_slice_tdd -- --exact prepass_slice_caches_bottom_surface_footprint_across_layers 2>&1 | tee target/test-output.log >/dev/null; grep -Eq '^test result: ok\. 1 passed; 0 failed;' target/test-output.log; rg -q -U --multiline '^[ \t]*assert!\(\s*cached_elapsed \* 3 < uncached_elapsed \* 2' crates/slicer-core/tests/algo_prepass_slice_tdd.rs || { echo 'FAIL: live ratio assert missing'; exit 1; }; rg -qF '// KEEP-review (core-brittle): per-layer footprint-recomputation guard; cached-vs-uncached ratio self-normalizes across machines; 1.5x minimum (measured ~3.5x debug / ~2.3x release).' crates/slicer-core/tests/algo_prepass_slice_tdd.rs || { echo 'FAIL: keep-review marker missing'; exit 1; }; rg -qF 'KEEP-review (core-brittle)' crates/slicer-core/tests/algo_prepass_slice_tdd.rs || { echo 'FAIL: keep-review marker missing'; exit 1; }; echo 'AC-3 PASS'`
- **AC-4. Given** the real `docs/specs/test-quality-remediation-plan.md`, **when** the implementation updates only its current §7 `core` row, **then** this read-only predicate passes only when the heading matches `^## 7\. Ledger\b` and the span ends at the next `^## Packet Queue\b`, the six-column header is exact, exactly one `core` row has state `partial` or `partial (parenthesized detail)`, Retired/changed symbols contains the exact token `core-brittle` plus all three real test names, Surviving/new coverage contains all three test names plus the oracle tokens `1200`, `800`, `from_secs(3)`, `from_secs(1)`, and `cached_elapsed`, Validation contains a whitespace-normalized token-boundary occurrence of `cargo test -p slicer-core --features host-algos --test algo_prepass_slice_tdd -- --exact prepass_slice_caches_bottom_surface_footprint_across_layers` with optional trailing flags or a closing code-span backtick allowed, plus the full gate strings `cargo check --workspace --all-targets`, `cargo clippy --workspace --all-targets -- -D warnings`, `cargo xtask check-literals`, and `cargo xtask check-test-quality --report`, and Remaining gap contains `core-cross`, `core-parity`, and `census-target-scope` while no longer containing `core-brittle`; no temporary copy, manufactured row, queue mutation, or synthetic positive is used. | `set -euo pipefail; rg -n '^## 7\. Ledger\b' docs/specs/test-quality-remediation-plan.md >/dev/null; python3 -c 'import re; from pathlib import Path as P; t=P("docs/specs/test-quality-remediation-plan.md").read_text(encoding="utf-8"); m=re.search(r"(?ms)^## 7\. Ledger\b[^\n]*\n(.*?)(?=^## Packet Queue\b)",t); assert m,"missing ledger span"; s=m.group(1); h=[[x.strip() for x in l.strip("|").split("|")] for l in s.splitlines() if l.startswith("| Wave |")]; assert len(h)==1 and h[0]==["Wave","State","Retired/changed symbols","Surviving/new coverage","Validation","Remaining gap"]; r=[l for l in s.splitlines() if re.match(r"^\|\s*core\s*\|",l)]; assert len(r)==1; w,st,ch,su,v,g=[x.strip() for x in r[0].strip("|").split("|")]; q=lambda x,y: re.search(r"(?<![A-Za-z0-9_])"+re.escape(x)+r"(?![A-Za-z0-9_])",y); qb=lambda x,y: re.search(r"(?<![A-Za-z0-9_-])"+re.escape(x)+r"(?![A-Za-z0-9_-])",y); assert w=="core" and re.fullmatch(r"partial(?:\s*\([^()\n]+\))?",st); assert qb("core-brittle",ch); assert all(q(x,ch) for x in ["compute_xy_footprint_is_fast_for_thousands_of_disjoint_facets","annotate_overhangs_is_fast_for_many_stacked_layers","prepass_slice_caches_bottom_surface_footprint_across_layers"]); assert all(q(x,su) for x in ["compute_xy_footprint_is_fast_for_thousands_of_disjoint_facets","annotate_overhangs_is_fast_for_many_stacked_layers","prepass_slice_caches_bottom_surface_footprint_across_layers"]); assert all(q(x,su) for x in ["1200","800","cached_elapsed"]); assert "from_secs(3)" in su and "from_secs(1)" in su; vn=re.sub(r"\s+"," ",v).strip(); own=r"(?<![A-Za-z0-9_-])cargo test -p slicer-core --features host-algos --test algo_prepass_slice_tdd -- --exact prepass_slice_caches_bottom_surface_footprint_across_layers(?=$|\s+-|[\x60])"; assert re.search(own,vn),"validation cell missing representative command"; assert "cargo check --workspace --all-targets" in vn; assert "cargo clippy --workspace --all-targets -- -D warnings" in vn, "clippy gate must carry -- -D warnings"; assert "cargo xtask check-literals" in vn; assert "cargo xtask check-test-quality --report" in vn, "quality gate must carry --report"; assert all(qb(x,g) for x in ["core-cross","core-parity","census-target-scope"]); assert not qb("core-brittle",g),"core-brittle must no longer be listed as an open gap"; print("core-brittle ledger predicate: PASS")'`

## Negative Test Cases

- **AC-N1. Given** that this packet shares `algo_prepass_slice_tdd.rs` with draft `core-retire` (FORWARD-DEP: its planned 7th test `prepass_slice_closing_radius_gate_applies_only_when_positive` may or may not have landed yet) and that the lib timing tests compile to zero under default features, **when** the edits land, **then** the integration file holds the 6 expected test names in file order, optionally followed by exactly that retire 7th name and nothing else, and the bare-feature lib filter for the footprint test reports `0 passed`, proving both the inherited roster and the feature flag are intact. | `set -euo pipefail; mkdir -p target; python3 -c 'import re; from pathlib import Path as P; exp=["slice_at_mid_height_produces_nonempty_polygons","slice_below_mesh_produces_empty_polygons","slice_above_mesh_produces_empty_polygons","unknown_object_returns_error","flat_bridge_enclosure_closing_avoids_round_arc_explosion","prepass_slice_caches_bottom_surface_footprint_across_layers"]; ret="prepass_slice_closing_radius_gate_applies_only_when_positive"; got=re.findall(r"#\[test\]\s*\n\s*fn\s+([A-Za-z0-9_]+)",P("crates/slicer-core/tests/algo_prepass_slice_tdd.rs").read_text(encoding="utf-8")); assert got==exp or got==exp+[ret], f"roster mismatch: {got}"; print("ROSTER-PASS")'; cargo test -p slicer-core --lib compute_xy_footprint_is_fast_for_thousands_of_disjoint_facets -- --nocapture 2>&1 | tee target/test-output.log >/dev/null; grep -Eq '^test result: ok\. 0 passed; 0 failed;' target/test-output.log`

## Verification

AC-1 through AC-3 and AC-N1 are the full acceptance matrix and must all run; AC-4 binds only the ledger row.

- `set -euo pipefail; mkdir -p target; cargo check --workspace --all-targets 2>&1 | tee target/core-brittle-check.log >/dev/null`
- `set -euo pipefail; mkdir -p target; cargo clippy --workspace --all-targets -- -D warnings 2>&1 | tee target/core-brittle-clippy.log >/dev/null`
- `set -euo pipefail; mkdir -p target; cargo test -p slicer-core --features host-algos --test algo_prepass_slice_tdd -- --exact prepass_slice_caches_bottom_surface_footprint_across_layers 2>&1 | tee target/test-output.log >/dev/null; grep -Eq '^test result: ok\. 1 passed; 0 failed;' target/test-output.log`

Cargo **test** commands use `set -euo pipefail`, `mkdir -p target`, `tee target/test-output.log`, and anchored result checks. Workspace check/clippy use the same compact-output wrapper with dedicated logs; they do not overwrite the test log. No workspace suite or guest freshness check is required because this packet is host-side test-only with no guest build input.

## Authoritative Docs

- `docs/specs/test-quality-remediation-plan.md` - §5.1 `BRITTLE` row, §6 feature-correct command/log rules, §7 ledger ownership, Packet Queue row #12, and the row-#11-generated resume pointer; read only those sections.
- `docs/22_test_quality.md` - delegated SUMMARY for earn-their-keep review standard and legitimate weak forms.
- `docs/adr/0064-existing-tests-retire-if-unjustified.md` - delegated SUMMARY for the retire-vs-keep burden of proof.
- `docs/adr/0065-test-quality-gate-with-delayed-enforce-mode.md` - delegated SUMMARY for report-mode closure obligation.
- `docs/spec_packets/core-paint/` - delegated predecessor summary; draft, independently `PREFLIGHT PASS`, exports none.

## Doc Impact Statement (Required)

Specific same-packet doc edits:

- `docs/specs/test-quality-remediation-plan.md` section "§7 Ledger core row" - implementation records this packet's KEEP-review dispositions, all three surviving guard names, exact oracle tokens, own representative validation command, and the remaining core-wave gap including the `census-target-scope` program-level gap. Verification grep: `rg -n '^## 7\. Ledger\b|^\| core \|' docs/specs/test-quality-remediation-plan.md`; AC-4 is the required real-plan content predicate and must read the accumulated row rather than manufacture a positive copy.
- No `docs/<NN>_*` section is added or amended by this packet (guard rationale lives in the test-adjacent marker comments pinned by AC-1 through AC-3). Confirming grep: `rg -l 'KEEP-review \(core-brittle\)' docs/ -g '!**spec_packets**' -g '!**specs**' || echo 'no numbered-doc marker (expected)'`.

No other documentation section, queue row, or packet is edited by this packet.

<!-- snippet: context-discipline -->
## Context Discipline Note

This packet was generated against the context_discipline preamble shared by `spec-packet-generator`, `swarm`, and `spec-review`. Downstream agents implementing or reviewing this packet must:

- treat `design.md`'s code change surface as the authoritative files-in-scope list
- honor `design.md`'s out-of-bounds list — those files must not be loaded directly
- delegate every cargo run and authoritative-doc fact-check
- obey the shared absolute context bands: 120k reading budget with hand-off at 150k (standard); the extended band (240k reading / 300k hard stop) only via swarm's escalation protocol

Aggregate context cost above is the sum of per-step costs in `implementation-plan.md`. If any single step is rated L, the packet must be split before activation (an extended-band run may carry a single L step only when `design.md` justifies why it cannot be split).
