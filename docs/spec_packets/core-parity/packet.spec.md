---
status: draft
packet: core-parity
task_ids:
  - core/PARITY
backlog_source: docs/specs/test-quality-remediation-plan.md
context_cost_estimate: M
copy_note: Emitted copy of the spec-packet-generator template; draft until the independent preflight passes and activation is approved.
---

# Packet Contract: core-parity

## Goal

Add the nine core parity assertions named in plan §5.1 (PARITY row) as test-only additions in `crates/slicer-core/tests/`, preserving every existing exact canonical pin, and record the packet's progress in plan §7's `core` ledger row only.

## Scope Boundaries

All changes are additive, test-only, and confined to nine existing slicer-core integration-test files (one step per file) plus the single §7 plan ledger row. No production behavior, thresholds, or tolerances change; no exact pin is removed or loosened; the census JSON, other packet directories, and all later crate waves are untouched. OrcaSlicer behavior is consulted only through delegated reads (orca-delegation snippet below).

## Prerequisites and Blockers

- Depends on: `core-cross` (row #13, `generated`); its one exported name (`offset_polygons_with_miter_limit_clamps_sharp_miter_corners`, `sdk host_wrappers_tdd.rs`) is consumed as a kept name only — not a dependency.
- Unblocks: the next queued `pending` row (per plan §"Resume and dependency exports"), generation only.
- Activation blockers: none for draft generation; the parent owns the independent preflight and activation approval.

## Acceptance Criteria

State ACs only here; `requirements.md` references their IDs.

- **AC-1. Given** the new test `get_nearest_beding_includes_vertex_at_exact_radius_boundary` in `crates/slicer-core/tests/arachne_beding_propagation_side_table.rs` driving `SkeletalTrapezoidationGraph::get_nearest_beding` (`crates/slicer-core/src/skeletal_trapezoidation/graph.rs`) on a hand-built 3-node chain whose populated neighbours sit at exactly `1000` units (`0.1` mm, `1 unit = 100 nm`) and `2000` units from an unpopulated query vertex, **when** the exact integration-test filter runs with `--features host-algos`, **then** exactly one test executes and passes asserting: radius `1000.0` returns the `1000`-unit neighbour's stored `Beading` (boundary inclusion via `edge_len <= radius_units`), radius `999.5` returns `None` (just outside), radius `0.0` on the unpopulated query vertex returns `None`, and the zero-radius lookup on the populated vertex returns its own stored `Beading`. | `set -euo pipefail; mkdir -p target; cargo test -p slicer-core --features host-algos --test arachne_beding_propagation_side_table -- --exact get_nearest_beding_includes_vertex_at_exact_radius_boundary 2>&1 | tee target/test-output.log >/dev/null; grep -Eq '^test result: ok\. 1 passed; 0 failed;' target/test-output.log`
- **AC-2. Given** the new test `apply_transitions_diagonal_source_mid_r_and_foot_sentinels` in `crates/slicer-core/tests/arachne_construction_apply_transitions_mirror_fix.rs` driving public `apply_transitions` on a hand-built twin pair whose source edge is diagonal (non-horizontal, so linear interpolation diverges from a true perpendicular projection), **when** the exact integration-test filter runs with `--features host-algos`, **then** exactly one test executes and passes asserting: the shared mid node's `distance_to_boundary` equals `mid_r` (`750_000.0` units = `75` mm at `UNITS_PER_MM = 10_000`; the perpendicular-foot radius `get_transition_thickness(lower_bc) / 2`), the two boundary-foot nodes (one per side) each carry `distance_to_boundary == 0.0` and `bead_count == None`, the two foot positions coincide across edge and twin sides, and the mid node's position lies on the input edge's line at the interpolated pos — with a comment documenting the acknowledged non-retained-source-projection divergence (per `insert_node`'s own doc in `crates/slicer-core/src/skeletal_trapezoidation/propagation.rs`), never asserting the unprojected true foot position. | `set -euo pipefail; mkdir -p target; cargo test -p slicer-core --features host-algos --test arachne_construction_apply_transitions_mirror_fix -- --exact apply_transitions_diagonal_source_mid_r_and_foot_sentinels 2>&1 | tee target/test-output.log >/dev/null; grep -Eq '^test result: ok\. 1 passed; 0 failed;' target/test-output.log`
- **AC-3. Given** the new test `apply_transitions_split_topology_exact_counts_and_cross_twin_patch` in `crates/slicer-core/tests/arachne_construction_insert_node_rib_split.rs` driving public `apply_transitions` on the existing `make_split_target_graph` fixture, **when** the exact integration-test filter runs with `--features host-algos`, **then** exactly one test executes and passes asserting exact split topology: exactly `3` new vertices (one shared mid node with `bead_count == Some(lower_bc)` and `distance_to_boundary == mid_r`; two boundary-foot nodes with `distance_to_boundary == 0.0` and `bead_count == None`), exactly `6` new edges (`4` `EdgeType::EXTRA_VD` rib edges forming two rib pairs plus `2` `NORMAL` second fragments, one per side), the cross-twin twin-links intact after the split, and — replacing the current `let _ = max_deviation_x; // placeholder` line in `apply_transitions_split_position_coincides_on_both_sides` — every new vertex's `position.x` within `1` unit of `5 mm`; the existing tests at lines 160, 219, 250, and 297 keep their exact assertions. | `set -euo pipefail; mkdir -p target; cargo test -p slicer-core --features host-algos --test arachne_construction_insert_node_rib_split -- --exact apply_transitions_split_topology_exact_counts_and_cross_twin_patch 2>&1 | tee target/test-output.log >/dev/null; grep -Eq '^test result: ok\. 1 passed; 0 failed;' target/test-output.log`
- **AC-4. Given** the new test `f5_invariant_node_distances_match_rib_geometry_and_boundary` in `crates/slicer-core/tests/arachne_construction_node_distance_perp_foot.rs` building the existing `l_shape` graph via public `SkeletalTrapezoidationGraph::from_polygons`, **when** the exact integration-test filter runs with `--features host-algos`, **then** exactly one test executes and passes asserting, per vertex, the F5-fix semantics from `crates/slicer-core/src/skeletal_trapezoidation/graph.rs`: `distance_to_boundary == 0.0` only for boundary/rib-foot nodes lying on a polygon segment (point-to-segment distance `<= 1` unit), no `NaN` and nothing below `0.0` on the L-shape, and for every `EXTRA_VD` rib pair the spine endpoint's distance equals the rib's geometric length computed independently from the two endpoint positions — while the existing locks `f5_invariant_unribbed_node_distances_are_nonnegative` (line 117), `f5_invariant_l_shape_has_rib_foot_nodes` (line 147), and `f5_invariant_unribbed_node_distance_within_input_bbox` (line 184) keep their exact assertions. | `set -euo pipefail; mkdir -p target; cargo test -p slicer-core --features host-algos --test arachne_construction_node_distance_perp_foot -- --exact f5_invariant_node_distances_match_rib_geometry_and_boundary 2>&1 | tee target/test-output.log >/dev/null; grep -Eq '^test result: ok\. 1 passed; 0 failed;' target/test-output.log`
- **AC-5. Given** the two new tests `dumbbell_wide_gap_not_dissolved_pins_exact_ring_topology` and `dumbbell_narrow_gap_dissolves_to_single_closed_ring` in `crates/slicer-core/tests/arachne_filter_noncentral_regions.rs`, both driving `run_arachne_pipeline` with `ArachneParams::default()` on the existing `dumbbell_polygon` fixture and on a narrow-gap variant whose central-region separation is `<= 0.4` mm, **when** each exact integration-test filter runs with `--features host-algos`, **then** each executes exactly one test and passes: the wide-gap (existing `dumbbell_polygon`, separation `1.5` mm) test pins the measured deterministic inset-0 topology (exact line count and per-line closed/junction-count baseline) with the documented canonical claim that canonical `filterNoncentralRegions` does NOT dissolve the `> max_dist` gap, and the narrow-gap variant (separation `<= 0.4` mm) test asserts canonical dissolution — the inset-0 output is exactly one closed ring (`is_closed == true`) — with its measured junction count pinned. | `set -euo pipefail; mkdir -p target; for t in dumbbell_wide_gap_not_dissolved_pins_exact_ring_topology dumbbell_narrow_gap_dissolves_to_single_closed_ring; do cargo test -p slicer-core --features host-algos --test arachne_filter_noncentral_regions -- --exact "$t" 2>&1 | tee target/test-output.log >/dev/null; grep -Eq '^test result: ok\. 1 passed; 0 failed;' target/test-output.log || { echo "FAIL: $t"; exit 1; }; done; echo 'AC-5 PASS'`
- **AC-6. Given** the new test `limited_inserts_single_centre_sentinel_at_cap_boundary` in `crates/slicer-core/tests/beading/limited.rs` calling `compute(24000.0, 6)` and `compute_and_strip(24000.0, 6)` on a `LimitedBeadingStrategy` with the fixture's exact parent parameters (`optimal_width 4000`, `default_transition_length 400`, `transition_filter_dist 1000`, `distribution_count 3`, `wall_transition_angle 0.17453292519943295`) and `max_bead_count 6` (the at-cap boundary case `bead_count == max_bead_count`, absent from the current fixture cases), **when** the exact integration-test filter runs with `--features host-algos`, **then** exactly one test executes and passes asserting the odd-cap shape: `bead_widths == [4000.0, 4000.0, 4000.0, 0.0, 4000.0, 4000.0, 4000.0]`, `toolpath_locations == [2000.0, 6000.0, 10000.0, 12000.0, 14000.0, 18000.0, 22000.0]` (single centre sentinel at index `max_bead_count / 2 == 3`, location = innermost bead's inner edge `10000 + 2000`), `bead_widths.len() == 7` (the odd total), `total_thickness == 24000.0`, `left_over == 0.0`, and `compute_and_strip` returns exactly the six uniform `4000.0`-wide beads — the fixture JSON and all existing tests stay untouched. | `set -euo pipefail; mkdir -p target; cargo test -p slicer-core --features host-algos --test beading_limited -- --exact limited_inserts_single_centre_sentinel_at_cap_boundary 2>&1 | tee target/test-output.log >/dev/null; grep -Eq '^test result: ok\. 1 passed; 0 failed;' target/test-output.log`
- **AC-7. Given** the new test `chain_junctions_land_at_documented_interpolated_positions` in `crates/slicer-core/tests/arachne_stitch_chain_junctions_t_to_fix.rs` running `generate_toolpaths` on the existing `make_f3_target_graph` fixture with `SymmetricBeadingStrategy`, **when** the exact integration-test filter runs with `--features host-algos`, **then** exactly one test executes and passes asserting full chain reachability with exact positions: `xs[0] == 1.25` mm and `xs[2] == 8.75` mm (the interpolated positions documented in the file's own `chain_traverses_v0_v1_v2_in_x_order_with_sane_span` doc comment, `t = (1.5 - 3.0) / (1.0 - 3.0) = 0.75` along each half-edge), `xs[1]` pinned to the measured value strictly between `xs[0]` and `xs[2]` (the merge of e0's `to` and rib_back's `from` at v1's bead-0 radius), all three junctions' `y == 0.0`, and `line.junctions.len() == 3`; the count lock `f3_invariant_chain_has_one_junction_per_endpoint_at_shared_vertex` (line 235) and the traversal lock (line 309) keep their exact assertions. | `set -euo pipefail; mkdir -p target; cargo test -p slicer-core --features host-algos --test arachne_stitch_chain_junctions_t_to_fix -- --exact chain_junctions_land_at_documented_interpolated_positions 2>&1 | tee target/test-output.log >/dev/null; grep -Eq '^test result: ok\. 1 passed; 0 failed;' target/test-output.log`
- **AC-8. Given** the new test `arachne_params_absent_keys_fall_back_to_defaults_per_key` in `crates/slicer-core/tests/arachne_pipeline.rs` asserting the module-fallback contract locally (no Orca counterpart; the guest's `arachne_params_from_config` cannot be called from a slicer-core integration test — established by the existing `arachne_params_defaults_when_keys_absent` doc comment; committed reading: §5.1 "module fallback" means this local fallback contract, NOT a WASM module fallback table), **when** the exact integration-test filter runs with `--features host-algos`, **then** exactly one test executes and passes asserting, on an empty `ConfigView`, that every wired key read by the module falls back to exactly the `ArachneParams::default()` field value: `min_central_distance → 0.0`, `min_width → 0.4`, `min_bead_width → 0.4`, `wall_transition_length → 0.4`, `wall_transition_angle → 10.0_f64.to_radians()`, `initial_layer_min_bead_width → 0.34`, `outer_wall_offset → 0.0` (the seven wired float keys, including the currently un-checked `min_bead_width`), plus `detect_thin_wall → print_thin_walls == false`; the existing `arachne_params_defaults_when_keys_absent` test body is not modified. | `set -euo pipefail; mkdir -p target; cargo test -p slicer-core --features host-algos --test arachne_pipeline -- --exact arachne_params_absent_keys_fall_back_to_defaults_per_key 2>&1 | tee target/test-output.log >/dev/null; grep -Eq '^test result: ok\. 1 passed; 0 failed;' target/test-output.log`
- **AC-9. Given** the new test `canonical_remove_small_first_keeps_line_simplify_first_drops_it` in `crates/slicer-core/tests/arachne_postprocess_order.rs` using an odd open `ExtrusionLine` with minimum junction width `0.4` (removal threshold `0.5 * 0.4 = 0.2` mm) whose out-and-back micro-segment polyline length is `>= 0.2` mm while its chord (post-simplification) length is `< 0.2` mm, **when** the exact integration-test filter runs with `--features host-algos`, **then** exactly one test executes and passes asserting both exact outcomes of the divergence fixture: canonical order — `remove_small_lines(line, 0.5, 0.4, false, false)` first — keeps the line (`len == 1`), while the old order — `simplify_toolpaths` first with the `ArachneParams::default()` gates `0.0025` / `0.000025` / `2e-6`, then `remove_small_lines` — drops it (`is_empty()`), proving the order swap (`stitch → remove_small → separate_out_inner_contour → simplify → remove_empty`) is observable; the file header's canonical-order pin (removal before simplification, per `WallToolPaths::generate`'s post-process call sequence) is preserved untouched, and `pipeline_smoke_after_order_swap` (line 52) keeps its exact assertion. | `set -euo pipefail; mkdir -p target; cargo test -p slicer-core --features host-algos --test arachne_postprocess_order -- --exact canonical_remove_small_first_keeps_line_simplify_first_drops_it 2>&1 | tee target/test-output.log >/dev/null; grep -Eq '^test result: ok\. 1 passed; 0 failed;' target/test-output.log`
- **AC-10. Given** the real `docs/specs/test-quality-remediation-plan.md`, **when** the implementation updates only its current §7 `core` row (no other plan section, no `Packet Queue` mutation, no census touch), **then** this read-only predicate passes only when the §7 heading matches `^## 7\. Ledger\b` and the span ends at the next `^## Packet Queue\b`, the six-column ledger header is exact, exactly one `core` row has state `partial` or `partial (parenthesized detail)`, its Surviving/new coverage cell contains all ten new test names plus the oracle tokens `exact_radius_boundary`, `mid_r`, `cross_twin`, `single_closed_ring`, `centre_sentinel`, `1.25mm`, `8.75mm`, `0.34`, `unwrap_or`, and `remove_small`, its Validation cell contains a whitespace-normalized token-boundary occurrence of `cargo test -p slicer-core --features host-algos --test arachne_postprocess_order` with optional trailing flags or a closing code-span backtick allowed, plus the four gate tokens `cargo check --workspace --all-targets`, `cargo clippy --workspace --all-targets -- -D warnings`, `cargo xtask check-literals`, and `cargo xtask check-test-quality --report`, its Retired/changed symbols cell contains none of the ten new test names (additive packet — nothing retired), and Remaining gap no longer contains `core-parity`; no temporary copy, manufactured row, or synthetic positive is used. | `set -euo pipefail; rg -q '^## 7\. Ledger\b' docs/specs/test-quality-remediation-plan.md; python3 -c 'import re; from pathlib import Path as P; t=P("docs/specs/test-quality-remediation-plan.md").read_text(encoding="utf-8"); m=re.search(r"(?ms)^## 7\. Ledger\b[^\n]*\n(.*?)(?=^## Packet Queue\b)",t); assert m,"missing ledger span"; s=m.group(1); h=[[x.strip() for x in l.strip("|").split("|")] for l in s.splitlines() if l.startswith("| Wave |")]; assert len(h)==1 and h[0]==["Wave","State","Retired/changed symbols","Surviving/new coverage","Validation","Remaining gap"]; rows=[l for l in s.splitlines() if re.match(r"^\|\s*core\s*\|",l)]; assert len(rows)==1,"exactly one core row"; ok=False
for r in rows:
 w,st,ch,su,v,g=[x.strip() for x in r.strip("|").split("|")]; q=lambda x,y: re.search(r"(?<![A-Za-z0-9_])"+re.escape(x)+r"(?![A-Za-z0-9_])",y); n="get_nearest_beding_includes_vertex_at_exact_radius_boundary apply_transitions_diagonal_source_mid_r_and_foot_sentinels apply_transitions_split_topology_exact_counts_and_cross_twin_patch f5_invariant_node_distances_match_rib_geometry_and_boundary dumbbell_wide_gap_not_dissolved_pins_exact_ring_topology dumbbell_narrow_gap_dissolves_to_single_closed_ring limited_inserts_single_centre_sentinel_at_cap_boundary chain_junctions_land_at_documented_interpolated_positions arachne_params_absent_keys_fall_back_to_defaults_per_key canonical_remove_small_first_keeps_line_simplify_first_drops_it".split(); e="exact_radius_boundary mid_r cross_twin single_closed_ring centre_sentinel 1.25mm 8.75mm 0.34 unwrap_or remove_small".split(); vn=re.sub(r"\s+"," ",v).strip(); own=r"(?<![A-Za-z0-9_])cargo test -p slicer-core --features host-algos --test arachne_postprocess_order(?=$|[\s;&|\x60])"; z=re.sub(r"\s+","",su); ok = w=="core" and re.fullmatch(r"partial(?:\s*\([^()\n]+\))?",st) and all(q(x,ch)==None for x in n) and all(q(x,z) for x in n) and all(q(x,z) for x in e) and re.search(own,vn) and "cargo check --workspace --all-targets" in vn and "cargo clippy --workspace --all-targets -- -D warnings" in vn and "cargo xtask check-literals" in vn and "cargo xtask check-test-quality --report" in vn and not q("core-parity",g)
 if ok: break
assert ok,"ledger predicate: no row satisfies the core-parity contract"; print("core ledger predicate: PASS")'`

## Negative Test Cases

- **AC-N1. Given** the arachne test files are auto-discovered integration targets gated in-file by `#![cfg(feature = "host-algos")]` (verified: each of the eight arachne `*_tdd`-style parity files in this packet carries the gate; `beading_limited` has an explicit `[[test]]` stanza without `required-features`), **when** the exact new AC-7 filter runs without `--features host-algos`, **then** it reports `0 passed` (the silent-green hazard), proving the flag in AC-1 through AC-9 is mandatory rather than decorative. | `set -euo pipefail; mkdir -p target; cargo test -p slicer-core --test arachne_stitch_chain_junctions_t_to_fix -- --exact chain_junctions_land_at_documented_interpolated_positions 2>&1 | tee target/test-output.log >/dev/null; grep -Eq '^test result: ok\. 0 passed; 0 failed;' target/test-output.log`
- **AC-N2. Given** the packet's additive-only contract, **when** a file-scope sweep runs over the tree, **then** (a) each of the ten new test names resolves to exactly one file: `rg -l '^fn <name>\s*\(' crates/slicer-core/tests` returns exactly the assigned file, and the names never appear in `crates/slicer-core/src/` (rejects production edits), and (b) every pre-existing test-name anchor in the nine touched files still resolves (`rg -n '^fn (get_nearest_beding_respects_0_1mm_radius_contract|get_nearest_beding_zero_radius_returns_self_only|get_nearest_beding_distance_upper_bound|apply_transitions_new_vertex_position_is_perpendicular_foot|apply_transitions_split_position_coincides_on_both_sides|f5_invariant_unribbed_node_distances_are_nonnegative|f5_invariant_l_shape_has_rib_foot_nodes|f5_invariant_unribbed_node_distance_within_input_bbox|dumbbell_single_central_region_inset0_ring_pair|limited_inserts_sentinels_at_cap|limited_raw_compute_retains_sentinels|f3_invariant_chain_has_one_junction_per_endpoint_at_shared_vertex|chain_traverses_v0_v1_v2_in_x_order_with_sane_span|arachne_params_defaults_when_keys_absent|pipeline_smoke_after_order_swap|remove_small_before_simplify_short_odd_line_removed)\s*\(' crates/slicer-core/tests` returns all sixteen lines — rejection of exact-pin removal. | `set -euo pipefail; python3 -c 'import re, subprocess
names="get_nearest_beding_includes_vertex_at_exact_radius_boundary apply_transitions_diagonal_source_mid_r_and_foot_sentinels apply_transitions_split_topology_exact_counts_and_cross_twin_patch f5_invariant_node_distances_match_rib_geometry_and_boundary dumbbell_wide_gap_not_dissolved_pins_exact_ring_topology dumbbell_narrow_gap_dissolves_to_single_closed_ring limited_inserts_single_centre_sentinel_at_cap_boundary chain_junctions_land_at_documented_interpolated_positions arachne_params_absent_keys_fall_back_to_defaults_per_key canonical_remove_small_first_keeps_line_simplify_first_drops_it".split(); home={"get_nearest_beding_includes_vertex_at_exact_radius_boundary":"arachne_beding_propagation_side_table.rs","apply_transitions_diagonal_source_mid_r_and_foot_sentinels":"arachne_construction_apply_transitions_mirror_fix.rs","apply_transitions_split_topology_exact_counts_and_cross_twin_patch":"arachne_construction_insert_node_rib_split.rs","f5_invariant_node_distances_match_rib_geometry_and_boundary":"arachne_construction_node_distance_perp_foot.rs","dumbbell_wide_gap_not_dissolved_pins_exact_ring_topology":"arachne_filter_noncentral_regions.rs","dumbbell_narrow_gap_dissolves_to_single_closed_ring":"arachne_filter_noncentral_regions.rs","limited_inserts_single_centre_sentinel_at_cap_boundary":"limited.rs","chain_junctions_land_at_documented_interpolated_positions":"arachne_stitch_chain_junctions_t_to_fix.rs","arachne_params_absent_keys_fall_back_to_defaults_per_key":"arachne_pipeline.rs","canonical_remove_small_first_keeps_line_simplify_first_drops_it":"arachne_postprocess_order.rs"}
for n in names:
 out=subprocess.run(["rg","-l",r"^fn "+n+r"\s*\(","crates/slicer-core/tests"],capture_output=True,text=True).stdout.split()
 assert len(out)==1 and out[0].endswith("tests/"+home[n]), f"name {n} must resolve to exactly {home[n]}, got {out}"
assert subprocess.run(["rg","-l",r"fn ("+ "|".join(names) + r")\s*\(","crates/slicer-core/src"],capture_output=True,text=True).stdout.strip()=="", "new names must not appear in production src"
old="get_nearest_beding_respects_0_1mm_radius_contract get_nearest_beding_zero_radius_returns_self_only get_nearest_beding_distance_upper_bound apply_transitions_new_vertex_position_is_perpendicular_foot apply_transitions_split_position_coincides_on_both_sides f5_invariant_unribbed_node_distances_are_nonnegative f5_invariant_l_shape_has_rib_foot_nodes f5_invariant_unribbed_node_distance_within_input_bbox dumbbell_single_central_region_inset0_ring_pair limited_inserts_sentinels_at_cap limited_raw_compute_retains_sentinels f3_invariant_chain_has_one_junction_per_endpoint_at_shared_vertex chain_traverses_v0_v1_v2_in_x_order_with_sane_span arachne_params_defaults_when_keys_absent pipeline_smoke_after_order_swap remove_small_before_simplify_short_odd_line_removed".split(); out=subprocess.run(["rg","-n",r"^fn ("+ "|".join(old) + r")\s*\(","crates/slicer-core/tests"],capture_output=True,text=True).stdout
assert len(old)==len([l for l in out.splitlines() if l]), f"all 16 pre-existing anchors must resolve, got:\n{out}"; print("AC-N2 PASS")'`

## Verification

AC-1 through AC-10 and AC-N1/AC-N2 are the full acceptance matrix and must all run; AC-10 binds only the ledger row.

- `set -euo pipefail; mkdir -p target; cargo check --workspace --all-targets 2>&1 | tee target/core-parity-check.log >/dev/null`
- `set -euo pipefail; mkdir -p target; cargo clippy --workspace --all-targets -- -D warnings 2>&1 | tee target/core-parity-clippy.log >/dev/null`
- `cargo xtask check-literals` and `cargo xtask check-test-quality --report` (report mode; fix or waive findings in the touched test code with `// test-quality:` waivers naming the protected surface) — full matrix in `requirements.md` §Verification Commands.

## Authoritative Docs

- `docs/specs/test-quality-remediation-plan.md` - ranged reads only: §5.1 PARITY row (lines 155-166), §6 patterns (lines 269-289), §7 ledger (lines 291-311), `Packet Queue` (lines 312-352); never the full 767-line file.
- `docs/22_test_quality.md` - delegated summary; the gate this remediation serves (enforce mode report until ADR-0065 flips it).
- `docs/21_data_defaults_and_fixtures.md` - direct range read of the struct-literal FRU/churn rule (new test literals of watched types must use `..` rest or an `// exhaustive:` waiver).

## Doc Impact Statement (Required)

- **`docs/specs/test-quality-remediation-plan.md`** section §7 `Ledger` `core` row - implementation records this packet's additive progress: ten new test names, oracle tokens, the representative validation command, and `core-parity` leaving Remaining gap. Verification grep: `rg -n '^## 7\. Ledger\b|^\| core \|' docs/specs/test-quality-remediation-plan.md | tail -5`; AC-10 is the required real-plan content predicate and must read the accumulated row rather than manufacture a positive copy.

<!-- snippet: orca-delegation -->
## OrcaSlicer Reference Obligations

All OrcaSlicer reads MUST be delegated to a sub-agent. Never load `OrcaSlicerDocumented/` into the implementer's own context. Dispatch contract: return `LOCATIONS` (file:line + 1-line context, ≤ 20 entries) or `SUMMARY` (≤ 200 words, no code unless asked). Code snippets in returns are capped at 30 lines.

Canonical upstream pinned: `https://github.com/OrcaSlicer/OrcaSlicer` at commit `40eab797c6a60a5949c0f92d00798da414c4b44a` (main, 2026-08-04); every path below resolves at that revision. Cite file + function, never line numbers.

Files to inspect for this packet:

- `https://github.com/OrcaSlicer/OrcaSlicer/blob/40eab797c6a60a5949c0f92d00798da414c4b44a/src/libslic3r/Arachne/SkeletalTrapezoidation.cpp` — `getNearestBeading` radius-inclusive nearest lookup and `connectJunctions` incident-edge seeding (items 1 and 7); `filterNoncentralRegions` gap-dissolve rule (item 5).
- `https://github.com/OrcaSlicer/OrcaSlicer/blob/40eab797c6a60a5949c0f92d00798da414c4b44a/src/libslic3r/Arachne/SkeletalTrapezoidation.hpp` — `BeadingPropagation` side-table struct (item 1).
- `https://github.com/OrcaSlicer/OrcaSlicer/blob/40eab797c6a60a5949c0f92d00798da414c4b44a/src/libslic3r/Arachne/BeadingStrategy/BeadingStrategy.hpp` — `getTransitionThickness` (items 2/3) and `getNonlinearThicknesses` (item 3).
- `https://github.com/OrcaSlicer/OrcaSlicer/blob/40eab797c6a60a5949c0f92d00798da414c4b44a/src/libslic3r/Arachne/BeadingStrategy/LimitedBeadingStrategy.cpp` — `compute`'s odd-centre cap-boundary branch (item 6).
- `https://github.com/OrcaSlicer/OrcaSlicer/blob/40eab797c6a60a5949c0f92d00798da414c4b44a/src/libslic3r/Arachne/SkeletalTrapezoidationGraph.cpp` — `makeRib` perpendicular foot (item 4).
- `https://github.com/OrcaSlicer/OrcaSlicer/blob/40eab797c6a60a5949c0f92d00798da414c4b44a/src/libslic3r/Arachne/SkeletalTrapezoidationJoint.hpp` — `distance_to_boundary` semantics (item 4).
- `https://github.com/OrcaSlicer/OrcaSlicer/blob/40eab797c6a60a5949c0f92d00798da414c4b44a/src/slic3r/GUI/PostProcessor.cpp` — `run_post_process_scripts` sequential execution (item 9).
- `https://github.com/OrcaSlicer/OrcaSlicer/blob/40eab797c6a60a5949c0f92d00798da414c4b44a/src/libslic3r/Arachne/WallToolPaths.cpp` — `generate`'s post-process call sequence ordering removal before simplification (item 9; the local test header already pins this — preserve it).
- Item 8 (module fallback) has NO Orca counterpart — local fallback behavior; delegated confirmation reads only if needed, never to invent parity.

<!-- snippet: context-discipline -->
## Context Discipline Note

This packet was generated against the context_discipline preamble shared by `spec-packet-generator`, `swarm`, and `spec-review`. Downstream agents implementing or reviewing this packet must:

- treat `design.md`'s code change surface as the authoritative files-in-scope list
- honor `design.md`'s out-of-bounds list — those files must not be loaded directly
- delegate every cargo run and authoritative-doc fact-check
- obey the shared absolute context bands: 120k reading budget with hand-off at 150k (standard); the extended band (240k reading / 300k hard stop) only via swarm's escalation protocol

Aggregate context cost above is the sum of per-step costs in `implementation-plan.md`. If any single step is rated L, the packet must be split before activation (an extended-band run may carry a single L step only when `design.md` justifies why it cannot be split).
