# Task Map: 293-speed-other-layers-emitter

Queue packet from the wayfinder map "Close the OrcaSlicer FFF feature gap"; authored under map ticket 67. Single-task packet (no backlog slice); this crosswalk exists for the S0 structural gate and records the map provenance.

| docs/07 task ID | Packet step | Primary docs | Expected code surface | OrcaSlicer refs | Context cost | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| `TASK-000` (queue packet, `task_ids: []`) | Steps 1–1b–4 | `docs/config/host-keys.toml`; `docs/DEVIATION_LOG.md` (DEV-185); `docs/15_config_keys_reference.md` (generated via `cargo xtask gen-config-docs`, Step 1b); `docs/01_system_architecture.md` (claim-system trigger test) | `crates/slicer-ir/src/feedrate.rs` (SPEED_KEYS row + field + default + aligned meta/bounds) + `crates/slicer-ir/src/resolved_config.rs` (twin + pair + `to_config_map` arm) + `crates/slicer-gcode/src/emit.rs` (internal-solid reseat + small-perimeter gate + percent resolution + emitter-side bounds) + `crates/slicer-gcode/tests/speed_p60_other_layers_emission_tdd.rs` (new) + `docs/config/host-keys.toml` | `OrcaSlicerDocumented/src/libslic3r/PrintConfig.cpp` (both keys' types/defaults/bounds + threshold: internal-solid coFloats 100 min 1; small-perimeter coFloatsOrPercents 50%-percent over outer min 1; threshold coFloats 0 min 0; all per-nozzle nullable vectors) + `OrcaSlicerDocumented/src/libslic3r/GCode.cpp` (erSolidInfill speed arm + minimum-cross-section guard reads + `extrude_loop` gate shape with three value arms + perimeter-only application) + `OrcaSlicerDocumented/src/libslic3r/libslic3r.h` (`SMALL_PERIMETER_LENGTH` `* 2 * PI` circumference conversion) + `OrcaSlicerDocumented/src/libslic3r/Fill/Fill.cpp` (`role_speed` erSolidInfill arm — no-dual-implementation evidence) + `OrcaSlicerDocumented/src/libslic3r/Print.cpp` (warn-only evidence for DEV-185(a)) | M | P60: Tier B held, owner `crates/slicer-gcode` stands (all canonical reads emission-side in `GCode` speed selection / `extrude_loop`; machine-gcode-emit sweep is the wrong seam; rectilinear parse is dead input, not a decision point); membership held 2-in, none shed, none returned (both live in canonical, zero-occurrence here); canonical scalarity NOT held (both per-nozzle vectors — DEV-185(b) scalar-global, ticket-125 future); defaults ARE near-identity (internal-solid 100 == shadowed sparse 100, threshold 0 silences the gate — AC-2 pins F-identical + exactly +2 config lines); host twin for the threshold is a separate input from the module width twin (DEV-185(c)); `speed == -1` entry has no port analogue (recorded, not stubbed). |

Copy costs from `implementation-plan.md`. Split before activation if any row is L or aggregate exceeds M.
