# Task Map: support-support-keys

Use this crosswalk when a packet spans more than one task ID, reopens prior work, or supersedes an earlier packet. **This packet emits the template's own skip clause:** it is a single-coherent-slice queue packet with `task_ids: []` (queue precedent — packets 234a, 253–264), so the `docs/07` crosswalk is N-A. Implementation is recorded against wayfinder ticket 20 (`docs/specs/orca-feature-gap/issues/20-author-packet-p13-support-support-support-planner.md`).

This revision **overwrites** the packet's prior Tier A draft in place (number 265 and slug `support-support-keys` retained, with explicit user approval) under the map's Authoring rules 1–7. The prior draft wired one key, verified six as already-live, and declared five "declared-with-gap"; rule 1 prohibits that disposition and rule 5 removes the plumbing exemption it relied on. No predecessor packet is superseded.

**Reopened prior work.** Packet `238a-support-pattern-config-keys` (`status: implemented`) declared and transported `support_bottom_z_distance` and recorded a "Bottom-z scope split" deferring the consuming semantics to 238b/238c. Both of those are also `implemented` and neither consumes the key, so the semantic half was orphaned. This packet supplies it and closes `docs/specs/support-parity-gap-register.md` row **G-05**. Inspect 238a only through a bounded `SUMMARY` dispatch if more context is needed; do not edit that packet directory.

**Keys handed to other owners.** `raft_first_layer_expansion` → packet `240-support-raft` (which already lists it by name). `support_style` → sibling-plan row 7 `organic-tree-engine` / TASK-441, which has no packet directory yet; its number must be derived from disk when it is authored.

Note for the closing agent: this packet adds no module, so `docs/07_implementation_status.md`'s module inventory does not change. It does move five keys from declared-unread to live, so `docs/15_config_keys_reference.md` must be regenerated.

| docs/07 task ID | Packet step | Primary docs | Expected code surface | OrcaSlicer refs | Context cost | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| — (queue packet, `task_ids: []`) | Steps 1–8 | `docs/00_project_overview.md`, `docs/01_system_architecture.md` §Claim System, `docs/04_host_scheduler.md` §Claim Resolution, `docs/08_coordinate_system.md`, `docs/21_data_defaults_and_fixtures.md`, `docs/15_config_keys_reference.md` (generated), `docs/specs/support-parity-gap-register.md` (rows G-05, G-09, RC-11) | `crates/slicer-core/{src/algos/overhang_annotation.rs,Cargo.toml,tests/support_critical_and_small_overhang_tdd.rs,tests/support_overhang_detection_tdd.rs}` + `crates/slicer-runtime/src/builtins/support_analysis_producer.rs` + `modules/core-modules/traditional-support-planner/{src/lib.rs,traditional-support-planner.toml,Cargo.toml,tests/support_gap_keys_tdd.rs}` + `modules/core-modules/tree-support-planner/{src/lib.rs,tree-support-planner.toml,Cargo.toml,tests/support_gap_keys_tdd.rs}` + `crates/slicer-scheduler/tests/integration/config_bounds_enforcement_tdd.rs` + one generated doc | `Support/SupportMaterial.cpp` (`detect_overhangs`, `PrintObjectSupportMaterial::top_contact_layers`), `Support/TreeSupport.cpp` (`detect_overhangs`, `draw_circles`, `generate_toolpaths`), `Support/TreeSupport3D.cpp` (`generate_overhangs`), `Support/SupportParameters.hpp` (`SupportParameters::SupportParameters`), `Support/TreeSupportCommon.hpp` (`TreeSupportSettings::TreeSupportSettings`), `Slicing.cpp` (`SlicingParameters::create_from_config`), `GCode.cpp` (`collect_layers_to_print` — cited as deliberately not borrowed, DIV-1) | L (aggregate; no single step above M) | Re-authored under the map's Authoring rules 1–7; **Tier B** (was Tier A). 10 keys in packet (5 live, 5 built), 2 returned, 0 dead-in-canonical. `SupportContactParams` is a watched struct-literal type — Step 1 owns the blast radius. `slicer-core` support tests require `--features host-algos`. Re-derive the literal count and every other ledger fact at point of use |
