# Task Map: brim-type-and-object-gap

Use this crosswalk when a packet spans more than one task ID, reopens prior work, or supersedes an earlier packet. **This packet emits the template's own skip clause:** it is a single-coherent-slice queue packet with `task_ids: []` (queue precedent — packets 234a, 253–265), so the `docs/07` crosswalk is N-A. Implementation is recorded against wayfinder ticket 12 (`docs/specs/orca-feature-gap/issues/12-author-packet-p05-others-brim-skirt-brim.md`).

**This is a split, not an overwrite.** The prior `docs/spec_packets/257-brim-type-and-brim-keys/` directory is split into `257a-brim-type-and-object-gap` (this packet) and `257b-brim-ears`, with explicit user approval, under the 210a/210b letter-suffix precedent and the map's Authoring rules 1–7. The prior draft declared all five ticket-12 keys and dispositioned four "declared-with-gap" — prohibited by rule 1 — and claimed `brim_type`'s `no_brim` arm as wired, which re-derivation from disk contradicted: `brim_type` is read nowhere in `modules/core-modules/skirt-brim/`. The prior draft's `preflight-report.md` described a packet that no longer exists and was removed with the split. No predecessor packet is superseded.

**Dependency.** `257b-brim-ears` has a hard dependency on this packet: ear anchors are detected on the per-object contour this packet creates, and the `brim_ears` value of `brim_type` is dispatched through the mode resolution this packet builds. `257a` must land first.

**Keys handed to no owner.** `brim_use_efc_outline` is returned to the queue and has **no owner** — elephant-foot compensation does not exist in this tree (`elefant_foot_compensation` occurs only as an `ORCA_CONFIG_PADDING` literal). `brim_type = painted` is an unshipped value with no owner — there is no point-valued brim paint carrier. Both need queue rows; the closing agent reports them and does not create them.

Note for the closing agent: this packet adds no module, so `docs/07_implementation_status.md`'s module inventory does not change. It moves two keys from unread to live, so `docs/15_config_keys_reference.md` must be regenerated.

| docs/07 task ID | Packet step | Primary docs | Expected code surface | OrcaSlicer refs | Context cost | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| — (queue packet, `task_ids: []`) | Steps 1–5 | `docs/00_project_overview.md`, `docs/03_wit_and_manifest.md` (manifest enum shape), `docs/08_coordinate_system.md`, `docs/21_data_defaults_and_fixtures.md`, `docs/ORCASLICER_ATTRIBUTION.md`, `docs/15_config_keys_reference.md` (generated) | `modules/core-modules/skirt-brim/{src/lib.rs,skirt-brim.toml,tests/brim_type_tdd.rs,tests/brim_object_gap_tdd.rs}` + `crates/slicer-scheduler/tests/integration/config_bounds_enforcement_tdd.rs` + one generated doc | `Brim.cpp` (`outer_inner_brim_area`, `use_brim_efc_outline`), `Print.hpp` (`PrintObject::has_brim`, `Print::has_auto_brim`), `PrintConfig.cpp` / `PrintConfig.hpp` (`BrimType` values and default), `PerimeterGenerator.cpp` (`_traverse_loops` — cited as deliberately not ported, DIV-3), `Support/SupportCommon.cpp` (`generate_support_toolpaths` — same) | M (aggregate; no step above M) | Re-authored under the map's Authoring rules 1–7; **Tier B** (was Tier A). 2 keys built, 1 returned (`brim_use_efc_outline`), 2 deferred to `257b`, 0 dead-in-canonical. `brim_type = painted` is an unshipped value, rejected by name. Builds contour brim from layer-0 `ExtrusionRole::OuterWall` entities — no WIT, no IR, no `ResolvedConfig` change. Re-derive `SkirtBrim`'s field count, the `object_id == "brim"` assertion sites, and every other ledger fact at point of use |
