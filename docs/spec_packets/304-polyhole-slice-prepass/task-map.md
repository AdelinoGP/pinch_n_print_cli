# Task Map: 304-polyhole-slice-prepass

This packet has **no `docs/07_implementation_status.md` task IDs** (`task_ids: []`). It is a wayfinder queue packet: its backlog slice is P87 in the map "Close the OrcaSlicer FFF feature gap", authored under ticket 94. The crosswalk below maps the queue's unit of account — the config key — onto packet steps, in place of a `TASK-###` column, matching the queue-packet convention set by packets 296-303.

| Queue unit (P87 key) | Packet step | Primary docs | Expected code surface | OrcaSlicer refs | Context cost | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| *(kernel prerequisite — no key)* | `Step 1` | `docs/ORCASLICER_ATTRIBUTION.md`, `docs/08_coordinate_system.md` | `crates/slicer-core/src/algos/polyhole.rs`, `crates/slicer-core/src/algos/mod.rs`, `crates/slicer-core/tests/algo_polyhole_tdd.rs` | `OrcaSlicerDocumented/src/libslic3r/PrintObject.cpp` (`create_polyholes`) | `S` | The polygon constructor: edge count, circumscribed radius, clockwise winding, and canonical's twist interleave. AC-1 and AC-2 land before any key is read. |
| `hole_to_polyhole`, `hole_to_polyhole_threshold`, `hole_to_polyhole_twisted`, `hole_to_polyhole_max_edges` | `Step 2` | `docs/08_coordinate_system.md` | `crates/slicer-core/src/algos/polyhole.rs`, `crates/slicer-core/tests/algo_polyhole_tdd.rs` | `OrcaSlicerDocumented/src/libslic3r/PrintObject.cpp` (`PrintObject::_transform_hole_to_polyholes`) | `M` | All four keys become live *as kernel parameters*: the gate (AC-3/AC-4), the detection threshold (AC-5), the twist (AC-6) and the edge cap (AC-7), plus every rejection path (AC-N1 through AC-N4). This is the step that makes the packet more than plumbing. |
| *(all four keys — seam)* | `Step 3` | `docs/04_host_scheduler.md` | `crates/slicer-scheduler/src/execution_plan.rs`, `crates/slicer-scheduler/tests/contract/stage_list_consistency_tdd.rs`, `crates/slicer-runtime/src/prepass.rs` | none | `S` | `PrePass::PolyholeTransform` enters `STAGE_ORDER` between Slice and OverhangAnnotation, classified host-only. AC-9. The `STAGE_ORDER` and `HOST_ONLY_STAGES` edits must not be separated. |
| `hole_to_polyhole`, `hole_to_polyhole_threshold`, `hole_to_polyhole_twisted`, `hole_to_polyhole_max_edges` | `Step 4` | `docs/21_data_defaults_and_fixtures.md` | `crates/slicer-ir/src/resolved_config.rs`, `crates/slicer-ir/tests/resolved_config_polyhole_tdd.rs` | `OrcaSlicerDocumented/src/libslic3r/PrintConfig.cpp` | `S` | All four keys become real `ResolvedConfig` fields at canonical defaults, so CLI, overlay and per-region resolution carry them. AC-8. Declaration alone is not coverage — Step 5 is what routes them. |
| `hole_to_polyhole` (per-region gate) | `Step 5` | `docs/04_host_scheduler.md`, `docs/02_ir_schemas.md` | `crates/slicer-runtime/src/builtins/polyhole_producer.rs`, `crates/slicer-runtime/src/prepass.rs`, `crates/slicer-runtime/tests/executor/prepass_polyhole_stage_order_tdd.rs` | `OrcaSlicerDocumented/src/libslic3r/PrintObjectSlice.cpp` (`PrintObject::slice`) | `M` | The config reaches the kernel through `RegionMapIR::config_for` per `RegionKey`, matching canonical's per-region read, and the result reaches the blackboard via `replace_slice_ir`. AC-10 and AC-11. |
| *(all four keys — ledger)* | `Step 6` | `docs/04_host_scheduler.md`, `docs/01_system_architecture.md`, `docs/DEVIATION_LOG.md` | `docs/04_host_scheduler.md`, `docs/01_system_architecture.md`, `docs/DEVIATION_LOG.md` | none (citations by file + function only) | `S` | AC-14 plus the Doc Impact greps. Re-derive the next free `DEV-###` here; the authoring-time value is a ledger fact and will have rotted. Three clauses: the elephant-foot ordering inversion, serial-where-canonical-is-parallel, and the single mutation target. |
| *(all four keys — queue accounting)* | `Step 7` | `docs/specs/orca-feature-gap/map.md` §Notes | `docs/specs/orca-feature-gap/issues/04-asset-tier-assignment.md`, `docs/specs/orca-feature-gap/issues/05-asset-packet-list.md` | none | `S` | AC-13. The tier table's `new polyhole module` owner is replaced by the host built-in on all three rows; the P87 heading links this packet. The queue count stays 409 — `hole_to_polyhole_max_edges` was never in it. |

Costs are copied from `implementation-plan.md` §Per-Step Budget Roll-Up. No row is `L`; the aggregate is `M`.
