# Task Map: infill-pattern-holder-mapping

Use this crosswalk when a packet spans more than one task ID, reopens prior work, or supersedes an earlier packet. **This packet emits the template's own skip clause:** it is a single-coherent-slice queue packet with `task_ids: []` (queue precedent — packets 234a, 253–261), so the `docs/07` crosswalk is N-A. Implementation is recorded against wayfinder ticket 15 (`docs/specs/orca-feature-gap/issues/15-author-packet-p08-strength-infill-infill-modules.md`), jointly with packet 262a.

262a and 262b together replace the single pre-split `262-infill-pattern-keys` packet (renamed to `262a-...` this session; nothing is marked `superseded` because no packet is being abandoned). 262a owns the four angle/multiline keys; 262b owns the two pattern-selection keys and `gap_fill_target`. Both must land for ticket 15 to close.

Note for the closing agent: this packet adds three core modules and a new claim ID, so `docs/07_implementation_status.md`'s module inventory and `docs/03_wit_and_manifest.md`'s claim table both change. Re-derive at completion time whether the `docs/07` inventory is generated or hand-maintained before editing it.

| docs/07 task ID | Packet step | Primary docs | Expected code surface | OrcaSlicer refs | Context cost | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| — (queue packet, `task_ids: []`) | Steps 1–8 (incl. 5b) | `docs/01_system_architecture.md` §Claim System, `docs/03_wit_and_manifest.md` §Known claim IDs, `docs/04_host_scheduler.md` §Claim Resolution, `docs/adr/0025-infill-linker-as-raw-emit-post-pass.md`, `docs/adr/0028-infill-postprocess-contract-prior-ir-and-partitioned-polygons.md`, `docs/adr/0056-integrated-modules-native-dispatch.md`, `docs/08_coordinate_system.md`, `docs/15_config_keys_reference.md` (generated) | three new `modules/core-modules/{crosshatch-infill,monotonic-infill,infill-gap-fill}/**` trees + `crates/slicer-scheduler/src/config_resolution.rs` + `crates/slicer-scheduler/tests/integration/{main.rs,config_resolution_pattern_holder.rs,manifest_ingestion_tdd.rs,config_bounds_enforcement_tdd.rs}` + `modules/core-modules/infill-linker/{src/lib.rs,tests/gap_fill_passthrough_tdd.rs}` (GapFill verbatim passthrough only) + `crates/slicer-runtime/tests/contract/native_infill_claim_resolution_tdd.rs` + root `Cargo.toml` + `crates/slicer-integrated-modules/{Cargo.toml,src/lib.rs}` + `crates/pnp-cli/Cargo.toml` + `docs/03`/`docs/04` | `OrcaSlicerDocumented/src/libslic3r/Fill/FillCrossHatch.cpp` (`FillCrossHatch::_fill_surface_single`, `generate_infill_layers`, `generate_repeat_pattern`, `generate_transform_pattern`, `generate_one_cycle`), `Fill/FillRectilinear.cpp` (`FillMonotonic::fill_surface`, `FillMonotonicLines::fill_surface`, `fill_surface_by_lines`), `Fill/FillBase.cpp` (`Fill::_create_gap_fill`, `Fill::fill_surface_extrusion`, `Fill::new_from_type`), `PrintConfig.cpp` / `PrintConfig.hpp` (the two pattern value lists, `enum GapFillTarget`) | L (aggregate; no single step above M) | Re-authored under the map's Authoring rules 1–6; Tier C. Re-derive the module count and the deviation-row count at completion time per the ledger-fact rule |
