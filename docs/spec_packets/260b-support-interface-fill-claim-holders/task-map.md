# Task Map: support-interface-fill-claim-holders

**Emitted because this packet supersedes prior work and depends on a sibling.** Together with `260a-support-interface-spacing-and-loops` it replaces the pre-rules packet `260-support-interface-keys`, which dispositioned `support_interface_pattern` "declared-with-gap" — prohibited by the map's Authoring rule 1. That directory no longer exists; its canonical evidence for this key is carried forward in `requirements.md` §Per-Key Canonical Evidence. The `docs/07` crosswalk is N-A: this is a single-coherent-slice queue packet with `task_ids: []`. Implementation is recorded against wayfinder ticket 18 (`docs/specs/orca-feature-gap/issues/18-author-packet-p11-support-interface-support-planner.md`).

| docs/07 task ID | Packet step | Primary docs | Expected code surface | OrcaSlicer refs | Context cost | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| — (queue packet, `task_ids: []`) | Step 0 | `design.md` §Open Questions | none (rulings only) | — | S | **Gate.** Three open `[BLOCK]`s: host selector field, two writers in one layer stage, angle metadata |
| — | Steps 1–5 | `docs/03_wit_and_manifest.md`, `docs/01_system_architecture.md`, `docs/04_host_scheduler.md`, `docs/08_coordinate_system.md` | `crates/slicer-scheduler/src/**`, `modules/core-modules/support-interface-{concentric,grid}/**`, `modules/core-modules/{traditional-support,tree-support}/src/lib.rs`, scheduler + runtime contract test files | `Support/SupportParameters.hpp` (`SupportParameters::SupportParameters`, `support_interface_angle()`), `Support/SupportCommon.cpp` (`generate_support_toolpaths`), `PrintConfig.hpp` (`SupportMaterialInterfacePattern`), `Fill/FillConcentric.cpp`, `Fill/FillRectilinear.cpp` | L (packet), no step L | Depends on `260a`; re-derive the crosswalk question at completion time per the ledger-fact rule |
