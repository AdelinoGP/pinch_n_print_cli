---
status: draft
packet: test-quality-remediation_05_core-support-dup-merges
task_ids:
  - core/DUP-CORE (support overhang)
  - core/DUP-CORE (support geometry)
backlog_source: docs/specs/test-quality-remediation-plan.md §5.1 DUP-CORE support slices
depends_on:
  - core-region-mapping-dup-merges
context_cost_estimate: S
copy_note: Approved core-support-dup-merges queue entry; task IDs are program wave/item IDs, not TASK-### mappings.
---

# Packet Contract: core-support-dup-merges

## Goal

Consolidate the coplanar-step overhang witness and the two inline support-geometry twins into their stronger integration-test survivors while preserving the exact step layer, expanded-back contact area, emitted-entry, empty-plan, and per-object schedule assertions.

## Scope Boundaries

Edit only `crates/slicer-core/tests/support_overhang_detection_tdd.rs`, `crates/slicer-core/tests/algo_support_geometry_tdd.rs`, the `#[cfg(test)] mod tests` block in `crates/slicer-core/src/algos/support_geometry.rs`, and—after both merges pass—the §7 `core` ledger row in `docs/specs/test-quality-remediation-plan.md`. The production bodies of `detect_support_contacts`, `build_emit_schedule`, and `execute_support_geometry`, every other `slicer-core` test, the Packet Queue, all contracts, and all other documentation remain unchanged.

## Prerequisites and Blockers

- Depends on: `core-region-mapping-dup-merges` for queue-generation order only; that draft packet exports no API, test file, or production symbol.
- Unblocks: `core-wall-sequence-dup-merges` packet generation only.
- Activation blockers: independent `spec-review --preflight`; no scope expansion.

## Acceptance Criteria

- **AC-1. Given** `support_overhang_detection_tdd.rs` has 19 pre-edit tests and only `coplanar_step_does_not_hide_the_contact` is absorbed, **when** the feature-correct target is listed and executed after the merge, **then** exactly 18 tests are discovered and pass, `overhang_is_detected_once_at_the_step_layer` appears exactly once, `coplanar_step_does_not_hide_the_contact` appears zero times, and the other 17 named tests remain discovered and passing. | `bash -lc 'set -euo pipefail; mkdir -p target; cargo test -p slicer-core --features host-algos --test support_overhang_detection_tdd -- --list 2>&1 | tee target/test-output.log >/dev/null; test "$(grep -c ": test$" target/test-output.log || true)" -eq 18; test "$(grep -c "^overhang_is_detected_once_at_the_step_layer: test$" target/test-output.log || true)" -eq 1; test "$(grep -c "^coplanar_step_does_not_hide_the_contact: test$" target/test-output.log || true)" -eq 0; for n in sharp_tails_add_first_layer_contacts_when_enabled sharp_tails_disabled_by_default_emits_none enforce_support_layers_forces_full_contacts_in_leading_layers enforce_support_layers_beyond_model_changes_nothing bridge_areas_are_removed_from_contacts_under_bridge_no_support bridge_removal_disabled_keeps_bridge_contacts cantilever_pass_records_wide_overhang_annotations overhang_is_detected_once_at_the_step_layer straight_column_produces_no_contacts a_region_sitting_on_a_different_region_below_is_not_a_contact shallower_threshold_yields_no_more_contact_area_than_a_plain_difference zero_angle_uses_the_overlap_offset_not_a_plain_difference threshold_angle_bump_catches_an_exactly_at_threshold_overhang threshold_angle_is_clamped_to_eighty_nine_degrees tiny_spots_are_filtered_out xy_expansion_grows_the_finished_contact blockers_are_subtracted_from_the_contact degenerate_inputs_do_not_panic; do test "$(grep -c "^${n}: test$" target/test-output.log || true)" -eq 1; done; cargo test -p slicer-core --features host-algos --test support_overhang_detection_tdd -- --nocapture 2>&1 | tee target/test-output.log >/dev/null; rg -q "test result: ok\. 18 passed; 0 failed" target/test-output.log; for n in sharp_tails_add_first_layer_contacts_when_enabled sharp_tails_disabled_by_default_emits_none enforce_support_layers_forces_full_contacts_in_leading_layers enforce_support_layers_beyond_model_changes_nothing bridge_areas_are_removed_from_contacts_under_bridge_no_support bridge_removal_disabled_keeps_bridge_contacts cantilever_pass_records_wide_overhang_annotations overhang_is_detected_once_at_the_step_layer straight_column_produces_no_contacts a_region_sitting_on_a_different_region_below_is_not_a_contact shallower_threshold_yields_no_more_contact_area_than_a_plain_difference zero_angle_uses_the_overlap_offset_not_a_plain_difference threshold_angle_bump_catches_an_exactly_at_threshold_overhang threshold_angle_is_clamped_to_eighty_nine_degrees tiny_spots_are_filtered_out xy_expansion_grows_the_finished_contact blockers_are_subtracted_from_the_contact degenerate_inputs_do_not_panic; do rg -q "test ${n} \.\.\. ok" target/test-output.log; done'`
- **AC-2. Given** both overhang candidates use `pillar_then_cap()` and `params(45.0, 0.2)`, and the absorbed test asserts only that the sweep is non-empty, **when** it is merged into `overhang_is_detected_once_at_the_step_layer`, **then** the survivor retains the stricter exact layer vector `vec![3_usize]`, the expanded-back area expectation `2.0 * 8.0 * 4.0`, the area tolerance `(got - expected).abs() < 0.1`, and the RC-1 coplanar-step regression rationale. | `bash -lc 'set -euo pipefail; mkdir -p target; F=crates/slicer-core/tests/support_overhang_detection_tdd.rs; awk "/^fn overhang_is_detected_once_at_the_step_layer\(\)/{flag=1} flag{print} flag&&/^}$/{exit}" "$F" > target/support-overhang-survivor.txt; test -s target/support-overhang-survivor.txt; rg -q -F "let layers = pillar_then_cap();" target/support-overhang-survivor.txt; rg -q -F "let contacts = sweep(&layers, &params(45.0, 0.2));" target/support-overhang-survivor.txt; rg -q -F "vec![3_usize]" target/support-overhang-survivor.txt; rg -q -F "let expected = 2.0 * 8.0 * 4.0;" target/support-overhang-survivor.txt; rg -q -F "(got - expected).abs() < 0.1" target/support-overhang-survivor.txt; rg -q -F "Regression pin for RC-1" target/support-overhang-survivor.txt; ! rg -q "fn coplanar_step_does_not_hide_the_contact" "$F"; cargo test -p slicer-core --features host-algos --test support_overhang_detection_tdd overhang_is_detected_once_at_the_step_layer -- --exact --nocapture 2>&1 | tee target/test-output.log >/dev/null; rg -q "test overhang_is_detected_once_at_the_step_layer \.\.\. ok" target/test-output.log'`
- **AC-3. Given** `algo_support_geometry_tdd.rs` has three pre-edit integration tests and `support_geometry.rs` has two pre-edit inline `#[cfg(test)]` tests, **when** the inline twins are consolidated into the integration-test home, **then** the integration target still discovers and passes exactly `emits_for_2_layer_fixture`, `build_emit_schedule_two_objects_per_object_semantics`, and `empty_plan_produces_empty_support`, while `support_geometry.rs` contains zero `#[test]` attributes, no `#[cfg(test)] mod tests`, and neither inline-only test definition. | `bash -lc 'set -euo pipefail; mkdir -p target; T=crates/slicer-core/tests/algo_support_geometry_tdd.rs; S=crates/slicer-core/src/algos/support_geometry.rs; cargo test -p slicer-core --features host-algos --test algo_support_geometry_tdd -- --list 2>&1 | tee target/test-output.log >/dev/null; test "$(grep -c ": test$" target/test-output.log || true)" -eq 3; for n in emits_for_2_layer_fixture build_emit_schedule_two_objects_per_object_semantics empty_plan_produces_empty_support; do test "$(grep -c "^${n}: test$" target/test-output.log || true)" -eq 1; done; test "$(grep -Ec "^[[:space:]]*#\[test\]" "$T" || true)" -eq 3; test "$(grep -Ec "^[[:space:]]*#\[test\]" "$S" || true)" -eq 0; ! rg -q "#\[cfg\(test\)\]|fn support_geometry_emits_for_2_layer_fixture|fn build_emit_schedule_two_objects_per_object_semantics" "$S"; cargo test -p slicer-core --features host-algos --test algo_support_geometry_tdd -- --nocapture 2>&1 | tee target/test-output.log >/dev/null; rg -q "test result: ok\. 3 passed; 0 failed" target/test-output.log; for n in emits_for_2_layer_fixture build_emit_schedule_two_objects_per_object_semantics empty_plan_produces_empty_support; do rg -q "test ${n} \.\.\. ok" target/test-output.log; done'`
- **AC-4. Given** the two inline support-geometry tests duplicate integration inputs while `empty_plan_produces_empty_support` is integration-only, **when** the consolidation is complete, **then** `emits_for_2_layer_fixture` still asserts `result.is_ok()` and non-empty `SupportGeometryIR.entries`, the schedule survivor still asserts exact `obj-A` layers `{1,3,5}` and exact `obj-B` layers `{0..5}` with the inline tests' `got` diagnostics retained, and the empty-plan witness still asserts successful empty `entries`. | `bash -lc 'set -euo pipefail; mkdir -p target; F=crates/slicer-core/tests/algo_support_geometry_tdd.rs; awk "/^fn emits_for_2_layer_fixture\(\)/{flag=1} flag{print} flag&&/^}$/{exit}" "$F" > target/support-geometry-emission-survivor.txt; awk "/^fn build_emit_schedule_two_objects_per_object_semantics\(\)/{flag=1} flag{print} flag&&/^}$/{exit}" "$F" > target/support-geometry-schedule-survivor.txt; awk "/^fn empty_plan_produces_empty_support\(\)/{flag=1} flag{print} flag&&/^}$/{exit}" "$F" > target/support-geometry-empty-survivor.txt; test -s target/support-geometry-emission-survivor.txt; test -s target/support-geometry-schedule-survivor.txt; test -s target/support-geometry-empty-survivor.txt; rg -q -F "assert!(result.is_ok());" target/support-geometry-emission-survivor.txt; rg -q -F "assert!(!ir.entries.is_empty());" target/support-geometry-emission-survivor.txt; rg -q -F "schedule.get(\"obj-A\")" target/support-geometry-schedule-survivor.txt; rg -q -F "[1u32, 3, 5]" target/support-geometry-schedule-survivor.txt; rg -q -F "schedule.get(\"obj-B\")" target/support-geometry-schedule-survivor.txt; rg -q -F "(0u32..6).collect::<BTreeSet<u32>>()" target/support-geometry-schedule-survivor.txt; rg -q -F "got {a_sched:?}" target/support-geometry-schedule-survivor.txt; rg -q -F "got {b_sched:?}" target/support-geometry-schedule-survivor.txt; rg -q -F "assert!(result.is_ok());" target/support-geometry-empty-survivor.txt; rg -q -F "assert!(result.unwrap().entries.is_empty());" target/support-geometry-empty-survivor.txt; cargo test -p slicer-core --features host-algos --test algo_support_geometry_tdd -- --nocapture 2>&1 | tee target/test-output.log >/dev/null; for n in emits_for_2_layer_fixture build_emit_schedule_two_objects_per_object_semantics empty_plan_produces_empty_support; do rg -q "test ${n} \.\.\. ok" target/test-output.log; done'`
- **AC-5. Given** the census manifest and `crates/slicer-core/Cargo.toml` register both integration targets behind `host-algos`, **when** registration is reconciled, **then** the census records `required == ["host-algos"]` for both targets and each Cargo `[[test]]` stanza retains `required-features = ["host-algos"]`; no bare feature-blind target command is accepted as evidence. | `bash -lc 'set -euo pipefail; python -c "import json,re,pathlib; d=json.loads(pathlib.Path(\"docs/specs/test-quality-remediation-census.json\").read_text())[\"slicer-core\"]; got={e[\"target\"]:e[\"required\"] for e in d}; names=(\"support_overhang_detection_tdd\",\"algo_support_geometry_tdd\"); assert all(got.get(n)==[\"host-algos\"] for n in names); t=pathlib.Path(\"crates/slicer-core/Cargo.toml\").read_text(); assert all(re.search(r\"\\[\\[test\\]\\]\\s*name = \\\"\"+re.escape(n)+r\"\\\"\\s*required-features = \\[\\\"host-algos\\\"\\]\",t) for n in names); print(\"PASS: both support targets require host-algos in census and Cargo\")"'`
- **AC-6. Given** report mode is the current test-quality enforcement mode, **when** the gate scans exactly the three touched Rust files, **then** it reports `0 finding(s) in 0 file(s)`; this packet does not claim the full `slicer-core` wave has reached its crate-wide zero-findings exit. | `bash -lc 'set -euo pipefail; mkdir -p target; cargo xtask check-test-quality --report crates/slicer-core/tests/support_overhang_detection_tdd.rs crates/slicer-core/tests/algo_support_geometry_tdd.rs crates/slicer-core/src/algos/support_geometry.rs 2>&1 | tee target/test-output.log >/dev/null; rg -q -F "check-test-quality: 0 finding(s) in 0 file(s) [report mode]" target/test-output.log'`
- **AC-7. Given** the six-column §7 remediation ledger and this support slice implemented without closing unrelated core work, **when** the section-anchored `core` row is parsed, **then** it is the sole `core` row, remains `partial`, records this packet's absorbed and surviving tests plus all required validation classes, and names `core-wall-sequence-dup-merges` as remaining work without changing the Packet Queue. | `bash -lc 'set -euo pipefail; python -c "from pathlib import Path; t=Path(\"docs/specs/test-quality-remediation-plan.md\").read_text(encoding=\"utf-8\"); s=t.split(\"## 7. Ledger\",1)[1].split(\"\\n## \",1)[0]; rows=[x for x in s.splitlines() if x.startswith(\"|\") and not x.startswith(\"|---\")]; headers=[x for x in rows if x.split(\"|\")[1].strip()==\"Wave\"]; assert len(headers)==1 and [x.strip() for x in headers[0].split(\"|\")[1:-1]]==[\"Wave\",\"State\",\"Retired/changed symbols\",\"Surviving/new coverage\",\"Validation\",\"Remaining gap\"]; core=[x for x in rows if x.split(\"|\")[1].strip()==\"core\"]; assert len(core)==1; c=[x.strip() for x in core[0].split(\"|\")[1:-1]]; assert len(c)==6; wave,state,changed,coverage,validation,gap=c; assert wave==\"core\" and state==\"partial\"; assert all(x in changed for x in (\"core-support-dup-merges\",\"coplanar_step_does_not_hide_the_contact\",\"overhang_is_detected_once_at_the_step_layer\",\"support_geometry_emits_for_2_layer_fixture\",\"build_emit_schedule_two_objects_per_object_semantics\")); assert all(x in coverage for x in (\"overhang_is_detected_once_at_the_step_layer\",\"emits_for_2_layer_fixture\",\"build_emit_schedule_two_objects_per_object_semantics\",\"empty_plan_produces_empty_support\")); assert all(x in validation for x in (\"cargo test\",\"cargo check --workspace --all-targets\",\"cargo clippy --workspace --all-targets -- -D warnings\",\"cargo xtask check-literals\",\"check-test-quality\")); assert \"core-wall-sequence-dup-merges\" in gap; q=t.split(\"## Packet Queue\",1)[1]; qrows=[x for x in q.splitlines() if x.startswith(\"|\") and not x.startswith(\"|---\")]; assert [x.strip() for x in qrows[0].split(\"|\")[1:-1]]==[\"#\",\"packet slug\",\"goal (one sentence)\",\"task ids\",\"depends on\",\"status\",\"packet dir\"]; assert any(\"core-support-dup-merges\" in x for x in qrows); print(\"PASS: anchored core ledger row has six partial-state evidence cells; Packet Queue table intact\")"'`

## Negative Test Cases

- **AC-N1. Given** only one overhang test and two inline tests are authorized for removal, **when** the survivor-map guard checks the final source, **then** the complete ordered 18-name overhang roster and ordered three-name integration roster must match exactly, each retained assertion must occur in its named survivor body, and any unaccounted rename/removal, lost layer/area/schedule/entry assertion, or surviving absorbed definition makes the command fail; the valid tree reconciles `19 → 18`, `3 → 3`, and inline `2 → 0`. | `bash -lc 'set -euo pipefail; python -c "import pathlib,re; o=pathlib.Path(\"crates/slicer-core/tests/support_overhang_detection_tdd.rs\").read_text(); a=pathlib.Path(\"crates/slicer-core/tests/algo_support_geometry_tdd.rs\").read_text(); s=pathlib.Path(\"crates/slicer-core/src/algos/support_geometry.rs\").read_text(); names=lambda x: re.findall(r\"(?m)^\\s*#\\[test\\]\\s*\\n\\s*fn\\s+([A-Za-z0-9_]+)\",x); body=lambda x,n: x.split(\"fn \"+n+\"()\",1)[1].split(\"\\n}\\n\",1)[0]; expected_o=[\"sharp_tails_add_first_layer_contacts_when_enabled\",\"sharp_tails_disabled_by_default_emits_none\",\"enforce_support_layers_forces_full_contacts_in_leading_layers\",\"enforce_support_layers_beyond_model_changes_nothing\",\"bridge_areas_are_removed_from_contacts_under_bridge_no_support\",\"bridge_removal_disabled_keeps_bridge_contacts\",\"cantilever_pass_records_wide_overhang_annotations\",\"overhang_is_detected_once_at_the_step_layer\",\"straight_column_produces_no_contacts\",\"a_region_sitting_on_a_different_region_below_is_not_a_contact\",\"shallower_threshold_yields_no_more_contact_area_than_a_plain_difference\",\"zero_angle_uses_the_overlap_offset_not_a_plain_difference\",\"threshold_angle_bump_catches_an_exactly_at_threshold_overhang\",\"threshold_angle_is_clamped_to_eighty_nine_degrees\",\"tiny_spots_are_filtered_out\",\"xy_expansion_grows_the_finished_contact\",\"blockers_are_subtracted_from_the_contact\",\"degenerate_inputs_do_not_panic\"]; expected_a=[\"emits_for_2_layer_fixture\",\"build_emit_schedule_two_objects_per_object_semantics\",\"empty_plan_produces_empty_support\"]; assert names(o)==expected_o and names(a)==expected_a and names(s)==[]; ob=body(o,\"overhang_is_detected_once_at_the_step_layer\"); eb=body(a,\"emits_for_2_layer_fixture\"); sb=body(a,\"build_emit_schedule_two_objects_per_object_semantics\"); xb=body(a,\"empty_plan_produces_empty_support\"); assert all(x in ob for x in (\"vec![3_usize]\",\"let expected = 2.0 * 8.0 * 4.0;\",\"(got - expected).abs() < 0.1\",\"Regression pin for RC-1\")); assert all(x in eb for x in (\"assert!(result.is_ok());\",\"assert!(!ir.entries.is_empty());\")); assert all(x in sb for x in (\"schedule.get(\\\"obj-A\\\")\",\"[1u32, 3, 5]\",\"schedule.get(\\\"obj-B\\\")\",\"(0u32..6).collect::<BTreeSet<u32>>()\",\"got {a_sched:?}\",\"got {b_sched:?}\")); assert all(x in xb for x in (\"assert!(result.is_ok());\",\"assert!(result.unwrap().entries.is_empty());\")); assert \"coplanar_step_does_not_hide_the_contact\" not in o and \"fn support_geometry_emits_for_2_layer_fixture\" not in s and \"fn build_emit_schedule_two_objects_per_object_semantics\" not in s; print(\"PASS: complete survivor rosters and scoped assertion union; 19->18, 3->3, inline 2->0\")"'`

## Verification

- `bash -lc 'set -euo pipefail; mkdir -p target; cargo test -p slicer-core --features host-algos --test support_overhang_detection_tdd -- --nocapture 2>&1 | tee target/test-output.log >/dev/null; rg -q "test result: ok\. 18 passed; 0 failed" target/test-output.log'`
- `bash -lc 'set -euo pipefail; mkdir -p target; cargo test -p slicer-core --features host-algos --test algo_support_geometry_tdd -- --nocapture 2>&1 | tee target/test-output.log >/dev/null; rg -q "test result: ok\. 3 passed; 0 failed" target/test-output.log'`
- `cargo check --workspace --all-targets && cargo clippy --workspace --all-targets -- -D warnings`

## Authoritative Docs

- `docs/specs/test-quality-remediation-plan.md` §§1–4, §5.1 DUP-CORE, §§6–7, and Packet Queue row #5/resume exports - direct bounded reads.
- `docs/22_test_quality.md` §§1–5 - direct read; earn-their-keep, production-oracle, duplicate-registration, and report-mode gate rules.
- `docs/adr/0064-existing-tests-retire-if-unjustified.md` - direct read; each removal requires a named survivor and an accounted census delta.
- `docs/adr/0065-test-quality-gate-with-delayed-enforce-mode.md` - direct read; touched-scope report-mode findings must be zero without claiming enforce mode.
- `docs/02_ir_schemas.md` §IR 9a `SupportGeometryIR` - direct bounded read; per-object accumulated-height schedule and `global_layer.index` contract.
- `docs/08_coordinate_system.md` §The Rule, §Conversion & Determinism, and §Conversion When Porting OrcaSlicer Code - direct bounded reads; geometry values and area conversions.
- `docs/specs/test-quality-remediation-census.json` entries for both targets and `crates/slicer-core/Cargo.toml` `[[test]]` stanzas - direct bounded reads; both require `host-algos`.

## Doc Impact Statement (Required)

Specific implementation-time documentation impact: update only the `core` row in `docs/specs/test-quality-remediation-plan.md` §7 Ledger to remain `partial`, record `core-support-dup-merges`, its absorbed/surviving tests and actual validations, and name `core-wall-sequence-dup-merges` as remaining work. Do not edit the Packet Queue or any other ledger row. AC-7's section-anchored six-column parser verifies this bookkeeping change; no IR, WIT, scheduler, manifest, host-service, SDK, or production-behavior contract changes.

<!-- snippet: orca-delegation -->
## OrcaSlicer Reference Obligations

All OrcaSlicer reads MUST be delegated to a sub-agent. Never load `OrcaSlicerDocumented/` into the implementer's own context. Dispatch contract: return `LOCATIONS` (file:line + 1-line context, ≤ 20 entries) or `SUMMARY` (≤ 200 words, no code unless asked). Code snippets in returns are capped at 30 lines.

Files to inspect for this packet:

- `OrcaSlicerDocumented/src/libslic3r/Support/SupportMaterial.cpp` — confirm canonical `detect_overhangs` retains the lower-layer growth, difference, and expand-back behavior already pinned by the surviving exact contact-area witness; no canonical code is ported or changed.

<!-- snippet: context-discipline -->
## Context Discipline Note

This packet was generated against the context_discipline preamble shared by `spec-packet-generator`, `swarm`, and `spec-review`. Downstream agents implementing or reviewing this packet must:

- treat `design.md`'s code change surface as the authoritative files-in-scope list
- honor `design.md`'s out-of-bounds list — those files must not be loaded directly
- delegate every cargo run and authoritative-doc fact-check
- obey the shared absolute context bands: 120k reading budget with hand-off at 150k (standard); the extended band (240k reading / 300k hard stop) only via swarm's escalation protocol

Aggregate context cost above is the sum of per-step costs in `implementation-plan.md`. If any single step is rated L, the packet must be split before activation (an extended-band run may carry a single L step only when `design.md` justifies why it cannot be split).
