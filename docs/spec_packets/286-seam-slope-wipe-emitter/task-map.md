# Task Map: 286-seam-slope-wipe-emitter

Queue packet from the wayfinder map "Close the OrcaSlicer FFF feature gap"; authored under map ticket 60. Single-task packet (no backlog slice); this crosswalk exists for the S0 structural gate and records the map provenance.

| docs/07 task ID | Packet step | Primary docs | Expected code surface | OrcaSlicer refs | Context cost | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| `TASK-000` (queue packet, `task_ids: []`) | Steps 1–1b–4 | `docs/config/host-keys.toml`; `docs/DEVIATION_LOG.md` (DEV-178); `docs/15_config_keys_reference.md` (generated via `cargo xtask gen-config-docs`, Step 1b); `docs/08_coordinate_system.md` (min-length/start-height math) | `crates/slicer-ir/src/resolved_config.rs` + `crates/slicer-gcode/src/emit.rs` + `crates/slicer-gcode/src/serialize.rs` (twin-shadowing arms) + `modules/core-modules/machine-gcode-emit/machine-gcode-emit.toml` + `crates/slicer-gcode/tests/seam_slope_wipe_emission_tdd.rs` | `OrcaSlicerDocumented/src/libslic3r/PrintConfig.cpp` (eight declarations + bounds) + `OrcaSlicerDocumented/src/libslic3r/GCode.cpp` (`GCode::extrude_loop` slope gating, `ExtrusionLoopSloped` ramp inputs, loop-wipe sites + neighbour qualification) + `OrcaSlicerDocumented/src/libslic3r/Layer.cpp` (`Layer::is_perimeter_compatible` grouping — dropped unless proven needed) | M | P53: 8 Tier B keys, owner `crates/slicer-gcode` stands (all canonical reads emission-time; machine-gcode-emit sweep is the wrong seam); Tier B sizing confirmed (six zero-occurrence + two padding-only — new slope/wipe logic, no existing decision point); scalar-global is parity (all canonical scalars — no ticket-125 vector model); one intended default CONFIG_BLOCK value change (live `wipe_on_loops = false` shadows the stale `"1"` twin, count unchanged; table untouched); slope + wipes inert at defaults; implementation sequences after packet 285 (additive generalisation, activation-blocked); loop/retract wipe coincidence precedence over draft 277 with no dependency edge (names reconciled). |

Copy costs from `implementation-plan.md`. Split before activation if any row is L or aggregate exceeds M.
