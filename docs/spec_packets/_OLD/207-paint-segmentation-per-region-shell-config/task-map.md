# Task Map: 207-paint-segmentation-per-region-shell-config

This packet carries a single backlog task (`TASK-323`) and a single deviation row (DEV-122), but it extends prior work under `TASK-253` / packet 128, so the crosswalk records that relationship explicitly.

| docs/07 task ID | Relationship | Packet step | Primary docs | Expected code surface | OrcaSlicer refs | Context cost | Notes |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `TASK-323` | new — closes DEV-122 | `Step 1`, `Step 2` | `docs/02_ir_schemas.md` §"Config Interner Contract (Normative — Packet 91)"; `docs/15_config_keys_reference.md` (generated by `gen_config_docs::run`, which `cargo xtask check-deviations` also invokes; delegated lookup only) | `crates/slicer-core/src/algos/paint_segmentation/mod.rs` (`painted_subsets` re-key; new `ShellParams` / `region_key_for_chain` / `ext_abs_mm` / `shell_params_from_config` / `resolve_shell_params`; `configs.first()` block deleted; `RoleWidthContext` repaired; new `#[cfg(test)] mod shell_config_resolver_tests` holding the six unit-level ACs, because the five items stay module-private and `tests/` is a separate crate), `crates/slicer-core/tests/paint_segmentation_per_region_shell_config_tdd.rs` (new — the four end-to-end ACs only) | `OrcaSlicerDocumented/src/libslic3r/MultiMaterialSegmentation.cpp` (`layer_color_stat` in `multi_material_segmentation_by_painting`) | `M` + `M` | Proves the task by AC-1 (resolver reads the painted `variant_chain`'s interned config, not the BASE chain's and not `configs[0]`), AC-2 (projection depth now tracks `top_shell_layers`), AC-4/AC-5 (`outer_wall_line_width` and `nozzle_diameter` honoured), AC-6 (per-object independence), AC-9 (per-`variant_chain` independence within one object), AC-3 (the violating call, the dead tuple and the `TODO:` are gone) and AC-N1…N4. |
| `TASK-253` (packet 128) | **extended, not superseded** | `Step 2` (AC-7 gate), `Step 3` | none — read-only relationship | read-only: the shell-index propagation block and `assert_per_object_shell_index_invariant` in `crates/slicer-core/src/algos/paint_segmentation/mod.rs` | none | included in `M` | Packet 128 keyed the shell-*index* accumulator by `ObjectId` and added `source_objects` to `painted_subsets`; it explicitly left config resolution, the `None` arm, `RoleWidthContext` and `propagate_top_bottom` untouched. This packet keys the *config* by that same `ObjectId` **plus the painted `variant_chain`**, which makes `source_objects` a singleton. AC-7 enumerates all six of its tests by name so a rename cannot hide a deletion. `TASK-253`'s ledger checkbox is currently unchecked while its archived spec reads `implemented`; do not flip it as a side effect. |
| `TASK-323` | closure | `Step 4`, `Step 5` | `docs/DEVIATION_LOG.md`; `docs/02_ir_schemas.md` | `docs/DEVIATION_LOG.md`, `docs/02_ir_schemas.md` | `OrcaSlicerDocumented/src/libslic3r/MultiMaterialSegmentation.cpp` (parity sentence in the closure note) | `S` + `S` | Proves closure by AC-8 (DEV-122 Status begins `Closed`, names packet 207, and `cargo xtask check-deviations --check` exits 0 — that gate covers doc 07's Open Deviation Map AND doc 15's generated tables) plus the doc-02 anchors `never a production config source` (matched with `rg -U` and `\s+` between tokens — that section is hard-wrapped) and `slice_has_paint`. |

Costs are copied from `implementation-plan.md` §Per-Step Budget Roll-Up. No row is `L`; the aggregate is `M`.

`TASK-323` is not present in `docs/07_implementation_status.md` today. The Packet Completion Gate adds it through a worker dispatch at closure — do not assume it exists when grounding this packet.

Packet 207 depends on packet 206 (`206-seam-paint-delivery`): both edit `execute_paint_segmentation`. 206 owns the head guards and the tail seam stamping; 207 owns the `configs.first()` block, the `painted_subsets` key and the `propagate_top_bottom` call site. Execute 207 against the post-206 tree.
