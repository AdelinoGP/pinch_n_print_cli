# Task Map: 247-visual-debug-silhouette-core

This packet spans four new task IDs, so this crosswalk is emitted; it owns the `docs/07_implementation_status.md` mapping. TASK-442..TASK-445 are **new rows** (re-derived 2026-08-27, preflight round 1, as max+1..max+4 over the union of: `docs/07_implementation_status.md` — highest TASK-380; `docs/specs/*.md` plan files — highest TASK-441, allocated by `docs/specs/support-generation-remediation-plan.md`'s organic-tree-engine row; all local `docs/spec_packets/*/task-map.md` excluding this packet — highest TASK-440, in `242-support-family-orca-closure`; and `origin/master`-only packets 243–246 — highest TASK-356). The completion-gate worker adds these rows to docs/07 with this packet as their source; no existing open TASK covers this work (visual-debug lineage TASK-267..270 closed with packets 157–161). Any future re-derivation must include `docs/specs/*.md` in the sweep — that is where the prior TASK-441 collision hid.

| docs/07 task ID | Packet step | Primary docs | Expected code surface | OrcaSlicer refs | Context cost | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| `TASK-442` | `Step 1` | `docs/specs/visual-debug-silhouette-side-views-plan.md` §6 | `crates/pnp-cli/src/visual_debug.rs` (`validate_request`, `ValidationError`, `VisualizationOptions.view`) | none (PnP-native) | M | Schema 1.2.0 gate + full silhouette fail-closed matrix (R1–R5 + interim rejections); proves no silhouette request can reach a renderer unvalidated |
| `TASK-443` | `Step 2` | plan §4.2 D7 | `crates/pnp-cli/src/visual_debug.rs` (`ImageEntry`, `LayerRangeEntry`) | none | M | 1.2.0 manifest shape with 1.0/1.1 serialization byte-compat pinned (incl. the gcode `layer_z: null` case the plan's D7 text missed) |
| `TASK-444` | `Steps 3–4` | plan §2, D1, D2, D9, §7 | `crates/slicer-runtime/src/visual_debug_render.rs`, `crates/slicer-runtime/src/lib.rs` (`SilhouetteView`, `render_silhouette_composite`, `compute_silhouette_viewport_bounds`, palette constants) | none | M | Exact interval-projection composite renderer: Slice-family per-region slabs, support-plan roles, paint order, W1/W2/occlusion warnings, determinism |
| `TASK-445` | `Steps 5–6` | plan D3, §5; `docs/19_visual_debug.md` | `crates/pnp-cli/src/visual_debug.rs` (`run_model_source` silhouette branch), `docs/19_visual_debug.md`, `docs/02_ir_schemas.md`, `docs/DEVIATION_LOG.md` | none | M | Per-(tap,view) bundle assembly, model-wide Z framing, filenames, manifest emission; docs + raft deviation row |

Costs copied from `implementation-plan.md`. No row is L; aggregate M.
