# Task Map: 206-seam-paint-delivery

This packet groups a single backlog task (`TASK-322`) but bundles three separate `docs/DEVIATION_LOG.md` rows that must land together, so the crosswalk is by deviation row rather than by task ID.

| docs/07 task ID | Deviation row | Packet step | Primary docs | Expected code surface | OrcaSlicer refs | Context cost | Notes |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `TASK-322` | DEV-123 (open seam-writer half) | `Step 1`, `Step 2`, `Step 3` | `docs/02_ir_schemas.md` §"IR 6 — SliceIR" | `crates/slicer-core/src/algos/paint_segmentation/seam_annotations.rs` (new), `crates/slicer-core/src/algos/paint_segmentation/mod.rs` | `OrcaSlicerDocumented/src/libslic3r/GCode/SeamPlacer.cpp` (`gather_enforcers_blockers`) | `S` + `M` + `S` | Proves the row by AC-1/AC-2/AC-3 + AC-N2: a production writer now populates `Custom("seam_enforcer"/"seam_blocker")` into `SlicedRegion.segment_annotations`, including on the seam-only short-circuit path. AC-N3 proves the already-closed region-split half is unregressed. |
| `TASK-322` | DEV-134 (arachne bias gap) | `Step 4`, `Step 5` | `docs/05_module_sdk.md` §"Paint-seam consumption" | `crates/slicer-core/src/perimeter_utils.rs`, `modules/core-modules/classic-perimeters/src/lib.rs`, `modules/core-modules/arachne-perimeters/src/lib.rs` | `OrcaSlicerDocumented/src/libslic3r/GCode/SeamPlacer.hpp`; `.../PerimeterGenerator.cpp` (negative evidence) | `S` + `M` | Proves the row by AC-6 (both generators exclude the same blocked corner) and AC-7 (one shared `pub fn seam_paint_boxes`, no duplicate definition). The promotion must precede the arachne call site. |
| `TASK-322` | DEV-133 (support-paint substring leak) | `Step 6` | `docs/05_module_sdk.md` §T-083 | `modules/core-modules/seam-planner-default/src/visibility.rs`, `modules/core-modules/seam-planner-default/src/lib.rs` | `OrcaSlicerDocumented/src/libslic3r/GCode/SeamPlacer.cpp` (`gather_enforcers_blockers` reads `mv->seam_facets` exclusively) | `M` | Proves the row by AC-4 (`SupportEnforcer` → `Neutral`), AC-5 (exact seam semantics classify correctly), AC-N1 (`Custom("SupportEnforcer")` → `Neutral`) and **AC-10** (the discriminator is reached from `run_seam_planning`, not only from a `#[path]`-included copy). AC-4/5/N1 alone cannot prove the row: they passed on a tree where `paint_annotation_type` had zero production callers. Must land before the writer is exercised in production; ordering is pinned in `requirements.md` §Step Completion Expectations. |
| `TASK-322` | all three rows | `Step 7`, `Step 8` | `docs/DEVIATION_LOG.md`; `docs/05_module_sdk.md` | `docs/DEVIATION_LOG.md`, `docs/05_module_sdk.md`, `crates/slicer-runtime/tests/executor/paint_channel_consumer_paths_tdd.rs` (header only) | none | `S` + `S` | Proves closure by AC-8 (three Status cells begin `Closed`, name packet 206, and `cargo xtask check-deviations --check` exits 0) and AC-9 (both doc-05 sections corrected). |

Costs are copied from `implementation-plan.md` §Per-Step Budget Roll-Up. No row is `L`; the aggregate is `M`.

`TASK-322` is not present in `docs/07_implementation_status.md` today. The Packet Completion Gate adds it through a worker dispatch at closure — do not assume it exists when grounding this packet.
