# Task Map: 209-scanline-pattern-service

Single-task packet, but the crosswalk is emitted because `TASK-325` does not yet exist in
`docs/07_implementation_status.md` (it is allocated by
`docs/specs/deviation-remediation-206-212-plan.md`) and because the packet retires a *different* task
id that was silently reused — ADR-0009 points its duplication follow-up at `TASK-270`, which `docs/07`
shows closed as the packet-160 visual-debug G-code renderer. Both facts are ledger facts: re-derive
the highest live `TASK-###` and TASK-270's current owner at the moment the row is written rather than
trusting this file.

**Re-scoped 2026-08-07.** The shared-kernel approach (`crates/slicer-core/src/scanline_fill.rs`,
`crates/slicer-core/tests/scanline_fill_tdd.rs`, `scanline_fill`, `ScanlineFillParams`, `FillSegment`,
the promotion of `adjust_solid_spacing`) is **forbidden** by
`docs/adr/0026-infill-linking-algorithms-in-linker-module.md` and has been removed from every row
below. The packet now reconciles the three copies **in place**; no `slicer-core` file appears in any
row's expected code surface, and none may. See `requirements.md` §"Re-scope 2026-08-07".

| docs/07 task ID | Packet step | Primary docs | Expected code surface | OrcaSlicer refs | Context cost | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| `TASK-325` | `Step 1` | `docs/adr/0026-infill-linking-algorithms-in-linker-module.md`, `docs/08_coordinate_system.md` | `modules/core-modules/rectilinear-infill/src/lib.rs`, `modules/core-modules/rectilinear-infill/tests/rectilinear_raw_emit_tdd.rs`, `modules/core-modules/rectilinear-infill/tests/rectilinear_infill_edge_cases_tdd.rs` | `Fill/FillRectilinear.cpp`, `Fill/FillBase.cpp` | `M` | Copy 1, the highest-blast-radius one and the only one with real scan-line coverage. Adopts canonical's half-open grid (`while scan_y < rmax_y`, `ceil(h/s)` lines) and deletes the two behaviours that only made sense under the inclusive grid: the top-boundary post-pass over `rotated_contour` and the `rmax_y - rmin_y < effective_spacing` bail. **Shipped infill geometry moves**; four self-captured fixture counts are re-baselined (`square_10mm_density_20_emits_n_raw_segments` 6 → 5, `polygon_with_hole_segments_split_around_hole` 8 → 7, `solid_spacing_adjusted_for_solid_role` 5 → 4 with its y-set losing the 10.0 mm top-boundary line, and `very_small_polygon_emits_no_paths_without_panic` → `..._emits_one_scan_row_...` 0 → 1) per `CLAUDE.md` §Test Discipline. The vertex axis does **not** change here — this copy's half-open test already reproduces canonical's crossing count. `adjust_solid_spacing` stays a private fn in this module (ADR-0026's 2026-08-05 amendment) with its body byte-identical; only its false port attribution is corrected. Five green infill binaries (7/2/9/7/4) are the evidence. |
| `TASK-325` | `Step 2` | `docs/08_coordinate_system.md` | `modules/core-modules/traditional-support/src/lib.rs`, `modules/core-modules/traditional-support/tests/support_fill_geometry_tdd.rs`, `modules/core-modules/traditional-support/tests/traditional_support_tdd.rs` | `Support/SupportCommon.cpp`, `Fill/FillRectilinear.cpp` | `M` | Copy 2. Carries the **correctness fix**: the strictly-between test drops **both** events at a true crossing vertex, losing a crossing and inverting inside/outside for the rest of that scan row. Replaced by the scan-parallel skip + half-open test + zero-length-span drop. Also moves the scan start to the rotated bbox min (canonical `x0 = bounding_box.min(0)`), switches the rotation reference to the per-ExPolygon bbox centre, and deletes the centroid fallback — which is dead code once every non-degenerate region gets a bbox-min line. Also corrects **four** `make_config(true, 0.2, …)` density literals to `20.0` (`extrusion_role_is_support_material`, `speed_factor_from_config`, `alternating_angle`, `empty_regions_no_output`): `support_density` is a percent, so `0.2` gives 200 mm spacing over a 10 mm square and those fixtures exercise exactly one degenerate boundary line. Fixture literal only — 8 changed diff lines, zero assertion edits. Closes the coverage hole that let support drift undetected: this module has **no** geometric-invariant test today. |
| `TASK-325` | `Step 3` | `docs/08_coordinate_system.md` | `modules/core-modules/support-surface-ironing/src/lib.rs`, `modules/core-modules/support-surface-ironing/tests/ironing_scanline_parity_tdd.rs` | `Fill/FillRectilinear.cpp` | `S` | Copy 3 — previously ruled out of scope, now **in** scope, because reconciling without it leaves the divergence half-fixed: it carries the same strictly-between crossing-vertex defect and the same `min_y + line_spacing` start. Its change is a strict subset of Step 2's minus the rotation: **no `rotate_point` is added** (ironing scans axis-aligned by design). It also gains the `min_x >= max_x` half of the degenerate guard (today it guards only `min_y >= max_y || line_spacing <= 0` and computes no x-bounds); AC-9's static grep and AC-N2's two-file loop both pin it. `ironing_tdd.rs`'s eleven tests must stay green **byte-unchanged** — every assertion in it is relational, not an absolute count. |
| `TASK-325` | `Step 4` | `docs/adr/0009-raft-as-layer-infill-role.md`, `docs/adr/0026-infill-linking-algorithms-in-linker-module.md` | `docs/DEVIATION_LOG.md`, `docs/adr/0009-raft-as-layer-infill-role.md`, `docs/07_implementation_status.md` | — | `S` | **DEV-127 stays Open** — the duplication is not removed, three copies remain, and its target close is still the WIT pattern-services packet. Its body gains a dated reconciliation note, names the third copy, and has the non-existent type `SupportFiller` corrected to `TraditionalSupport` (2×). Five `D-209-*` rows are filed. ADR-0009's three rotted pointers are corrected in place — `fill_expolygon_multi` → `scan_expolygon` (2×), `TASK-270` → `DEV-127` (2×), the dead `docs/specs/support-modules-orca-port.md` path (**4×**) — with **no `## Amendment` section and no normative change**: with no extraction there is nothing to amend, so the user-approved 2026-08-07 ADR-0009 override lapses unused. |

Costs are copied from `implementation-plan.md` §Per-Step Budget Roll-Up. Aggregate is `M`; no row is
L, so no split is required before activation. The ADR-0009 override that previously gated activation
is no longer needed at all — the approach it would have permitted is gone. The one remaining
open item is a `[FWD]` in `design.md` §Open Questions: whether correcting ADR-0009's three factual
pointers counts as an ADR edit for queue purposes. It is scoped so that dropping AC-15 and Step 4's
ADR edit removes it cleanly, with no other packet content depending on it.
