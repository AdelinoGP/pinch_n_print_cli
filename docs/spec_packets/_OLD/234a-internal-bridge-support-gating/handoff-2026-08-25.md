# Handoff Brief — 234a closure (2026-08-25)

Authoritative continuation input. Decisions below were made by the user via grilling
interview on 2026-08-25 and bind the resuming session.

## Run state

- Checkpoints (protected run): `1bb9f9f4` → `0fb82bc7` on parent `232968ea`. Tree clean.
- Steps DONE+verified: 1, 2a, 2b, 3, 4a, 4b, 4c, 5a, 5b. Gates green:
  check/clippy(-D)/check-literals/guests-fresh; core suite; integration bucket 311-pass;
  wedge (AC-N3) green; AC-N1 mixed-density GREEN.
- RED: AC-5 arbiter = 3 sites on calicat (fresh binary):
  `29.27mm²@z=4.600`, `11.56mm²@z=18.600`, `161.92mm²@z=29.600`
  (true site 404.8mm extruded @0.4mm width — IN window [29.15,29.75]×[300,700]mm).
- NOT STARTED: S6b golden ceremony, S6c F3/F4 doc rows, full spec-review, acceptance
  ceremony (`cargo xtask test --workspace` via FACT sub-agent).
- Prior report: conversation of 2026-08-24 (Swarm Execution Report, Status PARTIAL).

## User decisions (binding)

1. **Execution**: fresh session runs the ENTIRE remainder (AC-5 → S6b → S6c → review →
   acceptance ceremony → close-out).
2. **AC-5 attack order**: PARALLEL read-only pair first —
   (a) provenance audit: re-derive the frozen "exactly one @ Z≈29.45" claim against THIS
   calicat fixture + e2e profile (decision brief measurements vs our model/config);
   (b) mechanism sweep: hunt unported candidate-erasure gates in
   `OrcaSlicerDocumented/src/libslic3r/PrintObject.cpp`.
   If (a) shows fixture/profile divergence from what the brief measured, surface a
   bar-amendment proposal WITH evidence — do not silently re-pin, do not tune code to fit.
3. **S5c REOPENED — host-side, this packet**: implement `enable_extra_bridge_layer`
   emission semantics HOST-side (user explicitly chose this over deviation row/successor
   packet). Canonical semantics captured 2026-08-24: duplicate bridge-support material on
   layer lidx+1 wherever THAT layer's dense-interior footprint overlaps the CURRENT layer's
   qualified internal-bridge areas, bridging angle +π/2, gated by the key (canonical
   `eblApplyToAll || eblInternalBridgeOnly` analogues). Requires: net-new IR home for
   second-bridge geometry (e.g. `SlicedRegion.extra_bridge_areas`, `#[serde(default)]`,
   host-only unless justified otherwise — decide during design), authored in the prepass
   (cross-layer legal there), constructed at the InfillPostProcess arm alongside existing
   anchored construction. Packet docs MUST be amended FIRST (refine-draft pass):
   requirements §5 venue change, design.md change-surface + rejected-alternatives note,
   new step block w/ files/verification/cost, AC or negative-criterion coverage
   (default-off ⇒ byte-stable output; enabled ⇒ duplicated perpendicular layer).
   Bar-coherence note: arbiter/gcode bars read qualified AREAS and default-off emission,
   so an enabled-only extra layer keeps AC-5/AC-6 semantics intact.
4. **Close-out policy: FULL GATE.** draft→implemented ONLY when every pipe-suffixed AC
   (incl. AC-5, AC-6, AC-N2) passes AND the acceptance ceremony is green. No conditional
   close. Then: docs/07 reconciliation (ISSUE-82 terminus note), squash the two checkpoints
   into one honest packet commit (both are run checkpoints on one parent — squash permitted),
   flip status, mark ISSUE-82 rows.

## Fresh-session execution order

1. `/swarm docs/spec_packets/234a-internal-bridge-support-gating` (mode: implement;
   resume from checkpoints; verify tree clean + guests fresh first).
2. Refine-draft pass: fold decision 3 into packet docs before any S5c-host code.
3. Dispatch parallel read-only pair (decision 2). Merge → targeted fix round(s) with
   fresh-binary discipline (`cargo build -p pnp-cli`; verify mtime newer than sources
   before trusting ANY e2e measurement — stale binaries poisoned rounds 1–11).
4. On AC-5 green: S5c-host implementation (new step block), then S6b ceremony
   (BLESS_GOLDEN=1 flow; section-count diff table + Z-set identity + per-diff-class
   canonical reasoning BEFORE blessing; zero-sites not privileged), then S6c doc impact,
   single full spec-review, acceptance ceremony, close-out per decision 4.

## Known hazards for the resuming session

- Stale `pnp_cli.exe` invalidates e2e numbers silently. Always rebuild + mtime-check.
- Integration bucket had stale-CLI-era flakes; bucket currently 311-pass — keep it that way.
- `expansion_step`/`expansion_bottom_bridge` identifiers do NOT exist in the vendored Orca
  checkout; expansion was ported as-found (per-candidate expand + limiting-area + tail
  opening/closing). Record this discrepancy in the F4 doc row at S6c.
- SlicedRegion literal sweep facts (137 sites, FRU except production exhaustive ones) are
  in the 2026-08-24 transcript; re-derive cheaply with rg if needed rather than trusting.
