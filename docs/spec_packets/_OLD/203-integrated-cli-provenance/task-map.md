# Task Map: 203-integrated-cli-provenance

No `docs/07_implementation_status.md` TASK rows exist for the multi-edition program (verified in `docs/specs/multi-edition-distribution-plan.md` §"Backlog anchoring [FWD]"; `requirements.md` §Packet Metadata forbids creating them here). This packet spans two backlog anchors — `ADR-0056` and `ADR-0057` — so the crosswalk maps ADR decision/consequence clauses to packet steps.

| Backlog anchor | Packet step | Primary docs | Expected code surface | OrcaSlicer refs | Context cost | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| `ADR-0057 §Decision` (new `--no-integrated-modules` disables the integrated tier entirely; composes with `--no-default-module-paths`) | `Step 1` | `docs/adr/0057-three-editions-and-integrated-tier.md`; `201/design.md` item 5 + `201/packet.spec.md` AC-N2 (the "entry point, never a parameter" / `&[]`-identity contract — 201's, not an ADR's) | `crates/slicer-runtime/src/run.rs` (`SliceRunOptions` field + slice-path seam), `crates/pnp-cli/src/main.rs` (`Cmd::Slice` arg + literal) | none | M | Disable = empty registrations **and** empty native entries at the call site, never a `bool` on a loader signature; carries the struct-literal blast radius (measured 2026-08-10: 14 sites across 12 files; 11 FRU need no edit; the 3 full literals + the `impl Default` body need the field — re-derive before editing). Proves `AC-1`, and the slice half of `AC-2` |
| `ADR-0057 §Consequences` (`pnp_cli` module listing/diagnostics surface provenance: integrated vs external) | `Step 2` | `docs/17_agent_debugging.md` §Diagnose, `docs/adr/0056-integrated-modules-native-dispatch.md` Decision item 2 | `crates/slicer-runtime/src/diagnose.rs` (`run_diagnose` signature, integrated-aware load, `modules: [{id, provenance}]`), `crates/pnp-cli/src/main.rs` (`ModuleCmd::Diagnose` arg + caller) | none | S | Provenance mapped by a local `match` over `ModuleProvenance`; `crates/slicer-scheduler/src/manifest.rs` stays untouched. Proves `AC-3`, `AC-N1` |
| `ADR-0057 §Consequences` (…and any shadowing of an integrated module by an external one) | `Step 2` (display) + `Step 4` (evidence) | `docs/adr/0056-integrated-modules-native-dispatch.md` Decision item 2 (external override → WASM path) | existing `diagnostics` array in `crates/slicer-runtime/src/diagnose.rs`; assertion in `crates/pnp-cli/tests/integrated_provenance_tdd.rs` | none | S | 201 owns the warning string `external module X shadows integrated module X` (ADR-0056's consequence bullet verbatim, no inner quoting); this packet asserts it verbatim and never restates it in product code. Proves `AC-N2` |
| `ADR-0056 Decision item 2` (integrated modules are search tier 5; first-root-wins dedup) applied to the CLI introspection surface | `Step 3` | `docs/adr/0056-integrated-modules-native-dispatch.md` Decision item 2, `docs/adr/0057-three-editions-and-integrated-tier.md` | `crates/pnp-cli/src/main.rs` (`cli_integrated_registrations`, `load_dag_modules`, `ModuleCmd::ConfigSchema` arm, four `DagCmd` args), `crates/pnp-cli/Cargo.toml` (dep + `integrated-classic-perimeters` passthrough) | none | S | The `dag` verbs gain integrated-aware *loading* only; `crates/slicer-scheduler/src/dag_cli.rs` output schemas stay unchanged (`requirements.md` §Out of Scope). Proves `AC-4`, `AC-5` |
| `ADR-0057 §Decision` (flag exists so module developers can test pure-external setups on Hybrid/Integrated binaries) — executable evidence | `Step 4` | `CLAUDE.md` §Feature-gated test files, `CLAUDE.md` §Guest WASM Staleness | `crates/pnp-cli/tests/integrated_provenance_tdd.rs` (new, `required-features = ["integrated-classic-perimeters"]`), `crates/pnp-cli/Cargo.toml` `[[test]]` | none | M | Six `assert_cmd` tests driving the real `pnp_cli` binary; every command spells `--features integrated-classic-perimeters` or the target is skipped silently. Proves `AC-2` … `AC-5`, `AC-N1`, `AC-N2` |
| `ADR-0057 §Consequences` (provenance is a user/agent-facing surface) — documentation | `Step 5` | `docs/17_agent_debugging.md` §Diagnose and §"DAG introspection" | `docs/17_agent_debugging.md` only | none | S | Two section edits; §Diagnose exit-code contract (`0`/`1`/`2`) must survive unchanged. Proves `AC-N3` |

Costs copied from `implementation-plan.md` §Per-Step Budget Roll-Up. No row is L and the aggregate is `M`, so no split is required before activation.

Not mapped here (deliberate): `ADR-0056` Decision item 3 (native adapter/dispatch) and Decision item 4 (parity gate) belong to packets 202 and 204; `ADR-0057` §Decision editions table and §Consequences `cargo xtask dist` / CI belong to packet 205.
