# Task Map: 210a-support-planner-coord-t

Single-task packet, but the crosswalk is emitted because `TASK-326` is **net-new** (absent from `docs/07_implementation_status.md` today — re-derive that the slot is still free at the moment you register it, because the highest `TASK-###` moves while parallel packets land) and because the sibling `TASK-327` must be registered by packet `210b`, not by this row.

| docs/07 task ID | Packet step | Primary docs | Expected code surface | OrcaSlicer refs | Context cost | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| `TASK-326` | `Step 1` | `docs/08_coordinate_system.md` §"SDK Helpers", §"Point2 Wrapper" | `modules/core-modules/support-planner/tests/multi_neighbour_mst_tdd.rs`; `#[cfg(test)] mod tests` in `modules/core-modules/support-planner/src/lib.rs` | none | `S` | RED state: the rewritten `aggregate_neighbour_targets` cases plus `point_in_polygon_units_is_exact_on_contour_vertex`, `point_in_any_expoly_excludes_points_inside_holes` (AC-N8) and `node_position_roundtrips_beyond_f32_integer_ceiling` cannot compile against the `f32` node, and `smooth_branches_uses_truncating_integer_average` compiles but fails its assertion. `mm_unit_round_trip_envelope_is_5_120_003_units` (AC-N7) is deliberately GREEN on arrival — it pins a measured constant, not a migration. |
| `TASK-326` | `Step 2` | `docs/08_coordinate_system.md` §"Conversion & Determinism (Normative)", §"Conversion When Porting OrcaSlicer Code", §"Constant Conversion Table" | `modules/core-modules/support-planner/src/lib.rs` — `PlannedSupportNode`, `prim_mst`, `euclidean_distance`, `aggregate_neighbour_targets`, `point_in_polygon_units` (net-new), `point_in_any_expoly`, `clamp_to_avoidance`, `closest_point_on_polygon`, `closest_point_on_segment`, `push_interface_scan_lines`, `first_point_xyw`, the annotated `neighbours_of` binding, the `max_move_xy` / merge comparisons, and the `Point3WithWidth` emission sites | `OrcaSlicerDocumented/src/libslic3r/Support/TreeSupport.hpp` (`SupportNode::position` is `Point`/`coord_t`) | `M` | Proves `DEV-128`'s first half. Owns the complete six-site `PlannedSupportNode` struct-literal blast radius (four in `plan_for_object`, two in the in-file test module, zero under `tests/`) and the `prim_mst_on_two_nodes_returns_one_edge` assertion rewrite from `≈5.0 mm` to exactly `50_000` units. `point_in_any_expoly` is a **retype, not a rewrite** — its hole term (`contour && !any(hole)`) must survive, pinned by AC-N8; AC-7 alone would accept a contour-only body. `pub struct SupportPlanner` gains no field here (that is `210b`'s); its five external consumers are inventoried in `implementation-plan.md` Step 2 for completeness only. |
| `TASK-326` | `Step 3` (**the seam**) | `docs/08_coordinate_system.md` §"Conversion & Determinism (Normative)" | `modules/core-modules/support-planner/src/lib.rs` — `smooth_branches` rewritten once, `split_column_into_chains` (net-new private), `CHAIN_BREAK_THRESHOLD_UNITS` | `OrcaSlicerDocumented/src/libslic3r/Support/TreeSupport.cpp` (`TreeSupport::smooth_nodes` integer `Point` average, truncating `/3`, `max_move = scale_(support_line_width / 2)`) | `M` | **This is why the 210/211 collision was resolvable by ordering rather than by merging.** Both edits to the same twenty lines happen here, once; `210b` adds only a second caller. `smooth_nodes_tdd`'s four cases are the frozen guard. |
| `TASK-326` | `Step 4` | `CLAUDE.md` §Test Discipline | `modules/core-modules/support-planner/tests/orca_parity_tdd.rs` (`overhang_plate_fixture` margin only); `resources/golden/benchy_tree_support_orca_*.txt`; `resources/golden/support_regression_wedge_*.txt` | none | `S` | Sole owner of both frozen self-captured golden pairs and of the shared parity fixture. `isqrt` truncation, exact-integer `point_in_polygon_units` and the integer `max_move_xy` cap can each flip a merge/drop decision, so drift is resolved here and attributed to the retype. Tolerances frozen; any regeneration must be named in `docs/DEVIATION_LOG.md` or AC-8 clause (c) fails. |
| `TASK-326` | `Step 5` | `CLAUDE.md` §"Guest WASM Staleness" | no source change expected; guest rebuild plus `crates/slicer-runtime/tests/integration/support_invariants_wedge_tdd.rs` as a read-only oracle | none | `S` | Proves `DEV-128`'s *purpose*: `branch_endpoints_are_outside_support_collision_outlines` is invariant 2, the exact failure mode the deviation names as its trigger. Must run against a freshly built guest. AC-18's `>= 7` binary-count clause runs here. |
| `TASK-326` | `Step 5b` | `docs/DEVIATION_LOG.md` (`DEV-128`), `docs/07_implementation_status.md` (Workstream 3) | two ledger files plus the `//!` header of `modules/core-modules/support-planner/src/lib.rs` | none | `S` | Closes `DEV-128` only. Every ID and status is re-derived at edit time; nothing is quoted from this packet. `DEV-129`, `docs/15_config_keys_reference.md` and ADR-0010 §Status/§Context belong to `210b`. |

Costs are copied from `implementation-plan.md` §Per-Step Budget Roll-Up. No row is `L`; the aggregate is `M`.

## Sibling and superseded packets

- `docs/spec_packets/210b-support-interface-bottom-layers/` carries `TASK-327` and `DEV-129`. **Do not register `TASK-327` from this packet**, and do not implement `210b` from this session. `210b` depends on this packet being implemented and merged.
- `docs/spec_packets/211-support-interface-bottom-layers/` is `status: superseded` (by the merged `210`, which was subsequently re-split). Its directory is retained for provenance: do not revive it, do not edit it, do not delete it. The full history is in `requirements.md` §Provenance.

## Upstream / downstream

No upstream dependency: this packet depends on nothing.

Downstream: `210b` consumes `split_column_into_chains`, **`point_in_any_expoly`**, `first_point_xyw` and `push_interface_scan_lines` at the signatures listed in `packet.spec.md` §Exports Consumed by 210b. `point_in_any_expoly` — not the ring-level `point_in_polygon_units` — is the model-landing helper, because `LayerCollisionCache.collision_polys` is `Vec<ExPolygon>`; it is hole-honouring, pinned by AC-N8. Nothing else in the 206–212 queue consumes any symbol changed here — `aggregate_neighbour_targets`, `prim_mst`, `point_in_polygon_units`, `clamp_to_avoidance`, `smooth_branches` and `group_branches_into_columns` have no call sites outside `modules/core-modules/support-planner/`.
