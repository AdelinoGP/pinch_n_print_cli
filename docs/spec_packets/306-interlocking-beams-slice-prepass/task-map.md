# Task Map: 306-interlocking-beams-slice-prepass

This packet has no `docs/07_implementation_status.md` task IDs — it is a queue packet from the wayfinder map "Close the OrcaSlicer FFF feature gap". The crosswalk below maps the map's **queue keys** to packet steps instead, and records the one explicit mapping need that requires this file: the packet absorbs a second queue entry (P90 / wayfinder ticket 97) that would otherwise have been authored separately.

| Queue key (map row) | Packet step | Primary docs | Expected code surface | OrcaSlicer refs | Context cost | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| `interlocking_beam` (P89) | `Step 4` (rename + declaration), `Step 2` (generator gate) | `docs/15_config_keys_reference.md` | `crates/slicer-ir/src/resolved_config.rs`, `crates/slicer-core/src/algos/paint_segmentation/mod.rs`, `crates/slicer-core/src/algos/interlocking/mod.rs` | `InterlockingGenerator.cpp`, `MultiMaterialSegmentation.cpp`, `PrintConfig.cpp` | `S` + `M` | The only one of the six already live, under the PnP-invented name `mmu_segmented_region_interlocking_beam`; Step 4 retires that spelling and Step 2 adds its missing first read site |
| `interlocking_beam_width` (P89) | `Step 2` | `docs/08_coordinate_system.md` | `crates/slicer-core/src/algos/interlocking/mod.rs`, `crates/slicer-ir/src/resolved_config.rs` | `InterlockingGenerator.cpp`, `PrintConfig.cpp` | `M` | Drives the voxel cell's XY size and `generate_microstructure`'s split point |
| `interlocking_beam_layer_count` (P89) | `Step 2` | `docs/08_coordinate_system.md` | `crates/slicer-core/src/algos/interlocking/mod.rs`, `crates/slicer-ir/src/resolved_config.rs` | `InterlockingGenerator.cpp`, `PrintConfig.cpp` | `M` | Drives the voxel cell's Z size and the structure-layer stride; AC-6 pins the ceiling division |
| `interlocking_depth` (P90) | `Step 2` | `docs/08_coordinate_system.md` | `crates/slicer-core/src/algos/interlocking/voxel.rs`, `crates/slicer-core/src/algos/interlocking/mod.rs` | `InterlockingGenerator.cpp`, `VoxelUtils.cpp`, `PrintConfig.cpp` | `M` | **The reason P89 and P90 cannot be separate packets** — canonical's P89 enabling gate reads this P90 key |
| `interlocking_orientation` (P90) | `Step 2` | `docs/08_coordinate_system.md` | `crates/slicer-core/src/algos/interlocking/mod.rs` | `InterlockingGenerator.cpp`, `PrintConfig.cpp` | `M` | Threads through every ported function as an apply/unapply rotation pair; no rotation helper exists in `polygon_ops` and the kernel authors one |
| `interlocking_boundary_avoidance` (P90) | `Step 3` | `docs/08_coordinate_system.md` | `crates/slicer-core/src/algos/interlocking/mod.rs` | `InterlockingGenerator.cpp`, `PrintConfig.cpp` | `M` | Selects the whole `air_filtering` branch, including `handle_thin_areas` and `grow_border_areas_perpendicular` |
| — (seam, not a key) | `Steps 5–7` | `docs/04_host_scheduler.md`, `docs/01_system_architecture.md` | `crates/slicer-scheduler/src/execution_plan.rs`, `crates/slicer-runtime/src/builtins/interlocking_producer.rs`, `crates/slicer-runtime/src/prepass.rs`, `crates/slicer-runtime/tests/executor/prepass_interlocking_stage_order_tdd.rs` | `PrintObjectSlice.cpp` | `S` + `M` + `M` | Corrects ticket 04's owner from a guest module to a host prepass built-in; `PrepassStageOutput` (`crates/slicer-core/src/stage_io.rs`) has no `SliceIR` variant, so a module cannot write slices at all. Three steps rather than one because the ≤3-edit cap cannot hold the stage list, the producer and the tests together |
| — (evidence, not a key) | `Steps 8–10` | `docs/DEVIATION_LOG.md`, `docs/adr/`, `docs/04_host_scheduler.md`, `docs/01_system_architecture.md`, `docs/15_config_keys_reference.md` | `crates/slicer-runtime/tests/executor/cube_4color_interlocking_tdd.rs` | — | `S` + `S` + `S` | End-to-end proof on `resources/cube_4color.3mf`, then the ADR and two deviation rows, then the three doc edits |

Costs are copied from `implementation-plan.md` §Per-Step Budget Roll-Up. Aggregate is `M`; no row is L, so no split is required before activation.

## Absorbed queue entry

Wayfinder ticket 97 — "Author packet P90 — Multimaterial / Multimaterial advanced (2/2) — new: interlocking" — is **dissolved into this packet**, not superseded: it is a map ticket, not a packet directory, so nothing under `docs/spec_packets/` changes status. Its three keys appear in the table above under `Step 2` and `Step 3`. The dissolution is recorded in ticket 96's resolution and in the map's Decisions-so-far; ticket 97 itself is closed with a pointer here. The ticket-31 (P24) precedent covers this shape: a queue entry whose keys turn out to belong to another entry's decision point is folded rather than authored.
