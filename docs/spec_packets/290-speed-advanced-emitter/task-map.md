# Task Map: 290-speed-advanced-emitter

Queue packet from the wayfinder map "Close the OrcaSlicer FFF feature gap"; authored under map ticket 64. Single-task packet (no backlog slice); this crosswalk exists for the S0 structural gate and records the map provenance.

| docs/07 task ID | Packet step | Primary docs | Expected code surface | OrcaSlicer refs | Context cost | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| `TASK-000` (queue packet, `task_ids: []`) | Steps 1–1b–4 | `docs/config/host-keys.toml`; `docs/DEVIATION_LOG.md` (DEV-182); `docs/15_config_keys_reference.md` (generated via `cargo xtask gen-config-docs`, Step 1b); `docs/01_system_architecture.md` (claim-system trigger test) | `crates/slicer-ir/src/resolved_config.rs` + `crates/slicer-gcode/src/emit.rs` (post-loop smoothing stage + skip/split arms + bounds) + `crates/slicer-gcode/tests/speed_p57_ers_emission_tdd.rs` (new) + `docs/config/host-keys.toml` | `OrcaSlicerDocumented/src/libslic3r/PrintConfig.cpp` (three declarations + bounds, all scalar) + `OrcaSlicerDocumented/src/libslic3r/GCode.cpp` (`GCode::_do_export` gate + marker emission + `GCode::process_layers` position) + `OrcaSlicerDocumented/src/libslic3r/GCode/PressureEqualizer.cpp` (ctor + `adjust_volumetric_rate` skip list + limiter shape + `output_gcode_line` split rule) | M | P57: Tier B held, owner `crates/slicer-gcode` stands (all canonical reads emission-side; machine-gcode-emit sweep is the wrong seam; no smoother exists anywhere in tree — reuse nothing, fork nothing); membership held 3-in, none shed, none returned (all three live in canonical, all zero-occurrence here); canonical scalarity IS held (all three scalar in `GCodeConfig` — no ticket-125 vector arm); defaults ARE identity (slope `0` = stage does not exist, AC-2 pins it); text-parsing + one-layer-lookbehind not borrowed (IR-native port DEV-182(c)); short-travel/first-layer-travel/wipe-tower arms do not exist for this family (travels never limited). |

Copy costs from `implementation-plan.md`. Split before activation if any row is L or aggregate exceeds M.
