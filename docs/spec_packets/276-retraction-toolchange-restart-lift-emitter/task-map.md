# Task Map: 276-retraction-toolchange-restart-lift-emitter

Use this crosswalk when a packet spans more than one task ID, reopens prior work, or supersedes earlier packet work. This queue packet emits the explicit no-task clause: `task_ids: []`; implementation is recorded against [43 - Author packet P36 - Extruder / Nozzle / Retraction (1/2) - emitter](../specs/orca-feature-gap/issues/43-author-packet-p36-extruder-nozzle-retraction-emitter.md), and the `docs/07_implementation_status.md` crosswalk is N-A. No prior packet is reopened or superseded.

| docs/07 task ID | Packet step | Primary docs | Expected code surface | OrcaSlicer refs | Context cost | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| - (queue packet, `task_ids: []`) | Steps 1-5 | `docs/15_config_keys_reference.md` (generated); `docs/config/host-keys.toml` | `crates/slicer-ir/src/resolved_config.rs` + `crates/slicer-gcode/src/emit.rs` + `modules/core-modules/machine-gcode-emit/{machine-gcode-emit.toml,src/lib.rs,tests/*}` + `docs/DEVIATION_LOG.md` (DEV-171) | `OrcaSlicerDocumented/src/libslic3r/PrintConfig.cpp` (enum strings + defaults) + `GCodeWriter.cpp` (`lazy_lift`/`eager_lift`) + `Extruder.cpp` (deretraction fallback, restart-extra selection) + `GCode.cpp` (`_cut` placeholder publication) | M | Queue packet from the wayfinder map "Close the OrcaSlicer FFF feature gap"; authored under the map's Authoring rules: 8 of 10 P36 keys (all zero-occurrence, Tier B sizing confirmed), `retract_before_wipe` shed to P37 (needs wipe moves), `long_retractions_when_ec` returned to the queue (no geometric decision point), scalar-global with DEV-171 (vector model stays with the ticket-125 ruling). One intended default-output change: toolchange synthesis moves from `retract_length` 2.0 to `retract_length_toolchange` 10.0. |
