# Task Map: 288-walls-flow-compensation-emitter

Queue packet from the wayfinder map "Close the OrcaSlicer FFF feature gap"; authored under map ticket 62. Single-task packet (no backlog slice); this crosswalk exists for the S0 structural gate and records the map provenance.

| docs/07 task ID | Packet step | Primary docs | Expected code surface | OrcaSlicer refs | Context cost | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| `TASK-000` (queue packet, `task_ids: []`) | Steps 1–1b–4 | `docs/config/host-keys.toml`; `docs/DEVIATION_LOG.md` (DEV-180); `docs/15_config_keys_reference.md` (generated via `cargo xtask gen-config-docs`, Step 1b); `docs/01_system_architecture.md` (claim-system trigger test) | `crates/slicer-ir/src/resolved_config.rs` + `crates/slicer-gcode/src/emit.rs` (role-gated multiplier + small-area compensator + bounds) + `crates/slicer-gcode/tests/walls_p55_flow_emission_tdd.rs` (new) + `docs/config/host-keys.toml` | `OrcaSlicerDocumented/src/libslic3r/PrintConfig.cpp` (seven declarations + bounds + ten-pair model default) + `OrcaSlicerDocumented/src/libslic3r/GCode.cpp` (`GCode::extrude_entity` role mapping, gate split, composition position; `GCode::_needSAFC` + `SmallAreaInfillFlowCompensator::modify_flow` role set + per-segment position; `reduce_crossing_wall` detour reads as return evidence) | M | P55: Tier B held, owner `crates/slicer-gcode` stands (all canonical reads emission-time; machine-gcode-emit sweep is the wrong seam); membership re-sized 9→7+1 (five ratios + small-area pair in; `set_other_flow_ratios` already shed to 287 as a split-boundary adjustment with a backward FORWARD-DEP; `reduce_crossing_wall` returned — no avoidance planner, declaration-only per rule 1, shared missing feature with ticket-61's detour key); scalar-global is parity (all canonical scalars — no ticket-125 vector model); defaults identity (no CONFIG_BLOCK change, table untouched); pattern gate omitted DEV-180(c) (emitter has no pattern visibility; default holders rectilinear-family); print floor `0.01` adopted exactly; model default verbatim. |

Copy costs from `implementation-plan.md`. Split before activation if any row is L or aggregate exceeds M.
