# Task Map: support-interface-spacing-and-loops

Use this crosswalk when a packet spans more than one task ID, reopens prior work, or supersedes an earlier packet. **This packet emits the template's own skip clause** for the `docs/07` crosswalk: it is a single-coherent-slice queue packet with `task_ids: []` (queue precedent). It does, however, supersede prior work: together with `260b-support-interface-fill-claim-holders` it replaces the pre-rules packet `260-support-interface-keys`, which was authored before the map's Authoring rules 1–7 and dispositioned two of its four keys "declared-with-gap". That packet's directory no longer exists; its canonical evidence is carried forward in `requirements.md` §Per-Key Canonical Evidence. Implementation is recorded against wayfinder ticket 18 (`docs/specs/orca-feature-gap/issues/18-author-packet-p11-support-interface-support-planner.md`).

| docs/07 task ID | Packet step | Primary docs | Expected code surface | OrcaSlicer refs | Context cost | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| — (queue packet, `task_ids: []`) | Steps 1–6 | `docs/15_config_keys_reference.md` (generated), `docs/03_wit_and_manifest.md`, `docs/08_coordinate_system.md` | `modules/core-modules/{traditional-support,tree-support}/{*.toml,src/lib.rs,tests/*}`, `crates/slicer-core/src/support_regularize.rs`, scheduler + runtime integration arms, the `orca-matched-config.json` fixture | `PrintConfig.cpp` (three `def()`s), `Support/SupportParameters.hpp` (`SupportParameters::SupportParameters`), `Support/SupportCommon.cpp` (`generate_support_toolpaths`, `LoopInterfaceProcessor::generate`), `Support/SupportMaterial.hpp` (`has_contact_loops`), `Support/TreeSupport.cpp` (`generate_toolpaths`) | M | Queue packet from the wayfinder map "Close the OrcaSlicer FFF feature gap"; no TASK rows; re-derive the crosswalk question at completion time per the ledger-fact rule |
