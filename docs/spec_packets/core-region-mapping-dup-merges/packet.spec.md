---
status: draft
packet: core-region-mapping-dup-merges
task_ids:
  - core/DUP-CORE
backlog_source: docs/specs/test-quality-remediation-plan.md
context_cost_estimate: S
copy_note: Approved core-region-mapping-dup-merges queue entry; task IDs are program item IDs, not TASK-### mappings.
---

# Packet Contract: core-region-mapping-dup-merges

## Goal

Merge the two textually duplicated region-mapping cross-product tests `region_mapping_enumerate_chains` and `region_mapping_cross_product_entry_count` in `crates/slicer-core/tests/algo_region_mapping_tdd.rs` into the plan-named survivor `region_mapping_two_semantics_produces_cross_product_cardinality`, preserving the exact chain-set, cardinality, and ordering coverage of the file while retaining the absorbed AC-4 formula and AC-3 enumeration alias prose.

## Scope Boundaries

Edit only `crates/slicer-core/tests/algo_region_mapping_tdd.rs`: delete the two duplicate `#[test]` fns and extend the survivor's doc comment with the absorbed AC-4 formula prose. The 21 distinct tests — including `region_mapping_chains_ordered_by_aggregated_region_split_canonical_order` — stay byte-identical; no production code, other core test files, new tests, or behavior changes are authorized, and the implementation-time §7 `core` ledger row bookkeeping in `docs/specs/test-quality-remediation-plan.md` is the only documentation surface.

## Prerequisites and Blockers

- Depends on: None as an implementation prerequisite; `core-flow-consolidation` and `core-bridge-dup-merge` are generation-order dependencies only and export no API, no test files, and no symbols.
- Unblocks: `core-support-dup-merges` packet generation only.
- Activation blockers: Independent `spec-review --preflight`; no scope expansion.

## Acceptance Criteria

- **AC-1. Given** the merge deletes exactly the two duplicate `#[test]` fns from a 24-test target, **when** the feature-correct target is listed and the file is inspected, **then** the target discovers exactly `22` tests, the file contains exactly `22` `#[test]` attributes, the survivor `region_mapping_two_semantics_produces_cross_product_cardinality` is discovered exactly once, the 21 sibling tests each remain discovered exactly once, and the absorbed names `region_mapping_enumerate_chains` and `region_mapping_cross_product_entry_count` appear in neither the discovery list nor the file. | `bash -lc 'set -euo pipefail; mkdir -p target; cargo test -p slicer-core --features host-algos --test algo_region_mapping_tdd -- --list 2>&1 | tee target/test-output.log >/dev/null; test "$(grep -c ": test$" target/test-output.log || true)" -eq 22; test "$(grep -c "^region_mapping_enumerate_chains: test$" target/test-output.log || true)" -eq 0; test "$(grep -c "^region_mapping_cross_product_entry_count: test$" target/test-output.log || true)" -eq 0; test "$(grep -c "^region_mapping_two_semantics_produces_cross_product_cardinality: test$" target/test-output.log || true)" -eq 1; test "$(grep -c "^#\[test\]" crates/slicer-core/tests/algo_region_mapping_tdd.rs || true)" -eq 22; ! rg -q "region_mapping_enumerate_chains|region_mapping_cross_product_entry_count" crates/slicer-core/tests/algo_region_mapping_tdd.rs; for n in modifier_sub_regions_carry_only_their_own_delta identical_modifier_footprints_first_equal_priority_wins modifier_parent_bound_only_applies_to_effective_parameter_footprints painted_modifier_entries_are_spatially_composed painted_variant_ids_stay_outside_modifier_namespace painted_variant_id_at_modifier_parent_limit_never_becomes_sentinel region_map_has_expected_entry_count region_map_keys_are_correct region_map_cap_exceeded_returns_error empty_layer_plan_produces_empty_map region_mapping_paint_scan region_mapping_applies_per_tool_config_overlay_to_painted_tool region_mapping_per_tool_config_overrides_paint_semantic_on_same_key region_mapping_emits_empty_chain_for_unpainted_object region_mapping_emits_n_plus_1_chains_for_single_semantic_n_distinct_values region_mapping_two_semantics_produces_cross_product_cardinality region_mapping_chains_ordered_by_aggregated_region_split_canonical_order region_mapping_two_objects_with_disjoint_paint_emit_per_object_chains region_mapping_chain_derived_overlay_matches_layer_wide_overlay_when_aggregation_empty region_mapping_empty_aggregation_no_variants region_mapping_no_scalar_in_variant_chain region_mapping_config_interning; do test "$(grep -c "^${n}: test$" target/test-output.log || true)" -eq 1; done'`
- **AC-2. Given** the survivor must carry the union of both absorbed tests' assertions and every distinct sibling must keep its own contract, **when** the feature-correct target executes, **then** the full run reports `test result: ok. 22 passed; 0 failed`, the survivor passes by exact name, and the ordering witness `region_mapping_chains_ordered_by_aggregated_region_split_canonical_order` passes by exact name. | `bash -lc 'set -euo pipefail; mkdir -p target; cargo test -p slicer-core --features host-algos --test algo_region_mapping_tdd -- --nocapture 2>&1 | tee target/test-output.log >/dev/null; rg -q "test result: ok\. 22 passed; 0 failed" target/test-output.log; rg -q "test region_mapping_two_semantics_produces_cross_product_cardinality \.\.\. ok" target/test-output.log; rg -q "test region_mapping_chains_ordered_by_aggregated_region_split_canonical_order \.\.\. ok" target/test-output.log'`
- **AC-3. Given** the survivor's doc comment already carries the AC-3 enumeration wording (`Verifies SET membership of the enumerated chains.`) and the merge adds only the candidate-unique AC-4 formula prose, **when** the survivor's own block (its doc comment plus body, from the `/// AC-9 (c) / AC-3 / AC-4:` comment through the test's closing brace) is inspected after the merge, **then** the block retains the analytic cardinality assertion `assert_eq!(region_map.entries.len(), 6);`, the full six-chain set assertion with the `"expected cross-product chains"` message, the absorbed AC-4 formula prose `entries.len() == layers × active_regions × ∏(1 + K_i)` in the doc comment, and the pre-existing AC-3 enumeration wording `SET membership of the enumerated chains`; no other function's comment carries the AC-4 formula prose. | `bash -lc 'set -euo pipefail; F=crates/slicer-core/tests/algo_region_mapping_tdd.rs; mkdir -p target; awk "/^\/\/\/ AC-9 \(c\) \/ AC-3 \/ AC-4:/{flag=1} flag{print} flag&&/^}/{exit}" "$F" > target/ac3_window.txt; test -s target/ac3_window.txt; rg -q -F "assert_eq!(region_map.entries.len(), 6);" target/ac3_window.txt; rg -q -F "expected cross-product chains" target/ac3_window.txt; rg -q -F "entries.len() == layers" target/ac3_window.txt; rg -q -F "SET membership of the enumerated chains" target/ac3_window.txt; test "$(rg -c -F "entries.len() == layers" "$F" || echo 0)" -eq 1'`
- **AC-4. Given** the region-mapping slice is implemented without closing unrelated core work, **when** the narrow ledger update is recorded, **then** the `core` row in §7 is the sole selected row, its table header has exactly six columns `Wave`, `State`, `Retired/changed symbols`, `Surviving/new coverage`, `Validation`, and `Remaining gap`, its row has exactly six cells, has state `partial`, names `core-region-mapping-dup-merges`, `region_mapping_enumerate_chains`, `region_mapping_cross_product_entry_count`, `region_mapping_two_semantics_produces_cross_product_cardinality`, and `algo_region_mapping_tdd` in its changed-symbols cell, names `region_mapping_two_semantics_produces_cross_product_cardinality` and `region_mapping_chains_ordered_by_aggregated_region_split_canonical_order` in its coverage cell, records `cargo test`, `cargo check --workspace --all-targets`, `cargo clippy --workspace --all-targets -- -D warnings`, `cargo xtask check-literals`, and `check-test-quality` in its validation cell, and names the next pending core slice `core-support-dup-merges` in its remaining-gap cell. | `bash -lc 'set -euo pipefail; python -c "from pathlib import Path; t=Path(\"docs/specs/test-quality-remediation-plan.md\").read_text(); s=t.split(\"## 7. Ledger\",1)[1].split(\"\\n## \",1)[0]; rows=[x for x in s.splitlines() if x.startswith(\"|\") and not x.startswith(\"|---\")]; headers=[x for x in rows if x.split(\"|\")[1].strip()==\"Wave\"]; assert len(headers)==1 and [x.strip() for x in headers[0].split(\"|\")[1:-1]]==[\"Wave\",\"State\",\"Retired/changed symbols\",\"Surviving/new coverage\",\"Validation\",\"Remaining gap\"]; core=[x for x in rows if x.split(\"|\")[1].strip()==\"core\"]; assert len(core)==1; c=[x.strip() for x in core[0].split(\"|\")[1:-1]]; assert len(c)==6; wave,state,changed,coverage,validation,gap=c; assert wave==\"core\" and state==\"partial\"; assert all(x in changed for x in (\"core-region-mapping-dup-merges\",\"region_mapping_enumerate_chains\",\"region_mapping_cross_product_entry_count\",\"region_mapping_two_semantics_produces_cross_product_cardinality\",\"algo_region_mapping_tdd\")); assert all(x in coverage for x in (\"region_mapping_two_semantics_produces_cross_product_cardinality\",\"region_mapping_chains_ordered_by_aggregated_region_split_canonical_order\")); assert all(x in validation for x in (\"cargo test\",\"cargo check --workspace --all-targets\",\"cargo clippy --workspace --all-targets -- -D warnings\",\"cargo xtask check-literals\",\"check-test-quality\")); assert \"core-support-dup-merges\" in gap; print(\"PASS: anchored core ledger row has six validated cells\")"'`

## Negative Test Cases

- **AC-N1. Given** the survivor's oracle must not be weakened to a count-only or subset check by the merge, **when** the merged survivor runs and the file is inspected, **then** the full six-chain set assertion with the `"expected cross-product chains"` message and the analytic `assert_eq!(region_map.entries.len(), 6);` assertion both remain in the survivor, no new `#[test]` fn is added (the file holds exactly `22`), and the merge deletes no test other than the two absorbed names (the 21 named siblings in AC-1 each still discover exactly once). | `bash -lc 'set -euo pipefail; mkdir -p target; F=crates/slicer-core/tests/algo_region_mapping_tdd.rs; cargo test -p slicer-core --features host-algos --test algo_region_mapping_tdd -- --nocapture 2>&1 | tee target/test-output.log >/dev/null; rg -q "test result: ok\. 22 passed; 0 failed" target/test-output.log; rg -q -F "assert_eq!(region_map.entries.len(), 6);" "$F"; rg -q -F "assert_eq!(actual, expected, \"expected cross-product chains\");" "$F"; test "$(grep -c "^#\[test\]" "$F" || true)" -eq 22; ! rg -q "region_mapping_enumerate_chains|region_mapping_cross_product_entry_count" "$F"'`

## Verification

- `cargo check --workspace --all-targets`
- `cargo clippy --workspace --all-targets -- -D warnings`
- `bash -lc 'set -euo pipefail; mkdir -p target; cargo test -p slicer-core --features host-algos --test algo_region_mapping_tdd -- --list 2>&1 | tee target/test-output.log >/dev/null; test "$(grep -c ": test$" target/test-output.log || true)" -eq 22; cargo test -p slicer-core --features host-algos --test algo_region_mapping_tdd -- --nocapture 2>&1 | tee target/test-output.log >/dev/null; rg -q "test result: ok\. 22 passed; 0 failed" target/test-output.log; rg -q "test region_mapping_two_semantics_produces_cross_product_cardinality \.\.\. ok" target/test-output.log; ! rg -q "region_mapping_enumerate_chains|region_mapping_cross_product_entry_count" crates/slicer-core/tests/algo_region_mapping_tdd.rs'` — the primary contract: exact 22-count discovery with both absorbed names absent from discovery and file, the full target passing, and the survivor passing (same command as AC-1/AC-2's shared execution core, repeated rather than referenced).

## Authoritative Docs

- `docs/specs/test-quality-remediation-plan.md` §1, §4, §5.1, §6, Packet Queue rows #1–#5, and the resume/continuation-approval paragraphs - direct bounded reads.
- `docs/22_test_quality.md` §§1–5 - direct bounded read; duplicate-merge survivor-map discipline and the repaired-shape example that names the survivor.
- `docs/adr/0064-existing-tests-retire-if-unjustified.md` - direct read; each absorbed test needs a named survivor protecting its regression input.
- `docs/adr/0065-test-quality-gate-with-delayed-enforce-mode.md` - direct read; report-mode gate status.
- `docs/spec_packets/_OLD/93_region-mapping-cross-product.md` - direct read (63 lines); provenance of the `AC-3`/`AC-4`/`AC-9` acceptance aliases and the `∏(1 + K_i)` cardinality invariant the absorbed AC-4 prose locks.
- `docs/00_project_overview.md` normative document map and `.agents/doc-index.md` - direct reads.
- `.agents/skills/spec-review/references/preflight-gate.md` S0–S8 - direct read; symbol and target checks.

## Doc Impact Statement (Required)

Specific implementation-time documentation impact: `docs/specs/test-quality-remediation-plan.md` §7 Ledger - the implementation worker must update only the `core` ledger row with this region-mapping slice's partial state, retired/changed symbols (`region_mapping_enumerate_chains` and `region_mapping_cross_product_entry_count` absorbed by `region_mapping_two_semantics_produces_cross_product_cardinality` in `algo_region_mapping_tdd`), surviving coverage, actual validations, and the next pending core slice `core-support-dup-merges` as the remaining gap; it must not close the core wave or rewrite the queue/other waves. Verification is the AC-4 ledger parser command defined above. This is bookkeeping, not a production contract change.

<!-- snippet: context-discipline -->
## Context Discipline Note

This packet was generated against the context_discipline preamble shared by `spec-packet-generator`, `swarm`, and `spec-review`. Downstream agents implementing or reviewing this packet must:

- treat `design.md`'s code change surface as the authoritative files-in-scope list
- honor `design.md`'s out-of-bounds list — those files must not be loaded directly
- delegate every cargo run and authoritative-doc fact-check
- obey the shared absolute context bands: 120k reading budget with hand-off at 150k (standard); the extended band (240k reading / 300k hard stop) only via swarm's escalation protocol

Aggregate context cost above is the sum of per-step costs in `implementation-plan.md`. If any single step is rated L, the packet must be split before activation (an extended-band run may carry a single L step only when `design.md` justifies why it cannot be split).
