# Task Map: 267-printer-machine-power-recovery-emitter

Use this crosswalk when a packet spans more than one task ID, reopens prior work, or supersedes earlier packet work. This queue packet emits the explicit no-task clause: `task_ids: []`; implementation is recorded against [25 - Author packet P18 - Printer / Machine / Power / recovery - emitter](../specs/orca-feature-gap/issues/25-author-packet-p18-printer-machine-power-recovery-emitter.md), and the `docs/07_implementation_status.md` crosswalk is N-A. The closed `disable_m73` predecessor `TASK-279` is a historical backlog row, not an ownership row.

| docs/07 task ID | Packet step | Primary docs | Expected code surface | OrcaSlicer refs | Context cost | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| - (queue packet, `task_ids: []`) | Steps 1-5 | `docs/15_config_keys_reference.md` (generated); `docs/config/host-keys.toml` | `modules/core-modules/machine-gcode-emit/{machine-gcode-emit.toml,Cargo.toml,src/lib.rs,tests/*}` + `crates/slicer-gcode/src/emit.rs` + `crates/slicer-ir/src/resolved_config.rs` + `crates/slicer-runtime/src/run.rs` + `crates/slicer-runtime/tests/unit/host_keys_doc_lock_tdd.rs` + the scheduler bounds arm | `OrcaSlicerDocumented/src/libslic3r/GCode.cpp` (`print_machine_envelope`, `_do_export` recovery call sites) + `GCodeWriter.cpp` (`enable_power_loss_recovery`) + `PrintConfig.cpp`/`PrintConfig.hpp` (declarations) | S/M | Queue packet from the wayfinder map "Close the OrcaSlicer FFF feature gap"; authored under the map's Authoring rules: **mixed A/B** (was all-A), `silent_mode` returned to the queue (needs a per-variant machine-limit model, follow-up ticket 117), the envelope is the PnP scalar subset with missing groups recorded as divergences, and the postpass PrintStart rule changes to keep the envelope ahead of the start template. |