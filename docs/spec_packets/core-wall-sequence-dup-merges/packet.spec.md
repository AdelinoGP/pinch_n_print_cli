---
status: draft
packet: core-wall-sequence-dup-merges
task_ids:
  - core/DUP-CORE (wall sequence)
backlog_source: docs/specs/test-quality-remediation-plan.md §5.1 DUP-CORE wall-sequence slice
depends_on:
  - core-support-dup-merges
context_cost_estimate: S
copy_note: Approved core-wall-sequence-dup-merges queue entry; task IDs are program wave/item IDs, not TASK-### mappings.
---

# Packet Contract: core-wall-sequence-dup-merges

## Goal

Consolidate the three wall-sequence inline twins into their strictly stronger `wall_sequence_reorder_tdd` survivors, import the inline module's distinct N==2 sandwich case, and delete the complete duplicate `#[cfg(test)]` module while preserving all three modes, both edge cases, and the two-wall sandwich input.

## Scope Boundaries

Edit only `crates/slicer-core/tests/wall_sequence_reorder_tdd.rs` and the `#[cfg(test)] mod wall_sequence_reorder_tests` block in `crates/slicer-core/src/perimeter_utils.rs`, then—after the merge passes—the §7 `core` ledger row in `docs/specs/test-quality-remediation-plan.md`. The production bodies of `wall_sequence_reorder`, the `WallSequence` enum, every other `slicer-core` test, the Packet Queue, all contracts, and all other documentation remain unchanged.

## Prerequisites and Blockers

- Depends on: `core-support-dup-merges` for queue-generation order only; that draft packet exports no API, test file, or production symbol.
- Unblocks: `core-geometry-dup-review` packet generation only.
- Activation blockers: independent `spec-review --preflight`; no scope expansion.

## Acceptance Criteria

- **AC-1. Given** `wall_sequence_reorder_tdd.rs` has 5 pre-edit tests and `perimeter_utils.rs` has 4 pre-edit inline tests, **when** the merge completes and the feature-correct (ungated) target is listed and executed, **then** exactly 6 tests are discovered and pass in the TDD target, `inner_outer_inner_with_two_walls_swaps_outer_and_first_inner` appears exactly once there, the inline source contains zero `#[test]` attributes and no `#[cfg(test)]` module, and the other 5 TDD test names remain discovered exactly once. | `bash -lc 'set -euo pipefail; mkdir -p target; T=crates/slicer-core/tests/wall_sequence_reorder_tdd.rs; S=crates/slicer-core/src/perimeter_utils.rs; cargo test -p slicer-core --test wall_sequence_reorder_tdd -- --list 2>&1 | tee target/test-output.log >/dev/null; test "$(grep -c ": test$" target/test-output.log || true)" -eq 6; for n in inner_outer_canonical_order outer_inner_reversed_order inner_outer_inner_sandwich_order empty_walls_is_noop single_wall_unchanged_for_all_modes inner_outer_inner_with_two_walls_swaps_outer_and_first_inner; do test "$(grep -c "^${n}: test$" target/test-output.log || true)" -eq 1; done; test "$(grep -Ec "^[[:space:]]*#\[test\]" "$S" || true)" -eq 0; if rg -q "#\[cfg\(test\)\]|mod wall_sequence_reorder_tests" "$S"; then echo "inline test module still present"; exit 1; fi; cargo test -p slicer-core --test wall_sequence_reorder_tdd -- --nocapture 2>&1 | tee target/test-output.log >/dev/null; rg -q "test result: ok\. 6 passed; 0 failed" target/test-output.log'`
- **AC-2. Given** the three inline tests duplicate the TDD tests' identical 3-wall fixture while asserting only `perimeter_index` slots, **when** the merge is complete, **then** the survivors `inner_outer_canonical_order`, `outer_inner_reversed_order`, and `inner_outer_inner_sandwich_order` each retain their exact per-slot `perimeter_index` equality for all three slots and the loop-type assertions of the TDD originals; the absorbed inline names `inner_outer_is_canonical_no_reorder`, `outer_inner_reverses`, and `inner_outer_inner_sandwich` appear zero times in the tree. | `bash -lc 'set -euo pipefail; F=crates/slicer-core/tests/wall_sequence_reorder_tdd.rs; for n in inner_outer_canonical_order outer_inner_reversed_order inner_outer_inner_sandwich_order; do awk "/^fn ${n}\(\)/{flag=1} flag{print} flag&&/^}$/{exit}" "$F" > target/wall-seq-survivor.txt; test -s target/wall-seq-survivor.txt; for f in 0 1 2; do test "$(tr -d '
 
' < target/wall-seq-survivor.txt | grep -o "assert_eq!(walls\[$f\].perimeter_index" | wc -l)" -eq 1; done; grep -q "assert_eq!(walls\[0\].loop_type" target/wall-seq-survivor.txt; done; rg -q -F "three_wall_set()" "$F"; if rg -q "fn inner_outer_is_canonical_no_reorder|fn outer_inner_reverses|fn inner_outer_inner_sandwich" crates/slicer-core/src/perimeter_utils.rs "$F"; then echo "absorbed name still present"; exit 1; fi; rm -f target/wall-seq-survivor.txt'`
- **AC-3. Given** the inline module owns the only N==2 sandwich case, **when** it migrates to the TDD home, **then** the migrated test constructs exactly `[Outer(perimeter_index=0), Inner(perimeter_index=1)]`, calls `wall_sequence_reorder(&mut walls, WallSequence::InnerOuterInner, &[])`, asserts `walls[0].perimeter_index == 1` and `walls[1].perimeter_index == 0`, and its doc comment states the `[Inner_0, Outer]` result for N == 2. | `bash -lc 'set -euo pipefail; F=crates/slicer-core/tests/wall_sequence_reorder_tdd.rs; awk "/^fn inner_outer_inner_with_two_walls_swaps_outer_and_first_inner\(\)/{flag=1} flag{print} flag&&/^}$/{exit}" "$F" > target/wall-seq-two-wall.txt; test -s target/wall-seq-two-wall.txt; rg -q -F "make_wall(0, LoopType::Outer" target/wall-seq-two-wall.txt; rg -q -F "make_wall(1, LoopType::Inner" target/wall-seq-two-wall.txt; rg -q -F "WallSequence::InnerOuterInner" target/wall-seq-two-wall.txt; tr -d "
 
" < target/wall-seq-two-wall.txt | grep -q -F "assert_eq!(walls[0].perimeter_index, 1)"; tr -d "
 
" < target/wall-seq-two-wall.txt | grep -q -F "assert_eq!(walls[1].perimeter_index, 0)"; grep -q -F "N == 2" target/wall-seq-two-wall.txt; rm -f target/wall-seq-two-wall.txt'`
- **AC-4. Given** `empty_walls_is_noop` and `single_wall_unchanged_for_all_modes` have no inline twin and guard no-op boundaries, **when** the merge completes, **then** both tests remain unchanged in the TDD file: the empty-input test still passes an empty `Vec<WallLoop>` with mode `InnerOuterInner` and asserts emptiness, and the single-wall test still iterates all three `WallSequence` variants over a perimeter_index-42 outer wall and asserts length 1 with index 42. | `bash -lc 'set -euo pipefail; F=crates/slicer-core/tests/wall_sequence_reorder_tdd.rs; awk "/^fn empty_walls_is_noop\(\)/{flag=1} flag{print} flag&&/^}$/{exit}" "$F" > target/wall-seq-empty.txt; awk "/^fn single_wall_unchanged_for_all_modes\(\)/{flag=1} flag{print} flag&&/^}$/{exit}" "$F" > target/wall-seq-single.txt; test -s target/wall-seq-empty.txt; test -s target/wall-seq-single.txt; rg -q -F "let mut walls: Vec<WallLoop> = vec![];" target/wall-seq-empty.txt; rg -q -F "WallSequence::InnerOuterInner" target/wall-seq-empty.txt; rg -q -F "assert!(walls.is_empty());" target/wall-seq-empty.txt; rg -q -F "WallSequence::InnerOuter," target/wall-seq-single.txt; rg -q -F "WallSequence::OuterInner," target/wall-seq-single.txt; rg -q -F "WallSequence::InnerOuterInner," target/wall-seq-single.txt; rg -q -F "make_wall(42, LoopType::Outer" target/wall-seq-single.txt; rg -q -F "assert_eq!(walls[0].perimeter_index, 42);" target/wall-seq-single.txt; rm -f target/wall-seq-empty.txt target/wall-seq-single.txt'`
- **AC-5. Given** the census records `wall_sequence_reorder_tdd` with `required: []` and its `[[test]]` stanza in `crates/slicer-core/Cargo.toml` carries no `required-features`, **when** registration is reconciled, **then** the census still records `required == []` for the target, the stanza remains without `required-features`, and every packet command runs without a feature flag; no `host-algos` gate is introduced or claimed. | `bash -lc 'set -euo pipefail; python -c "import json,pathlib,re; d=json.loads(pathlib.Path(\"docs/specs/test-quality-remediation-census.json\").read_text())[\"slicer-core\"]; got={e[\"target\"]:e[\"required\"] for e in d}; assert got.get(\"wall_sequence_reorder_tdd\")==[], got; t=pathlib.Path(\"crates/slicer-core/Cargo.toml\").read_text(); s=re.search(r\"\\[\\[test\\]\\]\\s*name = \\\"wall_sequence_reorder_tdd\\\"\\s*\\n([^\\[]*)\", t); assert s is not None; assert \"required-features\" not in s.group(1); print(\"PASS: wall_sequence_reorder_tdd stays ungated in census and Cargo\")"'`
- **AC-6. Given** report mode is the current test-quality enforcement mode, **when** the gate scans exactly the two touched Rust files, **then** it reports `0 finding(s) in 0 file(s)`; this packet does not claim the full `slicer-core` wave has reached its crate-wide zero-findings exit. | `bash -lc 'set -euo pipefail; mkdir -p target; cargo xtask check-test-quality --report crates/slicer-core/tests/wall_sequence_reorder_tdd.rs crates/slicer-core/src/perimeter_utils.rs 2>&1 | tee target/test-output.log >/dev/null; rg -q -F "check-test-quality: 0 finding(s) in 0 file(s) [report mode]" target/test-output.log'`
- **AC-7. Given** the six-column §7 remediation ledger and this wall-sequence slice implemented without closing unrelated core work, **when** the section-anchored `core` row is parsed, **then** it is the sole `core` row, remains `partial`, records this packet's absorbed and surviving tests plus all required validation classes, and names `core-geometry-dup-review` as remaining work without changing the Packet Queue. | `bash -lc 'set -euo pipefail; python -c "from pathlib import Path; t=Path(\"docs/specs/test-quality-remediation-plan.md\").read_text(encoding=\"utf-8\"); s=t.split(\"## 7. Ledger\",1)[1].split(\"\\n## \",1)[0]; rows=[x for x in s.splitlines() if x.startswith(\"|\") and not x.startswith(\"|---\")]; headers=[x for x in rows if x.split(\"|\")[1].strip()==\"Wave\"]; assert len(headers)==1 and [x.strip() for x in headers[0].split(\"|\")[1:-1]]==[\"Wave\",\"State\",\"Retired/changed symbols\",\"Surviving/new coverage\",\"Validation\",\"Remaining gap\"]; core=[x for x in rows if x.split(\"|\")[1].strip()==\"core\"]; assert len(core)==1; c=[x.strip() for x in core[0].split(\"|\")[1:-1]]; assert len(c)==6; wave,state,changed,coverage,validation,gap=c; assert wave==\"core\" and state==\"partial\"; assert all(x in changed for x in (\"core-wall-sequence-dup-merges\",\"inner_outer_is_canonical_no_reorder\",\"outer_inner_reverses\",\"inner_outer_inner_sandwich\",\"inner_outer_inner_with_two_walls_swaps_outer_and_first_inner\")); assert all(x in coverage for x in (\"inner_outer_canonical_order\",\"outer_inner_reversed_order\",\"inner_outer_inner_sandwich_order\",\"inner_outer_inner_with_two_walls_swaps_outer_and_first_inner\",\"empty_walls_is_noop\",\"single_wall_unchanged_for_all_modes\")); assert all(x in validation for x in (\"cargo test\",\"cargo check --workspace --all-targets\",\"cargo clippy --workspace --all-targets -- -D warnings\",\"cargo xtask check-literals\",\"check-test-quality\")); assert \"core-geometry-dup-review\" in gap; q=t.split(\"## Packet Queue\",1)[1]; qrows=[x for x in q.splitlines() if x.startswith(\"|\") and not x.startswith(\"|---\")]; assert [x.strip() for x in qrows[0].split(\"|\")[1:-1]]==[\"#\",\"packet slug\",\"goal (one sentence)\",\"task ids\",\"depends on\",\"status\",\"packet dir\"]; assert any(\"core-wall-sequence-dup-merges\" in x for x in qrows); print(\"PASS: anchored core ledger row has six partial-state evidence cells; Packet Queue table intact\")"'`

## Negative Test Cases

- **AC-N1. Given** only the three inline twins are authorized for removal and only the N==2 case may migrate, **when** the survivor-map guard checks the final sources, **then** the complete ordered 6-name TDD roster and the inline source's zero-test state must match exactly, each retained assertion fragment must occur in its named survivor body, the three absorbed inline names must be absent repo-wide, and any unaccounted rename/removal, lost loop-type/identity/edge assertion, or surviving inline definition makes the command fail; the valid tree reconciles `5 → 6` and inline `4 → 0`. | `bash -lc 'set -euo pipefail; python -c "import pathlib,re; o=pathlib.Path(\"crates/slicer-core/tests/wall_sequence_reorder_tdd.rs\").read_text(); s=pathlib.Path(\"crates/slicer-core/src/perimeter_utils.rs\").read_text(); names=lambda x: re.findall(r\"(?m)^\\s*#\\[test\\]\\s*\\n\\s*fn\\s+([A-Za-z0-9_]+)\",x); body=lambda x,n: x.split(\"fn \"+n+\"()\",1)[1].split(\"\\n}\",1)[0]; expected=[\"inner_outer_canonical_order\",\"outer_inner_reversed_order\",\"inner_outer_inner_sandwich_order\",\"empty_walls_is_noop\",\"single_wall_unchanged_for_all_modes\",\"inner_outer_inner_with_two_walls_swaps_outer_and_first_inner\"]; got=names(o); assert got==expected,(got,expected); assert len(names(s))==0 and \"#[cfg(test)]\" not in s and \"mod wall_sequence_reorder_tests\" not in s; frags={\"inner_outer_canonical_order\":[\"perimeter_index, 0\",\"perimeter_index, 1\",\"perimeter_index, 2\",\"LoopType::Outer\"],\"outer_inner_reversed_order\":[\"perimeter_index, 2\",\"perimeter_index, 1\",\"perimeter_index, 0\"],\"inner_outer_inner_sandwich_order\":[\"perimeter_index, 1\",\"perimeter_index, 0\",\"perimeter_index, 2\"],\"inner_outer_inner_with_two_walls_swaps_outer_and_first_inner\":[\"perimeter_index, 1\",\"perimeter_index, 0\"]}; [body(o,n).index(f) for n,fs in frags.items() for f in fs]; assert "inner_outer_is_canonical_no_reorder" not in o and not re.search(r"fn outer_inner_reverses\b|fn inner_outer_inner_sandwich\b", o+s) and "inner_outer_inner_with_two_walls_swaps_outer_and_first_inner" not in s; print(\"PASS: 5 → 6 and inline 4 → 0 survivor map holds with all fragments\")"'`

## Verification

- `bash -lc 'set -euo pipefail; mkdir -p target; cargo test -p slicer-core --test wall_sequence_reorder_tdd -- --nocapture 2>&1 | tee target/test-output.log >/dev/null; rg -q "test result: ok\. 6 passed; 0 failed" target/test-output.log'`
- `cargo check --workspace --all-targets && cargo clippy --workspace --all-targets -- -D warnings && cargo xtask check-literals`
- `bash -lc 'set -euo pipefail; mkdir -p target; cargo xtask check-test-quality --report crates/slicer-core/tests/wall_sequence_reorder_tdd.rs crates/slicer-core/src/perimeter_utils.rs 2>&1 | tee target/test-output.log >/dev/null; rg -q -F "check-test-quality: 0 finding(s) in 0 file(s) [report mode]" target/test-output.log'`

## Authoritative Docs

- `docs/specs/test-quality-remediation-plan.md` §§1–4, §5.1 DUP-CORE, §§6–7, and Packet Queue row #6/resume exports - direct bounded reads.
- `docs/22_test_quality.md` §§1–5 - direct read; earn-their-keep, production-oracle, duplicate-registration, and report-mode gate rules.
- `docs/adr/0064-existing-tests-retire-if-unjustified.md` - direct read; each removal requires a named survivor and an accounted census delta.
- `docs/adr/0065-test-quality-gate-with-delayed-enforce-mode.md` - direct read; touched-scope report-mode findings must be zero without claiming enforce mode.
- `docs/01_system_architecture.md` wall-sequence range - direct bounded read; `perimeter_utils` (`wall_sequence_reorder` — reorders a `Vec<WallLoop>`) as the shared sequencing helper consumed by both perimeter modules.
- `docs/specs/test-quality-remediation-census.json` `wall_sequence_reorder_tdd` entry and `crates/slicer-core/Cargo.toml` `[[test]]` stanza - direct bounded reads; both register the target ungated (`required: []`, no `required-features`).

## Doc Impact Statement (Required)

Specific implementation-time documentation impact: update only the `core` row in `docs/specs/test-quality-remediation-plan.md` §7 Ledger to remain `partial`, record `core-wall-sequence-dup-merges`, its absorbed/surviving tests and actual validations, and name `core-geometry-dup-review` as remaining work. Do not edit the Packet Queue or any other ledger row. AC-7's section-anchored six-column parser verifies this bookkeeping change; no IR, WIT, scheduler, manifest, host-service, SDK, or production-behavior contract changes.

<!-- snippet: orca-delegation -->
## OrcaSlicer Reference Obligations

All OrcaSlicer reads MUST be delegated to a sub-agent. Never load `OrcaSlicerDocumented/` into the implementer's own context. Dispatch contract: return `LOCATIONS` (file:line + 1-line context, ≤ 20 entries) or `SUMMARY` (≤ 200 words, no code unless asked). Code snippets in returns are capped at 30 lines.

Files to inspect for this packet:

- `OrcaSlicerDocumented/src/libslic3r/PerimeterGenerator.cpp` — confirm canonical `PerimeterGenerator::process` wall-emission sequencing (outer-first canonical, reversed outer-last, and inner-outer-inner sandwich) remains the behavior pinned by the three surviving order tests; no canonical code is ported or changed.

<!-- snippet: context-discipline -->
## Context Discipline Note

This packet was generated against the context_discipline preamble shared by `spec-packet-generator`, `swarm`, and `spec-review`. Downstream agents implementing or reviewing this packet must:

- treat `design.md`'s code change surface as the authoritative files-in-scope list
- honor `design.md`'s out-of-bounds list — those files must not be loaded directly
- delegate every cargo run and authoritative-doc fact-check
- obey the shared absolute context bands: 120k reading budget with hand-off at 150k (standard); the extended band (240k reading / 300k hard stop) only via swarm's escalation protocol

Aggregate context cost above is the sum of per-step costs in `implementation-plan.md`. If any single step is rated L, the packet must be split before activation (an extended-band run may carry a single L step only when `design.md` justifies why it cannot be split).