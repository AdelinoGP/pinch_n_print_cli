# Task Map: 298-organic-tree-support-keys

Queue packet from the wayfinder map "Close the OrcaSlicer FFF feature gap"; authored under map ticket 79. Single-task packet (no backlog slice); this crosswalk exists for the S0 structural gate and records the map provenance.

| docs/07 task ID | Packet step | Primary docs | Expected code surface | OrcaSlicer refs | Context cost | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| `TASK-000` (queue packet, `task_ids: []`) | Steps 1–4 | `docs/01_system_architecture.md` (support-family claim seam; rule-4 trigger test does not fire); `docs/08_coordinate_system.md` (mm↔unit boundaries for brim/radius geometry); `docs/03_wit_and_manifest.md` (manifest stanza shape, declared-view whitelist); `docs/DEVIATION_LOG.md` (DEV-156 preserved, DEV-189 minted) | `modules/core-modules/tree-support-planner/tree-support-planner.toml` (6 organic rows) + `src/lib.rs` (style-gated selection, saturation, 3 literal updates) + `tests/organic_params_tdd.rs` (new) + `modules/core-modules/tree-support/tree-support.toml` (2 brim rows + style gate row) + `src/lib.rs` (brim block, first-layer emission) + `tests/tree_brim_tdd.rs` (new) + `docs/DEVIATION_LOG.md` (DEV-189) + `docs/15_config_keys_reference.md` (regen) | `OrcaSlicerDocumented/src/libslic3r/PrintConfig.cpp` (defaults) + `OrcaSlicerDocumented/src/libslic3r/Support/TreeSupportCommon.hpp` (organic settings semantics) + `OrcaSlicerDocumented/src/libslic3r/Support/TreeSupport.cpp` (`draw_circles` brim path) + `OrcaSlicerDocumented/src/libslic3r/Support/TreeSupport3D.cpp` (tip-offset non-borrow) | M | P72: Tier B held, owners confirmed `tree-support` (planner `PrePass::SupportGeometry` + renderer `Layer::Support`); membership 8/8, all wired, declaration-only 0; explicit-organic gate (DEV-189 scoping divergence — default/grid/snug-on-tree keep classic params); canonical scalarity held via existing per-object overlay (explicitly not ticket 125's tool axis); no range rejection (GUI hints — saturate, AC-N1); `Print.cpp` validations ride ticket 124; CONFIG_BLOCK honest side-effect only (rides 132, no padding edits); no queue-count change; no new fog, nothing out of scope. |

Copy costs from `implementation-plan.md`. Split before activation if any row is L or aggregate exceeds M.
