---
status: draft
packet: core-cross
task_ids:
  - 'core/CROSS'
backlog_source: docs/specs/test-quality-remediation-plan.md
depends_on:
  - core-brittle
context_cost_estimate: M
copy_note: Generated from the approved core-wave queue row #13; plan wave/item IDs are used under the standing mapping exemption, not TASK-### IDs.
---

# Packet Contract: core-cross

## Goal

Review the SDK/core wrapper coverage for the offset, simplify, and raycast/bounds families, keep the two genuinely distinct implementation pairs (simplify, raycast/bounds) with earn-their-keep rationale markers on every retained test, retain thin-delegate contract cover for the SDK offset delegation including the currently uncovered miter-limit arm, and record the dispositions in the §7 ledger row.

## Scope Boundaries

The test-only edit is limited to four test surfaces plus the ledger: `crates/slicer-sdk/tests/host_wrappers_tdd.rs` (markers on the six raycast/bounds/offset/simplify tests plus one new miter-limit delegate-contract test), `crates/slicer-sdk/src/host_batch.rs` (markers on the two offset batch unit tests), `crates/slicer-core/tests/aabb_tree_tdd.rs` (markers on the three AabbTree counterpart tests), and `crates/slicer-core/src/polygon_ops.rs` tests module (markers on the offset round-trip and RDP-simplify counterpart tests). A separate implementation step updates only the `core` row in §7 of `docs/specs/test-quality-remediation-plan.md`. Production code, thresholds, other core/SDK tests, fixtures on disk, registrations, contracts, the census manifest, the wasm-host CROSS row (§5.8), the runtime clip-trio CROSS row (§5.11), and packet #14 `core-parity` are out of scope; no OrcaSlicer behavior is consulted.

## Prerequisites and Blockers

- Depends on: `core-brittle` (row #12, `generated`; test-only and exports no symbols, APIs, or files consumed here — its three kept timing guards remain owned there and are not re-reviewed).
- Unblocks: `core-parity` (row #14) packet generation only.
- Activation blockers: none for draft generation; parent owns the independent preflight.

## Acceptance Criteria

- **AC-1. Given** `crates/slicer-sdk/tests/host_wrappers_tdd.rs` containing the existing `offset_polygons_shrinks_and_grows` and the NEW `offset_polygons_with_miter_limit_clamps_sharp_miter_corners` (a 10 mm × 10 mm square fixture via the existing `square(0, 100_000)` helper, `-1.0` mm delta, `Miter` join, `0.0` arc tolerance, explicit miter limit `1.2`; asserts both arms non-empty, `assert_ne!(grown, clamped, ...)` where `grown` is the default-limit wrapper result, and a strict point-count increase for the clamped contour), **when** the feature-correct `test`-feature filter runs, **then** exactly one test executes and passes; the file holds the exact marker lines `// KEEP-review (core-cross): thin-delegate cover — the wrapper must keep routing through slicer_core::polygon_ops::offset; catches delta-unit or join-conversion regressions in the delegate path.` above the existing test and `// KEEP-review (core-cross): sole cover of the Some(miter_limit) delegation arm → slicer_core::polygon_ops::offset_with_miter_limit; a dropped or ignored limit flattens clamped to the default-limit contour.` above the new test; and the live anchored `assert_ne!(\s*grown,\s*clamped` is present (not a commented-out copy). The 90° corner miter ratio is `1/sin(45°) ≈ 1.414`, so the core default limit `2.0` (polygon_ops.rs `offset`) does not clamp while `1.2` does; this test is the only SDK coverage of the `Some(miter_limit)` → `offset_with_miter_limit` delegation arm, which has zero coverage anywhere in the tree today. No fully-qualified Rust path grep is used as acceptance — the pins above are exact comment-line fixtures and anchored live-assert text, so the name-resolution-equivalence rule is vacuously satisfied. | `set -euo pipefail; mkdir -p target; cargo test -p slicer-sdk --features test --test host_wrappers_tdd -- offset_polygons_with_miter_limit_clamps_sharp_miter_corners --nocapture 2>&1 | tee target/test-output.log >/dev/null; grep -Eq '^test result: ok\. 1 passed; 0 failed;' target/test-output.log || { echo 'FAIL: miter-limit test did not pass'; exit 1; }; rg -qF '// KEEP-review (core-cross): thin-delegate cover — the wrapper must keep routing through slicer_core::polygon_ops::offset; catches delta-unit or join-conversion regressions in the delegate path.' crates/slicer-sdk/tests/host_wrappers_tdd.rs || { echo 'FAIL: thin-delegate marker missing'; exit 1; }; rg -qF '// KEEP-review (core-cross): sole cover of the Some(miter_limit) delegation arm → slicer_core::polygon_ops::offset_with_miter_limit; a dropped or ignored limit flattens clamped to the default-limit contour.' crates/slicer-sdk/tests/host_wrappers_tdd.rs || { echo 'FAIL: miter-arm marker missing'; exit 1; }; rg -q -U --multiline '^[ \t]*assert_ne!\(\s*grown,\s*clamped' crates/slicer-sdk/tests/host_wrappers_tdd.rs || { echo 'FAIL: live assert_ne pin missing'; exit 1; }; echo 'AC-1 PASS'`
- **AC-2. Given** the two offset-batch unit tests in `crates/slicer-sdk/src/host_batch.rs` (`batch_offset_keeps_results_aligned_with_inputs`, `batch_forms_agree_with_the_singular_forms`) exercising `offset_polygons_batch`'s native per-request mapping over the singular wrappers, **when** each `--lib` substring filter runs under `--features test`, **then** each executes exactly one test and passes, and the file holds the exact marker lines `// KEEP-review (core-cross): per-item delta alignment of the offset batch wrapper; catches a misaligned zip.` and `// KEEP-review (core-cross): native batch form delegates to the singular wrappers; catches batch/singular divergence.`. Substring filters are used instead of `--exact` because libtest `--exact` requires the full module path and would pin a brittle path. | `set -euo pipefail; mkdir -p target; for t in batch_offset_keeps_results_aligned_with_inputs batch_forms_agree_with_the_singular_forms; do cargo test -p slicer-sdk --features test --lib "$t" -- --nocapture 2>&1 | tee target/test-output.log >/dev/null; grep -Eq '^test result: ok\. 1 passed; 0 failed;' target/test-output.log || { echo "FAIL: $t"; exit 1; }; done; rg -qF '// KEEP-review (core-cross): per-item delta alignment of the offset batch wrapper; catches a misaligned zip.' crates/slicer-sdk/src/host_batch.rs || { echo 'FAIL: alignment marker missing'; exit 1; }; rg -qF '// KEEP-review (core-cross): native batch form delegates to the singular wrappers; catches batch/singular divergence.' crates/slicer-sdk/src/host_batch.rs || { echo 'FAIL: batch/singular marker missing'; exit 1; }; echo 'AC-2 PASS'`
- **AC-3. Given** the two SDK simplify tests in `crates/slicer-sdk/tests/host_wrappers_tdd.rs` (`simplify_polygon_drops_collinear_vertices`, `simplify_polygon_short_input_returned_as_is`) exercising the native inline collinear-drop branch of `host::simplify_polygon` — a genuinely distinct implementation from core `expolygons_simplify` (RDP), **when** the `simplify_polygon` substring filter runs under `--features test`, **then** exactly two tests execute and both pass, and the file holds the exact marker lines `// KEEP-review (core-cross): distinct inline collinear-drop impl (tolerance reserved for a future DP variant), kept separate from core expolygons_simplify (RDP).` and `// KEEP-review (core-cross): len<3 early-return witness for the distinct inline simplify.`. | `set -euo pipefail; mkdir -p target; cargo test -p slicer-sdk --features test --test host_wrappers_tdd -- simplify_polygon --nocapture 2>&1 | tee target/test-output.log >/dev/null; grep -Eq '^test result: ok\. 2 passed; 0 failed;' target/test-output.log || { echo 'FAIL: simplify pair did not both pass'; exit 1; }; rg -qF '// KEEP-review (core-cross): distinct inline collinear-drop impl (tolerance reserved for a future DP variant), kept separate from core expolygons_simplify (RDP).' crates/slicer-sdk/tests/host_wrappers_tdd.rs || { echo 'FAIL: distinct-simplify marker missing'; exit 1; }; rg -qF '// KEEP-review (core-cross): len<3 early-return witness for the distinct inline simplify.' crates/slicer-sdk/tests/host_wrappers_tdd.rs || { echo 'FAIL: short-input marker missing'; exit 1; }; echo 'AC-3 PASS'`
- **AC-4. Given** the three SDK raycast/bounds tests in `crates/slicer-sdk/tests/host_wrappers_tdd.rs` (`raycast_and_normal_route_through_installed_mesh_source`, `raycast_returns_none_without_source_documented_signal`, `object_bounds_returns_host_unavailable_without_source`) exercising the `MeshSource`-trait routing of `host::raycast_z_down` / `host::object_bounds` — a genuinely distinct path from core `AabbTree` (no core call on the native arm; verified at `crates/slicer-sdk/src/host.rs` `raycast_z_down`/`object_bounds`), **when** the `raycast` and `object_bounds` substring filters run under `--features test`, **then** exactly two, then exactly one, tests execute and pass, and the file holds the exact marker lines `// KEEP-review (core-cross): SDK raycast/bounds route through the MeshSource trait, distinct from core AabbTree; catches source-lookup or trait-plumbing regressions.`, `// KEEP-review (core-cross): documented None signal for missing source; negative control for the raycast contract.`, and `// KEEP-review (core-cross): explicit HostUnavailable (not a zero box) when no MeshSource is installed.`. | `set -euo pipefail; mkdir -p target; cargo test -p slicer-sdk --features test --test host_wrappers_tdd -- raycast --nocapture 2>&1 | tee target/test-output.log >/dev/null; grep -Eq '^test result: ok\. 2 passed; 0 failed;' target/test-output.log || { echo 'FAIL: raycast pair did not both pass'; exit 1; }; cargo test -p slicer-sdk --features test --test host_wrappers_tdd -- object_bounds --nocapture 2>&1 | tee target/test-output.log >/dev/null; grep -Eq '^test result: ok\. 1 passed; 0 failed;' target/test-output.log || { echo 'FAIL: object_bounds test did not pass'; exit 1; }; rg -qF '// KEEP-review (core-cross): SDK raycast/bounds route through the MeshSource trait, distinct from core AabbTree; catches source-lookup or trait-plumbing regressions.' crates/slicer-sdk/tests/host_wrappers_tdd.rs || { echo 'FAIL: raycast marker missing'; exit 1; }; rg -qF '// KEEP-review (core-cross): documented None signal for missing source; negative control for the raycast contract.' crates/slicer-sdk/tests/host_wrappers_tdd.rs || { echo 'FAIL: None-signal marker missing'; exit 1; }; rg -qF '// KEEP-review (core-cross): explicit HostUnavailable (not a zero box) when no MeshSource is installed.' crates/slicer-sdk/tests/host_wrappers_tdd.rs || { echo 'FAIL: HostUnavailable marker missing'; exit 1; }; echo 'AC-4 PASS'`
- **AC-5. Given** the three core AabbTree counterpart tests in `crates/slicer-core/tests/aabb_tree_tdd.rs` (`bounds_match_unit_cube_vertex_extrema`, `positive_z_raycast_from_below_hits_cube_bottom_face_first`, `raycast_all_hits_returns_sorted_entry_and_exit_intersections`) exercising `AabbTree::bounds` / `raycast_first_hit` / `raycast_all_hits` (`crates/slicer-core/src/aabb_tree.rs`), the counterparts this packet deliberately keeps distinct from the SDK `MeshSource` route, **when** each substring filter runs under `--features host-algos` (wave convention; the target has no `required-features` and no inner cfg header per census), **then** each executes exactly one test and passes, and the file holds the exact marker lines `// KEEP-review (core-cross): core AabbTree::bounds counterpart oracle; catches bounds-extrema regressions.`, `// KEEP-review (core-cross): core AabbTree::raycast_first_hit counterpart, distinct from the SDK MeshSource raycast path.`, and `// KEEP-review (core-cross): core AabbTree::raycast_all_hits sorted entry/exit contract.`. | `set -euo pipefail; mkdir -p target; for t in bounds_match_unit_cube_vertex_extrema positive_z_raycast_from_below_hits_cube_bottom_face_first raycast_all_hits_returns_sorted_entry_and_exit_intersections; do cargo test -p slicer-core --features host-algos --test aabb_tree_tdd -- "$t" --nocapture 2>&1 | tee target/test-output.log >/dev/null; grep -Eq '^test result: ok\. 1 passed; 0 failed;' target/test-output.log || { echo "FAIL: $t"; exit 1; }; done; rg -qF '// KEEP-review (core-cross): core AabbTree::bounds counterpart oracle; catches bounds-extrema regressions.' crates/slicer-core/tests/aabb_tree_tdd.rs || { echo 'FAIL: bounds marker missing'; exit 1; }; rg -qF '// KEEP-review (core-cross): core AabbTree::raycast_first_hit counterpart, distinct from the SDK MeshSource raycast path.' crates/slicer-core/tests/aabb_tree_tdd.rs || { echo 'FAIL: first-hit marker missing'; exit 1; }; rg -qF '// KEEP-review (core-cross): core AabbTree::raycast_all_hits sorted entry/exit contract.' crates/slicer-core/tests/aabb_tree_tdd.rs || { echo 'FAIL: all-hits marker missing'; exit 1; }; echo 'AC-5 PASS'`
- **AC-6. Given** the two core polygon-op counterpart tests in the `mod tests` of `crates/slicer-core/src/polygon_ops.rs` (`offset_round_trip_preserves_hole_nesting`, `expolygons_simplify_preserves_square`) exercising `offset`/`expolygons_simplify` — the independent core oracles the SDK thin wrapper and the SDK inline simplify deliberately remain separate from, **when** each `--lib` substring filter runs under `--features host-algos`, **then** each executes exactly one test and passes, and the file holds the exact marker lines `// KEEP-review (core-cross): core offset counterpart oracle — hole nesting survives the inflate_once tree path; independent of the SDK thin wrapper.` and `// KEEP-review (core-cross): core RDP simplify counterpart, distinct from the SDK inline collinear drop.`. | `set -euo pipefail; mkdir -p target; for t in offset_round_trip_preserves_hole_nesting expolygons_simplify_preserves_square; do cargo test -p slicer-core --features host-algos --lib "$t" -- --nocapture 2>&1 | tee target/test-output.log >/dev/null; grep -Eq '^test result: ok\. 1 passed; 0 failed;' target/test-output.log || { echo "FAIL: $t"; exit 1; }; done; rg -qF '// KEEP-review (core-cross): core offset counterpart oracle — hole nesting survives the inflate_once tree path; independent of the SDK thin wrapper.' crates/slicer-core/src/polygon_ops.rs || { echo 'FAIL: offset counterpart marker missing'; exit 1; }; rg -qF '// KEEP-review (core-cross): core RDP simplify counterpart, distinct from the SDK inline collinear drop.' crates/slicer-core/src/polygon_ops.rs || { echo 'FAIL: simplify counterpart marker missing'; exit 1; }; echo 'AC-6 PASS'`
- **AC-7. Given** the real `docs/specs/test-quality-remediation-plan.md`, **when** the implementation updates only its current §7 `core` row, **then** this read-only predicate passes only when the heading matches `^## 7\. Ledger\b` and the span ends at the next `^## Packet Queue\b`, the six-column header is exact, exactly one `core` row has state `partial` or `partial (parenthesized detail)`, Retired/changed symbols contains the exact token `core-cross` plus all fourteen reviewed test names (the six marked SDK wrapper tests, the new miter-limit test, the two marked batch tests, the three marked AabbTree tests, and the two marked polygon-op tests), Surviving/new coverage contains all fourteen names plus the oracle tokens `KEEP-review (core-cross)`, `assert_ne`, and `1.2`, Validation contains a whitespace-normalized token-boundary occurrence of `cargo test -p slicer-sdk --features test --test host_wrappers_tdd -- offset_polygons_with_miter_limit_clamps_sharp_miter_corners` with optional trailing flags or a closing code-span backtick allowed, plus the four gate tokens `cargo check --workspace --all-targets`, `cargo clippy --workspace --all-targets -- -D warnings`, `cargo xtask check-literals`, and `cargo xtask check-test-quality --report`, and Remaining gap contains `core-parity` and `census-target-scope` while no longer containing `core-cross`; no temporary copy, manufactured row, queue mutation, or synthetic positive is used. | `set -euo pipefail; rg -n '^## 7\. Ledger\b' docs/specs/test-quality-remediation-plan.md >/dev/null; python3 -c 'import re; from pathlib import Path as P; t=P("docs/specs/test-quality-remediation-plan.md").read_text(encoding="utf-8"); m=re.search(r"(?ms)^## 7\. Ledger\b[^\n]*\n(.*?)(?=^## Packet Queue\b)",t); assert m,"missing ledger span"; s=m.group(1); h=[[x.strip() for x in l.strip("|").split("|")] for l in s.splitlines() if l.startswith("| Wave |")]; assert len(h)==1 and h[0]==["Wave","State","Retired/changed symbols","Surviving/new coverage","Validation","Remaining gap"]; r=[l for l in s.splitlines() if re.match(r"^\|\s*core\s*\|",l)]; assert len(r)==1; w,st,ch,su,v,g=[x.strip() for x in r[0].strip("|").split("|")]; q=lambda x,y: re.search(r"(?<![A-Za-z0-9_])"+re.escape(x)+r"(?![A-Za-z0-9_])",y); qb=lambda x,y: re.search(r"(?<![A-Za-z0-9_-])"+re.escape(x)+r"(?![A-Za-z0-9_-])",y); assert w=="core" and re.fullmatch(r"partial(?:\s*\([^()\n]+\))?",st); names=["raycast_and_normal_route_through_installed_mesh_source","raycast_returns_none_without_source_documented_signal","object_bounds_returns_host_unavailable_without_source","offset_polygons_shrinks_and_grows","offset_polygons_with_miter_limit_clamps_sharp_miter_corners","simplify_polygon_drops_collinear_vertices","simplify_polygon_short_input_returned_as_is","batch_offset_keeps_results_aligned_with_inputs","batch_forms_agree_with_the_singular_forms","bounds_match_unit_cube_vertex_extrema","positive_z_raycast_from_below_hits_cube_bottom_face_first","raycast_all_hits_returns_sorted_entry_and_exit_intersections","offset_round_trip_preserves_hole_nesting","expolygons_simplify_preserves_square"]; assert qb("core-cross",ch); assert all(q(x,ch) for x in names); assert all(q(x,su) for x in names+["assert_ne","1.2"]); assert qb("KEEP-review (core-cross)",su); vn=re.sub(r"\s+"," ",v).strip(); own=r"(?<![A-Za-z0-9_-])cargo test -p slicer-sdk --features test --test host_wrappers_tdd -- offset_polygons_with_miter_limit_clamps_sharp_miter_corners(?=$|\s+-|[\x60])"; assert re.search(own,vn),"validation cell missing representative command"; assert "cargo check --workspace --all-targets" in vn; assert "cargo clippy --workspace --all-targets -- -D warnings" in vn, "clippy gate must carry -- -D warnings"; assert "cargo xtask check-literals" in vn; assert "cargo xtask check-test-quality --report" in vn, "quality gate must carry --report"; assert qb("core-parity",g) and qb("census-target-scope",g); assert not qb("core-cross",g),"core-cross must no longer be listed as an open gap"; print("core-cross ledger predicate: PASS")'`

## Negative Test Cases

- **AC-N1. Given** that `host_wrappers_tdd` carries `required-features = ["test"]` in `crates/slicer-sdk/Cargo.toml` (line ~105, confirmed by the census `required: ["test"]` entry) so a bare run silently reports nothing, and that this packet touches only marker comments plus one inserted test, **when** the implementation lands, **then** the SDK test file holds exactly twelve test names in file order (the eleven existing names with the new `offset_polygons_with_miter_limit_clamps_sharp_miter_corners` inserted immediately after `offset_polygons_shrinks_and_grows`), the core `aabb_tree_tdd.rs` holds exactly six test names in file order (all untouched), and a filter run without `--features test` is rejected by cargo (non-zero exit), proving the feature flag is mandatory rather than decorative. | `set -euo pipefail; mkdir -p target; python3 -c 'import re; from pathlib import Path as P; exp=["log_capture_records_all_levels_in_order","log_level_str_matches_host_mapping","take_log_messages_uninstalls_sink","object_bounds_returns_host_unavailable_without_source","raycast_and_normal_route_through_installed_mesh_source","raycast_returns_none_without_source_documented_signal","clip_polygons_union_produces_nonempty_result_for_real_input","offset_polygons_shrinks_and_grows","offset_polygons_with_miter_limit_clamps_sharp_miter_corners","simplify_polygon_drops_collinear_vertices","simplify_polygon_short_input_returned_as_is","now_us_is_monotonic_within_a_thread"]; got=re.findall(r"#\[test\]\s*\n\s*fn\s+([A-Za-z0-9_]+)",P("crates/slicer-sdk/tests/host_wrappers_tdd.rs").read_text(encoding="utf-8")); assert got==exp, f"roster mismatch: {got}"; print("SDK-ROSTER-PASS")'; python3 -c 'import re; from pathlib import Path as P; exp=["empty_mesh_reports_no_bounds_hits_or_closest_point","bounds_match_unit_cube_vertex_extrema","positive_z_raycast_from_below_hits_cube_bottom_face_first","raycast_all_hits_returns_sorted_entry_and_exit_intersections","closest_point_projects_queries_below_and_above_the_cube","ray_miss_returns_no_intersections"]; got=re.findall(r"#\[test\]\s*\n\s*fn\s+([A-Za-z0-9_]+)",P("crates/slicer-core/tests/aabb_tree_tdd.rs").read_text(encoding="utf-8")); assert got==exp, f"roster mismatch: {got}"; print("CORE-ROSTER-PASS")'; if cargo test -p slicer-sdk --test host_wrappers_tdd -- --list 2>&1 | tee target/test-output.log >/dev/null; then echo 'FAIL: expected required-features rejection'; exit 1; else echo 'FEATURE-REJECTION-PASS'; fi`

## Verification

AC-1 through AC-6 and AC-N1 are the full acceptance matrix and must all run; AC-7 binds only the ledger row.

- `set -euo pipefail; mkdir -p target; cargo check --workspace --all-targets 2>&1 | tee target/core-cross-check.log >/dev/null`
- `set -euo pipefail; mkdir -p target; cargo clippy --workspace --all-targets -- -D warnings 2>&1 | tee target/core-cross-clippy.log >/dev/null`
- `set -euo pipefail; mkdir -p target; cargo test -p slicer-sdk --features test --test host_wrappers_tdd -- offset_polygons_with_miter_limit_clamps_sharp_miter_corners --nocapture 2>&1 | tee target/test-output.log >/dev/null; grep -Eq '^test result: ok\. 1 passed; 0 failed;' target/test-output.log`

Cargo **test** commands use `set -euo pipefail`, `mkdir -p target`, `tee target/test-output.log`, and anchored result checks. Workspace check/clippy use the same compact-output wrapper with dedicated logs; they do not overwrite the test log. No workspace suite run is required. Because Step 2 edits `crates/slicer-sdk/src/host_batch.rs` (a guest-build-path source even for comment-only edits), the implementer runs `cargo xtask build-guests --check` once after Step 2 and treats a non-zero exit as stale-guests to rebuild (see `design.md` Architecture Constraints); this is a tree-consistency check, not an acceptance gate.

## Authoritative Docs

- `docs/specs/test-quality-remediation-plan.md` - §5.1 `CROSS` row, §6 feature-correct command/log rules, §7 ledger ownership, Packet Queue row #13, and the row-#12-generated resume pointer; read only those sections.
- `docs/22_test_quality.md` - §§1-3 and §5; earn-their-keep standard, legitimate weak forms, and the R1-R8 gate table (direct read; 215 lines).
- `docs/adr/0064-existing-tests-retire-if-unjustified.md` - direct read; the retire-if-unjustified burden of proof, which is the standard these KEEP rationales must satisfy.
- `docs/adr/0065-test-quality-gate-with-delayed-enforce-mode.md` - delegated SUMMARY for report-mode closure obligation.
- `docs/08_coordinate_system.md` - delegated SUMMARY for the 100 nm unit convention behind the new test's fixture literals.
- `docs/spec_packets/core-brittle/` - delegated predecessor summary; draft, independently `PREFLIGHT PASS`, exports none.

## Doc Impact Statement (Required)

Specific same-packet doc edit:

- `docs/specs/test-quality-remediation-plan.md` section "§7 Ledger core row" - implementation records this packet's KEEP-review dispositions, all fourteen reviewed/kept test names, exact oracle tokens, own representative validation command, and the remaining core-wave gap including the `census-target-scope` program-level gap. Verification grep: `rg -n '^## 7\. Ledger\b|^\| core \|' docs/specs/test-quality-remediation-plan.md`; AC-7 is the required real-plan content predicate and must read the accumulated row rather than manufacture a positive copy.
- No `docs/<NN>_*` section is added or amended by this packet (keep-rationale evidence lives in the test-adjacent marker comments pinned by AC-1 through AC-6). Confirming grep: `rg -l 'KEEP-review \(core-cross\)' docs/ -g '!**spec_packets**' -g '!**specs**' || echo 'no numbered-doc marker (expected)'`.

No other documentation section, queue row, or packet is edited by this packet.

<!-- snippet: context-discipline -->
## Context Discipline Note

This packet was generated against the context_discipline preamble shared by `spec-packet-generator`, `swarm`, and `spec-review`. Downstream agents implementing or reviewing this packet must:

- treat `design.md`'s code change surface as the authoritative files-in-scope list
- honor `design.md`'s out-of-bounds list — those files must not be loaded directly
- delegate every cargo run and authoritative-doc fact-check
- obey the shared absolute context bands: 120k reading budget with hand-off at 150k (standard); the extended band (240k reading / 300k hard stop) only via swarm's escalation protocol

Aggregate context cost above is the sum of per-step costs in `implementation-plan.md`. If any single step is rated L, the packet must be split before activation (an extended-band run may carry a single L step only when `design.md` justifies why it cannot be split).
