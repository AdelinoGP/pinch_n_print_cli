# Task Map: 289-speed-acceleration-emitter

Queue packet from the wayfinder map "Close the OrcaSlicer FFF feature gap"; authored under map ticket 63. Single-task packet (no backlog slice); this crosswalk exists for the S0 structural gate and records the map provenance.

| docs/07 task ID | Packet step | Primary docs | Expected code surface | OrcaSlicer refs | Context cost | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| `TASK-000` (queue packet, `task_ids: []`) | Steps 1–1b–4 | `docs/config/host-keys.toml`; `docs/DEVIATION_LOG.md` (DEV-181); `docs/15_config_keys_reference.md` (generated via `cargo xtask gen-config-docs`, Step 1b); `docs/01_system_architecture.md` (claim-system trigger test) | `crates/slicer-ir/src/resolved_config.rs` + `crates/slicer-gcode/src/emit.rs` (per-entity selection stage + flavor rendering + bounds) + `crates/slicer-gcode/tests/speed_p56_accel_emission_tdd.rs` (new) + `docs/config/host-keys.toml` | `OrcaSlicerDocumented/src/libslic3r/PrintConfig.cpp` (eleven declarations + bounds + percent bases) + `OrcaSlicerDocumented/src/libslic3r/GCode.cpp` (`GCode::_extrude` chain order + gates + composition position; `GCode::travel_to` default + flavor split; `GCode::_do_export` Calib arm as `[FWD]`) + `OrcaSlicerDocumented/src/libslic3r/GCodeWriter.cpp` (`GCodeWriter::set_acceleration_internal` forms + `GCodeWriter::supports_separate_travel_acceleration` set) + `OrcaSlicerDocumented/src/libslic3r/Print.cpp` (`Print::validate` warn-and-cap evidence) | M | P56: Tier B held, owner `crates/slicer-gcode` stands (all canonical reads emission-time; machine-gcode-emit sweep is the wrong seam; `set_acceleration`/`set_travel_acceleration` exist unwired — reuse, never fork); membership held 11-in, none shed, none returned (all eleven live in canonical, all zero-occurrence here); scalar-global is a recorded simplification (DEV-181(b) — canonical declares nine keys per-nozzle vector, the model stays with ticket 125); defaults NOT identity (new M204 lines at defaults, AC-2 pins them); short-travel/first-layer-travel/wipe-tower/Calib arms are `[FWD]`, not stubs. |

Copy costs from `implementation-plan.md`. Split before activation if any row is L or aggregate exceeds M.
