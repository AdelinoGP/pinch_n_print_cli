# Task Map: 285-seam-scarf-joint-emitter

Queue packet from the wayfinder map "Close the OrcaSlicer FFF feature gap"; authored under map ticket 59. Single-task packet (no backlog slice); this crosswalk exists for the S0 structural gate and records the map provenance.

| docs/07 task ID | Packet step | Primary docs | Expected code surface | OrcaSlicer refs | Context cost | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| `TASK-000` (queue packet, `task_ids: []`) | Steps 1a–6 | `docs/config/host-keys.toml`; `docs/DEVIATION_LOG.md` (DEV-177); `docs/15_config_keys_reference.md` (generated via `cargo xtask gen-config-docs`, Step 1b); `docs/08_coordinate_system.md` (gap/length math) | `crates/slicer-ir/src/resolved_config.rs` + `crates/slicer-gcode/src/emit.rs` + `modules/core-modules/machine-gcode-emit/machine-gcode-emit.toml` + `crates/slicer-gcode/tests/seam_scarf_joint_emission_tdd.rs` + re-baselined fixtures | `OrcaSlicerDocumented/src/libslic3r/PrintConfig.cpp` (eight declarations + bounds) + `OrcaSlicerDocumented/src/libslic3r/GCode.cpp` (`GCode::extrude_loop` gates/clip, `GCode::_extrude` factor/cap, `Wipe::wipe` + `Wipe::calculateWipeRetractionLengths` selection) + `OrcaSlicerDocumented/src/libslic3r/GCode/GCodeProcessor.cpp` (`apply_config` recognition — DEV-177(a), not wired) + `OrcaSlicerDocumented/src/libslic3r/Fill/Fill.cpp` + `OrcaSlicerDocumented/src/libslic3r/ExtrusionEntity.cpp` (concentric/slope arms — DEV-177(b), not wired) | M | P52: 8 Tier B keys, owner `crates/slicer-gcode` stands (all canonical reads emission-time; machine-gcode-emit sweep is the wrong seam); Tier B sizing confirmed (all zero-occurrence as behaviour — new scarf/slope/clip logic, no existing decision point); scalar-global is parity (all canonical scalars — no ticket-125 vector model); one intended default output change (live `seam_gap` 10% clip, count unchanged; padding twin untouched); scarf inert at defaults; AC-6 activation-blocked on draft 277 (FORWARD-DEP, names reconciled). |

Copy costs from `implementation-plan.md`. Split before activation if any row is L or aggregate exceeds M.
