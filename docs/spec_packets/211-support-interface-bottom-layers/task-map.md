# Task Map: 211-support-interface-bottom-layers

Single-task packet, but the crosswalk is emitted for two reasons: `TASK-327` is **net-new** (absent from `docs/07_implementation_status.md` today — re-derive that the slot is still free when you register it), and this packet **retires** a capability recorded as shipped under the existing `TASK-163b-diagnostic` row, so that row needs an amendment note rather than a new closure.

| docs/07 task ID | Packet step | Primary docs | Expected code surface | OrcaSlicer refs | Context cost | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| `TASK-327` | `Step 1` | `docs/15_config_keys_reference.md` (`support_interface_bottom_layers` note only) | `modules/core-modules/support-planner/src/lib.rs` — `resolve_interface_bottom_layers` (net-new `pub`), `SupportPlanner.support_interface_bottom_layers`, `from_config` parse | `OrcaSlicerDocumented/src/libslic3r/Support/SupportParameters.hpp` (`number_of_support_interface_bottom_layers`) | `S` | Ports the `< 0 ⇒ use the top count` fallback `DEV-129` names as canonical. Owns the two-site `SupportPlanner` struct-literal blast radius (`Ok(Self { … })` in `from_config`; `default_planner()` in the in-file test module). |
| `TASK-327` | `Step 2` | none | `modules/core-modules/support-planner/src/lib.rs` — `split_column_into_chains` (net-new private), `smooth_branches` rewritten to consume it | none | `S` | Behaviour-preserving extraction. Without it the bottom band would attach to `(object_id, region_id)` columns rather than to individual trees, and a duplicated copy of the gap walk would recreate `DEV-127`'s drift failure mode inside one file. Guarded by an unmodified `smooth_nodes_tdd`. |
| `TASK-327` | `Step 3` | `docs/08_coordinate_system.md` (only if packet 210 has landed) | `modules/core-modules/support-planner/src/lib.rs` — `densify_bottom_interface` (net-new private) and its guarded call after `smooth_branches` in `plan_for_object`; `modules/core-modules/support-planner/tests/interface_bottom_layers_tdd.rs` (net-new, six cases) | `OrcaSlicerDocumented/src/libslic3r/Support/TreeSupport.cpp` (`draw_circles` floor-area block), `.../TreeSupportCommon.hpp` (`support_floor_layers`) | `M` | The packet's core, and the answer to `DEV-129`'s design blocker: the landing is detected from the emitted plan plus the per-layer `LayerCollisionCache`, not from a `PlannedSupportNode` field that cannot be filled during a single top-down walk. |
| `TASK-327` | `Step 4` | `docs/adr/0010-typed-diagnostic-channel.md` §Status | `modules/core-modules/support-planner/src/lib.rs` (delete the code-1003 block), `.../support-planner.toml` (delete the "Not yet implemented" comment), `.../tests/diagnostics_tdd.rs` (rewrite the two bottom-layers cases) | none | `S` | Retires the stub only after Step 3 makes the warning false. The two pinned tests are rewritten to "exactly 0 code-1003 records", never weakened or deleted. |
| `TASK-327` | `Step 5` | `docs/15_config_keys_reference.md`, `docs/adr/0010-typed-diagnostic-channel.md`, `docs/DEVIATION_LOG.md` (`DEV-129`), `docs/07_implementation_status.md` | guest rebuild; `crates/slicer-runtime/tests/integration/support_invariants_wedge_tdd.rs` as a read-only oracle; four single-paragraph doc edits | none | `S` | Proves bands do not violate invariant 2 and lands the ledger. `DEV-129` moves `Open` → `Closed` and records the accepted approximation (footprint at `L_end - 1` vs. canonical's full downward `stTop`/`stBottom` search). |
| `TASK-163b-diagnostic` (existing, closed) | `Step 5` | `docs/07_implementation_status.md`, `docs/adr/0010-typed-diagnostic-channel.md` §Status | no code surface — an amendment note only | none | `S` | Amendment, not reopening. That row records codes 1001/1002/1003 as shipped by packet 118; code 1003 is now retired because its underlying gap is closed. Codes 1001 and 1002 are unaffected and ADR-0010's normative Decision is unchanged, so no ADR amendment deviation is required. |

Costs are copied from `implementation-plan.md` §Per-Step Budget Roll-Up. Aggregate is `M` and no row is `L`, so no split is required before activation.

## Upstream dependency

`210a-support-planner-coord-t` (`TASK-326`) is a **forward dependency on a `draft` packet**, not a satisfied one. It retypes `PlannedSupportNode` to `x: i64, y: i64` and `push_interface_scan_lines` to a `Point2` / `i64` signature; `densify_bottom_interface` calls the latter and reads the former's emitted positions. Implement against whichever signature is on disk at the time, verify, and re-verify after 210a merges. Never edit `docs/spec_packets/210a-support-planner-coord-t/` from this packet — if the two specs disagree on a signature, report it for reconciliation in both.

## Downstream consumption

`resolve_interface_bottom_layers` (`support-planner`, `pub fn (i32, i32) -> u32`) is the only net-new public symbol. No packet in the 206–212 queue consumes it, and no crate outside `modules/core-modules/support-planner/` depends on this module's library surface.
