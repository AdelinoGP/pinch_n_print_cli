# Task Map: 295-print-sequence-tool-ordering

Queue packet from the wayfinder map "Close the OrcaSlicer FFF feature gap"; authored under map ticket 73. Single-task packet (no backlog slice); this crosswalk exists for the S0 structural gate and records the map provenance.

| docs/07 task ID | Packet step | Primary docs | Expected code surface | OrcaSlicer refs | Context cost | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| `TASK-000` (queue packet, `task_ids: []`) | Steps 1–4 | `docs/03_wit_and_manifest.md` (`from_declared` whitelist: why no manifest row — host reads `ResolvedConfig` directly); `docs/DEVIATION_LOG.md` (DEV-187); `docs/01_system_architecture.md` (emission-stage ownership; rule-4 trigger test does not fire) | `crates/slicer-ir/src/resolved_config.rs` (three DSL rows: two `Vec<f64>` float-lists + one `u32`) + `crates/slicer-gcode/src/emit.rs` (ordering kernel + `emit_gcode` call site) + `crates/slicer-ir/tests/resolved_config_print_sequence_tdd.rs` (new) + `crates/slicer-gcode/tests/print_sequence_ordering_tdd.rs` (new) | `OrcaSlicerDocumented/src/libslic3r/PrintConfig.cpp` (`init_fff_params` two `coInts` + one `coInt`, defaults `{0}`/`{0}`/`0`) + `OrcaSlicerDocumented/src/libslic3r/GCode/ToolOrdering.cpp` (`apply_first_layer_order` sort + ignore-condition; `generate_first_layer_tool_order` area-base non-borrow; `get_recommended_filament_maps` grouping-range non-borrow; `reorder_extruders_for_minimum_flush_volume` record-shape non-borrow) | M | P66: Tier B held, owner corrected `tool-ordering` → `crates/slicer-gcode` emission stage (no `ToolOrdering` module exists here; per-layer tool order is owned by host emission — ticket-27/39/40 precedent); membership held 3-in, none shed, none returned (all live in canonical, zero-occurrence here); canonical `coInts` shape NOT held (DEV-187(a) float-list + rounding, explicitly not ticket 125's axis); grouping-record ranges NOT held (DEV-187(b)); area-ordered first-layer base NOT held (DEV-187(c)); bounds enforced (DEV-187(d)); defaults inert (AC-2 pins byte-identity); no queue-count change; no new fog, nothing out of scope. |

Copy costs from `implementation-plan.md`. Split before activation if any row is L or aggregate exceeds M.
