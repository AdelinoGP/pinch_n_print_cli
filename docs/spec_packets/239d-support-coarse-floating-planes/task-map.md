# Task Map: 239d-support-coarse-floating-planes

Use this crosswalk when a packet spans more than one task ID, reopens prior work, or supersedes an earlier packet. Skip it for a single-task packet unless another explicit mapping need requires it.

| docs/07 task ID | Packet step | Primary docs | Expected code surface | OrcaSlicer refs | Context cost | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| `TASK-523` | `Step 1` | `docs/07_implementation_status.md` (record), `docs/specs/support-independent-layer-z-split-plan.md` | `crates/slicer-core/src/algos/support_geometry.rs` (read-only), both planner `lib.rs` (read-only) | — | `S` | Measure-first: decimation reconciliation (tree planner has no `support_step`; host schedule never reaches the meshed-object planner path — tree's only `_support_geometry` read is the mesh-less legacy fallback, traditional param never read), coarse baseline (0.3/0.2 → 0 off-grid rows; 0.3/0.1 family-labeled — normal(auto) 85/299 support rows, tree(auto) exploratory run 248), disabled baseline for AC-N1 (hard-coded later as `P239D_DISABLED_COARSE_PITCH_BASELINE_Z`); establishes the per-bracket `local_support_gap` inputs (surviving support-bearing rows of each `(object_id, region_id)` run) for D1a |
| `TASK-524` | `Step 2` | `docs/spec_packets/239c-support-layer-height-producer/design.md` | `modules/core-modules/tree-support-planner/src/lib.rs`, `tests/tree_family_tdd.rs` | `TreeSupport.cpp` `plan_layer_heights`; `SupportCommon.cpp` `generate_support_layers` | `M` | Tree coarse derivation (binding Q1/Q2 + D1a predicate: coarse per bracket pair iff nonzero pitch >= `local_support_gap` — max positive anchor-Z difference between consecutive surviving support-bearing rows of the same run covered by the bracket — else 239c finer): bracket set per `(object_id, region_id)` contiguous run = sorted/deduped interface-role planes when count >= 2 (endpoints not added); with count < 2 (zero or one interface plane) that set supplemented with the run's first/last surviving support-bearing rows then sorted/deduped by `anchor_z` (lone genuine interface plane stays a bracket), only non-interface rows strictly inside each bracket pair replaced, genuine interface bracket entries survive; clone lower bracket + rewrite roles to `SupportBody` on synthesized planes (source `global_layer_index` captured into the local duplicate key/clone-source provenance decision only; emitted final `global_layer_index` assigned from the per-plane DEV-163 synthetic identity map, one shared plane identity; other provenance preserved), stack at `ceil(dist / pitch)` (tree `plan_layer_heights`, no EPSILON bias), grouping/midpoint only (no group-height: `SupportPlanEntry` has no height field), duplicate-key `(source global_layer_index, object_id, region_id, ordered body_ids, anchor_z)` prevention for synthesized candidates only (surviving real entries never deduplicated by emitted `global_layer_index`), nondecreasing entries + strictly increasing distinct planes, true-nearest `anchor_layer_index` (lower-index tie break); AC-2 (incl. one-interface-case assertion if the fixture naturally expresses it) + AC-N3 + AC-N4 (`adaptive_local_gap_stays_finer` — pitch 0.2 over covered surviving-row gaps 0.3 stays finer; NET-NEW test authored by this packet, present and passing in the existing `tree_family_tdd` target) tests |
| `TASK-525` | `Step 3` | `docs/spec_packets/239c-support-layer-height-producer/design.md` | `modules/core-modules/traditional-support-planner/src/lib.rs`, `tests/traditional_family_tdd.rs` | `SupportMaterial.cpp` `raft_and_intermediate_support_layers` | `M` | Traditional twin (same Q1/Q2 bracketing (count-conditional: >= 2 interface planes bracket alone, endpoints not added; < 2 supplemented with first/last surviving rows, sorted/deduped) + clone rules, genuine interface brackets survive; only non-interface rows strictly inside each bracket pair replaced) with the traditional formula `ceil((dist - EPSILON) / pitch)` (EPSILON bias retained) + `support_step` neutralization per binding Q3 (set 1 exactly for bracket pairs where nonzero pitch >= `local_support_gap`, no global bypass); AC-3 test |
| `TASK-526` | `Step 4` | `docs/spec_packets/239c-support-layer-height-producer/packet.spec.md` (test-naming convention) | `crates/slicer-runtime/tests/integration/support_family_closure.rs`, `tests/integration/main.rs` | — | `M` | Real-slice AC-1 (off-grid rows + E>0 on every off-grid support row; enabled distinct support `;Z:` superset in original output order) and AC-N1 (disabled baseline via the new `P239D_DISABLED_COARSE_PITCH_BASELINE_Z` const) with the wrapper convention |
| `TASK-527` | `Step 5` | `docs/07_implementation_status.md` (TASK-519 pattern) | `crates/slicer-gcode/src/emit.rs` (read-only) | `GCode.cpp` `_extrude` (comparison target) | `S` | Measure-first coarse `height_delta` verdict (applied height vs plane delta at E for a 0.3-pitch row) |
| `TASK-528` | `Step 6` | `docs/07_implementation_status.md` (TASK-527 record) | `crates/slicer-gcode/tests/gcode_emit_tdd.rs` | — | `S` | Verdict test asserting the recorded branch (AC-4) — on `CONSISTENT` against the recorded applied-height constants and declared delta |
| `TASK-529` | `Step 7` | `docs/19_visual_debug.md` (delegated) | `tmp/` artifacts, `tmp/239d-human-validation.md` | — | `M` | Human-gate artifacts: 0.3-pitch slices, visual-debug bundle, gate document |
| `TASK-530` | `Step 8` | `docs/07_implementation_status.md` (delegated), `docs/specs/support-independent-layer-z-split-plan.md`, `docs/specs/support-parity-gap-register.md` | docs only | — | `M` | Registration (TASK-523..530, queue row 4, gap row) + closure gates + full suite |

Copy costs from `implementation-plan.md`. Split before activation if any row is L or aggregate exceeds M.
