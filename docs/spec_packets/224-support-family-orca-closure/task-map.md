# Task Map: support-family-orca-closure

| docs/07 task ID | Packet step | Primary docs | Expected code surface | OrcaSlicer refs | Context cost | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| `TASK-335` | **Activation Gate** (`packet.spec.md` §Activation Gate — every listed support packet `implemented`, re-derived) &rarr; Steps 0, 1, 2, 3, 3a, 3b, 4, 5, 6, 7, 8. Step 4 (config keys) and Step 5 (honest tests) were re-opened 2026-09-03; Step 6's checklist must be regenerated against HEAD; Step 8's golden obligation is discharged by supersession (rename only) | `docs/specs/support-families-anchored-entities-plan.md` §§Visual And Differential Gates, Supersession | Host support aggregation (`crates/slicer-runtime/src/prepass.rs`, `crates/slicer-wasm-host/src/lib.rs` support-aggregation path); the four support modules (`modules/core-modules/tree-support-planner`, `traditional-support-planner`, `tree-support`, `traditional-support`); the runtime closure tests in `crates/slicer-runtime/tests/integration/support_family_closure.rs` mounted by `tests/integration/main.rs` (`[[test]] name = "integration" path = "tests/integration/main.rs"`); fixture/evidence requests; visual-debug inspection | `OrcaSlicerDocumented/src/libslic3r/Support/TreeSupport.cpp`; `SupportMaterial.cpp`; `SupportCommon.cpp` | M | **Do not start before the Activation Gate passes** — this packet measures support behaviour, so any evidence produced while a support packet is still open is stale on arrival. Re-derive every packet status with `grep '^status:' docs/spec_packets/<packet>/packet.spec.md`; freeze none of them here. Commit SHAs cited elsewhere in this packet were orphaned by the packet-224 squash and do not resolve (`design.md` §Orphaned commit references). Establishes authoritative behavioral closure and dispositions for superseded work. Closure commands must name the tests explicitly - the closure tests are bare `#[test] fn` wrappers in `tests/integration/main.rs` with no module prefix, so a bare `support_family_closure` filter matches zero tests and reports a vacuous green. The listed test names are the closure set as of the last execution pass; re-derive the current set from `crates/slicer-runtime/tests/integration/main.rs` before running. Use `cargo test -p slicer-runtime --test integration -- fixture_invariants family_reaches_region_routing invalid_geometry_fails matched_height_evidence differential_evidence final_gcode_roles supersedes_packet_213_and_task_329 task_163b_disposition support_never_intersects_model_at_exact_z accepted_demands_terminate_on_plate_or_model interface_is_topmost_and_carved_out no_overhang_mesh_produces_zero_support --exact`. |
