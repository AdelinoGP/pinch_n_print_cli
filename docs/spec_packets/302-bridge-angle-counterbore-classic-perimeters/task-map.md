# Task Map: 302-bridge-angle-counterbore-classic-perimeters

Single-task queue packet: P77 maps to one implementation slice. No multi-task crosswalk is needed — every step below serves the same two keys.

| docs/07 task ID | Packet step | Primary docs | Expected code surface | OrcaSlicer refs | Context cost | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| `TASK-000` (queue packet, `task_ids: []`) | Steps 1–4 | `docs/01_system_architecture.md` (prepass vs module seam; rule-4 trigger test does not fire); `docs/04_host_scheduler.md` (BASE timelines); `docs/DEVIATION_LOG.md` (DEV-193 minted) | `crates/slicer-ir/src/resolved_config.rs` (2 rows + map arms + manual PartialEq/Hash arms) + `crates/slicer-runtime/src/slice_postprocess_prepass.rs` (2 resolvers + overwrite + stage + 5 lib tests) + `prepass_slice_and_shell_tdd.rs` (2 executor tests) + lock arms + TOML rows + `docs/DEVIATION_LOG.md` + `docs/15_config_keys_reference.md` (regen) | `OrcaSlicerDocumented/src/libslic3r/LayerRegion.cpp` (`process_external_surfaces` arms borrowed; relative + align companions as named non-borrows) + `OrcaSlicerDocumented/src/libslic3r/PerimeterGenerator.cpp` (mode distinction borrowed; detector math as named non-borrow) + `OrcaSlicerDocumented/src/libslic3r/Layer.cpp` + `PrintObject.cpp` (recovery + slice-union as named non-borrows) + `OrcaSlicerDocumented/src/libslic3r/PrintConfig.cpp` (coFloat 0 / coEnum none) + `OrcaSlicerDocumented/src/libslic3r/Fill/FillBase.cpp` (orientation flow inherited, not re-armed) | `M` | P77: Tier B held, owner narrowed to the host prepass seam (`commit_shell_classification_builtin`'s bridge neighbourhood, ticket-36 precedent); membership 2/2, both wired, declaration-only 0; defaults ARE identity (AC-5); DEV-193 (relative-angle + align-offset + slice-union + reslice non-borrows); canonical scalarity held via existing per-object overlay (explicitly not ticket 125's tool axis); no numeric range rejection (ticket-113 rule; unknown enum spelling is the `flat_bridge_closing_join` fallback); no IR/WIT/emitter change; locked-path ADR-0062/0063 conformance pinned (AC-N2); neither key has a padding twin — honest absence (rides 132); no queue-count change; no new fog, nothing out of scope. |
