---
status: draft
packet: core-flow-consolidation
task_ids:
  - core/DUP-CORE
  - core/RETIRE
  - core/DUP-CORE-strengthen
backlog_source: docs/specs/test-quality-remediation-plan.md
context_cost_estimate: S
copy_note: Approved core-flow-consolidation queue entry; task IDs are program item IDs, not TASK-### mappings.
---

# Packet Contract: core-flow-consolidation

## Goal

Consolidate the inline and integration flow tests into `crates/slicer-core/tests/flow_tdd.rs`, preserving every distinct input/assertion case while strengthening the wider-bead production-oracle assertion.

## Scope Boundaries

Move and merge only the `#[cfg(test)]` test module in `crates/slicer-core/src/flow.rs` and the existing flow integration test home `crates/slicer-core/tests/flow_tdd.rs`. No production function, public type, export, test target, or behavior changes are authorized; the vertical-input correction test in `crates/slicer-core/src/lib.rs` belongs to the downstream `core-strengthen` packet.

## Prerequisites and Blockers

- Depends on: None; queue-generation dependencies are not implemented prerequisites.
- Unblocks: `core-bridge-dup-merge` packet generation only; `core-dup-merges` is superseded and has no packet directory.
- Activation blockers: Independent `spec-review --preflight`; no scope expansion.

## Acceptance Criteria

- **AC-1. Given** the current inline flow cases and integration flow cases, including `(0.4, 1.0)` spacing `0.1854 ± 1e-3` and `boundary * 0.5` rejection at the canonical threshold, **when** the consolidation is implemented, **then** one full `flow_tdd` execution passes every surviving witness for canonical spacing, wider spacing, below-layer-height positivity, threshold rejection, zero/negative width, actionable error text, round trip, monotonicity, `flow_to_width` zero/clamp behavior, all bridge branches, and all role-width matrices, while `flow.rs` has no `#[test]`. | `bash -lc 'set -euo pipefail; mkdir -p target; cargo test -p slicer-core --features host-algos --test flow_tdd -- --list 2>&1 | tee target/test-output.log >/dev/null; rg -q "canonical_0p4mm_bead_0p2mm_layer_spacing: test" target/test-output.log; cargo test -p slicer-core --features host-algos --test flow_tdd -- --nocapture 2>&1 | tee target/test-output.log >/dev/null; for n in canonical_0p4mm_bead_0p2mm_layer_spacing wider_bead_spacing_is_larger_than_canonical width_below_layer_height_still_has_positive_spacing spacing_errors_at_the_canonical_threshold zero_or_negative_width_errors production_reachable_negative_spacing_errors_with_actionable_message roundtrip_spacing_to_width_recovers_original spacing_is_monotone_in_line_width flow_to_width_zero_inputs_return_zero flow_to_width_result_is_at_least_spacing bridging_flow_non_thick_returns_ratio_unchanged bridging_flow_thick_round_cross_section_factor bridging_flow_thick_degenerate_inputs_fall_back_to_ratio resolve_role_width_bridge_and_first_layer_precedence_matrix resolve_role_width_bridge_fallback_covers_zero_and_absent_widths resolve_role_width_role_zero_and_positive_matrix resolve_role_width_auto_sentinel_uses_line_width_then_nozzle_width; do rg -q "test ${n} \.\.\. ok" target/test-output.log; done; ! rg -q -F "#[test]" crates/slicer-core/src/flow.rs'`
- **AC-2. Given** `line_width_to_spacing` uses `spacing = width - layer_height * (1 - PI/4)`, **when** the canonical and wider-bead cases run, **then** canonical `(0.4, 0.2)` is approximately `0.3571` within `1e-3`, wider `(0.5, 0.2)` equals the independently computed formula within `1e-4`, and wider spacing is greater than canonical spacing. | `bash -lc 'set -euo pipefail; mkdir -p target; cargo test -p slicer-core --features host-algos --test flow_tdd -- --list 2>&1 | tee target/test-output.log >/dev/null; rg -q "canonical_0p4mm_bead_0p2mm_layer_spacing: test" target/test-output.log; rg -q "wider_bead_spacing_is_larger_than_canonical: test" target/test-output.log; cargo test -p slicer-core --features host-algos --test flow_tdd -- --nocapture 2>&1 | tee target/test-output.log >/dev/null; rg -q "test canonical_0p4mm_bead_0p2mm_layer_spacing \.\.\. ok" target/test-output.log; rg -q "test wider_bead_spacing_is_larger_than_canonical \.\.\. ok" target/test-output.log'`
- **AC-3. Given** `width < layer_height` but `width - layer_height * (1 - PI/4) > 0`, **when** the below-layer-height case runs, **then** `(0.1, 0.2)` returns approximately `0.0571` within `1e-3`, and monotonic widths `0.3`, `0.4`, `0.5`, `0.6` produce nondecreasing spacing. | `bash -lc 'set -euo pipefail; mkdir -p target; cargo test -p slicer-core --features host-algos --test flow_tdd -- --list 2>&1 | tee target/test-output.log >/dev/null; rg -q "width_below_layer_height_still_has_positive_spacing: test" target/test-output.log; rg -q "spacing_is_monotone_in_line_width: test" target/test-output.log; cargo test -p slicer-core --features host-algos --test flow_tdd -- --nocapture 2>&1 | tee target/test-output.log >/dev/null; rg -q "test width_below_layer_height_still_has_positive_spacing \.\.\. ok" target/test-output.log; rg -q "test spacing_is_monotone_in_line_width \.\.\. ok" target/test-output.log'`
- **AC-4. Given** `(0.4, 2.0)` produces negative spacing and the inverse pair uses `(0.4, 0.2)`, **when** the actionable-error and round-trip witnesses run, **then** the error contains `0.40mm`, `2.00mm`, and `Increase the wall line width or reduce layer height`, and the recovered width differs from `0.4` by less than `1e-4`. | `bash -lc 'set -euo pipefail; mkdir -p target; cargo test -p slicer-core --features host-algos --test flow_tdd -- --list 2>&1 | tee target/test-output.log >/dev/null; rg -q "production_reachable_negative_spacing_errors_with_actionable_message: test" target/test-output.log; rg -q "roundtrip_spacing_to_width_recovers_original: test" target/test-output.log; cargo test -p slicer-core --features host-algos --test flow_tdd -- --nocapture 2>&1 | tee target/test-output.log >/dev/null; rg -q "test production_reachable_negative_spacing_errors_with_actionable_message \.\.\. ok" target/test-output.log; rg -q "test roundtrip_spacing_to_width_recovers_original \.\.\. ok" target/test-output.log'`
- **AC-5. Given** thick and non-thick bridge inputs, **when** all bridge-flow witnesses run, **then** the non-thick branch returns `0.85` unchanged, the canonical thick factor is approximately `1.5708` within `0.01`, and each non-positive bead/layer/nozzle input returns `0.9` without NaN. | `bash -lc 'set -euo pipefail; mkdir -p target; cargo test -p slicer-core --features host-algos --test flow_tdd -- --list 2>&1 | tee target/test-output.log >/dev/null; for n in bridging_flow_non_thick_returns_ratio_unchanged bridging_flow_thick_round_cross_section_factor bridging_flow_thick_degenerate_inputs_fall_back_to_ratio; do rg -q "${n}: test" target/test-output.log; done; cargo test -p slicer-core --features host-algos --test flow_tdd -- --nocapture 2>&1 | tee target/test-output.log >/dev/null; for n in bridging_flow_non_thick_returns_ratio_unchanged bridging_flow_thick_round_cross_section_factor bridging_flow_thick_degenerate_inputs_fall_back_to_ratio; do rg -q "test ${n} \.\.\. ok" target/test-output.log; done'`
- **AC-6. Given** the nine roles `OuterWall`, `InnerWall`, `ThinWall`, `TopSolidInfill`, `BottomSolidInfill`, `InternalSolidInfill`, `SparseInfill`, `BridgeInfill`, and `GapFill`, **when** all four role-width matrices run, **then** bridge and first-layer precedence, zero-width fallback, base-line fallback, and `1.125 * nozzle_diameter` fallback each return the independently selected value for every role. | `bash -lc 'set -euo pipefail; mkdir -p target; cargo test -p slicer-core --features host-algos --test flow_tdd -- --list 2>&1 | tee target/test-output.log >/dev/null; for n in resolve_role_width_bridge_and_first_layer_precedence_matrix resolve_role_width_bridge_fallback_covers_zero_and_absent_widths resolve_role_width_role_zero_and_positive_matrix resolve_role_width_auto_sentinel_uses_line_width_then_nozzle_width; do rg -q "${n}: test" target/test-output.log; done; cargo test -p slicer-core --features host-algos --test flow_tdd -- --nocapture 2>&1 | tee target/test-output.log >/dev/null; for n in resolve_role_width_bridge_and_first_layer_precedence_matrix resolve_role_width_bridge_fallback_covers_zero_and_absent_widths resolve_role_width_role_zero_and_positive_matrix resolve_role_width_auto_sentinel_uses_line_width_then_nozzle_width; do rg -q "test ${n} \.\.\. ok" target/test-output.log; done'`

## Negative Test Cases

- **AC-N1. Given** the exact threshold, zero width, and negative width inputs, **when** the rejection witnesses run, **then** `line_width_to_spacing(boundary, 0.2)` returns `Err` with `width_mm == boundary`, `layer_height_mm == 0.2`, and `spacing_mm <= 0.0`, `0.0` and `-0.4` also return `Err`, and the positive zero-layer witness returns exactly `0.4`. | `bash -lc 'set -euo pipefail; mkdir -p target; cargo test -p slicer-core --features host-algos --test flow_tdd -- --list 2>&1 | tee target/test-output.log >/dev/null; rg -q "spacing_errors_at_the_canonical_threshold: test" target/test-output.log; rg -q "zero_or_negative_width_errors: test" target/test-output.log; cargo test -p slicer-core --features host-algos --test flow_tdd -- --nocapture 2>&1 | tee target/test-output.log >/dev/null; rg -q "test spacing_errors_at_the_canonical_threshold \.\.\. ok" target/test-output.log; rg -q "test zero_or_negative_width_errors \.\.\. ok" target/test-output.log'`
- **AC-7. Given** the flow slice is implemented without closing unrelated core work, **when** the narrow ledger update is recorded, **then** the `core` row in §7 is the sole selected row, its table header has exactly six columns `Wave`, `State`, `Retired/changed symbols`, `Surviving/new coverage`, `Validation`, and `Remaining gap`, its row has exactly six cells, has state `partial`, names `core-flow-consolidation`, `flow.rs`, and `flow_tdd` in its changed-symbols cell, names `flow_tdd`, `width_below_layer_height_still_has_positive_spacing`, and `spacing_errors_at_the_canonical_threshold` in its coverage cell, records `cargo test`, `cargo check --workspace --all-targets`, `cargo clippy --workspace --all-targets -- -D warnings`, `cargo xtask check-literals`, and `check-test-quality` in its validation cell, and names the remaining non-flow gap `core-bridge-dup-merge` in its final cell. | `bash -lc 'set -euo pipefail; python -c "from pathlib import Path; t=Path(\"docs/specs/test-quality-remediation-plan.md\").read_text(); s=t.split(\"## 7. Ledger\",1)[1].split(\"\\n## \",1)[0]; rows=[x for x in s.splitlines() if x.startswith(\"|\") and not x.startswith(\"|---\")]; headers=[x for x in rows if x.split(\"|\")[1].strip()==\"Wave\"]; assert len(headers)==1 and [x.strip() for x in headers[0].split(\"|\")[1:-1]]==[\"Wave\",\"State\",\"Retired/changed symbols\",\"Surviving/new coverage\",\"Validation\",\"Remaining gap\"]; core=[x for x in rows if x.split(\"|\")[1].strip()==\"core\"]; assert len(core)==1; c=[x.strip() for x in core[0].split(\"|\")[1:-1]]; assert len(c)==6; wave,state,changed,coverage,validation,gap=c; assert wave==\"core\" and state==\"partial\"; assert all(x in changed for x in (\"core-flow-consolidation\",\"flow.rs\",\"flow_tdd\")); assert all(x in coverage for x in (\"flow_tdd\",\"width_below_layer_height_still_has_positive_spacing\",\"spacing_errors_at_the_canonical_threshold\")); assert all(x in validation for x in (\"cargo test\",\"cargo check --workspace --all-targets\",\"cargo clippy --workspace --all-targets -- -D warnings\",\"cargo xtask check-literals\",\"check-test-quality\")); assert \"core-bridge-dup-merge\" in gap and \"non-flow\" in gap; print(\"PASS: anchored core ledger row has six validated cells\")"'`

## Verification

- `cargo check --workspace --all-targets`
- `cargo clippy --workspace --all-targets -- -D warnings`
- `bash -lc 'set -euo pipefail; mkdir -p target; cargo test -p slicer-core --features host-algos --test flow_tdd -- --list 2>&1 | tee target/test-output.log >/dev/null; rg -q ": test$" target/test-output.log; cargo test -p slicer-core --features host-algos --test flow_tdd -- --nocapture 2>&1 | tee target/test-output.log >/dev/null; for n in canonical_0p4mm_bead_0p2mm_layer_spacing wider_bead_spacing_is_larger_than_canonical width_below_layer_height_still_has_positive_spacing spacing_errors_at_the_canonical_threshold zero_or_negative_width_errors production_reachable_negative_spacing_errors_with_actionable_message roundtrip_spacing_to_width_recovers_original spacing_is_monotone_in_line_width flow_to_width_zero_inputs_return_zero flow_to_width_result_is_at_least_spacing bridging_flow_non_thick_returns_ratio_unchanged bridging_flow_thick_round_cross_section_factor bridging_flow_thick_degenerate_inputs_fall_back_to_ratio resolve_role_width_bridge_and_first_layer_precedence_matrix resolve_role_width_bridge_fallback_covers_zero_and_absent_widths resolve_role_width_role_zero_and_positive_matrix resolve_role_width_auto_sentinel_uses_line_width_then_nozzle_width; do rg -q "test ${n} \.\.\. ok" target/test-output.log; done'`

## Authoritative Docs

- `docs/specs/test-quality-remediation-plan.md` §1, §4, §5.1, §6, Packet Queue, and Approved first-packet boundary - direct bounded reads.
- `docs/22_test_quality.md` §§1–5 - direct bounded read; oracle, negative-control, and zero-test discipline.
- `docs/adr/0064-existing-tests-retire-if-unjustified.md` - direct read; earn-their-keep disposition standard.
- `docs/adr/0065-test-quality-gate-with-delayed-enforce-mode.md` - direct read; report-mode gate status.
- `docs/00_project_overview.md` normative document map and `.agents/doc-index.md` - direct reads.
- `.agents/skills/spec-review/references/preflight-gate.md` S0–S8 - direct read; symbol and target checks.

## Doc Impact Statement

Specific implementation-time documentation impact: `docs/specs/test-quality-remediation-plan.md` §7 Ledger - the implementation worker must update only the `core` ledger row with this flow slice's partial state, retired/changed symbols, surviving coverage, actual validations, and remaining non-flow `core-bridge-dup-merge` gap; it must not close the core wave or rewrite the queue/other waves. Verification is AC-7's exact section-anchored six-column parser command. This is bookkeeping, not a production contract change.

<!-- snippet: context-discipline -->
## Context Discipline Note

This packet was generated against the context_discipline preamble shared by `spec-packet-generator`, `swarm`, and `spec-review`. Downstream agents implementing or reviewing this packet must:

- treat `design.md`'s code change surface as the authoritative files-in-scope list
- honor `design.md`'s out-of-bounds list — those files must not be loaded directly
- delegate every cargo run and authoritative-doc fact-check
- obey the shared absolute context bands: 120k reading budget with hand-off at 150k (standard); the extended band (240k reading / 300k hard stop) only via swarm's escalation protocol

Aggregate context cost above is the sum of per-step costs in `implementation-plan.md`. If any single step is rated L, the packet must be split before activation (an extended-band run may carry a single L step only when `design.md` justifies why it cannot be split).
