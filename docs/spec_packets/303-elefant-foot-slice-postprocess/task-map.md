# Task Map: 303-elefant-foot-slice-postprocess

This packet has **no `docs/07_implementation_status.md` task IDs** (`task_ids: []`). It is a wayfinder queue packet: its backlog slice is P86 in the map "Close the OrcaSlicer FFF feature gap", authored under ticket 93. The crosswalk below maps the two P86 config keys — the queue's unit of account — onto packet steps, in place of a `TASK-###` column, matching the queue-packet convention set by packets 296–302.

| Queue unit (P86 key) | Packet step | Primary docs | Expected code surface | OrcaSlicer refs | Context cost | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| *(kernel prerequisite — no key)* | `Step 1` | `docs/ORCASLICER_ATTRIBUTION.md`, `docs/08_coordinate_system.md` | `crates/slicer-core/src/algos/elephant_foot.rs`, `crates/slicer-core/src/algos/mod.rs` | `OrcaSlicerDocumented/src/libslic3r/ElephantFootCompensation.cpp` | `S` | Signatures, porting header, both canonical early-outs. Proves AC-1 and AC-3 before any key is read. |
| *(kernel prerequisite — no key)* | `Step 2` | `docs/13_slicer_helpers_crate.md`, `docs/08_coordinate_system.md` | `crates/slicer-core/src/algos/elephant_foot.rs` | `OrcaSlicerDocumented/src/libslic3r/ElephantFootCompensation.cpp` | `M` | The width-limited variable offset itself. AC-4 is the step's falsifier: it fails a uniform offset. |
| `elefant_foot_compensation`, `elefant_foot_compensation_layers` | `Step 3` | `docs/03_wit_and_manifest.md`, `docs/01_system_architecture.md` | `modules/core-modules/elefant-foot/{elefant-foot.toml,Cargo.toml}` | `OrcaSlicerDocumented/src/libslic3r/PrintConfig.cpp` | `S` | Both keys declared at canonical defaults on a new `Layer::SlicePostProcess` module. Declaration alone is not coverage — Steps 4–5 wire them. |
| `elefant_foot_compensation_layers` | `Step 4` | `docs/21_data_defaults_and_fixtures.md` | `modules/core-modules/elefant-foot/src/lib.rs` | `OrcaSlicerDocumented/src/libslic3r/PrintObjectSlice.cpp` | `M` | The taper divisor and layer-band guard become live: AC-6 asserts the canonical per-layer formula at `layers = 3`. The raft gate (AC-7) and the negative-spacing contract (AC-N1) land here too. |
| `elefant_foot_compensation` | `Step 5` | `docs/02_ir_schemas.md` | `modules/core-modules/elefant-foot/src/lib.rs` | `OrcaSlicerDocumented/src/libslic3r/PrintObjectSlice.cpp` | `M` | The magnitude reaches the kernel and the shrunk footprint is written back per region. AC-5 pins the default no-op; AC-8 pins the `RegionKey` identity. |
| *(both keys — build and packaging)* | `Step 6` | none (code reads only: `dist/editions.toml`, `xtask/src/{editions,build_guests}.rs`) | `modules/core-modules/elefant-foot/Cargo.toml` (conditional only) | none | `S` | AC-10: the guest component builds and the freshness gate exits `0`. Read-and-verify step — edition membership needs **no** edit (guests are discovered dynamically; `dist/editions.toml` names only the natively-integrated `hybrid` three). |
| *(both keys — ledger)* | `Step 7` | `docs/DEVIATION_LOG.md` | `docs/specs/orca-feature-gap/issues/{04-asset-tier-assignment.md,05-asset-packet-list.md}`, `docs/DEVIATION_LOG.md` | none (citations by file + function only) | `S` | AC-11 plus the Doc Impact greps. Re-derive the next free `DEV-###` here; the authoring-time value is a ledger fact and will have rotted. |

Costs are copied from `implementation-plan.md` §Per-Step Budget Roll-Up. No row is `L`; the aggregate is `M`.
