# Task Map: infill-angle-and-multiline-keys

Use this crosswalk when a packet spans more than one task ID, reopens prior work, or supersedes an earlier packet. **This packet emits the template's own skip clause:** it is a single-coherent-slice queue packet with `task_ids: []` (queue precedent — packets 234a, 253–261), so the `docs/07` crosswalk is N-A. Implementation is recorded against wayfinder ticket 15 (`docs/specs/orca-feature-gap/issues/15-author-packet-p08-strength-infill-infill-modules.md`), jointly with packet 262b.

This directory is the renamed `262-infill-pattern-keys`; the split into 262a/262b was approved this session (210a/210b, 238a/238b precedent). 262a owns the four angle/multiline keys; 262b owns pattern selection and fill-side gap fill. Neither packet supersedes the other — together they replace the single pre-split packet, and both must land for ticket 15 to close.

| docs/07 task ID | Packet step | Primary docs | Expected code surface | OrcaSlicer refs | Context cost | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| — (queue packet, `task_ids: []`) | Steps 1–6 | `docs/03_wit_and_manifest.md`, `docs/adr/0027-gyroid-multi-role-fill-holder.md`, `docs/adr/0028-infill-postprocess-contract-prior-ir-and-partitioned-polygons.md`, `docs/08_coordinate_system.md`, `docs/21_data_defaults_and_fixtures.md`, `docs/15_config_keys_reference.md` (generated) | `modules/core-modules/rectilinear-infill/{rectilinear-infill.toml,src/lib.rs,Cargo.toml,tests/*}`, `modules/core-modules/gyroid-infill/{gyroid-infill.toml,src/lib.rs,tests/*}`, `crates/slicer-scheduler/tests/integration/config_bounds_enforcement_tdd.rs`, `crates/slicer-runtime/tests/integration/gcode_header_thumbnail_config_blocks_tdd.rs`, both rebuilt guest `.wasm` artifacts | `OrcaSlicerDocumented/src/libslic3r/Fill/Fill.cpp` (`Layer::make_fills`, `group_fills`, `calculate_infill_rotation_angle`), `Fill/FillBase.cpp` (`multiline_fill`), `Fill/FillRectilinear.cpp` (`fill_surface_by_multilines`), `PrintConfig.cpp` (the four declarations) | M | Re-authored under the map's Authoring rules 1–6; Tier B. Re-derive the deviation-row count and the crosswalk question at completion time per the ledger-fact rule |
