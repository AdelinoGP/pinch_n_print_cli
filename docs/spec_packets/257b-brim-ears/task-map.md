# Task Map: brim-ears

Use this crosswalk when a packet spans more than one task ID, reopens prior work, or supersedes an earlier packet. **This packet emits the template's own skip clause:** it is a single-coherent-slice queue packet with `task_ids: []` (queue precedent — packets 234a, 253–265), so the `docs/07` crosswalk is N-A. Implementation is recorded against wayfinder ticket 12 (`docs/specs/orca-feature-gap/issues/12-author-packet-p05-others-brim-skirt-brim.md`).

**This is the second half of a split, and a net-new directory.** The prior `docs/spec_packets/257-brim-type-and-brim-keys/` is split into `257a-brim-type-and-object-gap` and `257b-brim-ears` (this packet), with explicit user approval, under the 210a/210b letter-suffix precedent and the map's Authoring rules 1–7. The prior draft dispositioned both ear keys "declared-with-gap" — prohibited by rule 1 — on a brim generator that emitted a rectangle around the plate's global bounding box, which has no object corners to detect. This packet supersedes nothing; `257a` carries the split's overwrite history.

**Hard dependency: `257a-brim-type-and-object-gap`**, which is `status: draft` at authoring time and is therefore a **FORWARD-DEP**, not a satisfied prerequisite. `257a` builds the per-object layer-0 contour that ear anchors are vertices of, the `brim_type` mode dispatch this packet extends, and the gap-offset island that turns each ear from a disc into an annulus. `implementation-plan.md` Step 0 is a gate that stops this packet if `257a` has not landed. The two packets also couple through `size_ear`, which canonical computes from `brim_object_gap` — `257a`'s key.

**Inherited divergence.** Ear anchors sit on the *deposited* outline (`257a`'s DIV-1: contour derived from layer-0 `ExtrusionRole::OuterWall` loops with a half-line-width compensation), not on slice polygons. This packet's DIV-2 records the inheritance; it must be read from `257a`'s `design.md` § Recorded Divergences rather than re-litigated here.

**Keys handed to no owner.** `brim_ears_outer_only` (DIV-3) is not a ticket-12 key; canonical uses it to let ear modes also produce an inner brim, and the port fixes it at the canonical default. It needs a queue row. `brim_type = painted` remains an unshipped value with no point-valued paint carrier — reported by `257a` and re-asserted here by AC-N3.

Note for the closing agent: this packet adds no module, so `docs/07_implementation_status.md`'s module inventory does not change. It moves two keys from absent to live, so `docs/15_config_keys_reference.md` must be regenerated.

| docs/07 task ID | Packet step | Primary docs | Expected code surface | OrcaSlicer refs | Context cost | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| — (queue packet, `task_ids: []`) | Steps 0–4 | `docs/00_project_overview.md`, `docs/03_wit_and_manifest.md` (manifest schema shape), `docs/08_coordinate_system.md`, `docs/21_data_defaults_and_fixtures.md`, `docs/ORCASLICER_ATTRIBUTION.md`, `docs/15_config_keys_reference.md` (generated), `docs/spec_packets/257a-brim-type-and-object-gap/design.md` §Recorded Divergences (that section only) | `modules/core-modules/skirt-brim/{src/lib.rs,skirt-brim.toml,tests/brim_ears_tdd.rs}` + `crates/slicer-scheduler/tests/integration/config_bounds_enforcement_tdd.rs` + one generated doc | `Brim.cpp` (`make_brim_ears_auto`, `outer_inner_brim_area`, `make_brim_ears`), `Polygon.cpp` (`convex_points`, `concave_points`), `MultiPoint.cpp` (`_douglas_peucker`) | M (aggregate; no step above M) | Authored under the map's Authoring rules 1–7; **Tier B**. 2 keys built, 0 returned, 0 dead-in-canonical; additionally ships the `brim_ears` value of `257a`'s `brim_type`. FORWARD-DEP on draft `257a` — Step 0 gates on it. No WIT, no IR, no `ResolvedConfig` change. The angle sense is the known trap: larger `brim_ears_max_angle` means MORE corners qualify. Re-derive `SkirtBrim`'s field count, `POLY_SIDE_COUNT`, test auto-discovery, and every other ledger fact at point of use |
