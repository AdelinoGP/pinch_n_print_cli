# Task Map: 252-visual-debug-silhouette-remaining-taps

This packet spans four new task IDs, so this crosswalk is emitted; it owns the `docs/07_implementation_status.md` mapping. TASK-458..TASK-461 are **new rows** (re-derived 2026-08-27 at authoring time, as max+1..max+4 over the union of: `docs/07_implementation_status.md`; `docs/specs/*.md` plan files; all local `docs/spec_packets/*/task-map.md` — highest TASK-457, in `251-visual-debug-silhouette-seam-overlays`; and `origin/master`-only packets 243–246 via `git show` — highest TASK-356). No existing open TASK covers this work (the gap is packet 247's `[FWD]` unowned-taps question; visual-debug lineage TASK-267..270 closed with packets 157–161). The completion-gate worker adds these rows to docs/07 with this packet as their source. Any future re-derivation must sweep `docs/specs/*.md` and `origin/master` task-maps, not just local packet dirs — ledger fact, re-derive at write time.

| docs/07 task ID | Packet step | Primary docs | Expected code surface | OrcaSlicer refs | Context cost | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| `TASK-458` | `Step 1` | `docs/specs/visual-debug-silhouette-side-views-plan.md` D1/D2/D8 (RegionMapping row) | `crates/slicer-runtime/src/visual_debug_render.rs` (RegionMapping extraction arm, tint classes, unjoined-entry warning) | none (PnP-native) | M | Render-time join against the capture's retained `Vec<SliceIR>`; per-region slabs; ascending-RGB `config_tint` class order — the determinism story 247 flagged |
| `TASK-459` | `Step 2` | plan fact 5, D1/D2/D8 (OverhangAnnotation row), §7 | `crates/slicer-runtime/src/visual_debug_render.rs` + `crates/slicer-runtime/src/lib.rs` (`SilhouetteSliceHeightIndex`, `build_silhouette_slice_height_index`, `render_silhouette_overhang_composite`, quartile palette, `RenderError::InvalidQuartile`) | none | M | Honest slabs without a `CapturedIr` change: single-height fast path + mixed-height `polygon_ops::intersection` partition; three fail-closed arms |
| `TASK-460` | `Step 3` | plan §6 R2, §8; packet 247 AC-N5 | `crates/pnp-cli/src/visual_debug.rs` (`SILHOUETTE_TAP_STAGE_IDS` lift, assembly routing, tool-mode `ToolColorUnavailable`), `crates/pnp-cli/tests/visual_debug_validation_tdd.rs` (arm retirement + acceptance) | none | S | Retires 247's interim rejections for exactly these two taps; `silhouette_unsupported_taps_rejected_with_reasons` survives with arena/MeshAnalysis/SeamPlanning arms (250's AC-N2 re-runs it) |
| `TASK-461` | `Step 4` | plan D3; `docs/19_visual_debug.md` | `crates/pnp-cli/tests/visual_debug_silhouette_bundle_tdd.rs` (end-to-end RegionMapping bundle), `docs/19_visual_debug.md` (two tap-table rows) | none | M | Wedge end-to-end uses RegionMapping only (overhang-band presence on the wedge is unverified — renderer-level fixtures own OverhangAnnotation, mirroring 247's support precedent) |

Costs copied from `implementation-plan.md`. No row is L; aggregate M.
