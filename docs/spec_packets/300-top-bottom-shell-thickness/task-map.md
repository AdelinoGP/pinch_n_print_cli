# Task Map: 300-top-bottom-shell-thickness

Single-task queue packet: P74 maps to one implementation slice. No multi-task crosswalk is needed — every step below serves the same two keys.

| docs/07 task ID | Packet step | Primary docs | Expected code surface | OrcaSlicer refs | Context cost | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| `TASK-000` (queue packet, `task_ids: []`) | Steps 1–3 | `docs/01_system_architecture.md` (prepass vs module seam; rule-4 trigger test does not fire); `docs/08_coordinate_system.md` (mm-domain `EPSILON`, no unit conversion); `docs/DEVIATION_LOG.md` (DEV-191 minted) | `crates/slicer-ir/src/resolved_config.rs` (2 rows) + `crates/slicer-runtime/src/slice_postprocess_prepass.rs` (resolver reads + 2 walk arms + 3 tests) + `docs/DEVIATION_LOG.md` + `docs/15_config_keys_reference.md` (regen) | `OrcaSlicerDocumented/src/libslic3r/PrintObject.cpp` (`discover_horizontal_shells` arms; `discover_vertical_shells` + `PrintObject::infill` scatter as same-shape precedent / named non-borrows) + `OrcaSlicerDocumented/src/libslic3r/PrintConfig.cpp` (defaults) | `M` | P74: Tier B held, owner narrowed to the host prepass seam (`commit_shell_classification_builtin`'s Pass-2 walks, ticket-36 precedent); membership 2/2, all wired, declaration-only 0; defaults ARE identity (AC-3); DEV-191 (scatter + spiral-gate + reslice non-borrows); canonical scalarity held via existing per-object overlay (explicitly not ticket 125's tool axis); no numeric range rejection (ticket-113 rule); no IR/WIT/emitter change; locked-path ADR-0062/0063 conformance pinned (AC-N1); CONFIG_BLOCK honest absence (rides 132, no padding edits); no queue-count change; no new fog, nothing out of scope. |
