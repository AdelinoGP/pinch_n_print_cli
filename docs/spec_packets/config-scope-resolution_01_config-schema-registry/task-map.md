# Task Map: config-schema-registry

This implemented packet owns only approved config-scope-resolution plan Packet Queue row 1 and the single backlog task `TASK-562`. The map is intentionally explicit because the row spans relocations, a new crate, registry tests, source repairs, and documentation closure; no row below implies ownership of queue rows 2–10.

| docs/07 task ID | Packet step | Primary docs | Expected code surface | OrcaSlicer refs | Context cost | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| `TASK-562` | Step 1a | Plan §Crate topology; docs/21 | `slicer-ir/src/config_schema.rs`, `slicer-ir/src/lib.rs`, scheduler manifest aliases | None | `M` | Moves config carriers, preserves public entries, and introduces the typed fields before final retirement. |
| `TASK-562` | Step 1b | Plan §Crate topology; ADR-0019 | `crates/slicer-ir/src/slice_ir.rs`, `crates/slicer-ir/src/lib.rs`, `crates/slicer-scheduler/src/manifest.rs`, `crates/slicer-scheduler/src/region_split.rs`, and `crates/slicer-scheduler/src/lib.rs` | None | `S` | `1b-i` moves `AggregatedRegionSplitEntry`/`RegionSplitValueType` and retains scheduler module aliases; `1b-ii` owns the exact IR flat aliases and the exact scheduler flat lines `pub use manifest::{ConfigFieldEntry, ConfigSchema, RegionSplitValueType};` and `pub use region_split::AggregatedRegionSplitEntry;`. |
| `TASK-562` | Step 1c | Plan §Registry channels; ADR-0067/0069 | `slicer-ir/src/resolved_config.rs`, scheduler `manifest.rs`/`execution_plan.rs` | None | `M` | Exact const-safe seven-field `HostRuntimeKey`; `wall_generator` carries `&'static [&'static str]` denials for `object`, `layer_range`, `modifier`, `paint_semantic`, and `tool`; assembly retains them while host JSON remains unchanged. |
| `TASK-562` | Step 1d | Plan §Crate topology; ADR-0019 | root `Cargo.toml`, `slicer-core` dependency/import, new `slicer-config` manifest/lib shell | None | `M` | Removes the core→scheduler edge and creates `slicer-config` with only `slicer-ir` plus dev-only `toml = "0.8"`. |
| `TASK-562` | Step 1e | Plan §Crate topology; docs/21 | scheduler execution-plan source plus five runtime/scheduler test files | None | `S` | Completes the ten non-manifest `ConfigFieldEntry` literal sites with FRU/waiver discipline before `validate` retirement. |
| `TASK-562` | Step 2 | Plan §Owner decisions item 1; docs/11 | `slicer-ir/src/config_schema.rs`, scheduler manifest/tests, runtime wire confirmation | None | `S` | Retires `validate` and bumps `CONFIG_SCHEMA_WIRE_VERSION` to `"1.3.0"` in one blast-radius-owned step. |
| `TASK-562` | Step 3a | Plan §Registry + §Reconciliation; ADR-0067/0068/0069; docs/22 | `slicer-config/src/lib.rs` and its two direct integration tests | None | `M` | Freezes the exact registry/channel API, carries and validates `wall_generator`'s five explicit narrower-scope denials, and tests represented-module selector rejection plus positive/negative reconciliation behavior. Packet 7 (`TASK-568`) remains owner of the broad machine/emitter deny-list rollout. |
| `TASK-562` | Step 3b | Plan §Selector/base-key fields | scheduler `manifest.rs` parser | None | `S` | Parses `selector`, `base_key`, and `denied_scopes` with optional defaults; no `validate`. |
| `TASK-562` | Step 4 | Plan §Census; docs/22 | `registry_census_tdd.rs` plus real 24-manifest TOML parse | None | `M` | Derives channel union; verifies 270/179/54 and `object_height:*`/`layer_height:*` without a roster. |
| `TASK-562` | Step 5a | Plan §Type conflicts; ADR-0067 | six bridge/width manifest declarations plus `traditional-support` enum | `OrcaSlicerDocumented/src/libslic3r/PrintConfig.cpp` (delegated) | `S` | `initial_layer_line_width` has exactly five owners; wave is bridge-only; classic gains only its base key. |
| `TASK-562` | Step 5b | Plan §Type conflicts; docs/03 | host layer executor and five guest reader sources | None | `M` | Checks each required key separately with an exact `get_abs_value` key argument, explicit non-empty base, and a result-consumer proof that rejects discarded semicolon calls and `_ = get_abs_value("bridge_line_width", nozzle_diameter);` while accepting assignments, returns, arguments, and fallback chains; receiver names and line breaks are not hardcoded. Classic remains a read-only control; guest artifacts are rebuilt/fresh and freshness is checked by exit code. |
| `TASK-562` | Step 6 | Plan §Cross-cutting + Doc Impact; ADR-0019 | **6a:** docs/03 manifest example/common fields/cross-validation, docs/05 diagnose checks, schema `VALID_SEVERITIES`/comment; **6b:** docs/04 `RegionMapping (Builtin) — aggregated_region_split Threading` section and ADR-0019 `Status`; generated docs/15 by tool | None | `S` | `6b` edits only the bounded docs/04 section plus ADR-0019 (two files, within the ≤3-file cap); the section-scoped verifier proves the stale deferred core→scheduler paragraph is replaced by relocated IR ownership and the removed normal edge. `6a` performs the permitted `VALID_SEVERITIES` code edit and the generated-doc gate uses a raw-pipe-safe first-key/final-owner parser. |

The packet remains `active`, preserves TASK-562 ownership, and exports no implementation status change. Subsequent queue rows consume its exports but remain independently queued.
