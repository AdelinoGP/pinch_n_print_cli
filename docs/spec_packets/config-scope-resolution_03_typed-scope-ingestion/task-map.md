# Task Map: typed-scope-ingestion

This single-task packet emits a task map because the approved batch request requires all five packet files and because `TASK-564` crosses ingestion, scheduler, runtime, oracle, visual-debug, and documentation seams.

| docs/07 task ID | Packet step | Primary docs | Expected code surface | OrcaSlicer refs | Context cost | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| `TASK-564` | Steps 1-8 (Step 5 has 5a/5b) | ADR-0067, ADR-0068, ADR-0069, `CONTEXT.md`, `docs/02_ir_schemas.md`, `docs/04_host_scheduler.md` perimeter-selection section, `docs/22_test_quality.md`, `docs/19_visual_debug.md` | `slicer-config` ingestion; model-io adapter; scheduler typed consumers and exact `wall_generator` selector extraction; wasm-host/runtime composition; runtime oracle; pnp-cli visual-debug test; typed-ingestion and perimeter-selection docs | None — no OrcaSlicer source port | `M` | Reconciles packet 01's registry/assembly dependency, consuming `AssemblyOutcome.registry` and opaque `AssemblyOutcome.warnings: Vec<RegistryWarning>` without relying on any warning variant or field; packet 03 consumes no `RegistryWarning` variant or field, and all variant/field details are non-exhaustive packet-01 diagnostics outside scope. Consumes packet 02's new oracle/registration and net-new `slicer-config` dev-dependency while reusing existing `zip`; proves decode-once typed deltas, exact five-value correction, warn-and-keep unknowns, typed perimeter selection, preserved non-selector routing, real bundle output, and removal of the raw `wall_generator` requirement from canonical scheduler docs. |
