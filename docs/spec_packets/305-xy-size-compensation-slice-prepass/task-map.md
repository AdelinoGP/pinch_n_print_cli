# Task Map: 305-xy-size-compensation-slice-prepass

This packet has **no `docs/07_implementation_status.md` task IDs** (`task_ids: []`). It is a wayfinder queue packet: its backlog slice is P88 in the map "Close the OrcaSlicer FFF feature gap", authored under ticket 95. The crosswalk below maps the queue's unit of account — the config key — onto packet steps, in place of a `TASK-###` column, matching the queue-packet convention set by packets 296-304.

| Queue unit (P88 key) | Packet step | Primary docs | Expected code surface | OrcaSlicer refs | Context cost | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| `xy_contour_compensation`, `xy_hole_compensation` | `Step 1` | `docs/ORCASLICER_ATTRIBUTION.md`, `docs/08_coordinate_system.md` | `crates/slicer-core/src/algos/xy_size_compensation.rs`, `crates/slicer-core/src/algos/mod.rs`, `crates/slicer-core/tests/algo_xy_size_compensation_tdd.rs` | `OrcaSlicerDocumented/src/libslic3r/PrintObjectSlice.cpp` (`PrintObject::_shrink_contour_holes`) | `S` | Both keys become live *as kernel parameters* immediately: the contour offset (AC-1), the hole offset and its sign (AC-2), both negative (AC-3), and the drop-on-annihilation rule (AC-6). The hole sign is the one thing that must be red before it is green. |
| `xy_contour_compensation`, `xy_hole_compensation` | `Step 2` | `docs/08_coordinate_system.md` | `crates/slicer-core/src/algos/xy_size_compensation.rs`, `crates/slicer-core/tests/algo_xy_size_compensation_tdd.rs` | `OrcaSlicerDocumented/src/libslic3r/PrintObjectSlice.cpp` (the compensation block in `PrintObject::slice_volumes`) | `M` | The two keys drive the branch selection, not just the offsets: their signs pick canonical's positive merge-and-re-split path (AC-7) or its negative merged-trimming path (AC-8), with the two-pass order (AC-4) and default identity (AC-5). This is the step that makes the packet more than plumbing. |
| *(both keys — seam)* | `Step 3` | `docs/04_host_scheduler.md` | `crates/slicer-scheduler/src/execution_plan.rs`, `crates/slicer-scheduler/tests/contract/stage_list_consistency_tdd.rs`, `crates/slicer-runtime/src/prepass.rs` | `OrcaSlicerDocumented/src/libslic3r/PrintObject.cpp` (`PrintObject::slice`) | `S` | `PrePass::XySizeCompensation` enters `STAGE_ORDER` between Slice and OverhangAnnotation, classified host-only. AC-10. The `STAGE_ORDER` and `HOST_ONLY_STAGES` edits must not be separated. |
| `xy_contour_compensation`, `xy_hole_compensation` | `Step 4` | `docs/21_data_defaults_and_fixtures.md` | `crates/slicer-ir/src/resolved_config.rs`, `crates/slicer-ir/tests/resolved_config_xy_size_compensation_tdd.rs` | `OrcaSlicerDocumented/src/libslic3r/PrintConfig.cpp` | `S` | Both keys become real `ResolvedConfig` fields at canonical default `0.0`, unbounded, so CLI, per-object overlay and per-region resolution carry them. AC-9, AC-N3. Declaration alone is not coverage — Step 5 is what routes them. |
| `xy_contour_compensation`, `xy_hole_compensation` (per-object gate) | `Step 5` | `docs/04_host_scheduler.md`, `docs/02_ir_schemas.md` | `crates/slicer-runtime/src/builtins/xy_size_compensation_producer.rs`, `crates/slicer-runtime/src/builtins/mod.rs`, `crates/slicer-runtime/src/prepass.rs`, `crates/slicer-runtime/tests/executor/prepass_xy_size_compensation_stage_order_tdd.rs` | `OrcaSlicerDocumented/src/libslic3r/PrintConfig.hpp` (`PrintObjectConfig` membership) | `M` | The config reaches the kernel through `RegionMapIR::config_for` per `RegionKey`, grouped by `object_id` to match canonical's per-`PrintObject` read, and the result reaches the blackboard via `replace_slice_ir`. AC-11, AC-12, AC-13. **AC-12 is the load-bearing one**: it proves shell classification consumes the compensated footprint, which is the whole reason the owner was corrected. |
| *(both keys — ledger)* | `Step 6` | `docs/04_host_scheduler.md`, `docs/01_system_architecture.md`, `docs/DEVIATION_LOG.md` | `docs/04_host_scheduler.md`, `docs/01_system_architecture.md`, `docs/DEVIATION_LOG.md`, `docs/15_config_keys_reference.md` (generator output) | none (citations by file + function only) | `S` | AC-16 plus the Doc Impact greps. Re-derive the next free `DEV-###` here; the authoring-time value is a ledger fact and will have rotted. Four clauses: the unported painted-object suppressions, the missing region-assignment bbox growth, the uncompensated modifier footprint, and serial-where-canonical-is-parallel. |
| *(both keys — queue accounting)* | `Step 7` | `docs/specs/orca-feature-gap/map.md` §Notes | `docs/specs/orca-feature-gap/issues/04-asset-tier-assignment.md`, `docs/specs/orca-feature-gap/issues/05-asset-packet-list.md` | none | `S` | AC-15. The tier table's `new contour-compensation module` owner is replaced by the host built-in on both rows; the P88 heading links this packet. The queue count stays 409 — no key is added or removed. |

Costs are copied from `implementation-plan.md` §Per-Step Budget Roll-Up. No row is `L`; the aggregate is `M`.
