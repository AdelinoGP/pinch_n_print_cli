---
status: draft
packet: test-quality-remediation_10_core-retire
task_ids:
  - 'core/RETIRE (excluding flow)'
backlog_source: docs/specs/test-quality-remediation-plan.md
context_cost_estimate: M
copy_note: Generated from the approved core-wave queue; implementation and queue bookkeeping remain downstream responsibilities.
---

# Packet Contract: core-retire

## Goal

Retire the two `slicer-core` tests that no production defect can falsify, and re-home the slice-closing-radius no-op intent as a real contrast pair that drives the production gate in the `host-algos`-gated prepass target.

## Scope Boundaries

This packet deletes `clip_operation_variants_are_distinct` from the inline test module of `crates/slicer-core/src/polygon_ops.rs`, deletes `slice_closing_radius_zero_is_noop` from `crates/slicer-core/tests/triangle_mesh_slicer_tdd.rs`, adds one replacement contrast-pair test plus its local mesh fixture to `crates/slicer-core/tests/algo_prepass_slice_tdd.rs`, and updates the owning `core` progress row in the remediation plan during implementation. It changes no production behavior, public API, manifest, feature, Cargo target, or fixture file, and the `flow.rs` retirement rows remain owned by `core-flow-consolidation`.

## Prerequisites and Blockers

- Depends on: `core-strengthen` (`status: draft`, independently `PREFLIGHT PASS`, exporting no symbols, files, fixtures, APIs, or test targets); the dependency serializes packet generation only. AC-1's survivor rationale names `core-strengthen`'s strengthened boolean test as the coverage that makes the retirement safe, but this packet's acceptance never executes that test and does not require it to be implemented first.
- Unblocks: queue row #11 `core-paint` packet generation after this packet is generated and independently reviewed.
- Activation blockers: an explicit activation request is still required; status remains `draft`.

## Acceptance Criteria

- **AC-1. Given** `clip_operation_variants_are_distinct` in the inline `tests` module of `crates/slicer-core/src/polygon_ops.rs`, whose body is `assert_ne!(ClipOperation::Union, ClipOperation::Difference)` over a fieldless enum with a derived `PartialEq`, **when** the retirement lands, **then** the name is absent from that file, the file's `#[test]` count drops from `21` to exactly `20`, and `ClipOperation` remains referenced by production code in the same file through the `op: ClipOperation` parameter of `clip_polygons`, so the type does not go dead. | `set -euo pipefail; if rg -q 'clip_operation_variants_are_distinct' crates/slicer-core/src/polygon_ops.rs; then echo 'FAIL: retired test still present'; exit 1; fi; n=$(rg -c '#\[test\]' crates/slicer-core/src/polygon_ops.rs); test "$n" -eq 20 || { echo "FAIL: test count is not 20"; exit 1; }; rg -q 'op: ClipOperation' crates/slicer-core/src/polygon_ops.rs || { echo 'FAIL: production ClipOperation param missing'; exit 1; }; echo 'AC-1 PASS'`
- **AC-2. Given** `slice_closing_radius_zero_is_noop` in `crates/slicer-core/tests/triangle_mesh_slicer_tdd.rs`, whose `let r = 0.0_f32` plus `if r > 0.0` branch is statically false so it asserts `polygons.clone() == polygons` and never calls `apply_slice_closing_radius`, **when** the retirement lands, **then** the name is absent from that file, the file's `#[test]` count drops from `12` to exactly `11`, and the surviving `slice_closing_radius_fuses_gap_within_two_r` still compiles and passes, keeping both the `apply_slice_closing_radius` import and the `unit_square_expolygon` helper live. | `set -euo pipefail; mkdir -p target; if rg -q 'slice_closing_radius_zero_is_noop' crates/slicer-core/tests/triangle_mesh_slicer_tdd.rs; then echo 'FAIL: retired test still present'; exit 1; fi; n=$(rg -c '#\[test\]' crates/slicer-core/tests/triangle_mesh_slicer_tdd.rs); test "$n" -eq 11 || { echo "FAIL: test count is not 11"; exit 1; }; cargo test -p slicer-core --test triangle_mesh_slicer_tdd -- --exact slice_closing_radius_fuses_gap_within_two_r 2>&1 | tee target/test-output.log >/dev/null; grep -Eq '^test result: ok\. 1 passed; 0 failed;' target/test-output.log`
- **AC-3. Given** the `host-algos`-gated target `algo_prepass_slice_tdd` (`required-features = ["host-algos"]` in `crates/slicer-core/Cargo.toml`), **when** the replacement test `prepass_slice_closing_radius_gate_applies_only_when_positive` is appended to that file, **then** the file's `#[test]` count rises from `6` to exactly `7` and the exact feature-correct filter runs one test with zero failures; because the stanza is the only gate (this file has no `#![cfg(feature = ...)]` inner attribute), Cargo skips the target outright without the feature - an explicit `--test algo_prepass_slice_tdd` fails with `requires the features: host-algos` and a bare `cargo test -p slicer-core` silently omits it - so the feature flag is mandatory, not decorative. | `set -euo pipefail; mkdir -p target; n=$(rg -c '#\[test\]' crates/slicer-core/tests/algo_prepass_slice_tdd.rs); test "$n" -eq 7 || { echo "FAIL: test count is not 7"; exit 1; }; cargo test -p slicer-core --features host-algos --test algo_prepass_slice_tdd -- --exact prepass_slice_closing_radius_gate_applies_only_when_positive 2>&1 | tee target/test-output.log >/dev/null; grep -Eq '^test result: ok\. 1 passed; 0 failed;' target/test-output.log`
- **AC-4. Given** a local two-island mesh fixture whose `z = 5.0` cross-section is the unit square `x in [0.0, 1.0]` plus the unit square `x in [1.05, 2.05]`, both `y in [0.0, 1.0]` (a 0.05 mm gap, matching the surviving fuses-test geometry), and a `RegionMapIR` whose single entry is keyed `RegionKey { global_layer_index: 0, object_id: "islands", region_id: 0, variant_chain: Vec::new() }`, **when** `execute_prepass_slice_single_layer` is called twice with that same key and only the interned `ResolvedConfig.slice_closing_radius` differing, **then** the `0.0` arm yields exactly `2` islands with combined area `200_000_000` unit-squared and bounding boxes `(0,0)`-`(1,1)` mm and `(1.05,0)`-`(2.05,1)` mm, and the `0.04` arm yields exactly `1` island whose bounding box spans `(0,0)`-`(2.05,1)` mm; both arms pass a populated `Option<&RegionMapIR>` so the contrast isolates the config value rather than the map's presence, and the gate-off arm pins `slice_closing_radius` to exactly `0.0` because `ResolvedConfig::default()` is `0.049`; the pin accepts either the field-init form `slice_closing_radius: 0.0,` or the assignment form `slice_closing_radius = 0.0;` (optional `f32` suffix) and requires the number to terminate, so a file containing only the `0.04` arm does not satisfy it. | `set -euo pipefail; mkdir -p target; cargo test -p slicer-core --features host-algos --test algo_prepass_slice_tdd -- --exact prepass_slice_closing_radius_gate_applies_only_when_positive 2>&1 | tee target/test-output.log >/dev/null; grep -Eq '^test result: ok\. 1 passed; 0 failed;' target/test-output.log; rg -q 'slice_closing_radius\s*[:=]\s*0\.0(_?f32)?\s*[,;]' crates/slicer-core/tests/algo_prepass_slice_tdd.rs || { echo 'FAIL: gate-off arm does not pin slice_closing_radius to exactly 0.0 in either the field-init or assignment form'; exit 1; }; echo 'AC-4 PASS'`
- **AC-5. Given** the real `docs/specs/test-quality-remediation-plan.md`, **when** the implementation updates only its current §7 `core` row, **then** this read-only predicate passes only when the heading matches `^## 7\. Ledger\b` and the span ends at the next `^## Packet Queue\b`, the six-column header is exact, exactly one `core` row has state `partial` or `partial (parenthesized detail)`, Retired/changed symbols contains the exact tokens `core-retire`, `clip_operation_variants_are_distinct`, and `slice_closing_radius_zero_is_noop`, Surviving/new coverage contains `prepass_slice_closing_radius_gate_applies_only_when_positive` and `slice_closing_radius_fuses_gap_within_two_r` plus the three whitespace-normalized function-count deltas `21->20`, `12->11`, and `6->7`, Validation contains a whitespace-normalized token-boundary occurrence of `cargo test -p slicer-core --features host-algos --test algo_prepass_slice_tdd -- --exact prepass_slice_closing_radius_gate_applies_only_when_positive` with optional trailing flags or a closing code-span backtick allowed, and Remaining gap contains `core-paint`, `core-brittle`, `core-cross`, `core-parity`, and `census-target-scope` while no longer containing `core-retire`; no temporary copy, manufactured row, queue mutation, or synthetic positive is used by this acceptance command. | `set -euo pipefail; rg -n '^## 7\. Ledger\b' docs/specs/test-quality-remediation-plan.md >/dev/null; python3 -c 'import re; from pathlib import Path as P; t=P("docs/specs/test-quality-remediation-plan.md").read_text(encoding="utf-8"); m=re.search(r"(?ms)^## 7\. Ledger\b[^\n]*\n(.*?)(?=^## Packet Queue\b)",t); assert m,"missing ledger span"; s=m.group(1); h=[[x.strip() for x in l.strip("|").split("|")] for l in s.splitlines() if l.startswith("| Wave |")]; assert len(h)==1 and h[0]==["Wave","State","Retired/changed symbols","Surviving/new coverage","Validation","Remaining gap"]; r=[l for l in s.splitlines() if re.match(r"^\|\s*core\s*\|",l)]; assert len(r)==1; w,st,ch,su,v,g=[x.strip() for x in r[0].strip("|").split("|")]; q=lambda x,y: re.search(r"(?<![A-Za-z0-9_])"+re.escape(x)+r"(?![A-Za-z0-9_])",y); assert w=="core" and re.fullmatch(r"partial(?:\s*\([^()\n]+\))?",st); assert all(q(x,ch) for x in ["core-retire","clip_operation_variants_are_distinct","slice_closing_radius_zero_is_noop"]); assert all(q(x,su) for x in ["prepass_slice_closing_radius_gate_applies_only_when_positive","slice_closing_radius_fuses_gap_within_two_r"]); zn=re.sub(r"\s+","",su); assert all(x in zn for x in ["21->20","12->11","6->7"]); vn=re.sub(r"\s+"," ",v).strip(); own=r"(?<![A-Za-z0-9_])cargo test -p slicer-core --features host-algos --test algo_prepass_slice_tdd -- --exact prepass_slice_closing_radius_gate_applies_only_when_positive(?=$|[\s;&|\x60])"; assert re.search(own,vn),"validation cell missing representative command"; assert all(q(x,g) for x in ["core-paint","core-brittle","core-cross","core-parity","census-target-scope"]); assert not q("core-retire",g),"core-retire must no longer be listed as an open gap"; print("core-retire ledger predicate: PASS")'`

## Negative Test Cases

- **AC-N1. Given** that a retirement packet must not silently delete, rename, or reorder neighbouring tests, **when** the edits land, **then** neither retired name resolves anywhere under `crates/`, and all three edited sites carry their exact expected surviving rosters in file order, including the inline `tests` module of `crates/slicer-core/src/polygon_ops.rs` at 20 names: `crates/slicer-core/tests/triangle_mesh_slicer_tdd.rs` holds exactly 11 names ending `slice_closing_radius_fuses_gap_within_two_r`, `staircase_boundary_z_produces_nonempty_cross_sections`, and `crates/slicer-core/tests/algo_prepass_slice_tdd.rs` holds exactly 7 names ending `prepass_slice_caches_bottom_surface_footprint_across_layers`, `prepass_slice_closing_radius_gate_applies_only_when_positive`. | `set -euo pipefail; for n in clip_operation_variants_are_distinct slice_closing_radius_zero_is_noop; do if rg -q "$n" crates/; then echo "FAIL: a retired name still resolves under crates/"; exit 1; fi; done; python3 -c 'import re,sys; from pathlib import Path as P; exp={"crates/slicer-core/src/polygon_ops.rs":["validate_polygon_simplicity_accepts_simple_square","validate_polygon_simplicity_rejects_bowtie","union_ex_merges_overlapping_squares","union_ex_reconstructs_hole_from_opposite_wound_rings","offset_erodes_hole_open_correctly_when_shrinking_solid","offset_iterative_erosion_mimics_emit_walls_loop","slice_closing_radius_round_trip_on_real_annulus_dimensions","offset_round_trip_preserves_hole_nesting","intersection_ex_returns_overlap","difference_ex_removes_overlap","opening_erodes_then_dilates","closing_ex_dilates_then_erodes","closing_ex_round_join_is_the_arc_tessellating_legacy_path","remove_small_removes_tiny_polygon","remove_small_removes_small_holes","expolygons_simplify_preserves_square","remove_duplicates_collapses_runs","clip_line_with_bbox_fully_inside","clip_line_with_bbox_fully_outside","clip_line_with_bbox_partial_clip"],"crates/slicer-core/tests/triangle_mesh_slicer_tdd.rs":["test_empty_mesh_produces_empty_layers","test_cube_sliced_at_half_height","test_cube_sliced_at_bottom","test_cube_sliced_at_top","test_cube_multiple_layers","test_unordered_cube_segments_still_chain_to_one_closed_loop","test_open_strip_slice_is_not_silently_emitted_as_closed_polygon","test_slice_through_mesh_vertex_still_forms_closed_triangle","test_shared_edge_with_opposite_windings_produces_closed_loop","slice_closing_radius_fuses_gap_within_two_r","staircase_boundary_z_produces_nonempty_cross_sections"],"crates/slicer-core/tests/algo_prepass_slice_tdd.rs":["slice_at_mid_height_produces_nonempty_polygons","slice_below_mesh_produces_empty_polygons","slice_above_mesh_produces_empty_polygons","unknown_object_returns_error","flat_bridge_enclosure_closing_avoids_round_arc_explosion","prepass_slice_caches_bottom_surface_footprint_across_layers","prepass_slice_closing_radius_gate_applies_only_when_positive"]}; bad=[(p,re.findall(r"#\[test\]\s*\n\s*fn\s+([A-Za-z0-9_]+)",P(p).read_text(encoding="utf-8"))) for p,n in exp.items() if re.findall(r"#\[test\]\s*\n\s*fn\s+([A-Za-z0-9_]+)",P(p).read_text(encoding="utf-8"))!=n]; [print("FAIL roster mismatch:",p,g) for p,g in bad]; sys.exit(1 if bad else 0)'; echo 'AC-N1 PASS'`

## Verification

AC-1 through AC-4 and AC-N1 are the full acceptance matrix and must all run; AC-5 binds only the ledger row.

- `set -euo pipefail; mkdir -p target; cargo check --workspace --all-targets 2>&1 | tee target/core-retire-check.log >/dev/null`
- `set -euo pipefail; mkdir -p target; cargo clippy --workspace --all-targets -- -D warnings 2>&1 | tee target/core-retire-clippy.log >/dev/null`
- `set -euo pipefail; mkdir -p target; cargo test -p slicer-core --features host-algos --test algo_prepass_slice_tdd -- --exact prepass_slice_closing_radius_gate_applies_only_when_positive 2>&1 | tee target/test-output.log >/dev/null; grep -Eq '^test result: ok\. 1 passed; 0 failed;' target/test-output.log`

## Authoritative Docs

- `docs/specs/test-quality-remediation-plan.md` - delegated summary of approved queue row #10, the three approved dispositions, the census-reconciliation decision, predecessor exports, and the ledger/queue boundary.
- `docs/adr/0064-existing-tests-retire-if-unjustified.md` - direct read of the earn-their-keep burden of proof, the compile-witness and parity carve-outs, and the census-reconciliation clause.
- `docs/22_test_quality.md` - direct read of §1 counterfactual, §2.1, §2.8, §3 legitimate weak forms, §4 questions 1/2/6, and §5 gate rule R1 whose legitimate-form column reads "none - retire instead".
- `docs/21_data_defaults_and_fixtures.md` - direct read of the watched-struct literal rule; `ObjectMesh` (7 named `pub` fields) and `ActiveRegion` (8) are watched, so new fixture literals use a `..Default::default()` rest or a reasoned `// exhaustive:` waiver, while `ResolvedConfig` is a documented §7.1 scanner blind spot.
- `docs/08_coordinate_system.md` - direct read of the 100 nm unit and `Point2::from_mm` for the expected island bounds and areas.
- `docs/adr/0065-test-quality-gate-with-delayed-enforce-mode.md` - direct read; the quality gate stays report-mode until final-wave promotion.
- `docs/spec_packets/test-quality-remediation_09_core-strengthen/` - delegated predecessor summary; draft, independently `PREFLIGHT PASS`, exports none.

## Doc Impact Statement (Required)

Specific same-packet doc edit:

- `docs/specs/test-quality-remediation-plan.md` §7 `Ledger` `core` row - implementation records the two retirements, the re-homed replacement, the three function-count deltas, its own validation command, and the remaining core-wave gap including the `census-target-scope` program-level gap. Verification grep: `rg -n '^## 7\. Ledger\b|^\| core \|' docs/specs/test-quality-remediation-plan.md`; AC-5 is the required real-plan content predicate and must read the accumulated row rather than manufacture a positive copy.

No other documentation section, queue row, or packet is edited by this packet.

<!-- snippet: context-discipline -->
## Context Discipline Note

This packet was generated against the context_discipline preamble shared by `spec-packet-generator`, `swarm`, and `spec-review`. Downstream agents implementing or reviewing this packet must:

- treat `design.md`'s code change surface as the authoritative files-in-scope list
- honor `design.md`'s out-of-bounds list — those files must not be loaded directly
- delegate every cargo run and authoritative-doc fact-check
- obey the shared absolute context bands: 120k reading budget with hand-off at 150k (standard); the extended band (240k reading / 300k hard stop) only via swarm's escalation protocol

Aggregate context cost above is the sum of per-step costs in `implementation-plan.md`. If any single step is rated L, the packet must be split before activation (an extended-band run may carry a single L step only when `design.md` justifies why it cannot be split).
