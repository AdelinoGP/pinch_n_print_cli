# Task Map: 296-slicing-mode-prepass

Queue packet from the wayfinder map "Close the OrcaSlicer FFF feature gap"; authored under map ticket 76. Single-task packet (no backlog slice); this crosswalk exists for the S0 structural gate and records the map provenance.

| docs/07 task ID | Packet step | Primary docs | Expected code surface | OrcaSlicer refs | Context cost | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| `TASK-000` (queue packet, `task_ids: []`) | Steps 1–4 | `docs/03_wit_and_manifest.md` (`from_declared` whitelist: why no manifest row — slicer-core reads `ResolvedConfig` via region-map); `docs/DEVIATION_LOG.md` (DEV-188); `docs/01_system_architecture.md` (prepass ownership; rule-4 trigger test does not fire) | `crates/slicer-ir/src/slice_ir.rs` (new `SlicingMode`) + `crates/slicer-ir/src/resolved_config.rs` (one `plain` field) + `crates/slicer-core/src/triangle_mesh_slicer.rs` (additive mode entry + Positive arm) + `crates/slicer-core/src/algos/prepass_slice.rs` (mode read) + `crates/slicer-ir/tests/resolved_config_slicing_mode_tdd.rs` (new) + `crates/slicer-core/tests/slicing_mode_fill_rule_tdd.rs` (new) | `OrcaSlicerDocumented/src/libslic3r/PrintConfig.cpp` (`init_fff_params` coEnum, default `Regular`) + `OrcaSlicerDocumented/src/libslic3r/PrintObjectSlice.cpp` (three-way switch) + `OrcaSlicerDocumented/src/libslic3r/TriangleMeshSlicer.hpp` (`MeshSlicingParams::SlicingMode` domain) | M | P69: Tier B held, owner corrected `layer-planner` → slicer-core prepass (no slicing behaviour in the layer-planner module — ticket-27 hazard); membership re-sized 2→1 with **no code change in this ticket** (`print_sequence` folded to 124, the sequential-printing feature that owns its validator); canonical per-object shape IS held via the existing overlay (explicitly not ticket 125's tool axis); Regular-as-EvenOdd recorded simplification DEV-188(a) (identical on valid manifold meshes); strict rejection DEV-188(b); CONFIG_BLOCK honest absence (rides 132); no queue-count change; no new fog, nothing out of scope. |

Copy costs from `implementation-plan.md`. Split before activation if any row is L or aggregate exceeds M.
