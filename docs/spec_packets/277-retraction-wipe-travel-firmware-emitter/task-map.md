# Task Map: 277-retraction-wipe-travel-firmware-emitter

Use this crosswalk when a packet spans more than one task ID, reopens prior work, or supersedes earlier packet work. This queue packet emits the explicit no-task clause: `task_ids: []`; implementation is recorded against [44 - Author packet P37 - Extruder / Nozzle / Retraction (2/2) - emitter](../specs/orca-feature-gap/issues/44-author-packet-p37-extruder-nozzle-retraction-emitter.md), and the `docs/07_implementation_status.md` crosswalk is N-A. No prior packet is reopened or superseded.

| docs/07 task ID | Packet step | Primary docs | Expected code surface | OrcaSlicer refs | Context cost | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| - (queue packet, `task_ids: []`) | Steps 1-6 | `docs/15_config_keys_reference.md` (generated); `docs/config/host-keys.toml` | `crates/slicer-ir/src/resolved_config.rs` + `crates/slicer-gcode/src/emit.rs` + `modules/core-modules/machine-gcode-emit/{machine-gcode-emit.toml,src/lib.rs,tests/*}` + `docs/DEVIATION_LOG.md` (DEV-172) | `OrcaSlicerDocumented/src/libslic3r/PrintConfig.cpp` (defaults + hop strings) + `GCodeWriter.cpp` (split, firmware, slope math) + `GCode.cpp` (gates, wipe, Z offset, placeholders) + `Extruder.cpp` (percent clamp, slope conversion) | M | Queue packet from the wayfinder map "Close the OrcaSlicer FFF feature gap"; authored under the map's Authoring rules: 11 of 11 P37 keys (all zero-occurrence as config-driven behaviour, Tier B sizing confirmed, no shed, no queue return), wipe trio lands as one new emission site (no wipe path exists today), scalar-global with DEV-172 (vector model stays with the ticket-125 ruling). Two intended default-output changes: short travels newly skip retracts (`retraction_minimum_travel` 2.0) and the default lift is newly slope-shaped (`z_hop_types` slope). |
