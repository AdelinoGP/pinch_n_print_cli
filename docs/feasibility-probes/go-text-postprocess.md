WIT_WORLD: slicer:postpass-text-postprocess/text-postprocess-module
TOOL_VERSIONS: go 1.26.5; wit-bindgen-go v0.7.0; wasm-tools 1.250.0; python3 3.14.4
GENERATION_COMMANDS: PATH="$(go env GOPATH)/bin:$PATH" wit-bindgen-go generate ../wit -w slicer:postpass-text-postprocess/text-postprocess-module
BUILD_COMMANDS: GOOS=wasip1 GOARCH=wasm go build -buildmode=c-shared -ldflags=-checklinkname=0 -o core.wasm .; wasm-tools component new --adapt "wasi_snapshot_preview1=$HOME/wasi-adapters/wasi_snapshot_preview1.reactor.wasm" -o comp.wasm core.wasm
COMPONENT_SHA256: 2fc5cc87af01ffecbf35a4c429833a2930e3abdba7556186166c85e41fde4510
HOST_COMMAND: PNP_FOREIGN_COMPONENT=F:/slicerProject/pinch_n_print_cli_2/docs/feasibility-probes/foreign-language-text-postprocess/go/comp.wasm cargo test -p slicer-wasm-host --test integration foreign_language_feasibility_tdd::foreign_language_text_postprocess_component -- --ignored --exact
HOST_OUTPUT: foreign component failed to instantiate: component imports instance `wasi:cli/environment@0.2.12`, but a matching implementation was not found in the linker
FAILURE_DETAIL: Go's WASI reactor adapter produces a component importing wasi:cli/environment@0.2.12; the slicer-only host linker does not provide that WASI instance, so instantiation fails before the oracle can execute.
RESULT: NOT_LOADABLE_OR_CORRECT

## Packet 225a host-WASI re-measurement
WIT_WORLD: slicer:postpass-text-postprocess/text-postprocess-module
TOOL_VERSIONS: go1.26.5 windows/amd64 (via GOTOOLCHAIN=auto); wit-bindgen-go v0.7.0 (only released version); go.bytecodealliance.org/cm v0.3.0; wasm-tools 1.250.0
GENERATION_COMMANDS: PATH="$(go env GOPATH)/bin:$PATH" wit-bindgen-go generate ../wit -w slicer:postpass-text-postprocess/text-postprocess-module
BUILD_COMMANDS: export-wired: GOOS=wasip1 GOARCH=wasm go build -buildmode=c-shared -ldflags=-checklinkname=0 -o core.wasm . (FAILS TO COMPILE — see FAILURE_DETAIL). Unwired scratch measurement (target/tmp-go-probe/go, main.go reduced to an empty `func main() {}` so no generated import package is linked): same go build command, then wasm-tools component new --adapt "wasi_snapshot_preview1=$HOME/wasi-adapters/wasi_snapshot_preview1.reactor.wasm" -o comp.wasm core.wasm. The `wasm-tools component embed` step from build.sh cannot run on the unwired module (embed's world requires the `run` export, which the module cannot provide: `failed to find export of interface slicer:postpass-text-postprocess/text-postprocess@1.0.0 function run`).
COMPONENT_SHA256: eb01f1096668e9a0115770dfd5c57a7961a5207343b04aa551e77efdaaf35766
HOST_COMMAND: PNP_FOREIGN_COMPONENT=F:/slicerProject/pinch_n_print_cli_2/target/tmp-go-probe/go/comp.wasm cargo test -p slicer-wasm-host --test integration foreign_language_feasibility_tdd::foreign_language_text_postprocess_component -- --ignored --exact
HOST_OUTPUT: foreign component failed to instantiate: no exported instance named `slicer:postpass-text-postprocess/text-postprocess@1.0.0` (test result: FAILED. 0 passed; 1 failed)
FAILURE_DETAIL: With the `run` export wired (main.go assigning `textpostprocess.Exports.Run`), compilation fails inside the wit-bindgen-go v0.7.0 GENERATED import bindings, reproduced 2026-08-13: `slicer\config\config-types/config-types.wasm.go:33:6: go:wasmimport: unsupported parameter type *cm.Option[string]` and `slicer\config\config-types/config-types.wasm.go:37:6: go:wasmimport: unsupported parameter type *cm.List[string]`. Go 1.24/1.25/1.26 all reject these go:wasmimport signatures; no newer generator release or cm shape aliases exist (surveyed last session). The only buildable component therefore omits the export entirely (unwired scratch build above); that measured component instantiates under the accommodated host but lacks the required exported instance, so the oracle fails before executing the text transformation. Verdict recorded as terminal per explicit user decision (2026-08-13): a language whose only released toolchain cannot compile this world is not loadable in practice.
RESULT: NOT_LOADABLE_OR_CORRECT
