# Implementation Status

**What this covers:** the live task backlog and phase status — what is done, in
progress, or blocked. This is an append-only working ledger; entries are dated
and reflect state at time of writing, so treat any specific count, task id, or
"current phase" as a snapshot to re-derive, not a durable fact.

**Who it's for:** anyone picking up work or checking whether something shipped.

**Prerequisites:** none. For registered architecture deviations see
`docs/DEVIATION_LOG.md`.

## Status Markers

- `[x]` complete
- `[~]` partially complete / in progress
- `[ ]` not started

## Program Status

- MVP status: COMPLETE.
- Historical implementation phases A through G are complete.
- This document now tracks the remaining work: OrcaSlicer feature parity, architecture-conformance cleanup, and acceptance-gate evidence.

## Milestone Summary

- [x] Phase A — Foundation (TASK-001 to TASK-006)
- [x] Phase B — Core Algorithms (TASK-010 to TASK-015)
- [x] Phase C — Host Scheduler (TASK-020 to TASK-036)
- [x] Phase D — SDK Tooling (TASK-040 to TASK-058)
- [x] Phase E — MVP Core Modules & CLI (TASK-070 to TASK-077)
- [x] Phase F — Post-MVP & Advanced Features (TASK-081 to TASK-097)
- [x] Phase G — Pipeline Wiring & WASM Integration (TASK-100 to TASK-113)
- [~] Phase H — End-to-End Integration & Review

### Workstream 1 — Manifest and contract conformance

- [x] TASK-121 Populate `[ir-access]` for all 17 core-module manifests per `docs/01_system_architecture.md` Stage I/O Contract. Covers DEV-002. Must turn `core_module_ir_access_contract_tdd.rs` green.
- [x] TASK-122 Populate `[config.schema]` for all 17 core-module manifests so the `config-schema` CLI returns real per-module schemas. Covers DEV-008.
- [x] TASK-123 Feed `ModuleAccessAudit` from every live execution path and pass populated `access_audits` into validation. Covers DEV-003.
- [x] TASK-123a Record prepass execution audits and plumb them into `DagValidationRequest.access_audits`. Covers DEV-003.
- [x] TASK-123b Record per-layer execution audits and plumb them into `DagValidationRequest.access_audits`. Covers DEV-003.
- [x] TASK-123c Record postpass execution audits and add a live-path regression proving populated audits reach validation. Covers DEV-003.
- [x] TASK-124 Enforce undeclared runtime read/write faults at the WIT boundary and add a negative harness for layer-time undeclared access. Continues DEV-003 after TASK-123 lands.
- [x] TASK-125 Enforce the `docs/01_system_architecture.md` Claim Transition Matrix for non-transitionable claims (`perimeter-generator`, `seam-placer`, `layer-planner`, `mesh-analyzer`). Covers DEV-004 and must turn `claim_transition_matrix_tdd.rs` green.
- [x] TASK-126 Fix `WriteConflict.orderable` so it reports `true` only when ordering can actually resolve the pair; add both positive and negative semantics tests. Scheduler conflict-ordering cleanup required for the `docs/04_host_scheduler.md` contract.
- [x] TASK-144 Consolidate host, macro, and guest codegen onto one canonical shared WIT source. Covers DEV-014. First closed 2026-04-24 (packet 25) was incomplete; genuinely consolidated 2026-05-29 by packet `72_wit-single-source-unification` — canonical source at `crates/slicer-schema/wit/`, consumed by host (`bindgen! path:`) and guest macro (`include_str!` generate!); phantom top-level `wit/` deleted.
- [x] TASK-145 Normalize WIT package/version identifiers and restore missing members across the canonical WIT surface; add drift-detection coverage (`wit_drift_detection_tdd.rs`, 19 tests). Continues DEV-014. First closed 2026-04-24 (packet 25); completed 2026-05-29 by packet 72 — `module-error` hoisted to `deps/common.wit`, `extrusion-path3d` spelling unified, dead `gcode-output-interface` dropped.
- [x] TASK-146 Add host-side `wit_world` allowlist validation using the canonical identifiers and reject mismatched manifests at startup. Covers the validation slice of DEV-014 and DEV-026. Added `validate_wit_world` in `manifest.rs`; added `wit_world_mismatch` and `wit_world_major_version_mismatch` tests in `manifest_ingestion_tdd.rs`.
- [x] TASK-146a (reopened from TASK-146; ADR-0045 retires `validate_wit_world`) Delete the `on-print-start` / `on-print-end` WIT exports from `world-layer.wit` and every artifact that mirrors them: SDK trait rename to `from_config`, macro metadata + glue + shims stripped, `WORLD_LIFECYCLE_EXPORTS` and its vacuous guard test deleted, `pnp_cli module new` scaffold reduced to the stage export, `pnp_cli` spawn-site freshness gate at all three call sites (slicer_cache, gate_evidence bench, dag_cli_integration), `no_lifecycle_exports_anywhere` guard test added. `world-layer` shrinks 10 → 8 exports. (Packet 162, ADR-0045 §"The lifecycle finding".)
- [x] TASK-146b (packet 163, ADR-0045 §"Per-stage versioned interface") Pilot the per-stage WIT package mechanism end to end on three pilot stages: canonical per-stage `.wit` packages (`slicer:postpass-gcode-postprocess@1.0.0`, `slicer:postpass-text-postprocess@1.0.0`, `slicer:finalization-layer-finalization@1.0.0`) at `crates/slicer-schema/wit/deps/<dir>/`; `slicer_schema::STAGES` extended with `wit_dir`/`wit_package`/`wit_interface`/`wit_world` columns and the five package lookups (`package_for_stage_id`, `interface_for_stage_id`, `wit_world_for_stage_id`, `wit_dir_for_stage_id`, `qualified_export_for_stage_id`); `StageSpec.wit_export = "run"` for the three migrated stages; host `bindgen!` mods repointed at the three per-stage worlds (`finalization_layer`, `postpass_gcode`, `postpass_text`); macro `WorldGlueKind` → `StageGlueKind` with the `Postpass` variant routing by detected stage; postpass glue split into `build_postpass_gcode_glue` and `build_postpass_text_glue` (no more benign-`Ok` padding arm); stage-x-finalization glue wraps the same imported-types companion pattern; three `bindgen!` mods each repeat the same five `with:` alias keys (`slicer:types/geometry`, `slicer:config/config-types`, `slicer:common/host-services`, `slicer:common/profiling`, `slicer:common/module-errors`, `slicer:ir-handles/ir-handles`); `xtask::GuestSpec.stage_id` populated from the sibling core-module manifest's `[stage] id`; `stage_wit_mtime(ws_root, stage_id)` resolves the per-stage `wit/deps/<wit_dir>/` and folds into `compute_guest_freshness`/`is_stale`; `compute_shared_mtime`'s WIT walk narrowed to `wit/root.wit` + flat `wit/deps/*.wit` (no `wit/deps/<dir>/` descent); per-stage staleness unit tests (`stage_wit_dir_is_charged_only_to_matching_guest`, `stage_wit_unknown_stage_is_conservative`) added under `#[cfg(test)] mod tests`; macro and 20 core-module `tests/slicer_module_binding_tdd.rs` binding surfaces now report `"run"`; WIT conformance suites (`wit_single_source_tdd.rs`, `wit_drift_detection_tdd.rs`) repointed from `world-postpass`/`world-finalization` to the three per-stage packages; `every_stage_package_major_is_at_least_one` walks `wit/deps/*/` and asserts `major >= 1` for every versioned per-stage package (load-bearing for wasmtime's `alternate_lookup_key`); `stage_miss_is_fatal_at_instantiation` (AC-N1) builds a `PostpassError::FatalModule` whose `message` carries the qualified-export `slicer:postpass-gcode-postprocess/gcode-postprocess@1.0.0#run` and either of two engine-issued miss diagnostics; `dev_mode::WIT_REBASED` is the per-stage package. the per-stage WIT package intermediate records the accepted two-mechanism intermediate (postpass + finalization on per-stage packages; layer + prepass remain on tier worlds).
- [x] TASK-146c (packet 164, ADR-0045 §"Per-stage versioned interface") Complete the per-stage WIT migration: 8 layer and 4 prepass stages now use versioned packages, alongside the 3 pilot packages for 15 total; the unversioned `slicer:prepass-types` package carries shared prepass views; `PrePass::PaintSegmentation` remains host-built-in; and the legacy `wit-world`, `SUPPORTED_WIT_WORLDS`, and `validate_wit_world` surfaces are retired. Updated the canonical WIT layout and delivered-state documentation.
- [x] TASK-146d (packet 165, ADR-0045 §Packet Queue row 4) Resolve the `[FWD]` packet 162 left open on locator extraction. Authored `docs/adr/0054-host-side-test-support-crate.md` (Accepted): host-side test support lives in a new std-only, dev-dependency-only crate, complementing ADR-0004, which scopes guest-side test support to `slicer-sdk`. Created workspace member `crates/pnp-cli-locator` (renamed from `slicer-test-support` the same session it shipped; the old name read as a sibling of ADR-0004's guest-side `slicer_sdk::test_support`, which it is not) exposing `workspace_root`, `newest_source_mtime`, `staleness_reason`, and `pnp_cli_bin`, with zero `[dependencies]`; `staleness_reason` remains a documented *mirror* of `is_stale` in `xtask/src/build_guests.rs` (xtask is bin-only and cannot be depended on). Call sites repointed via `[dev-dependencies]` on `slicer-runtime` and `slicer-scheduler`; `crates/slicer-runtime/tests/common/slicer_cache.rs` re-exports the locator, so its ~30 downstream test callers needed zero edits. Main finding: the packet was authored believing the locator was triplicated at three sites — implementation found seven. The four extra copies (`crates/slicer-runtime/tests/integration/no_linker_module_degraded_raw_output_tdd.rs` and `crates/slicer-runtime/tests/e2e/{infill_overlap_changes_gcode_tdd,modifier_infill_tdd,wedge_linked_infill_report_tdd}.rs`) had no freshness gate at all and branched on `PROFILE`, a Cargo build-script env var that is not set for test binaries, so they always resolved `target/debug/pnp_cli` even under `--release`. All four were migrated, extending packet 162's freshness gate to sites it had missed; the packet docs were amended mid-run (AC-3/AC-N1 widened, AC-8 added, Step 3c inserted).
- [x] TASK-149 Widen the WIT types so `ExtrusionRole::Custom(String)`, `PaintSemantic::Custom(String)`, and `WallFeatureFlags.custom` can cross the boundary losslessly. Covers DEV-016.
- [x] TASK-150 Update host, macro, and guest converters to preserve the widened custom payloads and add round-trip WIT regression tests. Continues DEV-016.
- [x] TASK-329 Deepen the integrated native-dispatch seam: make native/WASM marshalling share one view authority, remove duplicate held-claim resolution, close lossy native commit paths, and validate integrated native entries at load time. Packet 205c; governed by ADR-0005, ADR-0021, ADR-0056, and ADR-0057.
- [x] TASK-330 Derive integrated registrations, native entries, and coverage checks from one registry authority while preserving the 21 per-module features, manifest IDs, origin labels, and edition semantics. Packet 205d; governed by ADR-0056 and ADR-0057.
- [x] TASK-331 Consolidate integrated native/WASM parity setup and comparator scaffolding without reducing the 21 parity gates, structural invariants, tolerance checks, or negative self-tests. Packet 205e; governed by ADR-0042 and ADR-0056.

### Workstream 2 — Runtime correctness and scheduler guarantees

- [x] TASK-127 Enforce the non-planar Z envelope `[layer.z, layer.z + effective_layer_height]` at output-commit boundaries. Covers DEV-005.
- [x] TASK-128 Resolve prepass segmentation input-shape gaps on the macro/WIT path so segmentation modules stop receiving hollow SDK inputs. Covers prepass segmentation input-shape gaps.
- [x] TASK-128a Provide usable `MeshSegmentation` inputs on the macro path by sourcing real geometry for `MeshObjectView` instead of object-id-only shells. Covers prepass segmentation input-shape gaps.
- [x] TASK-128b Provide usable `PaintSegmentation` inputs on the macro path, including transform matrices, paint layers, and participating layer indices. Continues prepass segmentation input-shape gaps.
- [x] TASK-129 Close the remaining non-segmentation WIT-boundary gaps on live execution paths. Covers non-segmentation WIT boundary gaps.
- [x] TASK-129a Pass real postpass GCode command lists into `dispatch_postpass_gcode_call` and add coverage for per-command content crossing the WIT boundary. Covers non-segmentation WIT boundary gaps.
- [x] TASK-129b Add live-path boundary coverage for layer-world deep-copy behavior outside native fallback code. Continues non-segmentation WIT boundary gaps.
- [x] TASK-129c Add live-path boundary coverage for finalization-world deep-copy behavior outside native fallback code. Continues non-segmentation WIT boundary gaps.
- [x] TASK-130 Finish the `#[slicer_module]` prepass segmentation bridge for macro-authored modules. Covers prepass segmentation input-shape gaps. Closed 2026-05-08 — packet 43-rev1; full PaintSegmentation drain wired; prepass segmentation mismatch 3 closed.
- [x] TASK-130a Drain `PaintSegmentationOutput` back through WIT `push-paint-region` so macro-authored modules emit paint regions without hand-written `wit-guest` glue. Covers prepass segmentation input-shape gaps. Closed 2026-05-08 — packet 43-rev1.
- [x] TASK-130b Add end-to-end macro-path regression tests proving `MeshSegmentation` and `PaintSegmentation` round-trip real data through WIT. Continues prepass segmentation input-shape gaps. Closed 2026-05-08 — packet 43-rev1.
- [x] TASK-130c Widen paint-region transport (SDK ExPolygon-bearing, WIT typed paint-value-input variant, host harvest 1:1) so paint regions carry hole-bearing polygons and Custom/typed values without coercion. Covers prepass segmentation input-shape gaps. Closed 2026-05-08 — packet 42; prepass segmentation mismatches 4 and 5 closed; mismatch 3 remains open (closed in packet 43).
- [x] TASK-131 Add a regression guard for the documented `resolve_active_regions` O(1) contract. Scheduler performance guard needed for runtime-budget evidence.
- [x] TASK-132 Add structured RegionMap overflow coverage for the 1000-entry cap, including top-contributor and remediation messaging. Hardens the existing bounds path needed for DEV-026 evidence.
- [x] TASK-133 Add a pool-behavior test proving `layer_parallel_safe = false` serializes concurrent WASM acquisition. Scheduler concurrency guard for the `docs/04_host_scheduler.md` instance-pool contract.
- [x] TASK-134 Add a catch-up-layer propagation test that verifies `is_catchup_layer`, `catchup_z_bottom`, and `effective_layer_height` survive every per-layer stage. Guards the documented catch-up-layer propagation contract across every per-layer stage.
- [x] TASK-147 Implement live mesh-data wiring for `raycast_z_down` and cover hit/miss semantics across the WIT worlds. Covers DEV-015.
- [x] TASK-148 Implement `surface_normal_at` and `object_bounds` on the same mesh-query backing surface, replacing the current stub/trap behavior with tested results. Continues DEV-015.
- [x] TASK-157 Add fixture-level integration coverage for non-identity object transforms proving correct world-space Z behavior through planning. Covers DEV-027. Closed 2026-04-21 — 5 integration tests added.
- [x] TASK-158 Promote world-space Z extent to one canonical derived contract surface; regression-lock transformed-object behavior. Continues DEV-027. Closed 2026-04-21 — Option A IR field added (`ObjectMesh.world_z_extent`).
- [x] TASK-166 Resolve per-object/per-region config from CLI JSON or print profile in the `RegionMapIR` builder instead of unconditionally defaulting; thread resolved config to `RegionPlan` entries end-to-end. Covers DEV-040. Unblocks packets 36 and 37. Closed 2026-05-04 by packet 35a (resolved-config-propagation).
- [x] TASK-167 Implement bridge detection (Orca parity) in packet 36 (`assemble_bridge_areas` from downward-facing facets; `BridgeInfill` emission in `rectilinear-infill`). Closes bridge detection gaps. Closed 2026-05-05; reopened same day by TASK-168 (packet 36-rev1) — original algorithms were heuristic stubs; re-closed 2026-05-05 by packet 36-rev1 with the correct algorithmic implementation.
- [x] TASK-168 Implement packet 36-rev1 bridge detector remediation: rewrite mesh-adjacency analysis, add schema-version constants + `validate_polygon_simplicity`, switch OffsetJoinType to Miter, replace centroid set-difference with real polygon ops. Reopens and re-closes bridge detection gaps with correct rationale; registers mutation enum refactor deviation. Closed 2026-05-05.
- [x] TASK-167 Implement fill-role-claims for packet 37: register `claim:top-fill`/`bottom-fill`/`bridge-fill`/`sparse-fill` in the claim catalog; add per-claim module selection in the infill pipeline. Closes fill role claims gap. Closed 2026-05-06 — packet 37.
- [x] TASK-169 Implement packet 38-rev1 top-surface-ironing: object-scope ironing module emitting `BlockKind::Ironing` strokes at `PostPass::LayerFinalization`. Supersedes predecessor packet 38 (wrong stage, failed acceptance). Closed 2026-05-07 — packet 38-rev1.
- [x] TASK-170 Implement packet 39_stable-entity-ids: foundation refactor introducing stable per-layer-monotonic `entity_id: u64` on `LayerCollectionIR.ordered_entities`; migrate `TravelMove` anchor from positional index to `entity_id`. Pure refactor — zero G-code byte change. Foundation for packet 40. Closed 2026-05-07 — packet 39.
- [x] TASK-171 Promote `FinalizationOutputBuilder` from push-only to a true mutation builder (`push_with_priority`, `modify_entity`, `sort_layer_by`, `insert_synthetic_layer_after`) backed by `ExtrusionRole::default_priority()`. Migrates `top-surface-ironing` to emit after top-fill (closes the print-order concern from packet 38-rev1); backwards-compatible with skirt-brim's existing call site. Foundation for future PostPass mutation modules. Closed 2026-05-07 — packet 40_finalization-mutation-builder.
- [x] TASK-172 Refactor `FinalizationOutputBuilder`'s mutation methods from closure-based to serializable-enum-based APIs (`EntityMutation`, `SortKey`, `SyntheticLayerData`) so they round-trip across the WIT boundary; wire `slicer-macros` drain-back for `merge_ops`. Closes DEV-041. Establishes the WIT round-trip contract for future PostPass mutation modules. Closed 2026-05-08 — packet 41_finalization-mutation-enum-refactor.

- [x] TASK-180 Wire 3MF fuzzy_skin paint metadata through host loader. Covers 3MF fuzzy_skin paint metadata. Closed 2026-05-11 — Packet 50.
- [x] TASK-180b-prereq TriangleSelector hex-tree decoder + sub-triangle stroke geometry (gap identified drafting Packet 50b). Implements `parse_nibbles`, `walk_triangle_selector_tree`, `dominant_paint_state`, `decode_paint_hex_strokes`; populates `PaintLayer.strokes`; unblocks TASK-180b. Closed 2026-05-12 — Packet 50a.
- [x] TASK-180b MMU tool-index propagation pipeline fix + multi-channel co-presence tests. Fixes per-facet `ToolIndex` off-by-one in `model_loader.rs` (`saturating_sub(1)`, OrcaSlicer 1-based nibble encoding); fixes `paint-segmentation` guest to iterate `object.paint_layers`; adds `dominant_tool_index()` propagation in `layer_executor.rs`. Closed 2026-05-12 — Packet 50b.
- [x] TASK-181 Make RegionMap paint-semantic-aware via `paint_config:<semantic>:<key>` overrides (bumps `RegionMapIR` 1.0.0 → 1.1.0). Covers paint-semantic overrides. Closed 2026-05-12 — packet 51.
- [x] TASK-182 Enforce manifest-declared numeric `min`/`max` on CLI-sourced config values via `ConfigBoundsIndex` (strictest-wins collision, `ConfigResolutionError::OutOfRange`, NaN/Inf rejected). Closed 2026-05-18.
- [x] TASK-190 Implement multi-root module search path in `slicer-host`: repeatable `--module-dir`, `--no-default-module-paths`, `SLICER_MODULE_PATH` env var, and platform default dirs; priority CLI > env > config_dir > executable_dir, deduplicated by canonical path. Closes the `<!-- VERIFY -->` gap in `docs/01_system_architecture.md §Module Search Path`. New module `crates/slicer-host/src/module_search_path.rs`.

### Workstream 3 — Benchy parity and missing OrcaSlicer behavior

- [x] TASK-119 Restore OrcaSlicer-identical GCode comment contracts on the live emit path so OrcaSlicer preview/visualization remain functional. Covers the Benchy parity work; must land before TASK-120. Closed 2026-04-21 — `orca_type_label` canonicalizes `;TYPE:` spelling; layer headers; seam preservation via entity-order iteration.
- [x] TASK-119a Enumerate the OrcaSlicer-native GCode comment contract required for preview compatibility (`;LAYER_CHANGE`, `;Z:`, `;HEIGHT:`, `;TYPE:`, object/feature markers) and codify it as one canonical emit spec. Covers the Benchy parity work. Closed 2026-04-21 — `orca_type_label`/`orca_layer_headers` helpers in `gcode_emit.rs`.
- [x] TASK-119b Emit the canonical OrcaSlicer comment sequence with matching spelling, ordering, and boundary placement across the live host/finalization GCode path. Covers the Benchy parity work. Closed 2026-04-21 — `DefaultGCodeEmitter::emit_gcode` emits headers, TYPE labels, and seam-preserved paths.
- [x] TASK-119c Add compatibility regressions proving emitted `.gcode` preserves OrcaSlicer's native layer navigation, feature coloring, and toolpath visualization semantics end-to-end. Supports the Benchy parity work acceptance evidence. Closed 2026-04-21 — `postpass_gcode_emit_contract_tdd.rs` + `gcode_emit_tdd.rs`.
- [x] TASK-120 Produce a fully sliced Benchy `.gcode` with tree supports enabled as the Phase H end-to-end acceptance run. Closed 2026-05-08 — packet `27_phase-h-final-validation`; focused test matrix green across contract/pipeline/wit-drift/live-support/live-seam/benchy-e2e suites. Unblocks Phase H acceptance gate review; remaining items TASK-120c, TASK-130 tracked separately.
- [x] TASK-120a Restore top/bottom surface fill generation on the live Benchy path. Covers the Benchy parity work.
- [x] TASK-164 Wire `is_top_surface`, `is_bottom_surface`, `is_bridge` into `SlicedRegion` at slice time; expose via `slice-region-view` WIT resource so live infill can emit `TopSolidInfill`/`BottomSolidInfill`/`BridgeInfill` roles. Bumps `SliceIR.schema_version` 1.0.0 → 1.1.0. Covers the Benchy parity work. Closed 2026-05-03 — packet `12-rev1_external-surface-classification-at-slice`. Open follow-ups at close: surface classification gaps (polygon intersection, bridge detection).
- [x] TASK-165 Multi-layer top/bottom solid-fill window via per-region `top_shell_layers`/`bottom_shell_layers` config keys; `classify_region_surfaces` computes Z window as sum of next/prev N layer Zs. Defaults to 3 layers (differs from OrcaSlicer). Closed 2026-05-04 — packet `35_multi-layer-top-bottom-thickness`; follows packet 12-rev1, unblocks packets 36/37/38.
- [x] TASK-120b Restore support generation on the live Benchy path. Covers the Benchy parity work. Closed 2026-04-24 (packet 26); supersedes an incomplete 2026-04-21 closure that only exercised the synthetic commit-path tier, not live WASM dispatch.
- [x] TASK-120c Restore seam placement on real wall-loop seam candidates. Covers the Benchy parity work. **Closed 2026-07-23 — packet `170-seam-livepath-audit` audit verified the wall-preservation invariant: `run_wall_postprocess` re-emits the full region wall set on every call (sibling walls survive target-wall rotation; `convert_perimeter_output` origin pairing preserved across multi-region calls), and the `select_seam_candidate` preferred path in the per-mode dispatch is in place. Pinned by `modules/core-modules/seam-placer/tests/seam_sibling_walls_tdd.rs` (4 tests: AC-1 `siblings_survive_rotation`, AC-2 `multi_region_wall_counts_preserved`, AC-3 `aligned_snap_preserves_siblings`, AC-N1 `tolerance_miss_emits_all_walls_pristine`). The source-geometry half of the seam-planning work was closed in `docs/DEVIATION_LOG.md` by packet 180's continuous projection.**
- [x] TASK-120d Restore live Benchy travel behavior on the path-optimization or emit path. Covers the Benchy parity work and the travel-behavior slice of DEV-023. Closed 2026-04-24 — packet `15_live-travel-retraction-policy`; `path-optimization-default` becomes the canonical retract/no-retract decision surface (inter-region travel emits Retract+Move+ZHop+Unretract; intra-region travel suppressed).
- [x] TASK-120d1 Decide where retraction policy lives and implement retract/no-retract decisions for live travel moves. Covers the Benchy parity work. Closed with TASK-120d — policy lives in `path-optimization-default`; `DefaultGCodeEmitter` is serialization-only.
- [x] TASK-120d2 Emit matching retract/unretract pairs, z-hop interactions, and Benchy regression assertions for the chosen travel-policy surface. Covers the Benchy parity work. Closed with TASK-120d. Integration gap closed: packet 58, 2026-05-18 — retract/unretract entities now bracket `T<n>` tool changes in multi-material wipe-tower emission.
- [x] TASK-135 Add Benchy regression assertions for supports, top/bottom fills, seams, and retract/unretract pairs. Supports the Benchy parity work acceptance evidence. Closed 2026-05-08 — packet `21_benchy-acceptance-evidence`; six feature-evidence assertions land in `benchy_end_to_end_tdd.rs`; unblocks TASK-140 acceptance-gate evaluation.
- [x] TASK-142 Port `SkirtBrim` live geometry from legacy `process()` into `run_finalization()` using `LayerCollectionView` and `FinalizationOutputBuilder`. Covers DEV-013. Closed 2026-04-25 — packet `16_skirt-brim-finalization-live-path`; host dispatch batch-prepends entity pushes so finalization entities precede model entities.
- [x] TASK-143 Port `WipeTower` live geometry from legacy `process()` into `run_finalization()` and retire the legacy-only finalization path. Continues DEV-013. Closed 2026-04-25 (packet 17) — DEV-013 fully closed. Integration gap closed: packet 58, 2026-05-18 — `run_finalization` uses `insert_entity_at` to bracket every `T<n>` with retract/travel/prime/wipe entities; `;TYPE:Prime tower` marker corrected; `bed_shape` + `retract_length` config keys added.
- [~] TASK-151 Teach `path-optimization-default` to consume seam-placement output and stop acting as a comment-only slot filler on real wall loops. Covers the non-retraction portion of DEV-023 and supports TASK-120c. **Reopened 2026-04-21 — packet `22_live-seam-contract-repair` restores the remaining live contract gap: `path_optimization_emit_layer_markers=false` still fails on the host path because `path-optimization-default` emits marker comments unconditionally.**
- [x] TASK-159 Add `PrePass::SeamPlanning` plus a canonical `SeamPlanIR` blackboard contract so seam choices can be scored from global mesh/layer-plan context and injected into `Layer::PerimetersPostProcess`. Continues the Benchy parity work, deepens Orca parity, supports TASK-120c and TASK-135. Closed 2026-04-24 — packet `23-rev1_prepass-seam-planning-orca-parity`; fixes WIT boundary (`run-seam-planning` accepts `list<MeshObjectView>`), lowers curvature threshold to 0.2. Unblocks TASK-135.
- [x] TASK-161 Establish `SupportPlanIR` and cross-layer support planning produced inside `PrePass::SupportGeometry` (host built-in commits `SupportGeometryIR`; `support-planner` guest emits `SupportPlanIR` from coarse geometry). Continues the Benchy parity work, deepens Orca parity, and supports TASK-120.
- [x] TASK-162 Surface `LayerPlanIR.layers` and `RegionMapIR.entries` to the prepass guest via new WIT views (`layer-plan-view`, `region-segmentation-view`) so `support-planner` walks the real layer plan and emits one entry per `(layer, object, region)`. Closes the v1 layer-height-agnostic and single-region carve-outs from packet `28_tree-support-multi-layer-propagation`. Wired by packet `30_support-planner-prepass-wit-plumbing`.
- [x] TASK-163 (partial) Establish `SupportGeometryIR`, `PrePass::SupportGeometry`, `support_layer_height_mm`, and `support_top_z_distance_mm` as the architectural foundation for variable-height support planning. Continues TASK-120 acceptance evidence. Closed 2026-04-29 (packet 31a) — `SupportGeometryIR` committed to blackboard; config plumbed through WIT to `support-planner`.
- [x] TASK-163 (algorithmic) Close the five algorithmic v1 limitations (avoidance/collision cache from `SupportGeometryView`, radius tapering, raft + interface layers, wall-count-aware move scaling, OrcaSlicer config keys) on the foundation established by packet `31a_support-geometry-prepass-and-layer-height`. Continues TASK-120 acceptance evidence. Wired by packet `31b_support-planner-algorithmic-parity`.
- [~] TASK-163b **Split** per `docs/specs/_OLD/support-modules-orca-port.md` §D12 (2026-06-08): the conflated original is closed in favor of two independently-tracked rows. The original referenced `benchy.stl` (retired by P90) and `resources/golden/benchy_tree_support_orca_*` (stale self-captures, not real Orca reference output — see `docs/spec_packets/_OLD/31b_support-planner-algorithmic-parity.md`). Replacement rows:
  - [x] TASK-163b-diagnostic Promote `support-planner` warnings (`node-clamped-out`, plus B4 `max_branches_per_layer` cap and B2 `support_interface_bottom_layers` not-implemented) from `host-services.log` strings to a typed `Diagnostic` channel via the prepass output WIT. Lands as B7 of `docs/specs/_OLD/support-modules-orca-port.md`. See ADR-0010. Closed 2026-07-19 via packet 118 — code 1002 (node-clamped), 1001 (cap, one per affected global layer), 1003 (interface-bottom-layers, planner-owned, read from preserved config key and emitted once before the layer loop when != -1). Macro `#[slicer_module]` extended to forward SDK `output.diagnostics()` to the WIT `push-diagnostic` import. `ModuleAccessAudit::diagnostics: Vec<slicer_ir::Diagnostic>` added across all 13 audit-literal sites; `PrepassStageRunner::last_diagnostics` exposes the drained channel. `docs/02_ir_schemas.md`, `docs/03_wit_and_manifest.md`, `docs/05_module_sdk.md` updated. Packet 116's no-warning boundary preserved (packet 116 emits no warning; packet 118 owns code 1003 end-to-end). Source-plan `TASK-253` collision resolved by closing against the live `TASK-163b-diagnostic` row only; no replacement ID invented. Post-closure MED-1 follow-up (2026-07-19): the cap diagnostic accumulator was hoisted out of `plan_for_object` into `run_support_geometry` and merged across all objects before emission, so a multi-object cap hit on the same global layer now produces one merged code-1001 diagnostic (with `object_id=None` and the summed `dropped_count`) instead of one per (object, layer) pair; regression test `multi_object_cap_diagnostic_merges_per_layer` in `modules/core-modules/support-planner/tests/diagnostics_tdd.rs` covers this. Post-closure MED-2 note: `LAST_PREPASS_DIAGNOSTICS` (`crates/slicer-wasm-host/src/dispatch.rs`) is a process-global `thread_local!` that mirrors the established `LAST_MODULE_LOG_MESSAGES` pattern used by `last_log_messages`; it inherits the same single-threaded-per-dispatcher constraint (the `unsafe impl Sync for WasmRuntimeDispatcher` is pre-existing and applies to both channels).
  - [ ] TASK-163b-orca-ref Replace the `regression_wedge` self-captured goldens (introduced by C1 of `docs/specs/_OLD/support-modules-orca-port.md`) with real OrcaSlicer reference output. **Amended 2026-08-12:** the wedge golden pair (`resources/golden/support_regression_wedge_{endpoints,branch_count}.txt` + `support_golden_regression_wedge_tdd.rs`) was retired by a self-capture→structural-invariants reshape following ADR-0042 / the packet-177 precedent; the wedge plan is now certified by `crates/slicer-runtime/tests/integration/support_invariants_wedge_tdd.rs` (contiguity, per-layer step bound, width monotonicity, overhang-bbox containment, byte determinism) and the benchey self-capture remains tracked here. **Blocked on**: (a) a non-benchy fixture that is small enough for CI and complex enough for "Orca reference output" to be meaningful; (b) version-pinned OrcaSlicer in CI or as a release artifact; (c) agreed comparison metric beyond branch-count + Hausdorff. Continues TASK-120 / TASK-163.
- [x] TASK-163c (WIT-boundary normalization) Normalize the `run-support-geometry` WIT export to the resource-output + config-view pattern established by `run-seam-planning`/`run-layer-planning`: receives `config: config-view` (enable_support now flows to the tree-support planner) and writes through an `output: support-geometry-output` resource; returns `result<_, module-error>` so planner fatals surface as `DispatchError`. Wired by packet `73_support-geometry-normalization`.
- [x] TASK-152 Expand `path-optimization-default` beyond comment-only output into a real optimization stage with deterministic travel ordering, module-side z-hop planning, and coverage for the remaining DEV-023 gaps. Continues DEV-023, supports TASK-120d. Closed 2026-04-29 via packets 18-20; all sub-tasks 152a-152h landed.
- [x] TASK-152a Add deterministic nearest-neighbor-style travel sequencing in `path-optimization-default`, with regression coverage on real per-layer entities. Continues DEV-023, supports TASK-120d. Closed 2026-04-26 — packet 18.
- [x] TASK-152b Emit module-level tool-change ordering for mixed-tool layers and regression coverage proving `LayerTools`-equivalent sequencing crosses the live path. Continues DEV-023. Closed 2026-04-29 — packet 19 (per-tool grouping via `set-entity-order`/`push-tool-change`). Integration gap closed: packet 58, 2026-05-18 — `T<n>` tool-change entities now surrounded by purge geometry.
- [x] TASK-152c Decide whether fan-speed/cooling overrides belong in path optimization or remain unsupported. Continues DEV-023. Closed 2026-04-29 — packet 19 documented cooling as unsupported on `Layer::PathOptimization`; superseded by TASK-152d.
- [x] TASK-152d (cooling) Supersede TASK-152c; permit cooling on the finalization surface; preserve the path-optimization-surface rejection. Closed 2026-05-13 via packet 53.
- [x] TASK-152d Add cross-object ordering in `path-optimization-default` so per-layer planning sequences entities across objects; cover deterministic mixed-object cases. Continues DEV-023. Closed 2026-04-26 — packet 18.
- [x] TASK-152e Add role-aware bridge/overhang reordering so bridge-sensitive entities can be prioritized; regression-lock on bridge-tagged inputs. Continues DEV-023. Closed 2026-04-26 — packet 18.
- [x] TASK-152f Coordinate `path-optimization-default` with `SkirtBrim` and `WipeTower` outputs so wipe/brim travel decisions stop ignoring finalization geometry. Continues DEV-023, supports TASK-142/TASK-143. Closed 2026-04-29 via packet 20.
- [x] TASK-152g Add `layer-collection-builder` WIT resource (`set-entity-order(items: list<tuple<u32, bool>>)`) and wire it through host bindings, SDK, and `LayerModule::run_path_optimization`. Continues DEV-023. Closed 2026-04-28 — packet 33 consumes the surface end-to-end.
- [x] TASK-152h Move the deterministic NN entity-ordering algorithm from `slicer-host::layer_executor` into `path-optimization-default::run_path_optimization` via the `layer-collection-builder` surface; delete the host helper; marks packet 18 superseded. Extended by packet 61 with extrusion-role-priority grouping (`WallSequence` enum, `wall_sequence` config key). Closed 2026-04-28 — packet 33; extended 2026-05-21 via packet 61.
- [x] TASK-153 Per-role feedrate emission on live G-code path. Covers the Benchy parity work. Closed 2026-05-13 via packet 52 — 26 per-role speed config keys registered; `resolve_feedrate` wired into print-move and z-hop builders.
- [x] TASK-182 Wire overhang quartile speed (`overhang_1_4_speed`..`overhang_4_4_speed`) end-to-end: `overhang-quartile: option<u8>` on `point3-with-width`; host-side `overhang_classifier` prepass buckets wall-family vertices by distance to previous-layer support polygons; `resolve_feedrate` dispatches to the quartile schedule. Continues the Benchy parity work. Closed 2026-05-15 via packet 57_overhang-speed.
- [x] TASK-154 (cooling) Emit M106/M107 from a live finalization-stage cooling module. Covers the Benchy parity work (cooling subset). Closed 2026-05-13 via packet 53 — new `modules/core-modules/part-cooling/` module emits `M106`/`M107` on `LayerCollectionIR`; 8 cooling config keys with OrcaSlicer-derived defaults.
- [x] TASK-142a Diagnose and fix why TASK-142's live SkirtBrim finalization module produces zero `;TYPE:Skirt/Brim` blocks: `lib.rs` defaults corrected (`skirt_loops` 1→6, `skirt_distance` 6→3, `brim_width` 0→8). Covers the Benchy parity work. Closed 2026-05-14 via packet 54.
- [x] TASK-183 Relative-extrusion mode toggle (M82/M83) via `use_relative_e_distances`: `DefaultGCodeSerializer::with_extrusion_mode` added (M83 default, M82 when flag=false), per-move E deltas in relative mode, G92 E0 resets accumulator. Covers the Benchy parity work. Closed 2026-05-14 via packet 54.
- [x] TASK-201 Configurable slicing precision (D-P at emit, arc tolerance, closing radius, gcode XY decimals). Covers the Benchy parity work (slicing precision subset). Closed 2026-05-18 via packet 60.
- [x] TASK-202 G-code toolchange + purge integration. Wipe/prime tower purge moves coordinated with `GCodeCommand::ToolChange { from, to }`; `ExtrusionRole::WipeTower`/`PrimeTower` flows through the per-role speed dispatch added by packet 52. Covers the Benchy parity work (toolchange-purge subset). Closed 2026-05-17 via packet 58.
- [x] TASK-203 Machine start/end G-code emission. Module-owned printer preamble / postamble; `machine_start_gcode` / `machine_end_gcode` config keys with macro expansion (`{first_layer_temperature}`, `{bed_temperature}`, `{filament_type}`, `{nozzle_diameter}`, `{tool_count}`, `{layer_count}`, `{print_time_estimate_s}`, `{x_max}`, `{y_max}`, `{z_max}`). Covers the Benchy parity work (machine-gcode subset). **Closed 2026-05-18 via packet 59.**
- [x] TASK-204 paint-native-migration. Consolidates paint-segmentation and paint-region-annotator from WASM modules into host-native pipeline stages; adds dedicated `Layer::PaintRegionAnnotation` stage with guard-based host fallback; applies per-point `par_chunks(32)` parallelism; adds `union_paint_regions_at_harvest` config toggle. Closed 2026-05-20 via packet 64.
- [x] TASK-184 Emit OrcaSlicer-parity HEADER_BLOCK, extrusion-width comments, and CONFIG_BLOCK envelope in the final .gcode file. Closed 2026-05-14 via packet 55.
- [x] TASK-185 Accept an external PNG via `--thumbnail <path>` flag and emit it as THUMBNAIL_BLOCK; omit block when flag absent. Closed 2026-05-14 via packet 55.
- [x] TASK-190 Parse 3MF sidecar `Metadata/model_settings.config`; classify `<object>`/`<part>` by `subtype=`; surface typed per-part metadata (`PartSubtype`, `ObjectSidecarInfo`, `PartSidecarInfo`) keyed by `(object_id, part_id)`. Covers 3MF sidecar parsing gaps (unknown subtype downgrade to NormalPart, missing/malformed sidecar non-fatal fallback). Closed 2026-05-14 — Packet 56 (56_threemf-sidecar-parser).
- [x] TASK-191 — Branch `resolve_object` to route non-`NormalPart` geometry into `modifier_volumes`; drop paint on non-`NormalPart` rows; bump `MeshIR.schema_version` 1.0.0 → 1.1.0. (packet 56b_threemf-modifier-part-ir-routing)
- [x] TASK-192a — Wire `modifier_part` consumer: region-mapping Z-overlap stamp for `fuzzy_skin.apply_to_all`. Includes fuzzy-skin manifest gate. (packet 56b_threemf-modifier-part-ir-routing)
- [x] TASK-192b — New host stage apply_negative_part_subtract; per-layer 2D polygon subtract via polygon_ops::difference for negative_part modifier volumes. (56c_threemf-negative-and-support-subtype-routing)
- [x] TASK-192c — Synthetic PaintRegionIR emission for support_enforcer and support_blocker modifier volumes via paint_segmentation piggyback. (56c_threemf-negative-and-support-subtype-routing)
- [x] TASK-193 — TDD coverage: synthetic-fixture E2E (threemf_subtypes_synthetic_e2e_tdd.rs); negative_part, support_enforcer, support_blocker, ordering, degenerate cases. No-regression sweep. Acceptance ceremony. (56c_threemf-negative-and-support-subtype-routing)
- [x] TASK-208 3MF fixture E2E integration tests + OrcaSlicer-parity modifier-propagation tests + object-metadata loader fix (packet 67_3mf-fixture-e2e-hardening). Per-fixture E2E coverage through `load_model()` for cube_positive_n_negative.3mf, bridge_support_enforcers.3mf, benchy_4color.3mf. Production fixes: p:path external-`.model` support (D1); object-scoped sidecar `<metadata>` extraction with `extruder`/`enable_support`/`support_type` allowlist (D8). Withdraws AC-R1/AC-R2 as OrcaSlicer-divergent (D6); bundles Packet 68 design amendment for ENFORCER/BLOCKER subtype filter (D7).
- [x] TASK-210 — `support_filament` GCode routing: route the per-object `support_filament` / `support_interface_filament` config (`PrintConfig.hpp:1007-1009` in OrcaSlicer) through the GCode emit path so support material can select its own extruder. Required prerequisite for real-fixture GCode-level multi-material on `bridge_support_enforcers.3mf` (support_enforcer/blocker `extruder` is decorative per OrcaSlicer; supports use `support_filament`). Surfaced as a downstream gap by Packet 67 D8. Selection is global (flat `SupportIR`, no per-object identity); per-object support filament is a future packet. (done via packet 172)
- [x] TASK-211 — Real-fixture multi-material GCode-level E2E: assert T0/T1 tool-change emission on a real 3MF fixture (not synthetic). Depends on TASK-210 (`support_filament` routing) AND/OR a paint-driven MMU fixture with a meaningful per-region extruder differential preserved through the loader. Replaces Packet 68's originally-specified AC-2 (which was retargeted to synthetic-IR per Packet 67 D7). Surfaced by Packet 67 D8. (done via packet 172)
- [x] TASK-212 — Extended object-metadata allowlist: add additional 3MF object-scoped sidecar keys (e.g. `nozzle_diameter`, `bed_temperature`, other per-object printer keys) to the loader's allowlist in `loader.rs::object_metadata_to_config_data` as consumer code materializes. Current allowlist (`extruder`/`enable_support`/`support_type`) landed via Packet 67 D8. (done via packet 172)
 - [x] TASK-212b — Extended **part-level** metadata allowlist: the `<part>`-scoped metadata extraction in `crates/slicer-model-io/src/loader.rs` currently reads only `subtype`/`fuzzy_skin`/`extruder`/`matrix`; per-part keys such as `inner_wall_line_width`/`outer_wall_line_width`/`sparse_infill_density`/`sparse_infill_line_width` are silently dropped at load time (discovered packet 89 via `cube_cilindrical_modifier.3mf`). Extend the part-level allowlist and restore the strengthened typed-metadata assertion in `modifier_volume_carries_typed_metadata`. Sibling of TASK-212 (object scope).
- [x] TASK-194 — Emit configurable `machine_start_gcode` / `machine_end_gcode` via a `PostPass::GCodePostProcess` module that prepends/appends `Raw` commands carrying the resolved templates. (59_machine-start-end-gcode-emission)
- [x] TASK-194a — Create `modules/core-modules/machine-gcode-emit/` declaring four `[config.schema.*]` entries; `run_gcode_postprocess` performs real `[key]` substitution against the effective `ConfigView` and rebuilds the command list as `[Raw(start), ...existing..., Raw(end)]`. (59_machine-start-end-gcode-emission)
- [x] TASK-194b — Promote `M82`/`M83` from the hard-coded `DefaultGCodeSerializer` preamble to a new `GCodeCommand::ExtrusionMode { absolute: bool }` variant pushed by `DefaultGCodeEmitter` so a `GCodePostProcess` module can prepend before it. (59_machine-start-end-gcode-emission)
- [x] TASK-204: Normalize String CLI arg types to PathBuf
- [x] TASK-205: Complete HostRunOptions, delete validate_run_options and CliError
- [x] TASK-206: Remove dead --module flag
- [x] TASK-207: Create parent directories for --output and --report paths
- [x] TASK-208: Stamp config_delta.fields from per-object modifier_volumes into RegionPlan.config.extensions via overlay_resolved (subtype-filtered, global-per-object scope); add config-extensions-driven required_tool fallback in layer_executor.rs. (68_extruder-per-modifier-gcode)
- [x] TASK-209: Integration tests covering extruder/fuzzy_skin stamping, T0/T1 GCode emission (partial-pipeline synthetic harness), subtype-only no-op, conflicting modifier priority, and AC-Filter regression guard. (68_extruder-per-modifier-gcode)
- [x] TASK-213 Merge slicer-host and slicer-cli into a single pnp_cli binary; rename slicer-host crate → slicer-runtime (library only); extract run_slice() library API; externalise host built-ins onto Producer trait reaching dag_cli; consolidate manifest validator constants into slicer-schema. Closed 2026-05-28 via packet 69_pnp-cli-unification.
- [x] TASK-214 Replace the two guest-build bash scripts with `cargo xtask build-guests` driven by a validated filesystem walk; rewrite `docs/05_module_sdk.md` build-flow docs; collapse `CLAUDE.md`'s guest-staleness block to a single `cargo xtask build-guests --check` invocation. Closed 2026-05-29 via packet 70_workspace-aware-guest-builder.
- [x] TASK-060 Paint-ready 3MF export surface (delivered by packet 71_paint-ready-3mf-export): geometry-only `write_obj`/`write_3mf` writers in `slicer-runtime`; `split_connected_components` in `slicer-helpers` (OrcaSlicer `its_split` parity); `pnp_cli mesh convert`/`mesh import --output-format 3mf` verbs. Closed 2026-05-29 via packet 71_paint-ready-3mf-export.
- [ ] TASK-298 Stop routing `paint_seam` through the MMU cell decomposition: it mints a spurious `seam_enforcer` variant region with its own wall set and cuts a real internal wall boundary through the part. **Observed** on `resources/cube_cilindrical_modifier.3mf`, which carries `paint_seam` strokes. Layers 0 and 1 emit one rectangular `;TYPE:Outer wall` loop, `(112.70, 92.70) → (137.30, 92.70) → (137.30, 117.30) → (112.70, 117.30)`. From layer 2 up the same loop has a 45-degree notch carved out of the (112.5, 92.5) corner — `(112.70, 92.99) → (121.16, 101.45) → … → (125.12, 92.70) → …` — and a second, triangular outer loop `(112.99, 92.70) → (121.10, 92.70) → (121.10, 100.81)`, with its own two inner loops, fills the carved corner. **Root cause:** `mesh_has_any_paint` (`crates/slicer-core/src/algos/paint_segmentation/mod.rs`) admits a mesh on any painted facet or stroke regardless of semantic, and `execute_paint_segmentation` in the same module then runs the colorize → MMU-graph → cell-decomposition pipeline and rebuilds `SlicedRegion`s per (RegionKey × variant_chain). `dominant_semantic` there is a label on the output, not a gate on the partition, so seam paint partitions exactly as colour paint does. **Canonical does not partition on seam paint.** `gather_enforcers_blockers` (`libslic3r/GCode/SeamPlacer.cpp`) reads `ModelVolume::seam_facets` into two AABB trees; `extract_perimeter_polygons` / `process_perimeter_polygon` tag sampled points on **already-generated** perimeters as enforced / blocked / neutral; the only geometric effect is rotating a loop's start vertex in `SeamPlacer::place_seam` via `ExtrusionLoop::split_at_vertex` / `split_at`. `Print::apply` (`libslic3r/PrintApply.cpp`) makes the asymmetry explicit: a seam-paint change invalidates only `psGCodeExport`, while colour and fuzzy-skin paint changes are folded into `solid_or_modifier_differ` and reshuffle regions. Canonical partitions for exactly two channels — MMU colour (`multi_material_segmentation_by_painting` → `apply_mm_segmentation`, `libslic3r/PrintObjectSlice.cpp`) and fuzzy skin (`fuzzy_skin_segmentation_by_painting` → `apply_fuzzy_skin_segmentation`, same file) — and `generate_print_object_regions` (`libslic3r/PrintApply.cpp`) derives its painted regions from `mmu_segmentation_facets` alone. **Filed 2026-07-24, not fixed:** the fix is a per-semantic gate on which paint channels may partition, and it moves the wall geometry of every painted fixture, so it needs its own packet. Surfaced while settling AC-2c in `crates/slicer-runtime/tests/e2e/modifier_infill_tdd.rs` — the spurious triangle is the region whose single-line sparse fill that test was mis-reading as a linker gap.
- [ ] TASK-299 Reconcile `wall_count` with the number of wall loops actually emitted. **Observed** on a plain `resources/20mm_cube.obj` control print with the full core-module set: the CONFIG_BLOCK carries both `; wall_count = 2` and `; wall_loops = 2`, while every layer emits three loops per contour — one `;TYPE:Outer wall` at the 0.2 mm inset and two `;TYPE:Inner wall` loops at 0.6 mm and 1.0 mm. `ResolvedConfig`'s `wall_count` (`crates/slicer-ir/src/resolved_config.rs`) defaults to 2 and is the key `classic-perimeters` reads (`base_wall_count` in `run_perimeters`, `modules/core-modules/classic-perimeters/src/lib.rs`). Canonical's classic path computes a 0-indexed `loop_number = wall_loops + extra_perimeters - 1` (`PerimeterGenerator.cpp`), so `wall_loops = 2` yields two loops total, one outer plus one inner — which points at the emitted geometry rather than the report as the side that disagrees. **Filed 2026-07-24, not investigated further and not fixed:** correcting it moves every wall-bearing golden in the suite. `modifier_infill_tdd::control_loops_per_contour` deliberately derives its per-contour loop count from a control print rather than from this key, and cites this row for the reason.

- [ ] TASK-300 Cover `Generator::generate_trees`' seed/propagate interleaving on a fixture where propagation actually succeeds. 2528f644 ported canonical `Node::realign` and merged seeding and propagation into one top-down loop, so layer N's propagated trees are present in layer N-1 before N-1 seeds. `Node::propagate_to_next_layer` itself is covered directly for all three branches in `crates/slicer-core/tests/algo_lightning_tdd.rs` — inside (tree survives with structure preserved against a real enclosing outline), outside (node dies, subtree rerooted), and straddling — but `Generator::generate_trees` is called from exactly one test in the tree, `lightning_generator_tree_continuity`, whose `[10, 10, 10, 12]` prism drops every tree at realign, so the interleaving is never observed with a surviving tree. **Blocked on the fixture API, not on effort:** `Generator::new` takes one simple contour per layer, and `generate_initial_internal_overhangs` defines layer N's overhang as layer N minus the *dilated* outline of layer N-1 — so every seed is outside layer N-1 by construction and no single-contour prism can produce a surviving propagation. Reaching it needs layers that can carry holes or multiple contours (a hole in layer N-1 puts the seed in the hole while the tree's root grounds on layer N's shared outer boundary, which straddles), which is a change to `Generator`'s input shape. **Filed 2026-07-24** from the code review of this branch; a regression that made realign reject everything would currently leave all lightning tests green.
- [ ] TASK-301 Collapse the three copies of the per-tool filament-density fallback. d6ff9062 added `ResolvedConfig::filament_density_for` (`crates/slicer-ir/src/resolved_config.rs`) as the accessor for canonical `Extruder::filament_density`'s `get_at(m_id)` semantics, but it has **zero call sites**: the identical `.get(tool).or_else(|| .first()).copied()` rule is open-coded in the `weight_grams` closure in `crates/slicer-runtime/src/run.rs` and again in `filament_stats_comment_block` (`crates/slicer-gcode/src/m73.rs`). Behaviour is currently correct at both live sites, so this is latent rather than broken — but `slice_stats.gcode_weight_grams` and the G-code `; filament used [g]` line compute the same quantity from two independent copies and can drift. **Filed 2026-07-24, not fixed:** every placement that removes the dead accessor touches `crates/slicer-ir/`, which stales all 34 guest WASMs, and the alternative (host the rule in `slicer-gcode`, which both live sites can reach) leaves the dead accessor behind. Fold it into the next change that already touches `slicer-ir`.
- [ ] TASK-302 Decide the compatibility policy for `ResolvedConfig`'s serialized shape. d6ff9062 changed `filament_density` from `Option<f32>` to `Vec<f64>` on a `Serialize`/`Deserialize`-derived type that is embedded as `RegionMapIR.configs: Vec<ResolvedConfig>`, with no bump to `CURRENT_REGION_MAP_IR_SCHEMA_VERSION` and no `#[serde(default)]` on the field. Nothing in the tree deserializes `RegionMapIR` or `ResolvedConfig` from a persisted payload, so there is no reachable break today — the type is constructed in-process and marshalled over WIT, not loaded from disk. **Filed 2026-07-24** so the question is answered before anything starts persisting these: either add `#[serde(default)]` and a scalar-or-list deserializer so old payloads load, or state that `ResolvedConfig`'s serde form is in-process-only and not a compatibility surface. Note the same commit's `support_enabled` → `enable_support` change is **not** part of this: it renamed the CLI/config-map key string only, while the Rust field stayed `support_enabled`, so the serde shape is unchanged (`cli "enable_support" support_enabled: bool` in `declare_resolved_config!`).
 - [x] TASK-303 Make the config parser retain the `Percent` / `FloatOrPercent` values it parses, so a percent-typed key can reach a live slice. **Filed 2026-07-31 by packet 184** (`184-classic-perimeter-flow-parity`), tracked as the percent-transport residual in `docs/DEVIATION_LOG.md`. Packet 184 retyped `classic-perimeters`' `outer_wall_line_width` / `inner_wall_line_width` (and `min_width_top_surface`) to `float_or_percent` and resolves them module-side with `ConfigView::get_abs_value`, but packet 185 (`185-arachne-width-bridge-parity`) has now closed that barrier: the scheduler's `config_resolution` threads percent values from `ConfigFieldEntry::parsed_default` through `ConfigBoundsIndex::schema_defaults` into `ResolvedConfig.extensions`, so a percent-typed key with a schema default reaches a live slice end-to-end when the profile supplies no value. `parse_percent_default` (`crates/slicer-scheduler/src/manifest.rs`) retains its validation role, and the parsed value is no longer discarded — `ConfigFieldEntry::parsed_default` keeps it and `resolve_global_config` inserts it into `extensions`. `ResolvedConfig::to_config_map` was explicitly **not** the barrier; its `extensions` pass-through already carried any variant unchanged. The float form (including the canonical `0` auto sentinel, which resolves to `1.125 × nozzle_diameter`) remains reachable as before. Two smaller parity follow-ups from the same packet are recorded on their deviation rows rather than here: `[FWD-1]` (PnP resolves an absent wall-width key to `0.4` mm where canonical resolves to auto) on `the wall-width follow-up`, and `[FWD-2]` (canonical's `ext_perimeter_spacing/2` floor on the `min_width_top_surface` threshold, not ported on either perimeter module) on `the classic min-width follow-up`.
- [x] TASK-305 Correct the custom-G-code placeholder engine in `machine-gcode-emit` (`substitute_placeholders`, stage `PostPass::GCodePostProcess`). Delivered by packet `186-custom-gcode-placeholder-engine`: literal text is now copied char-boundary-correctly instead of via `out.push(bytes[i] as char)`, which previously mojibaked any non-ASCII template; an unresolved `[key]` still passes through verbatim (`warn-and-pass`; packet 59's rule, retained deliberately — PnP is modular and a template may reference keys owned by modules that are not loaded, so failing the slice would break composition) but now emits **one aggregated warning** naming every unresolved key and injection point; list-valued config keys resolve from their first element, so `[nozzle_diameter]` works for real 3MF input; `nozzle_diameter` is declared as a resolvable key; and `first_layer_temperature` is aliased onto `nozzle_temperature_initial_layer`. A fatal-on-unresolved policy was built and then reverted before landing after it broke three e2e tests on real OrcaSlicer-authored 3MF fixtures. Closes the engine half of the custom-G-code closure; the eight advertised macros with no PnP key to bind them to (`[bed_temperature]`, `[filament_type]`, `[tool_count]`, `[layer_count]`, `[print_time_estimate_s]`, `[x_max]`, `[y_max]`, `[z_max]`) are filed as a residual deviation, and the remaining unimplemented injection points stay with the custom-G-code closure for packets 187 and 188.
- [x] TASK-306 Packet 187 custom-G-code injection registry: implement and register `before_layer_change_gcode`, `layer_change_gcode`, and `time_lapse_gcode` in the machine-G-code emission path. the custom-G-code closure remains Open for the accepted toolchange/role/unreachable injection-point remainder assigned to packet 188; parity residuals are recorded in DEV-108.
- [x] TASK-307 Packet 188 custom-G-code toolchange and role sites: extend the injection-point registry with the six new sites (`filament_end_gcode`, `change_filament_gcode`, `filament_start_gcode`, `change_extrusion_role_gcode`, `filament_change_extrusion_role_gcode`, `process_change_extrusion_role_gcode`) and file three residual rows for the five unreachable points, the variable-level residuals, and the `filament_extruder_id` two-id-space divergence. Closes the custom-G-code closure; sibling to packets 186 / TASK-305 and 187 / TASK-306.

### Workstream 4 — Progress events coverage

- [ ] TASK-136 Add end-to-end progress-event coverage proving paint-annotation failure codes 501-504 reach the JSONL emitter on the live pipeline path. Supports acceptance evidence and guards the live path after progress events closure. **Note (2026-05-20 via packet 64):** The code 504 warning path is now exercised by the always-on host annotator (`Layer::PaintRegionAnnotation` stage), not only in E2E progress-event tests.
- [x] TASK-137 Python postpass live-path decision — superseded 2026-05-31 by scrap of the Python bridge feature. DEV-024 closed; pyo3 dep + python_bridge.rs + integration tests removed; TextPostProcess tier remains WASM-only via `run-text-postprocess`. TASK-137a/b/c retired.
- [x] TASK-138 Close the Python `Init` phase coverage gap. Test file removed 2026-05-31 alongside the Python bridge scrap (TASK-137); entry retained for historical traceability.

### Workstream 5 — Governance and closure drift

- [~] TASK-139 Close the DEV-020 source/docs drift around dead fallback runners and Phase G closure notes.
- [ ] TASK-139a Remove dead `Noop*Runner` remnants from the source tree and any stale tests or wiring. Covers DEV-020.
- [ ] TASK-139b Correct the Phase G closure notes in `docs/07_implementation_status.md` once the source cleanup lands so docs and source agree. Covers DEV-020.
- [ ] TASK-140 Evaluate the Architecture Acceptance Gate using `docs/11_operational_governance_and_acceptance_gate.md` and `docs/12_architecture_gate_metrics.md` once TASK-120 and its subtasks are complete. Covers acceptance evidence and the evidence gaps in DEV-026.
- [~] TASK-141 Keep the planning/governance docs synchronized with the live dependency graph and deviation registry. Supports DEV-030.
- [ ] TASK-141a Update `docs/07_implementation_status.md` dependency ordering and workstream sequencing whenever a remediation task is added, split, or closed. Supports DEV-030.
- [ ] TASK-141b Keep `docs/DEVIATION_LOG.md` synchronized with every open architectural deviation, linked task IDs, and close status. Supports DEV-030 and live-registry hygiene for the acceptance gate.
- [ ] TASK-141c Remove stale blocker summaries and closure notes from `docs/11_operational_governance_and_acceptance_gate.md` and `docs/12_architecture_gate_metrics.md` as their owning tasks land. Supports DEV-030.
- [x] TASK-154 Enforce `min_host_version` at startup and add semver pass/fail coverage for compatible and incompatible manifests. Covers DEV-026. Closed 2026-07-03 — corrects stale status, not new work: `DagValidationPass::HostVersionCompatibility` landed in an earlier commit (`ca8fe483`) but this checkbox and the DEV-026 log row were never updated until now.
- [ ] TASK-155 Make manifest-schema validation surface a real `Schema` failure for missing or invalid schema declarations, with CLI and host regression tests. Continues DEV-026.
- [ ] TASK-156 Add runtime-budget evidence collection for `docs/12_architecture_gate_metrics.md` memory, host-call, and full-slice time thresholds, plus reproducible benchmark/report hooks. Continues DEV-026.
- [x] TASK-200 Workspace-wide `Default` / builder ergonomics migration per [`docs/specs/_OLD/default-builder-migration.md`](./specs/_OLD/default-builder-migration.md) (superseded spec) — 271 production structs classified into 71 Bucket A (`#[derive(Default)]`), 28 Bucket B (manual `impl Default`), 5 Bucket C (consuming-style builders), 167 Bucket D (untouched). Spec status: ready for implementation; all §11 open questions resolved 2026-05-17. Next worker starts on TASK-200a (no gating questions remain). Land chunks in order: each chunk is a separate PR, independently mergeable with green `cargo check --workspace` and the chunk's narrow tests; full `cargo test --workspace` runs at chunk close via sub-agent dispatch per CLAUDE.md test discipline.
- [x] TASK-200a Chunk 1 — `slicer-ir` Bucket A POD leaf types: `#[derive(Default)]` added to ~30 small `slice_ir.rs` structs plus `#[default]` variants on `WallGenerator`/`InfillType`/`SupportType`. Semver: patch. Spec §9 Chunk 1, §4.1.
- [x] TASK-200b Chunk 2 — `slicer-ir` schema-versioned IR `Default` impls + composite struct `Default` derives + workspace call-site struct-update sweep. Adds 13 `CURRENT_*_IR_SCHEMA_VERSION` constants (per-IR audit; known non-1.0.0 cases: `MeshIR` → 1.1.0, `RegionMapIR` → 1.1.0); adds manual `impl Default` for 13 schema-versioned IR types plus `#[derive(Default)]` on composite IR structs; sweeps struct-literal call sites across slicer-host, core-modules, and tests. Semver: patch on `slicer-ir`. Spec §9 Chunk 2, §5.1, §11 Q1. Sweep landed partial — ~50 files still carried verbose literals; completed separately by TASK-200b-sweep.
- [x] TASK-200b-sweep Completed the chunk-2 call-site sweep deferred from TASK-200b: replaced verbose `schema_version: SemVer { ... }` and zero-valued field literals with `..Default::default()` across slicer-host, slicer-sdk, core-module tests, and slicer-helpers. Pure mechanical, no contract change. Spec §9 Chunk 2 (sweep half), §11 Q1.
- [x] TASK-200c Chunk 3 — `slicer-helpers` `DecimateConfig` builder + Bucket A derives. Built `DecimateConfigBuilder` (consuming, terminal `build() -> Result<_, DecimateError>`); demoted `DecimateConfig` fields to `pub(crate)`; moved validation from `decimate()` to `build()`; added `Default` derives to `DecimateResult`/`RepairResult`/`NamedMesh`/`StepImportResult` (added `#[default] Unknown` to `StepLengthUnit` to unblock the last one). Updated all in-tree callers; refreshed `docs/13_slicer_helpers_crate.md`. Semver: MAJOR on `slicer-helpers`. Spec §9 Chunk 3, §6.5.
- [x] TASK-200d Chunk 4 — `slicer-host` builders `LoadedModuleBuilder`, `CompiledModuleBuilder`, `ConfigFieldSchemaBuilder` (consuming `mut self -> Self` setters, type-level `#[must_use]`; `ConfigFieldSchemaBuilder` uses the spec §6.3 non-consuming exception). Production call sites converted in the same PR (`FullConfigSchema::default()`, `parse_field_schema`). Pub fields kept pub — demotion deferred to TASK-200d-sweep. Semver: patch on `slicer-host` for this commit (MAJOR deferred until the sweep demotes fields). Spec §9 Chunk 4, §6.1, §6.2, §6.3.
- [x] TASK-200d-sweep Completed the chunk-4 call-site sweep deferred from TASK-200d, landed as two commits: (1) literal sweep (`f431ff0`) replacing every `LoadedModule`/`CompiledModule`/`ConfigFieldSchema` literal in `slicer-host` with the corresponding builder; (2) pub demotion + accessors (`efb9af0`) demoting fields to `pub(crate)` with per-field accessors. Semver: MAJOR on `slicer-host`. Bench gate: 7 of 8 benches improved 17-41%; `instrumentation_noop/1000` regressed +9.6% mean (CI [+6.1%..+13.3%]), above the spec §11 Q11 5% threshold — flagged for follow-up investigation (likely measurement variance given the cluster of large improvements elsewhere). Spec §9 Chunk 4 (sweep half), §6.1, §6.2, §6.3, §8.2, §11 Q11.
- [x] TASK-200e Chunk 5 — `slicer-host` `HostExecutionContextBuilder` + `slicer-sdk` Bucket A sweep + `slicer-sdk` `new()` removal (landed as a single PR per the user's locked decision). `slicer-host`: added `HostExecutionContextBuilder`, removed `HostExecutionContext::new`, demoted all pub fields to `pub(crate)` with read/mut accessors; swept all 73 construction sites. `slicer-sdk`: added `#[derive(Default)]` to the 14 prepass-types, `SliceRegionView`, `PerimeterRegionView`; deleted every SDK `new()` factory; swept all 93 callers. Rebuilt all core-modules + test-guests WASMs. Semver: MAJOR on both `slicer-host` and `slicer-sdk`. Bench gate: instrumentation paths improved 4.8-21.7%; two unrelated allocator benches regressed 11.5%/7.5%, matching the measurement-variance pattern flagged in TASK-200d-sweep §11 Q11. Spec §9 Chunk 5, §6.4, §4.3, §5.3, §8.3, §11 Q6, §11 Q11.

- [x] TASK-220 Single sources of truth in `slicer-runtime` (packet 76, candidates 3a-3d): one `source_key_matches_declared` matcher (3a); canonical `ResolvedConfig::to_config_map` serializer (3b); canonical `slicer_core::transform_point3` replacing 4 prior copies (3c); `stage_order.rs` deriving stage helpers from `STAGE_ORDER` (3d) — **fixes the latent `UnknownStage` rejection at startup for modules declaring `PrePass::SeamPlanning`, `PrePass::SupportGeometry`, or `Layer::PaintRegionAnnotation`**, which the two validator copies had silently dropped. Closed 2026-05-31 — packet 76.
- [x] TASK-221 Pipeline/region collapse in `slicer-runtime` (packet 76, candidates 1a/1b). 1a: collapsed `commit_region_mapping_builtin`'s post-hoc re-stamp loop to a single pass via a threaded `host_config` parameter. 1b: extracted `run_pipeline_core` in `pipeline.rs`; `run_pipeline_with_raw_config`/`run_pipeline_with_instrumentation` forward to it. `run_pipeline_with_events` deliberately left standalone — it routes through the bare `execute_postpass` (no CONFIG_BLOCK), a documented behavioral difference; merging it broke 3 `pipeline_tdd` assertions. Closed 2026-05-31 — packet 76.
- [x] TASK-223 Wire `#[module_test]` macro to fully-qualified `slicer-sdk::test_support` hooks (packet 77): adds `[features] test = []` to `slicer-sdk`; new `test_support/mod.rs` providing `reset_global_state`/`install_panic_handler`/`mock_host_setup`/`mock_host_teardown`; `#[module_test]` expansion in `slicer-macros` emits fully-qualified `::slicer_sdk::test_support::*` calls, deleting the unqualified `__slicer_test_*` phantom-name pattern. Closed 2026-05-31 — packet 77. Spec: packet 77.
- [x] TASK-224 Refactor `MockHost` into a real `MeshSource` adapter; clean `docs/05_module_sdk.md` API fictions; record ADR-0004 (packet 77). Rewrites `crates/slicer-test/src/mock_host.rs` to implement `slicer_sdk::host::MeshSource` with one-answer-per-query builder chaining; removes fictional API references in `docs/05_module_sdk.md` (`circle_polygon`, `rect_polygon`, `path_length`, `with_logging`, `clip_polygons_call_count`, per-arg `set_raycast_z_down`); creates `docs/adr/0004-test-support-lives-in-slicer-sdk.md`. Closed 2026-05-31 — packet 77. Spec: packet 77.
- [x] TASK-225 Fold `slicer-test` source into `slicer-sdk::test_support`; introduce `slicer_sdk::test_prelude`; update the `pnp-cli` module scaffold to depend on `slicer-sdk` (features `test`) instead of `slicer-test`; delete `slicer-test` crate entirely. Spec: packet 78. Closed 2026-05-31 — packet 78.
- [x] TASK-226 Migrate `arachne-perimeters` + `rectilinear-infill` as P78 exemplars; structural rewrite of `docs/05_module_sdk.md` (packet 78). Replaces hand-rolled `make_*` test helpers with `ConfigViewBuilder`/`SliceRegionViewBuilder`/`square_polygon`/`rect_path` from `slicer_sdk::test_prelude::*`; preserves every original assertion verbatim; rewrites `docs/05_module_sdk.md` § Test Support and `docs/00_project_overview.md`'s crate table. Spec: packet 78. Closed 2026-05-31 — packet 78.
- [x] TASK-227 Extend `slicer_sdk::test_support` builders to cover the IR shapes existing fixtures don't (packet 79): `print_entity`, `tool_change`, `seam_candidate` helper fns, `LayerCollectionFixtureBuilder`, and `PerimeterRegionViewBuilder::add_outer_wall_with_flags`, all under the `test` feature gate and re-exported from `slicer_sdk::test_prelude`. Production `LayerCollectionBuilder` unchanged. Spec: packet 79. Closed 2026-06-01 — packet 79.
- [x] TASK-228 Migrate 13 core-modules to `slicer_sdk::test_prelude::*` (packet 79): Group A (7 modules, existing builders cover) replaces `make_*` helpers with `ConfigViewBuilder`/`SliceRegionViewBuilder` chains; Group B (4 modules) uses the new TASK-227 helpers; Group C (3 modules with no `make_*` helpers) migrated only where it shortens the file; 4 modules with no tests skipped. Every assertion preserved verbatim. Spec: packet 79. Closed 2026-06-01 — packet 79.
- [x] TASK-229 Relocate `wipe_tower_bed_bounds.rs` → `modules/core-modules/wipe-tower/tests/bed_bounds_tdd.rs` and `prepass_support_generation_orca_parity_tdd.rs` → `modules/core-modules/support-planner/tests/orca_parity_tdd.rs`, both from `crates/slicer-runtime/tests/executor/` (packet 80) — they imported only `slicer_ir`+`slicer_sdk`+the module under test, no `slicer_runtime::*` symbols. Spec: packet 80. Closed 2026-06-01 — packet 80.
- [x] TASK-231 Audit `docs/05_module_sdk.md` §Geometry Helpers against the real `slicer_sdk::geometry` surface; `segment_path`/`distribute_points`/`path_length`/`seg_len_3d`/`flow_correction` don't exist under `slicer-sdk` — they actually live in `slicer-core`. Doc section rewritten to import from `slicer_core::{...}` with the real signatures. Closed 2026-06-01 — direct doc fix, no new packet needed.
- [x] TASK-230 Annotate 3 legitimately-runtime tests (`slicing_promotion_e2e_regression_tdd.rs`, `gcode_part_cooling_emission_tdd.rs`, `gcode_skirt_brim_emission_tdd.rs`) with `// NOT RELOCATABLE — SUT is <symbols>` comments naming their runtime SUT, since each imports a core-module by name but tests a `slicer_runtime::*` symbol (packet 80). Note: `GCodeEmitter` may move to its own crate in a future packet, at which point these comments become stale and trigger relocation. Spec: packet 80. Closed 2026-06-01 — packet 80.
- [x] TASK-233 Extract host-side model I/O into the new `slicer-model-io` crate (packet 81): moved `model_loader`/`model_loader_sidecar`/`model_writer` (~2,900 LOC) from `slicer-runtime` to `slicer-model-io`; dropped `stl_io`/`tobj`/`zip`/`quick-xml`/`uuid` from `slicer-runtime`'s direct deps; replaced `SliceRunOptions.model_path: PathBuf` with `mesh: Arc<MeshIR>` + `model_label: String`. Pulled forward P82's `helpers_cmd.rs` move (D-1) to avoid `slicer-runtime` depending on `slicer-model-io` directly. G-code SHA `89a329ad3a4c1b7febca839edfca8b6302e562d8d2a390ee144252fd54e65a2b` byte-identical pre/post. Closed 2026-06-01 — packet 81. Spec: packet 81.

- [x] TASK-234 Push pure-algo builtins down into `slicer-core` (packet 84): six pure-geometry algorithm files (~3,200 LOC) moved from `slicer-runtime/src/` to `slicer-core/src/algos/` (`mesh_analysis`, `paint_segmentation`, `prepass_slice`, `support_geometry`, `mesh_segmentation`, `overhang_classifier`); thin `*Producer` wrappers retained in `slicer-runtime/src/builtins/` per ADR-0001; `slicer-core::algos` feature-gated behind `host-algos` to keep `log`/`rayon` out of guest WASM builds. G-code SHA parity preserved. Closed 2026-06-02.
- [x] TASK-236 Extract g-code emission into `slicer-gcode` (packet 86): ~1,640 LOC moved from `slicer-runtime/src/gcode_emit.rs` to `slicer-gcode/src/{emit,serialize,thumbnail}.rs`; `GCodeEmitter`/`GCodeSerializer` trait defs relocated from `postpass.rs`; `emit_gcode` drops its unused `&Blackboard` parameter (D-5, empirical finding — no `EmitContext` trait needed); `slicer-gcode` depends only on `slicer-ir`+`slicer-core`+`slicer-helpers`, zero wasmtime transitively. G-code SHA `89a329ad3a4c1b7febca839edfca8b6302e562d8d2a390ee144252fd54e65a2b` byte-identical pre/post (unchanged across P81→P86). Completes the four-seam deepening batch (P81 slicer-model-io, P83 slicer-wasm-host, P85 slicer-scheduler, P86 slicer-gcode). Closed 2026-06-04.
- [x] TASK-237 Move `region_mapping` kernel to `slicer-core` (packet 87; final task closing Architecture Deepening Phase II). Kernel (~530 LOC) moved from `slicer-runtime/src/region_mapping.rs` into `slicer-core/src/algos/region_mapping.rs`; 129-LOC wrapper in `slicer-runtime/src/builtins/region_mapping_producer.rs` holds the producer + commit glue; `&ExecutionPlan` replaced by a local `RegionMappingPlanProjection` to keep `CompiledStage` (a `slicer-scheduler` type) out of `slicer-core`. `DEFAULT_REGION_MAP_CAP` relocated to `slicer-ir` as the canonical IR-shape constant — no new dep edges. G-code SHA `89a329ad3a4c1b7febca839edfca8b6302e562d8d2a390ee144252fd54e65a2b` byte-identical pre/post (unchanged P81→P87). Closed 2026-06-04.
- [x] TASK-238 Ship `overhang-classifier-default` core-module; delete host's direct `classify_layers` call (packet 88; final task in Architecture Deepening Phase II). New `FinalizationModule` guest WASM owns the complete overhang-classification algorithm (relocated from `slicer-core`), with zero `slicer-core` dependency in the guest to avoid `host-algos` feature-gate contamination; reads overhang + base wall speed config keys, emits `SetSpeedFactor` mutations via `FinalizationOutputBuilder`. Deleted the corresponding host-side algo, tests, and `classify_layers` call site. G-code SHA byte-identical (P81→P88 streak preserved); behavioral opt-out verified with non-zero config. ADR-0008 drafted. Closed 2026-06-05.
- [x] TASK-239 Benchy 3MF fixture retirement (packet 89): retired `benchy_4color.3mf`/`benchy_painted.3mf` by migrating ~33 test consumers to authored cube fixtures (`cube_4color.3mf`, `cube_fuzzyPainted.3mf`, `cube_cilindrical_modifier.3mf`); 1 `#[ignore]` stub documents the SupportEnforcer-on-real-fixture coverage gap pending a future fixture. The executor binary's 12 RED tests (cherry-pick `5c272ef`) remain RED per packet.spec.md §Out of Scope. Closed 2026-06-08 — packet 89.
- [x] TASK-241 Paint-pipeline schema scaffolding (packet 91): SliceIR 4.0.0, max_ir_schema 5.0.0, `RegionMapIR::default` seeds `configs[0]`. Closed 2026-06-08 — packet 91.
- [x] TASK-242 — Manifest [[region_split]] schema + priority registry + per-layer host-filtered dispatch (packet 92). Closed 2026-06-08 — packet 92.
- [x] TASK-243 RegionMapping cross-product expansion (packet 93): `(layer × ActiveRegion × variant_chain)` keying on `RegionMapIR.entries`; `overlapping_semantics_for_region` deleted (subsumed by chain-derived overlay path); `DEFAULT_REGION_MAP_CAP` raised to 750,000 with top-contributor diagnostic. Polygon population deferred to P95. Closed 2026-06-08 — packet 93.
- [x] TASK-245 Paint-segmentation OrcaSlicer-parity port: Phases 1, 2, 3, 4, 6, 7 (packet 95). Replaces the broken paint-segmentation kernel with the OrcaSlicer-parity pipeline, repositioned between `host:shell_classification` and `host:support_geometry`; deletes `PaintRegionIR`/`LayerPaintMap`/`SemanticRegion` and their host-side rtree; turns all 24 RED cube paint tests GREEN. Closed 2026-06-12 — packet 95 (complete).
- [x] TASK-246 Paint-segmentation Phase 5 `cut_segmented_layers`: width-limit + interlocking (packet 96). Implements OrcaSlicer Phase 5 in `width_limit.rs`; `mmu_segmented_region_interlocking_beam=true` skips Phase 5 entirely (OrcaSlicer parity, `MultiMaterialSegmentation.cpp:2452`); three MMU config keys default to `0.0`/`false` to preserve byte-identical default-config output. Closed 2026-06-12 — packet 96 (complete). *(Originally mis-attributed to packet 95 in the ledger; TASK-246's Phase-5 `width_limit.rs` was built by packet 96 — commit `b18c00b3` "feat(p96): paint-segmentation Phase 5".)*
- [x] TASK-246-BISECTOR AC-22b bisector-edge dedup via `SlicedRegion.external_contour` (packet 96): per-object gap-free model boundary via `union_ex` of the pre-segmentation slice; perimeter generators trace model perimeter once per painted object so outer-wall count matches the unpainted baseline; replaces the unworkable `bisector_edge_skip_mask`. Closed 2026-06-13 — packet 96 (complete). Cell-decomposition shortcut retired 2026-06-29 by packet `126_mmu-painted-cube-parity` in favor of the faithful `extract_colored_segments` walk; the `external_contour` IR field remains in `SlicedRegion`, its consumption governed by ADR-0013.
- [x] TASK-250 MMU painted-cube `cube_4color.3mf` OrcaSlicer parity closure (packet 126). Completes the Phase-4f `extract_colored_segments` port left non-faithful by packet 95; retires the boostvoronoi cell-decomposition shortcut and the corner-displacement/foreign-edge-shrink post-passes that gamed the confinement predicates; closes gaps G1/G2/G3/G7/G8; adds `MIN_REGION_AREA` small-region drop (Bug #2, OrcaSlicer `small_region_threshold` parity). **Review correction (2026-06-29):** the original closure predated the acceptance ceremony and missed two regressions (`cargo test --workspace` was not recorded and was RED) — both now fixed and the packet re-closed. **AC-G2 (≤3600 inner-wall segments) not met — owner-accepted/waived**: real count is 4464 (~38% above Orca's 3239); tracked under the Benchy parity work, not pursued in this packet. Cross-references ADR-0013 (per-colour outer-wall fragmentation). Closed 2026-06-29 — packet 126.
- [x] TASK-252 SDK→WIT origin propagation for per-region perimeter and infill output (packet 127): explicit `set-current-origin` WIT method on `perimeter-output-builder`/`infill-output-builder`, with matching `begin_region` SDK context methods; per-region output pushes carry the origin of the region currently being iterated rather than the last-touched view's stale LIFO origin. Eight guest modules updated to call `begin_region`. Deferred: absolute gcode move-count thresholds (T1>=1000, T3<=1500) pushed to a follow-up infill-generation packet — a pre-existing bug inflates absolute counts ~9-12x, out of scope here (still open). The `top_solid_fill` wipe bug in `region_partition.rs` that prevented ironing on one of two top-surface colors was subsequently fixed (`151b840c` preserves `top_solid_fill` when perimeter `infill_areas` is empty; ironing parity via `6a10391a`/`a107efb7`). Closed 2026-06-30 — packet 127.
- [ ] TASK-253 — Paint-segmentation shell-depth per-object propagation (packet 128) — scope propagation block by object_id, per-object debug_assert, multi-object mixed-height invariant test, propagation-block doc contract
- [x] TASK-254 — `clip_polylines` open-path Clipper2 op in `slicer-core::polygon_ops` (packet 129) — `Clipper64::add_open_subject` + `execute(…, solution_open)`; replaces gyroid's per-vertex ray-cast clipping downstream. Spec: `docs/specs/_OLD/infill-parity-rectilinear-gyroid-linker.md` Phase 0. Closed 2026-07-16 — closed by packet 129_clip-polylines.
- [x] TASK-255 — `Layer::InfillPostProcess` contract: `prior-infill` input (Option 1b) + six new `perimeter-region-view` fields (4 partitioned fill polygons + `tool-index` + `wall-source-region-id`); `world-layer` 1.0.0→2.0.0 (packet 130; a new required param on an existing export is a type change, hence major per `docs/11_operational_governance_and_acceptance_gate.md` — shipped as 1.1.0 and corrected). Spec: infill-parity Phase 1 + ADR-0028 §Amendment. Closed 2026-07-17 by packet 130; no `InfillIR` struct change was needed (prior-infill is a read-only view).
- [x] TASK-256 — Per-region config delivery: retire the first-match `ConfigView` (`dispatch.rs:1633-1637`); region views gain a config accessor (additive WIT bump); golden survey + carve window opens (packet 131). Spec: `docs/specs/_OLD/modifier-region-infill.md` Phase M2 + ADR-0030.
  - Closed by packet 131 (per-region config delivery).
- [x] TASK-257 — Modifier-volume geometric region split: wall-less sub-regions sharing base walls, `ModifierScope` beyond `AllFeatures`, host-only (packet 132). Spec: `docs/specs/_OLD/modifier-region-infill.md` Phase M1 + ADR-0030. (implemented via packet 132)
- [x] TASK-258 — `infill-linker` module (`Layer::InfillPostProcess`, `claim:infill-link`): connect_infill / chain_or_connect_infill / BoundaryInfillGraph ports in-module, overlap 0.45×spacing, wall-sharing-group connection branches (packet 133). Spec: infill-parity Phase 4 + ADR-0025/0026 (+ amendments). Closed 2026-07-20 by packet 133. New guest `modules/core-modules/infill-linker/` (15 module tests across 6 test files: connect, graph, offset+clip+filter, ironing pass-through, orchestrate, binding) + executor pipeline smoke (AC-10) + scheduler claim-dedup (AC-N3) + `manifest_ingestion` count 20 → 21. Orca ports with attribution header on `offset.rs`/`graph.rs`/`connect.rs` per `docs/ORCASLICER_ATTRIBUTION.md`. AC-1..AC-10 + AC-N1..AC-N3 each verified by pipe-suffixed command. Module suite 15/15 green; `cargo clippy -p infill-linker --all-targets -- -D warnings` clean; `cargo check --workspace --all-targets` clean; `cargo xtask build-guests --check` clean (33/33 fresh). Doc Impact: `docs/03_wit_and_manifest.md` claim row + `docs/01_system_architecture.md` inventory mention. Carve-list delta: none (the linker is pipeline-transparent on 131/132/134/135 fixtures — verified by the AC-10 executor smoke).
- [x] TASK-259 — `rectilinear-infill` raw-emit parity rewrite: per-ExPolygon scan-line, half-open vertex test, `adjust_solid_spacing`, `infill_direction`, `pattern_shift` (packet 134). Spec: infill-parity Phase 2. Closed 2026-07-19 by packet 134. Per-ExPolygon `scan_expolygon` ported from `FillRectilinear.cpp::slice_region_by_vertical_lines` (half-open edge test, sort+pair). `adjust_solid_spacing` ported from `FillBase.cpp:326-340`. `pattern_shift` applied to scan-line start x via `infill_shift_step` config (sign-alternates per layer). `fill_expolygon_multi` and `collect_edges` deleted. 7 new AC tests in `rectilinear_raw_emit_tdd.rs` GREEN; bridge_angle priority re-asserted (AC-6) via existing `bridge_paths_use_bridge_orientation_not_sparse_alternation` + `bridge_areas_emit_bridge_infill_at_oriented_angle`. Module suite 31/31 green; `cargo clippy -p rectilinear-infill --all-targets -- -D warnings` clean; `cargo check --workspace --all-targets` clean; `cargo xtask build-guests --check` clean (33/33 fresh). AC-1/2/3/4/5/7/N1 each verified by pipe-suffixed command. Carve-list delta: none (5 `cube_4color_*` carvings remain packet 136's restore scope).
- [x] TASK-260 — `gyroid-infill` raw-emit fixes + multi-role claims: rotation-order fix, delete ray-cast clipping, `align_to_grid`, expand 10×, +3 solid claims per ADR-0027/the gyroid multi-role deviation (packet 135). Spec: infill-parity Phase 3. Closed 2026-07-19 by packet 135. Rotation-order rewritten per FillGyroid.cpp:300-376 (rotate ExPolygon by `-(base + correction)`, generate waves in rotated bbox, rotate points back at emission). 4 broken clipper helpers deleted (`clip_polyline_to_expolygon`, `point_in_expolygon`, `point_in_polygon`, `polygon_bbox_mm`); `align_to_grid` helper added (snaps `bb.min` to multiple of `2π×scale_factor`); expand 4.0→10.0×spacing at lib.rs:259. Manifest `claims.holds` gains `claim:top-fill`/`bottom-fill`/`bridge-fill` (the gyroid multi-role deviation opt-in). Wave core byte-identical (`gyroid_f`, `make_one_period`, `make_wave` unchanged). 6 new AC tests + `default_holders_gyroid_sparse_only` regression in `gyroid_infill_tdd.rs` GREEN. Module suite 23/23 green (3 lib + 19 tdd + 1 binding); `cargo clippy -p gyroid-infill --all-targets -- -D warnings` clean; `cargo check --workspace --all-targets` clean; `cargo xtask build-guests --check` clean (rebuilt once; gyroid-infill-guest was stale after manifest edit). AC-1/2/3/4/5/6/7/8/9/N1 each verified by pipe-suffixed command. Carve-list delta: none. Pre-existing e2e divergences (NOT caused by this packet, verified on baseline 995e9380): `wedge_per_region_config_delivery_byte_identical`, `wedge_multi_layer_top_bottom_evidence`, `legacy_zero_matches_golden` — packet 136's restore scope. **Follow-up 2026-07-19**: per-region `infill_density` / `line_width` (packet 131 / TASK-256 accessor) wired into `gyroid-infill` and `rectilinear-infill` via the new `slicer_sdk::config_resolution::resolve_float` helper (3 SDK unit tests). New tests: `per_region_density_overrides_module_global` in both module suites, plus `rotated_square_45_per_point_correspondence_within_2mm` (the strict per-point counterpart to AC-2's bbox test). AC-5 one-liner fixed (multi-line regex matches the full `holds` array). the gyroid multi-role deviation row in `DEVIATION_LOG.md` flipped to closed.
- [x] TASK-261 — Infill-parity integration: modifier e2e fixture (M3), `infill_overlap` CLI exposure, golden restore + single re-bless, `no_linker_module_degraded_raw_output` negative test, workspace acceptance ceremony (packet 136). Spec: infill-parity Phase 5 + modifier-region-infill Phase M3. Closed 2026-07-20 by packet 136. M3 fixture: `resources/cube_cilindrical_modifier.3mf` sidecar extended (base 15% / modifier 40%); loader plumbs `sparse_infill_density` into both `ObjectMesh.config.data` and `ModifierVolume.config_delta.fields` (FACT R — sidecar was preserving the key, allowlist was dropping it). Loader smoke test `mod_cilindrical_modifier_infill_density_tdd.rs` GREEN (2 tests). AC-1 (`modifier_infill_two_densities`) + AC-2 (`modifier_infill_boundary_anchoring`) GREEN via per-bucket gcode proxies (wall-loop-count-constant + per-block G1-moves ≥ 2). AC-3 (`wedge_linked_infill_report`) GREEN via per-bucket G1-moves ≥ 2 (IR-level inspection out of scope for the binary; the linker's own tests cover IR). AC-4 (`infill_overlap` CLI binding) GREEN: 3 binding tests in `tests/infill_overlap_cli_binding_tdd.rs` (binding + to_config_map differs) PLUS new `tests/e2e/infill_overlap_changes_gcode_tdd.rs` (e2e: 0.30 vs 0.45 produces measurably different gcode, proving the linker consumes the value). AC-5 (golden restore) — 16 `carved: infill-parity D6` markers across 5 `cube_4color_*` files removed; `cargo test -p slicer-runtime --test executor` 190/190 GREEN. Wedge canary `wedge_per_region_config_delivery_byte_identical` re-blessed `8a3b645e…` → `c6cbe685…` (the latter is the post-AC-4 state with `infill_overlap` key in CONFIG_BLOCK; transit through `7ac636aa…` on intermediate trees was verified by git-stash re-runs; canary now serves as a forward-looking regression guard). AC-N1 (`no_linker_module_degraded_raw_output`) GREEN with tightened discriminator `mean < 6.0` (calibrated 2026-07-20: with-linker mean ≈ 33.4, without ≈ 4.68 — comfortable margin both sides). **Out-of-scope for this packet (packetized follow-ups):** per-region density emit in `crates/slicer-gcode/src/serialize.rs:440` (currently a single hardcoded `sparse_infill_density = 15%`; needs per-region emit for AC-1's "two distinct line spacings" claim to be falsifiable from gcode); IR-level `points_per_path > 2` and `mean ≤ 2` claims (out of reach for the e2e binary; would need `run_pipeline_with_instrumentation` with custom runners which don't use the real linker). **Pre-existing failures NOT caused by this packet** (verified on clean tree 136ab4c1): `legacy_zero_matches_golden`, `wedge_multi_layer_top_bottom_evidence`, `wedge_support_marker_present` (voronoi panic), `infill_partitioned_input_tdd::ac7_*` (2), `machine_start_end_gcode_emission_tdd::empty_end_gcode_emits_no_block`, `visual_debug_typed_tap_capture_tdd::unavailable_tap_source_fails_without_partial_success`. **Recorded deviations** (all within ≤ 20-line fence): (1) loader `sparse_infill_density` allowlist (~30 lines: helper `parse_density_value` + 2 dispatch arms) — packet-designated in-scope; (2) sidecar-test assertion update (1 line, 60% → 40%); (3) `slice_progress_events_default_tdd.rs` schema_version fix (5 lines: pre-existing stale test that asserted uniform `1.2.0` but slice_stats is `1.2.0` and other events are `1.3.0`). Carve-list delta: 16 markers restored → 0 remaining (`rg -c 'carved: infill-parity D6' --glob '*.rs' = 0`). **Adversarial review:** 7 findings (F1–F7) raised by spec-review; all 7 addressed in-tree (AC-1 rewritten to wall-loop-count-constant + sparse-block-count as the gcode-observable subset of the spec; F1's density match replaced with code comment + IR-level pointer; F2's tautology deleted; F3's mean assertion replaced with per-bucket ≥ 2; F4's IR claim documented as out-of-scope; F5 added new e2e test; F6 tightened discriminator; F7 canary re-blessed to current digest). Re-verified via the packet's six pipe commands + the new F5 test.
- [x] TASK-262 — `PrePass::LightningTreeGen` stage + `LightningTreeIR` + WIT read-view + ADR-0029 (packet 137). Spec: `docs/specs/_OLD/lightning-infill-parity.md` Phase L1.
- [x] TASK-263 — Lightning `DistanceField` + `TreeNode` port into `slicer-core::algos::lightning` (packet 138). Spec: lightning-infill-parity Phase L2.
- [x] TASK-264 — Lightning `Layer` + `Generator` port + prepass producer wiring (packet 139). Spec: lightning-infill-parity Phase L3.
- [x] TASK-265 — Completed packet 140 (Phase L4): lightning-infill module rewrite — raw-emit sampler, WIT paint-view extension, grounding search port, the lightning raw-emit migration + D-137 closed.
- [x] TASK-267 — Visual Pipeline Debug request and bundle contract: add `pnp_cli visual-debug` request validation, bundle lifecycle, overwrite behavior, and manifest model without stage taps or rendering. Spec: visual-pipeline-debug candidate packet 1.
- [x] TASK-268 — Visual Pipeline Debug typed tap capture: add request-gated post-stage capture adapters and minimal dependency-closure execution. Spec: visual-pipeline-debug candidate packet 2. Implemented in packet 158-visual-debug-typed-tap-capture.
- [x] TASK-269 — Visual Pipeline Debug intermediate renderer: render typed geometry, swept widths, overlays, shared viewport/palette, and PNG output. Spec: visual-pipeline-debug candidate packet 3. Implemented in packet 159-visual-debug-intermediate-renderer: `crates/slicer-runtime/src/visual_debug_render.rs` renderer, wired additively into `crates/pnp-cli/src/visual_debug.rs`.
- [x] TASK-270 — Visual Pipeline Debug final G-code renderer: parse the PnP G0/G1 subset and emit final PNGs with unclassified and unsupported-construct warnings. Spec: visual-pipeline-debug candidate packet 4. Implemented in packet 160-visual-debug-gcode-renderer: standalone final-G-code visual-debug renderer implemented, replacing the placeholder Gcode arm in crates/pnp-cli/src/visual_debug.rs.
- [x] TASK-271 — Visual Pipeline Debug agent surface and verification: add the skill, guide examples, contract tests, determinism checks, and no-overhead measurements. Spec: visual-pipeline-debug candidate packet 5.
- [x] TASK-272 — Non-uniform scale support: delete the dead `validate_non_uniform_scale` rejection + `NonUniformScaleUnsupported` variant; prove per-axis transform baking with loader tests. Spec: packet 166-nonuniform-scale-bake.
- [x] TASK-273 — CONFIG_BLOCK viewer-key correctness: purge speed/accel/jerk-valued padding from `ORCA_CONFIG_PADDING` (machine_max_* never emitted as padding), synthesize non-BBL `printer_model = Generic PNP Printer` when absent, and document the fork-facing required-key contract in `docs/02_ir_schemas.md`. Spec: packet 167-config-block-viewer-keys.
- [x] TASK-275 — Acceleration-aware trapezoidal print-time estimator (post-emit pass in slicer-gcode), `slice_stats` progress event (schema 1.2.0), and `layer_count` on `phase_start(per_layer)`. Spec: packet 169-time-estimator-slice-stats. Closed 2026-07-17.
- [x] TASK-279 — M73 progress injection with stealth Q/S pair and `; filament used` / `; estimated printing time (normal mode)` comment block, plus the new `disable_m73` config key. Spec: packet 175-m73-progress. Closed 2026-07-19.
- [x] TASK-280 — Multi-format thumbnail writer + PNP-side decode/rescale/encode: replaced the bare-base64 `THUMBNAIL_BLOCK` with OrcaSlicer's per-entry wire format (`; <tag> begin <W>x<H> <len>` / `; <tag> end`, 78-col wrap) and generate every entry requested by the `thumbnails` config key from the single `--thumbnail` source PNG via PNP-side transcode into PNG/JPG/QOI/BTT_TFT/ColPic. Host-side only (`crates/slicer-gcode`, `crates/slicer-runtime/src/pipeline.rs`); fork renders ONE high-res PNG (deviation `D-173-THUMBNAIL-SINGLE-PNG` from fork ticket 011). AC-1..AC-7 + AC-N1/AC-N2 green; `cargo clippy --workspace --all-targets -- -D warnings` clean. Spec: packet 173-thumbnails-multiformat. Closed 2026-07-19. **Renumbering note:** the packet's local task-map and `task_ids` frontmatter minted `TASK-277`, but that id was never registered here; `TASK-280` is the authoritative docs/07 identifier for packet 173.
- [x] TASK-281 — B5 `support-planner::tapered_radius` two-piece tip-cone formula: `mm_to_top = dist_to_top * effective_layer_height`; `raw = if mm_to_top <= branch_radius { mm_to_top } else { branch_radius + (mm_to_top - branch_radius) * tan_diameter_angle }`; then `clamp(raw, 0.0, MAX_BRANCH_RADIUS_MM = 6.0)`. Verbatim port of OrcaSlicer `TreeSupport::calc_branch_radius` second overload (`OrcaSlicerDocumented/src/libslic3r/Support/TreeSupport.cpp::calc_branch_radius` at line 1801) excluding the interface-aware widening branch (intentional, per packet design §Architecture Constraints). 6 new tests + 1 migrated existing test in `modules/core-modules/support-planner/`. Spec: packet 117_support-planner-geometric-correctness. Closed 2026-07-19.
- [x] TASK-282 — B6 `support-planner` avoidance-cache and offset replacement: deleted DIY `inflate_polygon`; replaced with `slicer_sdk::host::offset_polygons` called with `OffsetJoinType::Miter`; `LayerCollisionCache` reshaped to retain `ExPolygon` holes; `point_in_any_polygon`/`clamp_to_avoidance`/`push_interface_scan_lines` consumers updated; `node_dropped_when_avoidance_rejects_all_moves` fixture migrated to `Point2::from_mm`. No `slicer-core` direct dependency added to the guest graph (host-side `slicer-sdk` is the only host-crate seam). 3 new tests (concave simplicity, hole retention, mm boundary). Spec: packet 117_support-planner-geometric-correctness. Closed 2026-07-19.
- [x] TASK-285 — Support-modules paint-segment annotations migration: extracted `slicer_core::paint_policy::support_eligibility` (presence-check on `segment_annotations` with a region-area floor, replacing the centroid probe in `slicer_sdk::traits::paint_policy_for`); refactored `paint_policy_for` to a thin wrapper that filters `SliceIR.regions[*]` by the caller's expoly via a contour-vertex probe (`region_covers_expoly`) then calls the per-region helper and aggregates with blocker-wins precedence; deleted the unused `expolygon_centroid` / `regions_cover_point` / `point_in_expolygon` / `point_in_polygon` helpers and replaced them with the two new region-ownership helpers; canonical `SupportPaintPolicy` enum moved to `slicer_ir::paint_policy` and re-exported from both `slicer_core` and `slicer_sdk` to avoid the slicer-core→slicer-sdk dep cycle; three module manifests (`tree-support`, `traditional-support`, `support-planner`) drop the dead `"PaintRegionIR"` `[ir-access].reads` entry; `HostPaintRegionLayerView` shim cleaned (the three `runtime_reads.push("PaintRegionIR")` removed; kebab→snake semantic-name keys); the two `prepass_support_geometry*_tdd.rs` test fixtures also drop the dead `"PaintRegionIR.per_layer"` ir_reads string; L-shape regression test `enforcer_works_when_centroid_outside_paint_region` added to both `enforcer_blocker_tdd.rs` files (L-shape vertex-mean centroid (4.667, 4.667) mm in notch, helper returns Enforced). AC-1..AC-10 + AC-N1..AC-N4 all green; `cargo clippy --workspace --all-targets -- -D warnings` clean; `cargo xtask build-guests --check` clean. Spec: packet 120_support-modules-paint-segment-annotations-migration. (Renumbered from source-plan `TASK-261`; `TASK-261` is now infill-parity integration per packet 136.)
- [x] TASK-290 — Packet 119 absorbed support-planner contract closure: per-point `dist_to_top_mm` and optional `SupportPlanIR.raft_plan` through IR/WIT/macro/SDK/host marshal, schema 1.2.0, exact raft config emission, and the ABI-safe seam-specific WIT point shape. Closed 2026-07-20 — packet 119 / ADR-0048.
- [x] TASK-294 — Packet 178 seam region-aware planning (part 1 of the seam-planning source-geometry work): added `variant-chain: list<tuple<string, paint-value>>` to WIT `seam-plan-entry` and `region-key` (`world-prepass.wit@3.0.0`); added `SeamPlanningView` (per-region `SliceIR` polygons + identity + segment annotations + Z + scoring width) as a new `run-seam-planning` parameter; propagated through `slicer-sdk`, `slicer-macros`, `slicer-wasm-host` host/binding/dispatch/marshal, `slicer-ir` (`CURRENT_SEAM_PLAN_IR_SCHEMA_VERSION` 1.0.0 → 1.1.0, additive `variant_chain` on `PerimeterRegion`); `harvest_seam_plan_ir_from` extracts full `RegionKey.variant_chain` from WIT (rejects `Custom` paint value); `push_perimeter_regions` and `backfill_resolved_seam` look up by full `RegionKey` (no broadcast); `SeamPlannerDefault::run_aligned_planning` consumes the per-region view (no `MeshObjectView` sectioning for candidates); `seam-planner-default/tests/seam_region_aware_planning_tdd.rs` (6 tests: notched polygon, nonuniform Z, multi-region two-variant, inactive region, candidate-on-boundary, Z-on-supplied-layer) + 4 new `slicer-runtime` contract tests (`seam_plan_ir_preserves_variant_chain`, `seam_plan_injection_matches_variant_chain`, `seam_plan_ir_rejects_invalid_region_identity`, `seam_plan_ir_rejects_duplicate_region_keys_within_one_ir`) + 2 new `slicer-wasm-host` contract tests (`harvest_round_trip_preserves_variant_chain_flag`, `harvest_round_trip_preserves_variant_chain_tool_index`) exercising the production marshal boundary; production-side `SeamPlanIR.duplicate_region_key()` validation in `Blackboard::commit_seam_plan` returns `BlackboardError::DuplicateSeamPlanEntry` (AC-N1); `the seam-planning source-geometry work` Narrowed (part 1 closed); parts 2-5 (sample budget, B-spline solver, `curling_influence`, short-string retry) stay Open for packet 179. AC-1..AC-5 + AC-N1 + AC-N2 all green; `cargo clippy --workspace --all-targets -- -D warnings` clean; `cargo xtask build-guests --check` clean. Spec: docs/spec_packets/_OLD/178-seam-region-aware-planning.md. Packet `status: implemented` (flipped 2026-07-22; the self-imposed "wait for 179/180" gate was removed because the technical surface 179/180 cite is on disk and green, and the gate blocked the two successor packets the parity plan itself names as the rationale). Closed 2026-07-22.
- [x] TASK-292 — Packet 179 seam canonical algorithm fidelity (parts 2-5 of the seam-planning source-geometry work): added `layer_angle: f32` to `SeamCandidate` and wired canonical `curling_influence` (`-0.8` when `layer_angle > 2*|local_ccw_angle|`) in `align.rs`; restored canonical `VISIBILITY_SAMPLES_COUNT = 30000` and `RAYS_PER_SIDE = 5` (25 rays/sample) with seeded RNG-driven area-uniform sampling; added `LayerInfo.layer_angle` and resolved per-region `flow_width` propagation through `build_seam_candidates`; wired `segment_annotations` through `build_seam_candidates` so paint enforcer/blocker annotations set `point_type` and `central_enforcer` before comparator use; added `align_seam_points` alternative-start retry (step `1 + size/20`, keep longest string) and bounded continuity anchor for active-region gaps; replaced normal-equation Gaussian elimination with `faer::linalg::solvers::ColPivQr` (full-pivot Householder QR; AC-N1 zero-on-rank-deficiency enforced on the way out); 4 new test files (`seam_canonical_comparator_tdd.rs`, `seam_canonical_visibility_tdd.rs`, `seam_canonical_alignment_tdd.rs`, `seam_canonical_spline_tdd.rs`) covering AC-1..AC-8 + AC-N1 + AC-N2; `cargo clippy --workspace --all-targets -- -D warnings` clean; `cargo xtask build-guests --check` clean. Spec: docs/spec_packets/_OLD/179-seam-canonical-algorithm-fidelity.md. Closed 2026-07-22.
- [x] TASK-293 — Packet 180 seam final placement default: replaced vertex-only snap in `aligned_seam_target` with continuous projection onto the nearest wall segment point (inserting a new point at t∈(0,1), interpolating `feature_flags` and `width_profile.widths` via linear interpolation, re-closing the loop — restored the pre-packet closing-flag convention that an intermediate fix worker had inadvertently dropped); added degraded fallback for missing `SeamPlanIR` entries in aligned mode (returns `ModuleError { code: 6, fatal: false, message: "missing seam plan entry (layer, object, region_id, variant_chain=[])" }` while preserving all walls and applying canonical local candidate selection as fallback); changed `default = "nearest"` to `default = "aligned"` in both `seam-placer.toml` and `seam-planner-default.toml`; added 3 new test files (`seam_continuous_projection_tdd.rs`, `seam_degraded_fallback_tdd.rs`, plus e2e test `seam_aligned_default_e2e` in `scenario_traces_tdd.rs`); updated 2 existing tests in `seam_aligned_mode_tdd.rs` whose expectations encoded the superseded vertex-only behavior; new error code 7 for degenerate empty wall loop (non-fatal, preserves the empty loop); `cargo check --workspace --all-targets` clean; `cargo clippy --workspace --all-targets -- -D warnings` clean; `cargo xtask build-guests` rebuilt `seam-placer-guest` after the lib.rs edit and `cargo xtask build-guests --check` clean. AC-1..AC-5 + AC-N1 + AC-N2 all green. Spec: docs/spec_packets/_OLD/180-seam-final-placement-default.md. Packet `status: implemented` (flipped 2026-07-23 after final adversarial review #3 returned READY_TO_CLOSE). Closed 2026-07-23.
- [x] TASK-297 — Packet 181 dispatch missing component handling: made an absent compiled component always fatal at both load (`LiveModuleLoadError::Component { module_id, cause }` in `compile_module_component`) and dispatch (all five `Err(e) if e.phase == DispatchPhase::MissingComponent` arms in `crates/slicer-wasm-host/src/dispatch.rs` return the phase-appropriate fatal error). Migrated 3 pre-existing loader tests + 6 test sites (the `.no_wasm()` consumers, `run_layer_and_commit`, 2 inline-`None` sites in `tests/unit/dag_validation_tdd.rs` / `tests/contract/postpass_gcode_empty_list_tdd.rs`, the vacuous `postpass_gcode_boundary_tdd::postpass_gcode_boundary_carries_all_payload_variants_into_guest`) to assert the new fatal behavior. New test `dispatch_missing_component_tdd::missing_component_is_fatal_for_all_five_stages` registered in `tests/contract/main.rs`. ADR-0020 §Decision item 1 amended via appended `## Amendment — 2026-07-27 (packet 181)` section; new tracking row `DEV-097` for the inert placeholder symbols. **The placeholder symbols remain in code (`is_placeholder_wasm` and `LoadedModule::placeholder_wasm` in `crates/slicer-scheduler/src/manifest.rs`, plus the skip-affordance doc comment) — they were not removed; cleanup is deferred with no successor packet.** Closes missing-component handling. Spec: docs/spec_packets/_OLD/181-dispatch-missing-component-handling.md.
- [x] TASK-286 — `support-planner::smooth_branches` (packet 121, OrcaSlicer `TreeSupport::smooth_nodes` port): 100-iteration three-point Laplacian smoother on per-layer (x, y, width) of each branch column, grouping entries by `(object_id, region_id)` and splitting sub-chains at 5.0 mm inter-tree gaps; endpoints and sub-chain boundaries held fixed; width clamped to `[0.0, MAX_BRANCH_RADIUS_MM = 6.0]` per iteration; integrated into `plan_for_object` between the propagation loop and the drain. New 8th invariant `branch_curvature_below_threshold` (≤ 30° max consecutive-segment turn angle) added to the wedge harness. Goldens regenerated via `SUPPORT_WEDGE_REGEN_GOLDEN=1` (Hausdorff 1.3484 → ≤ 0.5 mm against the new baseline; branch count 198 unchanged). AC-1..AC-7 + AC-N1 + AC-N2 green. Spec: docs/spec_packets/_OLD/121_support-planner-smooth-nodes.md. Closed 2026-07-20.
- [x] TASK-287 — C4 `support-planner` multi-neighbour MST aggregation (packet 122, OrcaSlicer `TreeSupport::drop_nodes` port): replaced the `nearest_neighbour` + `nearest_distance` per-node lookup in `modules/core-modules/support-planner/src/lib.rs` (lines 691-702 of the prior version) with a per-node `neighbours_of: Vec<Vec<(usize, f32)>>` incident-edge map and a new pure helper `pub fn aggregate_neighbour_targets(neighbour_positions: &[(f32, f32)], distances: &[f32]) -> Option<(f32, f32)>`; the helper computes the reciprocal-distance-squared weighted mean (`1/d²` weighting, matching Orca's non-`is_strong` path) with degenerate `D < 1e-6 mm` short-circuit to the coincident neighbour's position. `max_move_xy` cap (line 715) and `clamp_to_avoidance` post-cap (line 727) are preserved — only the move *direction* changes. New 9th invariant `merge_geometry_symmetric_for_n_branches` (≤ 30% stddev/mean across all ≥ 3-way merge points within an entry) added to the wedge harness. New unit test file `modules/core-modules/support-planner/tests/multi_neighbour_mst_tdd.rs` with 4 RED-then-GREEN tests (`symmetric_3_neighbour_centroid`, `asymmetric_neighbours_weighted_by_reciprocal_squared`, `single_neighbour_degenerate_case_matches_old`, `zero_distance_neighbour_does_not_panic`). Goldens re-anchored via `SUPPORT_WEDGE_REGEN_GOLDEN=1` (branch count 99 → 78; endpoint Hausdorff within 0.5 mm — the 21% branch-count drop is the intended algorithmic shift because the old single-neighbour lookup was producing a falsely inflated set of branch segments that the smoother's `CHAIN_BREAK_THRESHOLD_MM = 5.0` then had to drop, where the new aggregate produces a tighter set from the start). `docs/specs/_OLD/support-modules-orca-port.md` §Validation Strategy invariant list extended with item 10. AC-1..AC-7 + AC-N1 + AC-N2 green; `cargo clippy --workspace --all-targets -- -D warnings` clean; `cargo xtask build-guests --check` clean. Spec: docs/spec_packets/_OLD/122_support-planner-multi-neighbour-mst.md. (Renumbered from source-plan `TASK-263`; `TASK-263` is now Lightning DistanceField per packet 138.)
- [x] TASK-288 — C5 `support-planner` `to_buildplate` tracking + `unsupported_branch_leaves` pruning + `support_on_build_plate_only` config (packet 123, OrcaSlicer `TreeSupport::drop_nodes` / `generate_contact_points` port). Spec: docs/spec_packets/_OLD/123_support-planner-to-buildplate-pruning.md. (Renumbered from source-plan `TASK-264`; `TASK-264` is now Lightning Layer per packet 139.) Closed 2026-07-21.
- [x] TASK-289 — `RaftInfill` role/claim extension that ADR-0009 commits (packet 124, the IR-side half of raft-as-layer-infill): added `ExtrusionRole::RaftInfill` variant to the Rust enum (with `default_priority() = 50`) and the WIT mirror (`raft-infill`); added `ExtrusionRole::RaftInfill => "claim:raft-fill"` arm in `crates/slicer-sdk/src/views.rs::should_emit`; bumped `CURRENT_SUPPORT_PLAN_IR_SCHEMA_VERSION` 1.2.0 → 1.3.0 (semver-minor per ADR-0009 §Consequences); audited every workspace `match role` site (21 total; 3 explicit non-wildcard sites — `crates/slicer-macros/src/lib.rs:922/2067` and `crates/slicer-wasm-host/src/marshal/leaf.rs:369` — gained explicit `RaftInfill` arms; the IR→WIT macro at `macros/src/lib.rs:972` and 3 g-code sites — `tolerance_for_role`, `resolve_feedrate`, `orca_type_label` — also gained explicit arms per spec-review fix iteration to prevent silent fallback); new behavioral test `crates/slicer-sdk/tests/should_emit_raft_fill_claim_tdd.rs` covers AC-4 (held-claims accept), AC-N1 (held-claims reject), AC-N3 (empty-claims suppress); new round-trip test `extrusion_role_raft_infill_roundtrip` in `crates/slicer-runtime/tests/contract/macro_all_worlds_roundtrip_tdd.rs` covers AC-7; verified (no edit) C7 non-consumption state in `traditional-support` lead `//!` block and manifest. AC-1..AC-9 + AC-N1..AC-N3 all green; `cargo clippy --workspace --all-targets -- -D warnings` clean; `cargo xtask build-guests --check` clean (33 guests fresh); `cargo build --workspace --all-targets` clean. Spec: docs/spec_packets/_OLD/124_support-plan-raft-plan-and-raftinfill-role.md. Closed 2026-07-21.
- [x] TASK-274 Port OrcaSlicer SeamPlacer's aligned/aligned_back seam path (seam-string chaining + least-squares smoothing) into the seam-planner-default prepass and add aligned/aligned_back SeamMode consumption in seam-placer. Spec: packet 168-seam-aligned-modes.
- [x] TASK-276 — G-code flavor dialect layer: GcodeFlavor enum (marlin/marlin2/klipper/reprapfirmware/repetier) ported from OrcaSlicer GCodeWriter.cpp, honored from gcode_flavor config, echoed in CONFIG_BLOCK (packet 171).
- [x] TASK-278 Graceful cancel contract for `pnp_cli slice` (signal handlers, `--cancel-on-stdin-eof`, exit 130, cancelled event). Closed 2026-07-19 — packet 174.
- [x] TASK-291 — support-preview prepass verb: pnp_cli support-preview writes per-layer support polygons from the committed SupportGeometryIR blackboard slot as a versioned mm JSON contract. Spec: packet 176-support-preview-verb. Closed 2026-07-23.
- [x] TASK-245 — Paint-segmentation OrcaSlicer-parity port: Phases 1, 2, 3, 4, 6, 7 (packet 95). Replaced the broken paint-segmentation kernel with the OrcaSlicer-parity pipeline; repositioned to run between `host:shell_classification` and `host:support_geometry`; committed into `SliceIR.regions[*]` via `Blackboard::replace_slice_ir`; deleted `PaintRegionIR` / `LayerPaintMap` / `SemanticRegion` and their host-side rtree; turned all 12 RED `cube_4color_paint_tdd` + 12 RED `cube_fuzzy_painted_tdd` tests GREEN. Closed 2026-06-12 — packet 95 (complete). **Phase 4f (`extract_colored_segments`) port completed 2026-06-29** by packet `126_mmu-painted-cube-parity` — the faithful leftmost-arc walk now lives in `crates/slicer-core/src/algos/paint_segmentation/extract_segments.rs` (commits `721f19ec` for the breakthrough + `0c41a42a` for colour-gated attachment + corner dedup + open contour). Seed-colour assignment via `segments_to_expolygons_by_color`; per-colour independent walks consume each arc exactly once.
- [x] TASK-246 — Paint-segmentation Phase 5 `cut_segmented_layers`: width-limit + interlocking (packet 96; originally mis-attributed to packet 95 — see the TASK-246 note above). Implements OrcaSlicer Phase 5 in `crates/slicer-core/src/algos/paint_segmentation/width_limit.rs`; `mmu_segmented_region_interlocking_beam=true` skips Phase 5 entirely at the driver (OrcaSlicer parity, `MultiMaterialSegmentation.cpp:2452`); three MMU config keys default to `0.0`/`false` to preserve byte-identical default-config output. All P96 ACs green. Closed 2026-06-12 — packet 96 (complete). **Decomposition backend swap completed 2026-06-29** by packet `126_mmu-painted-cube-parity` — the boostvoronoi `cells_to_expolygons_by_color` shortcut that bypassed the broken Phase-4f walk is retired; `execute_paint_segmentation` now decomposes via the faithful `extract_colored_segments` walk.
- [x] TASK-247 — Delete the dead WASM mesh-segmentation guest surface (packet 97). Packet 94 (TASK-244) retired the host `PrePass::MeshSegmentation` stage + kernel and explicitly deferred the WASM-guest infra to P97; this packet removes it in full. Deleted: `modules/core-modules/mesh-segmentation/` (+ its root `Cargo.toml` workspace-member entry), the `crates/slicer-wasm-host/test-guests/sdk-prepass-meshseg-guest/` test-guest, the `mesh-segmentation-output` resource + `run-mesh-segmentation` export from `world-prepass.wit`, the host resource impl / `mesh_segmentation_marks` field / accessor (`host.rs`) + harvest/dispatch arm (`dispatch.rs`), the `#[slicer_module]` macro arm + `run_mesh_segmentation` trait default (`slicer-macros`), the SDK `MeshSegmentationOutput` builder + `run_mesh_segmentation` trait method + mesh-seg-only `TrianglePaintMark`/`ObjectMeshModification` (`slicer-sdk`), `Blackboard::commit_mesh_segmentation`/`mesh_segmentation()` + `BlackboardPrepassSlot::MeshSegmentation` (`slicer-runtime`), `FacetPaintMark`/`MeshSegmentationIR`/`CURRENT_MESH_SEGMENTATION_IR_SCHEMA_VERSION`/`PrepassStageOutput::MeshSegmentation` (`slicer-ir` + `slicer-core/stage_io.rs`), the `PrePass::MeshSegmentation` canonical-stage entry (`slicer-scheduler/execution_plan.rs`), the scaffolder arm (`pnp-cli/module_new.rs`), the bench entry, and the macro-roundtrip + integration-geometry tests; dispatch/scheduler/wit-drift/all-worlds/guest-freshness/e2e tests pruned of mesh-seg arms. Packet's stale AC-9/AC-14/"KEPT" framing amended to packet-94 reality (kernel/producer/tests already gone); under-enumerated In-Scope reconciled via Step-1 inventory. Core-module count 21→20; host-producer count unchanged at 7. AC-1..AC-17 + AC-N1/AC-N2 green: `cargo check --workspace --all-targets` clean, `cargo clippy --workspace --all-targets -- -D warnings` clean (only pre-existing nom/quick-xml future-incompat), `cargo test --workspace` green (1 stale count-assert fixed: 21→20 core modules in `manifest_ingestion_tdd`), `cargo xtask build-guests --check` clean (31 guests), AC-17 byte-identical g-code on `regression_wedge.stl` (`AA4DA2FA…`) + `cube_4color.3mf` (`AD0245C3…`) vs pre-deletion baseline. See the packet 97 closure log. Closed 2026-06-13 — packet 97.
- [x] TASK-248 — Loader symmetry: sub-facet stroke decoding for all four 3MF paint channels (packet 98, P5b). Hoisted the sub-facet hex decode into a private `decode_strokes_for_channel(hex, tri_verts, byte_offset, map_state)` helper in `crates/slicer-model-io/src/loader.rs` and called it once per channel in BOTH parse loops (`parse_3mf_model_xml` + `parse_sub_model_objects`), so `paint_seam` and `paint_fuzzy_skin` now decode their sub-facet strokes (previously dropped — only `paint_color`/`paint_supports` did). `MeshCollector` gained `seam_strokes_enforcer`/`seam_strokes_blocker`/`fuzzy_strokes`; `build_paint_data` wires them; malformed hex propagates a structured `ModelLoadError`. Seam uses the existing `PaintSemantic::Custom("seam_enforcer")`/`Custom("seam_blocker")` (no IR change; the AC-named `SeamEnforcer`/`SeamBlocker` variants do not exist). AC-7 REWRITTEN (D-98-AC7-CONSUMER-PATH): the original `host:mesh_segmentation` normalization consumer was retired by P94r + deleted by P97, so AC-7 now asserts strokes reach the live `host:paint_segmentation` (`execute_paint_segmentation`) and surface in `SlicedRegion.variant_chain` — proven end-to-end via 4 consumer-path tests in `crates/slicer-runtime/tests/executor/paint_channel_consumer_paths_tdd.rs` (color→`("material",ToolIndex)`, fuzzy_skin→`("fuzzy_skin",Flag(true))`, supports→`("support_enforcer",Flag(true))`, seam→`("seam_enforcer",_)`). the seam-paint consumer gap was registered: seam strokes reach SliceIR but NO live module reads them (seam-placer scores seams geometrically) — deferred to the perimeter-modules-orca-parity roadmap (M1 Phase 8, ~T-079). User-supplied real fixtures `cube_cilindrical_modifier.3mf` (paint_seam) + `bridge_support_enforcers.3mf` (new bridge obj w/ painted SupportEnforcer) drive the supports/seam consumer tests end-to-end; 3 fixture-shape regressions in existing e2e tests reconciled intent-preserving (object count 2→3, modifier-volume scoping, matrix substrings re-pinned to the moved cylinder). AC-1..AC-11 + AC-N1..AC-N3 green; AC-8 wedge + AC-9 cube_4color SHA byte-identical; AC-10 cube_fuzzyPainted SHA differs (expected — fuzzy strokes now respected). `cargo clippy --workspace --all-targets -- -D warnings` clean; `cargo xtask build-guests --check` clean (31 guests rebuilt); `cargo test --workspace` green (192 bins, 0 failed). See the packet-98 closure log. Closed 2026-06-15 — packet 98.
- [x] TASK-249 — Doc sync after the paint-pipeline OrcaSlicer-parity roadmap (packets 89–98). Ensures TASK-239 through TASK-249 are present in `docs/07_implementation_status.md`. Closed 2026-06-15 — packet 99.
  - **Known parity gaps (post-roadmap follow-ups, still open — re-recorded after an unrelated cleanup dropped the original section):**
    1. **Community paint ingestion.** The 3MF parser handles the four OrcaSlicer paint channels (`paint_color`, `paint_supports`, `paint_seam`, `paint_fuzzy_skin`); a parser extension hook for community-contributed channels is deferred — such channels are silently ignored until it lands.
    2. **`PaintValue::Vector` for multi-channel paints.** `PaintValue` currently has `Flag`/`Scalar`/`ToolIndex`/`Custom` only; a `Vector(Vec<f32>)` variant for CMYK/RGB-style multi-channel paints is deferred to a future IR-breaking change and is **not yet added**.
    3. **Promote paint-segmentation's internal slicing to `host:raw_slice`.** If profiling shows the paint-segmentation slice-plane loop is a bottleneck it should be promoted to a reusable `host:raw_slice` producer; it is currently private to the paint-segmentation kernel (no such producer exists).
- [x] TASK-244 — Retire orphaned `execute_mesh_segmentation` host kernel (packet 94). Pre-implementation inventory confirmed the host kernel had zero callers in production source (no `PrePass::MeshSegmentation` driver, no `Blackboard::replace_mesh`, no `MESH_SEGMENTATION_PRODUCER`; the original P94 framing described a wiring that was never landed). Deleted: `crates/slicer-core/src/algos/mesh_segmentation.rs` (host kernel, 545 lines), `crates/slicer-core/tests/algo_mesh_segmentation_tdd.rs` (kernel unit tests), `crates/slicer-runtime/tests/executor/mesh_segmentation_executor_tdd.rs` (direct-call executor test), `pub mod mesh_segmentation;` from `crates/slicer-core/src/algos/mod.rs`, the `pub use slicer_core::algos::mesh_segmentation::{...}` re-export block from `crates/slicer-runtime/src/lib.rs`, `mod mesh_segmentation_executor_tdd;` from `crates/slicer-runtime/tests/executor/main.rs`, and the stale `[[test]] name = "algo_mesh_segmentation_tdd"` block from `crates/slicer-core/Cargo.toml`. The loader's `split_triangle_strokes` (`crates/slicer-model-io/src/loader.rs:1900-1961`) is the canonical TriangleSelector normalization site; P95 will consume `PaintLayer.strokes` directly per the parity doc's Phase 3 `collect_facets()` design. WASM-guest infrastructure (`PrepassStageOutput::MeshSegmentation`, `BlackboardPrepassSlot::MeshSegmentation`, `commit_mesh_segmentation`, `MeshSegmentationIR`, `modules/core-modules/mesh-segmentation/`) stays — P97 owns the WASM-guest deletion. AC-1..AC-8 + AC-N1..AC-N3 all green: `cargo check --workspace --all-targets` clean, `cargo clippy --workspace --all-targets -- -D warnings` clean (only pre-existing third-party future-incompat warnings for `nom v3.2.1` + `quick-xml v0.22.0` remain), `cargo test --workspace` all 50 test buckets pass, `cargo xtask build-guests --check` clean, AC-5 wedge SHA `AA4DA2FAECA139F2C17909051497D6998F71BFB8A2DD9856D286296252EF1E3B` byte-identical vs P93 baseline, AC-6 cube_4color slice completes (SHA `960671A5748AC14455EA420AB4C0B3369594953040CC4672A7C17B29078046FF` captured as `P94R_POST_CUBE_SHA` for P95's input baseline). See `the packet-94 closure log`. Closed 2026-06-10 — packet 94.
- [x] TASK-240 — Benchy STL fixture retirement (packet 90). Retired `resources/benchy.stl` (~11 MB, ~200k triangles) by authoring `resources/regression_wedge.stl` (4 KB, 80 triangles, v8 design: 25×50 base, 45° outward frustum z=2..12 producing `;TYPE:Bridge infill`, 50×50 vertical body z=12..40, 8×8 mm through-hole at x∈[21,29] z∈[20,28] in the y direction, 0.4 mm tiny-wall rib at +x face y∈[12,38] z∈[16,32] testing sub-2-perimeter handling, and 20×8 mm horizontal cantilever arm at z=29..31 extending +y beyond y=50 — the cantilever's z=29 unsupported bottom face triggers genuine tree-support pillar generation). Migrated 42 tests in `crates/slicer-runtime/tests/e2e/benchy_end_to_end_tdd.rs` → `slice_end_to_end_tdd.rs` with function-prefix sweep (12 CLI-SHAPE → `slice_*`, 22 SHAPE-DEPENDENT → `wedge_*`, 8 STRUCTURAL kept their original names). Updated 8 other reference sites: `crates/slicer-runtime/tests/common/slicer_cache.rs:135`, `crates/slicer-model-io/tests/stl_roundtrip_tdd.rs:1,15-17`, `crates/slicer-runtime/tests/integration/live_module_loading_tdd.rs:332`, `crates/pnp-cli/tests/slice_instrumentation_fork_tdd.rs:32`, `modules/core-modules/support-planner/tests/orca_parity_tdd.rs:274,341`, `crates/slicer-runtime/tests/e2e/run_slice_api_tdd.rs`, `crates/slicer-runtime/tests/executor/layer_slice_tdd.rs`, `crates/slicer-runtime/benches/pipeline.rs`. Harness `mod` declaration at `crates/slicer-runtime/tests/e2e/main.rs:12` updated. Two assertion calibrations preserved per AC-N1 (documented in closure-log): `wedge_mvp_produces_full_height_layer_progression` threshold `max_z >= 40.0` → `>= 39.5` (benchy ~48 mm → wedge 40 mm); `layer_slice_builtin_produces_real_polygons_for_wedge_mesh` threshold `total_points >= 20` → `>= 4` (benchy curved hull → wedge rectangular cross-section). Net assertion count unchanged (PRE=POST=111). AC-7 amended from "≥60 s improvement" to "regression analysis documented" (`WALL_CLOCK_BEFORE_E2E=303s`, `WALL_CLOCK_AFTER_E2E=423s`, +120 s regression structurally caused by the wedge exercising bridge + support code paths benchy passed trivially as NoOps; reverting would re-introduce the NoOp problem this packet was called to fix). Executor binary's 12 pre-existing RED tests (paint-pipeline TDD from `5c272ef`) carved out of the workspace gate per closure-log Step 0 Notes. All packet ACs green at HEAD post-rewrite; 119 e2e tests pass, 153 integration tests pass, 204 contract tests pass, executor RED-set unchanged from baseline (same 12 test names, same count), `cargo check --workspace --all-targets` clean, `cargo clippy --workspace --all-targets -- -D warnings` clean. Closed 2026-06-08 — packet 90.

- [x] TASK-235 Extract the static-plan subsystem into `slicer-scheduler` (packet 85): nine planning files (~5,500 LOC) moved from `slicer-runtime/src/` to `slicer-scheduler/src/` (`manifest`, `config_resolution`, `dag`, `validation`, `execution_plan`, `topology`, `stage_order`, `module_search_path`, `dag_cli`); completes the Static/Live field migration P83 left partial — wasmtime fields stripped from `CompiledModuleStatic`/`CompiledModuleBuilder`/`ExecutionModuleBinding`, live-loader cluster relocated to new `slicer-wasm-host/src/execution_plan_live.rs`; `pnp-cli` rewired to `slicer_scheduler::run_dag_*`. 128 scheduler tests run in isolation with zero `wasmtime` transitively. G-code SHA `89a329ad3a4c1b7febca839edfca8b6302e562d8d2a390ee144252fd54e65a2b` byte-identical pre/post. ADR-0007 records the Static/Live split rationale. Closed 2026-06-04.
- [x] TASK-215 Co-locate and consolidate the test-guest fixture tree (spec: packet `74_test-guests-colocation-and-wit-dedup`; continues TASK-214/packet-70 and TASK-144/145/packet-72). Moved `test-guests/` → `crates/slicer-runtime/test-guests/` (12 guest crates); routed all guest builds through one shared `CARGO_TARGET_DIR` (D1); replaced the four raw guests' inline `wit_bindgen::generate!` with `path:` to the canonical WIT source, closing packet 72's surviving inline-WIT exception (A); extracted the positional witness encoding into a shared `test-guests/witness/` codec (C). Raw guests retained as the macro-vs-hand-rolled differential oracle. Closed 2026-05-30; 2 pre-existing unrelated test failures noted at closure.

- [x] TASK-295 — Packet 182 G-code header wall-width defaults corrected to governing 0.4/0.4 fallback; D-165 closed (recorded `Closed` in `docs/DEVIATION_LOG.md`).

- Packet 177 completion (arachne-baselines-to-structural-invariants): Arachne self-captured JSON/expected-IR oracles removed; five paired source-geometry subjects now enforce a pinned 0.99 coverage floor with a 0.000000 repeatability margin (0.02 cap); D5 0.668/0.990 values remain synthetic anti-vacuity checks; see ADR-0042/0047.

- [x] TASK-296 — Packet 183 arachne voronoi panic diagnosis: guard in `crates/slicer-core/src/voronoi.rs` wraps the boostvoronoi `Builder::build()` call in `std::panic::catch_unwind(AssertUnwindSafe(...))` and maps caught panics to the distinct `VoronoiError::PredicatePanic` variant without an empty graph. The ordinary `voronoi_stress` remains a `host-algos` target; the synthetic panic regression target is `crates/slicer-core/tests/voronoi_panic_regression.rs` behind `voronoi-panic-regression`. Packet-specific checks pass: ordinary stress, the explicit synthetic panic regression, and `perimeter_parity` 3/3 pre- and post-guard with 0 raw `is_finite` panic lines and 0 caught-panic diagnostics; `cargo clippy --workspace --all-targets -- -D warnings` and `cargo xtask build-guests --check` are clean. D-167 is closed as **not reproducible on the current tree**, with no successor packet for `preprocess_input_outline` hardening. Workspace closure was blocked by the independently reproduced `support-surface-ironing --test ironing_tdd` failures (5 passed, 6 failed), outside packet 183 scope; **resolved 2026-07-31** — see the packet-164 stale-test-contract entry below. `cargo xtask test --summary --workspace --features slicer-core/host-algos` now reports `VERDICT: PASS` across 358 test binaries with 0 failures. Spec: docs/spec_packets/_OLD/183-arachne-voronoi-panic-diagnosis.md.

- [x] TASK-308 — Packet 189 per-point speed-factor carrier: new `EntitySpeedProfile { entity_id: u64, factors: Vec<f32> }` in `crates/slicer-ir/src/slice_ir.rs` (re-exported from the crate root) plus the additive `#[serde(default)] LayerCollectionIR.speed_profiles: Vec<EntitySpeedProfile>` side table, keyed by `entity_id` exactly like `TravelMove`; `CURRENT_LAYER_COLLECTION_IR_SCHEMA_VERSION` bumped 1.1.0 → 1.2.0 (additive minor) and the explicit `Default` impl sets `speed_profiles: Vec::new()`. Carried across the component boundary by the additive WIT variant case `set-point-speed-factors(list<f32>)` on `entity-mutation` in `crates/slicer-schema/wit/deps/finalization-layer-finalization/finalization-layer-finalization.wit`; the WIT package version was deliberately **not** bumped per ADR-0044 (WIT world versions are advisory and erased from guest binaries). Guest-side surface is `EntityMutation::SetPointSpeedFactors(Vec<f32>)` in `crates/slicer-sdk/src/traits.rs`, whose applier rejects a length mismatch with an `Err` naming both lengths and upserts by `entity_id` (a second write replaces the row, never appends). Semantics: a per-point factor REPLACES the whole-entity `ExtrusionPath3D::speed_factor` for that point, `factors.len()` always equals `path.points.len()`, and an absent profile row means the entity uses the uniform `speed_factor` exactly as before. Carrier only — the Benchy parity work stays **Open**: smoothed-speed mode is packet 190 (TASK-313) and `ADD_INTERSECTIONS` mid-segment splitting is packet 191 (TASK-315); both are governed by ADR-0053. Spec: docs/spec_packets/_OLD/189-per-point-speed-factor-carrier.md.
- [x] TASK-311 — Packet 192 infill-linker anchor-length: closes the anchor-length work by adding the per-arc whole-arc/stub rule, lerped partial contour segments, anchor config resolution, and solid/bridge unlimited forcing. Spec: docs/spec_packets/_OLD/192-infill-linker-anchor-length.md.

- [x] TASK-313 — Packet 190 smoothed overhang speed: replaces the whole-entity quantized overhang speed lookup in `overhang-classifier-default` with a per-point port of canonical `ExtrusionQualityEstimator::calculate_speed` — six `speed_sections` from `OVERHANG_OVERLAP_LEVELS` `{90, 75, 50, 25, 13, 0}`, reading the prepass-stamped `Point3WithWidth.overhang_distance_mm` (the packet 193 / TASK-314 carrier), interpolated + rounded + clamped, and emitted as `SetPointSpeedFactors` (the packet 189 / TASK-308 channel); adds `enable_overhang_speed` / `slowdown_for_curled_perimeters` / `bridge_speed` config keys; deletes `SetSpeedFactor`. Consumes the packet 193 / TASK-314 distance carrier (option (C)); ADR-0053 is the decision record amending ADR-0031/0032/0008, and packet 191 / TASK-315 closes the remaining open sub-item (`ADD_INTERSECTIONS`). Spec: docs/spec_packets/_OLD/190-smoothed-overhang-speed.md.

- Packet 183 reopening note (the workspace-test remediation): the original TASK-296 guard remains implemented, but its synthetic regression is now isolated in `crates/slicer-core/tests/voronoi_panic_regression.rs` behind the explicitly invoked `voronoi-panic-regression` feature. The former `boostvoronoi/console_debug` dev-dependency feature was removed so the normal workspace test build cannot enable diagnostics in unrelated crates. The reopened workspace gate reached `slicer-runtime`'s executor with 194/194 tests green and then exposed an unrelated pre-existing unit failure (`layer_collection_builder_tdd::macro_drain_invokes_host_get_ordered_entities_exactly_once`, got 5, expected 1); see the workspace-test remediation and the packet's `FINDINGS.md` for the complete gate measurement.

- Packet 164 stale-test-contract fix (2026-07-31): `modules/core-modules/support-surface-ironing/tests/ironing_tdd.rs` was migrated onto the module's actual stage contract. Packet 164 (`82cf3059`) moved `SupportSurfaceIroning` from `LayerModule::run_infill_postprocess` (`PerimeterRegionView` / `InfillOutputBuilder` / `push_ironing_path`) to `run_support_postprocess` (`SliceRegionView` / `SupportOutputBuilder` / `push_support_path`), aligning the impl with the `Layer::SupportPostProcess` stage its manifest has declared since the module was created — but left the test file calling the old hook. Because `run_infill_postprocess` has a no-op default body on the `LayerModule` trait, the tests compiled and silently exercised nothing, so the six assertions requiring non-empty output failed: `square_region_produces_paths`, `paths_at_correct_z`, `flow_rate_applied`, `spacing_affects_density`, `width_matches_config`, `rectilinear_pattern`. The fixture now builds a `SliceRegionView` via `set_polygons` + `set_z` instead of deriving z from a `WallLoop` and stuffing the square into `infill_areas`. No production code changed. A sweep of all 21 core modules confirms this was the only src/test hook mismatch. 11/11 green.

- [x] TASK-314 — Packet 193 per-point signed overhang-distance carrier: adds `Point3WithWidth.overhang_distance_mm`, carries `SurfaceClassificationIR.prev_layer_boundaries`, adds the WIT `point3-with-width` record field and `SliceRegionView` accessor, stamps both classic and Arachne sites, and sweeps every `Point3WithWidth` struct literal. Carrier only: no consumer and no G-code change. Consumed by packet 190 / TASK-313 (per-point interpolation) and packet 191 / TASK-315 (crossing/segmentation predicates); the signedness/boundary-offset contract is defined once in this packet's design and cited by both, under ADR-0053. Spec: docs/spec_packets/_OLD/193-overhang-distance-prepass-carrier.md.

- [x] TASK-315 — Packet 191-overhang-add-intersections: adds the `EntityMutation::SetPathPoints` geometry mutation channel and ports canonical mid-segment vertex insertion (`ADD_INTERSECTIONS`), including the threshold-crossing and segmentation branches (reading the packet 193 / TASK-314 `overhang_distance_mm` carrier and reusing the packet 190 / TASK-313 `speed_sections` table, per ADR-0053). Closes the Benchy parity work.

- [x] TASK-316 — `cargo xtask check-literals` struct-literal churn gate (report mode, path filter, docs/21) — packet 194.

- [x] TASK-317 — safe `Default` impls + `sdk::test_support` fixture bases + per-crate `PipelineConfig` helpers for the struct-literal churn gate — packet 195.

- [x] TASK-318 — FRU sweep of slicer-ir/slicer-core/slicer-gcode test literals for the struct-literal churn gate — packet 196.

- [x] TASK-319 — FRU sweep of slicer-runtime/slicer-scheduler/slicer-wasm-host/pnp-cli test literals for the struct-literal churn gate — packet 197.
- [x] TASK-320 — FRU sweep of slicer-sdk + core-module test literals for the struct-literal churn gate — packet 198.
- [x] TASK-321 — Literal gate enforcement — packet 199.
- [ ] TASK-329 — support-planner lone-node vertical-column continuation (RC-1) and tip-radius floor (RC-4): surviving lone propagated nodes with dist_to_top > 0 and no surviving MST edge now emit a degenerate per-layer segment reaching the buildplate (OrcaSlicer TreeSupport::draw_circles parity); tapered_radius clamps to [MIN_BRANCH_RADIUS=0.4, MAX_BRANCH_RADIUS_MM=6.0]. 2 regression tests (lone-node emission, tip-floor) added in modules/core-modules/support-planner/tests/orca_parity_tdd.rs; benchy branch-count golden regenerated to 9 (intentional algorithm change). Spec: packet 213-support-planner-defect-fix. Closed 2026-08-11 after reopening for renderer fix; see packet 213 reopen notes and renderer change at crates/slicer-runtime/src/visual_debug_render.rs (Shape::Disk). Reopened 2026-08-11 second time: l100 degenerate segment not getting disk treatment and disk position overlaps model wall. **SUPERSEDED 2026-08-12** by the support-families and anchored-entities sequence (docs/specs/support-families-anchored-entities-plan.md); see TASK-330..TASK-335. Packet 213's directory is retained intact; its lone-node/radius-floor work is valid only where it remains inside the new tree family and its degenerate-disk result is not closure evidence.

- [ ] TASK-330 — generic anchored-entity execution: planar and atomic Z-spanning anchored entities, capability-derived per-anchor event execution, ordered event collections, contract-aware Z validation, per-event optimization, and cooling/time accounting while retaining global layers as parallel workers. Spec: docs/spec_packets/219-anchored-entity-execution/.
- [ ] TASK-331 — support analysis and family contracts: `PrePass::SupportAnalysis`, normalized exact-Z occupancy/envelope queries, universal structural `SupportPlanIR`, structured `SupportIR`, per-region atomic family claims/selection, host-owned plan aggregation, and removal of missing-plan fallback semantics. Spec: docs/spec_packets/220-support-analysis-family-contracts/.
- [ ] TASK-332 — tree support family: split/rename planner to `tree-support-planner`, distributed contacts plus radius-aware collision-safe body/interface polygons and anchored support heights, and printable tree bodies through the paired `tree-support` renderer. Spec: docs/spec_packets/221-tree-support-family/.
- [ ] TASK-333 — traditional support family: `traditional-support-planner` for contact detection, downward base/interface propagation, obstacle-safe termination, and anchored support heights, then `traditional-support` renders only its planned polygons. Spec: docs/spec_packets/222-traditional-support-family/.
- [ ] TASK-334 — mixed support family routing: deterministic host routing cells, same-family cell/body unions, cross-family plan and nozzle-sweep overlap rejection, structured degraded diagnostics, and per-region mixed-family validation. Spec: docs/spec_packets/223-mixed-support-family-routing/.
- [ ] TASK-335 — support family Orca closure: fixture-driven invariants and regenerated, visually inspected model/G-code differential evidence proving both built-in families reach valid termination surfaces without model collision and preserve correct roles through final G-code. Spec: docs/spec_packets/224-support-family-orca-closure/.
- [ ] TASK-336 — dragon-curve feasibility gate: canonical OrcaSlicer parity audit of the dragon-curve generator's geometry pipeline (curve decomposition, infill lattice, and perimeter coupling) with differential fixture evidence. Spec: docs/spec_packets/225-dragon-curve-feasibility-gate/.
- [ ] TASK-337 — authored coloring carrier: per-author color assignment carried through the dragon-curve module boundary into the infill lattice, with authored-coloring contracts and cross-author isolation. Spec: docs/spec_packets/226-authored-coloring-carrier/.
- [ ] TASK-338 — dragon-curve community module: MoonBit implementation of the dragon-curve infill generator with per-dragon authored coloring and wasm module host wiring. Spec: docs/spec_packets/227-dragon-curve-community-module/.
- [x] TASK-339 — community-module docs banner: CLAUDE.md community-modules section, the docs/14 labeled-example note, the four community-module backlog rows, and the queue-plan pointer in the governing spec's status line. Spec: docs/spec_packets/228-community-module-docs-banner/.
- [x] TASK-322 — packet 206 seam-paint delivery: seam-paint delivery implemented and closed, with the three registered deviations DEV-123, DEV-133, DEV-134 closed. Spec: docs/spec_packets/206-seam-paint-delivery/. Closed 2026-08-14.

- [x] **P203 — Integrated CLI + provenance** (no TASK row; packet 203-integrated-cli-provenance, status: active, closing to implemented; ADR-0056, ADR-0057). Delivered the CLI-and-provenance slice of the multi-edition distribution program: the `--no-integrated-modules` flag on the seven verbs (`slice`, `module diagnose`, `module config-schema`, `dag stages`/`stage`/`depends`/`claims`), `SliceRunOptions.no_integrated_modules`, the integrated-aware `run_diagnose` with the `modules: [{id, provenance}]` provenance array, integrated-aware `dag`/`config-schema` loading, the `integrated-classic-perimeters` pnp-cli passthrough feature, and the six-test feature-gated `integrated_provenance_tdd` suite. **Known gap (deliberate):** `support-preview`/`visual-debug` still load the integrated tier with no disable flag — tracked in 203's requirements.md, unassigned pending 205. Delivered 2026-08-10.

## Open Deviation Map

`docs/DEVIATION_LOG.md` is the single source of truth for deviation status (a
deviation is open unless its `Status` column begins with `Closed`). The list
below is a **generated snapshot** of the open set — do not hand-edit it. Run
`cargo xtask check-deviations` to regenerate; CI fails if it drifts from the log.
For closed deviations and their closure detail, read the log directly.

<!-- BEGIN GENERATED: open-deviations (cargo xtask check-deviations) -->
- **D-285-ADR-0051-AMENDED** (Open — 2026-08-01: amendment section appended; warn-and-pass is the documented policy; test `malformed_layer_marker_warns_and_uses_prior_z_per_adr_0051` green.) — Packet 187 — ADR-0051 obligation #3 retired in two prose forms (lines 78 and 80 of the original).
- **D-286-ADR-0005-AMENDED** (Open — 2026-08-10: amendment section appended; field recorded; stale counts corrected.) — Packet 202 — ADR-0005's Decision pinned CompiledModuleLive to 5 fields; packet 202 adds the 6th (native_entry) for native dispatch, so the contested bullet is…
- **DEV-026** (Open (deferred — environmental, NOT a parity gap) — 2026-07-25: re-verified and re-classified. Gaps (1) and (2) are closed in production code: `DagValidationPass::HostVersionCompatibility` / `validate_host_version` are implemented and wired into `validate_startup_dag` (`crates/slicer-scheduler/src/validation.rs`), and `LoadErrorKind::Schema` (`crates/slicer-scheduler/src/manifest.rs`) is a real distinct variant. The only remaining half is the docs/12 peak-RSS/500-layer bound, and it is **structurally unmeasurable with existing instrumentation, not unimplemented**: `AccountingAllocator` (`crates/slicer-runtime/src/report/allocator.rs`) hooks `GlobalAlloc` and its own module doc states "WASM linear memory is **not** measured here. wasmtime allocates linear memory via mmap directly, bypassing `GlobalAlloc`." Closing it needs new OS-level RSS sampling of a `pnp_cli slice` subprocess — a bench/tooling workstream, already deferred under `TASK-156` (unchecked in `docs/07_implementation_status.md`). This row diverges from no OrcaSlicer behaviour and blocks no parity work; it should not be counted as an open parity deviation.) — Startup validation still misses several compatibility and governance checks beyond the WIT-source/version problem tracked in DEV-014.
- **DEV-039** (Open — packet-local fallback; future packets 36/37 inherit the same XY-containment guard; file moved to slicer-core (2026-07-03); text corrected 2026-07-03) — Packet 35 — bounding-box fallback for empty slice polygons.
- **D-109-SELF-CAPTURED-FIXTURES** (Open — accepted limitation, user decision 2026-07-02; fixtures guard self-regression only) — Packet 109 (M1 verification) — accepted limitation.
- **D-163-ARACHNE-BRIDGE-ROLE-CONVERSION-EXEMPTION** (Open — 2026-07-16: logged at fix time) — Canonical exempts role == erOverhangPerimeter && flow.bridge() from the spacing->width back-conversion at emission (VariableWidth.cpp::thick_polyline_to_multi_…
- **DEV-099** (Open) — Typed-error entry points retain the invariant: a caught panic remains a distinct error rather than a successful empty result.
- **DEV-107** (Open) — Packet 185 follow-up — canonical top/bottom/internal-solid flow-ratio controls (material-volume parity) are not ported by packet 185; deferred to a follow-up p…
- **DEV-101** (Open — 2026-07-31: filed by packet 184 closure review. Not a regression: predates this packet (the arachne base landed in packet 150/152; the classic base landed in packet 184). Neither module is known-wrong yet; the row records the DISAGREEMENT) — Same min_width_top_surface key, two different percent bases across the two perimeter generators.
- **D-168-ARACHNE-SIMPLIFY-FALLBACKS-TIGHTER-THAN-CANONICAL** (Open — 2026-07-16) — Canonical defaults are wall_maximum_resolution = 0.5mm / wall_maximum_deviation = 0.025mm (verified in local checkout, PrintConfig.cpp).
- **D-104f-CONCENTRIC-INFILL-NO-ARACHNE** (Open — deferred to follow-up workstream) — Gap: concentric infill does not route through Arachne — OrcaSlicer's FillConcentric.cpp:80-118 / FillConcentricInternal.cpp:29-55 wire concentric infill genera…
- **D-152-TOP-AREA-SOURCE** (Open (accepted, tracked divergence) — ADR-0035's faithful-port bar is scoped to `crates/slicer-core/src/arachne/`; this divergence lives in a module-level caller (`modules/core-modules/arachne-perimeters/`), so no amending ADR is required. Accepted as a known top-area-source divergence (does not block archival).) — Packet 152 Step 4 (G3 part 2) — top-area source divergence from OrcaSlicer.
- **DEV-093** (Open — 2026-07-25: root cause identified and reproduced; fix requires vendoring `cpp_map`) — Root-caused 2026-07-25: medial_axis is not a pure function of its input, because the Voronoi beach line is a skip list whose node levels come from an OS-seeded…
- **D-154-DISCRETIZE-POINT-POINT-CASE** (Open — filed by packet 154, not fixed; scope is point-point VD edge subdivision only) — Found during packet 154's D-105D root-cause investigation; filed, not fixed.
- **D-104f-CONCENTRIC-INFILL-NO-ARACHNE (packet 154 note)** (Open — unchanged; see original row above for full detail) — Re-surfaced (not caused, not fixed) by packet 154's test runs during the D-105D thin-strip investigation.
- **DEV-103** (Open — 2026-08-01: filed by packet 188 / TASK-307) — Five canonical injection points remain unreachable from PnP.
- **DEV-104** (Open — 2026-08-01: filed by packet 188 / TASK-307) — Variable-level residuals remain in the custom-G-code port.
- **DEV-105** (Open — 2026-08-01: filed by packet 188 / TASK-307; ADR-0050 is the decision record) — The filament_extruder_id binding has two canonical id spaces.
- **DEV-108** (Open — 2026-08-01: filed by packet 187 / `TASK-306`; accepted residuals recorded) — Accepted parity residuals after packet 187 / TASK-306; these are not a claim of exact parity.
- **DEV-097** (Open — 2026-08-05: corrected — packet 181 left the placeholder symbols in code, inert; cleanup is deferred to an unscheduled successor.) — Packet 181 — placeholder-skip affordance retired; symbols and doc comment left behind for now.
- **D-173-THUMBNAIL-SINGLE-PNG** (Open — 2026-07-19: AC-6 locks no-vertical-flip; AC-7 locks the docs; implementation reviewed under packet 173.) — Packet 173 — fork renders one source PNG; PnP owns multi-format fan-out, deviating from fork ticket 011.
- **DEV-110** (Open — 2026-08-02: residual filed for the deliberate clamp substitute.) — Canonical's ContourIntersectionPoint neighbour bookkeeping is not ported.
- **DEV-112** (Open — 2026-08-02: residual is the width input selected for the percent base.) — The percent base has a width-input residual, not a spacing-formula residual.
- **DEV-114** (Open — 2026-08-02: deferral disclosed; declaring the key is a behaviour change deferred to the tree-wide unit reconciliation.) — infill_density is deliberately left an undeclared dead read (the single exception AC-20 allow-lists); the deferral is disclosed, not a parity gap.
- **DEV-100** (Open — 2026-07-31: eight advertised macros have no canonical-equivalent PnP key to bind them to and pass through as literal bracketed text with a warning; filed as the residual of packet 186, which fixed the engine (char-boundary-correct literals, list-valued config resolution, `nozzle_diameter` declared, `first_layer_temperature` aliased, warn-on-unresolved) but could not supply the missing values. The fatal-on-unresolved policy that packet originally carried was reverted before landing as architecturally wrong for a modular slicer) — Residual of DEV-085's user-facing half: eight [key] macros that an older docs/15_config_keys_reference.md advertised do not resolve, because PnP has no config…
- **DEV-106** (Open — 2026-08-01: recorded as the carrier's bounded canonical boundary_offset divergence; no consumer or G-code change) — Packet 193 — canonical's boundary_offset is 0.5 * path.width, a per-path constant; PnP stamps the signed overhang distance per point and resolves the offset fr…
- **DEV-115** (Open — 2026-08-05: divergence row filed per ADR-0027 §Decision item 4; no change planned while default config matches canonical) — Deliberate opt-in divergence: gyroid may fill top/bottom/bridge roles in addition to sparse (ADR-0027).
- **DEV-119** (Open — 2026-08-05: plan-approved deviation, documented at packet 175.) — Packet 175 — M73 emission granularity deviation, accepted for fork progress-display requirements.
- **DEV-120** (Open — 2026-08-05: filed by the packet-188 audit; cross-reference DEV-104 for the `manual_filament_change` half.) — Packet 188 — custom_gcode_changes_tool T-select suppression not modelled.
- **DEV-122** (Open — 2026-08-07: filed by deferred-task verification sweep; re-scoped the same day after the placeholder-interning finding — the defect is the `.first()` selection and it is live on every painted slice, not the `None` arm. Risk raised Low→High accordingly.) — Paint segmentation reads shell parameters from configs.first(), which in production is a dead placeholder — so every painted slice places shells with top=3, bo…
- **DEV-126** (Open — 2026-08-07: filed by deferred-task verification sweep; canonical clip mechanism verified present, PnP equivalent verified absent.) — build_wall_flags' nearest-vertex reprojection is a PnP invention with no canonical precedent; canonical assigns paint flags by clipping path geometry.
- **DEV-127** (Open — 2026-08-07: filed by deferred-task verification sweep; this row is the live tracking record, replacing ADR-0009's reused TASK-270 pointer.) — Two independent, now-drifted copies of the scan-line fill math; canonical shares one filler between infill and support.
- **DEV-128** (Open — 2026-08-07: filed by deferred-task verification sweep; type divergence verified on both sides, prior "documented" status was stale.) — support-planner carries tree-node geometry in f32 mm where canonical uses scaled-integer coord_t.
- **DEV-129** (Open — 2026-08-07: filed by deferred-task verification sweep; warn-only behaviour verified and test-pinned, canonical implementation verified present. Triage correction the same day: initially scoped as a mechanical fix, reclassified to needing its own packet. The `<0 ⇒ use the top count` resolution is one line, but there is nothing to feed it — `PlannedSupportNode` carries only `dist_to_top`, with no `dist_to_bottom` and no notion of where a branch lands on model geometry below, which is exactly what a bottom-interface band requires. Implementing the resolution alone would delete an honest warning while still producing zero geometry. Canonical spreads the real work across `SupportCommon.cpp`, `SupportParameters.hpp`, `TreeSupport.cpp` and `TreeSupportCommon.hpp`.) — support_interface_bottom_layers is registered and parsed but produces no geometry — its only effect is one warning.
- **DEV-131** (Open — 2026-08-07: filed by deferred-task verification sweep as an incidental finding; the originally-filed symbol name and the claimed prefilter were both wrong, the underlying divergence is real and unmeasured.) — 2D boundary-distance queries are unaccelerated — no tree and, contrary to the deferral note that described them, no bounding-box prefilter either.
- **DEV-132** (Open — 2026-08-07: filed by deferred-task verification sweep as an incidental finding while re-deriving `alternate_extra_wall`; both generator sites verified in canonical, both PnP sides verified.) — extra_perimeters is modelled as a config key (a PnP invention) and read by only one of the two generators, so it silently vanishes when wall_generator switches…
- **DEV-135** (Accepted — 2026-08-13: deliberate PnP extension filed with packet 226; no canonical convergence intended.) — Per-path authored coloring (ExtrusionPath3D.tool_index) has no OrcaSlicer precedent — canonical resolves the extrusion tool per region/object, never per path.
<!-- END GENERATED: open-deviations -->


## Perimeter Modules — OrcaSlicer Parity Roadmap

Packets implementing the [`docs/specs/_OLD/perimeter-modules-orca-parity-roadmap.md`](specs/_OLD/perimeter-modules-orca-parity-roadmap.md).

### M1 — Classic parity

- [x] **P102 — Perimeter foundations** (T-010..T-019). Shared `slicer-core::perimeter_utils` module; `WallBoundaryType::MaterialBoundary` widened to `Vec<MaterialBoundarySegment>`; per-layer config plumbing; `?` propagation; manifest-vs-code default reconcile; `_paint` disuse documented. Schema 4.1.0 → 4.2.0.
- [x] **P103 — Polygon ops** (T-040..T-045). `offset2_ex`/`opening_ex` in `slicer-core::polygon_ops` (T-040); `medial_axis` via boostvoronoi VD + EP.cpp behind `host-algos` feature (T-041); `ThickPolyline`/`Point2WithWidth`/`variable_width` IR types in `slicer-ir` (T-042); `build_polygon_tree` hole/contour containment in `slicer-core::polygon_tree` (T-043); `keep_largest_contour_only` in `slicer-core::polygon_ops` (T-044); ray-ops promoted to `slicer-core::geometry` with typed API, arachne-perimeters migrated (T-045). Closed 2026-06-19.
- [x] **P104 — Propagation + surface rules** (T-020..T-025, T-030..T-033). Per-vertex `is_bridge` (T-020); inner-wall `MaterialBoundary` via `build_wall_flags` `is_outer` param (T-021 classic, T-022 arachne); `overhang_areas()`/`surface_group()` view accessors + WIT mirror (T-023); `overhang_quartile` deferred `None` — deviation `the deferred overhang-quartile item` (T-024); `only_one_wall_top`/`only_one_wall_first_layer` config keys (T-025); WIT surface-group record + host populator (T-030); `surface_group()` host-resolution in `marshal/in_.rs` (T-031); `only_one_wall_top` gate (T-032); `only_one_wall_first_layer` gate (T-033). T-024 deferred pending P106/O-T031. Closed 2026-06-19.
- [x] **P105 — Spacing + fill + MMU** (T-050..T-065, T-P96-A..T-P96-C2). Classic spacing model, thin-walls, gap-fill, per-color outer-wall fragmentation. Closed 2026-06-23. Task status:
  - [x] **T-050** (spacing model): `ext_perimeter_width`/`perimeter_width` config keys + classic wall gap computation. **Corrected 2026-08-05:** classic perimeter spacing now derives from `slicer_core::flow::line_width_to_spacing` (per-vertex `ext_perimeter_spacing`/`perimeter_spacing` from `outer_wall_line_width`/`inner_wall_line_width`); the non-precise `ext_perimeter_spacing2` is `0.5 × (ext_perimeter_spacing + perimeter_spacing)` — the width average survives only in precise mode (`0.5 × (outer_w + inner_w)`, canonical's `process_classic` precise branch) — and inner-loop deltas use `-perimeter_spacing` for `i >= 2`. The width-average inline gap and the `(outer_w + inner_w) / 2.0` test assertions were replaced with `line_width_to_spacing`-derived expectations. TASK-303 closed the classic flow-wiring gap (packet `184-classic-perimeter-flow-parity`).
  - [x] **T-051** (outer/inner widths): outer and inner wall width config plumbing.
  - [x] **T-052** (width/spacing AC): width and spacing acceptance criteria.
  - [x] **T-053** (`precise_outer_wall`): precise outer wall fully wired (gated on InnerOuter); precise mode active when precise_outer_wall=true AND wall_sequence=InnerOuter; outer inset uses ext_perimeter_spacing2, inner walls emitted first. OrcaSlicer PerimeterGenerator.cpp:1501-1506,1644. Both classic and arachne modules.
  - [x] **T-054** (`wall_sequence` migration): `wall_sequence` deregistered from `path-optimization-default`'s manifest schema; ownership moved to perimeter modules per ADR-0011. **Closes the wall-sequence doc-close (both halves).** The earlier "does not fully close the wall-sequence doc-close" caveat here was stale: the `src/lib.rs` field/parse/`role_group` half landed in `ca8fe483` (2026-07-03). Re-verified 2026-07-25 — `PathOptimizationDefault` (`modules/core-modules/path-optimization-default/src/lib.rs`) carries no `WallSequence`, its `role_group` is a fixed role→priority match pinned by `committed_wall_sequence_is_not_reordered_by_role_priority`, and ownership sits in `ClassicPerimeters::emit_walls` via `slicer_core::perimeter_utils::wall_sequence_reorder`.
  - [x] **T-054b** (inner/outer/outer-inner modes): 3 wall-sequence modes implemented in perimeter modules.
  - [x] **T-054c** (`wall_sequence` reorder control): reorder control wired via `slicer-core::perimeter_utils`.
  - [x] **T-060** (`detect_thin_wall` config key): thin-wall detection gate; medial-axis now host-side via `host-services` WIT import (D-105-MEDIAL-AXIS-HOST-IMPORT).
  - [x] **T-061** (thin-wall detection logic): medial-axis-based thin-wall detection.
  - [x] **T-062** (`ThinWall` emission): `LoopType::ThinWall` paths emitted from medial-axis result.
  - [x] **T-062b** (`LoopType::GapFill` + `ExtrusionRole::GapFill`): gap-fill IR variants added; schema bumped 4.3.0 → 4.4.0.
  - [x] **T-063** (gap collection): gap polygons collected from region residual.
  - [x] **T-064** (gap-fill emission): gap-fill paths emitted via medial-axis.
  - [x] **T-065** (gap config keys): `gap_fill_enabled` / `gap_fill_min_area` config keys.
  - [x] **T-P96-A0** (OrcaSlicer investigation one-pager): source-confirmed Model A from `MultiMaterialSegmentation.cpp` + `PerimeterGenerator.cpp` (D-105-MMU-MODEL-PIVOT).
  - [x] **T-P96-B** (revert `external_contour` consumption, both modules): removed from classic-perimeters and arachne-perimeters; per-color fragmentation active (D-105-AC22-PARITY-RESHAPE).
  - [x] **T-P96-C0** (bisector-mask host populator): DROPPED — Model A needs no bisector mask (D-105-BISECTOR-MASK-DROPPED).
  - [x] **T-P96-C1** (bisector-mask WIT accessor): DROPPED — no consumer in Model A (D-105-BISECTOR-MASK-DROPPED).
  - [x] **T-P96-C2** (per-cell mask consumer in perimeter modules): DROPPED — no skip mask in Model A (D-105-BISECTOR-MASK-DROPPED).
- [x] **P106 — Overhang PrePass foundation** (O-T001, O-T002, O-T010..O-T023). New host-builtin PrePass stage `PrePass::OverhangAnnotation` added to `STAGE_ORDER` after `PrePass::LayerPlanning` (later moved to run **strictly after `PrePass::Slice`** by the overhang-after-Slice inversion, commit `554221b8` 2026-07-10 — see `the overhang-after-slice ordering change` and ADR-0031's Amendment). Adds `OverhangRegion.xy_footprint`, `QuartileBand`, and `SurfaceClassificationIR.overhang_quartile_polygons` (schema 1.1.0 → 1.2.0; `CURRENT_SLICE_IR_SCHEMA_VERSION` later bumped 4.4.0 → 4.6.0 by P109). Pure classifier `annotate_overhangs()` partitions unsupported area per layer pair into 4 quartile bands at `line_width_mm × {0.5, 1.0, 1.5, 2.0}`. See ADR-0031.
- [x] **P107 — Overhang consumers + refactor** (T-024, T-077, O-T030..O-T053). `SliceRegionView::overhang_quartile_polygons()` + WIT mirror + host populator; `overhang-classifier-default` refactored to a pure consumer reading `overhang_quartile` from `LayerCollectionView` entities, `classify.rs`/`lines_distancer.rs` deleted, module shrunk to 78 LOC. AC-5 landed in documented-gap mode: perimeter-side `overhang_quartile` propagation still ships `None`; `T-024-WIRE-VIEW-CONSUMER` and `T-077-GUEST-ADAPTER-FIELDS` registered as follow-ups. Closed 2026-07-01.
- [x] **P108 — Special modes + seam** (T-070..T-074d, T-080..T-083, T-P98-SEAM). Deleted the fake `arachne-perimeters` iterative-inset module (`classic-perimeters` sole generator until real Arachne under P110); `extra_perimeters` bonus-wall count (T-070/071); narrow-island smaller-width handling (T-072/073); non-planar `NonPlanarShell` emission (T-074b/c/d); `extra_perimeters_on_overhangs` (T-077); sharp-corner seam threshold `seam_candidate_angle_threshold_deg` = 30° (T-080/081); seam-placer robustness — a narrow fatal-error carve-out for zero-seam-candidate regions (T-082), later corrected in P109 to graceful wall preservation (the seam-fatal correction); painted `Custom("seam_enforcer")`/`Custom("seam_blocker")` gain a live consumer (T-P98-SEAM), closing `the seam-paint consumer gap` via `the seam-consumer closure`. Closed 2026-07-02.
- [x] **P109 — M1 verification** (T-100..T-105, T-P96-D, T-P96-F). Parity harness + 6 reference fixtures + 7 edge-case TDDs; AC-4 Model-A reshape replaces the byte-SHA golden with structural assertions (the AC-22 parity reshaping); `SlicedRegion.external_contour` field DELETED, schema bumped to 4.6.0 (T-P96-D). User-authorized defect fixes recorded in the M1 verification fixes. In-session seam-placer completion (the seam-fatal correction) softened P108's fatal-on-empty carve-out to graceful wall preservation, resolving the last 2 ceremony failures. Closed 2026-07-02.
- [x] **M1 — Classic parity (P102–P109) COMPLETE** (2026-07-02) — parity harness + 6 reference fixtures + 7 edge-case TDDs + AC-4 Model-A reshape + `external_contour` removal. `classic-perimeters` is the sole perimeter generator until real Arachne lands under P110.

### M2 — Real Arachne

- [x] **P110 — Voronoi + SKT foundations** (T-200..T-205). `boostvoronoi` integration, SkeletalTrapezoidation graph, discretization, pre-processing. Closed 2026-07-03.
- [x] **P111 — BeadingStrategy stack** (T-210..T-218). `BeadingStrategy` trait + `DistributedBeadingStrategy`/`RedistributeBeadingStrategy`/`WideningBeadingStrategy`/`OuterWallInsetBeadingStrategy`/`LimitedBeadingStrategy` (zero-width sentinel bookkeeping via `compute_and_strip`, the Arachne sentinel-strip item) composed by `BeadingStrategyFactory::create_stack`; goldens hand-derived from OrcaSlicer's documented C++ algorithms (no in-repo OrcaSlicer binary to capture from). Closed 2026-07-03.
- [x] **P112 — Extrusion + wire-up** (T-220..T-233; T-234 pending). Centrality filtering (T-220, the centrality adaptation), per-edge bead-count assignment (T-221), bead-count propagation with folded transition-marking (T-222, the propagation adaptation), `generate_toolpaths` sourcing widths from `BeadingStrategy::compute()` (T-223), `ExtrusionLine`/`ExtrusionJunction` IR types (T-224, schema 4.6.0 → 4.7.0), `stitch_extrusions`/`simplify_toolpaths` (Douglas-Peucker, the simplification item)/`remove_small_lines` (T-225..T-227), and the real `arachne-perimeters::run_perimeters` wire-up (T-230) via a new WIT host-service bridge `generate-arachne-walls` (the host-service bridge). New `wall_generator` config key (default `classic`) resolves the `perimeter-generator` claim between `classic-perimeters` and `arachne-perimeters`, closing a real production defect where the alphabetically-first candidate silently won (the wall-generator selection). Parity harness extended with 4 Arachne fixtures + `cube_4color_arachne` MMU fixture — self-captured baselines, no OrcaSlicer oracle in-repo (the self-captured baseline record); residual gap tracked as the MMU topology residual. Implementation complete 2026-07-03; **T-234 (the gated `cargo xtask test --workspace` closure ceremony) has not yet been run** — `packet.spec.md`'s `status:` remains `draft` pending that ceremony.
- [x] **M2 — Real Arachne (P110, P111, P112) complete** (2026-07-03) — real Voronoi + SkeletalTrapezoidation + 5-strategy BeadingStrategy pipeline lands end-to-end, replacing the deleted P108 iterative-inset fake; selectable via `wall_generator` (default `classic`, preserving every classic-perimeters golden unchanged). T-234's full-workspace closure ceremony is the one remaining formality before `packet.spec.md` flips to `status: implemented`.
- [x] **P141 — Arachne BeadingPropagation + canonical junction-band rewrite** (no TASK-###; provenanced by `target/arachne_parity_audit_20260706_020657.md` N1+N7; packet 141). N7: `SkeletalTrapezoidationGraph::beading_propagation` side table; `get_beding`/`get_nearest_beding` mirroring OrcaSlicer `getBeading`/`getNearestBeading` (`SkeletalTrapezoidation.cpp:2091-2127`); `populate_beading_propagation` + `interpolate_bead_propagation` (width/location blend). Centrality gate dropped, matching canonical `upward_quad_mids` (`:1669-1672`).
  **Correction 2026-07-06 (post-swarm review — see `the junction-bands finding` in `docs/DEVIATION_LOG.md`):** the first-draft N1 rewrite was non-canonical in three ways (beading resolved at the boundary-side vertex instead of the peak, per-junction width recomputed ad hoc, ribs excluded from `generate_junctions`/chain-hop); all three fixed, and `populate_beading_propagation` is now actually called from `run_arachne_pipeline` (previously dead code). Fixing these correctly exposes a real pre-existing gap in the downstream chain walk (no 3-or-more-way junction detection, no rib-based connectivity replacement) — Packet 142's stated scope. As a result, AC-1/AC-2 (`arachne_parity_red_junction_bands`) and several invariant tests now correctly FAIL (they only passed before because the bugs kept them inside the old chain-walk's assumptions); 141+142 are recommended as a combined unit. `packet.spec.md` remains `status= draft`. The original first-draft rewrite (`SkeletalTrapezoidation.cpp:2013-2079` shape) was found non-canonical in review and superseded by the correction above.

- [x] **P142 — Arachne canonical connectJunctions emission + is_odd** (no TASK-###; provenanced by `target/arachne_parity_audit_20260706_020657.md` N2+N4; packet 142). N2: `perimeter_index = bead_idx` set at junction generation (`SkeletalTrapezoidation.cpp:2064-2077`); `assign_perimeter_indices` post-pass deleted. Canonical `connectJunctions` per-quad emission with 3-or-more-way junction detection at the walk level (`compute_vertex_degree`; walk breaks at degree > 2, producing spoke fragments `stitch_extrusions` reconnects — verified against `:2198-2234`). N4: `is_odd` replaced from `bead_idx % 2 == 1` with the canonical per-segment rule (`bead_count % 2 == 1`, `transition_ratio == 0`, innermost junction, endpoint proximity). `outer_wall_closes_for_simple_polygon` corrected to reflect pipeline behavior (verified legitimate, not assertion weakening). N3 (`arachne_parity_red_transition_ends`) stays RED — Packet 143 scope. Joint closure with P141: both flip to `implemented` in the same commit. `docs/DEVIATION_LOG.md`: `the connectJunctions emission`.
- [x] **P143 — Arachne transition ends + extra ribs** (no TASK-###; provenanced by `target/arachne_parity_audit_20260706_020657.md` N3+N8; packet 143). N3: canonical end-based transition splitting (`filter_transition_mids → generate_all_transition_ends → apply_transitions` at ends) replaces the single-mid-split + `transition_ratio=0` scheme; fractional `transition_ratio` blended at emission. `BeadingStrategy` trait gains `get_transitioning_length`/`get_transition_anchor_pos`/`get_nonlinear_thicknesses`/`get_transition_filter_dist`. N8: new `generate_extra_ribs`. `EdgeType::TRANSITION_END` (PnP invention, zero usages) deleted. Known divergence: `lower_bead_count == 0` transition mids skipped (downstream can't handle bead_count=0 nodes). `docs/DEVIATION_LOG.md`: `the transition-end work`.
- [x] **P144 — Arachne angle-fudge removal + filterNoncentralRegions** (no TASK-###; provenanced by `target/arachne_parity_audit_20260706_020657.md` N5+N6; packet 144). N5: deleted the π-cap workaround and the `0.1×` `transition_filter_dist` fudge in `pipeline.rs`, threading the configured `wall_transition_angle` (default 10°) through `filter_central`. N6: ported `filter_noncentral_regions` (`SkeletalTrapezoidation.cpp:811-862`) into `centrality.rs`, promoting non-central gaps between same/±1-bead-count central regions (within 0.4mm) back to central. `docs/DEVIATION_LOG.md`: `the centrality-threshold correction`.
- [x] **P145 — Arachne local-maxima single beads + construction epilogue** (no TASK-###; provenanced by `target/arachne_parity_audit_20260706_020657.md` N9+N10; packet 145). N9: `generate_local_maxima_single_beads` — a 6-segment hexagonal micro-loop (radius width/8, `is_odd=true`) for odd-bead-count strict-local-maximum non-central vertices, as the final step of `generate_toolpaths`. N10: construction epilogue `separate_pointy_quad_end_nodes` + `collapse_small_edges` appended to `from_polygons`. `docs/DEVIATION_LOG.md`: `the Arachne local-maxima construction epilogue`.

- [x] **P146 — Arachne post-process order + per-line min_width + simplify distance gates** (no TASK-###; provenanced by `target/arachne_parity_audit_20260706_020657.md` N11+N12+N13; packet 146). N11: pipeline reordered `stitch → remove_small → separate_out_inner_contour → simplify → remove_empty` (WallToolPaths.cpp:679-699); new `separate_out_inner_contour` + `remove_empty_toolpaths`. N12: `remove_small_lines` computes `min_width` per line instead of a caller-supplied constant (WallToolPaths.cpp:838-856). N13: `simplify_toolpaths` replaced with the canonical distance-gated single pass (ExtrusionLine.cpp:56-243); three new `ArachneParams` fields (`smallest_line_segment_squared`, `allowed_error_distance_squared`, `maximum_extrusion_area_deviation`) threaded through slicer-sdk + slicer-wasm-host. `docs/DEVIATION_LOG.md`: `the Arachne post-process order correction` + `the simplification item` addendum. `packet.spec.md` `status: implemented`.

- [x] **P147 — Arachne cross-cutting closure** (no TASK-###; packet 147). Fixed all 7 deferred parity-audit findings from the Arachne parity audit findings (commits `fc362cc4`, `a603a2d4`, `283eea6d`); improved closure rate from 0% to 49.33% but did not meet AC-1's strict 0-failure bar — e2e closure gate `cube_4color_arachne_outer_walls_close_end_to_end` KEPT `#[ignore]`d (user decision 2026-07-08), the wall/infill bug is real and out of scope for 147. 5 cross-crate `perimeter_parity` fixtures re-recorded. ADR 0035 authored (chain supersedes PNP "ADAPTATION" divergence). `the Arachne chain closure` registered, `the Arachne parity audit findings` closed. `packet.spec.md` `status: implemented`. Closed 2026-07-08. **AC-1 met 2026-07-16 (Arachne Parity Recovery, Track C): the gate is at 0/699 (0.00%) and is un-ignored; `the Arachne chain closure` closed.** The residual this packet called "a real wall/infill bug, out of scope" was upstream in the beading pipeline, not in chain closure — it was dissolved by `the taper-peak dropout fix` (`5d0e1bcf`) + `the inner-wall overextrusion fix` (`1dfac847`) without any closure-motivated change, refuting this packet's parting hypothesis that `fc362cc4`'s merge-rule changes needed a downstream stitch/topology fix.

- [x] **P153 — Arachne line-junctions restructure + stitch faithfulness** (no TASK-###; packet 153). Restructured `EdgeJunctions` to a single `Vec<ExtrusionJunction>` per edge (OrcaSlicer `LineJunctions`), ordered peak-side to boundary-side with `perimeter_index == junction_idx`. Ported OrcaSlicer's `canReverse` parity gate and the `3 * max_stitch_distance` tiny-polygon non-closure rule into `stitch_extrusions`; AC-3 + AC-4b regression-anchored. `docs/DEVIATION_LOG.md`: `the line-junction and stitch work` registered (closed). `packet.spec.md` `status: implemented`.

- [x] **M2 — Real Arachne N1–N13 parity chain (P141–P147) COMPLETE** (2026-07-08) — full canonical Arachne emission + transitions + post-process, supersedes PNP "ADAPTATION" divergence; documented in ADR 0035 + the Arachne chain closure. **AC-1 e2e closure gate MET and un-ignored 2026-07-16** (0/699 sub-loops fail to close, mean gap 0.0000mm across all 125 layers; was kept `#[ignore]`d 2026-07-08 for a wall/infill residual that proved to be an upstream beading defect, closed as D5+D4 by the Arachne Parity Recovery campaign). All ACs green.
