# Deviation Log

Use this registry for intentional deviations from architecture docs.

> **Single source of truth.** This file is authoritative for deviation status. A
> deviation is **open** unless its `Status` column begins with `Closed`. Status
> summaries elsewhere (`docs/07_implementation_status.md` Open Deviation Map,
> `docs/14_deviation_audit_history.md`) are non-authoritative views generated
> from this table by `cargo xtask check-deviations`; when they disagree, this
> file wins. Do not hand-edit those views.

| ID | Date | Affected Section | Risk | Rationale | Mitigation Owner | Target Close | Status |
| --- | --- | --- | --- | --- | --- | --- | --- |
| D-285-ADR-0051-AMENDED | 2026-08-01 | docs/adr/0051-gcode-marker-contract-ownership.md §Obligation #3 / packet 187 | Low | **Packet 187 — ADR-0051 obligation #3 retired in two prose forms (lines 78 and 80 of the original). Retired clause quoted verbatim in the ADR's `## Amendment — 2026-08-01 (packet 187)` section. The "no silent guess" half of the obligation is preserved; the fatal-error half is replaced with a warn-and-pass policy specific to the postpass injection-point model (one `ERR_MALFORMED_LAYER_MARKER` warning per occurrence, prior Z reused, `Ok` return). A future consumer that cannot degrade gracefully remains on the original "fail loudly" rule and must record that choice in the amendment section. The `D-` series is the counter, separate from `DEV-###`; re-derive at the moment of writing.** | machine-gcode-emit maintainer | packet 187 closure | Open — 2026-08-01: amendment section appended; warn-and-pass is the documented policy; test `malformed_layer_marker_warns_and_uses_prior_z_per_adr_0051` green. |
| DEV-026 | 2026-04-16 | 04_host_scheduler.md §"Phase 3 DAG Validation" / 11_operational_governance_and_acceptance_gate.md §2 "Compatibility Policy" / 12_architecture_gate_metrics.md §"Compatibility" | High | Startup validation still misses several compatibility and governance checks beyond the WIT-source/version problem tracked in DEV-014. The remaining unique gaps are: (1) `min_host_version` is parsed but never compared at startup; (2) manifest-schema validation cannot currently surface a meaningful `Schema` failure path because missing schema falls back to defaults and parse failures collapse into TOML errors; (3) memory, full-slice time, and host-call budgets from docs/12 have no runtime measurement, diagnostics, or benchmark evidence. Layer-count and region-cap bounds are already enforced. Implement the host-side `wit_world` allowlist as part of the same validation pass used to close DEV-014 rather than tracking that subtask separately here. **Verified 2026-07-03:** gap (2) is now closed — `LoadErrorKind::Schema` (`crates/slicer-scheduler/src/manifest.rs`) is a real, distinct variant with specific field/message context, proven by `region_split_manifest_tdd.rs`; malformed manifests no longer collapse into generic TOML errors. **Gap (1) closed (this pass corrects stale status, not new work):** a 14th startup DAG validation pass, `DagValidationPass::HostVersionCompatibility`, was added and wired in production — the `DagValidationPass::HostVersionCompatibility` variant and the `validate_host_version` free function both live in `crates/slicer-scheduler/src/validation.rs`, `validate_startup_dag` (same file) calls `validate_host_version`, and it compares `request.host_version` against each loaded module's declared `min_host_version` via the existing `semver_lt` helper, pushing `SchedulerError::HostVersionIncompatible { module, required, available }` on failure. `run_slice` (`crates/slicer-runtime/src/run.rs`) populates `host_version` from the running host's own `CARGO_PKG_VERSION` at the single production call site. (Line pins previously recorded here had rotted and were dropped per the repo's cite-by-symbol rule; every symbol named resolves.) Documented in `docs/04_host_scheduler.md`'s "Validation Passes (in order)" and `docs/11_operational_governance_and_acceptance_gate.md` §2. This landed in an earlier commit (`ca8fe483`) but this log row was never updated to reflect it until now. **Gap (3) partially closed 2026-07-03 (time-only, downgraded per maintainer decision):** the docs/12 500-layer peak-RSS bound is not achievable with existing instrumentation — `AccountingAllocator` (`crates/slicer-runtime/src/report/allocator.rs`) only tracks host-heap allocations and explicitly excludes WASM linear memory (wasmtime mmaps it directly, bypassing `GlobalAlloc`) — and is deferred indefinitely (tracked under `TASK-156`; would need new OS-level RSS sampling around a subprocess run). The 50-layer full-slice-time bound now has reproducible real evidence: `crates/slicer-runtime/benches/gate_evidence.rs` (`cargo bench -p slicer-runtime --bench gate_evidence`) times a real `pnp_cli slice` subprocess against `resources/regression_wedge.stl` + `resources/test_config/gate_evidence_50l.json` (a `layer_height`/`first_layer_height` override coercing the 40mm-tall fixture to exactly 50 layers, verified via `;LAYER_CHANGE` count — not the normatively-named `benchy_50l_0p2_single_tool` fixture, which still doesn't exist). Measured this session (debug profile): **~438ms median** (`[435.25, 438.01, 440.97] ms`), well under the 10-second bound. Measures and reports rather than hard-failing (matches this repo's other benches — "slow; not in CI"). | slicer-host maintainer | Deferred — tracked under TASK-156 | Open (deferred — environmental, NOT a parity gap) — 2026-07-25: re-verified and re-classified. Gaps (1) and (2) are closed in production code: `DagValidationPass::HostVersionCompatibility` / `validate_host_version` are implemented and wired into `validate_startup_dag` (`crates/slicer-scheduler/src/validation.rs`), and `LoadErrorKind::Schema` (`crates/slicer-scheduler/src/manifest.rs`) is a real distinct variant. The only remaining half is the docs/12 peak-RSS/500-layer bound, and it is **structurally unmeasurable with existing instrumentation, not unimplemented**: `AccountingAllocator` (`crates/slicer-runtime/src/report/allocator.rs`) hooks `GlobalAlloc` and its own module doc states "WASM linear memory is **not** measured here. wasmtime allocates linear memory via mmap directly, bypassing `GlobalAlloc`." Closing it needs new OS-level RSS sampling of a `pnp_cli slice` subprocess — a bench/tooling workstream, already deferred under `TASK-156` (unchecked in `docs/07_implementation_status.md`). This row diverges from no OrcaSlicer behaviour and blocks no parity work; it should not be counted as an open parity deviation. |
| DEV-039 | 2026-05-04 | crates/slicer-core/src/algos/prepass_slice.rs::execute_prepass_slice_single_layer (moved/renamed from crates/slicer-host/src/layer_slice.rs::execute_layer_slice) | Low | **Packet 35 — bounding-box fallback for empty slice polygons.** When `slice_mesh_ex` yields zero polygons for a layer (e.g., perfectly flat top/bottom triangles skipped by the slicer), `execute_layer_slice` synthesizes a bounding-box polygon from the `ObjectMesh` vertex XY extents for the surface-classification XY-containment check. Without this fallback, surface-classified facets lying entirely above/below a flat-top/bottom layer would fail the XY-containment guard and surface classification would never fire on geometry that warrants it. This is packet-local; not part of the original packet 35 design. **Verified 2026-07-03:** the fallback logic itself is unchanged and still fires, but the function moved to `crates/slicer-core/src/algos/prepass_slice.rs:493-524` (line range corrected — was previously misquoted as 483-520) and was renamed `execute_prepass_slice_single_layer` during the host→core algo migration; no later packet replaced the heuristic with anything more precise. **Correction 2026-07-03:** the fallback's purpose has drifted from this row's original text — it now feeds `classify_region_surfaces` / `SlicedRegion.is_bridge` (bridge detection), not top/bottom-surface classification; `SlicedRegion` no longer has `is_top_surface`/`is_bottom_surface` fields, that responsibility moved to the separate `ShellClassification` pass in `crates/slicer-core/src/algos/paint_segmentation/top_bottom.rs`. The previously-cited test `multi_layer_thickness_tdd` does not exist anywhere in this repo's history (confirmed via `git log --all -S`); the real test exercising this path today is `crates/slicer-runtime/tests/unit/bridge_detector_tdd.rs::non_bridge_region_has_empty_bridge_areas`. | slicer-host maintainer | TBD | Open — packet-local fallback; future packets 36/37 inherit the same XY-containment guard; file moved to slicer-core (2026-07-03); text corrected 2026-07-03 (line range, real consumer, real test) |






| D-109-SELF-CAPTURED-FIXTURES | 2026-07-02 | crates/slicer-runtime/tests/integration/perimeter_parity.rs §fixture provenance (lines ~749-770) / docs/specs/perimeter-modules-orca-parity-roadmap.md §M1 (T-101) | Medium | **Packet 109 (M1 verification) — accepted limitation.** Roadmap T-101 asked to "Record OrcaSlicer reference outputs for 6 M1 fixtures". This environment cannot run OrcaSlicer, so the six M1 parity fixtures are SELF-CAPTURED baselines recorded from this pipeline's own output (see the provenance comment in `perimeter_parity.rs`). The harness therefore proves self-regression (geometry stays stable across commits), NOT per-vertex OrcaSlicer parity — it can detect drift from our own prior output but never divergence from OrcaSlicer. The roadmap's M1 "COMPLETE" claim is scoped accordingly. User accepted this limitation 2026-07-02 (packets-102–109 review). | slicer-runtime maintainer | — (accepted; revisit if an OrcaSlicer reference-capture environment becomes available) | Open — accepted limitation, user decision 2026-07-02; fixtures guard self-regression only |























| D-163-ARACHNE-BRIDGE-ROLE-CONVERSION-EXEMPTION | 2026-07-16 | modules/core-modules/arachne-perimeters/src/lib.rs::build_walls (emission back-conversion) | Low | Canonical exempts `role == erOverhangPerimeter && flow.bridge()` from the spacing->width back-conversion at emission (`VariableWidth.cpp::thick_polyline_to_multi_path` keeps the bridge flow's own width). PnP has no role analog at this seam — bridges are per-vertex `flow_factor`, not a path role — so the D-160 Bug B fix converts uniformly. Bridge vertices therefore get a back-converted width plus `bridging_flow`'s factor, where canonical substitutes the bridge flow width wholesale. Logged rather than silently mirrored; revisit if bridge extrusion parity work lands a role model at this seam. | core-modules maintainer | bridge-parity packet | Open — 2026-07-16: logged at fix time |
| DEV-099 | 2026-07-30 | `crates/slicer-core/src/medial_axis.rs` `medial_axis` catch/error conversion | Low | Typed-error entry points retain the invariant: a caught panic remains a distinct error rather than a successful empty result. `medial_axis` is exempt because `Err(())` becomes `Ok(vec![])`, so a caught panic can degrade to empty. No ADR. Cost needs design/measurement. | next packet touching `medial_axis` | design/measurement | Open |
| DEV-102 | 2026-08-01 | (deferred) | Medium | **Packet 185 follow-up** — canonical top/bottom/internal-solid flow-**ratio** controls (material-volume parity) are not ported by packet 185; deferred to a follow-up packet. The resolver (`resolve_role_width`) handles geometric widths/spacing only. | core-modules maintainer | flow-ratio controls packet | Open |
| DEV-101 | 2026-07-31 | `modules/core-modules/classic-perimeters/src/lib.rs` (`ClassicPerimeters::run_perimeters`, the `get_abs_value("min_width_top_surface", ...)` binding) vs `modules/core-modules/arachne-perimeters/src/lib.rs` (`emit_only_one_wall_top_second_pass`); manifests `classic-perimeters.toml` / `arachne-perimeters.toml` `[config.schema.min_width_top_surface]` | Low | **Same `min_width_top_surface` key, two different percent bases across the two perimeter generators.** Both modules declare the key as `float_or_percent` with default `"0.0"`, but they resolve the percent form against different reference widths: `classic-perimeters` passes `inner_wall_line_width` as the `get_abs_value` base (canonical `ratio_over` for this key is the perimeter width), while `arachne-perimeters` passes `preferred_bead_width_outer`. Consequence: the SAME percent string yields DIFFERENT absolute thresholds in the two modules whenever outer and inner wall widths differ — e.g. `"300%"` with `inner_wall_line_width = 0.4` and `preferred_bead_width_outer = 0.5` resolves to `1.2` mm under Classic and `1.5` mm under Arachne, so switching `wall_generator` silently changes which top sub-areas are eroded out of the top portion. The float form is unaffected (no base is consulted). Canonical OrcaSlicer has a single `ratio_over` for the key, so at most one of the two bases can be faithful; determining which — and aligning the other — is deferred. Already documented as a cross-module difference in `docs/15_config_keys_reference.md` (the two `min_width_top_surface` rows record `base: inner_wall_line_width` and `base: preferred_bead_width_outer` respectively) but had no deviation row until now. | core-modules maintainer | packet to reconcile the two percent bases against canonical `ratio_over` (verify canonical first; changing either base moves gated geometry for percent-valued profiles) | Open — 2026-07-31: filed by packet 184 closure review. Not a regression: predates this packet (the arachne base landed in packet 150/152; the classic base landed in packet 184). Neither module is known-wrong yet; the row records the DISAGREEMENT |
| D-168-ARACHNE-SIMPLIFY-FALLBACKS-TIGHTER-THAN-CANONICAL | 2026-07-16 | modules/core-modules/arachne-perimeters/src/lib.rs (wall_maximum_resolution/wall_maximum_deviation reads) + ArachneParams::default | Low | Canonical defaults are `wall_maximum_resolution = 0.5mm` / `wall_maximum_deviation = 0.025mm` (verified in local checkout, `PrintConfig.cpp`). PnP's CODE fallbacks are 0.05mm / 0.005mm (via `ArachneParams::default`'s `smallest_line_segment_squared = 0.0025` / `allowed_error_distance_squared = 0.000025`, which transcribed the meshfix_* constants instead). The old manifest defaults (0.5/0.025) matched canonical but lied about the code; the exhaustive reconcile guard (manifest_default_reconcile_tdd) now pins the manifest to the CODE fallback (0.05/0.005), making the code-vs-canonical divergence the explicit open item: when the keys are unset, PnP simplifies arachne walls 10x/5x less aggressively than upstream. Changing the code fallback moves every arachne fixture, so it needs its own predicted-movement re-record. Related follow-up owned by the same packet: the reconcile guard is exhaustive by ENUMERATION (transcribed table); the by-construction form (read sites consume a shared DEFAULTS table) is blocked on classic's DERIVED fallbacks (nozzle_diameter -> inner_wall_line_width -> line_width). | core-modules maintainer | simplify-parity packet | Open — 2026-07-16 |





| D-104f-CONCENTRIC-INFILL-NO-ARACHNE | 2026-07-09 | crates/slicer-runtime/tests/arachne_parity.rs §`arachne_parity_pipeline_concentric_infill_uses_arachne` | Low | Gap: concentric infill does not route through Arachne — OrcaSlicer's `FillConcentric.cpp:80-118` / `FillConcentricInternal.cpp:29-55` wire concentric infill generation through the same Arachne wall-toolpath machinery used for perimeters; this codebase's infill modules have no equivalent wiring. **Not closed by packet 149** — the red test `arachne_parity_pipeline_concentric_infill_uses_arachne` stays red as the explicit fingerprint of this gap (14 passed / 1 red in the `arachne_parity` suite this packet). Deferred to a follow-up workstream; no packet is currently scoped to close it. | core-modules maintainer | — (deferred; follow-up workstream TBD) | Open — deferred to follow-up workstream |








| D-152-TOP-AREA-SOURCE | 2026-07-11 | modules/core-modules/arachne-perimeters/src/lib.rs §`emit_only_one_wall_top_second_pass` / crates/slicer-sdk/src/views.rs §`top_solid_fill` | Medium | **Packet 152 Step 4 (G3 part 2) — top-area source divergence from OrcaSlicer.** PnP has no `upper_slices`/`lower_slices` access on `SliceRegionView`, so the G3 part-2 second-pass top-area is derived from `region.top_solid_fill()` (the host's precomputed top-region polygon) instead of OrcaSlicer's `diff_ex(infill_contour, upper_slices_clipped)` (`PerimeterGenerator.cpp:2160-2246`). The bridge-exclusion step (Orca carves `lower_slices_clipped_offset` out of the top area) is SKIPPED for the same reason. All other steps are ported: `min_width_top_surface` filter via `get_abs_value` (packet 150 mechanism), `offset2_ex` shrink `-top_surface_min_width` / expand `+top_surface_min_width + 0.85*perimeter_width`, second `WallToolPaths` over the non-top remainder with `inner_loop_number + 1` walls, `inset_idx` (`perimeter_index`) += 1 renumbering of inner walls BEFORE merge, and the empty-top fallback rerun with `inner_loop_number + 2`. AC-2 verified (single top wall + renumbering) via `tests/only_one_wall_top_tdd.rs::only_one_wall_top_second_pass`. | core-modules maintainer | — | Open (accepted, tracked divergence) — ADR-0035's faithful-port bar is scoped to `crates/slicer-core/src/arachne/`; this divergence lives in a module-level caller (`modules/core-modules/arachne-perimeters/`), so no amending ADR is required. Accepted as a known top-area-source divergence (does not block archival). |
| D-154-DISCRETIZE-POINT-POINT-CASE | 2026-07-14 | crates/slicer-core/src/skeletal_trapezoidation/graph.rs::discretize_edge (line 1280) | Low | **Found during packet 154's D-105D root-cause investigation; filed, not fixed.** PnP's `discretize_edge` returns `{start, end}` for ALL `!is_curved` edges — it conflates canonical `discretize()`'s branch 1 (`(!point_left && !point_right) \|\| vd_edge.is_secondary()` → `Points({start,end})`, `SkeletalTrapezoidation.cpp:236`) with its branch 3 (both cells are point-cells → a straight edge that is STILL subdivided by `discretization_step_size`, `:246`). Canonical branches at `SkeletalTrapezoidation.cpp:220` (dispatch), `:236` (branch 1), `:240` (branch 2, point-line), `:246` (branch 3, point-point). Consequence: point-point VD edges are never subdivided in PnP, so point-point channels that narrow between two vertices get no intermediate beading samples. Sub-gap noted in the same investigation: PnP's `discretize_edge` also omits branch 1's `is_secondary()` disjunct; whether PnP's `is_curved` flag already encodes secondary-edge status is unconfirmed. **Provably NOT the D-105D thin-strip root cause** — a rectangle's spine is a seg-seg edge (branch 1), not point-point (branch 3); confirmed the real root cause was `preprocess.rs::merge_short_segments` (see D-105D, closed). | arachne maintainer | TBD | Open — filed by packet 154, not fixed; scope is point-point VD edge subdivision only |
| D-104f-CONCENTRIC-INFILL-NO-ARACHNE (packet 154 note) | 2026-07-14 | crates/slicer-runtime/src/run.rs / crates/slicer-runtime/tests/arachne_parity.rs:915 | Low | **Re-surfaced (not caused, not fixed) by packet 154's test runs during the D-105D thin-strip investigation.** This gap was filed 2026-07-09 (packet 149, row above) and remains open under its original ID; this note only records that packet 154 independently re-confirmed it while running the full `arachne_parity` suite: `run.rs` never dispatches concentric infill through `run_arachne_pipeline`, whereas OrcaSlicer routes concentric infill wall generation through Arachne's `WallToolPaths` (`FillConcentric.cpp:80-118`). The test `arachne_parity_pipeline_concentric_infill_uses_arachne` (`crates/slicer-runtime/tests/arachne_parity.rs:915-938`) already proposes this exact deviation ID in its own assertion message. Pre-existing and out of packet 154's scope. | core-modules maintainer | — (deferred; follow-up workstream TBD) | Open — unchanged; see original row above for full detail |


| DEV-103 | 2026-08-01 | modules/core-modules/machine-gcode-emit/src/lib.rs custom-G-code injection registry / `PostPass::GCodePostProcess` | Low | **Five canonical injection points remain unreachable from PnP.** `file_start_gcode` is emitted before `; HEADER_BLOCK_START`, while `DefaultGCodeSerializer::serialize_gcode` writes `serialize_header_block` before iterating `gcode_ir.commands`; `ThumbnailAwareSerializer::serialize_gcode` inserts after `; HEADER_BLOCK_END`, so the file-start site cannot be reached. `wrapping_detection_gcode` is gated by canonical `enable_wrapping_detection`, which has no PnP occurrence, and canonical emits it from a per-extruder loop in `GCode::process_layer`. `machine_pause_gcode` and `template_custom_gcode` are gated by `CustomGCode::PausePrint` and `CustomGCode::Template` in `emit_custom_gcode_per_print_z`, but PnP has no per-print-Z custom-G-code item model. `printing_by_object_gcode` requires a by-object print sequence; PnP has no by-object path, and the default (\"print_sequence\", \"by layer\") in `ORCA_CONFIG_PADDING` is a default rather than a hard-code. | core-modules maintainers | TBD | Open — 2026-08-01: filed by packet 188 / TASK-307 |
| DEV-104 | 2026-08-01 | modules/core-modules/machine-gcode-emit/src/lib.rs custom-G-code placeholder bindings / toolchange and extrusion-role sites | Low | **Variable-level residuals remain in the custom-G-code port.** Canonical registers `filament_start_gcode`, `filament_end_gcode`, and `filament_change_extrusion_role_gcode` as `coStrings` per-filament vectors resolved with `get_at(filament_id)`, while PnP declares scalar string keys. The `change_filament_gcode` placeholder set is a scoped subset: canonical also exposes `flush_length_1` through `flush_length_4`, `x_after_toolchange`, `y_after_toolchange`, `z_after_toolchange`, travel points, temperatures, and retract lengths, none of which PnP models. Canonical initializes `m_last_extrusion_role` to `erNone` but PnP substitutes an empty string; `manual_filament_change` first-toolchange suppression is not modelled. PnP splices at the `;TYPE:` tag gate `m_last_processor_extrusion_role`, not the template gate `m_last_extrusion_role`, and canonical assigns those members independently. `WipeTowerIntegration::append_tcr` binds additional variables at `filament_start_gcode` that PnP does not surface. | core-modules maintainers | TBD | Open — 2026-08-01: filed by packet 188 / TASK-307 |
| DEV-105 | 2026-08-01 | modules/core-modules/machine-gcode-emit/src/lib.rs toolchange placeholder bindings / docs/adr/0050-custom-gcode-architecture.md | Medium | **The `filament_extruder_id` binding has two canonical id spaces.** At `filament_start_gcode` canonical binds it to the new tool as a raw FILAMENT id via `int(new_filament_id)`; at `filament_end_gcode` it binds it to the old tool as an EXTRUDER id via `int(get_extruder_id(old_filament_id))`. Those spaces differ on any multi-extruder-per-filament configuration. PnP's `GCodeCommand::ToolChange` carries tool indices only and models no filament-versus-extruder distinction, so it binds `to` at the start site and `from` at the end site: the OLD/NEW direction is faithful, but the id space is not representable. Any future multi-extruder-per-filament support must either carry both ids or accept a knowingly wrong substitution at one site. See `ADR-0050` and `docs/adr/0050-custom-gcode-architecture.md`. | core-modules maintainers | TBD | Open — 2026-08-01: filed by packet 188 / TASK-307; ADR-0050 is the decision record |
| DEV-102 | 2026-08-01 | modules/core-modules/machine-gcode-emit/src/lib.rs / packet 187 custom-G-code injection registry | Low | **Accepted parity residuals after packet 187 / `TASK-306`; these are not a claim of exact parity.** (a) At `layer_change_gcode`, `max_layer_z` is a canonical **dead-write** of `m_max_layer_z`; (a2) the other two `max_layer_z` sites have no divergence. (b) The BBL timelapse path remains unported, including `generate_timelapse_gcode`, `timelapse_inline_photo`, and `is_BBL_Printer`. (c) Six `layer_change_gcode` variables remain unmodelled: `most_used_physical_extruder_id`, `current_filament_id`, `current_nozzle_id`, `curr_y_acceleration_limit`, `curr_accumulated_mass`, and `curr_layer_mass`; `m_max_layer_z` is the underlying canonical member for the dead-write. (d) `change_layer` interleaving differs from canonical, including `add_object_change_labels`. These are accepted residuals for the exact parity audit and remain separate from the implemented injection points. | core-modules maintainers | packet 188 / future parity audit | Open — 2026-08-01: filed by packet 187 / `TASK-306`; accepted residuals recorded |
| DEV-097 | 2026-07-27 | crates/slicer-scheduler/src/manifest.rs (`is_placeholder_wasm` and `LoadedModule::placeholder_wasm` accessor; doc comment "runtime dispatch will skip them with a diagnostic") | Low | **Packet 181 — placeholder-skip affordance retired; symbols and doc comment left behind for now.** `is_placeholder_wasm` (`fs::metadata(wasm_path).len() <= 8`) and `LoadedModule::placeholder_wasm` (`crates/slicer-scheduler/src/manifest.rs`) are now inert for dispatch: the loader fatal at packet 181 step 3 escalates all three `compile_module_component` `None` branches to `LiveModuleLoadError::Component { module_id, cause }`, so an ≤8-byte stub no longer produces a warning-and-continue path. The manifest affordance doc comment ("runtime dispatch will skip them with a diagnostic") is also false. Symbols are removed in this packet's scope; the doc comment is left intact. The user can scaffold a module by building a `.wasm` that compiles (≥ a few kB). No successor packet exists in `.ralph/specs/` to own the cleanup. | slicer-scheduler maintainers | TBD | Open — 2026-07-27: recorded, not scheduled. |

| D-173-THUMBNAIL-SINGLE-PNG | 2026-07-19 | crates/slicer-gcode/src/thumbnail.rs / serialize.rs / crates/slicer-runtime/src/pipeline.rs / docs/02_ir_schemas.md | Medium | **Packet 173 — fork renders one source PNG; PnP owns multi-format fan-out, deviating from fork ticket 011.** Fork ticket 011 expected the fork to render one thumbnail per requested size/format. PnP's user-decided contract is single-source-PNG: the fork renders ONE high-res top-down PNG and passes it via `--thumbnail`; the `thumbnails` config key (`"WxH/EXT,WxH/EXT"`) carries the requested sizes/formats; PnP decodes the source, resizes per spec (`image::imageops::resize` with `FilterType::CatmullRom`), and encodes PNG/JPG/QOI/ColPic/BTT_TFT itself (`render_thumbnail_entries` in `crates/slicer-gcode/src/thumbnail.rs`). Per-format codecs: PNG/JPG/QOI via the `image` crate (`crates/slicer-gcode/Cargo.toml` dep `image = { version = "0.25", default-features = false, features = ["png", "jpeg", "qoi"] }`); ColPic is a Rust port of canonical `ColPic_EncodeStr`/`ColPicEncode`/`Byte8bitEncode`/`ADList0` (`OrcaSlicerDocumented/src/libslic3r/GCode/Thumbnails.cpp`) in `crates/slicer-gcode/src/thumbnail_colpic.rs` with the standard porting header; BTT_TFT is a Rust port of canonical `compress_thumbnail_btt_tft` in `crates/slicer-gcode/src/thumbnail_btt.rs`. Two further reductions vs canonical: (1) ColPic and BTT_TFT do NOT vertically flip the image — canonical does (the GL-buffer workaround), PnP source PNGs are already top-down; (2) ColPic chunk-size is implementation-chosen to keep each `;gimage:` / `;simage:` line ≤ 78 chars; canonical chunking is data-size-driven. Wire format is byte-exact to canonical: `; <tag> begin <W>x<H> <len>` / 78-col `; `-prefixed base64 / `; <tag> end`, with the outer `; THUMBNAIL_BLOCK_START`/`; THUMBNAIL_BLOCK_END` sentinels retained as a PnP extension. | n/a (user-decided) | TBD | Open — 2026-07-19: AC-6 locks no-vertical-flip; AC-7 locks the docs; implementation reviewed under packet 173. |



| D-283-ADR-0046-AMENDED | 2026-07-22 | `docs/adr/0046-aligned-seam-in-seam-planning-prepass.md` (L50, L97-98) + `docs/DEVIATION_LOG.md` (D-168-SEAM-PREPASS-SOURCE part 6) | Medium | OrcaSlicer's canonical default `seam_position` is `spAligned`; changing the default to `aligned` matches canonical behavior. | seam-placer + seam-planner-default maintainer | Closed at packet 180 closure | Open — to be set to Closed at packet 180 closure |

| DEV-110 | 2026-08-02 | `OrcaSlicerDocumented/src/libslic3r/Fill/FillBase.cpp` (`ContourIntersectionPoint`, `take_limited`) / `modules/core-modules/infill-linker/src/graph.rs` (`contour_stub`) | Medium | **Canonical's `ContourIntersectionPoint` neighbour bookkeeping is not ported.** Canonical's `take_limited` reaches `could_take_prev`, `could_take_next`, `trim_prev`, and `trim_next`; PnP substitutes a stub budget clamped to the arc reaching the next boundary position in the walk direction (AC-N3). Canonical's early-return path can leave an endpoint unconsumed so a later arc can claim it; PnP has no equivalent and consumes both endpoints of every arc it processes. | infill-linker maintainer | TASK-311 | Open — 2026-08-02: residual filed for the deliberate clamp substitute. |
| DEV-112 | 2026-08-02 | `modules/core-modules/infill-linker/src/orchestrate.rs` (`line_width_to_spacing`) / OrcaSlicer `Fill::group_fills` (`Flow::rounded_rectangle_extrusion_spacing`) | Medium | **The percent base has a width-input residual, not a spacing-formula residual.** PnP computes it as `line_width_to_spacing(line_width_mm, layer_height_mm)`, exactly canonical `Flow::rounded_rectangle_extrusion_spacing`, but its `line_width_mm` is the module's generic `line_width` key. Canonical `group_fills` takes `layerm.region().flow(*layer.object(), frInfill, ...).spacing()`, a per-role infill flow width that can differ under auto or percent width resolution. Both inputs are extrusion-width scale, so this is small; the formula matches and this is not the 5.6x line-spacing error. | infill-linker maintainer | TASK-311 | Open — 2026-08-02: residual is the width input selected for the percent base. |
| DEV-114 | 2026-08-02 | `modules/core-modules/infill-linker/src/orchestrate.rs` (`config_float(view.config(), "infill_density")`) / `modules/core-modules/infill-linker/infill-linker.toml` / `crates/slicer-ir/src/resolved_config.rs` (`ResolvedConfig::to_config_map`) / `rectilinear-infill` / `gyroid-infill` / `lightning-infill` manifests | Medium | **`infill_density` is deliberately left an undeclared dead read (the single exception AC-20 allow-lists); the deferral is disclosed, not a parity gap.** Fact (a): `orchestrate.rs` reads it via `config_float(view.config(), "infill_density")` while `infill-linker.toml` declares it nowhere, so `ConfigView::from_declared` drops it and `unwrap_or(0.2)` is the value on every slice. Fact (b): the key carries a fraction-vs-percent conflict between the code and three manifests — `rectilinear-infill`, `gyroid-infill` and `lightning-infill` declare `default = 20.0, min = 0.0, max = 100.0`, a percent, while the code is consistently a fraction: `ResolvedConfig` carries `infill_density: f32 = 0.2`, `to_config_map` emits `Float(0.2)`, `rectilinear-infill`'s fallback is `_ => 0.2`, and `crates/slicer-sdk/src/config_resolution.rs` tests assert fractions. The harm is a bounds/documentation inconsistency in those three manifests, NOT a live 100x spacing bug — their code reads the fraction correctly. The consequence for the linker is the reason to defer: declaring the key would make it honour a user-set density where today it is pinned at `0.2`, moving `sparse_spacing_mm` and hence `remove_short_polylines` and the infill-overlap offset on every non-default-density slice — a behaviour change that should land with the tree-wide unit reconciliation rather than inside an anchor-length packet. `infill_density` does not feed `anchor_base_mm`, so leaving it dead cannot affect this packet's parity claim. | infill-linker maintainer | TASK-311 | Open — 2026-08-02: deferral disclosed; declaring the key is a behaviour change deferred to the tree-wide unit reconciliation. |
| DEV-093 | 2026-07-25 | `crates/slicer-core/src/medial_axis.rs` `medial_axis` (and every other `boostvoronoi` caller: `crates/slicer-core/src/voronoi.rs` `voronoi_from_segments`, `crates/slicer-core/src/algos/paint_segmentation/voronoi_graph.rs`) via `boostvoronoi 0.12.1` -> `cpp_map 0.2.0` | High | **Root-caused 2026-07-25: `medial_axis` is not a pure function of its input, because the Voronoi beach line is a skip list whose node levels come from an OS-seeded `ThreadRng`.** `boostvoronoi 0.12.1` stores its beach line in a `cpp_map::skiplist::SkipList` (`beach_line.rs`); `cpp_map 0.2.0` picks each node's level with `rand::prelude::ThreadRng` (`THREAD_RNG` in its `skiplist_impl`, consumed by `random_level` / `coin_flip`), which is OS-seeded, advances on every insert, and exposes no seeding hook. The beach-line comparison predicate is not a strict weak ordering under floating point, so which node a search lands on depends on the random level structure. Near-degenerate inputs therefore resolve differently **from one call to the next inside a single process** - so this is not a per-process hash seed, and the original `HashMap`-iteration hypothesis is **disproven**. Two knock-on effects are visible: the build sometimes trips `assertion failed: fpv.is_finite()` in boostvoronoi's `robust_fpt`, which `medial_axis`'s `catch_unwind` converts to an empty result (zero polylines), and when it does not panic it can still emit a *different* polyline. **How it reaches G-code:** `com.core.classic-perimeters` wins the `perimeter-generator` claim over `com.core.arachne-perimeters` by first-winner dedup, and its `detect_thin_wall` branch feeds each sub-nozzle-width protrusion to the `medial_axis` host service; `ExtrusionRole::ThinWall` emits as `;TYPE:Inner wall` (`crates/slicer-gcode/src/emit.rs`). A flipped call is exactly one sliver inner-wall loop appearing or disappearing. **Evidence (0.2 mm benchy, release, `RAYON_NUM_THREADS=1`):** prepass output is structurally **identical** across runs - the `PNP_DUMP_PREPASS_IR` byte diff on `region_map` / `support_geometry` is pure `HashMap` serialisation order, and a structural compare showed 0 differing keys and 0 differing geometry. Per-stage arena fingerprints then showed the first divergence is always `Layer::Perimeters` / `com.core.classic-perimeters`, on a **byte-identical** `SliceIR` input, at 5 layers (36, 106, 107, 124, 158). Fingerprinting the host `medial_axis` boundary showed exactly 5 differing calls, each with an identical input hash and a different output - 4 of them `n=0` vs `n=1`. A standalone replay of the captured inputs varied within a single process across 10 consecutive identical calls. **Reproducer:** `crates/slicer-core/tests/medial_axis_determinism_tdd.rs` (`medial_axis_is_deterministic_across_repeated_calls`), `#[ignore]`d while this row is open; it is the acceptance criterion for the fix. Must be run in **release** - in debug every call panics internally and is caught, so the flip is masked and the test passes for the wrong reason. **Fix not yet applied.** `cpp_map 0.2.0` and `boostvoronoi 0.12.1` are the newest published versions, so there is no upstream fix to take. The entropy is a single line (`THREAD_RNG` in `cpp_map`'s `skiplist_impl`); removing it means vendoring `cpp_map` (~6.2k lines, MIT OR Apache-2.0) behind a `[patch.crates.io]` and swapping the `ThreadRng` for a fixed-seed PRNG - skip-list level randomness is only a balancing heuristic, so a deterministic PRNG preserves the expected O(log n) behaviour. That is a repo-level dependency decision and is left for a follow-up. **Consequence while open:** whole-output G-code byte comparison cannot attribute a behaviour change to a code change; any diff of roughly this shape and size is inside the noise band. The earlier partial mitigations remain correct but insufficient - the bucket-key sort in `merge_collinear_overlapping_simple` (`crates/slicer-core/src/medial_axis.rs`) removed one *input-order* entropy source, and the gap-fill width-band pre-filter in `modules/core-modules/classic-perimeters/src/lib.rs` removed one class of degenerate input; neither touches the RNG itself, and the thin-wall branch has no equivalent pre-filter. | slicer-core maintainers | TBD | Open - 2026-07-25: root cause identified and reproduced; fix requires vendoring `cpp_map` |
| DEV-094 | 2026-07-25 | `crates/slicer-sdk/src/host.rs` host-service wrappers vs the `slicer:common/host-services` interface (`crates/slicer-schema/wit/deps/common.wit`); ADR-0033's bridge pattern | Medium | **Phantom host bridge: most of `host-services` is declared in WIT and implemented host-side, but the guest-callable SDK wrapper never calls the import — it silently runs a native fallback that cannot work inside a guest.** ADR-0033 defines the bridge as a three-part shape (WIT decl -> host impl -> `cfg`-split SDK wrapper whose `wasm32` arm marshals the import). Only two wrappers in `crates/slicer-sdk/src/host.rs` actually carry that `#[cfg(target_arch = "wasm32")]` arm: `medial_axis` and `generate_arachne_walls`. Thirteen others had no `wasm32` arm at all and fell through to a native/local path: the six `log*` fns (`log`, `log_trace`, `log_debug`, `log_info`, `log_warn`, `log_error`), the three mesh queries (`raycast_z_down`, `surface_normal_at`, `object_bounds`), the three polygon ops (`clip_polygons`, `offset_polygons`, `simplify_polygon`), and `now_us`. **Partially remediated in the same session (2026-07-25):** the `log*` family is now bridged — `log` gained a `wasm32` arm calling the WIT import, and the five level-specific wrappers delegate to it — so the logging channel described below is live end to end. **Still unbridged: seven** — the three mesh queries, the three polygon ops, and `now_us`. Do not quote the bridged/unbridged counts without re-deriving them; grep `crates/slicer-sdk/src/host.rs` for `target_arch = "wasm32"` and compare against the `slicer:common/host-services` func list. **Why it is invisible:** guests build for `wasm32-unknown-unknown` (the target passed by `build_one_inner` in `xtask/src/build_guests.rs`), which has no WASI, so every fallback is inert rather than erroring — nothing fails to compile and nothing raises at runtime. `log` falls back to `eprintln!` on a target with no stderr; `now_us` falls back to a `std::time::Instant` process-start baseline that `wasm32-unknown-unknown` cannot provide; the mesh queries consult a thread-local `MeshSource` whose only installers are test-side (`test_support::install_mesh_source` from `crates/slicer-sdk/tests/host_wrappers_tdd.rs`, `crates/slicer-macros/tests/module_test_tdd.rs`, and `MockHost::install` in `crates/slicer-sdk/src/test_support/mock_host.rs`) — there is no production installer. **Split the class by consequence.** The three polygon ops are *correct but misplaced*: `slicer_core::polygon_ops` is backed by `clipper2-rust`, which compiles to wasm32, so the fallback computes the right answer, merely single-threaded inside the sandbox — exactly what ADR-0049 recorded for `offset-polygons` ("the `offset-polygons` import declared in `slicer:common/host-services` exists but the SDK never calls it"). This row generalises that one-function finding to the whole interface. The other ten are *phantom*: there is no working in-guest implementation behind them at all. **Live consequence today — module logging is a dead channel.** The host end is fully wired: `HostServices::log` in `crates/slicer-wasm-host/src/host.rs` pushes into `log_messages`, and `forward_module_logs` (`crates/slicer-wasm-host/src/dispatch.rs`) re-emits each entry through the `log` crate under target `slicer_module::<module-id>`. The guest end never calls the import, so nothing ever arrives. Seven production `host::log_warn` call sites exist — three in `modules/core-modules/classic-perimeters/src/lib.rs`, four in `modules/core-modules/arachne-perimeters/src/lib.rs` (every occurrence in `modules/core-modules/support-planner/src/lib.rs` is inside its `#[cfg(test)]` module, as are its `host::offset_polygons` calls). Two of the classic-perimeters ones are the *only* report that a thin-wall or gap-fill `medial_axis` call failed (`medial-axis-failed region=... fixture=thin_wall` / `fixture=gap_fill`). **Cross-reference DEV-093** (open): `slicer_core::medial_axis::medial_axis` wraps its boostvoronoi interaction in `catch_unwind` and converts a caught panic into `Ok(vec![])` rather than `Err`, so that diagnostic is suppressed twice over — even once the log bridge works, a caught panic still produces no log line, because the guest never sees an `Err` to report. **Latent trap, explicitly not a proven live bug.** `modules/core-modules/layer-planner-default/src/lib.rs` reads each object's height via `object_height(config, obj_id)` and falls back to `host::object_bounds(obj_id)`, then `.unwrap_or(0.0)`; a non-positive height `continue`s, silently dropping that object from the layer plan. In a guest `object_bounds` returns `Err(HostUnavailable)`, so the fallback yields `0.0` and the object disappears with no error anywhere. It is not reachable through the CLI today: both `run_slice` and the prepass context builder in `crates/slicer-runtime/src/run.rs` seed `object_height:<id>` into `config_source` for every object carrying a `world_z_extent`, and `object_world_z_extent` (`crates/slicer-model-io/src/loader.rs`) yields `Some` for any mesh with vertices — so the config path wins and the fallback is not taken. A 0.2 mm benchy slice producing the expected 240 layers was reported by the filing session as consistent with this; it was **not** re-measured for this row, and it was **never** demonstrated that the fallback is taken at all. The real exposure is an embedder that builds `MeshIR` without seeding `object_height:*`: it gets a silently empty layer plan instead of an error. **Risk rated Medium, justified:** not High, because no shipped path is known to miscompute — the one silent-wrong-answer consumer is preempted by host-seeded config, and the polygon ops compute correctly (if slowly) in-sandbox. Not Low, because the failure mode is silence in both directions: the entire guest->host diagnostic channel is dead in production while looking wired end to end, which directly obstructs diagnosing DEV-093, and the wrappers emit no compile-time or runtime signal that a guest is running a native fallback — anyone adding a new guest caller of a mesh query or `now_us` inherits a wrong answer with no warning. | slicer-sdk maintainers | TBD | Open - 2026-07-25: characterised, not fixed. Minimal fix is a `#[cfg(target_arch = "wasm32")]` arm on each affected wrapper per ADR-0033; the stronger fix makes a missing bridge a compile error on wasm32 instead of a silent native fallback |
| DEV-100 | 2026-07-31 | `modules/core-modules/machine-gcode-emit/src/lib.rs` `substitute_placeholders` (stage `PostPass::GCodePostProcess`) / `docs/15_config_keys_reference.md` | Medium | **Residual of DEV-085's user-facing half: eight `[key]` macros that an older `docs/15_config_keys_reference.md` advertised do not resolve, because PnP has no config key to bind them to.** `[bed_temperature]`, `[filament_type]`, `[tool_count]`, `[layer_count]`, `[print_time_estimate_s]`, `[x_max]`, `[y_max]` and `[z_max]` are emitted verbatim as literal bracketed text, with one aggregated warning naming them, whereas canonical resolves all eight. Packet `186-custom-gcode-placeholder-engine` originally made an unresolved placeholder a hard slice error; that policy was **reverted before landing** on the repo owner's ruling that it is architecturally wrong for a modular slicer — a module's `ConfigView` is scoped to its own manifest, so a key this module cannot resolve may legitimately be owned by a module that is not loaded, and aborting the slice breaks composition. Measured: the fatal policy broke three e2e tests across two real OrcaSlicer-authored 3MF fixtures, which reference `[first_layer_bed_temperature]`, `[initial_tool]` and `[max_layer_z]`. The residual harm is therefore the original one — bracketed literal text can reach a printer — now at least accompanied by a warning. **Measured canonical counterparts** (verified in this checkout unless noted): `[bed_temperature]` is a real canonical placeholder variable declared `coInts` in `TemperaturesConfigDef`, whose value comes from `get_bed_temp_key((BedType)curr_bed_type)` — a six-way dispatch over `supertack_plate_temp` / `cool_plate_temp` / `textured_cool_plate_temp` / `eng_plate_temp` / `hot_plate_temp` / `textured_plate_temp`. PnP models no bed-type dispatch at all, so it cannot be aliased onto `bed_temperature_initial_layer_single` without silently substituting the wrong number for five of the six bed types. `[filament_type]` is a real canonical `coStrings` option in `PrintConfigDef::init_fff_params`; it has zero occurrences anywhere under `crates/` or `modules/`. `[tool_count]` does not exist canonically; the nearest is `num_extruders` (`OtherSlicingStatesConfigDef`, coInt). `[layer_count]` does not exist; the nearest is `total_layer_count` (`PrintStatisticsConfigDef`, coInt) — PnP does carry the value, on `GCodeIR.metadata.layer_count`, but a `PostPass::GCodePostProcess` module sees only a `ConfigView` and cannot read it. `[print_time_estimate_s]` does not exist; the nearest is `print_time_sec` (`PrintStatisticsConfigDef`, coString). `[x_max]` and `[y_max]` do not exist; the nearest is `print_bed_max` (`DimensionsConfigDef`, coFloats, elements 0 and 1). `[z_max]` does not exist; the nearest is `printable_height`, or `max_layer_z` for the achieved top layer. **Domain asymmetry (the reason canonical accepts templates PnP now rejects):** canonical resolves a placeholder against a local `DynamicConfig` override layered over the entire print config plus roughly 119 explicit `placeholder_parser().set(...)` global assignments, whereas PnP's resolution domain is one module's manifest-declared key set plus its alias table. **Failure-handling divergence:** canonical's `GCode::placeholder_parser_process` catches the parser error, splices an inline failure marker into the output and lets the export continue, deferring a single aggregated throw to `GCode::check_placeholder_parser_failed` — so canonical ultimately **fails** the export. PnP adopts the aggregation but not the failure: it emits one `log_warn` naming every unresolved key and every injection point, leaves the bracketed text in place, and completes the slice. This is a deliberate divergence, not an oversight — see the modularity rationale above. It means a template canonical rejects will slice successfully in PnP, producing G-code containing literal `[key]` text. (All canonical facts in this row were measured. The `PrintConfigDef`/`TemperaturesConfigDef`/`PrintStatisticsConfigDef`/`OtherSlicingStatesConfigDef`/`DimensionsConfigDef` facts were measured in this repo's `OrcaSlicerDocumented/`; the `GCode.cpp` facts — `placeholder_parser_process`'s catch-record-and-continue, `check_placeholder_parser_failed`'s single aggregated `Slic3r::PlaceholderParserError` throw, and the assignment counts — were measured in a full sibling checkout, because this repo's mirror carries no `GCode.cpp`. Two refinements from that measurement: dedupe by template name is performed by `placeholder_parser_process`'s insert-if-absent guard rather than by `check_placeholder_parser_failed`, and the 119 figure counts `placeholder_parser().set(` specifically (110 `this->` + 9 `gcodegen.`) — a further 11 `m_placeholder_parser.set(` calls exist — 8 in `PrintApply.cpp` and 3 in `SLAPrint.cpp` — so the library-wide explicit-set total is 130.) Closing this needs either real PnP config keys for the print-statistics and bed-geometry quantities, or a decision to declare these eight permanently unsupported and document the warned verbatim passthrough as intended behaviour. | core-modules maintainers | TBD | Open — 2026-07-31: eight advertised macros have no canonical-equivalent PnP key to bind them to and pass through as literal bracketed text with a warning; filed as the residual of packet 186, which fixed the engine (char-boundary-correct literals, list-valued config resolution, `nozzle_diameter` declared, `first_layer_temperature` aliased, warn-on-unresolved) but could not supply the missing values. The fatal-on-unresolved policy that packet originally carried was reverted before landing as architecturally wrong for a modular slicer |
| DEV-106 | 2026-08-01 | `Point3WithWidth::overhang_distance_mm` / `SurfaceClassificationIR::prev_layer_boundaries` / `expolygon_to_path3d` / Arachne per-vertex overhang stamping | Low | **Packet 193 — canonical's `boundary_offset` is `0.5 * path.width`, a per-path constant; PnP stamps the signed overhang distance per point and resolves the offset from that point's own `width`.** Classic perimeters use one width per loop, so the values are identical there; arachne variable-width loops vary along the loop, bounded by the loop's width variation. Packets 190 and 191 read the already-normalised field rather than re-deriving the offset for interpretation. | slicer-core + perimeter-module maintainers | packet 193 / TASK-314 | Open — 2026-08-01: recorded as the carrier's bounded canonical boundary_offset divergence; no consumer or G-code change |
