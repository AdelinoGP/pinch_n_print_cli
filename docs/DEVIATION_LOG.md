# Deviation Log

Use this registry for intentional deviations from architecture docs.

> **Single source of truth.** This file is authoritative for deviation status. A
> deviation is **open** unless its `Status` column begins with `Closed`. Status
> summaries elsewhere (`docs/07_implementation_status.md` Open Deviation Map,
> `docs/14_deviation_audit_history.md`) are non-authoritative views generated
> from this table by `cargo xtask check-deviations`; when they disagree, this
> file wins. Do not hand-edit those views.

| ID | Date | Affected Section | Risk | Rationale | Mitigation Owner | Target Close | Status |
| --- | --- | --- | --- | --- | --- | --- | --- |
| DEV-009 | 2026-04-15 | 07_implementation_status.md Phase H / 01_system_architecture.md live pipeline expectations | High | The Phase H Benchy run is only partially correct on the live path: `.gcode` is produced, but top/bottom fill, supports, seam placement, retract/unretract behavior, and OrcaSlicer-compatible GCode comment metadata required for native preview visualization are still missing or incomplete. **Remediation progress:** packet 52_gcode-feedrate-emission has implemented per-role feedrate emission covering all ExtrusionRole variants (13 named + Custom) with 26 speed config keys, and F-tokens are now resolved on every print and travel move via resolve_feedrate (skirt→model and wipe-tower travel inserts also carry a resolved travel feedrate). **Progress 2026-05-14 (packet 54):** skirt-brim emit subset closed (TASK-142a — `lib.rs` defaults fix + 5 tests); relative-E subset closed (TASK-183 — M82/M83 toggle + 10 tests). Cooling subset closed via packet 53 (TASK-154); feedrate subset closed via packet 52 (TASK-153). DEV-009 fully closed for these subsets via packets 52+53+54. **Progress 2026-05-15 (packet 57):** packet 57 (`57_overhang-speed`) implements the per-quartile wall slowdown end-to-end: WIT field `overhang-quartile`, Rust mirror, `LinesDistancer2D` classifier, `resolve_feedrate` dispatch, and pipeline wire-in. DEV-009 is partially remediated; the remaining open items (smoothed-speed mode, six-band schedule parity, `ADD_INTERSECTIONS` mid-segment splitting, curled-edge slowdown) are deferred to follow-up packets. Remaining open DEV-009 items: seam placement end-to-end (`benchy_live_path_contains_resolved_seam_evidence_before_emit`), retract/unretract emission, top/bottom surface G-code evidence (tracked under DEV-033). 2026-05-18: doc-side gaps for packet-34 retraction mode, packet-52 per-role speeds, packet-53 cooling fan, packet-54 relative-E + skirt-brim, packet-55 header/thumbnail, packet-57 overhang quartile, packet-58 toolchange-purge, packet-59 machine start/end, packet-60 precision now documented in `docs/02_ir_schemas.md`, `docs/03_wit_and_manifest.md`, `docs/04_host_scheduler.md`, `docs/05_module_sdk.md`, `docs/01_system_architecture.md`, `docs/10_glossary_and_scenario_traces.md`, `docs/16_slicer_report.md`. See `docs/AUDIT_2026-05-18_doc_gaps.md`. **2026-06-29 — packet 126 closure (colour-parity subset):** packet `126_mmu-painted-cube-parity` closes the MMU colour-parity subset on the live `cube_4color.3mf` path. Retired the boostvoronoi cell-decomposition shortcut (`cells_to_expolygons_by_color`) and the corner-displacement post-passes in `voronoi_graph.rs` (commit `3e3a9ead`) in favour of the faithful `extract_colored_segments` leftmost-arc walk (commits `721f19ec` + `0c41a42a`, OrcaSlicer `MultiMaterialSegmentation.cpp:494-566` parity, cross-referenced to ADR-0013). Closes G1 (top/bottom diagonal-seam sliver), G2 (spurious inner walls on detail faces), G3 (first-layer perimeter colour from bottom face), G7 (default face colour reads the 3MF object base extruder), G8 (subdivided horizontal face skips tool-0 default projection). Verifies the S1–S3 landed fixes: G4 internal solid infill (14 `;TYPE:Internal solid infill` segments emitted), G5 volumetric flow (commit `17bb59bd` formula; emit test green), G6 shell inset (`propagate_top_bottom` test green), RC1 palette (`#FF9B00;#02BF06;#1800F2;#EC0006` in header), RC3 side colours (4 distinct `T<n>` in gcode). Adds the Bug #2 post-close fix: small-region drop (`MIN_REGION_AREA = 20_000_000` units² ≈ 0.2 mm²) at the tail of `execute_paint_segmentation` to suppress top-ironing sliver regions on the side-face harvest (OrcaSlicer `small_region_threshold` parity). Locks the Bug #1 leading-T-change consumer-side contract with two regression tests in `crates/slicer-gcode/tests/emit_tool_guard_tdd.rs`; on review the upstream "Bug #1 fix was missing `tool_index` on `OrderedEntityView`" diagnosis was incorrect — the live `dispatch::project_ordered_entities_from` (`crates/slicer-wasm-host/src/dispatch.rs:1486`) already populates `tool_index: entity.tool_index`, and `DefaultGCodeEmitter::emit_gcode` at `crates/slicer-gcode/src/emit.rs:319-343` already emits the leading `ToolChange { after_entity_index: u32::MAX, from: <prev>, to: <first entity tool> }` when `first_entity.tool_index != current_tool`. If the §REMAINING #1 visual symptom persists in the user's viewer, it lives in a different location than the handoff indicated (see `docs/17_agent_debugging.md` and the `debug-pipeline` skill). Clippy `--workspace --all-targets -- -D warnings` clean. Outstanding DEV-009 subsets (seam placement end-to-end `benchy_live_path_contains_resolved_seam_evidence_before_emit`, retract/unretract emission under TASK-120c/D-DEV-033) remain open and are tracked under TASK-120c — packet 126 closed only the colour-parity / painted-MMU subset. **Verified 2026-07-03:** the seam-placement, retract/unretract, and top/bottom-surface items named as "remaining open" above are now closed — the old `benchy_*` tests were retired (Packet 90) and have renamed, passing successors in `crates/slicer-runtime/tests/e2e/slice_end_to_end_tdd.rs` (`wedge_live_path_contains_resolved_seam_evidence_before_emit`, `wedge_default_emits_retract_commands`/`wedge_default_emits_unretract_commands`/`wedge_default_retract_unretract_counts_are_equal`, `wedge_gcode_contains_top_and_bottom_surface_evidence`), all green (see DEV-033, now closed). This row stays open only for the sub-items this verification did not check: smoothed-speed mode, six-band schedule parity, `ADD_INTERSECTIONS` mid-segment splitting, and curled-edge slowdown. | slicer-host + core-modules maintainers | TBD | Open — 2026-07-03: seam placement, retract/unretract, and top/bottom-surface evidence confirmed closed (see DEV-033); remaining open items are smoothed-speed mode, six-band schedule parity, ADD_INTERSECTIONS splitting, curled-edge slowdown (not yet re-verified) |
| DEV-010 | 2026-04-15 | 11_operational_governance_and_acceptance_gate.md / 12_architecture_gate_metrics.md / 07_implementation_status.md | Medium | The implementation is past MVP, but the Architecture Acceptance Gate has not yet been evaluated and the remaining governance closure work is still open. | project maintainer | TBD | Open |
| DEV-011 | 2026-04-15 | 01_system_architecture.md §WASM Instance Pools / 04_host_scheduler.md §Phase 4 | High (Partial) | Typed WASM dispatch linking is implemented end to end: `WasmRuntimeDispatcher` instantiates typed component-model guests for all four runner families, wires imports through the typed linker path, calls stage exports, and commits outputs back into pipeline state. All 17 core-module `.wasm` artifacts are real components and all four prepass stages are dispatched. This row remains partial only because some stage surfaces still cannot carry full semantics after dispatch: postpass GCode command bodies do not cross the boundary (DEV-006), finalization modules still keep live logic in legacy `process()` instead of `run_finalization()` (DEV-013), and prepass segmentation macro bindings still have hollow inputs or missing drain-back due to SDK↔WIT shape mismatch (DEV-025). Dispatcher instantiation and stage routing are no longer the blocker. **Verified 2026-07-03:** all three cited sub-gaps are resolved or moot — postpass GCode commands cross the boundary via a real `&[GCodeCommand]` list (`postpass.rs`), `SkirtBrim`/`WipeTower::run_finalization()` are full implementations (not legacy `process()` stubs), and `PrePass::MeshSegmentation` was retired entirely while `PrePass::PaintSegmentation` became a host built-in — mooting the WIT shape-mismatch question. Nothing remains blocking full-semantics dispatch. | slicer-host + core-modules maintainers | TBD | Closed — 2026-07-03 verification: all three cited stage-surface gaps resolved or architecturally obsoleted; WASM dispatch is fully live end to end |
| DEV-017 | 2026-04-16 | 02_ir_schemas.md §IR 10 LayerCollectionIR / 03_wit_and_manifest.md §"Path Optimization Output Contract" §"z-hops" | Low | **Verified by AUDIT-11 (2026-04-16):** Historical audit notes that had claimed `LayerCollectionIR.z_hops` and annotation passthrough were unwired are **stale** — these features are now fully implemented. Evidence: (1) `push-z-hop` method exists in on-disk `ir-types.wit:107`, macro inline WIT `lib.rs:2222`, and host inline WIT `wit_host.rs:282`. (2) Host commit path at `dispatch.rs:1378-1436` validates z-hop calls (bounds check, finite/positive height) and routes accepted z_hops to `arena.push_deferred_z_hop()`. (3) `LayerArena` has `deferred_z_hops: Vec<ZHop>` (`blackboard.rs:293`) with `push_deferred_z_hop` / `take_deferred_z_hops` (`blackboard.rs:452-458`). (4) Layer executor drains deferred z_hops into `LayerCollectionIR.z_hops` (`layer_executor.rs:393`). (5) `DefaultGCodeEmitter` consumes z_hops and renders lift+return travel moves (`gcode_emit.rs:155-175`). (6) Five tests prove the full pipeline: `path_optimization_commit_routes_z_hops_to_deferred_queue`, `path_optimization_z_hop_rejects_out_of_bounds_index`, `path_optimization_z_hop_rejects_invalid_hop_height`, `path_optimization_end_to_end_populates_z_hops`, `path_optimization_end_to_end_emitter_renders_z_hops` (all in `dispatch_tdd.rs`, all pass). Similarly, the old claim that comment/raw GCode commands were silently dropped at commit is false — `push-comment` and `push-raw` are accepted at PathOptimization (`dispatch.rs:1366-1377`), routed via `push_deferred_annotation` into `LayerAnnotation` records, drained into `LayerCollectionIR.annotations` (`layer_executor.rs:391-392`), and rendered by the GCode emitter (`gcode_emit.rs:139-152`). Verified by `path_optimization_commit_routes_comment_and_raw_to_deferred_annotations` test. **What remains deferred:** (a) guest reordering of `ordered_entities` — no WIT `layer-collection-builder` resource exists; reordering is reserved for a future step per docs/03 §680-687; (b) richer anchoring for PathOptimization overrides — currently anchored at "after last entity" only. **Verified 2026-07-03:** sub-item (a) is now closed for real — the WIT `layer-collection-builder` resource (`crates/slicer-schema/wit/deps/ir-types.wit`) exposes both `get-ordered-entities()` and `set-entity-order(...)`, consumed end to end by `path-optimization-default`. Sub-item (b) is no longer an open TODO: `docs/adr/0020-layer-stage-commit-as-per-stage-enum.md` documents anchor-at-end as a deliberate, settled decision — adding richer anchoring today would be speculative machinery for a capability that doesn't exist. | slicer-host maintainer | TBD | Closed — 2026-07-03 verification: entity reordering fully wired via layer-collection-builder; remaining anchor-at-end behavior is a settled ADR-0020 decision, not an open gap |
| DEV-020 | 2026-04-16 | 07_implementation_status.md §Phase G | Low | **Verified by AUDIT-14 (2026-04-16):** Phase G still overstates completion: the live wiring does use `WasmRuntimeDispatcher` for all four stage runners, but `main.rs:20-84` still retains all four dead `Noop*Runner` structs and the source/docs cleanup was never finished. The stale line references from the audit were discarded, but the underlying issue remains: Phase G is marked complete while dead fallback code is still present. `docs/07_implementation_status.md` tracks the cleanup under `TASK-139`. **Verified 2026-07-03:** the production binary (`crates/pnp-cli/src/main.rs`, post-Packet-69 rename) has zero `Noop*Runner` references. The only remaining hits are legitimate test-double fixtures (e.g. `crates/slicer-runtime/tests/integration/runtime_wiring_tdd.rs`) — a normal test pattern, not dead production code. | slicer-host maintainer | TBD | Closed — 2026-07-03 verification: dead Noop*Runner structs removed from production; remaining hits are legitimate test doubles |
| DEV-023 | 2026-04-16 | 05_module_sdk.md §"PathOptimization" / modules/core-modules/path-optimization-default/src/lib.rs | Low | **Verified by AUDIT-17 (2026-04-16):** The PathOptimization core module is still a minimal slot-filler; no material feature parity progress has been made. Evidence: (1) `path-optimization-default/src/lib.rs:57-84` — `run_path_optimization` only emits a deterministic comment marker (`; path-optimization layer N regions=R entities=E`) via `push_comment`. It does NOT implement: travel optimization (nearest-neighbor/TSP), seam scheduling consumption, multi-tool ordering, z-hop emission, cooling/fan overrides, multi-object ordering, bridge/overhang reordering, or wipe/brim coordination. (2) Module reads only `PerimeterRegionView` — does not access `ordered_entities` directly. (3) `layer_executor.rs:366-386` — fallback path still assembles from arena slots without path optimization output. (4) Host-side infrastructure IS now in place: z_hop routing (dispatch.rs:1378-1410), comment/raw passthrough (dispatch.rs:1366-1377), and GCode emitter rendering (gcode_emit.rs:139-175) are all wired. But the module itself has not grown new capabilities. (5) The sub-claims about fallback coverage, user-visible markers, and PerimeterRegionView-only reads remain accurate. 2026-05-18: doc-side gap for packet-33 module migration and packet-32 layer-collection-builder resource now documented. See `docs/AUDIT_2026-05-18_doc_gaps.md` rows G-05 / G-06. **Verified 2026-07-03:** `path-optimization-default/src/lib.rs` (695 lines) has grown real capability since this row was filed — `nearest_neighbor_permutation` (greedy NN travel ordering), `group_then_nearest_neighbor` (tool-index clustering + role-priority grouping + per-tool NN ordering + `ToolChangeRecord` emission), and config-driven `retract_mode`/`travel_z_hop`/`wall_sequence` handling. `docs/07_implementation_status.md` TASK-152 and sub-tasks 152a–152g closed 2026-04-26–29 via packets 18–20, all citing this row. Minor residual: `role_group()` buckets `BridgeInfill` with ordinary infill rather than giving it distinct priority, so TASK-152e's "role-aware bridge reordering" is thinner than its title implies — not enough to keep this row open. | core-modules maintainer | TBD | Closed — 2026-07-03 verification: module has real travel/tool-change/retract/z-hop/wall_sequence behavior; matches packets 18-20 closure of TASK-152 |
| DEV-026 | 2026-04-16 | 04_host_scheduler.md §"Phase 3 DAG Validation" / 11_operational_governance_and_acceptance_gate.md §2 "Compatibility Policy" / 12_architecture_gate_metrics.md §"Compatibility" | High | Startup validation still misses several compatibility and governance checks beyond the WIT-source/version problem tracked in DEV-014. The remaining unique gaps are: (1) `min_host_version` is parsed but never compared at startup; (2) manifest-schema validation cannot currently surface a meaningful `Schema` failure path because missing schema falls back to defaults and parse failures collapse into TOML errors; (3) memory, full-slice time, and host-call budgets from docs/12 have no runtime measurement, diagnostics, or benchmark evidence. Layer-count and region-cap bounds are already enforced. Implement the host-side `wit_world` allowlist as part of the same validation pass used to close DEV-014 rather than tracking that subtask separately here. **Verified 2026-07-03:** gap (2) is now closed — `LoadErrorKind::Schema` (`crates/slicer-scheduler/src/manifest.rs`) is a real, distinct variant with specific field/message context, proven by `region_split_manifest_tdd.rs`; malformed manifests no longer collapse into generic TOML errors. Gaps (1) and (3) remain genuinely open: `min_host_version` is still stored with a getter but has zero production call sites comparing it against anything at startup; runtime-budget instrumentation now exists (`AccountingAllocator`, per-call `wasm_initial_bytes`/`wasm_peak_bytes` sampling, `crates/slicer-runtime/benches/{pipeline,per_stage}.rs`) but the actual gate-evidence fixtures docs/12 requires (500-layer RSS bound, 50-layer full-slice-time bound) still don't exist — `docs/12_architecture_gate_metrics.md` has its own `<!-- VERIFY: fixtures not materialised -->` comment confirming this. | slicer-host maintainer | TBD | Open — 2026-07-03: narrowed to (1) min_host_version never compared at startup and (3) gate-evidence fixtures still unmaterialised; (2) manifest Schema error path is closed |
| DEV-030 | 2026-04-16 | 07_implementation_status.md §"Active Remediation Backlog" / 11_operational_governance_and_acceptance_gate.md | Medium | The planning and governance docs no longer present a single accurate closure plan. The retired audit registry had preserved stale blocker summaries and a mostly linear MVP sequence even though live state now breaks into parallel workstreams, and `docs/07_implementation_status.md` still carried some lagging closure notes. This row is the canonical documentation/planning drift entry. The current fix order is: Phase 1 enforcement/cleanup (`DEV-004`, `DEV-020`); Phase 2 feature completion (`DEV-009`, `DEV-013`, `DEV-015`, `DEV-023`, `DEV-025`, plus DEV-024 if Python is meant to be live); Phase 3 governance closure (`DEV-010` and the remaining runtime-validation evidence in DEV-026). DEV-029 is merged here. **Verified 2026-07-03:** the structural complaint is resolved — `docs/07_implementation_status.md`'s Open Deviation Map is now machine-generated by `cargo xtask check-deviations` and CI-gated (`.github/workflows/ci.yml` runs `check-deviations --check` on every build), so doc 07 can no longer silently drift from this file. This row's own "current fix order" citing DEV-004/013/015/025/024 as open Phase-2 blockers was itself the last piece of drift — all five were closed long ago and their rows were removed from this log during the 2026-07-02/03 cleanup. | slicer-host maintainer | TBD | Closed — 2026-07-03: doc07/DEVIATION_LOG.md drift is now structurally prevented by the CI-gated generator; this row's own fix-order text was the last piece of drift |
| DEV-033 | 2026-04-30 | crates/slicer-host/tests/benchy_end_to_end_tdd.rs / 07_implementation_status.md §DEV-009 | Medium | **Packet 31a-REV2 closure — pre-existing benchy TDD-red tests.** Five tests in `crates/slicer-host/tests/benchy_end_to_end_tdd.rs` were failing at packet 31a-REV2 closure and are NOT regressions introduced by this packet. They are pre-existing TDD-red gates for unimplemented features that were masked by a WASM linker error (stale WIT in `paint-segmentation/wit-guest/src/lib.rs`) now fixed in scope-fix #2 and tracked separately by packet 32. Failing tests: (1) `benchy_live_path_contains_resolved_seam_evidence_before_emit` — seam feature family; seam planning is implemented (TASK-159) but the benchy evidence assertion has not been closed; (2) `benchy_gcode_contains_balanced_retract_and_unretract_pairs` — retract feature family; retract emission is not yet implemented on the live path; (3) `benchy_gcode_contains_top_and_bottom_surface_evidence` — top-surface feature family; top/bottom surface detection is not yet implemented on the live path; (4) `benchy_prepass_seam_plan_matches_live_outer_wall_start` — seam feature family; live seam injection into outer-wall start not yet wired end-to-end; (5) `benchy_feature_evidence_failures_name_the_missing_family` — meta-test locking the names of the four missing feature families. All five belong to DEV-009. Current state: `cargo test -p slicer-host --test benchy_end_to_end_tdd`. **Verified 2026-07-03:** `crates/slicer-host/tests/benchy_end_to_end_tdd.rs` no longer exists — retired by commit `5fbb786d` (Packet 90 benchy.stl→regression_wedge.stl retirement). All 5 named tests have renamed, passing successors in `crates/slicer-runtime/tests/e2e/slice_end_to_end_tdd.rs` (`wedge_live_path_contains_resolved_seam_evidence_before_emit`, `wedge_default_emits_retract_commands`/`wedge_default_emits_unretract_commands`/`wedge_default_retract_unretract_counts_are_equal`, `wedge_gcode_contains_top_and_bottom_surface_evidence`, `slice_prepass_seam_plan_matches_live_outer_wall_start`, `slice_feature_evidence_failures_name_the_missing_family`) — all green. | slicer-host maintainer | TBD | Closed — 2026-07-03 verification: old benchy test file retired (Packet 90); all 5 named tests have renamed, passing successors |
| DEV-039 | 2026-05-04 | crates/slicer-core/src/algos/prepass_slice.rs::execute_prepass_slice_single_layer (moved/renamed from crates/slicer-host/src/layer_slice.rs::execute_layer_slice) | Low | **Packet 35 — bounding-box fallback for empty slice polygons.** When `slice_mesh_ex` yields zero polygons for a layer (e.g., perfectly flat top/bottom triangles skipped by the slicer), `execute_layer_slice` synthesizes a bounding-box polygon from the `ObjectMesh` vertex XY extents for the surface-classification XY-containment check. Without this fallback, surface-classified facets lying entirely above/below a flat-top/bottom layer would fail the XY-containment guard and `is_top_surface` / `is_bottom_surface` would never fire on geometry that warrants them. This is packet-local; not part of the original packet 35 design but load-bearing for `multi_layer_thickness_tdd` correctness. **Verified 2026-07-03:** the fallback logic itself is unchanged and still fires, but the function moved to `crates/slicer-core/src/algos/prepass_slice.rs:483-520` and was renamed `execute_prepass_slice_single_layer` during the host→core algo migration; no later packet replaced the heuristic with anything more precise. | slicer-host maintainer | TBD | Open — packet-local fallback; future packets 36/37 inherit the same XY-containment guard; file moved to slicer-core (2026-07-03) |
| DEV-049 | 2026-05-13 | docs/07_implementation_status.md § DEV-009 | High | **Cooling subset of DEV-009 (Benchy Phase H output partially correct) is now resolved via packet 53.** The live Benchy path now emits M106/M107 cooling commands from a `PostPass::LayerFinalization` cooling module. This closes the cooling/fan-speed gap in the live G-code path. Remaining DEV-009 gaps (seam placement, support structures, etc.) are tracked separately. **Verified 2026-07-03:** the Rationale above was already correct — cooling was fully implemented since packet 53; the Status column was simply left stale ("In Progress") after that closure. Confirmed 14/14 cooling tests green (`gcode_part_cooling_emission_tdd`, `part_cooling_tdd`, `cooling_config_schema_tdd`, `slicer_module_binding_tdd`). | slicer-host + core-modules maintainers | TBD | Closed — 2026-07-03: status column was a stale leftover from before packet 53's closure; cooling has been fully implemented and tested since 2026-05-13 |
| DEV-070 | 2026-06-12 | docs/adr/0011-perimeter-module-owns-wall-sequencing.md §"Decision" / modules/core-modules/path-optimization-default/manifest.toml §`config.schema.wall_sequence` / modules/core-modules/path-optimization-default/src/lib.rs §`wall_sequence` parsing | Medium | **Packet 61 — `wall_sequence` config registered in `path-optimization-default` despite ADR-0011 mandating perimeter-module ownership.** ADR-0011 §"Decision" requires perimeter modules (`classic-perimeters` and `arachne-perimeters`) to own wall-sequence reordering at generation time, and explicitly says `path-optimization-default` deregisters `wall_sequence`. Packet 61 implemented role-priority grouping (Skirt → InnerWall → OuterWall → Infill → …) AT path-optimization time and kept `wall_sequence` as a path-optimization config key, applying the inner/outer swap at emission time rather than at perimeter generation. The packet did not justify the deviation or propose a follow-up. Resolution options: (A) deregister `wall_sequence` from path-optimization-default and migrate the control to perimeter modules per ADR-0011, OR (B) supersede ADR-0011 with a new ADR documenting why emission-time wall-sequence override is the right architecture. Recorded here pending one of those resolutions. Old roadmap task T-076 was superseded by T-054b/T-054c. Packet 61's spec carries a deviation note pointing here. **Verified 2026-07-03:** Packet 105 (commit `262dc63c`) only did half the remediation. `path-optimization-default.toml`'s `[config.schema]` no longer declares `wall_sequence` — deregistered — and `classic-perimeters.toml`/`src/lib.rs` now declare it and call `wall_sequence_reorder(...)` — real ownership moved, per ADR-0011. But `path-optimization-default/src/lib.rs` still has a `wall_sequence: WallSequence` field, still parses `config.get("wall_sequence")` in `on_print_start`, and `role_group()` still branches on it for the module's own NN priority ordering — the "src/lib.rs enum/parse/role_group" removal the remediation promised never happened. `docs/07_implementation_status.md` TASK-054 claims "closes DEV-070" — that overstates it; only the manifest-facing half is done. | slicer-runtime + core-modules maintainers | Packet 105 | Open — 2026-07-03: manifest deregistration + classic-perimeters ownership landed in P105, but path-optimization-default's src/lib.rs still internally reads wall_sequence for its own NN priority; doc07 TASK-054 overclaims full closure |

| D-110-DROP-VARIABLE-WIDTH | 2026-06-19 | docs/specs/perimeter-modules-orca-parity-roadmap.md §M1/Phase-0 rename / docs/adr/0011-perimeter-module-owns-wall-sequencing.md §"Decision" / docs/DEVIATION_LOG.md DEV-070 | Low | **Decision: drop the iterative-inset fake-Arachne module instead of renaming it to `variable-width-perimeters` (which would be dead code once real Arachne ships).** The roadmap originally called M1 "Classic parity + truthful rename" — renaming `arachne-perimeters` to `variable-width-perimeters` so its iterative-inset algorithm was honestly labelled before M2 replaced it with real Arachne. The decision recorded here cancels that rename: the current `arachne-perimeters` module is deleted (not renamed) under P108, and real Arachne is introduced fresh under P110 (skeleton) + P112 (fill). End state: `classic-perimeters` and `arachne-perimeters` only — no `variable-width-perimeters` module ever ships. Cancels roadmap rename tasks T-090/T-091/T-092 (re-scoped to deletion under P108). Amends ADR-0011's module list: `wall_sequence` is owned by `classic-perimeters` and `arachne-perimeters` only. DEV-070's owner list is updated accordingly. Real Arachne replaces the fake in place via P110 (create) + P112 (fill). | P110 | TBD | Open — cancels T-090/091/092 rename; P108 deletes the module; P110 + P112 deliver real Arachne under the same `arachne-perimeters` id. |

| D-104-OVERHANG-QUARTILE-NONE | 2026-06-19 | crates/slicer-core/src/perimeter_utils.rs §`expolygon_to_path3d` helper / modules/core-modules/arachne-perimeters/src/lib.rs §inline path (arachne path construction) / crates/slicer-sdk/src/views.rs §`overhang_quartile_polygons` / modules/core-modules/overhang-classifier-default/src/lib.rs | Low | **Packet 104 T-024, updated by Packet 107.** `Point3WithWidth.overhang_quartile` is left `None` at all construction sites — both the shared `expolygon_to_path3d` helper and the arachne inline path — pending sibling roadmap `docs/specs/overhang-pipeline-restructuring.md` Phase 3 task O-T031. **Packet 107 delivered the consumer stack**: `SliceRegionView::overhang_quartile_polygons() -> &[QuartileBand]` (SDK), the WIT `quartile-band` record + accessor, the host populator (keyed by `global_layer_index`, filling both `overhang_areas` and `overhang_quartile_polygons` from `SurfaceClassificationIR`), and a refactored `overhang-classifier-default` (78 LOC) that reads per-vertex `Point3WithWidth.overhang_quartile` (MAX quartile per entity) and applies `EntityMutation::SetSpeedFactor`; `classify.rs`/`lines_distancer.rs` (wall-distance algorithm) DELETED per ADR-0031 (overhang classification at PrePass — landed at ADR slot 0031, not the originally-planned 0022). AC-6 regression: 4 baseline entities reproduced exactly (max deviation 0.0); Q4 delta (`overhang_4_4_speed`, previously dead config) is an intentional behavior change vs. the pre-refactor classifier, which skipped quartile≥4. No functional regression otherwise: the field is advisory; speed-factor keys fall back to zero when quartile stamps are absent. **Remaining gap** — the perimeter side (this entry's original construction sites) still writes `overhang_quartile = None`; end-to-end, the classifier emits zero speed factors until perimeter generation is wired to write per-vertex `Some(quartile)`. Tracked as follow-up **T-024-WIRE-VIEW-CONSUMER** (a production-coupled tripwire test, `overhang_pipeline_e2e_tdd.rs`, will fail loudly when this lands). | slicer-core + core-modules maintainer | T-024-WIRE-VIEW-CONSUMER landing packet | Partially closed (P107) — consumer stack (view accessor, WIT, populator, refactored classifier) closed; perimeter-side per-vertex propagation tracked as T-024-WIRE-VIEW-CONSUMER. **P109 verified consumer-stack closure via the perimeter-parity harness + `overhang_ramp` fixture; the remaining per-vertex propagation (T-024-WIRE-VIEW-CONSUMER) REMAINS OPEN — NOT fully closed.** |

| D-105-FLOW-NOT-WIRED | 2026-06-23 | crates/slicer-core/src/flow.rs / modules/core-modules/classic-perimeters + arachne-perimeters | Low | **Packet 105 (ACCEPTED/DEFERRED)** — `slicer_core::flow::line_width_to_spacing` (OrcaSlicer rounded width→spacing, e.g. 0.357 mm for 0.4 mm width) is implemented + unit-tested (`flow_tdd`) but NOT wired into the perimeter modules, which compute the wall gap as the width-average `(outer+inner)/2` inline. AC-1 asserts the width-average value (0.45 mm), so AC-1 ships green but encodes simplified spacing, not OrcaSlicer's true `ext_perimeter_spacing2`. RESOLVED (user decision 2026-06-24): KEEP flow.rs + flow_tdd as pending-integration; AC-1 ships with width-average spacing as an accepted simplification; wire flow + reshape AC-1 to true `ext_perimeter_spacing2` parity when the P106 `extra_perimeters` spacing consumer lands. **Update 2026-07-02 (packets-102–109 review):** the "P106" target went stale — the 104–112 packet renumber (`29ea405a`) turned P106 into the overhang-prepass packet, and the `extra_perimeters` consumer shipped in P108 without wiring flow to the perimeter-spacing path. Re-targeted to roadmap follow-up task **T-052-WIRE-FLOW** (see `docs/specs/perimeter-modules-orca-parity-roadmap.md` §"Inherited from P105"). **Correction 2026-07-03:** the "`flow.rs` still has no production caller" claim above is now false — `crates/slicer-core/src/algos/paint_segmentation/top_bottom.rs:139` calls `line_width_to_spacing` for MMU shell-step spacing (commit `17bb59bd`, 2026-06-26). That caller is unrelated to this row's actual subject: `classic-perimeters` still computes wall gap as width-average `(outer+inner)/2` inline with zero calls to `line_width_to_spacing`, so the perimeter-spacing parity gap remains open. | slicer-core maintainer | T-052-WIRE-FLOW | Accepted/Open — 2026-07-03: perimeter-spacing parity gap confirmed still open (classic-perimeters uses width-average, not line_width_to_spacing); the "no production caller" framing was outdated — flow.rs does have an unrelated caller in paint-segmentation |

| DEV-081 | 2026-07-01 | modules/core-modules/lightning-infill/src/lib.rs / docs/adr/0025-infill-linker-as-raw-emit-post-pass.md | Low | **Infill-parity effort (transitional).** ADR-0025 establishes Architecture A: `Layer::Infill` modules emit raw unlinked segments; a dedicated `Layer::InfillPostProcess` `infill-linker` module is the single place infill path connection happens. `rectilinear-infill` and `gyroid-infill` are rewritten to emit raw (this effort). `lightning-infill` (out of parity scope) currently self-links its own output, which is inconsistent with Architecture A. Until lightning switches to raw emit in a follow-up packet, the infill-linker handles both: it links raw segments from rectilinear/gyroid, and already-linked paths from lightning pass through unchanged (the linker links raw segments; already-linked paths are not re-broken unless re-clipping changes them). Not a blocker for the parity effort. **Update 2026-07-01 (grilling):** full OrcaSlicer lightning parity was pulled into the roadmap (ADR-0029, `docs/specs/lightning-infill-parity.md`, packets 137–140); this deviation closes at packet `140_lightning-module-rewrite`. Also noted: the pass-through premise is weaker than originally written — `ExtrusionPath3D` carries no module identity, so the linker cannot detect already-linked paths; the raw-emit conversion is the real fix. | core-modules maintainer | packet 140 | Open — closes at packet 140 (lightning module rewrite per ADR-0029) |

| DEV-082 | 2026-07-01 | modules/core-modules/gyroid-infill/gyroid-infill.toml (claims) / modules/core-modules/gyroid-infill/src/lib.rs (top/bottom/bridge emission) / docs/adr/0027-gyroid-multi-role-fill-holder.md | Low (opt-in; default config keeps rectilinear for solid) | **Infill-parity effort — intended as a deliberate divergence, but shipped incomplete.** PnP `gyroid-infill` is meant to be able to fill solid shells (top/bottom/bridge) as a user option; OrcaSlicer's gyroid is sparse-only (`Fill.cpp:926`). The emission code for this exists (`src/lib.rs:180-210`, gated by `region.should_emit(gate_role)`), but **the manifest never actually declared the claims it needs** — `gyroid-infill.toml` has only ever declared `claim:sparse-fill` (confirmed via full git history; this was never added, not a regression). Consequence: a user who sets `top_fill_holder = "gyroid-infill"` today gets silent no-op output for that role — the advertised opt-in feature is dead on arrival. **Found 2026-07-03** while verifying open deviations; **fix already scoped**: `.ralph/specs/135_gyroid-raw-emit/packet.spec.md` adds `claim:top-fill`, `claim:bottom-fill`, and `claim:bridge-fill` to the manifest (AC verifies all four claim lines are present) as part of realizing this deviation. ADR-0027's divergence rationale is still valid — only the manifest wiring was missing. | infill-modules maintainer | packet 135_gyroid-raw-emit | Open — 2026-07-03: manifest never declared the required claims (dead-on-arrival bug, not a shipped divergence); fix scoped in packet 135_gyroid-raw-emit |

| D-109-SELF-CAPTURED-FIXTURES | 2026-07-02 | crates/slicer-runtime/tests/integration/perimeter_parity.rs §fixture provenance (lines ~749-770) / docs/specs/perimeter-modules-orca-parity-roadmap.md §M1 (T-101) | Medium | **Packet 109 (M1 verification) — accepted limitation.** Roadmap T-101 asked to "Record OrcaSlicer reference outputs for 6 M1 fixtures". This environment cannot run OrcaSlicer, so the six M1 parity fixtures are SELF-CAPTURED baselines recorded from this pipeline's own output (see the provenance comment in `perimeter_parity.rs`). The harness therefore proves self-regression (geometry stays stable across commits), NOT per-vertex OrcaSlicer parity — it can detect drift from our own prior output but never divergence from OrcaSlicer. The roadmap's M1 "COMPLETE" claim is scoped accordingly. User accepted this limitation 2026-07-02 (packets-102–109 review). | slicer-runtime maintainer | — (accepted; revisit if an OrcaSlicer reference-capture environment becomes available) | Open — accepted limitation, user decision 2026-07-02; fixtures guard self-regression only |
