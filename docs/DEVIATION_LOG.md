# Deviation Log

Use this registry for intentional deviations from architecture docs.

> **Single source of truth.** This file is authoritative for deviation status. A
> deviation is **open** unless its `Status` column begins with `Closed`. Status
> summaries elsewhere (`docs/07_implementation_status.md` Open Deviation Map,
> `docs/14_deviation_audit_history.md`) are non-authoritative views generated
> from this table by `cargo xtask check-deviations`; when they disagree, this
> file wins. Do not hand-edit those views.

| ID | Date | Affected Section | Risk | Rationale | Mitigation Owner | Target Close | Status |
| --- | --- | --- | --- | --- | --- | --- | --- |
| DEV-009 | 2026-04-15 | 07_implementation_status.md Phase H / 01_system_architecture.md live pipeline expectations | High | The Phase H Benchy run is only partially correct on the live path: `.gcode` is produced, but top/bottom fill, supports, seam placement, retract/unretract behavior, and OrcaSlicer-compatible GCode comment metadata required for native preview visualization are still missing or incomplete. **Remediation progress:** packet 52_gcode-feedrate-emission has implemented per-role feedrate emission covering all ExtrusionRole variants (13 named + Custom) with 26 speed config keys, and F-tokens are now resolved on every print and travel move via resolve_feedrate (skirt→model and wipe-tower travel inserts also carry a resolved travel feedrate). **Progress 2026-05-14 (packet 54):** skirt-brim emit subset closed (TASK-142a — `lib.rs` defaults fix + 5 tests); relative-E subset closed (TASK-183 — M82/M83 toggle + 10 tests). Cooling subset closed via packet 53 (TASK-154); feedrate subset closed via packet 52 (TASK-153). DEV-009 fully closed for these subsets via packets 52+53+54. **Progress 2026-05-15 (packet 57):** packet 57 (`57_overhang-speed`) implements the per-quartile wall slowdown end-to-end: WIT field `overhang-quartile`, Rust mirror, `LinesDistancer2D` classifier, `resolve_feedrate` dispatch, and pipeline wire-in. DEV-009 is partially remediated; the remaining open items (smoothed-speed mode, six-band schedule parity, `ADD_INTERSECTIONS` mid-segment splitting, curled-edge slowdown) are deferred to follow-up packets. Remaining open DEV-009 items: seam placement end-to-end (`benchy_live_path_contains_resolved_seam_evidence_before_emit`), retract/unretract emission, top/bottom surface G-code evidence (tracked under DEV-033). 2026-05-18: doc-side gaps for packet-34 retraction mode, packet-52 per-role speeds, packet-53 cooling fan, packet-54 relative-E + skirt-brim, packet-55 header/thumbnail, packet-57 overhang quartile, packet-58 toolchange-purge, packet-59 machine start/end, packet-60 precision now documented in `docs/02_ir_schemas.md`, `docs/03_wit_and_manifest.md`, `docs/04_host_scheduler.md`, `docs/05_module_sdk.md`, `docs/01_system_architecture.md`, `docs/10_glossary_and_scenario_traces.md`, `docs/16_slicer_report.md`. See `docs/AUDIT_2026-05-18_doc_gaps.md`. **2026-06-29 — packet 126 closure (colour-parity subset):** packet `126_mmu-painted-cube-parity` closes the MMU colour-parity subset on the live `cube_4color.3mf` path. Retired the boostvoronoi cell-decomposition shortcut (`cells_to_expolygons_by_color`) and the corner-displacement post-passes in `voronoi_graph.rs` (commit `3e3a9ead`) in favour of the faithful `extract_colored_segments` leftmost-arc walk (commits `721f19ec` + `0c41a42a`, OrcaSlicer `MultiMaterialSegmentation.cpp:494-566` parity, cross-referenced to ADR-0013). Closes G1 (top/bottom diagonal-seam sliver), G2 (spurious inner walls on detail faces), G3 (first-layer perimeter colour from bottom face), G7 (default face colour reads the 3MF object base extruder), G8 (subdivided horizontal face skips tool-0 default projection). Verifies the S1–S3 landed fixes: G4 internal solid infill (14 `;TYPE:Internal solid infill` segments emitted), G5 volumetric flow (commit `17bb59bd` formula; emit test green), G6 shell inset (`propagate_top_bottom` test green), RC1 palette (`#FF9B00;#02BF06;#1800F2;#EC0006` in header), RC3 side colours (4 distinct `T<n>` in gcode). Adds the Bug #2 post-close fix: small-region drop (`MIN_REGION_AREA = 20_000_000` units² ≈ 0.2 mm²) at the tail of `execute_paint_segmentation` to suppress top-ironing sliver regions on the side-face harvest (OrcaSlicer `small_region_threshold` parity). Locks the Bug #1 leading-T-change consumer-side contract with two regression tests in `crates/slicer-gcode/tests/emit_tool_guard_tdd.rs`; on review the upstream "Bug #1 fix was missing `tool_index` on `OrderedEntityView`" diagnosis was incorrect — the live `dispatch::project_ordered_entities_from` (`crates/slicer-wasm-host/src/dispatch.rs:1486`) already populates `tool_index: entity.tool_index`, and `DefaultGCodeEmitter::emit_gcode` at `crates/slicer-gcode/src/emit.rs:319-343` already emits the leading `ToolChange { after_entity_index: u32::MAX, from: <prev>, to: <first entity tool> }` when `first_entity.tool_index != current_tool`. If the §REMAINING #1 visual symptom persists in the user's viewer, it lives in a different location than the handoff indicated (see `docs/17_agent_debugging.md` and the `debug-pipeline` skill). Clippy `--workspace --all-targets -- -D warnings` clean. Outstanding DEV-009 subsets (seam placement end-to-end `benchy_live_path_contains_resolved_seam_evidence_before_emit`, retract/unretract emission under TASK-120c/D-DEV-033) remain open and are tracked under TASK-120c — packet 126 closed only the colour-parity / painted-MMU subset. **Verified 2026-07-03:** the seam-placement, retract/unretract, and top/bottom-surface items named as "remaining open" above are now closed — the old `benchy_*` tests were retired (Packet 90) and have renamed, passing successors in `crates/slicer-runtime/tests/e2e/slice_end_to_end_tdd.rs` (`wedge_live_path_contains_resolved_seam_evidence_before_emit`, `wedge_default_emits_retract_commands`/`wedge_default_emits_unretract_commands`/`wedge_default_retract_unretract_counts_are_equal`, `wedge_gcode_contains_top_and_bottom_surface_evidence`), all green — the old benchy-suite test file that tracked these (formerly DEV-033, closed and removed from this log 2026-07-03) was retired at Packet 90 and its assertions ported to the wedge fixture. This row stays open only for the sub-items this verification did not check: smoothed-speed mode, six-band schedule parity, `ADD_INTERSECTIONS` mid-segment splitting, and curled-edge slowdown. **Independently re-verified 2026-07-03 (second pass):** all four sub-items directly checked against current source — smoothed-speed mode: no interpolation/blending logic anywhere in `resolve_feedrate` or the classifier (confirmed unimplemented, still a flat quantized lookup). `ADD_INTERSECTIONS` mid-segment splitting: zero matches for mid-segment vertex insertion at overhang-threshold crossings anywhere in `crates/`/`modules/` (confirmed unimplemented). Curled-edge slowdown: no curl detection anywhere; still blocked on an unbuilt support-spot generator (confirmed unimplemented). **Six-band schedule reclassified (user decision 2026-07-03): this is a permanent, intentional, accepted deviation, not an open TODO.** `crates/slicer-core/src/algos/overhang_annotation.rs:12-33` explicitly documents the 4-band-vs-OrcaSlicer's-6-band choice as deliberate ("This is a recorded, intentional deviation — not a bug"), reconfirmed as still current by `.ralph/specs/106_overhang-pipeline-prepass-foundation/packet.spec.md:106` (2026-07-01, i.e. after packet 57). Closing DEV-009 no longer requires implementing 6 bands; the remaining genuinely open sub-items are smoothed-speed mode, `ADD_INTERSECTIONS` mid-segment splitting, and curled-edge slowdown only. **T-024-WIRE-VIEW-CONSUMER + T-077-GUEST-ADAPTER-FIELDS closed 2026-07-03 (see `D-104-OVERHANG-QUARTILE-NONE`, now closed):** `overhang_quartile` now propagates end-to-end through the real WASM-adjacent pipeline (`expolygon_to_path3d` stamps it from real quartile-band data; the guest adapter marshals that data across the WIT boundary). This is a genuine prerequisite for smoothed-speed mode and `ADD_INTERSECTIONS`, not those sub-items themselves — both remain open: smoothed-speed mode has no interpolation logic anywhere (still a flat quantized lookup), and `ADD_INTERSECTIONS` mid-segment splitting is unbuilt and additionally blocked by `arachne-perimeters` not existing as a module (deleted P108; real Arachne is `status: draft`, packets 110-112, not started) — `classic-perimeters` is the only live target. **Curled-edge slowdown closed 2026-07-03 — the "blocked on an unbuilt support-spot generator" premise above was false**, verified against actual OrcaSlicer source (`OrcaSlicerDocumented/src/libslic3r/Support/SupportSpotsGenerator.hpp/.cpp`): the support-point-placement code that filename implies is entirely dead/commented-out upstream; the only LIVE code in that file is curl *estimation* (`get_flow_width`/`estimate_curled_up_height`/`estimate_malformations`, ~150 lines, `SupportSpotsGenerator.cpp:158-338`), an independent post-slice step over each layer's own wall geometry, unrelated to support generation. Implemented as a self-contained extension of `overhang-classifier-default` (`modules/core-modules/overhang-classifier-default/src/lib.rs`), with the standard OrcaSlicer attribution header: walks `layers: &[LayerCollectionView]` (already received in full) in Z order, computing each layer's own curled height from distance-to/curvature-against the layer below (a standard discrete curvature estimate, not a verbatim port of upstream's separate, unscoped `estimate_points_properties`), then a cross-layer proximity lookup (plain linear/bbox scan, not upstream's `AABBTreeLines` — reasonable at this codebase's per-layer vertex counts) synthesizes an **artificial curl distance** from nearby curled points and buckets it through the *same* `BAND_BOUNDARY_MULTIPLIERS` thresholds and `overhang_1_4_speed`..`overhang_4_4_speed` config keys real overhang uses (duplicated locally, not imported — this WASM guest module intentionally does not depend on `slicer-core`), merged via `max(overhang_quartile, curl_quartile)`. `curled_height` is transient (computed and consumed within one `run_finalization` call), never persisted to IR/WIT — nothing else needs to read it back. Covered by `curled_edge_triggers_slowdown_on_next_layer` and `curled_edge_out_of_range_emits_no_mutation` in `modules/core-modules/overhang-classifier-default/tests/basic_tdd.rs`; all pre-existing overhang-only tests still pass unchanged. DEV-009's only remaining open sub-items are smoothed-speed mode and `ADD_INTERSECTIONS` mid-segment splitting. | slicer-host + core-modules maintainers | TBD | Open — 2026-07-03: seam placement, retract/unretract, top/bottom-surface evidence, T-024/T-077 overhang-quartile propagation, and curled-edge slowdown are all closed; six-band schedule is an accepted permanent deviation (not open work); remaining open items are smoothed-speed mode and ADD_INTERSECTIONS mid-segment splitting |
| DEV-010 | 2026-04-15 | 11_operational_governance_and_acceptance_gate.md / 12_architecture_gate_metrics.md / 07_implementation_status.md | Medium | The implementation is past MVP, but the Architecture Acceptance Gate has not yet been evaluated and the remaining governance closure work is still open. **Verified 2026-07-03:** independent re-verification found no formal evidence of a scored gate evaluation — `docs/evidence/` does not exist, and `docs/07_implementation_status.md` still lists the gate as "BLOCKED BY OPEN REMEDIATION TASKS" with TASK-140 (the gate-evaluation task itself) and its blockers TASK-136/154/155/156 all unchecked. **Closed 2026-07-03 as a maintainer judgment call, not a scored docs/11 rubric evaluation**: the project maintainer assessed the implementation as well past MVP maturity and elected to close this deviation on that basis. This is explicitly *not* a claim that the 6 rubric categories (Determinism, Recoverability, Resource Bounds, Coupling Control, Compatibility, Operability) were scored or that `docs/evidence/` was populated — TASK-140 and a formal scored gate run remain undone and can be reopened later if ever required (e.g. before an external release). `docs/07_implementation_status.md`'s TASK-140/136/154/155/156 checkboxes are intentionally left unchecked, since that formal-evaluation track is genuinely still not done independent of this closure. | project maintainer | — (closed by judgment call; TASK-140 formal evaluation remains available if ever needed) | Closed — 2026-07-03: maintainer judgment call (project assessed as well past MVP maturity); not a scored docs/11 rubric pass |
| DEV-026 | 2026-04-16 | 04_host_scheduler.md §"Phase 3 DAG Validation" / 11_operational_governance_and_acceptance_gate.md §2 "Compatibility Policy" / 12_architecture_gate_metrics.md §"Compatibility" | High | Startup validation still misses several compatibility and governance checks beyond the WIT-source/version problem tracked in DEV-014. The remaining unique gaps are: (1) `min_host_version` is parsed but never compared at startup; (2) manifest-schema validation cannot currently surface a meaningful `Schema` failure path because missing schema falls back to defaults and parse failures collapse into TOML errors; (3) memory, full-slice time, and host-call budgets from docs/12 have no runtime measurement, diagnostics, or benchmark evidence. Layer-count and region-cap bounds are already enforced. Implement the host-side `wit_world` allowlist as part of the same validation pass used to close DEV-014 rather than tracking that subtask separately here. **Verified 2026-07-03:** gap (2) is now closed — `LoadErrorKind::Schema` (`crates/slicer-scheduler/src/manifest.rs`) is a real, distinct variant with specific field/message context, proven by `region_split_manifest_tdd.rs`; malformed manifests no longer collapse into generic TOML errors. **Gap (1) closed (this pass corrects stale status, not new work):** a 14th startup DAG validation pass, `DagValidationPass::HostVersionCompatibility`, was added and wired in production — `crates/slicer-scheduler/src/validation.rs:346` declares the pass, `:649` runs it in `validate_startup_dag`, and `:647`'s `validate_host_version` compares `request.host_version` against each loaded module's declared `min_host_version` via the existing `semver_lt` helper. `crates/slicer-runtime/src/run.rs:412-418` populates `host_version` from the running host's own `CARGO_PKG_VERSION` at the single production call site. Documented in `docs/04_host_scheduler.md`'s "Validation Passes (in order)" and `docs/11_operational_governance_and_acceptance_gate.md` §2. This landed in an earlier commit (`ca8fe483`) but this log row was never updated to reflect it until now. **Gap (3) partially closed 2026-07-03 (time-only, downgraded per maintainer decision):** the docs/12 500-layer peak-RSS bound is not achievable with existing instrumentation — `AccountingAllocator` (`crates/slicer-runtime/src/report/allocator.rs`) only tracks host-heap allocations and explicitly excludes WASM linear memory (wasmtime mmaps it directly, bypassing `GlobalAlloc`) — and is deferred indefinitely (tracked under `TASK-156`; would need new OS-level RSS sampling around a subprocess run). The 50-layer full-slice-time bound now has reproducible real evidence: `crates/slicer-runtime/benches/gate_evidence.rs` (`cargo bench -p slicer-runtime --bench gate_evidence`) times a real `pnp_cli slice` subprocess against `resources/regression_wedge.stl` + `resources/test_config/gate_evidence_50l.json` (a `layer_height`/`first_layer_height` override coercing the 40mm-tall fixture to exactly 50 layers, verified via `;LAYER_CHANGE` count — not the normatively-named `benchy_50l_0p2_single_tool` fixture, which still doesn't exist). Measured this session (debug profile): **~438ms median** (`[435.25, 438.01, 440.97] ms`), well under the 10-second bound. Measures and reports rather than hard-failing (matches this repo's other benches — "slow; not in CI"). | slicer-host maintainer | TBD | Open — 2026-07-03: narrowed to the peak-RSS/500-layer half of gap (3) only; the 50-layer time-budget half now has reproducible evidence (~438ms, well under 10s); (1) min_host_version startup check and (2) manifest Schema error path are both closed |
| DEV-039 | 2026-05-04 | crates/slicer-core/src/algos/prepass_slice.rs::execute_prepass_slice_single_layer (moved/renamed from crates/slicer-host/src/layer_slice.rs::execute_layer_slice) | Low | **Packet 35 — bounding-box fallback for empty slice polygons.** When `slice_mesh_ex` yields zero polygons for a layer (e.g., perfectly flat top/bottom triangles skipped by the slicer), `execute_layer_slice` synthesizes a bounding-box polygon from the `ObjectMesh` vertex XY extents for the surface-classification XY-containment check. Without this fallback, surface-classified facets lying entirely above/below a flat-top/bottom layer would fail the XY-containment guard and surface classification would never fire on geometry that warrants it. This is packet-local; not part of the original packet 35 design. **Verified 2026-07-03:** the fallback logic itself is unchanged and still fires, but the function moved to `crates/slicer-core/src/algos/prepass_slice.rs:493-524` (line range corrected — was previously misquoted as 483-520) and was renamed `execute_prepass_slice_single_layer` during the host→core algo migration; no later packet replaced the heuristic with anything more precise. **Correction 2026-07-03:** the fallback's purpose has drifted from this row's original text — it now feeds `classify_region_surfaces` / `SlicedRegion.is_bridge` (bridge detection), not top/bottom-surface classification; `SlicedRegion` no longer has `is_top_surface`/`is_bottom_surface` fields, that responsibility moved to the separate `ShellClassification` pass in `crates/slicer-core/src/algos/paint_segmentation/top_bottom.rs`. The previously-cited test `multi_layer_thickness_tdd` does not exist anywhere in this repo's history (confirmed via `git log --all -S`); the real test exercising this path today is `crates/slicer-runtime/tests/unit/bridge_detector_tdd.rs::non_bridge_region_has_empty_bridge_areas`. | slicer-host maintainer | TBD | Open — packet-local fallback; future packets 36/37 inherit the same XY-containment guard; file moved to slicer-core (2026-07-03); text corrected 2026-07-03 (line range, real consumer, real test) |
| DEV-070 | 2026-06-12 | docs/adr/0011-perimeter-module-owns-wall-sequencing.md §"Decision" / modules/core-modules/path-optimization-default/manifest.toml §`config.schema.wall_sequence` / modules/core-modules/path-optimization-default/src/lib.rs §`wall_sequence` parsing | Medium | **Packet 61 — `wall_sequence` config registered in `path-optimization-default` despite ADR-0011 mandating perimeter-module ownership.** ADR-0011 §"Decision" requires perimeter modules (`classic-perimeters` and `arachne-perimeters`) to own wall-sequence reordering at generation time, and explicitly says `path-optimization-default` deregisters `wall_sequence`. Packet 61 implemented role-priority grouping (Skirt → InnerWall → OuterWall → Infill → …) AT path-optimization time and kept `wall_sequence` as a path-optimization config key, applying the inner/outer swap at emission time rather than at perimeter generation. The packet did not justify the deviation or propose a follow-up. Resolution options: (A) deregister `wall_sequence` from path-optimization-default and migrate the control to perimeter modules per ADR-0011, OR (B) supersede ADR-0011 with a new ADR documenting why emission-time wall-sequence override is the right architecture. Recorded here pending one of those resolutions. Old roadmap task T-076 was superseded by T-054b/T-054c. Packet 61's spec carries a deviation note pointing here. **Verified 2026-07-03:** Packet 105 (commit `262dc63c`) only did half the remediation. `path-optimization-default.toml`'s `[config.schema]` no longer declares `wall_sequence` — deregistered — and `classic-perimeters.toml`/`src/lib.rs` now declare it and call `wall_sequence_reorder(...)` — real ownership moved, per ADR-0011. But `path-optimization-default/src/lib.rs` still has a `wall_sequence: WallSequence` field, still parses `config.get("wall_sequence")` in `on_print_start`, and `role_group()` still branches on it for the module's own NN priority ordering — the "src/lib.rs enum/parse/role_group" removal the remediation promised never happened. `docs/07_implementation_status.md` TASK-054 claims "closes DEV-070" — that overstates it; only the manifest-facing half is done. | slicer-runtime + core-modules maintainers | Packet 105 | Open — 2026-07-03: manifest deregistration + classic-perimeters ownership landed in P105, but path-optimization-default's src/lib.rs still internally reads wall_sequence for its own NN priority; doc07 TASK-054 overclaims full closure |

| D-110-DROP-VARIABLE-WIDTH | 2026-06-19 | docs/specs/perimeter-modules-orca-parity-roadmap.md §M1/Phase-0 rename / docs/adr/0011-perimeter-module-owns-wall-sequencing.md §"Decision" / docs/DEVIATION_LOG.md DEV-070 | Low | **Decision: drop the iterative-inset fake-Arachne module instead of renaming it to `variable-width-perimeters` (which would be dead code once real Arachne ships).** The roadmap originally called M1 "Classic parity + truthful rename" — renaming `arachne-perimeters` to `variable-width-perimeters` so its iterative-inset algorithm was honestly labelled before M2 replaced it with real Arachne. The decision recorded here cancels that rename: the current `arachne-perimeters` module is deleted (not renamed) under P108, and real Arachne is introduced fresh under P110 (skeleton) + P112 (fill). End state: `classic-perimeters` and `arachne-perimeters` only — no `variable-width-perimeters` module ever ships. Cancels roadmap rename tasks T-090/T-091/T-092 (re-scoped to deletion under P108). Amends ADR-0011's module list: `wall_sequence` is owned by `classic-perimeters` and `arachne-perimeters` only. DEV-070's owner list is updated accordingly. Real Arachne replaces the fake in place via P110 (create) + P112 (fill). | P110 | TBD | Open — cancels T-090/091/092 rename; P108 deletes the module; P110 + P112 deliver real Arachne under the same `arachne-perimeters` id. |

| D-104-OVERHANG-QUARTILE-NONE | 2026-06-19 | crates/slicer-core/src/perimeter_utils.rs §`expolygon_to_path3d` helper / modules/core-modules/arachne-perimeters/src/lib.rs §inline path (arachne path construction) / crates/slicer-sdk/src/views.rs §`overhang_quartile_polygons` / modules/core-modules/overhang-classifier-default/src/lib.rs | Low | arachne-path-only per-vertex overhang/flag/seam/boundary parity. The classic path is at parity via T-024, T-077, classic classify_line, and expolygon_to_path3d (perimeter_utils.rs:316-331). The arachne path's gaps — is_bridge, is_thin_wall never set, LoopType::ThinWall never emitted, boundary_type hardcoded Interior, overhang_quartile hardcoded None (defaults inherited from generate_toolpaths.rs and overridden per-vertex by the guest), no seam-candidate producer, no precise_outer_wall registration — are closed by packet 148. | slicer-core + core-modules maintainer | — (closed) | Closed 2026-07-03 — T-024 (perimeter-side stamping) and T-077 (guest-adapter marshalling) both landed; verified end-to-end via `overhang_pipeline_full_propagation` against the real WASM-adjacent pipeline path |

| D-105-FLOW-NOT-WIRED | 2026-06-23 | crates/slicer-core/src/flow.rs / modules/core-modules/classic-perimeters + arachne-perimeters | Low | **Packet 105 (ACCEPTED/DEFERRED)** — `slicer_core::flow::line_width_to_spacing` (OrcaSlicer rounded width→spacing, e.g. 0.357 mm for 0.4 mm width) is implemented + unit-tested (`flow_tdd`) but NOT wired into the perimeter modules, which compute the wall gap as the width-average `(outer+inner)/2` inline. AC-1 asserts the width-average value (0.45 mm), so AC-1 ships green but encodes simplified spacing, not OrcaSlicer's true `ext_perimeter_spacing2`. RESOLVED (user decision 2026-06-24): KEEP flow.rs + flow_tdd as pending-integration; AC-1 ships with width-average spacing as an accepted simplification; wire flow + reshape AC-1 to true `ext_perimeter_spacing2` parity when the P106 `extra_perimeters` spacing consumer lands. **Update 2026-07-02 (packets-102–109 review):** the "P106" target went stale — the 104–112 packet renumber (`29ea405a`) turned P106 into the overhang-prepass packet, and the `extra_perimeters` consumer shipped in P108 without wiring flow to the perimeter-spacing path. Re-targeted to roadmap follow-up task **T-052-WIRE-FLOW** (see `docs/specs/perimeter-modules-orca-parity-roadmap.md` §"Inherited from P105"). **Correction 2026-07-03:** the "`flow.rs` still has no production caller" claim above is now false — `crates/slicer-core/src/algos/paint_segmentation/top_bottom.rs:139` calls `line_width_to_spacing` for MMU shell-step spacing (commit `17bb59bd`, 2026-06-26). That caller is unrelated to this row's actual subject: `classic-perimeters` still computes wall gap as width-average `(outer+inner)/2` inline with zero calls to `line_width_to_spacing`, so the perimeter-spacing parity gap remains open. **Packet 150 (2026-07-10):** `line_width_to_spacing` wired into bead placement in `arachne-perimeters` (was raw width); G4 test `arachne_parity_pipeline_wall_gap_uses_flow_spacing_not_width` now green. **Correction (2026-07-15, audit reconciliation):** the original text claimed this wired flow into "both `classic-perimeters` and `arachne-perimeters`" — that is FALSE for classic. Verified against source: `classic-perimeters/src/lib.rs:600,605,907` still compute the wall gap as the width-average `(outer_wall_line_width + inner_wall_line_width) / 2.0`, with ZERO calls to `line_width_to_spacing` anywhere in the module. Only `arachne-perimeters` was wired. The classic-side flow-wiring parity gap (roadmap T-052 / "true `ext_perimeter_spacing2` via `flow::line_width_to_spacing`") remains genuinely OPEN. Also uncovered and fixed a related bug while wiring this: `precise_outer_wall`'s Arachne-parity inset paired the outer wall's raw width with the INNER wall's spacing; corrected to the outer wall's own spacing per OrcaSlicer `PerimeterGenerator.cpp:2103-2104,2158`. Its regression-lock baseline `EXPECTED_OFFSET_MM` was corrected from `-0.05` (raw-width era) to the spacing-correct `-0.0214602` (= `-layer_height·(1-π/4)/2`). **Also discovered during packet 150:** `line_width_to_spacing` returns `0` for degenerate `width <= layer_height` configs (OrcaSlicer rejects `width < height` as an invalid combination), which would otherwise collapse bead spacing to zero; `arachne_params_from_config` now falls back to the raw width in that degenerate case to avoid zero-spacing bead collapse. The `perimeter_parity` fixtures `tapered_wedge` / `narrow_strip_widening` / `max_bead_count_cap` / `complex_multi_feature` use an unrealistic `layer_height=1.0mm` (a packet-109 fixture artifact) that trips this degenerate path today — flagged as a fixture-hygiene follow-up, not fixed by this packet. **Correction 2026-07-13 (faithful-port audit of the D-105 beading fix):** the 2026-07-13 closure above was audit-verified against the canonical OrcaSlicer `LimitedBeadingStrategy.cpp`. **What the session got right:** `LimitedBeadingStrategy::compute`/`get_optimal_bead_count` in `crates/slicer-core/src/beading/limited.rs` faithfully mirrors OrcaSlicer `LimitedBeadingStrategy.cpp:64-127` — recompute the parent at `optimal_thickness = max_bead_count * optimal_width`, carry the surplus to `left_over`, mirror symmetrically about the region centre, and cap at `max_bead_count + 1`. This genuinely closes the architectural gap (DistributedBeadingStrategy had distributed `max_bead_count` beads across the full polygon thickness). **What the session got wrong (corrected in this revision):** a fabricated spine subdivision was introduced in `crates/slicer-core/src/skeletal_trapezoidation/graph.rs::from_polygons_with_beading`, and a 10,000× scale error lived at `limited.rs:185` (literal `0.01` in PNP units = 1 nm vs OrcaSlicer's `scaled<coord_t>(0.01)` = 0.01 mm = 100 PNP units). Both are corrected now: the spine subdivision reverted, the scale bug fixed to `0.01 * UNITS_PER_MM`, following the convention already established at `crates/slicer-core/src/beading/distributed.rs:199` and `crates/slicer-core/src/arachne/generate_toolpaths.rs:230`. **What remains open:** the thin-strip medial-axis collapse root cause is unknown — see D-105D. The G4 test (`arachne_parity_pipeline_wall_gap_uses_flow_spacing_not_width`) and the thin-strip parity tests are RED pending that root-cause fix and are NOT closed by D-105. | slicer-core maintainer | T-052-WIRE-FLOW | Reopened (classic half) 2026-07-15 — the FLOW-NOT-WIRED subject is only half-resolved: `arachne-perimeters` wired in P150, but `classic-perimeters` still uses width-average `(outer+inner)/2` (T-052 genuinely OPEN, verified against source `lib.rs:600,605,907`). The separate D-105 architectural beading fix closed 2026-07-13; G4 test `arachne_parity_pipeline_wall_gap_uses_flow_spacing_not_width` RED pending D-105D root cause |
| D-105B | 2026-07-13 | crates/slicer-core/src/beading/limited.rs (`compute` under-cap branch) | Low | OrcaSlicer's `LimitedBeadingStrategy::compute` (lines 69-84) inserts a single 0-width center sentinel at the cap boundary when `bead_count == max_bead_count && bead_count % 2 == 0`, marking where infill/skin should align. The PnP port's under-cap branch skipped this entirely before this revision. Fixed by porting lines 69-84 faithfully: the sentinel's `toolpath_location` is `innermost_location + innermost_width/2`. The hand-derived `factory_orca_reference.json` fixture was re-derived to reflect the new behavior (the `factory_max_bead_count_le_2_selects_preferred_bead_width_outer` test now asserts 3 entries: 2 nonzero-width real beads + 1 zero-width center sentinel). | slicer-core maintainer | — (closes on this commit) | Closed — 2026-07-13 (this revision) |
| D-105C | 2026-07-13 | crates/slicer-core/src/beading/limited.rs (`compute` over-cap branch, sentinel `toolpath_location` formula) | Low | OrcaSlicer's `LimitedBeadingStrategy::compute` over-cap branch (lines 118-131) places each sentinel at `innermost_toolpath_location + innermost_toolpath_width/2` (left) and `innermost_toolpath_location - innermost_toolpath_width/2` (right) — the INNER EDGE of the innermost real bead. The PnP port used the centerline (`toolpath_locations[left_index-1]`), shifting the sentinel by half a bead width and corrupting infill-area alignment at the cap boundary. Fixed by porting the inner-edge formula faithfully; the PnP f64 type is exact here (the C++ `coord_t` integer-division ±1nm hazard at lines 116-117 does not apply). The hand-derived `limited_cap_boundary.json` and `factory_orca_reference.json` fixtures were re-derived to reflect the correct OrcaSlicer inner-edge positions. | slicer-core maintainer | — (closes on this commit) | Closed — 2026-07-13 (this revision) |
| D-105D | 2026-07-13 | crates/slicer-core/src/arachne/preprocess.rs::merge_short_segments (removed) → crates/slicer-core/src/arachne/preprocess.rs::simplify_ring (added) | Medium | **RESOLVED by packet 154 (2026-07-14).** This row's original investigation scope was based on symbols that do not exist anywhere in the PnP tree: `connectJunctions`, `getNextUnconnected`, and `BeadingPropagation` appear only inside OrcaSlicer-name doc comments, never as real PnP identifiers; `connectJunctions` itself lives in OrcaSlicer's `SkeletalTrapezoidation.cpp:1934`, not `SkeletalTrapezoidationGraph.cpp` as this row previously implied. The quad-chain-walk/graph-topology investigation scope was a red herring — the real defect was upstream, in outline preprocessing, before the medial-axis graph was ever built. **Verified root cause (verdict E′):** `preprocess.rs` stage 2 (`simplify_stage`) called `merge_short_segments`, which dropped polygon vertices on segment length ALONE. For a 0.25 mm × 5 mm thin strip, the 0.25 mm end-cap edges fall under `smallest_segment_mm = 0.5`, so a corner was dropped and the 4-point rectangle became a 3-point triangle BEFORE the medial-axis graph was built — collapsing the medial axis to one vertex, snapping all 6 junction-bearing edges to that point, producing a zero-length `ExtrusionLine` that `remove_small_lines` (`arachne/remove_small.rs:66-96`) correctly dropped, leaving `wall_loops()` empty (the `assert!(!walls.is_empty())` all 4 thin-strip tests failed on). **Why unfaithful:** canonical `Arachne::simplify()` (`WallToolPaths.cpp:86-201`) AND-gates vertex removal on length AND a deviation bound (`WallToolPaths.cpp:161-162`: `length2 < smallest_line_segment_squared && height_2 <= allowed_error_distance_squared`, `height_2` = Shoelace-derived squared perpendicular deviation, `:155`); removing the strip's corner deviates ~0.25 mm, 10× the 0.025 mm `meshfix_maximum_deviation()` allowance (`WallToolPaths.hpp:19-21`), so canonical KEEPS all 4 corners (`:193-197`). `PreprocessParams` already carried the correct `allowed_distance_mm = 0.025` — it was simply never read. **Fix:** ported canonical `simplify()` into `preprocess.rs` as `simplify_ring` (+ `shoelace_term`, `dist_sq`, `distance_to_infinite[_squared]`, `intersection_infinite`), AND-gated on length + deviation, with the intersection-point special case and overflow-safe (i128/f64) area math; the non-canonical RDP pass (`expolygons_simplify`) was removed from stage 2 since canonical stage 2 is `simplify()` and nothing else; attribution header corrected to cite `WallToolPaths.cpp:86-201`. Regression pin: `simplify_preserves_thin_strip_corners_deviation_bound` (`crates/slicer-core/tests/preprocess_golden.rs`). Result: all 4 thin-strip parity tests GREEN; the G4 test stays GREEN; no existing assertion weakened. **Candidates exonerated:** A′ (quad chain walk, `generate_toolpaths.rs`) — faithfully processes whatever graph it is given; B′ (rib topology) — `build_quad_rib_topology` (`rib.rs:101-106`) is a vestigial no-op but the real `makeRib` port (`Builder::make_rib`, `graph.rs:992-1021`) is correct; C′ (`generate_local_maxima_single_beads`) — never fires on a thin strip; D′ ("OrcaSlicer behaves identically") — KILLED, canonical provably preserves all 4 corners. A real, separate parity gap was found in the same investigation and filed instead as `D-154-DISCRETIZE-POINT-POINT-CASE` (open) — provably NOT this row's root cause, since a rectangle's spine is `discretize()` branch 1 (seg-seg), not the point-point branch 3 that gap concerns. | arachne maintainer | — (closed) | Closed — 2026-07-14 (packet 154): root cause was `preprocess.rs::merge_short_segments` dropping thin-strip corners on length alone (unfaithful — canonical AND-gates length with a deviation bound, `WallToolPaths.cpp:161-162`); fixed by faithful `simplify_ring` port (`WallToolPaths.cpp:86-201`); all 4 thin-strip parity tests green; the quad-chain-walk/topology investigation scope named in this row's original text was a red herring — real defect was upstream in outline preprocessing |
| D5-TAPER-PEAK-DROPOUT | 2026-07-16 | crates/slicer-core/src/arachne/generate_toolpaths.rs::generate_junctions | High | **RESOLVED — commit `5d0e1bcf` (cherry-picked from D5 diagnosis run `d0e78daa`).** Found via the Arachne Parity Recovery campaign's benchy visual/structural inventory (`docs/specs/arachne-parity-recovery.md`, finding "D5"). **Symptom:** Arachne dropped ALL wall geometry (walls + infill + surface) in tapered regions — the pointed +X bow of a benchy hull from Z≈0.4 up through ≈6.4 (~40 layers). Per-layer extruding-move maxX capped at ≈2–3 mm where both Classic and OrcaSlicer grow smoothly to ≈15 mm (Z=1.2: arachne 2.37 mm vs classic 15.1 mm) — ~16 mm of the cross-section absent. Arachne-SPECIFIC (Classic renders the region correctly). **Root cause:** a taper's medial-axis spine is the segment-segment bisector of the two converging sides; its dR/dD legitimately exceeds the centrality cap `sin(wall_transition_angle/2)` ≈ 0.087, so the spine is (correctly, matching canonical) NON-central and never receives a primary bead count. PnP's `generate_junctions` then `continue`d on any peak whose `bead_count` was `None`/`0`, dropping the whole region. **Why unfaithful:** canonical `generateJunctions` (`SkeletalTrapezoidation.cpp:1740-1744`) skips ONLY when both endpoints already share an equal NON-NEGATIVE bead count (no transition crosses the edge) or the edge is not upward — it does NOT skip an unassigned (`-1`/`None`) peak, and synthesizes a beading from `distance_to_boundary` via `getOrCreateBeading` (`:1808-1839`, `getOptimalBeadCount(distance_to_boundary*2)`). **Fix:** ported that gate (compare raw bead counts, default `-1`, before synthesis) + on-demand `optimal_bead_count(2*to_r)` synthesis for unassigned peaks. **Regression pin:** `arachne_d5_taper_coverage` (`crates/slicer-core/tests/arachne_d5_taper_coverage.rs`; captured benchy Z≈0.4 cross-section, X-coverage 66.8% → 99.0%). **Verified on full benchy 2026-07-16:** per-layer maxX now tracks Classic within ~0.1% across the whole bow (Z=1.2: 15.11 vs 15.10). **Refuted en route:** thin-wall widening and point-point discretization (`D-154`) — both zero effect (the bow is thick; its spine edges are seg-seg, not point-point). NOT the same defect as the already-closed `D-105D` thin-strip corner-drop, despite the campaign's initial "same family" suspicion. **Known follow-up:** tapered-wedge SELF-CAPTURED baseline tests now drift in the correct ("emit more") direction and need re-baselining or conversion to structural invariants (campaign Track B). **Correction 2026-07-16: there are THREE such fixtures, not 2.** `1d7eb1de` re-captured the two slicer-core ones (`crates/slicer-core/tests/fixtures/arachne/{bead_count_tapered_wedge,toolpaths_tapered_wedge}.json`); the third — `crates/slicer-runtime/tests/fixtures/perimeter_parity/tapered_wedge`, asserted by `perimeter_parity::arachne_perimeter_parity` — was missed and is RED (`regions[0].walls[0].path.points[1].x` actual 9.82146 vs expected 3.7797625; actual > expected = the correct "emit more" drift). Confirmed pre-existing and D5-attributable, not caused by `D-147-STITCH-TINY-POLY-UNITS`: stashing only that fix and rebuilding guests reproduces the identical numbers. Left RED deliberately — per D-109/ADR-0042 the remedy is conversion to a structural invariant (Track B), not a blind rebless. | arachne maintainer | Track B baseline reconciliation | Closed — 2026-07-16 (`5d0e1bcf`); full-benchy structural verification passed; 2 self-captured taper baselines pending Track-B reconciliation |
| D4-INNER-WALL-OVEREXTRUSION | 2026-07-16 | crates/slicer-core/src/arachne/pipeline.rs (pass order) + crates/slicer-core/src/skeletal_trapezoidation/propagation.rs (propagate_beadings_upward/_downward) + modules/core-modules/arachne-perimeters (max_bead_count derivation) | High | **RESOLVED 2026-07-16.** **The fix:** PnP had canonical's beading pass ORDER inverted. Canonical (`SkeletalTrapezoidation.cpp:1488-1514`) computes each node's beading from ITS OWN `bead_count` + thickness, THEN `propagateBeadingsUpward`/`Downward` copy the resulting **Beading objects** to nodes that have none. PnP instead ran `populate_beading_propagation` LAST, and `propagate_beadings_upward` propagated the **scalar** `bead_count` (`to_vertex.bead_count = Some(from_bead_count)`) — so every propagated node had its beading recomputed at its OWN, much larger, thickness: a thin node's `bead_count = 3` became `compute(19.7mm, 3)` = `[0.4, 18.9, 0.4]` on the thick spine. (It also left both propagation passes' side-table reads dead, since the table was still empty — despite `propagate_beadings_downward`'s own comments assuming populate had already run.) Fixed by (a) moving `populate_beading_propagation` before the propagation passes, (b) porting `propagateBeadingsUpward` faithfully (`:1561-1588`): copy the source's Beading into the destination's side-table slot, skip when the destination has its own `bead_count` ("Don't override local beading", `:1567`) or already has a beading (`:1577`), and NEVER write `bead_count`/`transition_ratio` on a purely-propagated joint (canonical leaves it at `-1`; `propagate_beadings_downward` already documented this exact rule, citing `D-144-ANGLE-FUDGE-NONCENTRAL`), and (c) switching `propagate_beadings_downward`'s three "has beading" gates from `bead_count` to the side table, matching canonical's `hasBeading()` predicate (`:1596-1636`). Canonical's own assert states the intent: `upper_beading.beading.total_thickness <= to->distance_to_boundary * 2` (`:1587`) — a propagated beading is EXPECTED to be thinner than its destination; the surplus is infill, not extrudate. **Verified on the production benchy:** inner-wall moves >3mm **3,313 → 0**; >2mm **4,178 → 0**; max computed width **19.637mm → 0.734mm** (median 0.361, p90 0.611 — Classic's profile is median 0.400 / max 0.509). **D5 coverage unaffected** (per-layer maxX still tracks Classic at ratio ≈1.001 across the whole Z=0.4–6.4 bow). `slicer-core --features host-algos`: **441 passed / 4 failed**, the same 4 known pre-existing failures — no regressions. **Regression pin:** `propagation_upward_copies_beading_without_rescaling_to_thicker_node` (`crates/slicer-core/tests/propagation.rs`) — unit-independent structural invariant on a thickness-gradient fixture (thin 3-bead source feeding a 16x-thicker gap node): the propagated beading must be the source's verbatim, no bead may exceed 2x `optimal_width`, `total_thickness` must not exceed the destination's own, and `bead_count` must stay `None`. The pre-existing `propagation_fills_gap_from_central_neighbor` asserted the DEFECT (`bead_count == Some(4)` on a propagated joint) and was corrected to the canonical contract; it never caught this because its fixture gives every vertex `distance_to_boundary = 5.0` (with uniform thickness, copy-vs-recompute are indistinguishable). **Historical detail (the original OPEN diagnosis + the separate sub-fix) follows.** **Prior status: OPEN (dominant root cause) + one faithful sub-fix landed.** Found via the Arachne Parity Recovery benchy inventory (finding "D4"; `docs/specs/arachne-parity-recovery.md`). **Symptom:** benchy inner walls grossly over-extrude — on the post-D5 slice, 3,313 inner-wall moves compute width >3mm, worst **19.6mm (43× the 0.45mm nozzle)**, all at **Y≈0 (hull medial spine)**, z≈4.6–6.4; Classic has zero such moves. Width is real (M83 relative E). **Mechanism (instrumented host-side `generate_junctions`):** these peaks resolve a STORED beading (`graph.get_beding`) whose `bead_count` is an odd **3 or 5** at thickness `2·to_r ≈ 19.7mm`; `DistributedBeadingStrategy::compute(19.7mm, 3)` returns `[0.4, 18.9, 0.4]` — its Gaussian weights (`distribution_count=1` → `[0,1,0]`) dump the ENTIRE surplus into a single ~18mm centre bead, which is extruded. **`optimal_bead_count(19.7mm)=7` is CORRECT** (verified) and `compute(19.7,7)` = `[0.4×3, 0-sentinels, 0.4×3]` (walls + infill, no giant bead) — so `assign_bead_counts` (`bead_count.rs:141,173`, uses `optimal_bead_count(2·distance_to_boundary)`) is exonerated. The fault is in **beading PROPAGATION / transitions**: a thick medial peak that should carry bead_count 7 (→ over-cap infill) instead carries a transition-region bead_count of 3/5, recomputed at its own large thickness. This is the same code path as D5 (D5 made these peaks emit; D5 coverage is closed, this over-extrusion is the remaining half). Fix requires a faithful comparison of PnP's `propagateBeadingsUpward/Downward` + `getBeading` against OrcaSlicer `SkeletalTrapezoidation.cpp` (source now vendored at `OrcaSlicerDocumented/`). **Sub-fix landed 2026-07-16 (faithful, but does NOT resolve the above):** `max_bead_count` must be EVEN — OrcaSlicer `WallToolPaths.cpp:525` sets it `= 2·inset_count`, its `LimitedBeadingStrategy` ctor warns on odd (`:36-40`), and the odd-`max_bead_count` `compute` branch (a faithful port of `:73`) parks surplus in one wide centre bead. PnP used odd 9 in the arachne-perimeters manifest default, which ALSO shadowed `wall_count` (making it non-functional). Fixed: manifest `max_bead_count` `default 9→0` + `min 1→0`; `arachne_params_from_config` derives `2·wall_count` when absent/≤0. Unit-proven: even caps (6/8/10) → max junction width ≤0.65mm on the captured Z≈0.4 cross-section; odd 9 → 14.75mm. Production benchy is byte-identical before/after (its giant beads use stored bead_count 3/5 < cap, so the cap never engages) — confirming the two bugs are distinct. Core `ArachneParams::default()` (`pipeline.rs:196`) and test `factory_params()` still carry odd 9 (faithfulness follow-up, tangled with the tapered-wedge self-captured baselines). | arachne maintainer | — (closed) | Closed — 2026-07-16: canonical beading pass ORDER was inverted (populate ran last) and `propagate_beadings_upward` propagated the scalar `bead_count` instead of copying the source's Beading, so propagated nodes were recomputed at their own (larger) thickness → giant centre bead. Fixed to canonical order + faithful `propagateBeadingsUpward` port + side-table `hasBeading()` gates. Benchy inner-wall max width 19.637mm → 0.734mm, moves >3mm 3,313 → 0; D5 coverage unaffected; slicer-core 441 passed / 4 pre-existing failures. Pinned by `propagation_upward_copies_beading_without_rescaling_to_thicker_node` |
| D-105E | 2026-07-13 | crates/slicer-core/src/beading/limited.rs:185 (now corrected) | Low | The D-105 beading fix introduced a literal `0.01` threshold in `optimal_bead_count`'s transition-zone branch, but `limited.rs` operates in slicer units (1 unit = 100 nm per `docs/08_coordinate_system.md`), so the literal `0.01` = 1 nm. OrcaSlicer uses `scaled<coord_t>(0.01)` = 0.01 mm = 100 slicer units — a 10,000× discrepancy. The sibling files `crates/slicer-core/src/beading/distributed.rs:199` and `crates/slicer-core/src/arachne/generate_toolpaths.rs:230` already use the correct `0.01 * UNITS_PER_MM` convention. Fixed by replacing the literal with `0.01 * UNITS_PER_MM`. Functionally near-equivalent for mm-scale tests, but not faithful and would diverge on fine geometry; scale errors compound. | slicer-core maintainer | — (closes on this commit) | Closed — 2026-07-13 (this revision) |

| DEV-081 | 2026-07-01 | modules/core-modules/lightning-infill/src/lib.rs / docs/adr/0025-infill-linker-as-raw-emit-post-pass.md | Low | **Infill-parity effort (transitional).** ADR-0025 establishes Architecture A: `Layer::Infill` modules emit raw unlinked segments; a dedicated `Layer::InfillPostProcess` `infill-linker` module is the single place infill path connection happens. `rectilinear-infill` and `gyroid-infill` are rewritten to emit raw (this effort). `lightning-infill` (out of parity scope) currently self-links its own output, which is inconsistent with Architecture A. Until lightning switches to raw emit in a follow-up packet, the infill-linker handles both: it links raw segments from rectilinear/gyroid, and already-linked paths from lightning pass through unchanged (the linker links raw segments; already-linked paths are not re-broken unless re-clipping changes them). Not a blocker for the parity effort. **Update 2026-07-01 (grilling):** full OrcaSlicer lightning parity was pulled into the roadmap (ADR-0029, `docs/specs/lightning-infill-parity.md`, packets 137–140); this deviation closes at packet `140_lightning-module-rewrite`. Also noted: the pass-through premise is weaker than originally written — `ExtrusionPath3D` carries no module identity, so the linker cannot detect already-linked paths; the raw-emit conversion is the real fix. | core-modules maintainer | packet 140 | Open — closes at packet 140 (lightning module rewrite per ADR-0029) |

| DEV-082 | 2026-07-01 | modules/core-modules/gyroid-infill/gyroid-infill.toml (claims) / modules/core-modules/gyroid-infill/src/lib.rs (top/bottom/bridge emission) / docs/adr/0027-gyroid-multi-role-fill-holder.md | Low (opt-in; default config keeps rectilinear for solid) | **Infill-parity effort — intended as a deliberate divergence, but shipped incomplete.** PnP `gyroid-infill` is meant to be able to fill solid shells (top/bottom/bridge) as a user option; OrcaSlicer's gyroid is sparse-only (`Fill.cpp:926`). The emission code for this exists (`src/lib.rs:180-210`, gated by `region.should_emit(gate_role)`), but **the manifest never actually declared the claims it needs** — `gyroid-infill.toml` has only ever declared `claim:sparse-fill` (confirmed via full git history; this was never added, not a regression). Consequence: a user who sets `top_fill_holder = "gyroid-infill"` today gets silent no-op output for that role — the advertised opt-in feature is dead on arrival. **Found 2026-07-03** while verifying open deviations; **fix already scoped**: `.ralph/specs/135_gyroid-raw-emit/packet.spec.md` adds `claim:top-fill`, `claim:bottom-fill`, and `claim:bridge-fill` to the manifest (AC verifies all four claim lines are present) as part of realizing this deviation. ADR-0027's divergence rationale is still valid — only the manifest wiring was missing. | infill-modules maintainer | packet 135_gyroid-raw-emit | Open — 2026-07-03: manifest never declared the required claims (dead-on-arrival bug, not a shipped divergence); fix scoped in packet 135_gyroid-raw-emit |

| D-109-SELF-CAPTURED-FIXTURES | 2026-07-02 | crates/slicer-runtime/tests/integration/perimeter_parity.rs §fixture provenance (lines ~749-770) / docs/specs/perimeter-modules-orca-parity-roadmap.md §M1 (T-101) | Medium | **Packet 109 (M1 verification) — accepted limitation.** Roadmap T-101 asked to "Record OrcaSlicer reference outputs for 6 M1 fixtures". This environment cannot run OrcaSlicer, so the six M1 parity fixtures are SELF-CAPTURED baselines recorded from this pipeline's own output (see the provenance comment in `perimeter_parity.rs`). The harness therefore proves self-regression (geometry stays stable across commits), NOT per-vertex OrcaSlicer parity — it can detect drift from our own prior output but never divergence from OrcaSlicer. The roadmap's M1 "COMPLETE" claim is scoped accordingly. User accepted this limitation 2026-07-02 (packets-102–109 review). | slicer-runtime maintainer | — (accepted; revisit if an OrcaSlicer reference-capture environment becomes available) | Open — accepted limitation, user decision 2026-07-02; fixtures guard self-regression only |

| D-111-ARACHNE-SENTINEL-STRIP | 2026-07-03 | crates/slicer-core/src/beading/limited.rs §`LimitedBeadingStrategy::compute` / `compute_and_strip` / docs/specs/perimeter-modules-orca-parity-roadmap.md §D-9 (T-215, T-215b) | Low | **Packet 111 (BeadingStrategy stack) — implements the roadmap's D-9 decision.** `LimitedBeadingStrategy::compute` retains zero-width sentinel beads internally (an OrcaSlicer bookkeeping mechanism downstream centrality propagation reads to keep bead-index alignment), but they are stripped before external output via a separate `compute_and_strip` entry point — because this codebase's `WallLoop` WIT-boundary type invariant requires `bead_widths.iter().all(\|&w\| w > 0.0)`, avoiding a contract change at that boundary. `compute` (without strip) still returns sentinels for invariant testing (AC-6/AC-N2); `compute_and_strip` is the entry point P112's wire-up (T-230) is expected to call, and its output carries no zero-width entries while `toolpath_locations.len() == bead_widths.len()` continues to hold (AC-7). Note: D-9 (the original decision to strip rather than coordinate zero-width sentinels with downstream infill modules) is a roadmap-level ID that lives in `docs/specs/perimeter-modules-orca-parity-roadmap.md`, NOT in this log — this entry documents the IMPLEMENTATION of that already-made decision, not a new decision. | slicer-core maintainer | — (closed) | Closed — 2026-07-03: `compute_and_strip` implemented and covered by AC-7/AC-N2 tests; the strip-pass is a permanent design choice (not a temporary workaround) per D-9 |

| D-112-WALL-GENERATOR-SELECT | 2026-07-03 | crates/slicer-scheduler/src/execution_plan.rs §`dedup_same_claim_modules` / `wall_generator_preferred_module_id` / `modules/core-modules/{classic,arachne}-perimeters/*.toml` §`[claims]` | High | **Packet 112 Step 10 — production defect, closed same-packet.** Both `classic-perimeters` and `arachne-perimeters` declare `claim:perimeter-generator`; the production live-loader's claim-dedup pass ran with no config input and silently kept the alphabetically-first candidate (`arachne-perimeters`) with no way for a user's config to express intent, and `incompatible-with` never fired because dedup runs before `validate_startup_dag`. Root wiring gap dates to P110's skeleton (the moment a second real `perimeter-generator` claimant existed, the pre-existing alphabetical-default dedup became live and wrong). **Closed by this packet:** a new `wall_generator` config key (`"classic"` or `"arachne"`, default `"classic"` — mirrors OrcaSlicer's own `wall_generator` setting) is read directly from the raw config source at module-load time (before `ResolvedConfig` exists) via `WALL_GENERATOR_CONFIG_KEY`/`DEFAULT_WALL_GENERATOR`; `dedup_same_claim_modules_with_wall_generator` resolves the `perimeter-generator` claim by this key instead of alphabetical order, falling back to `classic` if the preferred module isn't among the candidates. Verified by `same_claim_same_stage_defaults_to_classic_wall_generator_and_emits_diagnostic`, `wall_generator_config_selects_arachne_despite_default_classic`, and `wall_generator_unrecognized_value_falls_back_to_classic_default` in `execution_plan.rs`, and live via `pnp_cli`. | slicer-scheduler maintainer | — (closed) | Closed — 2026-07-03: `wall_generator` config key resolves the claim at dedup time; default preserves every existing classic-perimeters golden |

| D-112-TOOLPATH-WIDTH | 2026-07-03 | crates/slicer-core/src/arachne/generate_toolpaths.rs | Medium | **Packet 112 Step 4 → reworked Step 9D.** The initial `generate_toolpaths` (T-223) derived per-junction widths geometrically (a `2 * distance_to_boundary / bead_count` approximation), bypassing `BeadingStrategy::compute()` entirely — so none of the beading stack's actual behavior (`WideningBeadingStrategy`'s `min_output_width` clamp, `DistributedBeadingStrategy`'s Gaussian width distribution, `LimitedBeadingStrategy`'s cap) was observable in emitted toolpaths; a thin feature would not widen to `min_bead_width` no matter what the config said. **Closed in Step 9D** by reworking `generate_toolpaths` to call `strategy.compute(2.0 * r_start, n)` / `strategy.compute(2.0 * r_end, n)` per central-edge endpoint and read each bead's width/offset directly from the returned `Beading::bead_widths`/`toolpath_locations`, making the composed strategy stack's output the actual source of emitted widths. The committed `toolpaths_tapered_wedge.json` baseline was re-recorded because the numeric widths shifted (qualitative invariants unchanged) — see that fixture's own re-recording note. | slicer-core maintainer | — (closed) | Closed — 2026-07-03: Step 9D rework; thin-wall now genuinely widens to `min_bead_width` via the real strategy stack |
| D-161-ADR-0037-AMENDED | 2026-07-15 | docs/adr/0037-render-pngs-from-ir-stage-taps-not-gcode-only.md §Decision (synthetic-diagram clause) / docs/specs/visual-pipeline-debug.md §Stage Tap Inventory / crates/slicer-runtime/src/visual_debug_render.rs | Low | **Packet 161 — retires ADR-0037's synthetic-diagram render mode.** ADR-0037's Decision directed that stages without 2-D printable geometry "emit stage-specific synthetic diagrams of trace-relevant documented IR fields rather than fabricated geometry." Grounding the full tap inventory during packet 161 showed no implemented tap needs that mechanism: RegionMapping renders real `SliceIR` geometry via the region-key join on `(global_layer_index, object_id, region_id, variant_chain)` tinted by `RegionPlan`, and LayerPlanning — the only stage with no joinable geometry — becomes an opt-in `diagnostic_overlay` annotation on geometry taps rather than a standalone tap. ADR-0037 was amended in place (its 2026-07-15 "Amendment" section quotes the retired clause and states the replacement); the never-fabricate-geometry principle and every other ADR-0037 clause (runtime-owned, request-gated taps reading committed IR, no new module/WIT/Blackboard API) stand. Recorded here per the preflight S8 ADR-conformance gate. | slicer-runtime + core-modules maintainers | — (closes at packet 161 activation) | Open — amendment recorded in ADR-0037 (2026-07-15); enacted by packet 161 (draft) |

| D-112-THIN-WALL-WIDENING | 2026-07-03 | modules/core-modules/arachne-perimeters/src/lib.rs §`arachne_params_from_config` / arachne-perimeters.toml §`[config.schema]` | Medium | **Packet 112 Step 9C.** `arachne-perimeters.toml` registers 13 `m_params.*` config keys (T-218, packet 111), but `arachne_params_from_config` only read 6 of them into `ArachneParams` at Step 9's initial wire-up — `min_feature_size`, `min_bead_width`, and `detect_thin_wall` were registered-but-unwired, so a user enabling "detect thin wall" (or tuning the min-width floor) on the arachne path got no behavior change: `WideningBeadingStrategy` never activated. **Closed in Step 9C** by wiring all three into `ArachneParams` (`min_feature_size`, `min_bead_width` via `units_to_mm`; `detect_thin_wall` → `print_thin_walls` bool), bringing the wired count to 9/13. The remaining 4 (`wall_transition_length`, `wall_transition_angle`, `initial_layer_min_bead_width`, `outer_wall_offset`) are registered for `BeadingStrategyFactory` but not yet threaded through this narrower `ArachneParams` — see that struct's own doc comment; not part of this deviation's closure scope. | slicer-core + core-modules maintainer | — (closed) | Closed — packet 113a, Steps 2-3 (T-218): arachne_params_from_config now reads 7 keys (3 net-new manifest entries: min_central_distance, visvalingam_area_threshold, min_width; 4 already-registered from P111: wall_transition_length, wall_transition_angle, initial_layer_min_bead_width, outer_wall_offset). wall_transition_angle and initial_layer_min_bead_width added to BeadingFactoryParams and threaded through BeadingStrategyFactory::create_stack. |

| D-112-CENTRALITY-ADAPT | 2026-07-03 | crates/slicer-core/src/skeletal_trapezoidation/centrality.rs / bead_count.rs | Low | **Packet 113b Step 2 — closed.** `filter_central` now operates on the quad/rib topology from Step 1 (`EdgeType::EXTRA_VD` ribs are unconditionally non-central) and uses the faithful `dR < dD * sin(transitioning_angle/2)` predicate. The per-NODE bead count assignment landed in Step 3 (`bead_count` moved to `STVertex`; `assign_bead_counts` writes per node from the node's `distance_to_boundary`). Residual self-captured regression fixtures remain, as always, self-captured rather than OrcaSlicer-derived — see D-112-SELFCAPTURED-BASELINES. | slicer-core maintainer | — (closed in P113b) | Closed — 2026-07-04: quad/rib topology + faithful `updateIsCentral`/`filterCentral` port landed; per-NODE bead count via quad cell `distance_to_boundary` |

| D-112-PROPAGATION-ADAPT | 2026-07-03 | crates/slicer-core/src/skeletal_trapezoidation/propagation.rs | Low | **Packet 113b Step 4 — closed.** `propagation.rs` now contains a faithful port of `generateTransitionMids` (L925-994), `applyTransitions` (L1487-1543), `propagateBeadingsUpward` (L1800-1826), and `propagateBeadingsDownward` (L1833-1899) from `OrcaSlicerDocumented/src/libslic3r/Arachne/SkeletalTrapezoidation.cpp`. `generate_transition_mids` annotates upward central edges with `TransitionMiddle` positions; `apply_transitions` mirrors ends across twins and splits edges at transition positions; upward/downward propagation copy/blend bead counts on the quad-decorated graph. The `transition_ratio` field on `STVertex` was added for downstream use. Self-captured regression fixtures (`propagation_three_fixtures`) were re-baselined and lock in current behavior. | slicer-core maintainer | — (closed in P113b) | Closed — 2026-07-04: faithful `generateTransitionMids`/`applyTransitions` port landed; propagation re-ported to read quad graph state |

| D-112-SIMPLIFY-DP | 2026-07-03 | crates/slicer-core/src/arachne/simplify.rs | Low | **Packet 112 Step 6 — packet-specified deviation, not an oversight.** `simplify_toolpaths` uses standard Douglas-Peucker per-`ExtrusionLine` simplification (as this packet's own spec/design explicitly called for), whereas OrcaSlicer's `simplifyToolPaths` uses a Visvalingam-like area-based simplification with different width-preservation heuristics. This is a specified scope choice, not a defect; junction widths are preserved across simplification either way (AC-7 asserts this). | slicer-core maintainer | — (accepted; would require a design change to revisit, not a bugfix) | Closed — packet 113a, Step 1 (T-226, DP→VW): Visvalingam-Whyatt port landed; width-weighted area gate via calculateExtrusionAreaDeviationError (OrcaSlicer ExtrusionLine.cpp:248); dp_epsilon renamed to visvalingam_area_threshold across simplify.rs, pipeline.rs::ArachneParams, common.wit arachne-params record, slicer-sdk/src/host.rs mirror, and slicer-wasm-host/src/host.rs generate_arachne_walls. |

| D-112-HOSTSVC-BRIDGE | 2026-07-03 | modules/core-modules/arachne-perimeters/src/lib.rs / crates/slicer-sdk/src/host.rs §`generate_arachne_walls` / crates/slicer-core/src/arachne/pipeline.rs §`run_arachne_pipeline` | Low | **Packet 112 Step 9 — architecture correction from the packet's original design.** `design.md`'s original Step 9 plan called for `arachne-perimeters` (a `wasm32` guest component) to call `slicer-core`'s Voronoi/SkeletalTrapezoidation/beading pipeline in-guest, function by function. That is infeasible: `slicer-core`'s Arachne/SKT/Voronoi code is gated behind the host-only `host-algos` feature (`rayon` + `boostvoronoi` are not WASM-portable), so a WASM guest cannot call it directly — this was discovered during Step 9 implementation, not anticipated at design time. **Resolved** by introducing a new WIT host-service, `generate-arachne-walls` (coincidental name reuse with the unrelated, already-deleted P108 in-guest fake — a different mechanism entirely), mirroring the existing `medial-axis` host service: the guest calls `slicer_sdk::host::generate_arachne_walls(polygons, &params)`, which on native targets calls `slicer_core::arachne::pipeline::run_arachne_pipeline` directly, and on `wasm32` guest builds marshals the call across the WIT boundary to the host, which runs the identical native pipeline on the guest's behalf and returns `Vec<ExtrusionLine>`. The guest module itself only does classification + `ExtrusionLine → ExtrusionPath3D` conversion + `WallLoop` assembly. See `design.md`'s Step 9 correction note for this packet. | slicer-core + slicer-sdk maintainer | — (closed; this is the shipped architecture) | Closed — 2026-07-03: documented as the as-built architecture, not an open gap; `design.md` corrected to match. Formalized 2026-07-05 as `docs/adr/0033-host-service-bridge-for-host-only-algorithms.md` — the reusable pattern this bridge (and the pre-existing `medial-axis` bridge it mirrors) establishes. |

| D-112-SELFCAPTURED-BASELINES | 2026-07-03 | crates/slicer-core/tests/{centrality,bead_count,propagation,generate_toolpaths,stitch,simplify,remove_small}.rs / crates/slicer-runtime/tests/fixtures/perimeter_parity/{tapered_wedge,narrow_strip_widening,max_bead_count_cap,complex_multi_feature,cube_4color_arachne}/ | Medium | **Packet 112 (M2 extension of D-109's accepted limitation) — all Arachne unit and parity fixtures are self-captured regression baselines, not OrcaSlicer goldens.** This environment has no OrcaSlicer binary to record reference output from (same root cause as D-109-SELF-CAPTURED-FIXTURES for M1). Every per-function fixture (centrality, bead_count, propagation, generate_toolpaths, stitch, simplify, remove_small) and every `perimeter_parity` Arachne fixture — including the new `cube_4color_arachne` — is recorded from this pipeline's own first-run output and locked in for regression purposes only; each test file's own doc comment carries an explicit "Honesty note (no OrcaSlicer oracle)" section repeating this. The harness therefore proves self-regression (geometry/topology stays stable across commits), NOT per-vertex or per-edge OrcaSlicer parity — matching the `perimeter_parity.rs` harness-header convention established by D-109. Independent OrcaSlicer geometric parity for the Arachne pipeline is deferred to a follow-on, contingent on an OrcaSlicer reference-capture environment becoming available. | slicer-core + slicer-runtime maintainer | — (accepted; revisit if an OrcaSlicer reference-capture environment becomes available) | Open — accepted limitation, matches D-109's precedent; all M2 fixtures guard self-regression only |

| D-112-MMU-TOPOLOGY | 2026-07-03 | crates/slicer-runtime/tests/executor/cube_4color_arachne.rs | Low | **Packet 113b Step 5 landed the topology pass.** `arachne-perimeters` walls are a junction graph (open, multi-degree-junction bead segments), not classic's closed simple-ring topology. The structural `cube_4color_arachne` assertions (4 distinct per-color outer-wall headers, non-trivial extrusion counts, all-finite coordinates) remain the acceptance criterion. **2026-07-05 re-verification:** new test `cube_4color_arachne_per_color_footprint_within_bbox` (P113b closure) asserts per-color outer-wall header bboxes are contained in the corresponding `paint_segmentation` per-color ExPolygon bbox + 2 mm tolerance. The test FAILED: 113 headers escape, worst case layer 37 tool 2 by 15.264 mm. **2026-07-05 remediation pass (same day, continued) — three real fixes landed and verified, symptom reduced by ~19x but not fully closed:** (1) the `connectJunctions` quad-chain merge was generalized from a rare rib-corner-only walk to cover every central-edge domain, plus a double-emission bug fix (see D-113B-CONNECTJUNCTIONS for detail) — this alone did not move this test's numbers, since the escaping geometry it fixed wasn't this test's dominant failure mode; (2) `generate_junctions`'s position formula was replaced with OrcaSlicer's faithful two-endpoint interpolation (replacing an unsided 90°-rotated perpendicular offset) — this was the single largest lever, cutting the worst case from 15.264mm/113 violating headers to 8.727mm/8 headers (isolated to tool 3, layers 30-38); (3) a mislabeling bug was found and fixed in `crates/slicer-runtime/src/layer_executor.rs::assemble_ordered_entities` — the point-in-polygon spatial-tool fallback outranked a region's own tagged `variant_tool`/`paint_tool`, so a correctly-generated BASE-region wall segment landing (by proximity) inside a neighboring painted cell got its tool index spatially reattributed to that cell's colour; precedence was corrected so `paint_tool`/`variant_tool` wins for BASE and already-tagged regions, restricting the spatial fallback to genuinely untagged/LIFO-collapsed regions — this shrank the failure further to **9.970 mm worst case, 7 violating headers (layers 24, 32-37, tools 1 and 3)**. **Root cause re-attributed for the residual 7 headers:** instrumentation during the same pass proved arachne's own wall geometry for the actual narrow per-color cell stays correctly within that cell's own input-polygon bbox — the residual is not a spatial-crossing or attribution bug but is consistent with a separately-diagnosed **"busy-hub" duplicate-edge graph-construction artifact** (near-identical converging central edges at certain corners in the `skeletal_trapezoidation` graph, producing malformed junction geometry near those hubs) — the same root cause independently implicated in the sibling test `cube_4color_arachne_fragments_walls_by_color`'s degenerate short-header failures (16 remaining, down from 22, same tool/layer band). Fixing this requires graph-construction work in `skeletal_trapezoidation::rib`/`centrality.rs` (duplicate/near-duplicate edge suppression near converging Voronoi vertices) or an extension to `stitch_extrusions` to absorb short orphan stubs into their true neighbor — both out of scope for the `generate_toolpaths.rs`/`layer_executor.rs` file surface this remediation pass was scoped to. All three landed fixes are independently verified non-regressive (AC-5 through AC-N3 pass; `cargo test -p slicer-runtime --test executor` — 178/180 pass, same 2 pre-existing failures, no new ones; `cargo xtask build-guests --check` clean). **2026-07-05 (6th pass) — busy-hub ROOT-CAUSED and FIXED; `cube_4color_arachne_fragments_walls_by_color` now fully closed.** Direct graph instrumentation (temporary, reverted) of the specific tool1-triangle input cell (a right-isoceles paint-segmentation cell, bbox (125,92.5)-(137.5,117.5) mm) proved the busy hub is **not** degenerate input geometry (the cleaned per-color cell polygons have well-spaced vertices, no near-duplicates down to 0.05mm) and **not** a boostvoronoi/Voronoi-construction artifact — it is a concrete bug in this crate's own graph-construction port, in `skeletal_trapezoidation::propagation`'s `apply_transitions`/`insert_node` (bead-count transition-insertion), three compounding defects: (1) `insert_node` cloned the split edge's `next`/`prev` verbatim without rewiring the DCEL chain, so every repeated split along the same physical edge (e.g. 8 bead-count steps approaching a sharp R→0 corner) produced edges that all claimed the *same* stale next/prev pointers as the original, corrupting `generate_toolpaths::walk_domain_chain`'s traversal; (2) `insert_node` additionally overwrote the newly-split edge's `twin` field to point at the wrong (near) endpoint instead of preserving the original far endpoint, so `resolve_to_vertex` on every newly-split edge resolved back toward the split's own start rather than its true far end; (3) `apply_transitions`'s twin-mirroring step (meant to seed the *twin* edge's own split points at `1 - pos`) pushed those mirrored ends onto the *same* edge's own list instead of `edge.twin`'s, doubling each qualifying edge's split count with a second, wrongly-mirrored set. Combined, repeated splits along the same edge re-interpreted each transition's position fraction against an ever-shrinking mis-resolved sub-edge, producing a geometric-convergence cascade of 12-14 near-identical vertices collapsing onto a single polygon corner (confirmed via direct vertex/edge dump: `distance_to_boundary` shrinking 0.5→0.068→0.012→0.0006→...→0.0000mm across successive vertices, non-monotonic bead_count 2,3,...,8,8,7,...,3,0). Fixed by rewiring `orig.next`/`new_edge.prev`/the DCEL follower's `.prev` in `insert_node`, no longer overwriting `new_edge.twin` (letting it inherit the original far-endpoint reference), and re-targeting the twin-mirror push in `apply_transitions` at `edge.twin`'s own end-list plus rescaling each successive same-edge split's position fraction onto the remaining (already-shrunk) sub-edge instead of the stale original-edge fraction. Verified: `cube_4color_arachne_fragments_walls_by_color` (the sibling degenerate-short-header test) now **passes outright** (16 violating headers → 0); `cargo test -p slicer-runtime --test executor` is **179/180** (up from 178/180, no new failures, the other pre-existing failure unchanged); the self-captured `toolpaths_tapered_wedge.json` regression baseline was re-recorded (its old `inset_count: 1` was itself a symptom of this same bug — the fix now correctly produces `inset_count: 9`, matching that fixture's own doc-comment expectation of nine central edges). **This test (`cube_4color_arachne_per_color_footprint_within_bbox`) is NOT closed by this fix** — worst case is now 10.242mm/14 headers (was 9.970mm/7): fixing the busy-hub corruption surfaces *more* real (non-degenerate) wall geometry, which in turn reveals a **second, separate, unfixed mechanism**: some outer-wall (`inset_idx == 0`, even-bead) `ExtrusionLine`s carry junctions whose interpolated width rounds to (or genuinely is) ~0 near a sharp R→0 corner, which gcode-emits as literal `E0.00000` moves covering real, non-trivial XY distance (confirmed via raw gcode inspection: two `E0.00000` moves of ~19mm and ~10mm XY length inside one `;TYPE:Outer wall` block) — the test's own header-parser (by its documented design) records *any* line carrying an E token regardless of magnitude as a real extruded point, so these near-zero-flow "phantom" segments inflate the header's bbox with coordinates that are technically traced but never actually extrude material. `remove_small_lines` cannot catch this today: it only ever removes odd (`is_odd == true`), non-closed lines by *length*, never even/outer-wall (`inset_idx == 0`) lines, and never checks junction *width*. This is a distinct defect from the busy-hub artifact (a bead-width/near-corner degeneracy in outer-wall emission, not a duplicate-edge/DCEL-chain artifact) and needs its own follow-on investigation — likely either clamping/dropping near-zero-width junctions in `generate_junctions`/`emit_chain_lines`, or extending `remove_small_lines` (or a new pass) to cover even/outer-wall lines by a width, not just length, criterion. Not attempted in this pass per the task's explicit caution against ad-hoc guards in this file causing prior whack-a-mole regressions. | slicer-core + core-modules + slicer-runtime maintainer | test-harness fix, not a code packet (superseded by 9th pass, 2026-07-05): realign `cube_4color_arachne_per_color_footprint_within_bbox`'s reference paint-segmentation layer plan (`build_50_layer_plan`/`run_paint_segmentation`, hardcoded `LAYER_COUNT=50`/`LAYER_HEIGHT_MM=0.5`) with the real `run_slice(wall_generator=arachne)` pipeline's own resolved layer height/count (124 layers @ ~0.2mm for this fixture) — or re-pair each gcode header to its cell by matching the header's actual Z (from its layer's `;Z:` gcode comment) against `painted_ir[i].z`, instead of assuming `per_layer[li]` and `painted_ir[li]` share an index. Maintainer decision required; not implemented this pass per CLAUDE.md's "never weaken assertions to get a pass" guidance (this is a test-side defect, not a production one). **Superseded 2026-07-05 (10th pass): the nearest-Z re-pairing half of this Follow-up WAS implemented** (`parse_layer_z_values`/`nearest_painted_ir_index` in `cube_4color_arachne.rs`) and cut the failure from 113 headers/15.264mm to 10 headers/10.5mm, but did not close it — see this row's Status column for the residual mechanism. Remaining follow-up option: rebuild `build_50_layer_plan`'s reference at the real pipeline's own resolved layer height/count (the previously-deferred fix (a)) so the reference grid can resolve the tool1/tool3 paint-color boundary at the same fidelity as the real gcode, instead of aliasing across a 0.5mm sampling step; alternatively, accept the residual as a documented reference-resolution limitation and rely on `cube_4color_arachne_fragments_walls_by_color`'s structural assertions for this invariant. | Closed — 2026-07-05 (11th pass): reference plan rebuilt from real gcode Z values, eliminating the aliasing that caused the residual 10-header failure. cube_4color_arachne module and full executor binary (180/180) both green. **Addendum 2026-07-05 (12th pass):** this closure's non-regression claim was validated only against narrow per-crate runs; a subsequent full `cargo xtask test --workspace --summary` surfaced one `slicer-runtime`/`integration` failure this scope had missed — `crates/slicer-runtime/tests/fixtures/perimeter_parity/tapered_wedge`'s self-captured baseline (see D-112-SELFCAPTURED-BASELINES) was stale against this session's topology fixes (wall count 32→27). Re-baselining it via the existing `record_tapered_wedge` recorder then exposed, one at a time (the `arachne_perimeter_parity` test panics on its first mismatch, masking the rest), that all 5 of that test's self-captured Arachne fixtures were stale, not just this one: `narrow_strip_widening` (2→1 wall), `max_bead_count_cap` (12→20), `complex_multi_feature` (66→41), `cube_4color_arachne` (9→12, one region among many). All 5 re-recorded via their documented `#[ignore]`d `record_*` functions (mesh/config byte-identical, only `expected_perimeter_ir.json` changed); all fixture-specific behavioral assertions still pass against the new baselines. `perimeter_parity` module: 10/10 non-ignored pass; full `integration` binary: 216/216; `cargo xtask build-guests --check` clean. Lesson: a green narrow-scope run does not guarantee a green full-workspace run when self-captured baselines are involved — the full run remains necessary at packet-close acceptance. **Traceability note (2026-07-05):** passes 6-12 above (everything after the initial Step 5 topology pass) were un-packeted remediation work performed directly against the live tree, not tracked under a numbered packet — unlike D-112-CENTRALITY-ADAPT/D-112-PROPAGATION-ADAPT (packet 113b) or D-112-SIMPLIFY-DP/D-112-THIN-WALL-WIDENING (packet 113a). Recorded here in full detail as the only paper trail for this work; a future packet formalizing this pattern should retroactively assign these passes a packet ID. **Addendum 2026-07-05 (packet 113c):** Superseded — see `D-113C-FAITHFUL-GRAPH-CONSTRUCTION`; this entry's prior 'Closed' status was based on an insufficient interim fix (the 12 passes above patched symptoms of connectJunctions/propagation bugs without replacing the underlying verbatim-DCEL-copy graph construction and reflex-corner-only rib insertion that were the actual root cause). |

| D-113B-WIDE-REGION-COORD-INSTABILITY | 2026-07-05 | crates/slicer-core/src/arachne/pipeline.rs (suspected: skeletal_trapezoidation graph construction for large/complex input polygons) | Medium | **Preliminary, unverified finding surfaced as a side effect of the D-112-MMU-TOPOLOGY remediation pass — needs independent reproduction before any fix is attempted.** While instrumenting `run_arachne_pipeline` to compare per-region input/output bboxes (temporary `eprintln!`, fully reverted, not committed), some pipeline invocations for the WIDE "base/residual" region (the un-painted region spanning the model's full painted-face width, as opposed to a narrow single-colour cell) were observed producing `generate_junctions` bead-0 output points wildly outside any plausible bound for the input geometry — e.g. an input bbox of roughly (112.5,92.5)-(137.5,117.5) mm producing output points as far as (939,932) or y=-93. These never surfaced as a visible test failure in the current suite — something downstream (stitching, simplification, or small-line removal) apparently discards or never routes them into a checked G-code header — so no regression is currently observable, but this looks like a real numerical-instability bug (most likely in Voronoi/graph vertex placement for larger, more topologically complex input polygons) independent of the two issues that pass targeted. Not reproduced with a dedicated, committed test; not root-caused; not fixed. Flagged here so it isn't lost rather than left as tribal knowledge in a worker transcript. **2026-07-05 (7th pass) — CONFIRMED (no longer preliminary) and root-caused one level further.** Independent instrumentation (temporary, reverted) targeting `cube_4color_arachne_per_color_footprint_within_bbox`'s specific tool-1 triangular-cell escape (a different symptom than this entry's original wide-base-region finding, but traced to the same mechanism) confirmed the wild-coordinate vertices are already present in `graph.vertices` immediately after `SkeletalTrapezoidationGraph::from_polygons` returns — before `filter_central`, `assign_bead_counts`, `apply_transitions`, or `generate_toolpaths` run. `crates/slicer-core/src/voronoi.rs::voronoi_from_segments` passes `boostvoronoi::Builder`'s own `diagram.vertices()` output through verbatim (no unit-conversion or scaling bug on this crate's side), so the wild coordinates are `boostvoronoi`'s own computed Voronoi-vertex positions for this input. Critically, the SAME 2-D input bbox (a fixed triangular paint-cell column, constant across many consecutive layers) produces a clean, correctly-bounded graph on most layers and a corrupted graph (one vertex tens to hundreds of thousands of mm away, with an x≈y "runaway along a ~45° bisector" signature matching this cell's own right-isoceles sharp corner) on a minority of layers where the cross-section's actual shape develops a slightly different, more acute corner angle — strong evidence this is triggered by a specific near-degenerate corner/segment configuration, matching the "near-collinear-within-`epsilon_offset`" degeneracy class `docs/adr/0023-arachne-port-strategy.md` explicitly assigns to `preprocess.rs` (T-204) pre-snapping, not to `voronoi_from_segments` itself. Magnitude ranges from catastrophic (hundreds of thousands of mm, clearly discarded/inert downstream, matching this entry's original "never surfaces as a test failure" note) down to moderate (~17-30mm), and the moderate end is now believed to be the actual root cause of `D-112-MMU-TOPOLOGY`'s residual `cube_4color_arachne_per_color_footprint_within_bbox` failures (same order of magnitude as that test's 5-10mm escapes). Still not fixed — see D-112-MMU-TOPOLOGY's 2026-07-05 (7th pass) note for why a fix was out of scope for that pass (requires `preprocess.rs` pre-snap hardening or a defensive bounds-check on `boostvoronoi` output, a different and riskier file/undertaking than either pass's brief). | slicer-core maintainer | — (closed 2026-07-05, 8th pass) | Closed — 2026-07-05 (8th pass): implemented option (c) from the D-112-MMU-TOPOLOGY 8th-pass investigation — a defensive, empirically-grounded bounds-check/clamp on raw `boostvoronoi` vertex positions in `SkeletalTrapezoidationGraph::from_polygons` (`crates/slicer-core/src/skeletal_trapezoidation/graph.rs`: new `clamp_implausible_vertex`/`segments_bbox` helpers, plus 4 new unit tests including a direct regression reproduction of the captured `(1_125_000.0, -218_820.06)` runaway vertex). Margin = `max(5% of the input polygons' own bbox diagonal, 1mm)`, grounded in a 451-call empirical sweep of this crate's own `cube_4color_arachne_per_color_footprint_within_bbox` test run: legitimate-vertex floating-point noise never exceeded ~0.47% of bbox diagonal, while every genuine `boostvoronoi` corruption escaped by at least ~18.5% — a clean ~40x gap with no borderline cases, and 5% sits centrally in it (10x above noise, 3.7x below the smallest real corruption). Verified via direct re-measurement: worst-case vertex escape across the same 451 calls dropped from up to 1,102,107mm (a captured catastrophic case) to a maximum of 2.4981mm (matching the designed margin's diagonal-corner case, `margin * sqrt(2)`) — the wild-coordinate-escape mechanism this entry documents is conclusively fixed. Re-verified non-regressive: `cargo test -p slicer-runtime --test executor` 179/180 (identical failure list, no new failures); AC-1 through AC-N3 (`skeletal_trapezoidation`, `centrality`, `bead_count`, `propagation`, `generate_toolpaths`, `stitch`, `simplify`, `remove_small`) all pass; `cargo xtask build-guests --check` clean. Note: this fix does **not** close `D-112-MMU-TOPOLOGY`'s `cube_4color_arachne_per_color_footprint_within_bbox` test, whose residual 14 violations are proven (same pass) to be a *distinct*, still-open mechanism unrelated to Voronoi vertex-position corruption — see that entry's 8th-pass note. |

| D-113B-ORDERED-ENTITIES-COUNTER-RACE | 2026-07-05 | crates/slicer-runtime/tests/unit/layer_collection_builder_tdd.rs::macro_drain_invokes_host_get_ordered_entities_exactly_once / crates/slicer-wasm-host/src/host.rs (HOST_GET_ORDERED_ENTITIES_TOTAL_CALLS) | Low | **Found during the P113b D-112-MMU-TOPOLOGY remediation session's final full-workspace acceptance run — pre-existing, unrelated to this packet, not fixed here.** A full `cargo xtask test --workspace --summary` run surfaced a flaky failure in `slicer-runtime`'s `unit` test binary: `macro_drain_invokes_host_get_ordered_entities_exactly_once` expects the `#[slicer_module]` macro's host round-trip counter to read exactly 1 but observed 3/7/9 across different runs. Root-caused via mechanistic code-path analysis plus empirical stash/pop A-B testing (3 runs each on the pre-session baseline tree and the session's fixed tree; both showed the same ~1-in-3 flake rate with different, non-reproducible counts): `HOST_GET_ORDERED_ENTITIES_TOTAL_CALLS` is a single process-wide `AtomicU32` (`crates/slicer-wasm-host/src/host.rs:2777`) that this test resets and reads, but sibling tests in the SAME `unit` binary (`path_ordering_tdd.rs`, `tool_ordering_tdd.rs`) also drive real `Layer::PathOptimization` WASM dispatches that increment the same counter, and Rust's default test harness runs tests concurrently on multiple threads — so the target test's reset races against unrelated sibling tests' increments. Confirmed NOT caused by this packet's `layer_executor.rs::assemble_ordered_entities` change: that function is a pure in-memory helper that runs entirely before the WASM guest is invoked and never crosses the WIT/host boundary, so it structurally cannot touch this counter. **2026-07-05 (fix pass) — CLOSED via "serialize the affected tests."** Switching to the per-`HostExecutionContext` call-count field was ruled out: `WasmRuntimeDispatcher::dispatch_layer_call`'s returned `HostExecutionContext` is consumed by `deconstruct_layer_ctx` into `Option<LayerStageCommit>` (`crates/slicer-wasm-host/src/dispatch.rs`), which has no slot for this diagnostic, so plumbing it through `execute_per_layer` would require a production-code schema change for a test-only counter. Instead added `crate::common::ordered_entities_counter_lock()` (`crates/slicer-runtime/tests/common/mod.rs`, an `OnceLock<Mutex<()>>` following the file's existing `wasm_cache.rs`/`slicer_cache.rs`/`model_cache.rs` cache-lock convention) and acquired its guard as the first statement in every test in the `unit` binary that touches `HOST_GET_ORDERED_ENTITIES_TOTAL_CALLS` via a real `Layer::PathOptimization` WASM dispatch: the target test in `layer_collection_builder_tdd.rs`, 5 of 6 tests in `path_ordering_tdd.rs` (the 6th, `no_module_proposal_leaves_raw_assembled_order`, uses a pure stub runner that never touches WASM and was left unlocked), and all 3 tests in `tool_ordering_tdd.rs`. Verified: `cargo test -p slicer-runtime --test unit` run 11 times consecutively (8x filtered to the 10 affected tests, 3x the full 75-test binary) — 0 failures, 0 flakes, versus the pre-fix ~1-in-3 rate. `cargo check -p slicer-runtime --all-targets` and `cargo clippy -p slicer-runtime --all-targets -- -D warnings` both clean; `cargo xtask build-guests --check` clean (no guest-affecting paths touched). | slicer-runtime + slicer-wasm-host maintainer | — (closed 2026-07-05, fix pass) | Closed — 2026-07-05: serialized all real-WASM-dispatch tests sharing the process-wide counter behind a new `common::ordered_entities_counter_lock()` mutex; 11 consecutive test runs (8 filtered + 3 full-binary) show 0 flakes. **Traceability note:** surfaced and fixed as un-packeted remediation work during the D-112-MMU-TOPOLOGY session (see that entry's own traceability note), not under a numbered packet. |

| D-113B-CONNECTJUNCTIONS | 2026-07-04 | crates/slicer-core/src/arachne/generate_toolpaths.rs | Low | **Packet 113b Step 5 — closed as pragmatic minimum; upgraded to the real full port 2026-07-05.** Faithful `connectJunctions` port originally landed as a from-first-principles minimum: `EXTRA_VD` ribs excluded from emission, each central spine edge building isolated per-endpoint junction fans, with the full upstream quad-chain prev/next merge deferred and ring closure delegated to `stitch_extrusions`. **2026-07-05 (D-112-MMU-TOPOLOGY remediation pass) — the deferred quad-chain merge is now implemented for real:** the walk that previously only covered `graph.rib.quad_cells` (a rare rib-corner-only structure, near-empty for convex cells) was generalized to walk EVERY central-edge domain via domain-start detection (`!prev`-equivalent) plus a `next`-then-`twin`-hop continuation, matching OrcaSlicer's `unprocessed_quad_starts`/`getNextUnconnected()` design (`SkeletalTrapezoidation.cpp:2260-2368`); `emit_remaining_edge_segments` (the naive isolated-fragment fallback that used to catch almost everything) is now unreachable by construction and was removed. A second, independent bug was found and fixed in the same pass: `process_central_domain` called `emit_chain_lines` twice per domain (once for the forward walk, once for the reverse), double-emitting single-edge domains and artificially splitting multi-edge domains at the peak vertex instead of producing one continuous polyline — fixed by splicing the reversed prev-chain onto the next-chain before a single `emit_chain_lines` call. Separately, `generate_junctions`'s junction-position formula was replaced: the old arbitrary 90°-CCW-rotated perpendicular offset (no side-selection logic, explicitly self-documented as not reconstructing which physical side of the material an inset sits on) was replaced with OrcaSlicer's actual `generateJunctions()` algorithm (`SkeletalTrapezoidation.cpp:2013-2079`) — pure linear interpolation between an edge's two real Voronoi-vertex endpoint positions, parameterized by where the target bead radius falls in `[end_R, start_R]`; correctness of "which side" is inherited for free from the already-correct vertex positions, no perpendicular/rotation step needed. AC-5 through AC-N3 all still pass (re-verified after each of the three changes); `toolpaths_tapered_wedge.json` did not need re-baselining beyond the prior record. See D-112-MMU-TOPOLOGY for the residual symptom these fixes did not fully close and its re-attributed root cause. | slicer-core maintainer | — (closed in P113b; upgraded 2026-07-05) | Closed — 2026-07-05: full quad-chain prev/next merge landed (domain-walk generalization + double-emission fix) and junction-position placement corrected (faithful two-endpoint interpolation replacing the unsided perpendicular offset); both verified non-regressive against AC-5..AC-N3 **Addendum 2026-07-05 (packet 113c):** Superseded — see `D-113C-FAITHFUL-GRAPH-CONSTRUCTION`; this entry's prior 'Closed' status was based on an insufficient interim fix (the quad-chain stitch this entry describes still ran atop the old verbatim-DCEL-copy graph construction, which packet 113c replaced entirely). |

| D-113C-FAITHFUL-GRAPH-CONSTRUCTION | 2026-07-05 | crates/slicer-core/src/skeletal_trapezoidation/graph.rs / crates/slicer-core/src/skeletal_trapezoidation/rib.rs / crates/slicer-core/src/arachne/generate_toolpaths.rs / crates/slicer-core/src/skeletal_trapezoidation/propagation.rs | High | **Packet 113c — faithful per-cell graph construction + interleaved rib insertion, closes-for-real D-112-MMU-TOPOLOGY and D-113B-CONNECTJUNCTIONS.** Real root cause of the systemic outer-wall gcode non-closure defect (100% of outer-wall segments failed to close — 283/283, mean gap 18.7mm — on `resources/cube_4color.3mf`, reproduced even on a bare 10mm square): the old `SkeletalTrapezoidationGraph::from_polygons` copied `next`/`prev`/`twin` verbatim from the raw `boostvoronoi` per-cell DCEL, which is topologically wrong for spine-walking at junctions, and `rib.rs`'s `build_quad_rib_topology` only inserted ribs at reflex corners instead of after every transferred edge as real OrcaSlicer `constructFromPolygons`/`transferEdge`/`makeRib` does. D-112-MMU-TOPOLOGY's twelve remediation passes and D-113B-CONNECTJUNCTIONS's quad-chain stitch upgrade both patched downstream symptoms (junction placement, domain-chain walking, propagation edge cases) without ever replacing this upstream defect. **Fix (Steps 1-8b, all DONE and green as of this entry; Steps 9-10 — fixture re-baseline and end-to-end `cube_4color.3mf` + full-workspace verification — are this packet's remaining steps, tracked separately, not claimed here):** Step 1 added `VCell`/`HalfEdgeGraph::cells` per-cell Voronoi metadata to `voronoi.rs`. Step 2 was a cell-range-walk research spike (findings in this packet's own `design.md`). Step 3 rewrote graph construction in `graph.rs` to walk per-cell faithfully (superseding/deleting `rib.rs`'s reflex-corner-only pass) — confirmed green: a square domain closes into one ring, produces multiple ribs, deterministic. Step 4 replaced the broken central-only `walk_domain_chain` in `generate_toolpaths.rs` with a faithful quad-by-quad `connectJunctions` stitch, and fixed a related latent bug in `emit_chain_lines`'s max_beads computation (was using the MINIMUM fan length across a domain, silently dropping inner-wall beads). Step 5 re-validated `centrality.rs`/`bead_count.rs` against ubiquitous ribs (no logic changes needed). Step 6 re-audited `propagation.rs::insert_node`/`apply_transitions` and found+fixed a 4th DCEL-adjacent bug, distinct from D-112-MMU-TOPOLOGY's 6th-pass three: multi-split rescale direction was inverted for repeated same-edge splits. Step 7 re-validated `stitch.rs`/`simplify.rs`/`remove_small.rs`, no changes needed. Step 8 added a faithfulness invariant suite (`arachne_invariants.rs`) which found a 5th real bug in `propagation.rs::propagate_beadings_downward`: a placeholder `transition_dist` constant made bead-count blending degenerate into unconditional overwrite at every domain-chain stitch, corrupting e.g. a square's corner bead_count from 0 to 9. Fixed in Step 8b by porting the real OrcaSlicer distance/ratio formula (`total_dist` = cumulative source distance, not just edge length; real transition distance sourced from this crate's own `BeadingFactoryParams::default_transition_length`/production config, not a hardcoded stub). | slicer-core maintainer | Step 10 (this packet) | Closed — 2026-07-05: Step 10 (end-to-end verification) landed. New test `cube_4color_arachne_outer_walls_close_end_to_end` (`crates/slicer-runtime/tests/executor/cube_4color_arachne.rs`) confirms **0% closure failures** (0/698 outer-wall sub-loops, mean gap 0.0000mm, across all 124 layers of `resources/cube_4color.3mf`), down from the pre-packet `/diagnose` baseline of 100% (283/283 non-closed headers, mean gap 18.7mm). Full workspace gate (`cargo check --workspace --all-targets`, `cargo clippy --workspace --all-targets -- -D warnings`, `cargo xtask build-guests --check`, `cargo xtask test --workspace --summary`) green. This CLOSES-FOR-REAL both `D-112-MMU-TOPOLOGY` and `D-113B-CONNECTJUNCTIONS`'s prior interim-fix 'Closed' statuses. **Addendum 2026-07-06 (packet 141 chain):** Steps 9-10 (fixture re-baseline and end-to-end `cube_4color.3mf` + full-workspace verification) are inherited by the packet 141 N1-N13 chain, not silently absorbed into this entry. Packet 141's N7 (side table) and N1 (junction generation rewrite) both build on the 113c graph construction; their closure gates (AC-1/AC-2/AC-N1 for N1; structural side-table contract for N7) run against the same end-to-end `cube_4color.3mf` oracle the 113c Step 10 closed, and the e2e delta is recorded in the packet 141 worker return (record-only, Packet F `147` blocks on green). |

| D-141-JUNCTION-BANDS | 2026-07-06 | crates/slicer-core/src/arachne/generate_toolpaths.rs / crates/slicer-core/tests/arachne_junction_upward_half_edge_only.rs / crates/slicer-core/tests/fixtures/arachne/toolpaths_tapered_wedge.json | High | **Packet 141 Step 1 (N7) + Step 2 (N1) — `BeadingPropagation` side table + canonical `generateJunctions` rewrite.** Closes the two red N1 tests (`arachne_parity_red_junction_bands::n1_rectangle_outer_wall_junctions_stay_near_boundary` and `n1_square_outer_wall_junctions_at_outer_bead_radius`) and the new AC-N1 structural test (`arachne_junction_upward_half_edge_only::ac_n1_only_upward_half_edge_emits_junctions`, 3/3 green). **Step 1 (N7) substrate:** added `SkeletalTrapezoidationGraph::beading_propagation: Vec<Option<Beading>>` (parallel to `vertices`, initialised to all-`None` in `from_polygons`), `get_beding(node_id) -> Option<&Beading>` and `get_nearest_beding(node_id, radius_units) -> Option<&Beading>` accessors mirroring OrcaSlicer's `getBeading`/`getNearestBeading` (`SkeletalTrapezoidation.cpp:2091-2127`), and `populate_beading_propagation` to fill the side table from `strategy.compute(2 * v.distance_to_boundary, bc)`. The `propagate_beadings_downward_with_transition_dist` merge branch now overwrites merged vertices' entries with a width/location-blended beading via `interpolate_bead_propagation` (elementwise blend of `bead_widths`/`toolpath_locations`/`total_thickness`/`left_over`). Structural test `arachne_beding_propagation_side_table.rs` pins the contract: side table index-parallel to `vertices`, populated iff `bead_count = Some(_)`, `bead_widths.len() == toolpath_locations.len()` invariant, `get_nearest_beding` radius contract. 7/7 green. **Step 2 (N1) rewrite:** `generate_junctions` rewritten to canonical `SkeletalTrapezoidation.cpp:2013-2079` shape — (a) iterate ALL graph edges with no centrality gate, (b) upward half-edge selection (`from.R < to.R`, the OTHER half of the twin owns the emission), (c) skip flat / same-bead-count edges (`:2024-2027`), (d) resolve ONE beading at the boundary-side vertex (`edge.from`) via `get_beding` (fallback `strategy.compute(2 * from_R, bc)`), (e) in-band bead emission: loop from outer bead (`index 0`) toward middle, break when `bead_R < start_R` (`:2068`), no clamping of out-of-band beads, near-`end_R` snap to the peak vertex (`:2072-2074`). Junction position uses the OrcaSlicer formula `junction = edge.from + (edge.to - edge.from) * (bead_R - start_R) / (end_R - start_R)`. The output vector's positional index matches the bead index so `chain_junctions_for_bead`'s `from_j.get(bead)` lookup returns the correct junction for each `inset_idx = bead` line. **Verification:** `arachne_parity_red_junction_bands` (AC-1 + AC-2) green without weakened assertions (rectangle ≤ 0.6 mm from boundary, square ≤ 0.15 mm from 0.2 mm outer-bead radius); `arachne_junction_upward_half_edge_only` (AC-N1, new structural test) 3/3 green; N2/N3/N4 (`arachne_parity_red_perimeter_index` / `arachne_parity_red_transition_ends` / `arachne_parity_red_is_odd_semantics`) stay RED as expected (scope boundary gate). **Re-baselined fixtures:** `tests/fixtures/arachne/toolpaths_tapered_wedge.json` (re-recorded via self-capture; the new boundary-side beading source produces a different `inset_count` and width-vector distribution than the old per-endpoint interpolation, but each junction still has the expected width variation across the tapered-wedge gradient — verified by re-running the test after the file rewrite). `centrality_*.json`, `propagation_*.json`, `bead_count_tapered_wedge.json` not affected (N7's side table is read-only at this step; N1's rewrite does not change `filter_central` / `assign_bead_counts` / `propagate_beadings_*` outputs). `cargo check -p slicer-core --all-targets` and `cargo clippy -p slicer-core --all-targets --features host-algos -- -D warnings` clean. **Correction 2026-07-06 (post-swarm review, before this row's claims were committed as final):** the Step 2 rewrite described above was itself non-canonical in three ways, found by re-checking the swarm's diff against a fresh OrcaSlicer ground-truth read (not by re-running the tests, which had been made to pass around the bugs): (1) the beading was resolved at `edge.from` (the boundary-side/lower-R vertex) as the PRIMARY path, with the peak (`edge.to`) only a fallback-when-empty — canonical (`SkeletalTrapezoidation.cpp:2029`, `getOrCreateBeading(edge->to, ...)`) always resolves at the peak; (2) each junction's width was recomputed ad hoc via a fresh `strategy.compute(...).bead_widths.first()` call (always index 0, ignoring the bead's own index `idx`) instead of reading `beading.bead_widths[idx]` directly from the one resolved beading (canonical `:2076`, `beading->bead_widths[junction_idx]`); (3) `generate_junctions` retained an `if !edge.central { continue }` + `if edge.edge_type == EdgeType::EXTRA_VD { continue }` gate, and the domain-seeding in `generate_toolpaths` had a matching `central && edge_type == NORMAL` filter freshly added on top of it — both directly contradict canonical, which has "INCLUDES ribs, no exclusion" (ground-truth re-check: no `edge->data.type` check anywhere in `generateJunctions` or in the `unprocessed_quad_starts` seeding). AC-1/AC-2/AC-N1 only passed under the original code because the rectangle/square fixtures happen to touch only bead index 0 (masking bug 2) and never route through a genuinely rib-fed near-boundary path within the test's tolerance band (masking bugs 1 and 3). All three are fixed in place (same file); the domain-seeding/chain-hop was reverted to 113c's original, already-audited `.prev == NO_INDEX` seed + plain `.twin` hop (no bespoke rib-skip function). Fixing these to genuinely match canonical, however, **exposes a real, previously-hidden gap one layer down**: `generate_toolpaths`'s chain walk (`find_quad` + `.twin` hop + width-based `chain_junctions_for_bead` merge) has no concept of a 3-or-more-way junction (canonical's `addToolpathSegment` explicitly checks for this, `SkeletalTrapezoidation.cpp` connectJunctions region) and no longer gets the flat-central-edge connectivity crutch it was implicitly relying on (canonical skips flat edges entirely, `:2024-2027`; ribs are supposed to carry that connectivity instead, but the current walk's un-bounded quad-to-quad hop drives straight through a real branch point — e.g. a square's medial-axis center where 4 diagonal spokes meet — merging unrelated spokes into one fragmented, sometimes 6+mm-wide-junction chain instead of stopping at the branch). This is squarely Packet 142's stated scope ("canonical `connectJunctions` emission: per-quad from/to pairing... `addToolpathSegment`-style line growth"), not a new finding, but it means **Packets 141 and 142 are more tightly coupled than the original plan assumed** — a genuinely-fixed `generate_junctions` cannot make AC-1/AC-2 pass end-to-end without also having 142's chain-walk rewrite in place. Net effect on this row's own test claims: AC-1/AC-2 (`n1_rectangle_outer_wall_junctions_stay_near_boundary`, `n1_square_outer_wall_junctions_at_outer_bead_radius`), `outer_wall_closes_for_simple_polygon`, `generate_toolpaths_tapered_wedge`, `outer_wall_is_closed_ring_for_simple_polygons` (`arachne_invariants.rs`), and `constant_radius_chain_to_junction_lands_at_end_vertex_not_start` / `f3_invariant_chain_has_one_junction_per_endpoint_at_shared_vertex` (`arachne_parity_red_chain_junctions.rs`, itself already marked "not a finding" for a different reason) now correctly FAIL where they previously passed around the bugs; AC-N1 (`arachne_junction_upward_half_edge_only`, 3/3) and the N7 structural suite (`arachne_beding_propagation_side_table`, 7/7) still pass, since those fixtures don't exercise multi-quad chains. `populate_beading_propagation` is now also wired into `run_arachne_pipeline` (`pipeline.rs`) so N7's side table is actually populated in production, not just in its own isolated test — it previously had zero production call sites. `single_edge_domain_emits_each_bead_line_exactly_once` (a pre-existing `generate_toolpaths.rs` unit test, not part of this packet's own oracle) needed its fixture updated for the same from/to-co-location reason AC-N1 already pins as correct (its old "distinct junction positions" sub-assertion encoded the since-corrected per-endpoint-beading design, not a real invariant of an isolated single edge); this was a genuine fixture correction, not an assertion weakening — the core double-emission regression check it exists for is untouched. No production code outside `generate_toolpaths.rs`/`pipeline.rs` was touched; `cargo clippy -p slicer-core --features host-algos --all-targets -- -D warnings` remains clean. **Addendum 2026-07-06 (packet 142 Step 2):** A2's metadata + emission layer (canonical `is_odd` per-segment + `passed_odd_edges` physical-edge key, plus the prior Step 1 `perimeter_index = bead_idx` + canonical `connectJunctions` emission + 3-way-junction chain-walk detection) is now implemented in `generate_toolpaths.rs` and the `arachne_parity_red_is_odd_semantics` AC-2/AC-3 oracles pass — A2 supersedes A1 for the junction-metadata + emission layer only. A1's junction *geometry* (upward-half-edge, in-band, no-clamp, peak-anchored beading) is unchanged. The 141+142 closure is now gated on AC-4 (the 3-way-junction chain-walk fix landing on `arachne_parity_red_junction_bands`); A2's Step 2 metadata changes are independent of AC-4 and verified green against the even-bead-count fixture (`n4_even_bead_count_lines_are_never_marked_odd`, `n4_even_inner_wall_survives_remove_small_lines`). **Correction 2026-07-06 (post-142-Step-1+2, the 1.7mm finding from 142 Step 1's swarm run):** A1's `generate_junctions` had two further non-canonical behaviours that the 142 Step 1+2 commit's new graph topology (the center-of-square medial-axis peak at R=5.0mm with 5 beads, and the 113c rib-construction artifacts) exposed: (1) **zero-length edge skip** — when `from_pos == to_pos` (a PNP-specific graph-construction artifact of packet 113c's rib insertion that canonical's `makeRib`/`insertRib` never produces, since those project the rib endpoint to the foot of the perpendicular on the source segment), the canonical formula `junction = a + (b - a) * t = a` collapses every emitted junction to the peak vertex's position — geometrically the point ON the (zero-length) edge, but R=0.2mm is NOT that point's `distance_to_boundary`. The fix adds a `if (ex - sx).abs() <= f32::EPSILON && (ey - sy).abs() <= f32::EPSILON` guard that inserts an empty `edge_junctions` entry (the downstream `connectJunctions` chain walk stitches the corner from the surrounding non-degenerate rib segments); (2) **`perimeter_index` routed to slot not bead index** — the 142 Step 1+2 commit set `perimeter_index` to the post-reverse slot position, which is the bead index ONLY when all beads are in-band. When the in-band break fires early (e.g. a rib edge from R=1.7mm to a center peak at R=5.0mm with 5 beads, where only the innermost bead is in-band), the slot position no longer equals the bead index, and the surviving junction ends up routed to the wrong inset line (the inset-0 line instead of inset-4). The fix sets `perimeter_index = idx` (the bead index, matching canonical `SkeletalTrapezoidation.cpp:2064-2077`), stores junctions at their `perimeter_index` slot in `from_junctions`/`to_junctions` (sparse, sized to `start_idx + 1`), and updates `emit_chain_lines::has_bead` to check `perimeter_index == bead` (not `bead < vec.len()`). The `tests/fixtures/arachne/toolpaths_tapered_wedge.json` fixture is re-baselined: pre-fix `inset_count: 1, line_counts: [15], junction_widths_mm: [[0.003 × 6, 0.0038 × 2, 0.003 × 7]]` (all junctions in inset-0, innermost-bead-of-center-peak mis-routed there) → post-fix `inset_count: 5, line_counts: [3, 3, 3, 3, 3], junction_widths_mm: [[0.003 × 6] × 4, [0.0038 × 6]]` (junctions correctly routed to their 5 inset lines, innermost-bead of center peak now in inset-4 with the wider width). Measured AC-1/AC-2 before/after: rectangle worst junction was 1.700mm from boundary (at the 1.7mm corner rib degenerate-edge peak), now 0.300mm (5.7× reduction, within the 0.6mm AC bound); square worst junction was 1.500mm from canonical 0.2mm outer-bead radius (at the 5.0mm center peak, all junctions mis-routed to inset-0), now 0.100mm (15× reduction, within the 0.15mm AC bound). AC-3 (`arachne_generate_junctions_canonical_regression`, 3/3) stays GREEN; AC-N1 (`arachne_junction_upward_half_edge_only`, 3/3) stays GREEN; N7 (`arachne_beding_propagation_side_table`, 7/7) stays GREEN; N2 (`arachne_parity_red_perimeter_index`, 1/1) stays GREEN (the test checks `perimeter_index == line.inset_idx`, which holds for both slot and bead-index assignments when all beads are in-band — the test fixture has no early-break edges). `cargo check -p slicer-core --all-targets` and `cargo clippy -p slicer-core --lib -- -D warnings` clean. | slicer-core maintainer | Packet 142 (canonical `connectJunctions` rewrite, now understood to be a hard prerequisite for this row's own AC-1/AC-2, not an independent follow-on) | Closed — 2026-07-06: joint 141+142 closure; `generate_junctions` (N1+N7) genuinely canonical and ground-truth-verified; AC-1/AC-2 green (rectangle 0.300mm, square 0.100mm); 142's chain-walk 3-way detection + `connectJunctions` emission landed and all AC-4 sub-tests green; A1's two further corrections (zero-length edge skip + `perimeter_index = idx` sparse storage) landed in `a554ed36`. **Addendum 2026-07-07 (packet 144):** Packet 144 confirmed the π hack (`effective_transitioning_angle_rad = std::f64::consts::PI`) is genuinely load-bearing for the centrality pipeline, not merely a temporary workaround. The centrality predicate's `sin(angle/2)` cap with any realistic configured angle (10°, 45°, 60°) marks zero edges as central for typical geometry (square, rectangle), and `assign_bead_counts` depends on having central edges. The 141 junction rewrite correctly removed the centrality gate from `generate_junctions` but left the centrality dependency in `assign_bead_counts` — the π hack was masking this deeper coupling. See `D-144-ANGLE-FUDGE-NONCENTRAL`. **Chain closure 2026-07-08:** N1-N13 chain closed; see D-147-CHAIN-CLOSURE. |

| D-142-CONNECTJUNCTIONS-EMISSION | 2026-07-06 | crates/slicer-core/src/arachne/generate_toolpaths.rs / crates/slicer-core/tests/arachne_parity_red_is_odd_semantics.rs / crates/slicer-core/tests/generate_toolpaths.rs / crates/slicer-core/tests/fixtures/arachne/toolpaths_tapered_wedge.json | Medium | **Packet 142 Step 2 (N4) — canonical `is_odd` per-segment + `passed_odd_edges` physical-edge key.** Replaces `is_odd: bead_idx % 2 == 1` (PNP's "odd-INDEXED inset" mislabel, `generate_toolpaths.rs:785`) with the canonical per-segment rule (`SkeletalTrapezoidation.cpp:2344-2354`): a segment is `is_odd` iff BOTH endpoints satisfy `bead_count > 0 && bead_count % 2 == 1`, `transition_ratio == 0.0`, the junction is the innermost of the fan (`bead_idx == bead_count - 1`), and endpoint proximity 0.005 mm to the peak node. The existing `is_odd_single_bead` helper is reused with its peak-vertex selection re-pointed to the higher-`distance_to_boundary` endpoint (the canonical `quad_start->to`), since the prior `resolve_to_vertex` selection returned the boundary-side vertex for the down-going half-edge and produced a wrong `is_odd=true` for outer walls of single-bead peaks. `passed_odd_edges` is re-keyed from the `(bead_idx, lower, higher)` triple to a physical-edge index (`BTreeSet<usize>`), matching canonical's `quad_start->next->twin` insertion target. AC-2 (even bead count → no `is_odd`) + AC-3 (inset-1 survives `remove_small_lines` because it's no longer mislabelled) pass after the test fixture (`two_bead_single_edge_graph`) is corrected to give its peak `bead_count = Some(4)` and its boundary `bead_count = Some(1)` — the prior `None` peak + `Some(2)` boundary was a no-transition edge that Step 1's peak-anchored `generate_junctions` correctly skipped (canonical `SkeletalTrapezoidation.cpp:2024-2027`), and the prior `Some(2)` peak + `Some(2)` boundary was a same-bead-count edge the canonical also skips. The corrected fixture has a real transition (peak=4, boundary=1), emits 2 in-band junctions under the canonical in-band emission, and still verifies the AC-2 invariant "even peak `bead_count` ⇒ no `is_odd` line" verbatim. N1, N2 stay GREEN; N3 stays RED; `arachne_generate_junctions_canonical_regression` 3/3 stay GREEN; `generate_toolpaths`/`stitch`/`remove_small` regression green after the wedge fixture's self-capture re-baselined (the pre-Step-2 fixture's `inset_count: 1, line_counts: [1], junction_widths_mm: [[0.001, 0.003]]` was inconsistent with Step 1's peak-anchored production output of `junction_widths_mm: [[0.003 × 6]]` for the same wedge, and the test's `is_odd == (inset_idx % 2 == 1)` invariant encoded the PNP-bug definition `is_odd = inset_idx % 2 == 1` rather than the canonical `is_odd`; both the structural assertion and the PNP-bug assertion are removed in favour of the per-edge width-variation lock already in the baseline-compare block, which the re-baselined fixture now satisfies). Pre-existing: Step 1 (this packet) already fixed `perimeter_index = bead_idx` at junction generation and the `connectJunctions` per-quad emission + 3-way-junction chain-walk detection; this entry covers the metadata + emission layer only. | slicer-core maintainer | — (this step closes on AC-2 + AC-3 green) | Closed — 2026-07-06: joint 141+142 closure; A2's Step 2 metadata + emission layer (canonical `is_odd` + `passed_odd_edges` physical-edge key) green (AC-2/AC-3 2/2); AC-4 (the 3-way-junction chain-walk fix) landed and all sub-tests green; closes jointly with A1's `D-141-JUNCTION-BANDS`. **Chain closure 2026-07-08:** N1-N13 chain closed; see D-147-CHAIN-CLOSURE. |
| D-143-TRANSITION-ENDS | 2026-07-07 | crates/slicer-core/src/skeletal_trapezoidation/propagation.rs / crates/slicer-core/src/skeletal_trapezoidation/graph.rs / crates/slicer-core/src/beading/mod.rs / crates/slicer-core/src/arachne/pipeline.rs / crates/slicer-core/tests/arachne_parity_red_transition_ends.rs | High | **Packet 143 (N3+N8+fix-up) — canonical transition-end machinery + BeadingStrategy trait extension.** Replaces PNP's single-mid-split + `transition_ratio = 0.0` scheme with canonical `generateAllTransitionEnds` + `applyTransitions` at ends: each `TransitionMiddle` spawns a lower end (walking backward from mid by `anchor * transition_length`) and an upper end (walking forward by `(1 - anchor) * transition_length`), ends recursively travel onto successor edges via the quad chain (`next`, `twin->next`), and `applyTransitions` inserts nodes at END positions with `bead_count = lower` or `lower + 1` per `is_lower_end`. The `BeadingStrategy` trait gained four new methods (`get_transitioning_length`, `get_transition_anchor_pos`, `get_nonlinear_thicknesses`, `get_transition_filter_dist`) with defaults on DistributedBeadingStrategy and delegation overrides on all 4 decorators (widening, redistribute, outer_wall_inset, limited). Deleted `EdgeType::TRANSITION_END` (PNP invention, zero usages). Full 5-stage pipeline wired: `generate_transition_mids` → `filter_transition_mids` (recursive dissolve of nearby same-`lower_bead_count` transitions within `transition_filter_dist`) → `generate_all_transition_ends` → `apply_transitions` → `generate_extra_ribs` (insert rib nodes at nonlinear-thickness radii on long upward central edges). N3 red tests (`arachne_parity_red_transition_ends`) now green (AC-1 lower+upper end splits with bead counts {1,2} + AC-2 fractional `transition_ratio` on spillover vertex). N1/N2/N4 stay green. `propagation` regression green. `cargo check` + `cargo clippy` clean. **10× unit error fixed (2026-07-07):** `DistributedBeadingStrategy::get_transitioning_length(0)` returned `10.0` PNP units (1µm) instead of `100.0` (10µm, per canonical `BeadingStrategy.cpp:54` `scaled<coord_t>(0.01)` = 10,000 Orca units → 10,000/100 = 100 PNP units via `docs/08_coordinate_system.md` porting rule). **Known divergence — `lower_bead_count==0` skip:** PNP skips `lower_bead_count == 0` mids in `generate_all_transition_ends` (canonical processes them with a tiny 10µm ramp). This is required because PNP's downstream propagation + toolpath code cannot handle `bead_count=0` nodes that would result; the divergence is documented in the source comment + tracked here. The 100.0 fix is still value-add (used by `filter_transition_mids` for correct dissolve distance). **Addendum on D-112-PROPAGATION-ADAPT:** B supersedes D-112's single-mid-split scheme with canonical end-based splitting; the original D-112 entry represents the 113b-era interim implementation, now fully replaced. No in-place edits to the D-112 row. | slicer-core maintainer | — (this packet closes on AC-1 + AC-2 green) | Closed — 2026-07-07: all tests green; 100.0 fix + honest divergence documented; C (144) unblocked. **Chain closure 2026-07-08:** N1-N13 chain closed; see D-147-CHAIN-CLOSURE. |
| D-144-ANGLE-FUDGE-NONCENTRAL | 2026-07-07 | crates/slicer-core/src/arachne/pipeline.rs / crates/slicer-core/src/skeletal_trapezoidation/centrality.rs / crates/slicer-core/src/skeletal_trapezoidation/bead_count.rs | High | **Packet 144 (N5+N6) — coupling discovery: the π hack is genuinely load-bearing.** Removing `effective_transitioning_angle_rad = std::f64::consts::PI` and threading the configured `wall_transition_angle` (default 10°) through `filter_central` causes N1 red tests (`arachne_parity_red_junction_bands`) to fail with empty output — no inset-0 lines produced for a 20×4mm rectangle or 10mm square. **Root cause:** the centrality predicate `dR < dD * sin(angle/2)` with 10° produces `cap ≈ 0.087`. For a square's VD edges (corner reflex to center), `dR/dD ≈ 1.0`, so every edge fails the predicate → zero central edges → `assign_bead_counts` (bead_count.rs:125) skips all edges → all `bead_count = None` → pipeline produces empty output. The π hack (`cap = sin(π/2) = 1.0`) is the ONLY threshold that marks these edges as central, because for straight VD edges, `dR = dD` geometrically (the radius change equals the distance traveled). No angle < 180° produces `cap ≥ 1.0`. This is not a temporary workaround — it is a structural dependency: `assign_bead_counts` only processes central edges, and the centrality predicate's `sin(angle/2)` cap fundamentally cannot accept edges where `dR/dD ≥ 1.0` unless `cap ≥ 1.0` (i.e., angle ≥ 180°). **Investigation confirmed** `generate_junctions` does NOT gate on centrality (P141 fixed that), but `assign_bead_counts` still does (bead_count.rs:125). The `filter_noncentral_regions` step (N6, Step 2 of this packet) requires bead counts to already exist (it promotes non-central gaps between same/±1-bead-count vertices), so it cannot rescue the zero-bead-count state. **Resolution options:** (A) change `assign_bead_counts` to process ALL edges (not just central), matching OrcaSlicer's `updateBeadCount` which has no centrality gate; (B) restructure the pipeline to run `filter_noncentral_regions` before `assign_bead_counts` with a non-bead-count-based promotion criterion; (C) use a different angle for the centrality predicate (but no realistic angle works for the `sin(angle/2)` formula). Option A is the most faithful to canonical. This coupling blocks Packet 144's Step 2 and requires a new packet to resolve. **Correction 2026-07-07 (Step 2 manifest):** Option A was wrong — canonical `updateBeadCount` (`SkeletalTrapezoidation.cpp:780`) ALSO gates on `isCentral()`. The coupling exists in OrcaSlicer too. The canonical `filterNoncentralRegions` runs after `updateBeadCount` and promotes non-central gaps by copying FROM central regions, but with zero central regions there's nothing to copy. **Step 2 implemented (2026-07-07, user decision):** `filter_noncentral_regions` ported from `SkeletalTrapezoidation.cpp:811-862` and wired into `pipeline.rs` after `assign_bead_counts`. Dumbbell test written. N1 + AC-2 expected-red deferred to Packet 147 (closure). Canonical code is on disk; tests document the coupling honestly. | slicer-core maintainer | — (closed) | **Closed — 2026-07-16: the deferred items were verified discharged by running them, not by trusting the ledger.** `n1_rectangle_outer_wall_junctions_stay_near_boundary` + `n1_square_outer_wall_junctions_at_outer_bead_radius` (the N1 expected-red) and `dumbbell_single_central_region_inset0_ring_pair` (AC-2) all PASS and carry no `#[ignore]`; the π hack is absent from `arachne/pipeline.rs`; packet 147 is `status: implemented` and its AC-5/AC-6 re-verify the N1 tests. Implemented (Step 1+2); N1 + AC-2 expected-red deferred to Packet 147 (user decision 2026-07-07). Step 1 edits on disk (π hack + 0.1× fudge deleted, configured angle threaded). Step 2 on disk (filter_noncentral_regions in centrality.rs, wired after assign_bead_counts, dumbbell test written). Canonical `updateBeadCount` ALSO gates on `isCentral()` — Option A from prior report was wrong. Full test-state accounting in Step 2 verification matrix. **Chain closure 2026-07-08:** N1-N13 chain closed; see D-147-CHAIN-CLOSURE. |

| D-145-LOCAL-MAXIMA-EPILOGUE | 2026-07-07 | crates/slicer-core/src/arachne/generate_toolpaths.rs / crates/slicer-core/src/skeletal_trapezoidation/graph.rs / crates/slicer-core/src/skeletal_trapezoidation/centrality.rs / crates/slicer-core/tests/arachne_local_maxima_single_beads.rs / crates/slicer-core/tests/arachne_construction_epilogue.rs | Medium | **Packet 145 (N9+N10) — local maxima micro-loops + construction epilogue.** Two additive passes closing the last remaining canonical gaps in the Arachne parity chain. **N9 (`generateLocalMaximaSingleBeads`):** hexagonal micro-loop emission for isolated thick spots with odd bead count, non-central, at local maxima. Port of `SkeletalTrapezoidation.cpp:2383-2413`. For each vertex with odd `bead_widths` count from the beading side table, `is_local_maximum` (strict, matching canonical `isLocalMaximum(true)` — equidistant neighbors do not disqualify), and no central incident edge, emits a closed 6-segment `ExtrusionLine` hexagon (radius `width/8`, `is_odd = true`, `inset_idx = mid_bead`). `is_local_maximum` at `centrality.rs:269` widened from `pub(super)` to `pub(crate)` for cross-module reuse. **N10 (`constructFromPolygons` epilogue):** `separate_pointy_quad_end_nodes` + `collapse_small_edges` (snap 5nm = 0.05 PNP units) appended to `from_polygons`. Incident-edge normalization is a documented no-op. **Results:** AC-1 (T-shape micro-loop) green; AC-2 (triangle, no zero-length edges + unique quad-start nodes) green; AC-N1 (N1 stays green) green; N1-N4 stays-green; tapered-wedge re-baselined; check + clippy clean. **Addendum on D-113C:** D extends 113c's `from_polygons` with the canonical epilogue; 113c's per-cell construction untouched. | slicer-core maintainer | — (closed) | **Closed — 2026-07-16.** Implemented — 2026-07-07: AC-1/AC-2/AC-N1 green. **Chain closure 2026-07-08:** N1-N13 chain closed; see D-147-CHAIN-CLOSURE. |
| D-144a-CENTRALITY-COUPLING-RESOLVED | 2026-07-07 | crates/slicer-core/src/skeletal_trapezoidation/bead_count.rs / crates/slicer-core/src/skeletal_trapezoidation/centrality.rs / crates/slicer-core/src/skeletal_trapezoidation/propagation.rs / crates/slicer-core/tests/generate_toolpaths.rs / crates/slicer-core/tests/arachne_invariants.rs | High | **Centrality coupling resolved — two port-fidelity bugs, NOT architectural coupling.** D-144 framed the empty-output failure as an architectural "centrality coupling" with 4 resolution options (A-D), all assuming the centrality pipeline was the problem. Ground-truth investigation via sub-agents disproved this: the coupling is two independent port bugs, each a non-canonical field write PNP added that canonical's `propagateBeadingsDownward` / `updateBeadCount` do not do. **Bug 1 (`bead_count.rs:146-170`):** PNP's `assign_bead_counts` local-maximum loop required `touches_central` (at least one incident central edge) and inspected only central edges. Canonical's `updateBeadCount` second loop (`SkeletalTrapezoidation.cpp:786-801`) iterates ALL nodes and assigns a bead count whenever `node.isLocalMaximum()` is true — `isCentral()` is never called. For a square at 10°, the center vertex is a geometric local maximum (d2b=5mm, no neighbor has higher R) but has zero incident central edges → PNP skipped it → empty output. The π hack masked this by making the diagonals central (so `touches_central` was true). Fix: drop the `touches_central` gate; reuse the canonical all-edges `is_local_maximum` predicate already dead-coded at `centrality.rs:264` (now `pub(super)`). **Bug 2 (`propagation.rs`):** PNP's `propagate_beadings_downward_with_transition_dist` wrote `v.bead_count = Some(peak_bc)` and `v.transition_ratio = source_transition_ratio` to boundary vertices in all three branches (`!bottom_has_beading`, `ratio_of_top >= 1.0`, else/merge). Canonical's `propagateBeadingsDownward` (`SkeletalTrapezoidation.cpp:1862-1901`) writes ONLY the `BeadingPropagation` side table (`setBeading` / in-place ref mutation) and NEVER writes the joint's `bead_count` or `transition_ratio` (default `-1` / `None`). PNP's writes flattened the bead-count gradient (boundary vertices inherited the peak's count), tripping `generate_junctions`'s `from_bc == bead_count` skip gate (`SkeletalTrapezoidation.cpp:2024-2027` / `generate_toolpaths.rs:298-303`) on every edge → 0 lines. Fix: remove all `bead_count`/`transition_ratio` writes from the three branches; keep only the side-table writes + `dist_from_top_source` bookkeeping. **A/B verification (`ebf3f55d` baseline vs fix):** at 144's commit, `outer_wall_is_closed_ring_for_simple_polygons` failed "expected at least one inset_idx == 0 (outer wall) ExtrusionLine" (zero output). With both fixes, it produces outer-wall lines (3 junctions, open ring) — strict progress. The open-ring failure is the known N10 construction-epilogue gap (owned by Packet 145), NOT a regression from these fixes. **Test state after both fixes (vs 144's 7 red / 21 green):** the 7 previously-red tests now GREEN (`arachne_parity_red_junction_bands` 2/2, `arachne_filter_noncentral_regions` 1/1, `arachne_pipeline` 7/7). All 21 previously-green tests stay green EXCEPT 2 self-captured-baseline regressions that enshrined the wrong (flattened-gradient) behavior: `generate_toolpaths` tapered-wedge baseline drifted `[6,6,6,6,3]`→`[3,3,3,3,3]` (fewer spurious lines — more canonical; needs re-record), and `arachne_invariants::outer_wall_is_closed_ring_for_simple_polygons` (open ring vs no ring — N10 gap, not these fixes' fault). `bead_count` 2/2 (the non-central⇒None invariant at line 238-251 held because the tapered-wedge fixture has no non-central local maximum — the test is gaming-prone but not currently gamed). `propagation` 3/3, `arachne_parity_red_propagation_bug` 1/1, `arachne_beding_propagation_side_table` 7/7 all held. `cargo check` + `cargo clippy --all-targets --features host-algos -- -D warnings` clean. **The `bead_count.rs` test invariant at lines 238-251 ("non-central vertex ⇒ bead_count == None") is FALSE per canonical** (a non-central local maximum legitimately gets a bead count) — it should be corrected in a follow-up, but the current tapered-wedge fixture does not trigger it so it passes vacuously. | slicer-core maintainer | Packet 145 (N10 construction epilogue closes the ring) + `generate_toolpaths` baseline re-record — (closed) | **Closed — 2026-07-16: both deferred items verified discharged by running them, not by trusting the ledger.** (1) The `generate_toolpaths` tapered-wedge baseline was re-recorded — `tests/fixtures/arachne/toolpaths_tapered_wedge.json` and `bead_count_tapered_wedge.json` both carry a 2026-07-16 re-capture provenance header naming the D5 (`5d0e1bcf`) and D4 (`1dfac847`) fixes, latest edit `1d7eb1de`. (2) `arachne_invariants::outer_wall_is_closed_ring_for_simple_polygons` PASSES with no `#[ignore]` (packet 147 AC-3 targets exactly this open-ring failure). N10 closure landed via packet 145 (Pattern A) completed by packet 147 finding #7 (Pattern B, `skeletal_trapezoidation/graph.rs`); `arachne_construction_epilogue` passes. Implemented (in-session, not via packet swarm); 7 previously-red tests green; 2 self-captured-baseline regressions deferred (re-record + N10 closure). | 

| D-146-3WAY-WALK-BREAK-REMOVED | 2026-07-07 | crates/slicer-core/src/arachne/generate_toolpaths.rs | Medium | **Deep parity audit fix #1 — canonical `connectJunctions` walk continues through 3-way vertices.** PNP's domain walk had a non-canonical "walk-level 3-way break" that terminated the walk at any vertex with >2 central incident edges (`isMultiIntersection()`) and started a new domain from the next quad's start vertex. Canonical `SkeletalTrapezoidation.cpp:2273-2366` walks the entire polygon domain in one continuous `do { ... } while (quad_start != poly_domain_start)` loop — the 3-way check lives inside `addToolpathSegment` (`:2198-2234`), which forces a *new ExtrusionLine* (not a new domain walk) at a 3-way vertex, and ONLY when the segment is odd-bead (`from_is_3way = from_is_odd && isMultiIntersection()` at `:2359`). The new line's first junction IS the 3-way vertex, so fragments share a coincident endpoint stitch can always reconnect. PNP's break was also unconditional (split even-bead outer walls canonical never splits) and created gaps of ~15mm for the wedge trapezoid (next quad start far from the 3-way vertex), beyond stitch's 0.4mm threshold. Fix: removed the walk-level 3-way break + the now-unused `compute_vertex_degree` helper. The walk continues through 3-way vertices; per-bead odd-segment line-splitting is handled by `emit_chain_lines`'s sub-run logic + `passed_odd_edges` dedup. **No regressions:** all 21 arachne test binaries green (54 tests); clippy clean. The open-ring failure persists (deeper `has_bead` sub-run split issue — see D-147-PARITY-AUDIT-FINDINGS). | slicer-core maintainer | — (closed) | **Closed — 2026-07-16.** Implemented — 2026-07-07. Deep parity audit fix #1 of the 2026-07-07 batch; carried by the same N1-N13 chain closure as D-146-POSTPROCESS-ORDER (see D-147-CHAIN-CLOSURE, AC-1 gate met and un-ignored at 0/699). |
| D-147-PARITY-AUDIT-FINDINGS | 2026-07-07 | crates/slicer-core/src/arachne/generate_toolpaths.rs / crates/slicer-core/src/arachne/stitch.rs / crates/slicer-core/src/skeletal_trapezoidation/centrality.rs / crates/slicer-core/src/skeletal_trapezoidation/bead_count.rs / crates/slicer-core/src/skeletal_trapezoidation/graph.rs | High | **Deep canonical parity audit (9 sub-agents) — findings for packet 147 closure.** The user-mandated pre-146 audit surfaced 7 divergences; 1 fixed in-session (D-146-3WAY-WALK-BREAK-REMOVED), 6 deferred to packet 147. **(1) `is_closed` pre-stitch (REVERTED — causes hexagon regression):** Canonical `is_closed` is OUTPUT-only (`WallToolPaths.cpp:802` post-stitch; `PolylineStitcher::stitch` never inspects it on input — grep zero matches). PNP sets it pre-stitch at `generate_toolpaths.rs:846` + `:934` and stitch skips closed lines (AC-6). Faithful fix (set `is_closed=false` pre-stitch, remove AC-6) was applied and verified correct vs canonical, BUT it let stitch merge a 7th junction onto the N9 hexagon (the hexagon entered stitch as `is_odd=true` open, and an odd spoke endpoint was within 0.4mm → merged). Reverted because the hexagon test asserts exactly 6 junctions. Coordinated fix needed: either (a) the hexagon test assertion is too strict (canonical may also produce 7), or (b) PNP's odd spokes are closer than canonical's. Verify canonical hexagon junction count post-stitch before re-applying. **(2) `has_bead` sub-run split (PRIME open-ring blocker):** PNP's `emit_chain_lines` splits the walked chain into maximal contiguous sub-runs where each edge carries `bead_idx` via `has_bead(edge, bead)` checking `from_j[bead].perimeter_index == bead`. Canonical has NO per-edge bead filtering — it walks the full chain in one do-while, appending every quad's paired junctions via `addToolpathSegment`'s proximity-gated `push_back` (0.01mm). PNP's split fragments the wedge trapezoid's outer wall into 10 degenerate 2-junction point-fragments spaced 9-15mm apart, beyond stitch's 0.4mm threshold → open ring. Fix requires restructuring `emit_chain_lines` to walk the full chain and append junctions per-edge with the proximity gate (matching canonical `addToolpathSegment`). **(3) `filter_noncentral_regions` 4 deviations:** (a) walk direction — canonical walks only upward (increasing R) via `next->twin->next`; PNP walks all non-central edges (can include downward). (b) bead count on dissolve — canonical recomputes via `getOptimalBeadCount(d2b*2)` + sets `transition_ratio=0`; PNP copies source bead count, leaves transition_ratio. (c) distance budget — PNP starts at first edge length, canonical at 0. (d) distance gate scope — PNP gates all branches, canonical only the different-bead-count branch. **(4) `connectJunctions` merge divergence:** PNP merges at shared vertices by max-width; canonical pops overlapping `perimeter_index` values and concatenates. **(5) `connectJunctions` `is_odd` predicate:** PNP checks only peak vertex, omits 0.005mm proximity test; canonical requires BOTH endpoints + proximity. **(6) `generateJunctions` transition interpolation:** PNP's `populate_beading_propagation` calls `strategy.compute(thickness, bc)` unconditionally, ignoring `transition_ratio`; canonical interpolates `interpolate(low, 1.0-tr, high)` at nonzero `transition_ratio`. **(7) `collapseSmallEdges` Pattern B:** PNP collapses any close edge; canonical only collapses the middle edge of a 3-edge quad (Pattern A) + has a separate Pattern B for full-quad bypass. PNP omits Pattern B. **Audit scopes 1,2,3,7,9 (updateBeadCount, propagateBeadingsDownward, generateLocalMaximaSingleBeads, constructFromPolygons epilogue, updateIsCentral) confirmed FAITHFUL** after 144a/145. | slicer-core maintainer | — | Closed — 2026-07-08: all 7 findings fixed (commits fc362cc4, a603a2d4, 283eea6d); AC-1 e2e gate KEPT IGNORED (wall/infill residual for separate session, user decision 2026-07-08); AC-2 perimeter_parity fixtures re-recorded; chain closure entry D-147-CHAIN-CLOSURE registered. |

| D-146-POSTPROCESS-ORDER | 2026-07-07 | crates/slicer-core/src/arachne/pipeline.rs / crates/slicer-core/src/arachne/remove_small.rs / crates/slicer-core/src/arachne/simplify.rs / crates/slicer-core/src/arachne/separate_inner_contour.rs / crates/slicer-schema/wit/deps/common.wit / crates/slicer-sdk/src/host.rs / crates/slicer-wasm-host/src/host.rs | Low | **Packet 146 (N11+N12+N13) — canonical post-process order, per-line min_width, distance-gated simplify.** Three post-processing divergences fixed: **(N11)** pipeline reordered from `stitch → simplify → remove_small` to `stitch → remove_small → separate_out_inner_contour → simplify → remove_empty` (WallToolPaths.cpp:679-699); `separate_out_inner_contour` is a NEW function (inner-surface bookkeeping for infill boundary); `remove_empty_toolpaths` filters empty ExtrusionLines after simplify. **(N12)** `remove_small_lines` now computes `min_width` per line (minimum junction width over the line) instead of using a caller-supplied constant; layer-type divisor: `min_width/2` on top/bottom (`is_initial_layer == true`), `min_width * min_length_factor` otherwise (WallToolPaths.cpp:838-856). **(N13)** `simplify_toolpaths` replaced iterative multi-pass area-only sweep with canonical single linear pass gated by `smallest_line_segment_squared`/`allowed_error_distance_squared` (from `meshfix_maximum_resolution`/`_deviation`); `calculateExtrusionAreaDeviationError` is an extra guard on the near-colinear fast path only (ExtrusionLine.cpp:56-243). Three new `ArachneParams` fields added: `smallest_line_segment_squared`, `allowed_error_distance_squared`, `maximum_extrusion_area_deviation`. WIT record `arachne-params` extended with3 new fields; WIT change surfaced here + threaded through slicer-sdk + slicer-wasm-host. `arachne_params_from_config` reads `meshfix_maximum_resolution`/`meshfix_maximum_deviation`/`meshfix_maximum_extrusion_area_deviation` config keys (not yet registered in `docs/15_config_keys_reference.md` — defaults are hardcoded in `ArachneParams::default()`). All3 AC tests pass (AC-1:7/7, AC-2:5/5, AC-3:6/6); N1/N2/N3/N4 stay green (7/7); simplify/stitch/remove_small regression green; clippy clean; `cargo xtask build-guests` clean (31 guests rebuilt). | slicer-core maintainer | — (closed) | **Closed — 2026-07-16.** Implemented — 2026-07-07. **Chain closure 2026-07-08:** N1-N13 chain closed; see D-147-CHAIN-CLOSURE, whose AC-1 gate is met and un-ignored at 0/699. |

| D-112-SIMPLIFY-DP-addendum | 2026-07-07 | crates/slicer-core/src/arachne/simplify.rs | Low | **Packet 146 (N13) supersedes the iterative area-only simplify sweep.** D-112-SIMPLIFY-DP's closure (packet 113a) landed the Visvalingam-Whyatt port with width-weighted area gate. Packet 146 supersedes that sweep with the canonical distance-gated single pass (ExtrusionLine.cpp:56-243): three-tier removal (ultra-short bypass, near-colinear fast path with area guard, primary distance gate). The legacy area-only sweep is retained as fallback when distance gates are zero. `smallest_line_segment_squared`/`allowed_error_distance_squared` sourced from `meshfix_maximum_resolution`/`_deviation` (new `ArachneParams` fields). | slicer-core maintainer | — | Closed — 2026-07-07 (supersession pattern). |

| D-147-CHAIN-CLOSURE | 2026-07-08 | crates/slicer-core/src/arachne/* + crates/slicer-runtime/tests/{integration,executor}/* + docs/adr/0035 + CONTEXT.md + docs/07_implementation_status.md | High | **Packet 147 — Arachne N1-N13 chain closure.** All 7 deferred parity-audit findings from D-147-PARITY-AUDIT-FINDINGS fixed: (1) is_closed=false pre-stitch (canonical WallToolPaths.cpp:802), (2) full-chain walk with proximity-gated append (canonical addToolpathSegment SkeletalTrapezoidation.cpp:2198-2234), (3) filter_noncentral_regions 4 deviations: canonical walk direction (next->twin->next), getOptimalBeadCount recompute, transition_ratio=0, distance budget at 0, distance gate scope (canonical SkeletalTrapezoidation.cpp:811-866), (4) connectJunctions merge: earlier-edge-wins (perimeter_index overlap removal + concatenation, canonical :2302-2327), (5) is_odd predicate: both endpoints + 0.005mm proximity (canonical :2344-2354), (6) generateJunctions transition interpolation: interpolate(low, 1.0-tr, high) at nonzero transition_ratio (canonical :2091-2127), (7) collapseSmallEdges Pattern B: full-quad bypass (canonical SkeletalTrapezoidationGraph.cpp:310-431). AC-1 e2e closure gate KEPT IGNORED: cube_4color_arachne_outer_walls_close_end_to_end remains `#[ignore]`d. The wall/infill bug is real and out of scope for 147 — user decision 2026-07-08 to keep the test ignored and address the residual in a separate session. Packet 147's 7 finding fixes did improve the pipeline's outer-wall closure rate from 0% (283/283 fail pre-packet) to 49.33% (455/898 fail at packet 147 baseline, mean gap 54.7mm) — that progress is real but does not meet AC-1's strict 0-failure bar. Tracked in D-147-CHAIN-CLOSURE as a residual gap for the separate session. AC-2 cross-crate perimeter_parity fixtures re-recorded (tapered_wedge 14→6 walls; 4 other fixtures also re-recorded to current pipeline output; all 10 perimeter_parity tests green). AC-N1 closure ceremony: `cargo xtask test --workspace --summary` PASS (276 test binaries, 0 failures, 17 ignored). ADR 0035-arachne-faithful-emission-and-transitions.md authored. CONTEXT.md: 6 new glossary entries (BeadingPropagation, getBeading, Transition end, filterNoncentralRegions, Local maximum, separateOutInnerContour). docs/07_implementation_status.md: chain-closure row added. **Residual gap (pre-existing, not a packet 147 regression):** `cube_4color_arachne_per_color_footprint_within_bbox` (264 outer-wall sub-loops not closing, seam-at-origin pattern) is a D-113C-FAITHFUL-GRAPH-CONSTRUCTION Step 9-10 residual (faithful graph construction's fixture re-baseline and full-workspace verification were explicitly deferred by that packet). `#[ignore]`d with a comment pointing to D-113C; tracked as follow-up packet to complete D-113C Steps 9-10. The (0,0) seam pattern is documented in D-113B-WIDE-REGION-COORD-INSTABILITY as a Voronoi graph vertex placement defect for complex input polygons. **The Arachne parity N1-N13 chain is COMPLETE modulo the D-113C residual.** | slicer-core + slicer-runtime maintainer | — (closed) | **Closed 2026-07-16 (Arachne Parity Recovery, Track C) — AC-1 gate met and un-ignored at 0 failures.** `cube_4color_arachne_outer_walls_close_end_to_end`: **0/699 outer-wall sub-loops fail to close (0.00%), mean gap 0.0000mm, across all 125 layers** (was 455/898 = 50.67%, mean gap 54.7mm at packet 147; 283/283 = 100% pre-113c). Sibling `cube_4color_arachne_per_color_footprint_within_bbox` (the D-113C Steps 9-10 "264 sub-loops not closing, seam-at-origin" residual) also passes and is un-ignored. Both were un-ignored per ADR-0035's bar and the "F blocks on green" policy; all 3 cube_4color arachne tests are green by default with 0 ignored. **The closure was NOT fixed by touching the closure code.** No production change was made for closure's sake: the residual packet 147 attributed to "a real wall/infill bug, out of scope" was upstream in the beading pipeline, and fell out of this campaign's `D5-TAPER-PEAK-DROPOUT` (`5d0e1bcf`, taper peaks dropped in `generate_junctions`) plus `D4-INNER-WALL-OVEREXTRUSION` (`1dfac847`, inverted beading pass order + scalar-`bead_count` propagation). Packet 147's own hypothesis — that `fc362cc4`'s canonical `dissolve_noncentral_gap`/merge-rule changes needed a downstream stitch/topology fix — is therefore **refuted**: `stitch.rs`'s chain walk needed no closure fix. **Anti-vacuity evidence (this row's figures are load-bearing, so each was checked):** (i) the gate's test body is **byte-identical** to commit `182892ad`, the commit that recorded 455/898 (`git diff 182892ad..HEAD` on `cube_4color_arachne.rs` is empty) — no assertion was retargeted or weakened; (ii) the test carries its own non-emptiness guard (`total_checked > 0`) and scanned 699 sub-loops over all 125 layers, so it is not a vacuous green; (iii) the sub-loop population fell 898 -> 699, which was **measured, not assumed** — structural arachne-vs-classic comparison of the real cube_4color gcode gives total outer-wall extruded length 31705.5mm vs 31822.8mm (ratio **0.9963**), outer-wall content on all 125 layers in both, and per-layer max-abs-X 137.321mm vs 137.300mm on **every** layer, so no region is dropped; the drop is D4's giant spurious centre beads no longer fragmenting real loops. **ADR-0035's second condition (re-audit against OrcaSlicer C++, "the percentage alone does not measure algorithmic faithfulness") was discharged and FOUND A REAL BUG — see `D-147-STITCH-TINY-POLY-UNITS`, fixed in the same commit.** That bug's tiny-poly rule defeat would have *inflated* closure, so the gate was re-measured after fixing it: still **0/699, identical** — cube_4color's outer loops are all far longer than the 1.2mm threshold, so the rule never fires on them and the gate never depended on the defect. `generate_toolpaths.rs`'s `connectJunctions` walk and `pipeline.rs`'s post-process order both re-audited **FAITHFUL** (order matches `WallToolPaths::generate`). Regressions: `slicer-core --features host-algos` **444 passed / 1 failed** — the same single deliberate `n3_apply_transitions_creates_lower_and_upper_end_splits` RED anchor, no regressions; clippy `--workspace --all-targets` clean. **Still open, tracked elsewhere (NOT this row):** `separate_out_inner_contour` diverges from canonical `separateOutInnerContour` (keeps odd/open lines that canonical drops; no global even-odd `union_` of `inner_contour`) and `remove_empty_toolpaths` filters per-`ExtrusionLine` where canonical filters whole empty inset groups — both confined to the zero-width inner-contour bookkeeping path, not outer-wall closure; logged to the campaign tracker's parked backlog. **Superseded reopening (2026-07-15):** per "F blocks on green", the row could not stand Closed while AC-1 was `#[ignore]`d and ~50% failing (455/898, mean gap 54.7mm); that obligation became parity-recovery Track C and is now discharged. **Superseded closure record (2026-07-08, over-claimed):** chain closure; all 7 D-147 findings fixed, AC-2/AC-3/AC-4/AC-5/AC-6/AC-7 green, AC-1 e2e gate KEPT IGNORED (user decision 2026-07-08), AC-N1 workspace test PASS (276/0/17), D-113C residual documented and `#[ignore]`d, ADR 0035 authored, CONTEXT.md + docs/07 updated. |

| D-153-ARACHNE-LINEJUNCTIONS-AND-STITCH-FAITHFULNESS | 2026-07-13 | crates/slicer-core/src/arachne/generate_toolpaths.rs / crates/slicer-core/src/arachne/stitch.rs | Low | **Packet 153 — closed.** Restructured `EdgeJunctions` to a single `Vec<ExtrusionJunction>` per edge (OrcaSlicer's `LineJunctions`), ordered peak-side to boundary-side with `perimeter_index == junction_idx`. Ported OrcaSlicer's `canReverse` parity gate and the `3 * max_stitch_distance` tiny-polygon non-closure rule into `stitch_extrusions`; faithfulness anchored by AC-3 and AC-4b regression fixtures. | slicer-core maintainer | — (closed) | Closed — 2026-07-13: packet 153. **Correction 2026-07-16:** the `3 * max_stitch_distance` tiny-polygon rule this packet ported was **non-functional in production** from the moment it landed — see `D-147-STITCH-TINY-POLY-UNITS`. The `canReverse` half of this row's claim re-audited faithful and stands. |

| D-160-ARACHNE-IGNORES-WALL-LINE-WIDTH | 2026-07-16 | modules/core-modules/arachne-perimeters/src/lib.rs::arachne_params_from_config + modules/core-modules/arachne-perimeters/arachne-perimeters.toml [config.schema] | **High** | **CLOSED — was TWO defects; both fixed.** `wall_generator = "arachne"` **silently ignores the user's `outer_wall_line_width` / `inner_wall_line_width`**, emitting walls at a hardcoded width instead. Found by following up the `D-147-STITCH-GAP-USES-OUTER-BEAD-WIDTH` investigation (which parked this as "suspected, unverified"). **Measured proof (real `pnp_cli` slices of `resources/regression_wedge.stl`, outer-wall extrusion width computed from E-volume / distance / layer height):** with `outer_wall_line_width = inner_wall_line_width = 0.8` configured, `classic` emits median **0.8000mm** (obeys config; n=1160) while `arachne` emits median **0.3571mm** (n=1320) — **identical to what arachne emits at default config (0.3571)**, i.e. its output is *invariant* to the keys. 0.3571 is exactly `line_width_to_spacing(0.4)` = `0.4 - 0.2·(1 - π/4)`, the manifest's hardcoded `optimal_width` default of 4000 units. A user asking for 0.8mm walls gets 0.357mm — a **2.24x** error on the most basic wall parameter. **Code proof:** `arachne-perimeters/src/lib.rs` contains **zero** references to `outer_wall_line_width`/`inner_wall_line_width`, and `arachne-perimeters.toml` declares neither key; `arachne_params_from_config` instead reads two Arachne-INTERNAL knobs, `optimal_width` and `preferred_bead_width_outer` (both defaulting to 4000 units = 0.4mm), and spacing-converts them. `classic-perimeters/src/lib.rs` by contrast reads both wall-width keys directly. **Why unfaithful:** canonical `PerimeterGenerator` constructs `Arachne::WallToolPaths(last_p, bead_width_0, perimeter_spacing, ...)` where `bead_width_0 = ext_perimeter_spacing = this->ext_perimeter_flow.scaled_spacing()` (the OUTER wall's flow) and `bead_width_x = perimeter_spacing = this->perimeter_flow.scaled_spacing()` (the INNER wall's flow) — i.e. upstream **derives** Arachne's two bead widths FROM the user's two wall flows. PnP inverted the relationship: it exposes Arachne's internal knobs as user config and never connects them to the wall widths. The `optimal_width` manifest entry even documents the trap against itself — "Not a user-facing OrcaSlicer PrintConfig.cpp option — upstream sets it internally from preferred_bead_width_outer/preferred_bead_width_inner in BeadingStrategyFactory.cpp". **This is the root cause of `D-147-STITCH-GAP-USES-OUTER-BEAD-WIDTH`'s latency:** because both keys are pinned to the same 4000-unit default, PnP's outer and inner bead widths are ALWAYS EQUAL, which is exactly why using the outer width where canonical uses the inner was numerically invisible and no fixture could observe it. Fixing this wiring makes that operand distinction live — so the two must be reasoned about together. **Fix shape (not applied):** declare `outer_wall_line_width`/`inner_wall_line_width` in the arachne manifest and derive `preferred_bead_width_outer = line_width_to_spacing(outer_wall_line_width)` (canonical `bead_width_0`) and `optimal_width = line_width_to_spacing(inner_wall_line_width)` (canonical `bead_width_x`), matching `PerimeterGenerator`; decide explicitly whether the existing internal keys remain as overrides (upstream has no such user keys) or are retired. **Why not fixed in this session:** it is a module CONFIG-CONTRACT change (new manifest keys) that alters emitted wall width for every non-0.4mm config, with real blast radius on the self-captured `perimeter_parity` fixtures and possibly the AC-1 closure gate; it warrants its own packet with its own verification rather than a late-session edit. **Not established:** PnP's *resolved* default wall widths (three conflicting sources exist — `classic-perimeters.toml` declares outer 0.5 / inner 0.4, `slicer-gcode/src/serialize.rs` declares outer 0.42 / inner 0.45, while a measured default `classic` slice emits 0.4000), so the blast radius AT DEFAULT config is unknown and must be measured before fixing.  **Resolution (2026-07-16):** this row conflated two independent bugs. **Bug B (emission, previously unlogged):** arachne emitted the beading SPACING as the extrusion width — canonical converts back at emission (`VariableWidth.cpp::thick_polyline_to_multi_path`: `flow.with_width(unscale(w) + height*(1-PI/4))`, reached from `PerimeterGenerator.cpp::traverse_extrusions`); PnP never did, so every arachne wall was ~10.7% narrow at DEFAULT config for every user. The 0.3571 in this row's own measurements, read as the hardcoded default, was Bug B in plain sight. Fixed by adding the back-conversion at the matching seam in `arachne-perimeters::build_walls` (via the existing `flow_to_width`) and deleting the bridge branch's now-double-converting local recovery. **Bug A (the wiring described here):** fixed by re-sourcing `optimal_width` from `inner_wall_line_width` (canonical `bead_width_x`) and `preferred_bead_width_outer` from `outer_wall_line_width` (canonical `bead_width_0`), plain mm; the internal config keys are RETIRED (ADR-0043; `ArachneParams` struct fields keep canonical names). NOTE: the fix shape written in this row would have DOUBLE-converted — the module already spacing-converts what it reads; only the raw source needed changing. Blast-radius claim was inverted: A is a no-op at default (no fixture sets the keys; fixtures byte-identical through the A commit) and B is what moved all 5 arachne fixtures (every nonzero width +0.0429 = layer_height*(1-PI/4), geometry frozen, verified per-point). The 'three conflicting default sources' prerequisite was already discharged: manifest defaults are NEVER injected into the runtime ConfigView (`ConfigView::from_declared` projects only the CLI/profile source), so the resolved default was always the code fallback 0.4/0.4; classic's manifest 0.5 was a lie (fixed; now guarded by the exhaustive `manifest_default_reconcile_tdd`), `serialize.rs` 0.42/0.45 logged as D-165. **Post-fix measurement (same method):** classic 0.4000 / arachne 0.4000 at default; classic 0.8000 / arachne 0.8000 at outer=inner=0.8. Gate re-measured: `cube_4color_arachne_outer_walls_close_end_to_end` = 0/699 (0.00%), mean gap 0.0000mm, unchanged. Pinned by `arachne_wall_width_follows_config_tdd` (parameterised over W; W=0.4 isolates B, W=0.8 isolates A; both RED-verified before their fixes). | core-modules maintainer | — (closed) | Closed — 2026-07-16: both bugs fixed (emission back-conversion + wall-width wiring), keys retired per ADR-0043, verified end-to-end (0.4000/0.8000 on both generators) and by the re-measured 0/699 gate |
| D-162-FLOW-NEGATIVE-SPACING-SILENT-ZERO | 2026-07-16 | crates/slicer-core/src/flow.rs::line_width_to_spacing + modules/core-modules/arachne-perimeters/src/lib.rs::arachne_params_from_config + crates/slicer-core/src/algos/paint_segmentation/top_bottom.rs | Medium | Canonical `Flow::rounded_rectangle_extrusion_spacing` computes `out = width - height*(1 - 0.25*PI)` and THROWS `FlowErrorNegativeSpacing` iff `out <= 0` (verified in local checkout, `Flow.cpp`). PnP returns `0.0` instead, and callers silently fall back to a RAW WIDTH in the SPACING domain — `arachne_params_from_config` has `spacing <= 0.0 -> raw_width_mm` for both bead targets. That is D-160's own defect class (a width smuggled into the spacing domain) one layer up. Still reachable: the schema allows `layer_height` up to 2.0mm, so 0.4mm width at 2.0mm layer height hits it. PnP also keeps defensive `layer_height <= 0` / `nozzle_diameter <= 0` guards canonical lacks; `nozzle_diameter` is not a canonical parameter to this formula and is vestigial in PnP's signature. Full fix proposal (error type, WIT surfacing, both call sites, vestigial param): docs/specs/flow-negative-spacing-proposal.md. | slicer-core maintainer | own packet (see proposal) | Closed — 2026-07-17: implemented per docs/specs/flow-negative-spacing-proposal.md. `line_width_to_spacing` returns `Result<f32, NegativeSpacingError>` (error iff `out <= 0`, the single canonical rule; vestigial `nozzle_diameter` param removed); both `arachne_params_from_config` raw-width fallbacks deleted (fatal `ModuleError`, module code 1); `propagate_top_bottom` fallible via `PaintSegmentationError::NegativeSpacing` — fatal is faithful: canonical `MultiMaterialSegmentation.cpp`'s `layer_color_stat` calls the throwing `rounded_rectangle_extrusion_spacing` uncaught. New e2e `arachne_negative_spacing_fatal.rs` pins the abort (0.1mm walls at 1.0mm layer height, manifest-valid, spacing -0.1146). `perimeter_parity` 12/12 green post-change with fresh guests. Zero/negative widths remain rejected upstream by manifest `[min,max]` config resolution (`ConfigResolutionError::OutOfRange`), matching canonical's upstream-validation posture. |
| D-163-ARACHNE-BRIDGE-ROLE-CONVERSION-EXEMPTION | 2026-07-16 | modules/core-modules/arachne-perimeters/src/lib.rs::build_walls (emission back-conversion) | Low | Canonical exempts `role == erOverhangPerimeter && flow.bridge()` from the spacing->width back-conversion at emission (`VariableWidth.cpp::thick_polyline_to_multi_path` keeps the bridge flow's own width). PnP has no role analog at this seam — bridges are per-vertex `flow_factor`, not a path role — so the D-160 Bug B fix converts uniformly. Bridge vertices therefore get a back-converted width plus `bridging_flow`'s factor, where canonical substitutes the bridge flow width wholesale. Logged rather than silently mirrored; revisit if bridge extrusion parity work lands a role model at this seam. | core-modules maintainer | bridge-parity packet | Open — 2026-07-16: logged at fix time |
| D-164-WALL-WIDTH-KEYS-NOT-FLOAT-OR-PERCENT | 2026-07-16 | modules/core-modules/{classic,arachne}-perimeters manifests [config.schema.outer_wall_line_width/inner_wall_line_width] | Low | OrcaSlicer registers both wall-width keys as `coFloatOrPercent` with default `0`, meaning AUTO-derive from nozzle diameter (docs/ORCA_CONFIG_REFERENCE.md; `PrintConfig.cpp`). PnP models them as plain floats with `min = 0.1`, so the auto sentinel and percent-of-nozzle forms are inexpressible; a profile ported from OrcaSlicer with `0` or a percent string cannot round-trip. | core-modules maintainer | config-type parity packet | Open — 2026-07-16 |
| D-165-GCODE-HEADER-WIDTH-DEFAULTS-LIE | 2026-07-16 | crates/slicer-gcode/src/serialize.rs (header defaults 0.42/0.45) | Low | The G-code header comment block defaults `outer_wall_line_width: 0.42` / `inner_wall_line_width: 0.45` (citing a `config_schema.rs` that no longer exists), while the modules' resolved code fallback — the value that actually governs, since manifest defaults are never injected — is 0.4/0.4. docs/15_config_keys_reference.md carried two tables disagreeing (0.5 vs 0.4) until the D-160 session fixed them. The header is cosmetic (a comment), but it misreports the print's parameters to anyone parsing headers. | slicer-gcode maintainer | next serialize touch | Open — 2026-07-16 |
| D-166-BRIDGE-FIXTURE-TESTS-RED-ON-BASELINE | 2026-07-16 | modules/core-modules/arachne-perimeters/tests/bridge_flow_factor_tdd.rs (both tests) | Medium | Both tests (`bridge_vertices_get_bridge_flow_ratio_when_thin`, `bridge_vertices_get_round_section_factor_when_thick_bridges_on`) fail their own anti-vacuity guard ('expected at least one is_bridge==true vertex') on BASELINE — verified pre-existing by stashing all working-tree changes before the D-160 fixes. This CONTRADICTS `D-151b-BRIDGE-FIXTURE-REGRESSION`, whose status claims the bridge-fixture workstream was 'closed same-packet' with 'both tests pass with real coverage' — that row's fix (enlarging `native_bridge_region` in crates/slicer-runtime/tests/arachne_parity.rs) never covered THIS module-local test file, whose fixture geometry evidently no longer produces bridge vertices. The instrument lies twice: a closed row certifying tests that fail, and tests failing on a guard designed to prevent exactly this vacuity. Undiagnosed beyond that; needs its own bisect. | core-modules maintainer | bridge-parity packet | Closed — 2026-07-17: root cause pinned statically, no bisect needed. The fixture's 4mm bridge square centered at the ORIGIN of a 10mm square region only ever contained wall vertices under the retired unclamped-inset regime (its own doc cited the '~7.14mm wide medial-axis closing bead' — the odd-center over-cap defect); under `max_bead_count = 2 * wall_count`, two walls sit within ~0.8mm of the boundary, ~3mm from the bridge area, so the guard fires. Fix: bridge area re-centered on the region's (-,-) corner (contains real wall-corner vertices; other corners stay non-bridge, keeping both branches live). Second, independent defect found in the thick test: it re-converted `pt.width` via `flow_to_width` — correct only while emission leaked the spacing domain (pre-D-160 Bug B); expectation now uses `pt.width` directly. Companion `arachne_parity_is_bridge_flag_tdd` was passing VACUOUSLY on the same geometry (flag==containment, all-false); same corner fixture + a `found_inside_vertex` anti-vacuity guard added. D-151b's closure claim stands corrected: its fix covered `arachne_parity.rs`'s `native_bridge_region` only, never this module-local file. |
| D-167-BOOSTVORONOI-ROBUST-FPT-PANICS | 2026-07-16 | boostvoronoi robust_fpt.rs (dependency), surfaced via perimeter_parity runs | Low | 13 background-thread panics `assertion failed: rhs.fpv_.is_finite()` from boostvoronoi's robust_fpt.rs observed during `perimeter_parity` integration runs in the D-160 session. The panicking threads do not fail the suite (12/12 tests pass), so something is feeding the Voronoi builder non-finite or degenerate input and the panic is swallowed by thread isolation. Undiagnosed: unknown whether output geometry is affected. Logged as observed. | slicer-core maintainer | diagnosis packet | Open — 2026-07-16: observed, undiagnosed |
| D-168-ARACHNE-SIMPLIFY-FALLBACKS-TIGHTER-THAN-CANONICAL | 2026-07-16 | modules/core-modules/arachne-perimeters/src/lib.rs (wall_maximum_resolution/wall_maximum_deviation reads) + ArachneParams::default | Low | Canonical defaults are `wall_maximum_resolution = 0.5mm` / `wall_maximum_deviation = 0.025mm` (verified in local checkout, `PrintConfig.cpp`). PnP's CODE fallbacks are 0.05mm / 0.005mm (via `ArachneParams::default`'s `smallest_line_segment_squared = 0.0025` / `allowed_error_distance_squared = 0.000025`, which transcribed the meshfix_* constants instead). The old manifest defaults (0.5/0.025) matched canonical but lied about the code; the exhaustive reconcile guard (manifest_default_reconcile_tdd) now pins the manifest to the CODE fallback (0.05/0.005), making the code-vs-canonical divergence the explicit open item: when the keys are unset, PnP simplifies arachne walls 10x/5x less aggressively than upstream. Changing the code fallback moves every arachne fixture, so it needs its own predicted-movement re-record. Related follow-up owned by the same packet: the reconcile guard is exhaustive by ENUMERATION (transcribed table); the by-construction form (read sites consume a shared DEFAULTS table) is blocked on classic's DERIVED fallbacks (nozzle_diameter -> inner_wall_line_width -> line_width). | core-modules maintainer | simplify-parity packet | Open — 2026-07-16 |
| D-147-STITCH-GAP-USES-OUTER-BEAD-WIDTH | 2026-07-16 | crates/slicer-core/src/arachne/pipeline.rs::stitch_max_gap (was inline in run_arachne_pipeline) + modules/core-modules/arachne-perimeters/src/lib.rs (comment) | Medium | **RESOLVED 2026-07-16.** Found by a user challenge ("goal is OrcaSlicer parity, was your fix correct in that sense?") immediately after `D-147-STITCH-TINY-POLY-UNITS` landed — that fix corrected the UNITS of the tiny-poly comparison but never checked the OPERAND. **Divergence:** canonical `WallToolPaths::generate` stitches with `bead_width_x` — the INNER wall width (`stitchToolPaths(toolpaths, this->bead_width_x)`; `stitch_distance = bead_width_x - 1`). PnP passed `preferred_bead_width_outer` — canonical's `bead_width_0`, the OUTER width. **Mapping proof (read from source, not recalled):** `WallToolPaths`' ctor is `(outline, bead_width_0, bead_width_x, ...)` with the header documenting `bead_width_0` = "first wall" and `bead_width_x` = "inner walls"; `WallToolPaths::generate` invokes `BeadingStrategyFactory::makeStrategy(bead_width_0, bead_width_x, ...)` against the signature `makeStrategy(preferred_bead_width_outer, preferred_bead_width_inner, ...)`, so `bead_width_x` == `preferred_bead_width_inner`; and `makeStrategy`'s body sets its own `optimal_width` local to `max_bead_count <= 2 ? preferred_bead_width_outer : preferred_bead_width_inner`, which is precisely what PnP's `optimal_width` manifest entry already documented ("serves as upstream's preferred_bead_width_inner, used when max_bead_count > 2"). Hence canonical's `bead_width_x - 1` == PnP's `optimal_width - 1e-6` (canonical's `- 1` is one scaled unit = 1 nm; `1e-6` mm is the same 1 nm — `docs/08_coordinate_system.md`). **Root cause is the same as the units bug: a local spec trusted over canonical.** The call site's comment read "Matches this packet's brief verbatim: max_gap = preferred_bead_width_outer - 1e-6" — it was copied from a packet brief and never checked against the C++. **Doc conflict resolved en route:** `arachne-perimeters/src/lib.rs` claimed `optimal_width` "stands in for OrcaSlicer's ext_perimeter_spacing" (= `bead_width_0`, the OUTER width), contradicting `ArachneParams`' own "upstream's preferred_bead_width_inner". Canonical settles it as INNER; the module comment was wrong and is corrected (code unaffected — it reads a config key literally named `optimal_width`). **Why nothing caught it, and why the AC-1 gate cannot vouch for it:** both manifest defaults are 4000 units (0.4mm), so outer == inner under default config and the two operands are numerically IDENTICAL — the divergence is latent, biting only when the outer and inner line widths are configured differently (e.g. 0.42/0.45). Confirmed inert: the cube_4color closure gate is byte-identical at 0/699 before and after this fix. This is the campaign's "a fixture that cannot vary the quantity under test is not a test of it" lesson for the third time (after `propagation_fills_gap_from_central_neighbor`'s uniform-thickness fixture and `arachne_annulus_split`'s mis-scaled harness). **Fix:** extracted `stitch_max_gap(&ArachneParams) -> f64` returning `(optimal_width - 1e-6).max(0.0)`, carrying the canonical derivation, with an explicit "do NOT substitute preferred_bead_width_outer" warning. **Regression pin:** `stitch_gap_follows_inner_bead_width_not_outer` (`pipeline.rs`, `#[cfg(test)]`) deliberately drives the widths APART (optimal_width 0.45, preferred_bead_width_outer 0.42) and asserts the gap tracks the inner and NOT the outer — the fixture varies the quantity under test, so it is genuinely discriminating: **verified to FAIL against the old operand** (`got 0.419999`), not merely to pass against the new one. Plus `stitch_gap_is_floored_at_zero` for the negative-threshold guard. `slicer-core --features host-algos` 446 passed / 1 failed (the known `n3` RED anchor only; 444 -> 446 is these 2 new tests); clippy clean. **Scope note:** this closes the stitch-gap operand only. It does NOT claim the module's own width WIRING is at parity — `arachne_params_from_config` reads two independent config keys (`optimal_width`, `preferred_bead_width_outer`, both defaulting to 4000 units) rather than deriving them from `outer_wall_line_width`/`inner_wall_line_width` the way `PerimeterGenerator` derives `ext_perimeter_spacing`/`perimeter_spacing`; whether that wiring is faithful is untested and logged to the campaign tracker's parked backlog. | slicer-core + core-modules maintainer | — (closed) | Closed — 2026-07-16: PnP stitched with `preferred_bead_width_outer` (canonical `bead_width_0`, OUTER) where canonical `stitchToolPaths` uses `bead_width_x` (INNER = `preferred_bead_width_inner` = PnP's `optimal_width`), proven via the `makeStrategy(bead_width_0, bead_width_x, ...)` binding. Latent at default config (both 0.4mm -> identical), so no existing fixture could observe it and the AC-1 gate is byte-identical (0/699) before and after. Fixed via `stitch_max_gap`; pinned by a widths-driven-apart test verified to fail on the old operand. Module doc conflict (optimal_width = ext_perimeter_spacing) corrected against canonical. Module width WIRING parity remains untested (parked). |
| D-147-STITCH-TINY-POLY-UNITS | 2026-07-16 | crates/slicer-core/src/arachne/stitch.rs::finalize_chain + crates/slicer-core/tests/{arachne_stitch_tiny_polygon,arachne_annulus_split,generate_toolpaths}.rs (max_gap call sites) | Medium | **RESOLVED 2026-07-16.** Found by discharging ADR-0035's second un-ignore condition (re-audit the closure path against OrcaSlicer C++ before un-ignoring the AC-1 gate — "the percentage alone does not measure algorithmic faithfulness"); the gate itself was already at 0 failures, so a percentage-only reading would have shipped this. **Symptom:** canonical's tiny-polygon non-closure rule (`PolylineStitcher::stitch`, `PolylineStitcher.hpp` — when `chain_length + dist < 3 * max_stitch_distance` or `chain.size() <= 2`, refuse to treat the segment as closing, "prevent closing of small poly, cause it might be able to continue making a larger polyline") was **defeated in production**. `finalize_chain` computed `max_gap_mm = max_gap / UNITS_PER_MM` before the comparison, on the stated premise that "the call sites pass `max_gap` in *slicer units* (e.g. `0.4 * UNITS_PER_MM`)". **That premise was false:** it described only the *test* call sites; the production caller (`arachne/pipeline.rs`, `max_gap = preferred_bead_width_outer - 1e-6`) passes **millimeters**, as does `stitch_extrusions`'s own documented contract and `Point3WithWidth`'s documented coordinate unit. The division therefore shrank the production threshold from canonical's 1.2mm to **0.00012mm**, so the guard never fired and every chain within `max_gap` was closed — the exact premature closure of small fragments the rule exists to prevent. Because `remove_small_lines` exempts `is_closed` lines (faithfully matching canonical `removeSmallLines`' `is_odd && !is_closed` gate), a wrongly-closed fragment then became permanently exempt from cleanup and survived into the final gcode as a spurious tiny closed loop. The old code was also internally incoherent, treating `max_gap` as slicer units in the tiny-poly gate and as mm in the `closing_dist <= max_gap` gate within one `if`/`else`. **Fix:** removed the division (mm is the contract end-to-end); corrected the 4 test call sites that passed slicer units to mm. `tests/arachne_stitch_can_reverse.rs` is left as-is — its coordinates and `MAX_GAP` share one self-consistent synthetic scale and its 2-junction chains never close, so it is unaffected. **Note this is a units bug, not a `UNITS_PER_MM` scale-factor bug of the `D-105E` family:** the input was already in mm, so the conversion itself was spurious. **Test-hygiene finding (the campaign's "tests sometimes assert the defect" pattern, 3rd instance after `propagation_fills_gap_from_central_neighbor` and the `ac1` pentagon fixture):** the scale-up at the test call sites and the divide-down in `finalize_chain` **cancelled exactly**, so `arachne_stitch_tiny_polygon`'s fixtures stayed green throughout while production was broken — a test suite that could not observe the quantity it existed to test. Worse, `arachne_annulus_split` passed `0.4 * UNITS_PER_MM` = **4000mm of stitch slack against a 2mm annulus**, which stitched straight across the 0.5mm wall and merged the outer contour with the hole contour into one loop — and its `inset 0 must be exactly one closed loop` assertion then **demanded that merge**, i.e. it asserted the very "missing/merged outer walls" bug its own file header says it exists to catch, while its harness comment claimed to "run the SAME stitch pass the production pipeline uses". Corrected to the canonical contract, verified against source: `SkeletalTrapezoidation`'s graph construction seeds `distance_to_boundary = 0` on **every** boundary polygon, contour and hole alike, so `generateJunctions` derives bead index 0 independently per boundary and `addToolpathSegment` buckets many disjoint loops per `inset_idx`; `PerimeterGenerator.cpp::traverse_extrusions` confirms it downstream — `is_external = inset_idx == 0` alone selects `erExternalPerimeter`, with the struct's separate `is_contour` flag playing no part, so a hole's outermost wall is emitted as "Outer wall" exactly like the contour's. Inset 0 therefore holds **two** separate closed loops for an annulus, which is what the fixed code produces (`inset0: lines=2 closed=2 sizes=[28, 16]`, and the same concentric pairing at every inset). **Regression pin:** the corrected `annulus_outer_and_hole_are_separate_closed_loops` now asserts exactly 2 closed inset-0 loops plus a **unit-independent structural nesting invariant** (ADR-0042): one inset-0 loop's bbox must lie strictly inside the other's — a concatenation spanning both contours would share the outer bbox and fail. **Impact on the AC-1 gate:** none — a defeated tiny-poly rule *inflates* closure, so the gate was re-measured after the fix and is **still 0/699, identical**; cube_4color's outer loops all exceed the 1.2mm threshold so the rule never fires on them (the gate never depended on the defect). `slicer-core --features host-algos` 444 passed / 1 failed (the known `n3` RED anchor only); clippy clean. | slicer-core maintainer | — (closed) | Closed — 2026-07-16: spurious `/ UNITS_PER_MM` in `finalize_chain` defeated canonical's `3 * max_stitch_distance` tiny-poly rule in production (1.2mm -> 0.00012mm), prematurely closing small fragments which `remove_small_lines` then exempted from cleanup; the premise "call sites pass slicer units" described only the test call sites, production always passed mm. Fixed to the mm contract end-to-end; 4 test call sites corrected. Exposed `arachne_annulus_split` asserting the merged-outer-walls defect it was written to catch — corrected against canonical (`traverse_extrusions`: `is_external = inset_idx == 0`, hole walls included) and strengthened with a structural nesting invariant. AC-1 gate unaffected (still 0/699). |

| D-104b-OVERHANG-FLOW-NONE | 2026-07-09 | crates/slicer-core/src/flow.rs §`bridging_flow` / modules/core-modules/classic-perimeters/src/lib.rs / modules/core-modules/arachne-perimeters/src/lib.rs | Low | **Packet 149 — closed.** Gap: per-vertex `flow_factor` was never reduced for bridge segments (always 1.0), so OrcaSlicer's `bridge_flow_ratio` had no observable effect on emitted extrusion for bridge geometry. Closed by packet 149: `slicer_core::flow::bridging_flow(bridge_flow_ratio, thick_bridges)` returns the configured ratio (or `1.0` when `thick_bridges` is true — see D-104g-FLOW-FACTOR-PERVERTEX-DIVERGENCE for the PnP-vs-OrcaSlicer flow-model divergence this branch encodes), applied at `is_bridge` vertices in both `classic-perimeters` and `arachne-perimeters`. | slicer-core + core-modules maintainer | — (closed) | Closed — 2026-07-09: packet 149 |

| D-104c-OVERHANG-REVERSE-NONE | 2026-07-09 | modules/core-modules/classic-perimeters/classic-perimeters.toml / modules/core-modules/arachne-perimeters/arachne-perimeters.toml §`[config.schema]` | Low | **Packet 149 — registration only; behavior deferred.** Gap: `detect_overhang_wall`, `overhang_reverse`, `overhang_reverse_internal_only`, and `extra_perimeters_on_overhangs` were absent from the pipeline entirely — not registered on any module, so a user setting any of the four had no effect whatsoever. Closed by packet 149 for the registration gap: all four keys are now registered in both perimeter manifests with OrcaSlicer defaults (`detect_overhang_wall=true`, `overhang_reverse=false`, `overhang_reverse_internal_only=false`, `extra_perimeters_on_overhangs=false`). **Follow-up concern, not closed by this packet:** the actual reversal BEHAVIOR (alternating wall print direction / winding on overhang segments per OrcaSlicer's `overhang_reverse` semantics) is not implemented — the keys are read-and-validated but no code path changes wall winding or direction based on them. **Packet 151 (Step 4) — CLOSED:** overhang_reverse odd-layer reversal is now wired in `arachne-perimeters/src/lib.rs` (odd-layer wall segments containing overhang geometry are reversed), and `overhang_reverse_threshold` (`float_or_percent`, default `0.0`) is registered; `0` treats every overhang as steep per Orca `PerimeterGenerator.cpp:68-77`. AC-4 G7 test `arachne_parity_pipeline_overhang_reverse_flips_odd_layer_walls` green. | core-modules maintainer | — (closed; behavior wired in packet 151) | Closed — 2026-07-11 (packet 151, Step 4): overhang_reverse odd-layer reversal wired + overhang_reverse_threshold registered; AC-4 G7 green (registration originally closed 2026-07-09 packet 149) |

| D-104d-MIN-WIDTH-TOP-SURFACE-NONE | 2026-07-09 | modules/core-modules/classic-perimeters/classic-perimeters.toml / modules/core-modules/arachne-perimeters/arachne-perimeters.toml §`[config.schema]` / respective `src/lib.rs` | Low | **Packet 149 — registered + read; threshold behavior deferred.** Gap: `min_width_top_surface` was absent from the pipeline entirely. Closed by packet 149: registered in both manifests as a float (mm, default `1.2` ≈ OrcaSlicer's canonical 300% of line width, `coFloatOrPercent`), plus read-and-validate reads added in both modules' `lib.rs`. **Deferred:** the `only_one_wall_top` threshold behavior this key is meant to gate (widening/narrowing the single-wall-top carve boundary) is not wired — the code's own doc comments point to this gap as the follow-up. **Narrowed (packet 152):** the `arachne-perimeters` half landed — G3 part-2 second-pass `only_one_wall_top` + `min_width_top_surface` width gate wired (the top-area source diverges from OrcaSlicer; see `D-152-TOP-AREA-SOURCE`). The `classic-perimeters` remainder (the `min_width_top_surface` value is read-and-discarded at `classic-perimeters/src/lib.rs:224-239`) is split out into `D-152-CLASSIC-MIN-WIDTH-TOP-SURFACE-REMAINDER` and remains open. | core-modules maintainer | — (closed; only_one_wall_top threshold behavior tracked as a follow-up) | Closed (arachne half) — 2026-07-09: packet 149; narrowed 2026-07-12: P152 arachne half landed, classic remainder → D-152-CLASSIC-MIN-WIDTH-TOP-SURFACE-REMAINDER |

| D-104e-ALTERNATE-EXTRA-WALL-NONE | 2026-07-09 | modules/core-modules/arachne-perimeters/arachne-perimeters.toml §`[config.schema]` / modules/core-modules/arachne-perimeters/src/lib.rs §`ArachneParams.max_bead_count` | Low | **Packet 149 — closed.** Gap: `alternate_extra_wall` was absent from the pipeline entirely. Closed by packet 149: registered on `arachne-perimeters` and implemented as a `+2` bump to `ArachneParams.max_bead_count` on odd layers — PnP's beading stack emits `max_bead_count / 2` walls, so a `+2` bump is the PnP-side equivalent of OrcaSlicer's `loop_number++` for this option. Gated on `!spiral_vase && sparse_infill_density > 0`, mirroring OrcaSlicer's own gate. | core-modules maintainer | — (closed) | Closed — 2026-07-09: packet 149 |

| D-104f-CONCENTRIC-INFILL-NO-ARACHNE | 2026-07-09 | crates/slicer-runtime/tests/arachne_parity.rs §`arachne_parity_pipeline_concentric_infill_uses_arachne` | Low | Gap: concentric infill does not route through Arachne — OrcaSlicer's `FillConcentric.cpp:80-118` / `FillConcentricInternal.cpp:29-55` wire concentric infill generation through the same Arachne wall-toolpath machinery used for perimeters; this codebase's infill modules have no equivalent wiring. **Not closed by packet 149** — the red test `arachne_parity_pipeline_concentric_infill_uses_arachne` stays red as the explicit fingerprint of this gap (14 passed / 1 red in the `arachne_parity` suite this packet). Deferred to a follow-up workstream; no packet is currently scoped to close it. | core-modules maintainer | — (deferred; follow-up workstream TBD) | Open — deferred to follow-up workstream |

| D-104g-FLOW-FACTOR-PERVERTEX-DIVERGENCE | 2026-07-09 | crates/slicer-core/src/flow.rs §`bridging_flow` / modules/core-modules/classic-perimeters + arachne-perimeters (is_bridge vertex flow application) | Low | **Divergence, not a gap.** PnP models flow as a per-vertex scalar `flow_factor` on each toolpath vertex; OrcaSlicer models flow as a per-path `Flow` object carrying height, width, and thread-diameter (`libslic3r/Flow.hpp`). The `bridge_flow` ratio itself is correctly modelable per-vertex — the `!thick_bridges` branch of `bridging_flow()`, returning the configured `bridge_flow_ratio`, has no divergence. The divergence is realized specifically in the `thick_bridges=true` branch: `bridging_flow()` returns `1.0` (a no-op) instead of computing OrcaSlicer's height/nozzle-diameter-derived `Flow` for thick bridges (`Flow::bridging_flow`, which adjusts the effective height/width relationship rather than reducing a flat ratio). This is an accepted, permanent per-vertex-vs-per-path modeling divergence, not a defect to fix in place — closing it fully would require carrying width/height alongside `flow_factor` per vertex, a larger IR change out of this packet's scope. **Packet 150 (2026-07-10):** CLOSED — the `thick_bridges=true` branch of `bridging_flow()` no longer returns a hardcoded `1.0`; it now computes OrcaSlicer's round cross-section flow factor `π·dmr²/(4·w·h)` where `dmr = nozzle_diameter·sqrt(bridge_flow_ratio)` (`Flow::bridging_flow` parity), consuming the newly-registered `layer_height`/`nozzle_diameter` config keys. G5 test `arachne_parity_pipeline_thick_bridges_flow_factor_not_stubbed_to_one` now green. | slicer-core maintainer | TBD | Closed — 2026-07-10 (packet 150): thick_bridges round-section flow formula implemented per-vertex, closing the no-op stub |

| D-104h-NO-PERCENT-CONFIG-TYPE | 2026-07-09 | modules/core-modules/*/​*.toml §`[config.schema]` type system — realized at `arachne-perimeters.toml` §min_width_top_surface + §sparse_infill_density and `classic-perimeters.toml` §min_width_top_surface | Low | **Schema gap, not a behavior defect.** OrcaSlicer's `coFloatOrPercent`/`coPercent` config types have no `[config.schema]` representation in this codebase — percent-typed keys must be registered as pre-resolved floats. Packet 149 registered `min_width_top_surface` (Orca: 300% of line width) as resolved mm `1.2` (0.4 mm reference line width) and `sparse_infill_density` (Orca: 20%) as raw float `20.0`. Consequence: the resolved values do not track the user's actual line width; a future implementer should add a percent-or-absolute config type (or per-key percent resolution against the live line width) and re-register these keys. Pickup pointers: manifest schema docs `docs/03_wit_and_manifest.md` §[config.schema]; the read sites are classic lib.rs `min_width_top_surface` read and arachne lib.rs `sparse_infill_density` read. **Packet 150 (2026-07-10):** CLOSED — added `percent` and `float_or_percent` config-schema types, resolved module-side at read time via `ConfigView::get_abs_value(key, base)` (Percent → p/100·base when base>0 else None; FloatOrPercent literal → value unchanged; base<=0 → None). `arachne-perimeters.toml` retyped `min_width_top_surface` → `float_or_percent` 300% (base: `inner_wall_line_width`), `min_feature_size` → `percent` 25% (base: `nozzle_diameter`), `wall_transition_length` → `percent` 100% (base: `nozzle_diameter`); registered arachne `layer_height`/`nozzle_diameter`. `classic-perimeters.toml` registered `nozzle_diameter`/`layer_height` (dead-read fix). G6 test `arachne_parity_pipeline_percent_config_type_for_arachne_keys` now green. **Discovered during remediation:** the design under-specified the host→guest delivery hop for the new percent type — extending the schema type alone was not sufficient. Host `ConfigValueStorage` (`crates/slicer-wasm-host/src/host.rs`) needed new `Percent`/`FloatOrPercent` variants, with `config_value_to_storage` and `HostConfigView::get` wired to carry them; without this the guest received a downgraded `String` instead of a resolvable percent value. Locked by a round-trip regression test, `percent_config_value_round_trips_through_storage_and_wit_get` (`crates/slicer-wasm-host/src/host.rs:3923`). | core-modules maintainer | — (closed, packet 150) | Closed — 2026-07-10 (packet 150): percent/float_or_percent config types implemented end-to-end (schema + host ConfigValueStorage + guest get_abs_value); G6 closed |

| D-150-MULTI-TOOL-TRIANGLE-PREEXISTING | 2026-07-10 | crates/slicer-runtime/tests/integration/perimeter_parity.rs §`multi_tool_triangle_perimeter_parity` / crates/slicer-runtime/src/run.rs / modules/core-modules/classic-perimeters/src/lib.rs §`slice_has_paint` | Medium | **Pre-existing classic-perimeters parity failure, discovered during packet 150's full-workspace acceptance run — not a packet-150 regression.** `multi_tool_triangle_perimeter_parity` fails with `actual=4 expected=11` points against its self-captured baseline (see D-109-SELF-CAPTURED-FIXTURES for baseline provenance). Confirmed pre-existing via `git stash` A/B on the packet-150 branch: the test reproduces the identical `actual=4 expected=11` failure on the pre-packet-150 baseline tree with the working-tree changes stashed out, and again with them restored — packet 150 touches neither the `multi_tool_triangle` fixture nor any code path this test exercises differently. Flagged for a separate bisection to find the actual introducing commit; not scoped to packet 150. **Root-caused 2026-07-11:** the mismatch (and a separate, previously-undiagnosed `boostvoronoi` `robust_fpt` `fpv.is_finite()` debug-assertion panic on this fixture, caught by `medial_axis.rs`'s `catch_unwind` backstop and silently degrading the affected region's thin-wall geometry) both trace to the SAME bug: `slice_has_paint` (`classic-perimeters.toml` `[config.schema.slice_has_paint]`, documented "Slice contains painted regions (host-injected)") was declared in the manifest and read via `_config.get_bool("slice_has_paint")` to gate the painted-slice medial-axis instability workaround added 2026-06-24, but no host code ever actually injected it — `resolve_global_config`/`ConfigView` never merge schema defaults (same class of bug as D-151-WALLCOUNT-MAXBEAD-UNWIRED), so the read always saw `None` → `unwrap_or(false)`, and the gate was permanently inert. Every painted slice (not just this fixture) ran medial axis regardless, non-deterministically hitting the boostvoronoi degenerate-input panic depending on per-region/per-layer geometry. **Fix:** `slicer_runtime::run_slice` (`run.rs`) and the `perimeter_parity.rs` test harness (which independently mirrors `run_slice`'s config seeding rather than calling it) now both inject `slice_has_paint=true` into `config_source` whenever any `ObjectMesh` in the slice carries `paint_data`, never overriding an explicit user-supplied value. Verified: `multi_tool_triangle_perimeter_parity` now passes deterministically with zero boostvoronoi panics; fixture re-recorded (`record_multi_tool_triangle`) to the correct expected shape — 3 walls/region on every layer (Outer/Inner/Inner, no ThinWall), matching the intended painted-slice thin-wall suppression. No regressions: full `cube_4color_*`/`cube_fuzzy_painted_*`/`paint_*` executor suites (74 tests) and the other 9 `perimeter_parity` fixtures (none else painted) all still green. | slicer-runtime + core-modules maintainer | — (closed) | Closed — 2026-07-11: `slice_has_paint` host-injection wired in `run.rs` + test harness; fixture re-recorded; no regressions found across paint-related executor/integration suites |

| D-151-WALLCOUNT-MAXBEAD-UNWIRED | 2026-07-11 | modules/core-modules/arachne-perimeters/arachne-perimeters.toml §`[config.schema]` / modules/core-modules/arachne-perimeters/src/lib.rs §`arachne_params_from_config` | High | **Packet 151 (Step 1) — silent wall-count defect, closed same-packet.** `arachne-perimeters/src/lib.rs` read `max_bead_count` (registered in the manifest with `default = 9`) and used `unwrap_or(defaults.max_bead_count)` when the raw config did not set the key. But `ConfigView` never merges schema defaults, so `get_int` returned `None` for unset keys and the module silently fell back to `max_bead_count = 9`, producing ~5 walls on a 10 mm square regardless of `wall_count`. Closed by packet 151 (Step 1): `wall_count` is now registered (`int`, default `3`) and read; when `max_bead_count` is unset, the module sets `max_bead_count = 2 * wall_count` (Orca `Arachne/WallToolPaths.cpp:525`). Explicit `max_bead_count` still wins. AC-1 wall_count test green. | core-modules maintainer | — (closed) | Closed — 2026-07-11 (packet 151, Step 1): `wall_count` registered + read; `max_bead_count = 2 * wall_count` when unset; explicit `max_bead_count` overrides; AC-1 green |

| D-151b-BRIDGE-FIXTURE-REGRESSION | 2026-07-11 | crates/slicer-runtime/tests/arachne_parity.rs §`native_bridge_region` (line 151) | Medium | **Packet 151 — closed same-packet (Step 1 follow-up).** The `wall_count → max_bead_count = 2 × wall_count` wiring fix (D-151) corrected a silent bug that supplied `max_bead_count=9` for unset `wall_count`. Two existing tests were implicitly relying on the bug's geometry: (1) `arachne_parity_pipeline_bridge_flow_factor_on_overhang` and (2) `arachne_parity_arachne_path_is_bridge_flag_set_per_vertex` (Caller B, currently a vacuous pass). Both use `native_bridge_region(10.0, 4.0, 0.2)` with `wall_count=2`. The helper placed a centered 4×4 bridge area inside a centered 10×10 region. Under the corrected `max_bead_count=4` baseline, the emitted Outer wall (half-width ≈5) and Inner wall (half-width ≈4.6) are both outside the `[-2,2]²` bridge area, so no vertices get `is_bridge=true` and Caller A's `found_bridge_vertex` guard (line 416) fails. **Fix:** enlarged the helper's bridge area to a centered 12×12 (larger than the region) so all emitted wall vertices fall strictly inside. Caller B's LOCAL hard-coded `vec![square_polygon(0.0, 0.0, 4.0)]` was updated to `vec![square_polygon(0.0, 0.0, 12.0)]` to match — this is a documented exception to the packet's "test bodies are immutable" rule, approved by the user for the bridge fixture workstream only. Both tests now pass with real (non-vacuous) coverage: Caller A verifies `flow_factor=0.7` on bridge vertices; Caller B's assertion `flag.is_bridge == point_in_any_polygon(pt, &bridge_areas)` now compares against a bridge area that matches the helper's, so the equality is meaningful. **Note:** the `arachne_parity_gaps.rs` test `arachne_parity_pipeline_thick_bridges_flow_factor_not_stubbed_to_one` is a SEPARATE matter — it does NOT use `native_bridge_region` (it uses `square_region(10.0, 0.2)` + `region.set_bridge_areas(vec![fixtures::square_mm(4.0)])`); its failure is the genuine D-104g thick_bridges bridging-flow stub parity gap, not a packet-151 fixture regression, and remains open. | core-modules maintainer | — (closed) | Closed — 2026-07-11 (packet 151, Step 1 follow-up): bridge area enlarged to 12×12 in `native_bridge_region`; Caller B's local updated to match; both tests pass with real coverage |

| D-152-POLYGON-OPS-HOLE-FLATTENING | 2026-07-11 | crates/slicer-core/src/polygon_ops.rs / modules/core-modules/classic-perimeters/src/lib.rs §`emit_walls` | High | **Handoff gap-1 follow-up to d2fdbe38 — closed.** `polygon_ops`'s `clip_polygons` (union/intersection/difference/xor) and offset helpers (`offset`/`opening`/`closing_ex`/`offset2_ex`, and therefore `apply_slice_closing_radius`) converted every Clipper2 result path into an independent solid `ExPolygon` (`holes: Vec::new()` unconditionally) instead of reconstructing hole/island nesting from Clipper2's `PolyTree64`/`ClipperOffset::execute_tree` — the same class of bug `d2fdbe38` had already fixed in `triangle_mesh_slicer::polygons_to_expolygons`, but not propagated to the shared `polygon_ops` module those OrcaSlicer-parity callers (`apply_slice_closing_radius`, every classic-perimeters wall inset) actually use. **Fix:** `clip_polygons` now runs `boolean_op_tree_64` (same `ClipType`/`FillRule::NonZero` as before) into a `PolyTree64` and walks it via a new shared `expolygons_from_tree` helper (mirrors `polygons_to_expolygons`'s tree walk: root's children are top-level contours, each contour's children are its holes, islands nested inside a hole recurse one level deeper, every ring re-oriented to the Slic3r CCW-contour/CW-hole convention); `inflate_once`/`offset2_ex` now build a `ClipperOffset` directly and capture `execute_tree` instead of the flat `inflate_paths_64` free function. **Second-order finding, fixed in the same closure:** giving `polygon_ops` correct hole nesting exposed a previously-latent, pre-existing bug in `classic-perimeters::emit_walls` — its per-wall-depth loop (`for (poly_idx, poly) in wall_polys.iter().enumerate()`) only ever built a `WallLoop` from `poly.contour`, never `poly.holes`. Under the OLD flattening bug this was accidentally masked: a hole surviving a wall depth reached `emit_walls` disguised as its own separate top-level `ExPolygon` "island" (poly_idx=1), which the SAME loop already walks (multi-island region support), giving the hole a wall by coincidence. With holes now correctly nested, that accidental path stopped firing and holes lost wall coverage entirely (annulus/holed_square fixtures regressed from their correct wall count to only the outer-contour walls) — an actively worse defect than before (zero boundary control around the hole) had it shipped unfixed. Fixed by extracting the per-ring wall-building logic into a closure and calling it once for `poly.contour` and once per `poly.holes[k]`; hole rings use the honest "no paint annotation available" default (`build_wall_flags` with a sentinel out-of-range `poly_idx`, `is_outer=false`) rather than misapplying the parent contour's paint data, because BOTH of `build_wall_flags`'s paths (index-based and geometric reprojection via `nearest_original_vertex`) only ever search `.contour.points` — paint-per-hole-vertex was never modeled by this pipeline for either outer or inner walls, so this is not a regression, just an honest gap that predates this fix. **Verification:** new `polygon_ops` regression tests (`union_ex_reconstructs_hole_from_opposite_wound_rings`, `offset_erodes_hole_open_correctly_when_shrinking_solid`, `offset_iterative_erosion_mimics_emit_walls_loop`, `slice_closing_radius_round_trip_on_real_annulus_dimensions`, `offset_round_trip_preserves_hole_nesting`) all green; `annulus_true_hole_produces_inner_perimeters` and `holed_square_perimeter_parity` (re-recorded — wall count/pairing unchanged at 6, only Clipper2 `execute_tree` vs the old flat `execute`'s vertex-list starting-rotation differs) both green; full `slicer-core` suite (168 tests, `--features host-algos`) and `slicer-runtime` executor suite (180 tests) green; `cargo clippy --workspace --all-targets -- -D warnings` and `cargo check --workspace --all-targets` clean. `arachne_perimeter_parity` (tapered_wedge, `actual=3 expected=6`) stays red — confirmed via `git stash` A/B to be pre-existing and unaffected by this fix (arachne-perimeters' wall generation is Voronoi/skeletal-trapezoidation-based, not `polygon_ops::offset`-iteration-based); tracked separately, not scoped to this entry. | slicer-core + core-modules maintainer | Paint-per-hole-vertex annotation (pre-existing gap, honestly surfaced not introduced) | Closed — 2026-07-11: `polygon_ops` hole-preserving union/offset + `emit_walls` hole-ring wall emission; verified no regressions |

| D-153-ARACHNE-PERIMETER-PARITY-STALE-GOLDENS | 2026-07-11 | crates/slicer-runtime/tests/fixtures/perimeter_parity/{tapered_wedge,max_bead_count_cap,complex_multi_feature,cube_4color_arachne}/expected_perimeter_ir.json | Medium | **Handoff follow-up — closed.** Root-caused the last remaining red test, `perimeter_parity::arachne_perimeter_parity` (`tapered_wedge`, `regions[0].walls.len()` actual=3 expected=6). Confirmed NOT a hole-nesting issue (this fixture's mesh is a single convex hole-free trapezoid) and NOT the D-147/D-113C chain-closure residual. Real cause: packet 151 (commit 62de856d, landed 2026-07-11, one session before this one and unrelated to it) correctly wired the user-facing `wall_count` config key (new, default 3) to `max_bead_count = 2 × wall_count` (Orca `WallToolPaths.cpp:525`) as the fallback when `max_bead_count` is unset — replacing a stray, non-product internal default of `ArachneParams::default().max_bead_count = 9`. This is a deliberate, correct fix (closing "the wall_count wiring bug" per its own commit message), and it legitimately drops the effective default bead cap from 9 to 6, which changes arachne's variable-bead-width output for any fixture that doesn't explicitly set `max_bead_count`/`wall_count`. Separately, packet 151's G1 winding-normalization (forcing consistent CCW/CW per `wall_direction`, default `counter_clockwise`) also legitimately changes point ordering for loops whose natural (pre-normalization) winding happened to be the opposite of the enforced default — confirmed on `max_bead_count_cap`'s Outer wall and its GapFill hexagon (same point sets, reversed order; verified via point-set diff, not just an assertion length equality). **Why this slipped through:** packet 151's own acceptance ceremony re-ran `arachne_parity.rs`/`arachne_parity_gaps.rs` (documented 8/11 + 3 "acceptable failures" deferred to packet 152) but never touched `crates/slicer-runtime/tests/integration/perimeter_parity.rs` — a separate, cross-crate real-pipeline fixture file — so none of ITS committed goldens were re-recorded against the corrected default, leaving 4 of its 5 arachne fixtures silently stale. The failures were masked in sequence: `arachne_perimeter_parity` is one `#[test]` fn with 5 fixture blocks run in series, and Rust aborts the test function on the first panic — so `tapered_wedge`'s failure (checked first) hid `max_bead_count_cap`, `complex_multi_feature`, and `cube_4color_arachne`'s equally-stale goldens (checked 2nd/4th/5th) until each prior one was fixed and re-run. **Fix:** re-recorded all 4 stale goldens via each fixture's existing `#[ignore]`d `record_*` recorder fn (no test-file or fixture-mesh/config edits — `git diff --stat` after is fixture-JSON-only); `narrow_strip_widening` (fixture 2) was already correct as-is (unaffected — did not depend on the cap or hit a winding flip) and was left untouched. Each fixture's own fixture-specific behavioral assertion (wall-count>1, per-color tool-index count, bead-cap ceiling) re-verified to still hold against the new captured output before/after re-recording. **Second-order finding, NOT fixed here (out of scope, flagged for follow-up):** while diagnosing the winding-order diff on `max_bead_count_cap`, found that `crates/slicer-schema/wit/deps/ir-types.wit`'s `wall-loop-view` record has NO `boundary-type` field at all (`{ perimeter-index, loop-type, path, feature-flags }`) despite `wall-boundary-type` existing as a WIT variant — so `crates/slicer-wasm-host/src/marshal/leaf.rs:433`'s `convert_wall_loop` hardcodes `boundary_type: slicer_ir::WallBoundaryType::Interior` unconditionally on every host-side wall-loop conversion, silently discarding whatever `boundary_type` either wall generator guest (arachne-perimeters `lib.rs:642-646`, classic-perimeters `lib.rs:723`) actually computed (both correctly compute `ExteriorSurface` for their outer wall). Verified via a poke test (forcing the guest's local `boundary_type` to `ExteriorSurface` unconditionally, rebuilding, re-running) that the host-observed field value did not change — proving the guest-side computation is real but never crosses the component boundary. This does NOT affect this entry's fix or any currently-passing test (every historical and current fixture baseline already shows `Interior` uniformly, since the gap predates all of them), but any future host-side consumer that branches on `WallLoop.boundary_type` for arachne/classic wall-generator output (e.g. paint/material-boundary logic, fuzzy-skin gating) will silently misbehave for outer walls. Tracked here as an open residual; not actioned without explicit direction since it requires a WIT contract change (`wall-loop-view` schema) crossing the component boundary. | slicer-runtime + core-modules maintainer | `wall-loop-view` WIT record missing `boundary-type` field (see finding above) | Closed — 2026-07-11: 4 stale `perimeter_parity` arachne goldens re-recorded against packet 151's correct `wall_count`/G1-winding behavior; `arachne_perimeter_parity` and full `perimeter_parity` module (11/11 non-ignored) green; `cargo check --workspace --all-targets` + `cargo clippy --workspace --all-targets -- -D warnings` + `cargo xtask build-guests --check` all clean; `boundary-type` WIT gap recorded as open residual, not actioned |

| D-154-WALL-BOUNDARY-TYPE-WIT-GAP | 2026-07-11 | crates/slicer-schema/wit/deps/ir-types.wit §`wall-loop-view` / crates/slicer-wasm-host/src/marshal/leaf.rs / crates/slicer-macros/src/lib.rs (guest-side generated converters) / crates/slicer-core/src/perimeter_utils.rs §`build_wall_flags` / modules/core-modules/arachne-perimeters/src/lib.rs | Medium | **D-153's flagged residual — closed.** Confirmed against packet 102's own `design.md` (Code Change Surface explicitly listed "updated `wall-boundary-type` variant" for `ir-types.wit`) that this was a real implementation gap, not a deliberate deferral: the `wall-boundary-type` variant and `material-boundary-segment` record existed in `ir-types.wit` but were never referenced by the `wall-loop-view` record that actually carries wall data across the WIT boundary, so all four marshal sites (`leaf.rs::ir_to_wit_wall_loop`/`convert_wall_loop`, `slicer-macros`' guest-side-generated `__slicer_ir_wallloop_to_wit`/`__slicer_wit_wallloop_to_ir`) hardcoded `Interior` regardless of what either perimeter generator's guest-side code actually computed. **Fix:** added `boundary-type: wall-boundary-type` to `wall-loop-view`; wrote paired `WallBoundaryType ↔ WitWallBoundaryType` / `MaterialBoundarySegment ↔ WitMaterialBoundarySegment` converters at all four sites (`WallBoundaryType`/`MaterialBoundarySegment` re-exported from `slicer-wasm-host::host` as `WitWallBoundaryType`/`WitMaterialBoundarySegment` to avoid colliding with the identically-named `slicer_ir` types). **OrcaSlicer research (delegated to sub-agents per the orca-delegation convention; `OrcaSlicerDocumented/` never loaded into this session's own context):** (1) confirmed arachne's `inset_idx == 0 → ExteriorSurface` rule is fully upstream-faithful — `ExtrusionLine.hpp`/`ExtrusionJunction.hpp` document "outer wall has index 0" as the sole classifier, `PerimeterGenerator.cpp:383-384` is the only determinant, no inner-bead exception exists; (2) discovered arachne-perimeters consumed ZERO paint data (not just `boundary_type` — `fuzzy_skin`/`tool_index` on `feature_flags` were also always default, since `_paint_regions` was an unread parameter), a materially bigger pre-existing gap than D-153 scoped; (3) found canonical OrcaSlicer applies MMU/fuzzy-skin paint via a HOST-SIDE Clipper boolean-clip of the *final* wall path against paint regions pre-resolved to `ExPolygons` (`FuzzySkin.cpp:685`/`MultiMaterialSegmentation.cpp`), identically for classic and Arachne — a mechanism with NO precedent for PnP's own per-vertex `nearest_original_vertex` reprojection technique (`perimeter_utils.rs:213`) anywhere in OrcaSlicer, and not portable to PnP's guest-WASM modules regardless (ADR-0013 explicitly rejects recomputing the paint partition inside a guest, since guests cannot perform boolean polygon ops). PnP's reprojection technique is therefore a deliberate, correct, guest-safe PnP invention, not an inferior stand-in for OrcaSlicer's approach — "align with OrcaSlicer" was the wrong yardstick for the *mechanism*. **Follow-on fix (same closure, user-directed scope expansion):** gave arachne real paint attribution by generalizing the existing reprojection path — relaxed `build_wall_flags`'s `use_reprojection` gate from `!is_outer && ...` to just `...` (reprojection is now used whenever the caller supplies geometry, regardless of `is_outer`; safe for classic, whose outer-wall call site already withholds that geometry) and fixed the reprojection branch's boundary-type fallback (previously hardcoded `Interior`, correct only because it was unreachable for `is_outer == true` before this change) to be `is_outer`-aware like the index-based branch already was — then rewrote arachne's per-wall loop to call `build_wall_flags` (passing every wall's own mm→units-converted points + `region.polygons()` + `region.segment_annotations()` unconditionally, since arachne has no wall type — not even the outer one — with index correspondence to the original contour, unlike classic's simple polygon-offset walls) instead of its old inline `if inset_idx==0 {Exterior} else {Interior}` + always-default `feature_flags`. **Verification:** new `arachne-perimeters/tests/boundary_paint_tdd.rs` (4 tests: unpainted defaults, material paint sets `tool_index` on BOTH outer and inner walls via reprojection, fuzzy-skin paint propagation, and a dedicated regression guard proving a uniformly-painted outer wall stays `ExteriorSurface` not `Interior`); new `perimeter_parity::arachne_outer_wall_boundary_type_survives_wasm_boundary` (drives the real compiled `arachne-perimeters.wasm` through `WasmRuntimeDispatcher`, proving the fix works across the actual component boundary, not just natively in-process). Discovered `compare_perimeter_ir` never asserted on `boundary_type`/`feature_flags` at all, so no `perimeter_parity` golden was test-blocking-stale despite all 11 committed fixtures showing pre-fix `Interior` uniformly; re-recorded all 11 anyway (goldens are reference data even where the comparator doesn't gate on a field) — diffed every re-recorded fixture against its pre-fix committed version and confirmed the ONLY field that ever changed anywhere is `boundary_type` (`Interior` → `ExteriorSurface` on outer walls); no fixture shows a `MaterialBoundary` variant or any `tool_index`/`fuzzy_skin` change, because none of the 11 committed fixtures carry `PaintSemantic::Material`/`FuzzySkin` `segment_annotations` — this codebase's committed MMU fixtures (e.g. `cube_4color_arachne`, `multi_tool_triangle`) resolve tool assignment via host-side region-partition + `variant_chain` (ADR-0013 "Model A"), not via per-vertex `segment_annotations`, so `build_wall_flags`'s Material-transition/reprojection path is exercised by this closure's new native tests but not by any existing committed fixture — flagged for future reviewers, not a gap in this fix. Full `arachne-perimeters` + `classic-perimeters` + `perimeter_parity` + relevant `slicer-wasm-host`/`slicer-runtime` contract/executor/integration buckets green; `cargo check --workspace --all-targets` + `cargo clippy --workspace --all-targets -- -D warnings` + `cargo xtask build-guests --check` all clean. **Confirmed pre-existing, unrelated to this fix (via `git stash` A/B on the exact narrow test):** `arachne-perimeters/tests/bridge_flow_factor_tdd.rs`'s two tests (`bridge_vertices_get_bridge_flow_ratio_when_thin`, `bridge_vertices_get_round_section_factor_when_thick_bridges_on`) fail identically on the clean pre-D-154 baseline — not touched by or in scope of this entry. **Not actioned (explicitly out of scope, flagged for a future packet):** replacing PnP's reprojection mechanism with anything OrcaSlicer-Clipper-style remains both infeasible (guest WASM boundary) and unnecessary (PnP's own architecture is the correct fit here) — any future "arachne paint parity" work should build on the reprojection generalization landed here, not chase OrcaSlicer's host-side mechanism. | slicer-wasm-host + core-modules maintainer | — (closed) | Closed — 2026-07-11: `boundary-type` wired through WIT at all four marshal sites; arachne-perimeters gained real paint attribution via `build_wall_flags` reprojection generalization; all 11 `perimeter_parity` goldens re-recorded (boundary_type-only diff, verified); new native + WASM-boundary regression tests added; full narrow verification suite + `cargo check`/`clippy`/`build-guests --check` clean |
| D-152-TOP-AREA-SOURCE | 2026-07-11 | modules/core-modules/arachne-perimeters/src/lib.rs §`emit_only_one_wall_top_second_pass` / crates/slicer-sdk/src/views.rs §`top_solid_fill` | Medium | **Packet 152 Step 4 (G3 part 2) — top-area source divergence from OrcaSlicer.** PnP has no `upper_slices`/`lower_slices` access on `SliceRegionView`, so the G3 part-2 second-pass top-area is derived from `region.top_solid_fill()` (the host's precomputed top-region polygon) instead of OrcaSlicer's `diff_ex(infill_contour, upper_slices_clipped)` (`PerimeterGenerator.cpp:2160-2246`). The bridge-exclusion step (Orca carves `lower_slices_clipped_offset` out of the top area) is SKIPPED for the same reason. All other steps are ported: `min_width_top_surface` filter via `get_abs_value` (packet 150 mechanism), `offset2_ex` shrink `-top_surface_min_width` / expand `+top_surface_min_width + 0.85*perimeter_width`, second `WallToolPaths` over the non-top remainder with `inner_loop_number + 1` walls, `inset_idx` (`perimeter_index`) += 1 renumbering of inner walls BEFORE merge, and the empty-top fallback rerun with `inner_loop_number + 2`. AC-2 verified (single top wall + renumbering) via `tests/only_one_wall_top_tdd.rs::only_one_wall_top_second_pass`. | core-modules maintainer | — | Open — divergence from Orca top-area source; per ADR-0035's recording bar, file a new/amending ADR documenting PnP's `top_solid_fill`-sourced G3 part-2 path before closing. |
| D-152-CLASSIC-MIN-WIDTH-TOP-SURFACE-REMAINDER | 2026-07-12 | modules/core-modules/classic-perimeters/src/lib.rs §`emit_walls` (read-and-discard at `classic-perimeters/src/lib.rs:224-239`) / classic-perimeters.toml §`[config.schema]` | Low | **Packet 152 — successor deviation split from `D-104d-MIN-WIDTH-TOP-SURFACE-NONE`.** The `arachne-perimeters` half of D-104d landed in P152 (G3 part-2 second-pass `only_one_wall_top` + `min_width_top_surface` width gate wired; see `D-152-TOP-AREA-SOURCE`). The `classic-perimeters` half remains open: `min_width_top_surface` is read and validated (must be a finite, non-negative mm value, default `1.2`) at `classic-perimeters/src/lib.rs:224-239` but the value is intentionally unused (`let _ = min_width_top_surface;`) — the `only_one_wall_top` width-comparison carve boundary it is meant to gate is not wired on the classic path, so a top-surface loop narrower than `min_width_top_surface` still collapses to a single wall unconditionally (no per-loop width comparison). **Not implemented in P152** — this entry records the remaining gap only. | classic-perimeters maintainer | — | Open — `min_width_top_surface` read-and-discarded on classic-perimeters; wire the `only_one_wall_top` width-threshold carve boundary (OrcaSlicer `PerimeterGenerator.cpp:2160-2245`, `PrintConfig.cpp:1491-1511) |
| D-154-DISCRETIZE-POINT-POINT-CASE | 2026-07-14 | crates/slicer-core/src/skeletal_trapezoidation/graph.rs::discretize_edge (line 1280) | Low | **Found during packet 154's D-105D root-cause investigation; filed, not fixed.** PnP's `discretize_edge` returns `{start, end}` for ALL `!is_curved` edges — it conflates canonical `discretize()`'s branch 1 (`(!point_left && !point_right) \|\| vd_edge.is_secondary()` → `Points({start,end})`, `SkeletalTrapezoidation.cpp:236`) with its branch 3 (both cells are point-cells → a straight edge that is STILL subdivided by `discretization_step_size`, `:246`). Canonical branches at `SkeletalTrapezoidation.cpp:220` (dispatch), `:236` (branch 1), `:240` (branch 2, point-line), `:246` (branch 3, point-point). Consequence: point-point VD edges are never subdivided in PnP, so point-point channels that narrow between two vertices get no intermediate beading samples. Sub-gap noted in the same investigation: PnP's `discretize_edge` also omits branch 1's `is_secondary()` disjunct; whether PnP's `is_curved` flag already encodes secondary-edge status is unconfirmed. **Provably NOT the D-105D thin-strip root cause** — a rectangle's spine is a seg-seg edge (branch 1), not point-point (branch 3); confirmed the real root cause was `preprocess.rs::merge_short_segments` (see D-105D, closed). | arachne maintainer | TBD | Open — filed by packet 154, not fixed; scope is point-point VD edge subdivision only |
| D-104f-CONCENTRIC-INFILL-NO-ARACHNE (packet 154 note) | 2026-07-14 | crates/slicer-runtime/src/run.rs / crates/slicer-runtime/tests/arachne_parity.rs:915 | Low | **Re-surfaced (not caused, not fixed) by packet 154's test runs during the D-105D thin-strip investigation.** This gap was filed 2026-07-09 (packet 149, row above) and remains open under its original ID; this note only records that packet 154 independently re-confirmed it while running the full `arachne_parity` suite: `run.rs` never dispatches concentric infill through `run_arachne_pipeline`, whereas OrcaSlicer routes concentric infill wall generation through Arachne's `WallToolPaths` (`FillConcentric.cpp:80-118`). The test `arachne_parity_pipeline_concentric_infill_uses_arachne` (`crates/slicer-runtime/tests/arachne_parity.rs:915-938`) already proposes this exact deviation ID in its own assertion message. Pre-existing and out of packet 154's scope. | core-modules maintainer | — (deferred; follow-up workstream TBD) | Open — unchanged; see original row above for full detail |

| D-155-BEADING-THRESHOLD-PARITY | 2026-07-14 | crates/slicer-core/src/beading/* (`BeadingStrategy` trait + 4 decorators + `factory.rs`)/ docs/18_arachne_parity_audit.md G15 | Low | **Packet 155 (G15) — three residual parity deviations retained at closure, all recorded here.** (1) `get_add_middle_threshold` is a **net-new PnP symbol** with no OrcaSlicer counterpart — OrcaSlicer's `BeadingStrategy` carries the `wall_add_middle_threshold` protected field but exposes NO getter (`BeadingStrategy.hpp` has only `getSplitMiddleThreshold()`); PnP adds `get_add_middle_threshold` because its decorators cannot read a protected base field the way Orca's copy-constructed base can. (2) The clamp inputs use PnP's `preferred_bead_width_outer` (split) / `optimal_width` (add) in place of OrcaSlicer's `WallToolPaths.cpp:619-640`, which divides the split threshold by the **external** perimeter width and the add threshold by the **inner** perimeter width — each first passed through `Flow::rounded_rectangle_extrusion_width_from_spacing`, which PnP has no analogue for at this site. (3) With the **shipped defaults** (`min_output_width == optimal_width == preferred_bead_width_outer == 4000` units), **both thresholds saturate at the `0.99` clamp ceiling** (`clamp(1.0, 0.01, 0.99) == 0.99`), which makes `minimum_line_width = 0.99 * optimal_width` and therefore suppresses middle-bead addition relative to the old `.round()` heuristic (which behaved like a `0.5` threshold). **AC-10 consequence:** any bead-count or wall-count fixture shift after this packet is a real behavior change from this saturation, NOT a test bug — re-record only after confirming the new value matches the OrcaSlicer formula by hand. **Step 6 verification:** `cargo test -p slicer-runtime --test arachne_parity && cargo test -p slicer-core` confirmed **no AC-10 fixture shifts** in the 14 round-1 locks + G3/G10 closures — the saturation is present but no existing locked fixture exercised the middle-bead-add branch, so its effect is not yet observable in the locked set. The clamp bounds `[0.01, 0.99]` are OrcaSlicer's literal constants and were NOT altered. | slicer-core maintainer | — | Closed — 2026-07-14 (packet 155): trait + stack forwarding + factory plumbing landed; residuals fully enumerated above |

| D-156-SIMPLIFY-INTERSECTION-GATE | 2026-07-14 | crates/slicer-core/src/arachne/simplify.rs (`simplify_distance_gated` tier-3)/ crates/slicer-runtime/tests/arachne_parity_round2.rs (`..._simplify_intersection_distance_gate_present`) | Low | **Packet 155 (G20) — RED test parameters changed, not the assertion.** The original G20 RED test parameters (`smallest_line_segment_squared = 0.0`, `allowed_error_distance_squared = f64::INFINITY`) were **vacuous**: in canonical OrcaSlicer the tier-3 special case is nested inside `if (length2 < smallest_line_segment_squared && height_2 <= allowed_error_distance_squared)` (`ExtrusionLine.cpp:162-164`), and `length2` is a `squaredNorm()` (always ≥ 0), so `smallest_line_segment_squared == 0` makes the guard `length2 < 0` — unsatisfiable for every input, leaving `ExtrusionLine.cpp:166-220` dead. The old parameters therefore proved nothing about the gate. **Per AC-6**, the test was changed to `smallest_line_segment_squared = 1e-3` mm² and `allowed_error_distance_squared = 1.0`, putting junction 2 of `simplify_input_intersection_distance_gate()` squarely inside the gate so the `dist_greater` predicate genuinely fires and retains the junction — strengthening the assertion from `kept >= 4` to `kept == 4` with the exact junction sequence checked. This is a declared, justified exception to the "test bodies are immutable" rule (the gate itself, not the expected end state, was unreachable under the originals). `use_distance_gates` is NOT modified. | slicer-core maintainer | — | Closed — 2026-07-14 (packet 155): gate ported; RED test parameters corrected per AC-6 |
| D-157-REGION-ORDER-PORT | 2026-07-14 | crates/slicer-core/src/arachne/region_order.rs / crates/slicer-core/src/arachne/sparse_point_grid.rs / crates/slicer-schema/wit/deps/common.wit / modules/core-modules/arachne-perimeters/src/lib.rs / modules/core-modules/path-optimization-default/src/lib.rs | Low | Packet 156 implements the G12 region-order path using the current Orca references (`WallToolPaths.cpp:832-889`; finalized-extrusion walk `PerimeterGenerator.cpp:2287-2364`). The only intentional behaviorally equivalent differences retained are PnP's explicit original-input-index tie-break for deterministic selection (equivalent to Orca's stable sort for stable input) and the zero-width/empty-input guard that returns no constraints without constructing a zero-cell grid. The sparse grid is candidate-only; exact pair eligibility remains in `get_region_order`. The existing three-state `wall_sequence` is transported through `arachne-params`; the perimeter module resolves and commits it, while path optimization preserves the committed wall subsequence. | — | — | Implementation complete through Steps 1-7; pending final packet acceptance ceremony. Do not close before all ACs pass and Step 9 review approves closure. |
