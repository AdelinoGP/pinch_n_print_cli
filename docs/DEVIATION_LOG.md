# Deviation Log

Use this registry for intentional deviations from architecture docs.

<!-- DEV-025 closure summary (2026-05-08): overall status: closed. All five SDK↔WIT prepass segmentation mismatches resolved. Mismatch 3 closed-by-Packet-43 (43-rev1_macro-prepass-segmentation-output-drain) via TASK-130a/TASK-130b on 2026-05-08. Mismatches 4+5 closed by Packet 42 (TASK-130c). Mismatches 1+2 closed by TASK-128a/TASK-128b. Full TASK-130 cluster (TASK-128a, TASK-128b, TASK-130, TASK-130a, TASK-130b, TASK-130c) retired. -->

| ID | Date | Affected Section | Risk | Rationale | Mitigation Owner | Target Close | Status |
| --- | --- | --- | --- | --- | --- | --- | --- |
| DEV-001 | 2026-04-14 | 05_module_sdk.md §"Python Bridge (TextPostProcess tier)" / 00_project_overview.md §"Python bridge" | Low | Initially shipped as a `python3 -I` subprocess driver because pyo3 0.20 did not support Python 3.14. **Closed 2026-04-14** by upgrading to `pyo3 = "0.28.3"` (feature `auto-initialize`) and replacing the subprocess backend with an embedded PyO3 implementation inside `PythonBridge::run_text`. No subprocess path remains. | slicer-host maintainer | 2026-04-14 | Closed |
| DEV-002 | 2026-04-15 | 01_system_architecture.md §"Module Access Contract" / 01_system_architecture.md §"Stage I/O Contract" | High | All 16 core-module manifests currently declare empty `[ir-access].reads` and `[ir-access].writes`, so the shipped manifest contract does not match the documented stage-level IR access requirements. | core-modules maintainer | TBD | Closed — all 17 core-module manifests now populate `[ir-access]` declarations; `core_module_ir_access_contract_tdd.rs` green (TASK-121) |
| DEV-003 | 2026-04-15 | 04_host_scheduler.md §"Call-Time Access Enforcement" / 01_system_architecture.md §"Module Access Contract" | High | `DagValidationRequest.access_audits` and `ModuleAccessAudit` exist, but the live prepass / layer / postpass paths do not populate them, so undeclared reads and writes are not rejected on real runs. | slicer-host maintainer | TBD | Closed — `ModuleAccessAudit` records now populated from all live prepass/layer/postpass paths; undeclared runtime accesses rejected at the WIT boundary (TASK-123–124) |
| DEV-004 | 2026-04-15 | 01_system_architecture.md §"Allowed Claim Transition Matrix" | High | Startup validation does not enforce the non-transitionable claim rows because the conflict scope includes `global_layer_index`, allowing layer-varying holders for claims that docs mark as stable-only. | slicer-host maintainer | TBD | Closed — Claim Transition Matrix now enforced for all non-transitionable claims; `claim_transition_matrix_tdd.rs` green (TASK-125) |
| DEV-005 | 2026-04-15 | 01_system_architecture.md §"Non-Planar Envelope" | High | No host-side boundary currently rejects outputs whose Z leaves the required interval `[layer.z, layer.z + effective_layer_height]`. | slicer-host maintainer | TBD | Closed — non-planar Z envelope now enforced at output-commit boundaries (TASK-127) |
| DEV-006 | 2026-04-15 | 03_wit_and_manifest.md / 05_module_sdk.md | High | Resource-level deep copy is VERIFIED CLOSED for the layer world and finalization world. The remaining runtime marshaling work is now narrower: (1) `PostPass::GCodePostProcess` still receives an empty commands list because `dispatch_postpass_gcode_call` passes `&[]`, so per-command GCode content never crosses the WIT boundary; (2) native-only test or fallback paths do not exercise the full WIT deep-copy boundary; (3) prepass segmentation marshaling is no longer tracked here because its remaining failures are structural SDK↔WIT shape mismatches, consolidated under DEV-025. Use DEV-006 for remaining non-segmentation data-boundary gaps only. | slicer-macros + slicer-host maintainers | TBD | Closed — postpass GCode command bodies now cross the WIT boundary via real command lists; layer-world and finalization-world boundary coverage also landed (TASK-129a/b/c) |
| DEV-007 | 2026-04-15 | 04_host_scheduler.md §"PrePass lifecycle" | Medium | `PrePass::MeshSegmentation` and `PrePass::PaintSegmentation` modules exist. The dispatcher handles these stages through typed `PrepassModule` bindings (`dispatch_prepass_call` at dispatch.rs:483-489). All 17 core-module `.wasm` artifacts are now real components (built via `build-core-modules.sh`); `PLACEHOLDER_MODULES` is empty. The `mesh-segmentation` and `paint-segmentation` `.wasm` files are 103 KB and 62 KB respectively. **Closed 2026-04-15** — the gap described here (placeholder stubs) is resolved. **AUDIT-12 update (2026-04-16):** The remaining limitation noted in the original close (dispatch returning UnknownStage for MeshSegmentation/PaintSegmentation) is now also resolved — dispatch.rs:474-490 handles all four prepass stages. 12 tests prove the full dispatch + harvest pipeline. | slicer-host maintainer | 2026-04-15 | Closed |
| DEV-008 | 2026-04-15 | 03_wit_and_manifest.md §"Manifest schema" / 05_module_sdk.md §"Config schema" | Medium | Core-module manifests ship with empty `[config.schema]` blocks, so the CLI schema query surface is present but does not describe the real configuration semantics of the built-in modules. | core-modules maintainer | TBD | Closed — all 17 core-module manifests now populate `[config.schema]` declarations (TASK-122) |
| DEV-009 | 2026-04-15 | 07_implementation_status.md Phase H / 01_system_architecture.md live pipeline expectations | High | The Phase H Benchy run is only partially correct on the live path: `.gcode` is produced, but top/bottom fill, supports, seam placement, retract/unretract behavior, and OrcaSlicer-compatible GCode comment metadata required for native preview visualization are still missing or incomplete. **Remediation progress:** packet 52_gcode-feedrate-emission has implemented per-role feedrate emission covering all ExtrusionRole variants (13 named + Custom) with 26 speed config keys, and F-tokens are now resolved on every print and travel move via resolve_feedrate (skirt→model and wipe-tower travel inserts also carry a resolved travel feedrate). **Progress 2026-05-14 (packet 54):** skirt-brim emit subset closed (TASK-142a — `lib.rs` defaults fix + 5 tests); relative-E subset closed (TASK-183 — M82/M83 toggle + 10 tests). Cooling subset closed via packet 53 (TASK-154); feedrate subset closed via packet 52 (TASK-153). DEV-009 fully closed for these subsets via packets 52+53+54. **Progress 2026-05-15 (packet 57):** packet 57 (`57_overhang-speed`) implements the per-quartile wall slowdown end-to-end: WIT field `overhang-quartile`, Rust mirror, `LinesDistancer2D` classifier, `resolve_feedrate` dispatch, and pipeline wire-in. DEV-009 is partially remediated; the remaining open items (smoothed-speed mode, six-band schedule parity, `ADD_INTERSECTIONS` mid-segment splitting, curled-edge slowdown) are deferred to follow-up packets. Remaining open DEV-009 items: seam placement end-to-end (`benchy_live_path_contains_resolved_seam_evidence_before_emit`), retract/unretract emission, top/bottom surface G-code evidence (tracked under DEV-033). 2026-05-18: doc-side gaps for packet-34 retraction mode, packet-52 per-role speeds, packet-53 cooling fan, packet-54 relative-E + skirt-brim, packet-55 header/thumbnail, packet-57 overhang quartile, packet-58 toolchange-purge, packet-59 machine start/end, packet-60 precision now documented in `docs/02_ir_schemas.md`, `docs/03_wit_and_manifest.md`, `docs/04_host_scheduler.md`, `docs/05_module_sdk.md`, `docs/01_system_architecture.md`, `docs/10_glossary_and_scenario_traces.md`, `docs/16_slicer_report.md`. See `docs/AUDIT_2026-05-18_doc_gaps.md`. | slicer-host + core-modules maintainers | TBD | Open |
| DEV-010 | 2026-04-15 | 11_operational_governance_and_acceptance_gate.md / 12_architecture_gate_metrics.md / 07_implementation_status.md | Medium | The implementation is past MVP, but the Architecture Acceptance Gate has not yet been evaluated and the remaining governance closure work is still open. | project maintainer | TBD | Open |
| DEV-011 | 2026-04-15 | 01_system_architecture.md §WASM Instance Pools / 04_host_scheduler.md §Phase 4 | High (Partial) | Typed WASM dispatch linking is implemented end to end: `WasmRuntimeDispatcher` instantiates typed component-model guests for all four runner families, wires imports through the typed linker path, calls stage exports, and commits outputs back into pipeline state. All 17 core-module `.wasm` artifacts are real components and all four prepass stages are dispatched. This row remains partial only because some stage surfaces still cannot carry full semantics after dispatch: postpass GCode command bodies do not cross the boundary (DEV-006), finalization modules still keep live logic in legacy `process()` instead of `run_finalization()` (DEV-013), and prepass segmentation macro bindings still have hollow inputs or missing drain-back due to SDK↔WIT shape mismatch (DEV-025). Dispatcher instantiation and stage routing are no longer the blocker. | slicer-host + core-modules maintainers | TBD | Partial — dispatch/linking fixed; remaining stage-surface gaps tracked in DEV-006, DEV-013, and DEV-025 |
| DEV-012 | 2026-04-15 | 05_module_sdk.md §"Module Entry Point" / 03_wit_and_manifest.md | Medium | Historical audit notes had recorded that `#[slicer_module]` emitted only marker methods with no `wit_bindgen::generate!` export bindings and placeholder `extern "C"` shims. **This is now fixed.** As of 2026-04-15, the macro emits real `wit_bindgen::generate!`-backed typed export glue for ALL FOUR WIT worlds (postpass, finalization, prepass, layer) via `build_postpass_world_glue`, `build_finalization_world_glue`, `build_prepass_world_glue`, and `build_layer_world_glue`. Placeholder `extern "C" fn ... -> i32 { 0 }` shims are suppressed for all supported worlds (`real_glue_world.is_some()` gate). Each world's Guest impl routes through the corresponding SDK trait (`PostpassModule`, `FinalizationModule`, `PrepassModule`, `LayerModule`) with typed ConfigView adaptation. The layer world covers all 8 stage exports + 2 lifecycle exports. The prepass world covers all 4 stage exports (MeshAnalysis, LayerPlanning, MeshSegmentation, PaintSegmentation). **AUDIT-03 update (2026-04-15):** All 17 core modules now use `#[slicer_module]` at the source level — none bypass the macro. All 17 `.wasm` artifacts are real components (38–265 KB), none are 8-byte placeholders. The `PLACEHOLDER_MODULES` array in `build-core-modules.sh` is empty. The macro's wasm32 export glue is exercised by real core-module components, not just test guests. Verified by: `all_worlds_glue_tdd.rs`, `postpass_text_glue_tdd.rs`, `binding_surface_tdd.rs`, `slicer_module_tdd.rs`, `macro_all_worlds_roundtrip_tdd.rs`, `macro_postpass_text_roundtrip_tdd.rs`, `macro_finalization_deep_copy_tdd.rs`. SDK test guests: `sdk-layer-infill-guest`, `sdk-postpass-text-guest`, `sdk-prepass-guest`, `sdk-finalization-guest`. | slicer-macros maintainer | N/A (fixed) | Closed |
| DEV-013 | 2026-04-15 | 05_module_sdk.md §"FinalizationModule" / modules/core-modules/skirt-brim, modules/core-modules/wipe-tower | High | The finalization WIT boundary and `LayerCollectionView` deep copy are in place, but the two finalization core modules (`SkirtBrim`, `WipeTower`) still keep their real geometry logic in legacy `process(&mut Vec<LayerCollectionIR>)` while `run_finalization()` remains the default no-op. Because the live host path dispatches finalization through `call_run_finalization()`, those modules currently emit no skirt/brim/wipe-tower output through WASM. Distinct fix task: port each module's `process()` behavior into `run_finalization()` using `LayerCollectionView` reads plus `FinalizationOutputBuilder` writes, then delete or demote the legacy API path. **Partial close 2026-04-25 (packet 16):** `SkirtBrim::run_finalization()` fully implemented; host dispatch batch-prepend fix landed; all 5 skirt-brim acceptance tests pass. **Full close 2026-04-25 (packet 17):** `WipeTower::run_finalization()` fully implemented using `LayerCollectionView` tool-change discovery and `FinalizationOutputBuilder::push_entity_to_layer()`. All 5 wipe-tower acceptance tests pass (4 module-level + 1 live host integration against real `wipe-tower.wasm`). `wipe-tower.wasm` rebuilt. Both finalization core modules now emit geometry through `run_finalization()` on the live WASM path. | core-modules maintainer | 2026-04-25 | Closed — both SkirtBrim and WipeTower ported to run_finalization(); live WASM path verified |
| DEV-014 | 2026-04-16 | 03_wit_and_manifest.md §"WIT File Organization" | Medium | WIT compatibility is still split across multiple sources: on-disk `wit/`, macro inline WIT, host inline WIT, and hand-written test guests. That duplication has already caused real drift: package naming differs (`slicer:world-layer@1.0.0` vs `slicer:layer-world@1.0.0`), `needs-support` is missing from inline WIT, postpass inline `gcode-output-builder` omits `push-z-hop`, `ir-types` version literals diverge, and schema/CLI constants follow the on-disk identifiers while macro/host codegen follows the inline identifiers. This row is the canonical WIT compatibility task. Distinct fix tasks: (1) move macro, host, and test guests onto one shared WIT source; (2) normalize package/version literals and missing members; (3) add host-side `wit_world` allowlist validation using the same canonical identifiers as CLI and schema. | slicer-macros + slicer-host maintainers | 2026-04-24 | Closed 2026-04-24 — macro, host, and test-guest codegen consolidated onto one canonical WIT source in `wit/`; package/version literals normalized; `validate_wit_world` enforces allowlist at startup; `wit_drift_detection_tdd.rs` locks all surface members (TASK-144–146, packet `25_wit-canonical-surface-lock`) |
| DEV-015 | 2026-04-16 | 01_system_architecture.md §"Host Services" / 03_wit_and_manifest.md §"host-api.wit" | High | Geometry host services (`clip_polygons`, `offset_polygons`, `simplify_polygon`) are now backed by real Clipper2 (`clipper2-rust`) via `slicer_core::polygon_ops`. Verified by 13 passing tests in `host_services_tdd.rs` — union, intersection, difference, XOR, offset (positive/negative), simplify (collinearity removal), empty-input edge cases, and monotonicity of `now_us`. **However, mesh query host services remain stubs:** `raycast_z_down` returns `Ok(None)` (no mesh data), `surface_normal_at` returns `Ok(None)`, and `object_bounds` returns a `wasmtime::Error` trap with "not yet wired" diagnostic. These mesh queries are propagated identically across all four WIT worlds (layer, prepass, finalization, postpass). This row is partial: geometry ops are real, mesh queries are not. | slicer-host maintainer | TBD | Closed — `raycast_z_down`, `surface_normal_at`, and `object_bounds` now backed by real mesh-query implementations; hit/miss semantics verified across all WIT worlds (TASK-147–148) |
| DEV-016 | 2026-04-16 | 02_ir_schemas.md §ExtrusionRole / PaintSemantic / WallFeatureFlags / 03_wit_and_manifest.md §deps/types.wit §deps/ir-types.wit | Medium | **Closed 2026-04-20.** The structural payload-dropping WIT limitation is removed. `types.wit` now declares `variant extrusion-role { ..., custom(string) }`, `ir-types.wit` now declares `variant paint-semantic { ..., custom(string) }`, and `wall-feature-flag` carries `custom`. Host, macro, and guest boundary adapters preserve custom payload strings instead of synthesizing empty values, and reserved `custom(string)` tags preserve built-in `PrimeTower` / `Skirt` roles losslessly across the WIT boundary. Canonical docs now describe the widened shapes and the reserved built-in tags. | slicer-macros maintainer | 2026-04-20 | Closed |
| DEV-017 | 2026-04-16 | 02_ir_schemas.md §IR 10 LayerCollectionIR / 03_wit_and_manifest.md §"Path Optimization Output Contract" §"z-hops" | Low | **Verified by AUDIT-11 (2026-04-16):** Historical audit notes that had claimed `LayerCollectionIR.z_hops` and annotation passthrough were unwired are **stale** — these features are now fully implemented. Evidence: (1) `push-z-hop` method exists in on-disk `ir-types.wit:107`, macro inline WIT `lib.rs:2222`, and host inline WIT `wit_host.rs:282`. (2) Host commit path at `dispatch.rs:1378-1436` validates z-hop calls (bounds check, finite/positive height) and routes accepted z_hops to `arena.push_deferred_z_hop()`. (3) `LayerArena` has `deferred_z_hops: Vec<ZHop>` (`blackboard.rs:293`) with `push_deferred_z_hop` / `take_deferred_z_hops` (`blackboard.rs:452-458`). (4) Layer executor drains deferred z_hops into `LayerCollectionIR.z_hops` (`layer_executor.rs:393`). (5) `DefaultGCodeEmitter` consumes z_hops and renders lift+return travel moves (`gcode_emit.rs:155-175`). (6) Five tests prove the full pipeline: `path_optimization_commit_routes_z_hops_to_deferred_queue`, `path_optimization_z_hop_rejects_out_of_bounds_index`, `path_optimization_z_hop_rejects_invalid_hop_height`, `path_optimization_end_to_end_populates_z_hops`, `path_optimization_end_to_end_emitter_renders_z_hops` (all in `dispatch_tdd.rs`, all pass). Similarly, the old claim that comment/raw GCode commands were silently dropped at commit is false — `push-comment` and `push-raw` are accepted at PathOptimization (`dispatch.rs:1366-1377`), routed via `push_deferred_annotation` into `LayerAnnotation` records, drained into `LayerCollectionIR.annotations` (`layer_executor.rs:391-392`), and rendered by the GCode emitter (`gcode_emit.rs:139-152`). Verified by `path_optimization_commit_routes_comment_and_raw_to_deferred_annotations` test. **What remains deferred:** (a) guest reordering of `ordered_entities` — no WIT `layer-collection-builder` resource exists; reordering is reserved for a future step per docs/03 §680-687; (b) richer anchoring for PathOptimization overrides — currently anchored at "after last entity" only. | slicer-host maintainer | TBD | Partial — z_hops + annotations resolved, entity reordering deferred |
| DEV-018 | 2026-04-16 | 04_host_scheduler.md §"PrePass lifecycle" / 03_wit_and_manifest.md §"world-prepass.wit" | Low | **Verified by AUDIT-12 (2026-04-16):** Historical audit notes that had claimed `PrePass::MeshSegmentation` and `PrePass::PaintSegmentation` were not dispatched by the host are **stale** — both stages are now fully dispatched. Evidence: (1) `export_name_for_stage` maps both stages (`dispatch.rs:33,36`). (2) `dispatch_prepass_call` match arms handle all four prepass stages (`dispatch.rs:474-490`): MeshAnalysis → `call_run_mesh_analysis`, LayerPlanning → `call_run_layer_planning`, MeshSegmentation → `call_run_mesh_segmentation`, PaintSegmentation → `call_run_paint_segmentation`. (3) Host inline WIT in `wit_host.rs:444-471` defines `mesh-segmentation-output` resource with `mark-triangle-paint` and `paint-segmentation-output` resource with `push-paint-region`. (4) Resource impls on `HostExecutionContext` collect marks (`wit_host.rs:2091-2126`) and paint entries (`wit_host.rs:2054-2089`) with validation. (5) Harvest functions convert WIT output to IR: `harvest_mesh_segmentation_ir` (`dispatch.rs:1006-1026`) and `harvest_paint_segmentation_ir` (`dispatch.rs:907-996`). (6) `PrepassStageRunner::run_stage` routes all four stages to their harvest functions (`dispatch.rs:1084-1121`). (7) Twelve tests pass: 6 for mesh-segmentation (empty IR, config-driven marks, determinism, invalid marks rejection, commit through execute_prepass, macro drain order) and 6 for paint-segmentation (empty regions, config-driven regions, determinism, malformed config diagnostic, invalid entries rejection, commit through execute_prepass) — all in `dispatch_tdd.rs`. **Remaining limitation (not a dispatch gap):** The macro's `build_prepass_world_glue` comment at `lib.rs:996-998` is stale — it says MeshSegmentation/PaintSegmentation "are not yet routed by the host's wit_host.rs" but they are. The macro's actual code does route them (lines 1249-1332), though PaintSegmentation passes empty `sdk_objects` and the drain-back from `PaintSegmentationOutput` to WIT `push-paint-region` is not fully wired in the macro path (canonical modules use hand-written wit-guest instead). This is a macro-side data marshaling limitation, not a dispatch gap. | slicer-host maintainer | 2026-04-15 | Closed — dispatch fully wired, macro comment stale |
| DEV-019 | 2026-04-16 | 04_host_scheduler.md §"Phase 4 Execution" / 09_progress_events.md / 07_implementation_status.md TASK-108 | Low | **Verified by AUDIT-13 (2026-04-16):** Historical audit notes that had claimed the paint-annotation annotator (TASK-108) was not wired into `pipeline.rs` / `layer_executor.rs` are **stale** — the annotator is fully wired into the live pipeline. Evidence: (1) `layer_executor.rs:19-21` imports `execute_slice_postprocess_paint_annotation`, `paint_annotation_warning_to_progress_event`, and error types. (2) `execute_per_layer_with_events` (`layer_executor.rs:199-215`) accepts a `LayerProgressSink` and passes it through to `execute_single_layer`. (3) `execute_single_layer` (`layer_executor.rs:244-395`) calls `run_paint_annotation` at two points: (a) at the end of `Layer::SlicePostProcess` stage (`layer_executor.rs:349-352`), and (b) as a fallback if no SlicePostProcess stage was scheduled (`layer_executor.rs:358-360`). (4) `run_paint_annotation` (`layer_executor.rs:402-456`) calls `execute_slice_postprocess_paint_annotation`, converts warnings via `paint_annotation_warning_to_progress_event`, and pushes them to `sink.record()`. (5) Fatal annotation errors become typed `LayerExecutionError::PaintAnnotation` (`layer_executor.rs:427-430`). (6) `pipeline.rs:124` (`run_pipeline_with_events`) passes the sink through to `execute_per_layer_with_events` (`pipeline.rs:154`). (7) `main.rs:231-235` constructs `JsonLinesEmitter` + `RuntimeProgressSink` + `SliceEventCollector` and calls `run_pipeline_with_events`. (8) 8 integration tests in `paint_annotation_integration_tdd.rs` all pass: warnings reach sink with code 504 + fatal=false, degraded flag is set, JSONL output is byte-identical across runs, missing semantic surfaces as typed fatal error (code 501), no-op when no paint regions, empty pipeline emits no spurious events, and main.rs production entry path is guarded. (9) 10 unit tests in `slice_postprocess_paint_annotation_tdd.rs` all pass covering the annotator's internal logic. | slicer-host maintainer | 2026-04-16 | Closed — annotator fully wired |
| DEV-020 | 2026-04-16 | 07_implementation_status.md §Phase G | Low | **Verified by AUDIT-14 (2026-04-16):** Phase G still overstates completion: the live wiring does use `WasmRuntimeDispatcher` for all four stage runners, but `main.rs:20-84` still retains all four dead `Noop*Runner` structs and the source/docs cleanup was never finished. The stale line references from the audit were discarded, but the underlying issue remains: Phase G is marked complete while dead fallback code is still present. `docs/07_implementation_status.md` tracks the cleanup under `TASK-139`. | slicer-host maintainer | TBD | Open — dead code remains, Phase G overstates completion |
| DEV-021 | 2026-04-16 | 07_implementation_status.md §TASK-111 | Low | **Verified by AUDIT-15 (2026-04-16):** Historical audit notes that had claimed TASK-111 overstated real component coverage are now stale — the situation has been fully resolved. Evidence: (1) All 17 canonical `.wasm` files are real component-model binaries ranging from 38 KB (skirt-brim, wipe-tower) to 266 KB (arachne-perimeters). None are 8-byte placeholders. (2) `PLACEHOLDER_MODULES=()` in `build-core-modules.sh` is empty. (3) All 17 modules use `#[slicer_module]` at the source level (guarded by `core_module_macro_adoption_tdd.rs`). (4) The old TASK-111 wording that motivated the audit no longer exists in `docs/07`. (5) The claim that only one module had a real artifact is false. | slicer-host maintainer | 2026-04-16 | Closed — all 17 modules have real .wasm components |
| DEV-022 | 2026-04-16 | 03_wit_and_manifest.md §"config-view resource" / 05_module_sdk.md §"ConfigView" | Low | `ConfigView` mutability and host pre-filtering are verified CLOSED by AUDIT-16: `fields` is private, the live path binds through `bind_module_config_view` → `ConfigView::from_declared`, and regression tests cover both behaviors. The only unresolved sub-issue originally bundled into this row was the missing host-side `wit_world` gate, which is now consolidated into DEV-014 so that WIT compatibility work has a single canonical owner. No independent engineering task remains here. | slicer-host maintainer | 2026-04-16 | Closed — ConfigView issues fixed; residual WIT-world gate merged into DEV-014 |
| DEV-023 | 2026-04-16 | 05_module_sdk.md §"PathOptimization" / modules/core-modules/path-optimization-default/src/lib.rs | Low | **Verified by AUDIT-17 (2026-04-16):** The PathOptimization core module is still a minimal slot-filler; no material feature parity progress has been made. Evidence: (1) `path-optimization-default/src/lib.rs:57-84` — `run_path_optimization` only emits a deterministic comment marker (`; path-optimization layer N regions=R entities=E`) via `push_comment`. It does NOT implement: travel optimization (nearest-neighbor/TSP), seam scheduling consumption, multi-tool ordering, z-hop emission, cooling/fan overrides, multi-object ordering, bridge/overhang reordering, or wipe/brim coordination. (2) Module reads only `PerimeterRegionView` — does not access `ordered_entities` directly. (3) `layer_executor.rs:366-386` — fallback path still assembles from arena slots without path optimization output. (4) Host-side infrastructure IS now in place: z_hop routing (dispatch.rs:1378-1410), comment/raw passthrough (dispatch.rs:1366-1377), and GCode emitter rendering (gcode_emit.rs:139-175) are all wired. But the module itself has not grown new capabilities. (5) The sub-claims about fallback coverage, user-visible markers, and PerimeterRegionView-only reads remain accurate. 2026-05-18: doc-side gap for packet-33 module migration and packet-32 layer-collection-builder resource now documented. See `docs/AUDIT_2026-05-18_doc_gaps.md` rows G-05 / G-06. | core-modules maintainer | TBD | Open — module remains MVP slot-filler, host infrastructure ready for richer module |
| DEV-024 | 2026-04-16 | 05_module_sdk.md §"Python Bridge (TextPostProcess tier)" / 07_implementation_status.md Workstreams | Low | AUDIT-18 showed that most sub-claims in the old open-tasks entry were stale and already belong to other rows. The only distinct engineering task that remains from this audit is `TASK-104`: the Python bridge exists and passes its own tests, but `main.rs` still routes the live pipeline through `WasmRuntimeDispatcher` for all postpass execution, so `PythonPostpassRunner` is not a supported live-path backend. Distinct fix task: either wire the Python bridge behind an explicit runtime-selection path with acceptance coverage, or update the status docs to mark it as intentionally non-live. Benchy feature work belongs to DEV-009 and finalization legacy API work belongs to DEV-013. | slicer-host maintainer | TBD | Open — Python bridge implemented but not on the live path |
| DEV-025 | 2026-04-16 | 03_wit_and_manifest.md §world-prepass.wit / 05_module_sdk.md §PrepassModule | High | **Closed 2026-05-08 — overall status: closed. Mismatch 3 closed-by-Packet-43 (43-rev1). All five mismatches resolved.** **Verified by AUDIT-19 (2026-04-16):** SDK↔WIT shape alignment is **partially broken** for both prepass segmentation stages. The structural mismatches are confirmed. **Mismatch 1 — MeshSegmentation input:** WIT `run-mesh-segmentation` provides `objects: list<object-id>` (strings only). SDK `PrepassModule::run_mesh_segmentation` declares `&[MeshObjectView]` (rich struct: `vertices`, `triangles`, `paint_layers`). Macro bridge (`lib.rs:1271-1279`) constructs `MeshObjectView` with only `object_id` populated — `vertices`, `triangles`, `paint_layers` are all `Vec::new()`. No geometry crosses. The canonical module works around this by deriving marks from `mesh_seg_mark:*` config keys rather than geometry — architecturally correct for current WIT, but makes `MeshObjectView` decorative. Status: closed by TASK-128a (Packet 43-rev1 scope) — WIT boundary operates on object-ids; SDK shells are the intended macro-path pattern; docs updated. **Mismatch 2 — PaintSegmentation input:** Same WIT pattern (`objects: list<object-id>` only). SDK `run_paint_segmentation` declares `&[PaintSegmentationObjectView]` (rich: `vertices`, `triangles`, `paint_layers`, `transform_matrix`, `participating_layer_indices`). Macro bridge (`lib.rs:1325`) passes `sdk_objects: Vec::new()` — completely empty. The trait method runs against empty input. Status: closed by TASK-128b (Packet 43-rev1 scope) — same bounded-shell pattern as mismatch 1; sdk_objects populated from object-id list. **Mismatch 3 — PaintSegmentation output drain (non-functional):** Macro arm for `PrePass::PaintSegmentation` (`lib.rs:1319-1334`) called the SDK trait method then drained **nothing** — no loop calling `paint-segmentation-output::push-paint-region`. The `sdk_output` built by the trait method was discarded. Any `#[slicer_module]`-authored PaintSegmentation module produced zero output. The canonical `paint-segmentation` module bypassed this via a **hand-written wit-guest**. Status: **closed-by-Packet-43 (TASK-130a) on 2026-05-08** — `build_prepass_world_glue` now iterates `sdk_output.regions()` and calls `_output.push_paint_region` for each region; Step 2.5 fixed macro inline-WIT scope (`use geometry.{ex-polygon, polygon, point2}` + explicit Rust use statements in segmentation_helpers); Step 2.6 fixed host `layer-idx` alias drift (canonical `type layer-idx = s32`; four non-paint view records retain u32; negative-index rejection in push_paint_region validator; i32→u32 cast in dispatch.rs harvest at IR boundary). **Missing tests:** No test verifies geometry crossing the prepass segmentation boundary. `paint_segmentation_tdd.rs` tests SDK trait in isolation; `mesh_segmentation_tdd.rs` explicitly documents geometry absence. **Fix options:** (a) add host-service mesh-query calls for modules to retrieve geometry by object-id; (b) redesign WIT to include geometry (breaking); (c) accept decorative shells and update trait docs. **Mismatch 4 — Paint value channel string-coerced (2026-05-08):** The WIT `paint-region-entry.value: string` was parsed by the host (`crates/slicer-host/src/dispatch.rs:1975-1985 parse_value`) using a four-grammar guesser falling back to `ToolIndex(0)`, silently degrading `Custom`-semantic non-numeric values. Status: closed by Packet 42 (TASK-130c) on 2026-05-08. **Mismatch 5 — SDK paint-region polygons hole-blind (2026-05-08):** `PaintRegionEntry::contour_points: Vec<[f64;2]>` cannot represent regions with interior holes; OrcaSlicer's facet-painted MMU segmentation natively produces ExPolygons[layer][extruder] (Clipper convention: CCW outer + CW inner holes — see OrcaSlicerDocumented/generated_documentation/02_core_data_structures.md:211-225). Real-world failures: SupportEnforcer ring around unpainted pillar, Material ring around unpainted core, FuzzySkin window-frame paint. Status: closed by Packet 42 (TASK-130c) on 2026-05-08. | slicer-macros + slicer-host maintainers | 2026-05-08 | Closed — overall status: closed. All five mismatches resolved; mismatch 3 closed-by-Packet-43 (TASK-130a/130b) on 2026-05-08; mismatches 4 + 5 closed by Packet 42 (TASK-130c) on 2026-05-08; mismatches 1 + 2 closed by TASK-128a/128b. 2026-05-18: confirmed; IR 4 PaintRegionIR shape in `docs/02_ir_schemas.md` already aligned. No further doc work. |
| DEV-026 | 2026-04-16 | 04_host_scheduler.md §"Phase 3 DAG Validation" / 11_operational_governance_and_acceptance_gate.md §2 "Compatibility Policy" / 12_architecture_gate_metrics.md §"Compatibility" | High | Startup validation still misses several compatibility and governance checks beyond the WIT-source/version problem tracked in DEV-014. The remaining unique gaps are: (1) `min_host_version` is parsed but never compared at startup; (2) manifest-schema validation cannot currently surface a meaningful `Schema` failure path because missing schema falls back to defaults and parse failures collapse into TOML errors; (3) memory, full-slice time, and host-call budgets from docs/12 have no runtime measurement, diagnostics, or benchmark evidence. Layer-count and region-cap bounds are already enforced. Implement the host-side `wit_world` allowlist as part of the same validation pass used to close DEV-014 rather than tracking that subtask separately here. | slicer-host maintainer | TBD | Open — host semver, manifest-schema validation, and runtime budget evidence remain incomplete |
| DEV-027 | 2026-04-16 | 01_system_architecture.md §"Z-Axis Convention" / 08_coordinate_system.md | Medium | **Closed 2026-04-21 by swarm implementation of packet 10_transform-aware-world-z.** All gaps identified in the partial state are now resolved: (1) `ObjectMesh.world_z_extent: Option<(f32, f32)>` added as a first-class derived IR field (Option A, no schema bump — v1.0.0 not released), computed and cached at `ObjectMesh` construction time in `model_loader.rs:load_model`; (2) Seven integration fixture tests created: `translated_object_z_floor_tdd` (translate(0,0,10mm) world-Z anchor ≥10.0), `rotated_object_world_extent_tdd` (rotate_x(90deg) degenerate extent), `transformed_model_world_z_tdd` (general transformed inputs), `multi_object_transform_world_z_tdd` (LCM synchronization with different transforms), `world_z_canonical_surface_tdd` (canonical surface regression lock), `non_uniform_scale_tdd` (NON_UNIFORM_SCALE_UNSUPPORTED error path), `world_z_below_floor_tdd` (WORLD_Z_BELOW_FLOOR error path); (3) `validate_non_uniform_scale` and `validate_world_z_floor` added to `model_loader.rs` with proper error codes; (4) `main.rs:153` updated to use cached `object.world_z_extent` field instead of on-demand `object_world_z_extent()` call. All 32 tests pass. `cargo build --workspace` succeeds. `cargo clippy --workspace -- -D warnings` passes clean. | slicer-host + core-modules maintainers | 2026-04-21 | Closed — world Z first-class IR field, 7 integration fixture tests, transform error paths, canonical surface regression lock |
| DEV-028 | 2026-04-16 | 05_module_sdk.md §"PrepassModule" / 03_wit_and_manifest.md §world-prepass.wit | Low | **Verified by AUDIT-22 (2026-04-16):** Historical audit notes that had claimed the layer planner duplicated planning logic in a hand-written `wit-guest` and missed the SDK-output → WIT drain are now stale — the hand-written duplicate is gone. **Evidence:** (1) `layer-planner-default/src/lib.rs:29` uses `#[slicer_module] impl PrepassModule` — canonical authoring path, same as all other 16 core modules. (2) `layer-planner-default/wit-guest/src/lib.rs:1-19` now just does `pub use layer_planner_default::DefaultLayerPlanner` with a comment explicitly stating it replaces the previous hand-written `wit_bindgen::generate!` duplicate. No planning logic lives in the wit-guest. (3) The macro's prepass-world drain for `PrePass::LayerPlanning` (lib.rs:1211-1240) fully drains `sdk_output.layers()` by calling `_output.push_layer()` for each layer proposal. (4) Host dispatches `PrePass::LayerPlanning` via `dispatch.rs:479-481` → `call_run_layer_planning` → harvests via `harvest_layer_plan_ir` (dispatch.rs:1084-1086). (5) `core_module_components_tdd.rs:28` proves layer-planner-default loads as a real component with `wasm_component = Some`. **Remaining semantic gap (not a routing mismatch):** The macro's `run_layer_planning` (lib.rs:1193-1248) calls `sdk_output.layers()` and propagates the SDK planner's fatal error (code 4: "no objects with positive height") when no objects have positive height. The old hand-written wit-guest may have emitted a fallback single first-layer proposal instead. This is a behavioral difference, not a routing gap. | slicer-macros + slicer-host maintainers | TBD | Closed — hand-written wit-guest duplicate removed; macro owns all WIT glue; sdk_output→WIT drain fully wired; remaining gap is SDK fatal error vs. fallback proposal semantics (not a routing mismatch) |
| DEV-029 | 2026-04-16 | 07_implementation_status.md §"Active Remediation Backlog" / 12_architecture_gate_metrics.md | High | AUDIT-23 established that the blocker-summary drift was real, but it did not define an independent engineering task beyond the broader planning/governance drift captured in DEV-030. The still-open blockers identified there are already owned elsewhere: DEV-004 (claim-transition enforcement), DEV-009 (Benchy feature gaps), DEV-010 (acceptance gate), and DEV-020 (dead `Noop*Runner` cleanup). Keep DEV-030 as the canonical documentation/planning entry. | slicer-host maintainer | 2026-04-16 | Closed — blocker-summary drift merged into DEV-030 |
| DEV-030 | 2026-04-16 | 07_implementation_status.md §"Active Remediation Backlog" / 11_operational_governance_and_acceptance_gate.md | Medium | The planning and governance docs no longer present a single accurate closure plan. The retired audit registry had preserved stale blocker summaries and a mostly linear MVP sequence even though live state now breaks into parallel workstreams, and `docs/07_implementation_status.md` still carried some lagging closure notes. This row is the canonical documentation/planning drift entry. The current fix order is: Phase 1 enforcement/cleanup (`DEV-004`, `DEV-020`); Phase 2 feature completion (`DEV-009`, `DEV-013`, `DEV-015`, `DEV-023`, `DEV-025`, plus DEV-024 if Python is meant to be live); Phase 3 governance closure (`DEV-010` and the remaining runtime-validation evidence in DEV-026). DEV-029 is merged here. | slicer-host maintainer | TBD | Open — planning docs still lag the real dependency graph; use this row as the canonical remediation-plan tracker |
| DEV-031 | 2026-04-26 | cli/slicer-cli/src/cmd_run.rs (test module) | Low | Two `slicer-cli::cmd_run::tests::*` unit tests assumed Unix-only environment conditions and failed on Windows or on any developer machine where `slicer-host` is on `PATH`. **Affected tests:** `find_wasm_binary_correct_path_from_module_name` (asserted `path.to_string_lossy().contains("target/slicer/cool-perimeters.wasm")`, which is false on Windows where `PathBuf::join` uses `\\` separators) and `execute_in_reaches_host_check` (asserted `Err(RunError::MissingHostBinary)` with no way to force the host probe to fail when `slicer-host` is installed on `PATH`). Closed by packet `29_slicer-cli-cmd-run-cross-platform`: path test now uses `PathBuf` equality with a `LEGACY_BROKEN_FORM:` anti-pattern comment; host-check test sets `SLICER_HOST_BIN=__slicer_host_test_marker_does_not_exist__` via a `TestEnvGuard` RAII helper. A new `check_host_binary_default_is_slicer_host` test locks down the production default. Production behavior is byte-identical when `SLICER_HOST_BIN` is unset. | slicer-cli maintainer | 2026-04-26 | Closed — cross-platform test fixes landed via packet 29_slicer-cli-cmd-run-cross-platform |
| DEV-032 | 2026-04-28 | 05_module_sdk.md §"PathOptimization" / 04_host_scheduler.md §"Layer Execution" | Low | path-optimization-module-ordering (packet 33) moved the entity-ordering algorithm from the host helper into path-optimization-default via the layer-collection-builder WIT surface introduced by packet 32. Packet 32 defined `ordered-entities` and `region-key` on `layer-collection-view`; packet 33 consumed that surface and implemented `run_path_optimization` with the same nearest-neighbor traversal and bridge-priority tiebreak as packet 18. The algorithm is bit-identical to packet 18; only the module location changed (host helper → path-optimization-default WASM module). Packet 18 remains the authoritative acceptance test source. 2026-05-18: scheduler doc updated to remove host-fallback fiction. See `docs/AUDIT_2026-05-18_doc_gaps.md` G-06. | slicer-host maintainer | 2026-04-28 | Closed — entity-ordering moved to path-optimization-default via WIT surface; algorithm bit-identical to packet 18 |
| DEV-033 | 2026-04-30 | crates/slicer-host/tests/benchy_end_to_end_tdd.rs / 07_implementation_status.md §DEV-009 | Medium | **Packet 31a-REV2 closure — pre-existing benchy TDD-red tests.** Five tests in `crates/slicer-host/tests/benchy_end_to_end_tdd.rs` were failing at packet 31a-REV2 closure and are NOT regressions introduced by this packet. They are pre-existing TDD-red gates for unimplemented features that were masked by a WASM linker error (stale WIT in `paint-segmentation/wit-guest/src/lib.rs`) now fixed in scope-fix #2 and tracked separately by packet 32. Failing tests: (1) `benchy_live_path_contains_resolved_seam_evidence_before_emit` — seam feature family; seam planning is implemented (TASK-159) but the benchy evidence assertion has not been closed; (2) `benchy_gcode_contains_balanced_retract_and_unretract_pairs` — retract feature family; retract emission is not yet implemented on the live path; (3) `benchy_gcode_contains_top_and_bottom_surface_evidence` — top-surface feature family; top/bottom surface detection is not yet implemented on the live path; (4) `benchy_prepass_seam_plan_matches_live_outer_wall_start` — seam feature family; live seam injection into outer-wall start not yet wired end-to-end; (5) `benchy_feature_evidence_failures_name_the_missing_family` — meta-test locking the names of the four missing feature families. All five belong to DEV-009. Current state: `cargo test -p slicer-host --test benchy_end_to_end_tdd`. | slicer-host maintainer | TBD | Open — pre-existing TDD-red gates; not regressions from 31a-REV2; tracked under DEV-009 |
| DEV-034 | 2026-04-30 | crates/slicer-host/src/prepass.rs / modules/core-modules/paint-segmentation/wit-guest/src/lib.rs | Low | **Packet 31a-REV2 — beyond-scope fixes applied during Step 12.** Two fixes applied during the Step 12 fixup worker exceeded the original packet design.md Code Change Surface list. **Fix 1 (paint-segmentation inline WIT):** `modules/core-modules/paint-segmentation/wit-guest/src/lib.rs` contains an embedded inline WIT world block used by `wit-bindgen`. The Step 8 codebase comment sweep correctly identified the file for comment normalization at line 211 but missed the inline WIT world block. The Step 12 fixup completed the structured WIT change: resource → record conversion, `run-support-geometry` export rename, `support-geometry-output` field. Without this fix the `paint-segmentation.wasm` artifact would fail to link at runtime due to WIT mismatch. **Fix 2 (prepass.rs phase-2 ordering):** After Steps 5/6, two latent ordering bugs surfaced when WASM modules were rebuilt and exercised end-to-end: `commit_region_mapping_builtin` was called even when LayerPlan was never committed (crashing plans whose stage list omits `PrePass::LayerPlanning`), and `commit_support_geometry_builtin` was not called after phase-1 LayerPlanning, causing `PrePass::SupportGeometry` prerequisite failures when the host built-in's preconditions were satisfied. The fix extends the 31a-REV1 ordering invariant (which only covered LayerPlanIR) to RegionMapIR and SupportGeometryIR. Both fixes are now documented in the packet 31a-REV2 design.md Code Change Surface and packet.spec.md Acceptance Criteria. | slicer-host maintainer | 2026-04-30 | Closed — both fixes documented in 31a-REV2 specs; packet closed |
| DEV-035 | 2026-05-03 | crates/slicer-host/src/layer_slice.rs:120 / crates/slicer-host/src/mesh_analysis.rs:213 | Low | **Packet 12-rev1 — any-vertex-in-polygon approximation in `classify_region_surfaces`.** `classify_region_surfaces` uses an any-vertex-in-region-polygon test (a vertex from a classified facet lies inside `region.polygons[0].contour`) as the v1 surface-region overlap heuristic. This deliberately diverges from OrcaSlicer's `LayerRegion::process_external_surfaces`, which uses `expand_merge_surfaces`, `expand_bridges_detect_orientations`, and `union_ex` (polygon-expansion and full intersection). The approximation can miss surface flags for thin regions whose polygon has no facet vertex inside it. Tightening to a proper polygon-polygon intersection was deferred to packet 36. **Closed 2026-05-05 by packet 36 (`assemble_bridge_areas` function in `mesh_analysis.rs`).** REOPENED 2026-05-05 by TASK-168 (packet 36-rev1) — closure rationale was incorrect; algorithms were heuristic stubs (bbox/centroid). Will be re-closed by packet 36-rev1 acceptance with the correct rewritten-algorithm rationale. Re-closed 2026-05-05 by packet 36-rev1 / TASK-168 — `partition_expoly_by_bridges` in `modules/core-modules/rectilinear-infill/src/lib.rs` now uses real `slicer_core::polygon_ops::{intersection, difference}` for set-difference partitioning, replacing the prior centroid-containment heuristic. | slicer-host maintainer | 2026-05-05 | Closed |
| DEV-036 | 2026-05-03 | crates/slicer-host/src/mesh_analysis.rs:213 | Low | **Packet 12-rev1 — `bridge_regions` empty in production.** `classify_region_surfaces` correctly sets `is_bridge` when fed synthetic `BridgeRegion` input in tests, but `mesh_analysis.rs:213` initialized `bridge_regions: Vec::new()` and never pushed bridge regions. All production runs saw `is_bridge = false` until packet 36 lands real bridge detection. **Closed 2026-05-05 by packet 36 (`execute_mesh_analysis_with` function in `mesh_analysis.rs` populates `bridge_regions` on the live path).** REOPENED 2026-05-05 by TASK-168 (packet 36-rev1) — closure rationale was incorrect; algorithms were heuristic stubs (bbox/centroid). Will be re-closed by packet 36-rev1 acceptance with the correct rewritten-algorithm rationale. Re-closed 2026-05-05 by packet 36-rev1 / TASK-168 — `compute_bridge_metrics` in `crates/slicer-host/src/mesh_analysis.rs` now performs real mesh half-edge adjacency analysis: down-facing cluster seed via `is_bridge_candidate` predicate, anchor-edge run identification, perpendicular-projection anchor_width_mm, longest-anchor-run-derived bridge_direction_deg, and facet-projection union for xy_footprint. | slicer-host maintainer | 2026-05-05 | Closed |
| DEV-037 | 2026-05-03 | wit/deps/ir-types.wit:72-74 / crates/slicer-macros/src/lib.rs (`__slicer_adapt_slice_regions`) / modules/core-modules/gyroid-infill/src/lib.rs | Medium | **Packet 12-rev1 — scope expansion: WIT contract extended.** Packet `12-rev1_external-surface-classification-at-slice` design.md declared `wit/` out of scope, but discovery during implementation revealed the `slice-region-view` WIT resource in `wit/deps/ir-types.wit` was missing the three flag accessor methods (`is-top-surface`, `is-bottom-surface`, `is-bridge`) entirely; the host's `SlicedRegion` record-level flags could not reach the guest without these. Three accessor methods were added at lines 72-74 of `wit/deps/ir-types.wit`, matching setter wiring was added in `crates/slicer-macros/src/lib.rs::__slicer_adapt_slice_regions`, and propagation was added to `modules/core-modules/gyroid-infill/src/lib.rs`. This triggers a rebuild of all 19 WASM core modules (also originally out of scope). 2026-05-18: claim catalog and DAG validation pass 2 documented. See `docs/AUDIT_2026-05-18_doc_gaps.md` G-08. | slicer-host maintainer | 2026-05-03 | Closed — WIT accessors added, macro adapter and gyroid-infill wired; WASM rebuild required |
| DEV-038 | 2026-05-03 | crates/slicer-host/tests/benchy_end_to_end_tdd.rs (`benchy_feature_evidence_failures_name_the_missing_family`) | Low | **Packet 12-rev1 — latent retract-evidence test fix.** The retract sub-assertion in `benchy_feature_evidence_failures_name_the_missing_family` was changed from `M207`/`M208` (firmware-mode retract pattern) to `G1 E-` / `G1 E<pos> F` (default GCode-mode pattern) to match the `retract_mode = "GCode"` setting in `resources/test_config/benchy-tree-support.json`. The prior assertion was a latent test bug from packet 34's retract-mode work; the `top_surface`-family assertion targeted by packet 12-rev1's AC is unchanged. 2026-05-18: claim catalog and DAG validation pass 2 documented. See `docs/AUDIT_2026-05-18_doc_gaps.md` G-08. | slicer-host maintainer | 2026-05-03 | Closed — retract sub-assertion pattern corrected; no production behavior change |
| DEV-039 | 2026-05-04 | crates/slicer-host/src/layer_slice.rs::execute_layer_slice | Low | **Packet 35 — bounding-box fallback for empty slice polygons.** When `slice_mesh_ex` yields zero polygons for a layer (e.g., perfectly flat top/bottom triangles skipped by the slicer), `execute_layer_slice` synthesizes a bounding-box polygon from the `ObjectMesh` vertex XY extents for the surface-classification XY-containment check. Without this fallback, surface-classified facets lying entirely above/below a flat-top/bottom layer would fail the XY-containment guard and `is_top_surface` / `is_bottom_surface` would never fire on geometry that warrants them. This is packet-local; not part of the original packet 35 design but load-bearing for `multi_layer_thickness_tdd` correctness. | slicer-host maintainer | TBD | Open — packet-local fallback; future packets 36/37 inherit the same XY-containment guard |
| DEV-040 | 2026-05-04 | crates/slicer-host/src/dispatch.rs §"RegionPlan builder" / 04_host_scheduler.md §"PrePass lifecycle" | Medium | **Packet 35 surfaced — user config ignored in RegionMapIR construction.** The `RegionMapIR` builder in `dispatch.rs` (specifically the path constructing `RegionPlan` entries during `PrePass::RegionMapping`) unconditionally calls `ResolvedConfig::default()` instead of resolving per-object/per-region config from CLI JSON config or print profile. This means `top_shell_layers`, `bottom_shell_layers`, and every other `ResolvedConfig` field are always hardcoded defaults (3/3) end-to-end through the CLI binary—even when the user passes `--config '{"top_shell_layers": 4}'` or similar. Packet 35's helper/test-fixture tests pass because they hand-construct `RegionMapIR` with explicit values; packet 35's binary E2E test (`benchy_multi_layer_top_bottom_evidence`) was strengthened to bypass the binary path precisely BECAUSE config propagation is broken. The gap is architectural (PRODUCER side: build IR from CLI config) and falls outside packet 35's scope (CONSUMER side: use IR in `execute_layer_slice`). Packets 36 (bridge detector), 37 (per-claim module selection), and any future packet wanting user-tunable behavior will hit this. **Closed 2026-05-04 by packet 35a; resolve_global_config + per-object overlay stamps RegionPlan.config end-to-end via PrePass::RegionMapping.** 2026-05-18: per-object `object_config:<id>:<key>` namespace documented in `docs/02_ir_schemas.md` IR 5 and cross-referenced in `docs/03_wit_and_manifest.md`. See `docs/AUDIT_2026-05-18_doc_gaps.md` G-07. | slicer-host maintainer | 2026-05-04 | Closed |
| DEV-041 | 2026-05-08 | crates/slicer-macros/src/lib.rs (finalization-world drain-back loop) / wit/world-finalization.wit | Medium | **Packet 40 — `merge_ops` drain-back not wired across WIT boundary.** The macro-generated finalization-world drain-back loop in `crates/slicer-macros/src/lib.rs` replays `priority_pushes` from the guest-side builder back to the host across the WIT boundary, but does NOT replay the three `MergeOp` variants (`modify_entity`, `sort_layer_by`, `insert_synthetic_layer_after`). A WASM module that calls those three new methods will silently no-op: the guest-side builder records the call into `merge_ops`, but the drain-back loop discards the recordings before the host applies them. SDK-side (in-process) tests cover the methods correctly via the closure path — `apply_to` operates directly on the `Vec<MergeOp>` in memory — and the host merge handles empty `merge_ops` without error. The gap is exclusive to the WIT boundary for guest WASM modules. No WASM module shipped in packet 40 exercises these methods (`top-surface-ironing` only uses `push_entity_with_priority`, which IS drained); the gap is dormant until a future packet introduces a WASM module that consumes `modify_entity` / `sort_layer_by` / `insert_synthetic_layer_after`. Mitigation: that future packet must extend `build_finalization_world_glue` to drain the three remaining MergeOp variants, plus add WIT method shapes that can carry an opaque closure or a serialized op marker (the current SDK `Box<dyn FnOnce>` shape does not cross WIT). | slicer-macros maintainer | TBD | Closed 2026-05-08 — Packet 41 (`41_finalization-mutation-enum-refactor` / TASK-172) refactored `FinalizationOutputBuilder` mutation methods to serializable enums (`EntityMutation`, `SortKey`, `SyntheticLayerData`); the `slicer-macros` `run_finalization` drain-back now forwards `merge_ops` via WIT; AC-5 (`modify_entity_round_trips_through_wit`) and NEG-3 (`modify_entity_unknown_id_round_trips_error`) confirm round-trip behavior end-to-end with a WASM test guest. 2026-05-18: serialisable mutation enums and builder methods documented in `docs/03_wit_and_manifest.md`, `docs/05_module_sdk.md`. See `docs/AUDIT_2026-05-18_doc_gaps.md` G-03 / G-04. |
| DEV-042 | 2026-05-06 | wit/deps/ir-types.wit §`slice-region-view` / crates/slicer-host/src/dispatch.rs (`dispatch_layer_call`) / crates/slicer-host/src/execution_plan.rs (`CompiledModule.claims`) / .ralph/specs/37_fill-role-claims/design.md §"Architecture Constraints — No WIT signature changes" / §"Out-of-Bounds Files" | Low | **Packet 37 — `slice-region-view.held-claims` accessor added across the WIT boundary, with required dispatch and execution-plan plumbing.** Packet 37 design.md declared `wit/` out of scope ("No WIT signature changes — claims live in scheduler validation") and listed `crates/slicer-host/src/dispatch.rs` as an explicitly out-of-bounds file, but the held-claim set produced by the host's resolver must reach guest infill modules so they can self-filter at emit time. There is no other in-place mechanism: `SliceRegionData` already carried `held_claims: Vec<String>` on the host record, but without a corresponding WIT method on `slice-region-view`, the guest could not read it; and without a per-call resolver invocation in dispatch, the held-claim set for a given `(module, region)` could not be computed at the right point. The minimum cohesive change is therefore three coordinated edits: (1) a single accessor `held-claims: func() -> list<string>` added to the `slice-region-view` resource at `wit/deps/ir-types.wit:80-83`, mirrored on `SliceRegionView` in `crates/slicer-sdk/src/views.rs:276` and consumed via `SliceRegionView::should_emit(role)` (line 297) in the three core infill modules; (2) `crates/slicer-host/src/dispatch.rs:343` (`dispatch_layer_call`) extended to call `resolve_held_claims` for the dispatched module against the resolved holders for the region, populating `SliceRegionData.held_claims`; (3) `crates/slicer-host/src/execution_plan.rs:651` extended with `CompiledModule.claims` so each module's declared claim set is captured at plan-compilation time and available to dispatch without re-parsing the manifest. All three edits are additive: no existing WIT field or method is modified, no existing `dispatch_layer_call` parameter is removed, and module-side filtering is gated by the empty-list = "holds all four" convention so default behavior is unchanged. Triggers a rebuild of the WASM core modules. | slicer-host maintainer | 2026-05-06 | Closed — accessor added; dispatch and execution-plan plumbing wired; SDK helper `should_emit` centralizes the convention; three core infill modules consume it; DEV-042 registered |
| DEV-043 | 2026-05-08 | wit/world-finalization.wit / crates/slicer-host/src/wit_host.rs §`HostFinalizationOutputBuilder` impl / crates/slicer-macros/src/lib.rs / .ralph/specs/40_finalization-mutation-builder/design.md §"Out-of-Bounds Files" | Medium | **Packet 40 — WIT scope expansion + DEV-047 item (c) carry-forward closure.** Packet 40's `design.md` declared all `wit/` files, `crates/slicer-host/src/wit_host.rs`, and the WIT mirror of `FinalizationOutputBuilder` out-of-bounds, with the gate: *"if Step 0 finds a WIT mirror, packet escalates to user before proceeding."* Step 0 found that `FinalizationOutputBuilder` IS exposed across the WIT boundary as a guest-callable resource, and `top-surface-ironing` (a WASM guest) needs to call the new `push_entity_with_priority(..., Ironing.default_priority())` to ship the substantive print-quality fix end-to-end. User explicitly authorized the scope expansion (Swarm escalation prompt at Step 0). Files modified that do not appear in `design.md`'s in-scope list: (1) `wit/world-finalization.wit` — added the four new method shapes on the `finalization-output-builder` resource; (2) `crates/slicer-host/src/wit_host.rs` — added the corresponding `impl HostFinalizationOutputBuilder for HostExecutionContext` methods at `:4602-4707`; (3) `crates/slicer-macros/src/lib.rs` — extended the finalization-world glue to plumb `priority_pushes` (the `merge_ops` drain-back gap is tracked separately as DEV-041). Closure rationale: expansion is the cleanest path to ship the AC-6 print-quality fix; SDK-side `FinalizationOutputBuilder` is unchanged in shape; host-side `apply_to` semantics are identical regardless of whether ops arrived via in-process closures or via the WIT drain. **DEV-047 item (c) carry-forward closure (combined here):** packet 40 also closed the deferred migration noted in DEV-047 item (c) (formerly labeled DEV-039 in pre-renumbering refs). `modules/core-modules/skirt-brim/src/lib.rs::run_finalization` now calls `output.push_entity_to_layer(layer_index, path, region_key)` (legacy alias → priority 0 → preserves frame-around-object prepend semantics; AC-8 verifies). `modules/core-modules/wipe-tower/src/lib.rs::run_finalization` now calls `output.push_entity_with_priority(layer_index, path, region_key, ExtrusionRole::WipeTower.default_priority())`. Both modules' live WASM paths no longer construct `PrintEntity { entity_id: 0, ... }` literals or instantiate a local `LayerEntityIdGen`; ID stamping is owned by the host merge sequence. Legacy `process()` paths (test-only, not on the live WASM dispatch route) still construct `PrintEntity { entity_id: 0, ... }` literals; those sites carry `TODO(packet-41/DEV-047)` breadcrumbs and will be retired by packet 41. | slicer-host + slicer-macros + core-modules maintainers | 2026-05-08 | Closed — WIT scope expansion documented; DEV-047 item (c) live-path carry-forward closed; legacy `process()` test-only sites flagged for packet 41 |
| DEV-044 | 2026-05-10 | crates/slicer-host/src/model_loader.rs §`load_3mf` / crates/slicer-host/src/cli.rs §`Run` / 01_system_architecture.md §"Host Mesh Loader" / 02_ir_schemas.md §`FacetPaintData` | High | **2026-05-10 spec-review of Packet 43-rev1 — Paint input surface stubbed.** PaintSegmentation is contract-green at the WIT/IR layer (DEV-025 closed 2026-05-08) but has NO user-reachable input surface on the live binary path. `load_3mf` at `crates/slicer-host/src/model_loader.rs:280-352` (`parse_3mf_model_xml`) parses only `<vertex>` / `<triangle>` XML and silently discards every Bambu/Orca paint metadata namespace (`custom_supports`, `paint_color`, `support_blocker`, `seam_painting`). `model_loader.rs:150` unconditionally sets `ObjectMesh::paint_data: None`. Neither `slicer-host` (production CLI, `crates/slicer-host/src/cli.rs:18-43`) nor the dev `slicer-cli` (`cli/slicer-cli/src/main.rs:18-60`) exposes any paint flag. As a result every code path downstream of `paint_data` — `paint_segmentation.rs:70-130`, `wit_host.rs:2498/2653`, the layer-world `paint-region-layer-view` at `wit/deps/ir-types.wit:194-218` — operates on always-`None` input on the live binary path. The committed implementation of `PrePass::PaintSegmentation` is unfalsifiable end-to-end. This is the class of "implemented at the contract, unfalsifiable end-to-end" gap the Acceptance Gate (`docs/11_operational_governance_and_acceptance_gate.md:77-86`) was too lenient to catch. **Failing TDD-RED tests committed 2026-05-10:** `crates/slicer-host/tests/benchy_painted_e2e_tdd.rs` (2 tests: `painted_3mf_fixture_is_committed`, `painted_benchy_3mf_reaches_paint_segmentation`). Closure planned via Packet 50 (`paint-input-3mf-ingestion`) — extend `parse_3mf_model_xml` to honor paint metadata namespaces and commit `resources/benchy_painted.3mf` fixture. Scope intentionally `.3mf` only; STL+sidecar JSON ingestion is rejected as YAGNI. 2026-05-18: paint-input IR already documented in `docs/02_ir_schemas.md` IR 0 / IR 4 / IR 5. Per-paint-region override namespace `paint_config:<semantic>:<key>` confirmed documented at IR 5 and `docs/03_wit_and_manifest.md`. See `docs/AUDIT_2026-05-18_doc_gaps.md`. | slicer-host maintainer | TBD | Closed — Packet 50, 2026-05-11 |
| DEV-045 | 2026-05-10 | crates/slicer-host/src/region_mapping.rs / crates/slicer-host/src/config_resolution.rs / crates/slicer-host/src/prepass.rs §`execute_prepass_with_builtins_configured` / crates/slicer-ir/src/slice_ir.rs §`RegionPlan` / 01_system_architecture.md §"RegionMapping" | High | **2026-05-10 spec-review of Packet 43-rev1 — RegionMap paint-blind.** The host built-in `crates/slicer-host/src/region_mapping.rs:103-248` contains zero occurrences of the tokens "paint*" / "semantic". `RegionPlan` (`crates/slicer-ir/src/slice_ir.rs:1028-1033`) has no paint-semantic dimension; `RegionKey` (`:1006-1015`) keys on `(global_layer_index, object_id, region_id)` only. `crates/slicer-host/src/config_resolution.rs` (closed DEV-040 2026-05-04) recognises only `object_config:<id>:<key>` (line 84, 195); no `paint_config:<semantic>:<key>` namespace. Unknown config keys silently fall through to `cfg.extensions` (`:169-171`, `:280`) — no diagnostic. Consequence: `PaintSemantic::Custom("fuzzy_skin")` and similar values cross IR via `PaintRegionLayerView` (`wit/deps/ir-types.wit:194-218`) but cannot bind to per-region `ResolvedConfig` overrides. Paint is useful today only for tool/material differentiation via `ActiveRegion.tool_index` (`slice_ir.rs:289-291`); arbitrary semantics like "fuzzy_skin" or "ironing" have no settings-binding path on the live host scheduler. This violates the RegionMap responsibility stated in `docs/01_system_architecture.md:107-114` ("decides modules + pre-filtered config + active claims per (layer, object, region)") and the `PaintSemantic::Custom(String)` documented IR transport contract at `docs/02_ir_schemas.md:103-122`. **Failing TDD-RED test committed 2026-05-10:** `crates/slicer-host/tests/benchy_painted_overrides_e2e_tdd.rs` (`paint_config_override_visibly_differs_gcode`). Closed via Packet 51 (`paint-semantic-region-overrides`) — `paint_config:` namespace added to `config_resolution.rs`; `RegionPlan` extended with `paint_overrides: BTreeMap<PaintSemantic, ResolvedConfig>` (additive; `RegionMapIR` schema bumped 1.0.0 → 1.1.0); `region_mapping.rs` reads `PaintRegionIR` and stamps per-semantic overlays into `RegionPlan.config`. 2026-05-18: paint-input IR already documented in `docs/02_ir_schemas.md` IR 0 / IR 4 / IR 5. Per-paint-region override namespace `paint_config:<semantic>:<key>` confirmed documented at IR 5 and `docs/03_wit_and_manifest.md`. See `docs/AUDIT_2026-05-18_doc_gaps.md`. **Reopened 2026-05-27 during handoff review:** the host paint-segmentation fallback in `prepass.rs` ran AFTER region-mapping (both the unreachable-in-production fast-path at `:486-510` and the live phase-2 path), silently dropping every `paint_config:<semantic>:<key>` override on the live path despite the packet-51 region-mapping wiring being correct. **Re-closed 2026-05-27** — fast-path removed (see DEV-061); paint-seg fallback moved into canonical `STAGE_ORDER` position immediately before the sole region-mapping built-in per `docs/04_host_scheduler.md:111-119`; regression `paint_semantic_override_propagates_through_full_prepass` in `tests/prepass_paint_semantic_override_ordering_tdd.rs`. | slicer-host maintainer | 2026-05-12 | Closed — Packet 51, 2026-05-12; reopened 2026-05-27 (STAGE_ORDER violation); re-closed 2026-05-27 (Commit 1 of handoff plan) |
| DEV-046 | 2026-05-13 | crates/slicer-host/src/model_loader.rs §`parse_3mf_model_xml` / §`load_model` | Medium | **Closed 2026-05-13.** 3MF loader now applies `<build>/<item>` transform and nested `<component>` transform to mesh vertices at load time. For `resources/benchy_painted.3mf` the effective transform `"1 0 0 0 1 0 0 0 1 110 100 24"` shifts mesh vertices by (110, 100, 24) mm; Z-range is now [0, 48] mm instead of [-24, 24]. Multiple `<item>` elements produce multiple `ObjectMesh` instances. Malformed transforms (wrong float count) produce `ModelLoadError::ThreeMfParse`. Negative-determinant (mirror) transforms are applied as-is. Paint stroke vertices are transformed alongside mesh vertices; facet-level paint data indices remain valid. | slicer-host maintainer | 2026-05-13 | Closed |
| DEV-047 | 2026-05-07 | wit/world-finalization.wit / crates/slicer-macros/src/lib.rs / crates/slicer-host/src/wit_host.rs / modules/core-modules/wipe-tower/src/lib.rs / modules/core-modules/skirt-brim/src/lib.rs / docs/02_ir_schemas.md §SliceIR versioning rule | Medium | **Packet 39 — stable-entity-ids breaking schema bump + WIT-boundary scope expansion.** Packet `39_stable-entity-ids` bumped `SliceIR.schema_version` from 1.2.0 to 2.0.0 (major). Breaking: `TravelMove.after_entity_index: u32` replaced by `TravelMove.entity_id: u64` (rename + type change per docs/02_ir_schemas.md:889 versioning rule). Additive: `PrintEntity` gained `entity_id: u64`. Impact: internal-only — no external persisted IR consumers, but backward-incompatible with any pre-Packet-39 serialized IR snapshots. **WIT-boundary scope expansion (logged retroactively):** the original packet's Step 0 WIT-exposure check was scoped only to `TravelMove`/`entity_idx` and missed `PrintEntity`'s exposure via `print-entity-view`. Three groups of out-of-bounds files were edited as structural necessities: (a) `wit/world-finalization.wit` and `crates/slicer-macros/src/lib.rs` added `entity-id: u64` to `print-entity-view` and the macro-generated PrintEntity construction; (b) `crates/slicer-host/src/wit_host.rs` host marshaller populated the new field at the boundary; (c) `modules/core-modules/wipe-tower/src/lib.rs` and `modules/core-modules/skirt-brim/src/lib.rs` stamped `entity_id` at their direct `PrintEntity` construction sites (these modules bypass `FinalizationOutputBuilder`). Items (a) and (b) closed by Packet 39. Item (c) live-path closed by Packet 40 (see DEV-043) — both modules' `run_finalization` now use the builder API (`push_entity_to_layer` / `push_entity_with_priority`). Legacy `process()` paths (test-only, not on live WASM dispatch) still construct `PrintEntity { entity_id: 0, ... }` literals; carry `TODO(packet-41/DEV-047)` breadcrumbs. **Renumbered 2026-05-13 from DEV-039**: this entry was originally registered as DEV-039 in `docs/14_deviation_audit_history.md` but the ID was already taken in this canonical log (DEV-039 / 2026-05-04 packet-35 bbox fallback for empty slice polygons, still Open). Renumbered to DEV-047 to resolve the collision; cross-refs in `docs/14_deviation_audit_history.md`, packets 40/41 specs, and module TODO breadcrumbs updated. 2026-05-18: IR 10 schema bumped to 2.0.0 in `docs/02_ir_schemas.md`; stable-ID contract documented across IR, SDK, and architecture docs. See `docs/AUDIT_2026-05-18_doc_gaps.md` G-02. | slicer-host + slicer-macros + core-modules maintainers | 2026-05-08 | Closed — items (a)(b)(c-live) closed by Packets 39 and 40 (cross-ref DEV-043); legacy `process()` test-only sites flagged for packet 41 retirement |
| DEV-048 | 2026-05-13 | docs/05_module_sdk.md § Layer Stage Module Surface Rejections / docs/07_implementation_status.md TASK-152c | Low | **TASK-152c superseded by TASK-152d; cooling now supported on finalization surface (not path-optimization).** Packet 53 (`53_gcode-cooling-fan-emission`) moves cooling fan control from the rejected `Layer::PathOptimization` surface to the live `PostPass::LayerFinalization` surface via a dedicated cooling module. Old rejection wording removed from docs/05; TASK-152c marked superseded in docs/07. 2026-05-18: `PartCooling` finalization role documented in `docs/01_system_architecture.md`. See `docs/AUDIT_2026-05-18_doc_gaps.md` G-13. | slicer-host maintainer | TBD | Closed |
| DEV-049 | 2026-05-13 | docs/07_implementation_status.md § DEV-009 | High | **Cooling subset of DEV-009 (Benchy Phase H output partially correct) is now resolved via packet 53.** The live Benchy path now emits M106/M107 cooling commands from a `PostPass::LayerFinalization` cooling module. This closes the cooling/fan-speed gap in the live G-code path. Remaining DEV-009 gaps (seam placement, support structures, etc.) are tracked separately. | slicer-host + core-modules maintainers | TBD | In Progress |
| DEV-050 | 2026-05-14 | crates/slicer-host/src/model_loader_sidecar.rs §`parse_part_subtype` | Low | **Packet 56 — Partial subtype coverage; unknown subtypes downgrade to `NormalPart` with `log::warn!`.** The sidecar parser (`parse_3mf_sidecar` in `model_loader_sidecar.rs`) recognizes exactly five subtypes: `normal_part`, `modifier_part`, `negative_part`, `support_enforcer`, `support_blocker`. Any unrecognized `subtype=` attribute value in `<part subtype="...">` is silently downgraded to `PartSubtype::NormalPart` and a `log::warn!` is emitted at target `slicer_host::model_loader::sidecar` containing the unrecognized string and the substring `"downgrading to normal_part"`. This is intentional: hand-edited sidecars or future OrcaSlicer subtype additions should not break the loader. Packet 56b's `resolve_object` branching and Packet 56c's downstream consumers will treat `NormalPart` as a safe default for unrecognized subtypes. (Note: originally planned as DEV-047; renumbered to DEV-050 because DEV-047 was already claimed by packet 39.) 2026-05-18: 3MF sidecar metadata already documented at `docs/02_ir_schemas.md` IR 0. No further doc work. | slicer-host maintainer | 2026-05-14 | Closed — Packet 56, 2026-05-14 |
| DEV-051 | 2026-05-14 | crates/slicer-host/src/model_loader_sidecar.rs §`parse_3mf_sidecar` | Low | **Packet 56 — Missing or malformed `Metadata/model_settings.config` is non-fatal.** The sidecar parser handles two absence cases: (1) missing entry — `zip::ZipArchive::by_name("Metadata/model_settings.config")` returns `Err` → `parse_3mf_sidecar` returns `HashMap::new()` with NO warning (absence is the default for fixtures without sidecars, e.g., `resources/benchy_painted.3mf`); (2) malformed XML — `quick_xml::Reader` returns `Err` during the parse loop → returns `HashMap::new()` AND emits `log::warn!` at target `slicer_host::model_loader::sidecar` containing `"treating all parts as normal_part"`. In both cases `load_model` does NOT return `Err`; the load continues with an empty sidecar map, which is equivalent to all parts being `NormalPart`. (Note: originally planned as DEV-049; renumbered to DEV-051 because DEV-049 was already claimed by packet 53.) 2026-05-18: 3MF sidecar metadata already documented at `docs/02_ir_schemas.md` IR 0. No further doc work. | slicer-host maintainer | 2026-05-14 | Closed — Packet 56, 2026-05-14 |
| DEV-052 | 2026-05-14 | crates/slicer-host/src/model_loader.rs §`resolve_object` | Low | **Packet 56b — Paint data on non-`NormalPart` rows (modifier, negative, support enforcer, support blocker) is dropped at load time with `log::warn!`.** No consumer needs paint on a modifier; preserving it wastes IR memory and risks double-counting. When `resolve_object` routes a part into `ObjectMesh.modifier_volumes`, any accumulated `paint_data` for that part is dropped and a `log::warn!` is emitted at the existing log target containing the substring `"paint data on non-normal part dropped"` and the part id. 2026-05-18: `ModifierVolume` / `ModifierScope` documented at `docs/02_ir_schemas.md` IR 0; routing rules added to `docs/04_host_scheduler.md`. See `docs/AUDIT_2026-05-18_doc_gaps.md` G-16. | slicer-host maintainer | 2026-05-14 | Closed — Packet 56b, 2026-05-14 |
| DEV-054 | 2026-05-18 | wit/world-finalization.wit §`finalization-output-builder` / crates/slicer-sdk/src/traits.rs §`FinalizationOutputBuilder` / crates/slicer-host/src/wit_host.rs §`HostFinalizationOutputBuilder` / crates/slicer-host/src/gcode_emit.rs §wipe-tower marker / crates/slicer-host/src/postpass.rs §`PostpassError` / modules/core-modules/wipe-tower/src/lib.rs §`run_finalization` / crates/slicer-ir/src/resolved_config.rs §`declare_resolved_config!` | Medium | **Packet 58 — G-code Toolchange Purge Integration: closed the integration gap between packets 17, 19, 11, 15, 34 so multi-material G-code emits `;TYPE:Prime tower` blocks bracketing every `T<n>` tool change with retract → travel → prime → wipe entities.** (1) **G-code marker**: `;TYPE:Wipe tower` (ModularSlicer-only spelling) replaced by `;TYPE:Prime tower` (OrcaSlicer canonical, `ExtrusionEntity.cpp:648`). (2) **G-code structure**: when `wipe_tower_enabled=true`, every `T<n>` line is preceded by a retract entity and followed by a travel + prime + wipe-block. Single-material output (`wipe_tower_enabled=false`, default) is unchanged. (3) **WIT**: `finalization-output-builder` gained three additive methods (`insert-entity-at`, `set-entity-order`, `get-ordered-entities`) mirroring PathOptimization's `layer-collection-builder` surface. (4) **SDK**: `FinalizationOutputBuilder::apply_to` now remaps `ToolChange.after_entity_index` and `ZHop.after_entity_index` on insert/permute. (5) **Host**: host-side bindings for the three new methods added to `wit_host.rs`; `gcode_emit.rs:271` flipped to `";TYPE:Prime tower"`; defensive `MissingToolchangePurge` guard added (gated on `wipe_tower_enabled=true`). (6) **Postpass**: `PostpassError::MissingToolchangePurge { layer_index, tool_change_index }` added (additive variant). (7) **Module**: `generate_purge_paths` emits retract/travel/walls/prime entities; `run_finalization` uses `insert_entity_at` to bracket every `T<n>`; reads `bed_shape` for placement validation. (8) ...
| DEV-053 | 2026-05-15 | modules/core-modules/fuzzy-skin/src/lib.rs §`apply_to_all` / §`point_distance` config key lookups | Low | **Packet 56b — Config key normalization (kebab→snake) in fuzzy-skin module required for modifier-part stamp to take effect.** `packet.spec.md` originally listed `modules/core-modules/fuzzy-skin/src/lib.rs` as out-of-scope. During implementation the module was found to read config keys using kebab-case (`"apply-to-all"`, `"point-distance"`) while the host stamps and the manifest declare snake_case keys (`"apply_to_all"`, `"point_distance"`) per CLAUDE.md convention. Without this fix the modifier-part `apply_to_all = true` stamp would be silently ignored by the module. Two-line key normalization; no behavioral change for correct callers. 2026-05-18: `ModifierVolume` / `ModifierScope` documented at `docs/02_ir_schemas.md` IR 0; routing rules added to `docs/04_host_scheduler.md`. See `docs/AUDIT_2026-05-18_doc_gaps.md` G-16. | core-modules maintainer | 2026-05-15 | Closed — Packet 56b, 2026-05-15 |
| DEV-058 | 2026-05-25 | crates/slicer-host/src/support_geometry.rs §`collect_polygons_at_z` | Low | **Plan v4 (slicing promotion, Commit 4) — Upper-bracket layer polygons for non-aligned Z queries.** `PrePass::SupportGeometry` was previously stubbed (`Vec::new()`). It now binary-searches `LayerPlanIR.global_layers` for the slice index matching the requested Z; on a non-aligned Z (between two adjacent model layers) it returns the **upper-bracket** slice's polygons rather than interpolating or unioning the two bracketing slices. Rationale: upper-only is conservative for support pillars (catches the overhang above the gap); polygon interpolation is OrcaSlicer-faithful but adds substantial complexity. Documented as a quality trade-off; revisit if real-world support adherence regressions surface. | slicer-host maintainer | 2026-05-25 | Closed — Plan v4 Commit 4 |
| DEV-059 | 2026-05-25 | crates/slicer-host/src/support_geometry.rs §`build_emit_schedule` / crates/slicer-ir/src/resolved_config.rs §`support_layer_height_mm` | Low | **Plan v4 / Block A2 — Per-object `support_layer_height_mm` runtime consumption wired.** `DEFAULT_SUPPORT_LAYER_HEIGHT_MM` removed. `build_emit_schedule()` precomputes per-object emit schedules from `region.resolved_config.support_layer_height_mm`: `0.0` → emit every model layer; positive → accumulate `effective_layer_height` and emit when ≥ target. Main loop replaced with per-object schedule lookup + per-object `support_layer_index` counter. Validation (`validate_support_layer_heights`), field declaration (`resolved_config.rs:498`), and runtime consumption all wired; per-object semantics now match `resolved_config.rs:493` doc-comment. Regression tests: `build_emit_schedule_two_objects_per_object_semantics` (unit) and `per_object_support_layer_height_is_honored_by_execute_support_geometry` (integration). | slicer-host maintainer | 2026-05-27 | Closed — Block A2, 2026-05-27 |
| DEV-060 | 2026-05-27 | dispatch.rs prepass match / wit/world-prepass.wit / test-guests/sdk-prepass-paintseg-guest/ / crates/slicer-macros | Low | WASM paint-segmentation dispatch path removed. Paint-seg migrated to host builtin in packet-64 (commit e6369eb); WIT export, records, host plumbing, test guest, and macro derivation now deleted to prevent reintroduction of the dual path. Re-introduce only with deliberate ADR. | slicer-host maintainer | 2026-05-27 | Closed — design decision documented |
| DEV-062 | 2026-05-27 | crates/slicer-host/src/support_geometry.rs §`build_emit_schedule`, §`execute_support_geometry` / modules/core-modules/support-planner/src/lib.rs / crates/slicer-ir/src/slice_ir.rs §`SupportGeometryIR.entries` | High (latent in production for multi-region objects) | **Partial close (Q4+Q5b) — twin per-region bugs collapsed for multi-region-per-layer objects' support cadence and key indexing.** Pre-fix: (1) `build_emit_schedule` advanced the per-object accumulator per region visit, over-emitting support boundaries when an object had ≥2 active regions on one layer (e.g., 6-layer × 2-region fixture emitted at every model layer instead of at every 2nd). (2) `execute_support_geometry`'s `support_layer_index` counter was per-region-visit, producing `global_support_layer_index` values that disagreed between sibling regions on the same emit-layer, contradicting the documented per-(layer, object, region) entry-key semantics at `crates/slicer-ir/src/slice_ir.rs §SupportGeometryIR.entries`. Both fixes ship in the same commit: schedule accumulator collapses per-(object, layer) with a `debug_assert!` enforcing inter-region agreement on `support_layer_height_mm`; counter shares a per-(object, emit-layer) idx across sibling regions via a layer-local scratch map. Regression `per_layer_cadence_for_multi_region_object` in `crates/slicer-host/tests/support_geometry_slice_consumption_tdd.rs` (asserts 6 entries, max idx=2, regions {0,1} per idx, and non-empty polygon consumption). **Open — Q5c (counter ≠ model-layer-index) deferred:** `support-planner/src/lib.rs:211` still reads `entry.global_support_layer_index` as a model-layer index into `collision_cache: Vec<LayerCollisionCache>` (sized by `layer_plan.layers.len()`). Post-Q5b that field is a per-(object, emit-layer) counter (0,1,2,...), NOT the model-layer index, so multi-emit-layer prints with `support_layer_height_mm > 0` still place tree-support branches against the wrong layer's collision geometry. Q5c (the producer↔consumer index alignment) is the only remaining sub-bug; closed by DEV-063. The `debug_assert!` introduced here is permanent (see DEV-064 — per-region `support_layer_height_mm` was evaluated and explicitly scoped out). | slicer-host maintainer | 2026-05-27 | Closed — Q4+Q5b in main-PR Commit 4 (`c63bb4d`), 2026-05-27; Q5c in DEV-063, 2026-05-27 |
| DEV-061 | 2026-05-27 | crates/slicer-host/src/prepass.rs §pre-99136b5 fast-path region-mapping branch | Low | **Pre-99136b5 fast-path region-mapping branch removed for STAGE_ORDER alignment.** Commit `99136b5` introduced a "fast-path" region-mapping invocation at `prepass.rs:486-510` that fired when callers pre-committed `LayerPlanIR` to the `Blackboard` before `execute_prepass_with_builtins_configured`. No production caller does (`pipeline.rs:175, 245` never pre-commit). The branch silently violated canonical `STAGE_ORDER` from `docs/04_host_scheduler.md:111-119` (running `RegionMapping` before `PaintSegmentation`) and inverted the guard-based fallback contract at `docs/04_host_scheduler.md:704` for test callers that pre-committed `LayerPlanIR`. Removed; the single canonical region-mapping position now lives post-`PaintSegmentation` fallback. Visibility of `execute_prepass_with_builtins_configured` widened from `pub(crate)` to `pub` so the integration regression at `tests/prepass_paint_semantic_override_ordering_tdd.rs` can drive the full prepass dispatcher directly. | slicer-host maintainer | 2026-05-27 | Closed — Commit 1 of handoff plan, 2026-05-27 |
| DEV-064 | 2026-05-27 | crates/slicer-host/src/support_geometry.rs §`build_emit_schedule` (`debug_assert!`) / crates/slicer-ir/src/resolved_config.rs §`support_layer_height_mm` / docs/15_config_keys_reference.md `support_layer_height_mm` row | Low (intentional scope decision) | **Per-region `support_layer_height_mm` cadence explicitly scoped out.** A follow-up worktree proposed lifting `support_layer_height_mm` from per-object to per-region, motivated by OrcaSlicer parity for modifier-volume-driven support-density variation. After review the user judged the implementation cost (schedule key migration to `(object_id, region_id)`, validator-shape change, downstream test fan-out, additional E2E coverage) disproportionate to the feature's value. `support_layer_height_mm` stays a per-object config; the `debug_assert!` in `build_emit_schedule` enforcing inter-region agreement on the same `(object, layer)` is the **permanent** invariant guard, not a transitional placeholder. Doc comment on `build_emit_schedule` updated to reflect the final contract. DEV-062's body updated to drop the per-region follow-up language. No code-behavior change in this row — purely a documentation/policy record of the scope decision. | slicer-host maintainer | 2026-05-27 | Closed — scope decision, 2026-05-27 |
| DEV-063 | 2026-05-27 | crates/slicer-host/src/support_geometry.rs §`execute_support_geometry` / modules/core-modules/support-planner/src/lib.rs:211 / crates/slicer-ir/src/slice_ir.rs §`SupportGeometryKey.global_support_layer_index` | High (latent in production for any `support_layer_height_mm > 0`) | **Q5c — Producer↔consumer alignment: `global_support_layer_index` is the model layer index.** Pre-fix `execute_support_geometry` assigned `global_support_layer_index` from a per-(object, emit-layer) sequential counter (0, 1, 2, ...) shared across sibling regions via a layer-local scratch map (introduced in DEV-062's Q5b). The `support-planner` guest at `modules/core-modules/support-planner/src/lib.rs:211` reads this field as an index into `collision_cache: Vec<LayerCollisionCache>` (sized by `layer_plan.layers.len()` at `:204-205`). For any `support_layer_height_mm > 0` configuration, the counter values fell below the actual model-layer indices, so tree-support collision polygons were placed against the wrong model layers. Fix: drop the counter and per-layer scratch map entirely; write `global_support_layer_index = global_layer.index` directly. The `SupportGeometryKey` IR doc comment at `crates/slicer-ir/src/slice_ir.rs:957` updated to make the model-layer-index contract explicit. Regression `global_support_layer_index_equals_model_layer_index` in `crates/slicer-host/tests/support_geometry_slice_consumption_tdd.rs` asserts indices `{1, 3, 5}` (model layer indices for `support_layer_height_mm=0.4`, 0.2mm layers, 6 model layers); pre-fix produced `[0, 1, 2]`. Existing `per_layer_cadence_for_multi_region_object` test updated for the new contract (indices `{1, 3, 5}` instead of `{0, 1, 2}`). | slicer-host maintainer | 2026-05-27 | Closed — Commit 1 of per-region handoff plan, 2026-05-27 |
| DEV-056 | 2026-05-25 | crates/slicer-ir/src/slice_ir.rs §`SlicedRegion` / modules/core-modules/rectilinear-infill/src/lib.rs:142 / modules/core-modules/gyroid-infill/src/lib.rs:130 | Medium | **Plan v4 (slicing promotion, Commit 1) — Bottom-first precedence flip on shell-zone overlap.** `SlicedRegion`'s `is_top_surface: bool` / `is_bottom_surface: bool` were replaced by `top_shell_index: Option<u8>` / `bottom_shell_index: Option<u8>` plus polygon-precise `top_solid_fill: Vec<ExPolygon>` / `bottom_solid_fill: Vec<ExPolygon>` (schema 2.1.0 → 3.0.0). The consumer modules `rectilinear-infill` and `gyroid-infill` previously checked top-shell first when both classifications applied (single-layer prints, thin overhangs, regions stamped by both shell projections). They now check bottom-shell first to match OrcaSlicer's `PrintObject::detect_surfaces_type` convention (`OrcaSlicerDocumented/src/libslic3r/PrintObject.cpp:1541-1892`). User-visible effect: regions that were `TopSolidInfill` under both classifications now emit `BottomSolidInfill`. New regression `bottom_wins_over_top_on_overlap` in `modules/core-modules/rectilinear-infill/tests/top_bottom_fill_tdd.rs` pins the convention. | slicer-ir + core-modules maintainers | 2026-05-25 | Closed — Plan v4 Commit 1 |
| DEV-057 | 2026-05-25 | crates/slicer-ir/src/slice_ir.rs §`SlicedRegion::top_shell_index` / §`SlicedRegion::bottom_shell_index` | Low | **Plan v4 (slicing promotion, Commit 1) — Shell-depth index uses `Option<u8>` even though config source is `u32`.** `ResolvedConfig::top_shell_layers` / `bottom_shell_layers` are `u32` (`crates/slicer-ir/src/resolved_config.rs:433-435`). The IR field `top_shell_index` / `bottom_shell_index` is `Option<u8>` to keep `SlicedRegion` cache-friendly (every region × every layer pays this cost). Configurations requesting `top_shell_layers > 255` will saturate via `try_into().unwrap_or(u8::MAX)` in the upcoming `PrePass::ShellClassification` built-in. Practical impact: zero — no real print uses >255 shell layers. Config validation may emit a warning in a follow-up if real-world configurations approach the limit. | slicer-ir + slicer-host maintainers | 2026-05-25 | Closed — Plan v4 Commit 1 |
| DEV-055 | 2026-05-25 | crates/slicer-host/src/model_loader.rs §`parse_3mf_model_xml` (component branch) / §`load_external_model_objects` / .ralph/specs/67_3mf-fixture-e2e-hardening/design.md §"Architecture Constraints — No production code changes" / §"Out-of-Bounds Files" | Medium | **Packet 67 — 3MF production-extension `p:path` parsing added in a packet declared test-only.** Packet 67's `design.md` declared "No production code changes" and listed every file under `crates/slicer-host/src/` (except narrow signature-read surfaces) as out-of-bounds. During Step 1 implementation the two new fixtures (`resources/cube_positive_n_negative.3mf`, `resources/bridge_support_enforcers.3mf`) were discovered to use the 3MF "production extension" `p:path` attribute on `<component>` elements, which references external `.model` files inside the archive. The pre-existing `parse_3mf_model_xml` parsed only inline `<object>` definitions and silently dropped `p:path` references, leaving the fixtures unparseable. ~170 lines were added across `model_loader.rs`: (1) struct field `ParsedComponent.external_path: Option<String>` at line 306; (2) `<component>` branch in `parse_3mf_model_xml` at lines 1249–1284 capturing the `p:path` attribute (strips the `p:` prefix via `local_name`); (3) `load_external_model_objects` helper at line 451 that opens each referenced archive entry, runs `parse_3mf_model_xml` recursively, and merges the resulting objects into the parent model; (4) call-site in the main parser at line 1376. Production behavior change: 3MF archives using `p:path` external `.model` references now load successfully instead of silently dropping component meshes. Root cause of the scope deviation: fixture parser-compatibility was not validated against `load_model()` during packet authoring; this is being mitigated by adding a fixture-validation gate to the `spec-packet-generator` skill (see SKILL.md §5 "Per packet" checklist). The capability is registered in `docs/07_implementation_status.md` TASK-208 row (`rg -c 'TASK-208.*3mf.fixture.e2e' docs/07_implementation_status.md` → 1). No WIT, IR, or scheduler changes; purely an additive 3MF parser feature on the host. Verified by all 12 tests in `crates/slicer-host/tests/threemf_fixture_e2e_tdd.rs` (the two `p:path`-bearing fixtures cannot be loaded without this code). | slicer-host maintainer | 2026-05-25 | Closed — Packet 67, 2026-05-25 (resolved post-implementation review, commit `94ca756`) |

## External plan references

- **DEV-064** — handoff plan that proposed (and was scoped out of) per-region `support_layer_height_mm` cadence: `C:\Users\agpen\.claude\plans\handoff-per-region-support-layer-height.md`. User-local; may be archived or deleted without affecting DEV-064's record.
