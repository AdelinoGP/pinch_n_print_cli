# Deviation Log

Use this registry for intentional deviations from architecture docs.

> **Single source of truth.** This file is authoritative for deviation status. A
> deviation is **open** unless its `Status` column begins with `Closed`. Status
> summaries elsewhere (`docs/07_implementation_status.md` Open Deviation Map)
> are non-authoritative views generated from this table by
> `cargo xtask check-deviations`; when they disagree, this file wins. Do not hand-edit
> those views.

> **Escape literal `|` in a row.** The Rationale column routinely quotes C++, and
> a verbatim `||` splits the row into extra columns — `cargo xtask check-deviations`
> then rejects it (`malformed deviation row ... found 9`). Write `&#124;&#124;`,
> which renders as `||` and carries no pipe. Found on `DEV-168`, 2026-09-03.

| ID | Date | Risk | Rationale | Status |
| --- | --- | --- | --- | --- |
| DEV-173 | 2026-09-04 | Medium | **Knowingly shipping on `clipper2-rust` with an unbounded recursion in `check_split_owner`.** The port diverges from canonical `ClipperBase::CheckSplitOwner` (`clipper.engine.cpp`) in two ways: (a) it recurses on the raw, unresolved `split_idx` whenever `pts.is_none()`, *before* `get_real_outrec` resolution and *before* the `recursive_split` marker is set, so a cycle among pts-less outrecs recurses forever; canonical resolves, marks, then recurses. (b) It demotes `IsValidOwner` from a precondition to a mutation that re-points `split.owner` and accepts the split regardless, which can manufacture the owner cycles the guard exists to prevent. Reached only via the PolyTree path (`boolean_op_tree_64` -> `recursive_check_owners` -> `check_split_owner`), which `clip_polygons` (`crates/slicer-core/src/polygon_ops.rs`) uses for contour/hole hierarchy. **Symptom is a hard process abort — a stack overflow, not a catchable error, with no G-code and no diagnostic.** Measured: depth exceeded 10,000 frames on a 1 GiB stack from 1,601 projected 3DBenchy triangles; threshold was exactly n=1600 ok / n=1601 overflow. Both 1.0.3 and 1.1.0 carry the defect byte-for-byte, so a version bump is not a fix; the flat `union_64` API would bypass it but requires reconstructing the contour/hole hierarchy by hand, a canonical-parity risk. A local fix was written and verified (35 lines restoring canonical ordering, plus two regression tests constructing the splits cycle directly) but **was not retained** — vendoring the crate to hold it cost ~19k lines in-tree. The reachable trigger was instead removed at our call site: `region_needs_support` (`crates/slicer-core/src/algos/mesh_analysis.rs`) no longer unions every non-overhang facet. **There is therefore no regression coverage for this bug, by design.** Re-triggering requires a polygon set whose Clipper splits graph forms a cycle among pts-less outrecs — self-intersecting or non-manifold input is the likely source. This row also records the accompanying `clipper2-rust` 1.0.3 -> 1.1.0 bump: it was verified end-to-end on both models but **no 1.0.3-vs-1.1.0 polygon-output parity comparison was performed**. (Affected section: `check_split_owner` in the `clipper2-rust` crate; `clip_polygons` in `crates/slicer-core/src/polygon_ops.rs`; Mitigation owner: slicer-core maintainer; Target close: upstream fix or a retained patch) | Open — 2026-09-04: user-accepted risk. Not reachable on `tmp/3dbenchy.stl` or `tmp/base.stl` after the `mesh_analysis` fix; both slice clean on stock 1.1.0. |
| DEV-174 | 2026-09-04 | Medium | **`com.core.tree-support-planner` fails to route support bodies on dense meshes, dropping a contiguous band of support layers.** A 2.46 M-triangle miniature (`tmp/base.stl`, 495 layers) produced **29,108 non-fatal code-1200 diagnostics**, all of the form `support demand '<id>' unmet for body '<tree-body-...>': body rejected: routing-cell collision`, and the slice reported `degraded: true` with `fatal_error_count: 0`. Inspection of the emitted G-code shows **roughly 60 consecutive layers missing support in the middle of the print while the rest of the print is correct** — i.e. the failure is a contiguous band, not scattered dropouts, which points at routing-cell collision handling rather than per-demand noise. Reproduced identically with and without the clipper2 patch, so it is independent of DEV-173 and of the packet's `mesh_analysis` and bed-placement changes. Not diagnosed this session. (Affected section: routing-cell collision handling in `modules/core-modules/tree-support-planner/src/lib.rs`; Mitigation owner: tree-support-planner maintainer; Target close: unscheduled) | Open — 2026-09-04: recorded for a dedicated diagnosis session at user request; G-code evidence observed by the user. |
| DEV-166 | 2026-09-03 | Medium | **Packet `241-support-agg-rasterizer` — `support_area_rasterizer = "agg"` reproduces canonical macro-block snapping, which breaks two PnP-only invariants; the divergence is ACCEPTED and the mode ships OPT-IN.** Canonical `SupportGridPattern` (`SupportMaterial.cpp`) floods each `oversampling * oversampling` macro block INDEPENDENTLY in `seed_fill_block`, so an extraction can be strictly larger than the polygons it was built from: the carry grows by at most one macro-block extent (measured 2026-09-03 at the matched default profile: one macro block = `pixel_size` 4167 x `oversampling` 6 = 25002 units = 2.5002 mm, re-derived from the profile by `MacroBlockExtent::assert_consistent` in `crates/slicer-runtime/tests/integration/support_agg_rasterizer_tdd.rs` rather than hardcoded). This is canonical BEHAVIOUR, not a porting defect — canonical deliberately stretches supports into the grid so the zig-zag support snake can run along grid lines, and it consequently prints that material where no overhang demanded it. Three hypotheses that the halo was a PnP porting bug were tested and REFUTED: extraction is pixel-granular (`contours_simplified` never receives `oversampling`); `dilate_trimming_region` is a correct erosion (measured 144 -> 100 set cells, where a dilation would give 196); and `seed_fill_block` is correctly two-pass, block-local, and gated on the dilated mask at BOTH endpoints. Consequences in PnP: (a) support is printed where PnP's demand model demands none (the emit loop derives bodies from `propagated_by_layer`) and the block-snapped halo crosses PnP's per-region foreign-territory bar; (b) the inflated carry routes around an obstacle that would otherwise close every route, so PnP's structured `SupportPlanDeclineReason::NoRoute` / diagnostic `code: 1203` decline does not fire under `agg` whenever the blocking occupancy is LOCAL — i.e. does not also cover the macro-block band the halo floods into. The decline DOES still fire under `agg` when occupancy covers the whole grid neighbourhood, because the trimming mask then blocks seed fill everywhere and the carry genuinely empties; both halves are pinned by `agg_does_not_decline_no_route_where_legacy_semantic_does` and `agg_path_still_declines_no_route_when_occupancy_closes_every_layer` (`modules/core-modules/traditional-support-planner/tests/agg_rasterizer_tdd.rs`). Canonical has NO decline concept at all: when trimming closes every route, `diff(carry, trimming)` goes empty before rasterization and the caller simply skips the lower layers — so neither broken invariant exists upstream, and block-snapping cannot preserve them. An asymmetric clamp of the carry and the printed area to `pre_grid_carry` was implemented and then REJECTED by binding human decision (2026-09-03) and REMOVED from the code: it reduced the agg arm to legacy plus a global `offset_to_slice`, deleting the very behaviour the port exists to reproduce. PnP resolution: `agg` ships UNCLAMPED and OPT-IN, and `legacy_semantic` is the DEFAULT, retaining every PnP invariant (structured decline, territory bar, demand-only emission) for everyone who does not explicitly opt in. (Affected section: `RasterizerMode::Agg` arm of `SupportPlanner::plan_candidate`, `modules/core-modules/traditional-support-planner/src/lib.rs`; packet 241 design.md §Data and Contract Notes; Mitigation owner: traditional-support-planner maintainer; Target close: a later packet that gives PnP a canonical-style demand model able to absorb block-snapped halos, at which point `agg` could become the default.) The AC-6/AC-7 and F-I1 metrics originally recorded on this row were measured UNDER the removed clamp and were SUPERSEDED by the Step-14 re-measurement (2026-09-03, clamp removed, guests fresh): AC-6 wall leakage 0 events / 0.0 units² in BOTH modes; AC-7 abrupt column drops legacy 3 -> agg 0, total emitted area 225789129333 units² (2257.89 mm²) legacy vs 354695221947 units² (3546.95 mm²) agg = +57.09 % (RECORDED, not gated — the ±25 % guard was retired because this halo is the accepted canonical behaviour), macro-block containment 0.0 units² outside on 26/26 layers over 0 difference pieces with a bisected smallest containing grow of 22754 units against the derived 25002-unit extent; AC-8 26 of 26 body layers diverge with both modes reaching the plate; F-I1 agg - control = +984.12 mm² (+38.40 % of control) against control - legacy = +304.94 mm², max per-layer symdiff 38.1980 mm² on 26/26 layers. Full figures: packet 241 requirements.md §Recorded metrics appendix. | Open — 2026-09-03: divergence accepted and documented; clamp rejected and removed; `agg` opt-in, `legacy_semantic` default. Post-clamp-removal metrics RE-MEASURED and recorded (Step 14 complete); closure gates re-run green (Step 16 complete). Packet 241 remains `draft`: `agg_wedge_plan_is_nonempty_and_reaches_beneath_overhang` is an open failure under `agg` (duplicate SupportPlanIR support-region entries — Step 17; unblocked 2026-09-03 by a documented temporary producer-side merge, root cause unfixed and tracked as DEV-167) and the human-gate dry-run artifacts are clamp-era and await regeneration. Packet 241 closes NARROW and NOT GREEN with AC-N2 RED. |
| D-285-ADR-0051-AMENDED | 2026-08-01 | Low | **Packet 187 — ADR-0051 obligation #3 retired in two prose forms of the original. Retired clause quoted verbatim in the ADR's `## Amendment — 2026-08-01 (packet 187)` section. The "no silent guess" half of the obligation is preserved; the fatal-error half is replaced with a warn-and-pass policy specific to the postpass injection-point model (one `ERR_MALFORMED_LAYER_MARKER` warning per occurrence, prior Z reused, `Ok` return). A future consumer that cannot degrade gracefully remains on the original "fail loudly" rule and must record that choice in the amendment section. The `D-` series is the counter, separate from `DEV-###`; re-derive at the moment of writing.** (Affected section: `docs/adr/0051-gcode-marker-contract-ownership.md §Obligation #3 / packet 187`; Mitigation owner: machine-gcode-emit maintainer; Target close: packet 187 closure) | Open — 2026-08-01: amendment section appended; warn-and-pass is the documented policy; test `malformed_layer_marker_warns_and_uses_prior_z_per_adr_0051` green. |
| D-286-ADR-0005-AMENDED | 2026-08-10 | Low | **Packet 202 — ADR-0005's Decision pinned `CompiledModuleLive` to 5 fields; packet 202 adds the 6th (`native_entry`) for native dispatch, so the contested bullet is quoted verbatim and the 6th field recorded in the ADR's new `## Amendment — 2026-08-10 (packet 202)` section (precedent: D-285-ADR-0051-AMENDED); the amendment also corrects two stale bindgen counts (4→15) in the ADR's §Decision and §Verification.** (Affected section: docs/adr/0005-runner-traits-in-slicer-wasm-host.md §Decision bullet 3 / §Decision co-location sentence / §Verification; Mitigation owner: slicer-wasm-host maintainer; Target close: packet 202 closure) | Open — 2026-08-10: amendment section appended; field recorded; stale counts corrected. |
| DEV-026 | 2026-04-16 | High | Startup validation still misses several compatibility and governance checks beyond the WIT-source/version problem tracked in DEV-014. The remaining unique gaps are: (1) `min_host_version` is parsed but never compared at startup; (2) manifest-schema validation cannot currently surface a meaningful `Schema` failure path because missing schema falls back to defaults and parse failures collapse into TOML errors; (3) memory, full-slice time, and host-call budgets from docs/12 have no runtime measurement, diagnostics, or benchmark evidence. Layer-count and region-cap bounds are already enforced. Implement the host-side `wit_world` allowlist as part of the same validation pass used to close DEV-014 rather than tracking that subtask separately here. **Verified 2026-07-03:** gap (2) is now closed — `LoadErrorKind::Schema` (`crates/slicer-scheduler/src/manifest.rs`) is a real, distinct variant with specific field/message context, proven by `region_split_manifest_tdd.rs`; malformed manifests no longer collapse into generic TOML errors. **Gap (1) closed (this pass corrects stale status, not new work):** a 14th startup DAG validation pass, `DagValidationPass::HostVersionCompatibility`, was added and wired in production — the `DagValidationPass::HostVersionCompatibility` variant and the `validate_host_version` free function both live in `crates/slicer-scheduler/src/validation.rs`, `validate_startup_dag` (same file) calls `validate_host_version`, and it compares `request.host_version` against each loaded module's declared `min_host_version` via the existing `semver_lt` helper, pushing `SchedulerError::HostVersionIncompatible { module, required, available }` on failure. `run_slice` (`crates/slicer-runtime/src/run.rs`) populates `host_version` from the running host's own `CARGO_PKG_VERSION` at the single production call site. (Line pins previously recorded here had rotted and were dropped per the repo's cite-by-symbol rule; every symbol named resolves.) Documented in `docs/04_host_scheduler.md`'s "Validation Passes (in order)" and `docs/11_operational_governance_and_acceptance_gate.md` §2. This landed in an earlier commit but this log row was never updated to reflect it until now. **Gap (3) partially closed 2026-07-03 (time-only, downgraded per maintainer decision):** the docs/12 500-layer peak-RSS bound is not achievable with existing instrumentation — `AccountingAllocator` (`crates/slicer-runtime/src/report/allocator.rs`) only tracks host-heap allocations and explicitly excludes WASM linear memory (wasmtime mmaps it directly, bypassing `GlobalAlloc`) — and is deferred indefinitely (tracked under `TASK-156`; would need new OS-level RSS sampling around a subprocess run). The 50-layer full-slice-time bound now has reproducible real evidence: `crates/slicer-runtime/benches/gate_evidence.rs` (`cargo bench -p slicer-runtime --bench gate_evidence`) times a real `pnp_cli slice` subprocess against `resources/regression_wedge.stl` + `resources/test_config/gate_evidence_50l.json` (a `layer_height`/`first_layer_height` override coercing the 40mm-tall fixture to exactly 50 layers, verified via `;LAYER_CHANGE` count — not the normatively-named `benchy_50l_0p2_single_tool` fixture, which still doesn't exist). Measured this session (debug profile): **~438ms median** (`[435.25, 438.01, 440.97] ms`), well under the 10-second bound. Measures and reports rather than hard-failing (matches this repo's other benches — "slow; not in CI") (Affected section: `04_host_scheduler.md §"Phase 3 DAG Validation" / 11_operational_governance_and_acceptance_gate.md §2 "Compatibility Policy" / 12_architecture_gate_metrics.md §"Compatibility"`; Mitigation owner: slicer-host maintainer; Target close: Deferred — tracked under TASK-156). | Open (deferred — environmental, NOT a parity gap) — 2026-07-25: re-verified and re-classified. Gaps (1) and (2) are closed in production code: `DagValidationPass::HostVersionCompatibility` / `validate_host_version` are implemented and wired into `validate_startup_dag` (`crates/slicer-scheduler/src/validation.rs`), and `LoadErrorKind::Schema` (`crates/slicer-scheduler/src/manifest.rs`) is a real distinct variant. The only remaining half is the docs/12 peak-RSS/500-layer bound, and it is **structurally unmeasurable with existing instrumentation, not unimplemented**: `AccountingAllocator` (`crates/slicer-runtime/src/report/allocator.rs`) hooks `GlobalAlloc` and its own module doc states "WASM linear memory is **not** measured here. wasmtime allocates linear memory via mmap directly, bypassing `GlobalAlloc`." Closing it needs new OS-level RSS sampling of a `pnp_cli slice` subprocess — a bench/tooling workstream, already deferred under `TASK-156` (unchecked in `docs/07_implementation_status.md`). This row diverges from no OrcaSlicer behaviour and blocks no parity work; it should not be counted as an open parity deviation. |
| DEV-039 | 2026-05-04 | Low | **Packet 35 — bounding-box fallback for empty slice polygons.** When `slice_mesh_ex` yields zero polygons for a layer (e.g., perfectly flat top/bottom triangles skipped by the slicer), `execute_layer_slice` synthesizes a bounding-box polygon from the `ObjectMesh` vertex XY extents for the surface-classification XY-containment check. Without this fallback, surface-classified facets lying entirely above/below a flat-top/bottom layer would fail the XY-containment guard and surface classification would never fire on geometry that warrants it. This is packet-local; not part of the original packet 35 design. **Verified 2026-07-03:** the fallback logic itself is unchanged and still fires, but the function moved to `crates/slicer-core/src/algos/prepass_slice.rs` (line range corrected — was previously misquoted as 483-520) and was renamed `execute_prepass_slice_single_layer` during the host→core algo migration; no later packet replaced the heuristic with anything more precise. **Correction 2026-07-03:** the fallback's purpose has drifted from this row's original text — it now feeds `classify_region_surfaces` / `SlicedRegion.is_bridge` (bridge detection), not top/bottom-surface classification; `SlicedRegion` no longer has `is_top_surface`/`is_bottom_surface` fields, that responsibility moved to the separate `ShellClassification` pass in `crates/slicer-core/src/algos/paint_segmentation/top_bottom.rs`. The previously-cited test `multi_layer_thickness_tdd` does not exist anywhere in this repo's history (confirmed via `git log --all -S`); the real test exercising this path today is `crates/slicer-runtime/tests/unit/bridge_detector_tdd.rs::non_bridge_region_has_empty_bridge_areas` (Affected section: `crates/slicer-core/src/algos/prepass_slice.rs::execute_prepass_slice_single_layer (moved/renamed from crates/slicer-host/src/layer_slice.rs::execute_layer_slice)`; Mitigation owner: slicer-host maintainer; Target close: TBD). | Open — packet-local fallback; future packets 36/37 inherit the same XY-containment guard; file moved to slicer-core (2026-07-03); text corrected 2026-07-03 |
| D-109-SELF-CAPTURED-FIXTURES | 2026-07-02 | Medium | **Packet 109 (M1 verification) — accepted limitation.** Roadmap T-101 asked to "Record OrcaSlicer reference outputs for 6 M1 fixtures". This environment cannot run OrcaSlicer, so the six M1 parity fixtures are SELF-CAPTURED baselines recorded from this pipeline's own output (see the provenance comment in `perimeter_parity.rs`). The harness therefore proves self-regression (geometry stays stable across commits), NOT per-vertex OrcaSlicer parity — it can detect drift from our own prior output but never divergence from OrcaSlicer. The roadmap's M1 "COMPLETE" claim is scoped accordingly. User accepted this limitation 2026-07-02 (packets-102–109 review) (Affected section: `crates/slicer-runtime/tests/integration/perimeter_parity.rs §fixture provenance (lines ~749-770) / docs/specs/_OLD/perimeter-modules-orca-parity-roadmap.md §M1 (T-101)`; Mitigation owner: slicer-runtime maintainer; Target close: — (accepted; revisit if an OrcaSlicer reference-capture environment becomes available)). | Open — accepted limitation, user decision 2026-07-02; fixtures guard self-regression only |
| D-163-ARACHNE-BRIDGE-ROLE-CONVERSION-EXEMPTION | 2026-07-16 | Low | Canonical exempts `role == erOverhangPerimeter && flow.bridge()` from the spacing->width back-conversion at emission (`VariableWidth.cpp::thick_polyline_to_multi_path` keeps the bridge flow's own width). PnP has no role analog at this seam — bridges are per-vertex `flow_factor`, not a path role — so the D-160 Bug B fix converts uniformly. Bridge vertices therefore get a back-converted width plus `bridging_flow`'s factor, where canonical substitutes the bridge flow width wholesale. Logged rather than silently mirrored; revisit if bridge extrusion parity work lands a role model at this seam (Affected section: `modules/core-modules/arachne-perimeters/src/lib.rs::build_walls (emission back-conversion)`; Mitigation owner: core-modules maintainer; Target close: bridge-parity packet). | Open — 2026-07-16: logged at fix time |
| DEV-099 | 2026-07-30 | Low | Typed-error entry points retain the invariant: a caught panic remains a distinct error rather than a successful empty result. `medial_axis` is exempt because `Err(())` becomes `Ok(vec![])`, so a caught panic can degrade to empty. No ADR. Cost needs design/measurement (Affected section: ``crates/slicer-core/src/medial_axis.rs` `medial_axis` catch/error conversion`; Mitigation owner: next packet touching `medial_axis`). | Open |
| DEV-107 | 2026-08-01 | Medium | **Packet 185 follow-up** — canonical top/bottom/internal-solid flow-**ratio** controls (material-volume parity) are not ported by packet 185; deferred to a follow-up packet. The resolver (`resolve_role_width`) handles geometric widths/spacing only (Affected section: `(deferred)`; Mitigation owner: core-modules maintainer; Target close: flow-ratio controls packet). | Open |
| DEV-101 | 2026-07-31 | Low | **Same `min_width_top_surface` key, two different percent bases across the two perimeter generators.** Both modules declare the key as `float_or_percent` with default `"0.0"`, but they resolve the percent form against different reference widths: `classic-perimeters` passes `inner_wall_line_width` as the `get_abs_value` base (canonical `ratio_over` for this key is the perimeter width), while `arachne-perimeters` passes `preferred_bead_width_outer`. Consequence: the SAME percent string yields DIFFERENT absolute thresholds in the two modules whenever outer and inner wall widths differ — e.g. `"300%"` with `inner_wall_line_width = 0.4` and `preferred_bead_width_outer = 0.5` resolves to `1.2` mm under Classic and `1.5` mm under Arachne, so switching `wall_generator` silently changes which top sub-areas are eroded out of the top portion. The float form is unaffected (no base is consulted). Canonical OrcaSlicer has a single `ratio_over` for the key, so at most one of the two bases can be faithful; determining which — and aligning the other — is deferred. Already documented as a cross-module difference in `docs/15_config_keys_reference.md` (the two `min_width_top_surface` rows record `base: inner_wall_line_width` and `base: preferred_bead_width_outer` respectively) but had no deviation row until now (Affected section: ``modules/core-modules/classic-perimeters/src/lib.rs` (`ClassicPerimeters::run_perimeters`, the `get_abs_value("min_width_top_surface", ...)` binding) vs `modules/core-modules/arachne-perimeters/src/lib.rs` (`emit_only_one_wall_top_second_pass`); manifests `classic-perimeters.toml` / `arachne-perimeters.toml` `[config.schema.min_width_top_surface]``; Mitigation owner: core-modules maintainer; Target close: packet to reconcile the two percent bases against canonical `ratio_over` (verify canonical first; changing either base moves gated geometry for percent-valued profiles)). | Open — 2026-07-31: filed by packet 184 closure review. Not a regression: predates this packet (the arachne base landed in packet 150/152; the classic base landed in packet 184). Neither module is known-wrong yet; the row records the DISAGREEMENT |
| D-168-ARACHNE-SIMPLIFY-FALLBACKS-TIGHTER-THAN-CANONICAL | 2026-07-16 | Low | Canonical defaults are `wall_maximum_resolution = 0.5mm` / `wall_maximum_deviation = 0.025mm` (verified in local checkout, `PrintConfig.cpp`). PnP's CODE fallbacks are 0.05mm / 0.005mm (via `ArachneParams::default`'s `smallest_line_segment_squared = 0.0025` / `allowed_error_distance_squared = 0.000025`, which transcribed the meshfix_* constants instead). The old manifest defaults (0.5/0.025) matched canonical but lied about the code; the exhaustive reconcile guard (manifest_default_reconcile_tdd) now pins the manifest to the CODE fallback (0.05/0.005), making the code-vs-canonical divergence the explicit open item: when the keys are unset, PnP simplifies arachne walls 10x/5x less aggressively than upstream. Changing the code fallback moves every arachne fixture, so it needs its own predicted-movement re-record. Related follow-up owned by the same packet: the reconcile guard is exhaustive by ENUMERATION (transcribed table); the by-construction form (read sites consume a shared DEFAULTS table) is blocked on classic's DERIVED fallbacks (nozzle_diameter -> inner_wall_line_width -> line_width) (Affected section: `modules/core-modules/arachne-perimeters/src/lib.rs (wall_maximum_resolution/wall_maximum_deviation reads) + ArachneParams::default`; Mitigation owner: core-modules maintainer; Target close: simplify-parity packet). | Open — 2026-07-16 |
| D-104f-CONCENTRIC-INFILL-NO-ARACHNE | 2026-07-09 | Low | Gap: concentric infill does not route through Arachne — OrcaSlicer's `FillConcentric.cpp` / `FillConcentricInternal.cpp` wire concentric infill generation through the same Arachne wall-toolpath machinery used for perimeters; this codebase's infill modules have no equivalent wiring. **Not closed by packet 149** — the red test `arachne_parity_pipeline_concentric_infill_uses_arachne` stays red as the explicit fingerprint of this gap (14 passed / 1 red in the `arachne_parity` suite this packet). Deferred to a follow-up workstream; no packet is currently scoped to it (Affected section: `crates/slicer-runtime/tests/arachne_parity.rs §`arachne_parity_pipeline_concentric_infill_uses_arachne``; Mitigation owner: core-modules maintainer; Target close: — (deferred; follow-up workstream TBD)). | Open — deferred to follow-up workstream |
| D-152-TOP-AREA-SOURCE | 2026-07-11 | Medium | **Packet 152 Step 4 (G3 part 2) — top-area source divergence from OrcaSlicer.** PnP has no `upper_slices`/`lower_slices` access on `SliceRegionView`, so the G3 part-2 second-pass top-area is derived from `region.top_solid_fill()` (the host's precomputed top-region polygon) instead of OrcaSlicer's `diff_ex(infill_contour, upper_slices_clipped)` (`PerimeterGenerator.cpp`). The bridge-exclusion step (Orca carves `lower_slices_clipped_offset` out of the top area) is SKIPPED for the same reason. All other steps are ported: `min_width_top_surface` filter via `get_abs_value` (packet 150 mechanism), `offset2_ex` shrink `-top_surface_min_width` / expand `+top_surface_min_width + 0.85*perimeter_width`, second `WallToolPaths` over the non-top remainder with `inner_loop_number + 1` walls, `inset_idx` (`perimeter_index`) += 1 renumbering of inner walls BEFORE merge, and the empty-top fallback rerun with `inner_loop_number + 2`. AC-2 verified (single top wall + renumbering) via `tests/only_one_wall_top_tdd.rs::only_one_wall_top_second_pass` (Affected section: `modules/core-modules/arachne-perimeters/src/lib.rs §`emit_only_one_wall_top_second_pass` / crates/slicer-sdk/src/views.rs §`top_solid_fill``; Mitigation owner: core-modules maintainer). | Open (accepted, tracked divergence) — ADR-0035's faithful-port bar is scoped to `crates/slicer-core/src/arachne/`; this divergence lives in a module-level caller (`modules/core-modules/arachne-perimeters/`), so no amending ADR is required. Accepted as a known top-area-source divergence (does not block archival). |
| DEV-093 | 2026-07-25 | High | **Root-caused 2026-07-25: `medial_axis` is not a pure function of its input, because the Voronoi beach line is a skip list whose node levels come from an OS-seeded `ThreadRng`.** `boostvoronoi 0.12.1` stores its beach line in a `cpp_map::skiplist::SkipList` (`beach_line.rs`); `cpp_map 0.2.0` picks each node's level with `rand::prelude::ThreadRng` (`THREAD_RNG` in its `skiplist_impl`, consumed by `random_level` / `coin_flip`), which is OS-seeded, advances on every insert, and exposes no seeding hook. The beach-line comparison predicate is not a strict weak ordering under floating point, so which node a search lands on depends on the random level structure. Near-degenerate inputs therefore resolve differently **from one call to the next inside a single process** — so this is not a per-process hash seed, and the original `HashMap`-iteration hypothesis is **disproven**. Two knock-on effects are visible: the build sometimes trips `assertion failed: fpv.is_finite()` in boostvoronoi's `robust_fpt`, which `medial_axis`'s `catch_unwind` converts to an empty result (zero polylines), and when it does not panic it can still emit a *different* polyline. **How it reaches G-code:** `com.core.classic-perimeters` wins the `perimeter-generator` claim over `com.core.arachne-perimeters` by first-winner dedup, and its `detect_thin_wall` branch feeds each sub-nozzle-width protrusion to the `medial_axis` host service; `ExtrusionRole::ThinWall` emits as `;TYPE:Inner wall` (`crates/slicer-gcode/src/emit.rs`). A flipped call is exactly one sliver inner-wall loop appearing or disappearing. **Evidence (0.2 mm benchy, release, `RAYON_NUM_THREADS=1`):** prepass output is structurally **identical** across runs — the `PNP_DUMP_PREPASS_IR` byte diff on `region_map` / `support_geometry` is pure `HashMap` serialisation order, and a structural compare showed 0 differing keys and 0 differing geometry. Per-stage arena fingerprints then showed the first divergence is always `Layer::Perimeters` / `com.core.classic-perimeters`, on a **byte-identical** `SliceIR` input, at 5 layers (36, 106, 107, 124, 158). Fingerprinting the host `medial_axis` boundary showed exactly 5 differing calls, each with an identical input hash and a different output — 4 of them `n=0` vs `n=1`. A standalone replay of the captured inputs varied within a single process across 10 consecutive identical calls. **Reproducer:** `crates/slicer-core/tests/medial_axis_determinism_tdd.rs` (`medial_axis_is_deterministic_across_repeated_calls`), `#[ignore]`d while this row is open; it is the acceptance criterion for the fix. Must be run in **release** — in debug every call panics internally and is caught, so the flip is masked and the test passes for the wrong reason. **Fix not yet applied.** `cpp_map 0.2.0` and `boostvoronoi 0.12.1` are the newest published versions, so there is no upstream fix to take. The entropy is a single line (`THREAD_RNG` in `cpp_map`'s `skiplist_impl`); removing it means vendoring `cpp_map` (~6.2k lines, MIT OR Apache-2.0) behind a `[patch.crates.io]` and swapping the `ThreadRng` for a fixed-seed PRNG — skip-list level randomness is only a balancing heuristic, so a deterministic PRNG preserves the expected O(log n) behaviour. That is a repo-level dependency decision and is left for a follow-up. **Consequence while open:** whole-output G-code byte comparison cannot attribute a behaviour change to a code change; any diff of roughly this shape and size is inside the noise band. The earlier partial mitigations remain correct but insufficient — the bucket-key sort in `merge_collinear_overlapping_simple` (`crates/slicer-core/src/medial_axis.rs`) removed one *input-order* entropy source, and the gap-fill width-band pre-filter in `modules/core-modules/classic-perimeters/src/lib.rs` removed one class of degenerate input; neither touches the RNG itself, and the thin-wall branch has no equivalent pre-filter. (Affected section: `crates/slicer-core/src/medial_axis.rs` `medial_axis` via `boostvoronoi 0.12.1` -> `cpp_map 0.2.0`; Mitigation owner: slicer-core maintainers; Target close: TBD) | Open — 2026-07-25: root cause identified and reproduced; fix requires vendoring `cpp_map` |
| D-154-DISCRETIZE-POINT-POINT-CASE | 2026-07-14 | Low | **Found during packet 154's D-105D root-cause investigation; filed, not fixed.** PnP's `discretize_edge` returns `{start, end}` for ALL `!is_curved` edges — it conflates canonical `discretize()`'s branch 1 (`(!point_left && !point_right) \|\| vd_edge.is_secondary()` → `Points({start,end})`, `SkeletalTrapezoidation.cpp`) with its branch 3 (both cells are point-cells → a straight edge that is STILL subdivided by `discretization_step_size`). Canonical `discretize()` (`SkeletalTrapezoidation.cpp`) has dispatch, branch 1, branch 2 (point-line), and branch 3 (point-point) cases. Consequence: point-point VD edges are never subdivided in PnP, so point-point channels that narrow between two vertices get no intermediate beading samples. Sub-gap noted in the same investigation: PnP's `discretize_edge` also omits branch 1's `is_secondary()` disjunct; whether PnP's `is_curved` flag already encodes secondary-edge status is unconfirmed. **Provably NOT the D-105D thin-strip root cause** — a rectangle's spine is a seg-seg edge (branch 1), not point-point (branch 3); confirmed the real root cause was `preprocess.rs::merge_short_segments` (see D-105D, closed) (Affected section: `crates/slicer-core/src/skeletal_trapezoidation/graph.rs::discretize_edge`; Mitigation owner: arachne maintainer; Target close: TBD). | Open — filed by packet 154, not fixed; scope is point-point VD edge subdivision only |
| D-104f-CONCENTRIC-INFILL-NO-ARACHNE (packet 154 note) | 2026-07-14 | Low | **Re-surfaced (not caused, not fixed) by packet 154's test runs during the D-105D thin-strip investigation.** This gap was filed 2026-07-09 (packet 149, row above) and remains open under its original ID; this note only records that packet 154 independently re-confirmed it while running the full `arachne_parity` suite: `run.rs` never dispatches concentric infill through `run_arachne_pipeline`, whereas OrcaSlicer routes concentric infill wall generation through Arachne's `WallToolPaths` (`FillConcentric.cpp`). The test `arachne_parity_pipeline_concentric_infill_uses_arachne` (`crates/slicer-runtime/tests/arachne_parity.rs`) already proposes this exact deviation ID in its own assertion message. Pre-existing and out of packet 154's scope (Affected section: `crates/slicer-runtime/src/run.rs / crates/slicer-runtime/tests/arachne_parity.rs`; Mitigation owner: core-modules maintainer; Target close: — (deferred; follow-up workstream TBD)). | Open — unchanged; see original row above for full detail |
| DEV-103 | 2026-08-01 | Low | **Five canonical injection points remain unreachable from PnP.** `file_start_gcode` is emitted before `; HEADER_BLOCK_START`, while `DefaultGCodeSerializer::serialize_gcode` writes `serialize_header_block` before iterating `gcode_ir.commands`; `ThumbnailAwareSerializer::serialize_gcode` inserts after `; HEADER_BLOCK_END`, so the file-start site cannot be reached. `wrapping_detection_gcode` is gated by canonical `enable_wrapping_detection`, which has no PnP occurrence, and canonical emits it from a per-extruder loop in `GCode::process_layer`. `machine_pause_gcode` and `template_custom_gcode` are gated by `CustomGCode::PausePrint` and `CustomGCode::Template` in `emit_custom_gcode_per_print_z`, but PnP has no per-print-Z custom-G-code item model. `printing_by_object_gcode` requires a by-object print sequence; PnP has no by-object path, and the default ("print_sequence", "by layer") in `ORCA_CONFIG_PADDING` is a default rather than a hard-code (Affected section: `modules/core-modules/machine-gcode-emit/src/lib.rs custom-G-code injection registry / `PostPass::GCodePostProcess``; Mitigation owner: core-modules maintainers; Target close: TBD). | Open — 2026-08-01: filed by packet 188 / TASK-307 |
| DEV-104 | 2026-08-01 | Low | **Variable-level residuals remain in the custom-G-code port.** Canonical registers `filament_start_gcode`, `filament_end_gcode`, and `filament_change_extrusion_role_gcode` as `coStrings` per-filament vectors resolved with `get_at(filament_id)`, while PnP declares scalar string keys. The `change_filament_gcode` placeholder set is a scoped subset: canonical also exposes `flush_length_1` through `flush_length_4`, `x_after_toolchange`, `y_after_toolchange`, `z_after_toolchange`, travel points, temperatures, and retract lengths, none of which PnP models. Canonical initializes `m_last_extrusion_role` to `erNone` but PnP substitutes an empty string; `manual_filament_change` first-toolchange suppression is not modelled. PnP splices at the `;TYPE:` tag gate `m_last_processor_extrusion_role`, not the template gate `m_last_extrusion_role`, and canonical assigns those members independently. `WipeTowerIntegration::append_tcr` binds additional variables at `filament_start_gcode` that PnP does not surface (Affected section: `modules/core-modules/machine-gcode-emit/src/lib.rs custom-G-code placeholder bindings / toolchange and extrusion-role sites`; Mitigation owner: core-modules maintainers; Target close: TBD). | Open — 2026-08-01: filed by packet 188 / TASK-307 |
| DEV-105 | 2026-08-01 | Medium | **The `filament_extruder_id` binding has two canonical id spaces.** At `filament_start_gcode` canonical binds it to the new tool as a raw FILAMENT id via `int(new_filament_id)`; at `filament_end_gcode` it binds it to the old tool as an EXTRUDER id via `int(get_extruder_id(old_filament_id))`. Those spaces differ on any multi-extruder-per-filament configuration. PnP's `GCodeCommand::ToolChange` carries tool indices only and models no filament-versus-extruder distinction, so it binds `to` at the start site and `from` at the end site: the OLD/NEW direction is faithful, but the id space is not representable. Any future multi-extruder-per-filament support must either carry both ids or accept a knowingly wrong substitution at one site. See `ADR-0050` and `docs/adr/0050-custom-gcode-architecture.md` (Affected section: `modules/core-modules/machine-gcode-emit/src/lib.rs toolchange placeholder bindings / docs/adr/0050-custom-gcode-architecture.md`; Mitigation owner: core-modules maintainers; Target close: TBD). | Open — 2026-08-01: filed by packet 188 / TASK-307; ADR-0050 is the decision record |
| DEV-108 | 2026-08-01 | Low | **Accepted parity residuals after packet 187 / `TASK-306`; these are not a claim of exact parity.** (a) At `layer_change_gcode`, `max_layer_z` is a canonical **dead-write** of `m_max_layer_z`; (a2) the other two `max_layer_z` sites have no divergence. (b) The BBL timelapse path remains unported, including `generate_timelapse_gcode`, `timelapse_inline_photo`, and `is_BBL_Printer`. (c) Six `layer_change_gcode` variables remain unmodelled: `most_used_physical_extruder_id`, `current_filament_id`, `current_nozzle_id`, `curr_y_acceleration_limit`, `curr_accumulated_mass`, and `curr_layer_mass`; `m_max_layer_z` is the underlying canonical member for the dead-write. (d) `change_layer` interleaving differs from canonical, including `add_object_change_labels`. These are accepted residuals for the exact parity audit and remain separate from the implemented injection points (Affected section: `modules/core-modules/machine-gcode-emit/src/lib.rs / packet 187 custom-G-code injection registry`; Mitigation owner: core-modules maintainers; Target close: packet 188 / future parity audit). | Open — 2026-08-01: filed by packet 187 / `TASK-306`; accepted residuals recorded |
| DEV-097 | 2026-07-27 | Low | **Packet 181 — placeholder-skip affordance retired; symbols and doc comment left behind for now.** `is_placeholder_wasm` (`fs::metadata(wasm_path).len() <= 8`) and `LoadedModule::placeholder_wasm` (`crates/slicer-scheduler/src/manifest.rs`) are now inert for dispatch: the loader fatal at packet 181 step 3 escalates all three `compile_module_component` `None` branches to `LiveModuleLoadError::Component { module_id, cause }`, so an ≤8-byte stub no longer produces a warning-and-continue path. The manifest affordance doc comment ("runtime dispatch will skip them with a diagnostic") is also false. Symbols are NOT removed in this packet's scope: `is_placeholder_wasm` and `LoadedModule::placeholder_wasm` remain in place and are inert; cleanup is deferred to an unscheduled successor packet. The user can scaffold a module by building a `.wasm` that compiles (≥ a few kB). No successor packet exists in `docs/spec_packets/` to own the cleanup (Affected section: `crates/slicer-scheduler/src/manifest.rs (`is_placeholder_wasm` and `LoadedModule::placeholder_wasm` accessor; doc comment "runtime dispatch will skip them with a diagnostic")`; Mitigation owner: slicer-scheduler maintainers; Target close: TBD). | Open — 2026-08-05: corrected — packet 181 left the placeholder symbols in code, inert; cleanup is deferred to an unscheduled successor. |
| D-173-THUMBNAIL-SINGLE-PNG | 2026-07-19 | Medium | **Packet 173 — fork renders one source PNG; PnP owns multi-format fan-out, deviating from fork ticket 011.** Fork ticket 011 expected the fork to render one thumbnail per requested size/format. PnP's user-decided contract is single-source-PNG: the fork renders ONE high-res top-down PNG and passes it via `--thumbnail`; the `thumbnails` config key (`"WxH/EXT,WxH/EXT"`) carries the requested sizes/formats; PnP decodes the source, resizes per spec (`image::imageops::resize` with `FilterType::CatmullRom`), and encodes PNG/JPG/QOI/ColPic/BTT_TFT itself (`render_thumbnail_entries` in `crates/slicer-gcode/src/thumbnail.rs`). Per-format codecs: PNG/JPG/QOI via the `image` crate (`crates/slicer-gcode/Cargo.toml` dep `image = { version = "0.25", default-features = false, features = ["png", "jpeg", "qoi"] }`); ColPic is a Rust port of canonical `ColPic_EncodeStr`/`ColPicEncode`/`Byte8bitEncode`/`ADList0` (`OrcaSlicerDocumented/src/libslic3r/GCode/Thumbnails.cpp`) in `crates/slicer-gcode/src/thumbnail_colpic.rs` with the standard porting header; BTT_TFT is a Rust port of canonical `compress_thumbnail_btt_tft` in `crates/slicer-gcode/src/thumbnail_btt.rs`. Two further reductions vs canonical: (1) ColPic and BTT_TFT do NOT vertically flip the image — canonical does (the GL-buffer workaround), PnP source PNGs are already top-down; (2) ColPic chunk-size is implementation-chosen to keep each `;gimage:` / `;simage:` line ≤ 78 chars; canonical chunking is data-size-driven. Wire format is byte-exact to canonical: `; <tag> begin <W>x<H> <len>` / 78-col `; `-prefixed base64 / `; <tag> end`, with the outer `; THUMBNAIL_BLOCK_START`/`; THUMBNAIL_BLOCK_END` sentinels retained as a PnP extension (Affected section: `crates/slicer-gcode/src/thumbnail.rs / serialize.rs / crates/slicer-runtime/src/pipeline.rs / docs/02_ir_schemas.md`; Mitigation owner: n/a (user-decided); Target close: TBD). | Open — 2026-07-19: AC-6 locks no-vertical-flip; AC-7 locks the docs; implementation reviewed under packet 173. |
| DEV-110 | 2026-08-02 | Medium | **Canonical's `ContourIntersectionPoint` neighbour bookkeeping is not ported.** Canonical's `take_limited` reaches `could_take_prev`, `could_take_next`, `trim_prev`, and `trim_next`; PnP substitutes a stub budget clamped to the arc reaching the next boundary position in the walk direction (AC-N3). Canonical's early-return path can leave an endpoint unconsumed so a later arc can claim it; PnP has no equivalent and consumes both endpoints of every arc it processes (Affected section: ``OrcaSlicerDocumented/src/libslic3r/Fill/FillBase.cpp` (`ContourIntersectionPoint`, `take_limited`) / `modules/core-modules/infill-linker/src/graph.rs` (`contour_stub`)`; Mitigation owner: infill-linker maintainer; Target close: TASK-311). | Open — 2026-08-02: residual filed for the deliberate clamp substitute. |
| DEV-112 | 2026-08-02 | Medium | **The percent base has a width-input residual, not a spacing-formula residual.** PnP computes it as `line_width_to_spacing(line_width_mm, layer_height_mm)`, exactly canonical `Flow::rounded_rectangle_extrusion_spacing`, but its `line_width_mm` is the module's generic `line_width` key. Canonical `group_fills` takes `layerm.region().flow(*layer.object(), frInfill, ...).spacing()`, a per-role infill flow width that can differ under auto or percent width resolution. Both inputs are extrusion-width scale, so this is small; the formula matches and this is not the 5.6x line-spacing error (Affected section: ``modules/core-modules/infill-linker/src/orchestrate.rs` (`line_width_to_spacing`) / OrcaSlicer `Fill::group_fills` (`Flow::rounded_rectangle_extrusion_spacing`)`; Mitigation owner: infill-linker maintainer; Target close: TASK-311). | Open — 2026-08-02: residual is the width input selected for the percent base. |
| DEV-114 | 2026-08-02 | Medium | **`infill_density` is deliberately left an undeclared dead read (the single exception AC-20 allow-lists); the deferral is disclosed, not a parity gap.** Fact (a): `orchestrate.rs` reads it via `config_float(view.config(), "infill_density")` while `infill-linker.toml` declares it nowhere, so `ConfigView::from_declared` drops it and `unwrap_or(0.2)` is the value on every slice. Fact (b): the key carries a fraction-vs-percent conflict between the code and three manifests — `rectilinear-infill`, `gyroid-infill` and `lightning-infill` declare `default = 20.0, min = 0.0, max = 100.0`, a percent, while the code is consistently a fraction: `ResolvedConfig` carries `infill_density: f32 = 0.2`, `to_config_map` emits `Float(0.2)`, `rectilinear-infill`'s fallback is `_ => 0.2`, and `crates/slicer-sdk/src/config_resolution.rs` tests assert fractions. The harm is a bounds/documentation inconsistency in those three manifests, NOT a live 100x spacing bug — their code reads the fraction correctly. The consequence for the linker is the reason to defer: declaring the key would make it honour a user-set density where today it is pinned at `0.2`, moving `sparse_spacing_mm` and hence `remove_short_polylines` and the infill-overlap offset on every non-default-density slice — a behaviour change that should land with the tree-wide unit reconciliation rather than inside an anchor-length packet. `infill_density` does not feed `anchor_base_mm`, so leaving it dead cannot affect this packet's parity claim (Affected section: ``modules/core-modules/infill-linker/src/orchestrate.rs` (`config_float(view.config(), "infill_density")`) / `modules/core-modules/infill-linker/infill-linker.toml` / `crates/slicer-ir/src/resolved_config.rs` (`ResolvedConfig::to_config_map`) / `rectilinear-infill` / `gyroid-infill` / `lightning-infill` manifests`; Mitigation owner: infill-linker maintainer; Target close: TASK-311). | Open — 2026-08-02: deferral disclosed; declaring the key is a behaviour change deferred to the tree-wide unit reconciliation. 2026-08-31 (wayfinder ticket 107): the fraction-vs-percent conflict in fact (b) is resolved — the key is now canonical-percent everywhere under the Orca name `sparse_infill_density` (manifests 20.0 [0,100], modules divide by 100, `ResolvedConfig` field renamed with default 20.0); the linker's dead read is re-pointed to the new name with percent conversion and remains undeclared, so the deferral itself is intact. |
| DEV-100 | 2026-07-31 | Medium | **Residual of DEV-085's user-facing half: eight `[key]` macros that an older `docs/15_config_keys_reference.md` advertised do not resolve, because PnP has no config key to bind them to.** `[bed_temperature]`, `[filament_type]`, `[tool_count]`, `[layer_count]`, `[print_time_estimate_s]`, `[x_max]`, `[y_max]` and `[z_max]` are emitted verbatim as literal bracketed text, with one aggregated warning naming them, whereas canonical resolves all eight. Packet `186-custom-gcode-placeholder-engine` originally made an unresolved placeholder a hard slice error; that policy was **reverted before landing** on the repo owner's ruling that it is architecturally wrong for a modular slicer — a module's `ConfigView` is scoped to its own manifest, so a key this module cannot resolve may legitimately be owned by a module that is not loaded, and aborting the slice breaks composition. Measured: the fatal policy broke three e2e tests across two real OrcaSlicer-authored 3MF fixtures, which reference `[first_layer_bed_temperature]`, `[initial_tool]` and `[max_layer_z]`. The residual harm is therefore the original one — bracketed literal text can reach a printer — now at least accompanied by a warning. **Measured canonical counterparts** (verified in this checkout unless noted): `[bed_temperature]` is a real canonical placeholder variable declared `coInts` in `TemperaturesConfigDef`, whose value comes from `get_bed_temp_key((BedType)curr_bed_type)` — a six-way dispatch over `supertack_plate_temp` / `cool_plate_temp` / `textured_cool_plate_temp` / `eng_plate_temp` / `hot_plate_temp` / `textured_plate_temp`. PnP models no bed-type dispatch at all, so it cannot be aliased onto `bed_temperature_initial_layer_single` without silently substituting the wrong number for five of the six bed types. `[filament_type]` is a real canonical `coStrings` option in `PrintConfigDef::init_fff_params`; it has zero occurrences anywhere under `crates/` or `modules/`. `[tool_count]` does not exist canonically; the nearest is `num_extruders` (`OtherSlicingStatesConfigDef`, coInt). `[layer_count]` does not exist; the nearest is `total_layer_count` (`PrintStatisticsConfigDef`, coInt) — PnP does carry the value, on `GCodeIR.metadata.layer_count`, but a `PostPass::GCodePostProcess` module sees only a `ConfigView` and cannot read it. `[print_time_estimate_s]` does not exist; the nearest is `print_time_sec` (`PrintStatisticsConfigDef`, coString). `[x_max]` and `[y_max]` do not exist; the nearest is `print_bed_max` (`DimensionsConfigDef`, coFloats, elements 0 and 1). `[z_max]` does not exist; the nearest is `printable_height`, or `max_layer_z` for the achieved top layer. **Domain asymmetry (the reason canonical accepts templates PnP now rejects):** canonical resolves a placeholder against a local `DynamicConfig` override layered over the entire print config plus roughly 119 explicit `placeholder_parser().set(...)` global assignments, whereas PnP's resolution domain is one module's manifest-declared key set plus its alias table. **Failure-handling divergence:** canonical's `GCode::placeholder_parser_process` catches the parser error, splices an inline failure marker into the output and lets the export continue, deferring a single aggregated throw to `GCode::check_placeholder_parser_failed` — so canonical ultimately **fails** the export. PnP adopts the aggregation but not the failure: it emits one `log_warn` naming every unresolved key and every injection point, leaves the bracketed text in place, and completes the slice. This is a deliberate divergence, not an oversight — see the modularity rationale above. It means a template canonical rejects will slice successfully in PnP, producing G-code containing literal `[key]` text. (All canonical facts in this row were measured. The `PrintConfigDef`/`TemperaturesConfigDef`/`PrintStatisticsConfigDef`/`OtherSlicingStatesConfigDef`/`DimensionsConfigDef` facts were measured in this repo's `OrcaSlicerDocumented/`; the `GCode.cpp` facts — `placeholder_parser_process`'s catch-record-and-continue, `check_placeholder_parser_failed`'s single aggregated `Slic3r::PlaceholderParserError` throw, and the assignment counts — were measured in a full sibling checkout, because this repo's mirror carries no `GCode.cpp`. Two refinements from that measurement: dedupe by template name is performed by `placeholder_parser_process`'s insert-if-absent guard rather than by `check_placeholder_parser_failed`, and the 119 figure counts `placeholder_parser().set(` specifically (110 `this->` + 9 `gcodegen.`) — a further 11 `m_placeholder_parser.set(` calls exist — 8 in `PrintApply.cpp` and 3 in `SLAPrint.cpp` — so the library-wide explicit-set total is 130.) Closing this needs either real PnP config keys for the print-statistics and bed-geometry quantities, or a decision to declare these eight permanently unsupported and document the warned verbatim passthrough as intended behaviour (Affected section: ``modules/core-modules/machine-gcode-emit/src/lib.rs` `substitute_placeholders` (stage `PostPass::GCodePostProcess`) / `docs/15_config_keys_reference.md``; Mitigation owner: core-modules maintainers; Target close: TBD). | Open — 2026-07-31: eight advertised macros have no canonical-equivalent PnP key to bind them to and pass through as literal bracketed text with a warning; filed as the residual of packet 186, which fixed the engine (char-boundary-correct literals, list-valued config resolution, `nozzle_diameter` declared, `first_layer_temperature` aliased, warn-on-unresolved) but could not supply the missing values. The fatal-on-unresolved policy that packet originally carried was reverted before landing as architecturally wrong for a modular slicer |
| DEV-106 | 2026-08-01 | Low | **Packet 193 — canonical's `boundary_offset` is `0.5 * path.width`, a per-path constant; PnP stamps the signed overhang distance per point and resolves the offset from that point's own `width`.** Classic perimeters use one width per loop, so the values are identical there; arachne variable-width loops vary along the loop, bounded by the loop's width variation. Packets 190 and 191 read the already-normalised field rather than re-deriving the offset for interpretation (Affected section: ``Point3WithWidth::overhang_distance_mm` / `SurfaceClassificationIR::prev_layer_boundaries` / `expolygon_to_path3d` / Arachne per-vertex overhang stamping`; Mitigation owner: slicer-core + perimeter-module maintainers; Target close: packet 193 / TASK-314). | Open — 2026-08-01: recorded as the carrier's bounded canonical boundary_offset divergence; no consumer or G-code change |
| DEV-115 | 2026-08-05 | Low | **Deliberate opt-in divergence: gyroid may fill top/bottom/bridge roles in addition to sparse (ADR-0027).** OrcaSlicer uses gyroid for sparse infill only (`Fill.cpp`); PnP's `gyroid-infill` manifest holds `claim:top-fill` / `claim:bottom-fill` / `claim:bridge-fill` in addition to `claim:sparse-fill` (landed by packet 135 / TASK-260), so a user who sets the fill-holder keys to `gyroid-infill` gets gyroid waves for solid shells and bridges. Defaults remain rectilinear for those roles, so default behaviour matches OrcaSlicer's sparse-only gyroid. The opt-in guard is regression-pinned by `default_holders_gyroid_sparse_only` in `modules/core-modules/gyroid-infill/tests/gyroid_infill_tdd.rs` (Affected section: `modules/core-modules/gyroid-infill/gyroid-infill.toml` + `docs/adr/0027-gyroid-multi-role-fill-holder.md`; Mitigation owner: gyroid-infill maintainer; Target close: — (accepted divergence)). | Open — 2026-08-05: divergence row filed per ADR-0027 §Decision item 4; no change planned while default config matches canonical |
| DEV-119 | 2026-08-05 | Medium | **Packet 175 — M73 emission granularity deviation, accepted for fork progress-display requirements.** Canonical `process_line_move` emits M73 progress per G1 move; PnP emits at stream start, at changed-value layer boundaries, and at stream end only (per-G1-line M73 would insert O(commands) lines for negligible fork value, since hosts sample at seconds granularity — layer-boundary emission is the plan-approved deviation). Additionally, canonical runs a second, stealth-mode estimator for the `M73 Q/S` mask; PnP uses the same estimate for `Q/S` as for `P/R` (the single-estimate contract is plan-mandated; a future stealth estimator would amend packet 175's tests). `disable_m73` gates M73 only; the `; filament used` / `; estimated printing time (normal mode)` comment block is unconditional (Affected section: `crates/slicer-gcode/src/m73.rs::inject_m73` + wiring at the estimator call site in `DefaultGCodeEmitter::emit_gcode`; Mitigation owner: slicer-gcode maintainer; Target close: — (accepted for fork progress-display requirements, packet 175 / TASK-279)). | Open — 2026-08-05: plan-approved deviation, documented at packet 175. |
| DEV-120 | 2026-08-05 | Low | **Packet 188 — `custom_gcode_changes_tool` T-select suppression not modelled.** Canonical `GCode::set_extruder` always invokes `m_writer.toolchange` but drops its emitted `T<n>` text when `custom_gcode_changes_tool` finds a matching `T<id>` inside the parsed `change_filament_gcode` (a non-empty template alone does not suppress the select). PnP does not parse the template and must not invent that suppression: it always re-emits the emitter-produced `ToolChange`, because dropping a tool change the emitter produced would silently change which tool prints. The companion suppression — canonical skipping the *entire* first `change_filament_gcode` under `manual_filament_change` when `m_toolchange_count == 1`, where PnP emits the template at every toolchange including the first — is already covered by DEV-104 ("`manual_filament_change` first-toolchange suppression is not modelled"); this row records the `custom_gcode_changes_tool` half only (Affected section: `modules/core-modules/machine-gcode-emit/src/lib.rs` toolchange emission (`GCodeCommand::ToolChange` re-emit; no template parsing); Mitigation owner: core-modules maintainers; Target close: TBD). | Open — 2026-08-05: filed by the packet-188 audit; cross-reference DEV-104 for the `manual_filament_change` half. |
| DEV-126 | 2026-08-07 | Medium | **`build_wall_flags`' nearest-vertex reprojection is a PnP invention with no canonical precedent; canonical assigns paint flags by clipping path geometry.** `build_wall_flags` and its helper `nearest_original_vertex` (`crates/slicer-core/src/perimeter_utils.rs`), consumed by both `modules/core-modules/arachne-perimeters/src/lib.rs` and `modules/core-modules/classic-perimeters/src/lib.rs`, map each inset-ring vertex to the nearest *original* polygon vertex and inherit that vertex's `PaintSemantic::Material` / `PaintSemantic::FuzzySkin` annotation — a per-vertex nearest-neighbour lookup. Canonical instead clips the finished wall path against the fuzzy region's `ExPolygons`: `apply_fuzzy_skin` (Polygon and `Arachne::ExtrusionLine` overloads), `group_region_by_fuzzify`, `should_fuzzify` and `fuzzy_polyline` (`Feature/FuzzySkin/FuzzySkin.cpp`) route through `Algorithm::split_line(fuzzified, r.expolygons, true)` and fuzzify only the `clipped` runs. `split_line` has zero occurrences anywhere under `crates/` or `modules/`, PnP's `fuzzy-skin` module included; canonical's only closest-point use at this seam is `ripple_anchor_arc_mm`'s arc-length anchor, which is unrelated to flag assignment. **Consequence corrected 2026-08-07 (packet 208 grounding): the stated mis-attribution is UNREACHABLE on the live path, because the annotations the reprojection reads are never written.** `build_wall_flags` reads only `segment_annotations[PaintSemantic::Material]` (as `PaintValue::ToolIndex`) and `segment_annotations[PaintSemantic::FuzzySkin]` (as `PaintValue::Flag`), but the sole production writer of `segment_annotations` is `build_modifier_segment_annotations` (`crates/slicer-core/src/algos/paint_segmentation/mod.rs`), which emits only `SupportEnforcer`/`SupportBlocker` as `PaintValue::Flag(true)` (`slice_modifier_volumes`, `crates/slicer-core/src/algos/paint_segmentation/modifier_volumes.rs`: "Material etc. are not modifier semantics"). Painted variant regions are built with `segment_annotations` empty ("segment_annotations stays empty (D14): FuzzySkin travels on variant_chain, not here"), and `crates/slicer-runtime/src/layer_executor.rs` states outright that paint v2 does not write `segment_annotations[Material]` today; `SlicedRegionUpdateBuilder::set_segment_annotations` has test call sites only. So `nearest_original_vertex` always resolves against an empty map, `WallBoundaryType::MaterialBoundary` never occurs in production, and the only live paint signal at this seam is the whole-region `variant_fuzzy` bit read from `variant_chain()`, which needs no reprojection. This is the same single-writer root cause behind DEV-123's open half and DEV-133. The divergence from canonical is therefore real but currently INERT: the row records a PnP-invented mechanism that is dead on the live path, not an active mis-attribution. Two further corrections: `clip_polylines` (`crates/slicer-core/src/polygon_ops.rs`, Clipper2 open-subject intersection, with production callers in `crates/slicer-core/src/algos/lightning/` and `modules/core-modules/infill-linker/src/offset.rs`) already exists, so "no Clipper line-split infrastructure in-tree" was wrong — what is missing is the TAGGED form (canonical `Algorithm::split_line` attributes per-junction `clipped`/`src_idx` via a ClipperZ `ZFillFunction`, and `clipper2-rust`'s `Point<T>` has no `z` field); and the tests pinning the current technique are five files, not two — `crates/slicer-core/tests/inner_wall_concave_reprojection_tdd.rs`, `inner_wall_material_boundary_tdd.rs`, `perimeter_utils_three_tool_boundary_tdd.rs`, `modules/core-modules/{classic,arachne}-perimeters/tests/boundary_paint_tdd.rs`, and `crates/slicer-runtime/tests/contract/inner_wall_boundary_type_tdd.rs`. The technique has its own tests (`crates/slicer-core/tests/inner_wall_concave_reprojection_tdd.rs`, `inner_wall_material_boundary_tdd.rs`) but no ADR or deviation row recorded it as a deliberate divergence. Note for scoping: replacing it with the canonical clip shape touches currently-shipped, tested classic-perimeters behaviour and needs Clipper-based line-split infrastructure that does not exist in-tree (Affected section: `crates/slicer-core/src/perimeter_utils.rs` (`build_wall_flags`, `nearest_original_vertex`) and its two perimeter-module call sites; Mitigation owner: slicer-core + core-modules maintainers; Target close: TBD). | Open — 2026-08-07: filed by deferred-task verification sweep; canonical clip mechanism verified present, PnP equivalent verified absent. |
| DEV-127 | 2026-08-07 | Low | **Three independent scan-line fill copies had drifted in vertex testing, rotation reference, scan start, and scan-grid handling.** `scan_expolygon` in `modules/core-modules/rectilinear-infill/src/lib.rs`, `TraditionalSupport::fill_expolygon` in `modules/core-modules/traditional-support/src/lib.rs`, and `support-surface-ironing` in `modules/core-modules/support-surface-ironing/src/lib.rs` are now reconciled by packet 209: the copies converge on vertex test, rotation reference, scan start, and scan grid semantics without de-duplicating them. The duplication is NOT removed; the target close remains the WIT pattern-services packet. The real remedy is WIT-interface pattern services (modules invoking each other's algorithms across the component boundary), which does not exist yet. (Affected section: `modules/core-modules/rectilinear-infill/src/lib.rs` (`scan_expolygon`) / `modules/core-modules/traditional-support/src/lib.rs` (`TraditionalSupport::fill_expolygon`) / `modules/core-modules/support-surface-ironing/src/lib.rs` (`support-surface-ironing`); Mitigation owner: core-modules maintainer; Target close: WIT pattern-services packet) | Open — 2026-08-15: packet 209 reconciled scan-line semantics while retaining all three implementations. |
| D-209-HALF-OPEN-SCAN-GRID-ADOPTED | 2026-08-15 | Medium | `rectilinear-infill`'s inclusive grid was a divergence and is corrected to canonical `ceil(w/s)`; the top-boundary post-pass and sub-spacing bail are deleted; four self-captured fixture counts were re-baselined (square 6 → 5, hole 8 → 7, solid 5 → 4, tiny polygon 0 → 1). Canonical's `align_to_grid` anchor and the `full_infill()` `(line_spacing + SCALED_EPSILON)/2` inset remain Open, unported parity gaps. `make_fill_lines` shows the half-open bound is separable from the **inset**, and does **not** show it separable from `align_to_grid` (which it merges into the bbox before reading `min.x()`, so it sets the grid **origin**). (Affected section: `modules/core-modules/rectilinear-infill/src/lib.rs`; Mitigation owner: rectilinear-infill maintainer; Target close: parity follow-up) | Recorded — 2026-08-15: half-open grid adopted; `align_to_grid` anchor and full-infill inset remain Open. |
| D-209-ADJUST-SOLID-SPACING-DIVERGENCE | 2026-08-15 | Medium | **Open.** Three arithmetic axes remain divergent: (i) canonical uses `(width - EPSILON)` as the numerator of both divisions — `number_of_intervals = (width - EPSILON) / distance`, then `distance_new = (width - EPSILON) / number_of_intervals` — where PnP uses bare `width`; (ii) canonical truncates (C++ integer conversion) where PnP calls `.round()`; (iii) on the over-1.2× branch canonical returns `floor(distance * 1.2 + 0.5)` (the cap) where PnP returns the original `distance`. (Affected section: `adjust_solid_spacing`; Mitigation owner: rectilinear-infill maintainer; Target close: parity follow-up) | Open — 2026-08-15: arithmetic divergence remains. |
| D-209-TANGENTIAL-TOUCH-SPAN-SPLIT | 2026-08-15 | Low | **Open.** Canonical's neighbour-product tangency filter is not ported; the half-open predicate plus the zero-length-span rule is coverage-equivalent but emits two abutting spans where canonical emits one at an interior tangential touch. (Affected section: scan-line edge intersection handling in the three fill copies; Mitigation owner: core-modules maintainer; Target close: parity follow-up) | Open — 2026-08-15: tangency filter remains unported. |
| DEV-128 | 2026-08-07 | Low | **`support-planner` carries tree-node geometry in `f32` mm where canonical uses scaled-integer `coord_t`.** `Pt { x: f32, y: f32 }` and the surrounding node math in `modules/core-modules/support-planner/src/lib.rs` (legacy support-planner) and `modules/core-modules/tree-support-planner/src/lib.rs` (tree-support-planner) are `f32` throughout (`branch_angle_deg`, `merge_distance_mm`, `line_width_mm`, `MAX_BRANCH_RADIUS_MM`, the `neighbours_of: Vec<Vec<(usize, f32)>>` MST edge weights, and `smooth_branches`' `(prev.0 + cur.0 + next.0) / 3.0`). Canonical's `SupportNode::position` is an Eigen `Point` of `coord_t` (i64 at 1e-6 mm) and `TreeSupport::smooth_nodes` (`TreeSupport.cpp`) averages those integers directly, with `max_move = scale_(support_line_width / 2)` likewise in scaled-integer space. The exposure is accumulated rounding across the 100 smoothing iterations and the MST edge weights on dense models with large XY coordinates, which would surface as invariant-2 (collision-free) failures rather than as a visible artefact. Note the repo's own hazard: PnP's unit is 100 nm, not canonical's 1 nm, so a port must divide canonical constants by 100 (`docs/08_coordinate_system.md`). The claim that this was "documented but not scheduled" no longer holds — the only record is the archived `docs/specs/_OLD/support-modules-orca-port.md`, with no live task id, no ADR, and no row until this one. The distinct mm-space-`f32` tradeoff in `docs/spec_packets/_OLD/60_configurable-slicing-precision.md` concerns Douglas-Peucker and is not this (Affected section: `modules/core-modules/support-planner/src/lib.rs` (legacy support-planner) and `modules/core-modules/tree-support-planner/src/lib.rs` (tree-support-planner) (`Pt`, `smooth_branches`, `neighbours_of` MST weights); Mitigation owner: support-planner maintainer; Target close: TBD — trigger is invariant-2 failures on dense large-XY models). | Open — 2026-09-08: packet 238b identifies both legacy and tree-support-planner paths; the separate precision waivers remain justified. Originally 2026-08-07: filed by deferred-task verification sweep; type divergence verified on both sides, prior "documented" status was stale. |
| DEV-131 | 2026-08-07 | Low | **2D boundary-distance queries are unaccelerated — no tree and, contrary to the deferral note that described them, no bounding-box prefilter either.** `signed_distance_to_boundary` (`crates/slicer-core/src/perimeter_utils.rs`) loops over every edge of every `ExPolygon` for each query vertex with no bbox test and no early-out; `segment_intersections` and `nearest_reference_point` (`modules/core-modules/overhang-classifier-default/src/lib.rs`) likewise scan the full `boundary` slice per segment. Canonical accelerates the same query: `LinesDistancer` / `distance_from_lines_extra` (`AABBTreeLines.hpp`) builds an AABB tree over indexed lines and is held per-`PrintObject` in `prev_layer_boundaries` / `prev_curled_extrusions`, consumed by `estimate_points_properties` and `ExtrusionQualityEstimator::estimate_extrusion_quality` (`GCode/ExtrusionProcessor.hpp`). PnP does have `AabbTree` (`crates/slicer-core/src/aabb_tree.rs`), but it is a **3D mesh-triangle** tree ported from `AABBTreeIndirect.hpp` for raycasts and closest-point-on-mesh, and is not wired into any 2D boundary-distance path. Two corrections to the historical framing, both material: the symbol `LinesDistancer2D` named by the original deferral **does not exist anywhere** in `crates/` or `modules/` (its only in-tree mentions are a packet-88 note in `docs/05_module_sdk.md`, which already carries a `<!-- VERIFY: no LinesDistancer2D symbol was found -->` marker, and the archived `docs/spec_packets/_OLD/57_overhang-speed.md` that is the sole source of the "defer BVH, profile-only-if-needed" decision); and the AABB prefilter that deferral credited the code with is absent, so the cost is worse than the note claimed. **Impact is unmeasured** — there is no benchmark covering this path — so this row records a structural divergence, not a demonstrated latency problem. Reopen trigger: profile the per-layer classify pass and act if boundary-distance queries dominate it (Affected section: `crates/slicer-core/src/perimeter_utils.rs` (`signed_distance_to_boundary`) / `modules/core-modules/overhang-classifier-default/src/lib.rs` (`segment_intersections`, `nearest_reference_point`); Mitigation owner: slicer-core + core-modules maintainers; Target close: TBD — gated on profiling evidence). | Open — 2026-08-07: filed by deferred-task verification sweep as an incidental finding; the originally-filed symbol name and the claimed prefilter were both wrong, the underlying divergence is real and unmeasured. |
| DEV-141 | 2026-08-21 | Medium | **`smooth_outward` deliberately diverges from canonical `clip_narrow_corner`, because canonical applies its chamfer offset to the wrong vertex.** In canonical `clip_narrow_corner` (`MutablePolygon.cpp`, itself adapted from Cura's `smooth_corner_complex()`), the "one side far, the other blocked" branch runs `if (forward == Far) { std::swap(p0, p2); std::swap(p02, p22); }` and then closes with `(backward == Far ? *it2 : *it0) += (v.cast<double>() * t).cast<coord_t>();`. `forward` tracks the `it2` side (it advances via `it2.next()`) and `backward` the `it0` side (`it0.prev()`), while the computed offset `v * t` is measured from `p0` — the FAR endpoint after the swap — so the ternary moves the *opposite* iterator. `clip_narrow_corner`'s own comment ("Find point on (p0, p02) at distance shortcut_length from p2") and its `assert(dfar2 >= shortcut_length2)` both describe the far side. The port in `clip_narrow_corner` (`crates/slicer-core/src/smooth_outward.rs`) instead selects `if forward == Far { it2 } else { it0 }`, i.e. it moves the far vertex. Every other statement of the branch is a verbatim port. This is an intentional divergence FROM canonical on the grounds that canonical is wrong; a byte-for-byte parity comparison against OrcaSlicer on any polygon that reaches this branch will therefore differ, and that difference is expected rather than a porting defect. The module-level `# Deviations from canonical` section of `crates/slicer-core/src/smooth_outward.rs` records the same reasoning at the source (Affected section: `crates/slicer-core/src/smooth_outward.rs` (`clip_narrow_corner`, the far/blocked branch target selection); Mitigation owner: slicer-core maintainer; Target close: TBD — close if the fix is accepted upstream, or reopen the analysis if canonical's ternary turns out to be load-bearing). | Open — 2026-09-08: packet 238b resolved tree-planner smoothing separately; DEV-141 remains the independent smooth_outward deviation. Originally 2026-08-21: filed by packet 224 (support-family Orca closure) after a line-by-line re-port of the module against canonical. The divergence is deliberate and comment-pinned in the source; it has not been submitted upstream. Renumbered from the originally-filed DEV-135 during the parity/support-planners-clean rebase: that ID was independently allocated on mainline by packet 226 (authored-coloring carrier). |
| DEV-142 | 2026-08-21 | Low | **`draw_circles`' unconditional 0.0125 mm simplify has no canonical counterpart; canonical simplifies only `base_areas`, only on the square-support path, and at a roughly 100x coarser tolerance.** Canonical `draw_circles` (`TreeSupport.cpp`) does not use `RESOLUTION` at all: its only simplification is `if (SQUARE_SUPPORT) { for (auto &area : base_areas) area.simplify(scale_(line_width / 2), &base_areas_simplified); }` — tolerance `line_width / 2`, applied to the base areas only. The port applies `DRAW_CIRCLES_RESOLUTION_MM` (0.0125 mm, libslic3r's `SCALED_RESOLUTION` grade) to every emitted role unconditionally, via `expolygons_simplify` in `modules/core-modules/tree-support-planner/src/lib.rs`. Retained deliberately: the regions emitted here are a union of many per-node ellipses (up to `CIRCLE_RESOLUTION_FINE` = 100 vertices each) plus this port's swept capsules, and a vertex-grade cleanup is what keeps the serialized `SupportPlanIR` from carrying on the order of a hundred near-collinear vertices per node per layer. Adopting canonical's `line_width / 2` tolerance instead would coarsen branch outlines by roughly two orders of magnitude on the non-square path, where canonical does not simplify at all. Consequence: support-branch outlines carry fewer vertices than canonical's, with no vertex moving further than the 0.0125 mm tolerance. Impact on printed geometry is unmeasured (Affected section: `modules/core-modules/tree-support-planner/src/lib.rs` (`DRAW_CIRCLES_RESOLUTION_MM` and its `expolygons_simplify` call inside the `draw_circles` port); Mitigation owner: tree-support-planner maintainer; Target close: TBD — revisit if the square-support path is ever ported, which needs canonical's gated `line_width / 2` simplify as a separate branch). | Open — 2026-09-08: packet 238b resolved tree-planner smoothing separately; this row retains only the measured draw_circles simplification deviation. Originally 2026-08-21: filed by packet 224; canonical's simplify site verified as `base_areas`-only and `SQUARE_SUPPORT`-gated, the divergence is a deliberate retention. Renumbered from the originally-filed DEV-136 during the parity/support-planners-clean rebase: that ID was independently allocated on mainline by packet 226 (authored-coloring carrier). |
| DEV-143 | 2026-08-21 | Low | **`smooth_nodes` relaxes in `f64` scaled units and rounds once; canonical truncates to integer on every one of its 100 passes.** Canonical `TreeSupport::smooth_nodes` (`TreeSupport.cpp`) relaxes node positions and radii with the unweighted three-point kernel `pts1[i] = (pts[i-1] + pts[i] + pts[i+1]) / 3` in `Point` (`coord_t`) arithmetic, so each pass truncates. The port (`smooth_nodes`, `modules/core-modules/tree-support-planner/src/lib.rs`) runs the identical kernel, iteration count (`SMOOTH_NODES_ITERATIONS`), Jacobi update, endpoint pinning and commit rules — including canonical's deliberate asymmetry where the committed position is the final pass' output while the committed `movement = (pts[i+1] - pts[i-1]) / 2` is read off that pass' input — but carries the intermediate arrays as `f64` in scaled units and rounds only at commit. Accumulating canonical's truncation over 100 passes biases every chain toward its head; the port declines to reproduce that bias. Same class as DEV-128 (the older `support-planner`'s `f32` mm node math) but the opposite direction: here the port is *more* precise than canonical, not less. Consequence: smoothed node positions are not bit-identical to OrcaSlicer's; the divergence is bounded by the pinned chain endpoints and by `max_move` (`scale_(support_line_width / 2)`), which the port applies in the same scaled space (Affected section: `modules/core-modules/tree-support-planner/src/lib.rs` (`smooth_nodes` relaxation arrays); Mitigation owner: tree-support-planner maintainer; Target close: TBD — only if bit-exact node-position parity with canonical is ever required). | Open — 2026-08-21: filed by packet 224 alongside DEV-142; kernel, iteration count and commit rules verified canonical, arithmetic width verified as the sole divergence. Renumbered from the originally-filed DEV-137 during the parity/support-planners-clean rebase: that ID was independently allocated on mainline by packet 226 (authored-coloring carrier). |
| DEV-144 | 2026-08-21 | Medium | **`need_extra_wall` is computed per node but has no per-node transport, so it degrades to a per-layer capability string.** Canonical sets `SupportNode::need_extra_wall` in `TreeSupport::smooth_nodes` (a merge point, or a node whose movement outruns its own `max_move_dist`), interpolates it across a chain ("interpolate need_extra_wall in the end"), and `TreeSupport::generate_toolpaths` consumes it to bump the wall count on exactly those nodes. The port computes and interpolates the flag faithfully on the node arena in `modules/core-modules/tree-support-planner/src/lib.rs`, but `SupportPlanIR` now carries the `wall_counts` evidence carrier, but the emitted plan entry still ORs the flag across the layer and, when any node on that layer carries it, pushes the capability string `"tree-branch-extra-wall"` alongside `"tree-branch-skeleton"`. No renderer consumes that capability today, so the extra wall is not printed; advertising it was chosen over silently dropping the requirement. Consequence: tree branches at merge points and fast-moving nodes print with the ordinary wall count, i.e. thinner than canonical at exactly the places canonical reinforces. Closing this needs a renderer consumer for the `wall_counts` carrier, not a module-local edit (Affected section: `modules/core-modules/tree-support-planner/src/lib.rs` (`need_extra_wall` on the node arena; the `"tree-branch-extra-wall"` capability push) / `SupportPlanIR` node payload, which has no wall channel; Mitigation owner: tree-support-planner maintainer + IR owner; Target close: TBD — gated on a `SupportPlanIR` per-node wall field). | Open — 2026-09-08: packet 238b closes the carrier implementation question with wall_counts evidence; only the remaining renderer coverage gap is open. Originally 2026-08-21: filed by packet 224; canonical's producer and consumer both verified, the PnP producer is present and correct and only the transport is missing. Renumbered from the originally-filed DEV-138 during the parity/support-planners-clean rebase: that ID was independently allocated on mainline by packet 226 (authored-coloring carrier). |
| DEV-147 | 2026-08-23 | Low | Packet 236 Step 4 / G-23: the tree-support regression tripwire now feeds occupancy outlines only at the actual floating plate cross-section from `overhang_plate_fixture`; lower layers correctly represent empty space, so collision and avoidance execute without carving every branch. The prior all-layer occupancy produced a false 0 branches / 0 endpoints capture; the corrected self-capture measures 7 branches / 154 endpoints. This is a fixture-correction rebaseline, not planner behaviour: no planner source was edited and tolerances remain frozen at branch count ±10% and Hausdorff ≤0.5mm (Affected section: `modules/core-modules/tree-support-planner/tests/orca_parity_tdd.rs` (`benchy_tree_support_regression_tripwire`); Mitigation owner: tree-support-planner maintainer; Target close: packet 236). | Open — 2026-08-23: classified as a fixture-correction rebaseline, not an algorithm change. |
| DEV-148 | 2026-08-24 | Medium | **Packet 234a (original edition) — internal-bridge-over-infill qualifies ZERO sites on calicat where canonical produces EXACTLY ONE near Z≈29.45.** The landed ShellClassification prepass qualification runs canonical support math but never qualifies a site: skip histogram over 174 layer-visits is 156× `top_solid_fill_empty`, 9× `unsupported_empty`, 9× `qualified_empty` (recorded in the ISSUE-82 parity closure decision brief §0). Root cause candidate RC-A: `unsupported_span_areas` (`crates/slicer-core/src/algos/bridge_over_infill.rs`) initializes the unsupported carrier as a bounding-box complement of the lower fills (`fill_envelope`) while canonical `PrintObject::bridge_over_infill` initializes from the fill polygons themselves, then closes/shrinks/diffs grown solids. Additionally the IR lacks a dense-interior (`stInternalSolid`) taxonomy and construction anchors on polygon stand-ins rather than walls; full coverage/anchoring gap tracked as F4 of `docs/specs/_OLD/bridge-parity-plan.md`. Measured evidence: bridge-parity-plan.md F3 addendum + decision brief (Affected section: `crates/slicer-core/src/algos/bridge_over_infill.rs` (`unsupported_span_areas`, `fill_envelope`) / `crates/slicer-runtime/src/slice_postprocess_prepass.rs` (`gate_internal_bridge_sites`); Mitigation owner: slicer-core + slicer-runtime maintainers; Target close: packet 234a closure edition). | Closed — 2026-09-08: packet 234a closure machinery addressed the zero-site root cause; remaining matched-profile divergences are DEV-149, DEV-150, and DEV-153 |
| DEV-149 | 2026-08-25 | Medium | **Matched-profile low-z mid-stack internal-bridge qualification diverges from canonical.** The port bridges plateau-cap bands over sparse infill that matched-profile Orca leaves as solid infill; the emitted band sizes themselves match Orca. Evidence: `tmp/calicat_matched_profile.gcode` vs `tmp/calicat_orcaSlicer.gcode` (Affected section: shell classification / internal-bridge qualification; Mitigation owner: shell-classification track; Target close: shell-classification parity packet). | Open — 2026-08-25: filed by packet 234a closure; explicitly out of scope under the owner ruling. |
| DEV-150 | 2026-08-25 | Medium | **Matched-profile cavity-site coverage is below canonical.** Qualified area is approximately 143 mm² versus approximately 262 mm²-equivalent at 526.27 mm × 0.49875. Structural pipeline-outline findings show canonical grows final bridge area beyond the candidate surface via `construct_anchored_polygon` void-filling, then `shapes ∩ total_fill − total_top`; the port never grows beyond the authored band. Canonical uses a 0.81×flow-height depth-window factor versus the port's 1.0×layer-height factor, and deep-phase spacing serves bridging versus solid roles differently (Affected section: infill construction / bridge-area qualification; Mitigation owner: infill/construction track; Target close: infill/support parity packet). | Open — 2026-08-25: filed by packet 234a closure; explicitly out of scope under the owner ruling. |
| DEV-153 | 2026-08-25 | Medium | **Matched-profile internal-bridge role-labeling gap.** A fresh debug slice of `resources/calicat.stl` under the matched-oracle profile produced zero `;TYPE:Internal Bridge` sections, while bridge-related geometry was emitted under other labels (`Bridge` / `Internal solid infill`). AC-6 pins the measured labeled-section truth rather than treating absent labels as canonical parity. **Half closed 2026-09-02** (SchemaBridgeMap ticket 19, R1): the infill module emitted the host-qualified internal-bridge sites (`internal_bridge_areas`, appended to `bridge_areas` by `gate_internal_bridge_sites`) as external `BridgeInfill`, because the guest `SliceRegionView` had no way to tell them apart; it now splits `bridge_areas` by `internal_bridge_areas` and emits the intersection as `InternalBridgeInfill`. Measured on `calicat.stl` under the matched-oracle profile: 3 `Internal Bridge` layers (z = 4.45 / 18.45 / 29.45) where there were 0, with combined bridge-labelled extrusion 25.39 mm -> 25.49 mm, i.e. a relabel rather than new geometry. **Still open:** the distance from 25.49 mm to canonical's ~950.56 mm reference — most calicat bridge geometry is still not classified as bridge at all and lands under `Internal solid infill`. (Affected section: G-code role labeling / infill emitter; Mitigation owner: infill/emitter track; Target close: infill/emitter role-label parity packet). | Open — 2026-08-25: filed by packet 234a closure review; explicitly out of scope under the owner ruling. Label half closed 2026-09-02 by SchemaBridgeMap ticket 19; classification half remains. |
| DEV-151 | 2026-08-25 | Low | **`enable_extra_bridge_layer` duplicates carry anchor-derived angles instead of canonical parent+π/2.** The carrier-free delivery is retained with owner approval; only duplicate-angle parity remains divergent (Affected section: internal-bridge extra-layer emission; Mitigation owner: slicer-runtime maintainer; Target close: follow-up bridge-angle packet). | Open — 2026-08-25: filed by packet 234a closure. |
| DEV-152 | 2026-08-25 | Low | **`top_solid_infill_flow_ratio` is unrepresentable in the config schema.** It was omitted from the matched profile; canonical treats it as a volume-only knob with no bar impact (Affected section: config schema / top-solid flow resolution; Mitigation owner: config and infill maintainers; Target close: config parity packet). | Open — 2026-08-25: filed by packet 234a closure. |
| DEV-154 | 2026-08-24 | Low | **Support line width is key-based rather than flow-model based.** PnP has no flow model, so `support_line_width` is declared as a `float_or_percent` key: default `0` resolves automatically to `nozzle_diameter`, a percentage resolves against `nozzle_diameter`, and an explicit millimetre value passes through. Canonical `Flow.cpp` `auto_extrusion_width(frSupportMaterial, nozzle_diameter)` returns `nozzle_diameter`, and `opt_key_to_flow_role` maps `support_line_width` to `frSupportMaterial` (Affected section: support-line-width config schema and resolver; Mitigation owner: support module maintainer; Target close: introduce a shared flow model). | Open — 2026-08-24: filed by packet 238a, intentional representation divergence. Renumbered from the originally-filed DEV-148 during the 2026-08-28 merge of `origin/master` into `parity/support-features`: that ID was independently allocated on mainline by packet 234a. |
| DEV-156 | 2026-08-27 | Medium | **Organic tree engine not implemented; canonically-organic styles alias to the classic engine's Strong style.** Canonical `SupportParameters.hpp` resolves `support_style` for a tree support type as: `grid`/`snug` degrade to `smsDefault`, and `smsDefault` selects `smsTreeOrganic` (the `TreeSupport3D.cpp` engine, which `TreeSupport::generate` forks to only for `smsTreeOrganic`). This port implements only the classic `TreeSupport.cpp` engine, so `TreeSupportStyle::from_config` (`modules/core-modules/tree-support-planner/src/lib.rs`) maps every canonically-organic input (`default`, `organic`, `grid`, `snug` on a tree family) to **Strong** — a deliberate product decision (2026-08-27): defaults get the best-verified classic style rather than a silent semantic gap. Explicit `support_style=organic` additionally emits a once-per-slice code-1005 Warn (`organic_substitution_requested`); the plain `default` alias is silent. Non-tree support types resolve `Default`, mirroring canonical's degradation of tree styles off tree types. Note canonical's `is_strong` movement-composition block in `drop_nodes` is dead code (unconditionally overwritten by the final `normal(direction_to_outer)` chain), so aliasing to Strong changes only the neighbour-sum weighting versus canonical Default (Affected section: `TreeSupportStyle::from_config` / `resolve_tree_style` contract surface / `tests/tree_style_styles_tdd.rs`; Mitigation owner: support module maintainer; Target close: organic-engine port packet queued in `docs/specs/support-generation-remediation-plan.md`). | Open — 2026-08-27: alias + Warn implemented; organic engine port queued. Renumbered from the originally-filed DEV-150 during the 2026-08-28 merge of `origin/master` into `parity/support-features`: that ID was independently allocated on mainline by packet 234a. |
| DEV-157 | 2026-08-28 | Low | **PnP models `extra_perimeters` as a global config key while canonical carries it per-`Surface`; carried forward from DEV-132 half (b) when packet `212-extra-perimeters-parity` closed half (a).** Canonical's `Surface::extra_perimeters` (`Surface.hpp`) is written in code by exactly one place — `PrintObject::make_perimeters` (`PrintObject.cpp`) — and the BBS patch short-circuits the body of that per-region loop with a bare `continue`, so the field is in practice always `0` upstream; the only other write is the deserialization helper `from_json(const json&, Surface&)` (`Print.cpp`). Per-`Surface` plumbing would therefore port a dead field. There is likewise no `add("extra_perimeters", ...)` in `PrintConfigDef::init_fff_params` (`PrintConfig.cpp`), so the PnP config key is an invention on both sides of the seam. PnP already exposes per-region configuration through `SliceRegionView::config()` (`crates/slicer-sdk/src/views.rs`); the remaining unresolved distinction is only canonical per-`Surface` `extra_perimeters` versus PnP’s resolved-config representation. **Risk is Low because the canonical counterpart is dead code:** with `surface.extra_perimeters` pinned at `0` upstream, `loop_number = wall_loops + surface.extra_perimeters - 1` in `process_classic` / `process_arachne` (`PerimeterGenerator.cpp`) reduces to the global form PnP already implements, so the divergence has no observable parity cost today. Revisit only if an upstream revision restores the `make_perimeters` loop body (Affected section: `crates/slicer-sdk/src/views.rs` (`SliceRegionView::config()`) / resolved per-region configuration / `modules/core-modules/{classic,arachne}-perimeters` (`[config.schema.extra_perimeters]`, the global key); Mitigation owner: core-modules maintainer; Target close: deferred — revisit if canonical revives per-`Surface` writes). | Open — 2026-08-28: filed by packet `212-extra-perimeters-parity` as the surviving half of DEV-132; canonical dead-field status and the PnP per-region seam gap both verified. |
| DEV-158 | 2026-08-28 | Low | **Raft layers are not rendered in silhouette side views.** `SupportPlanIR` entries with a negative `global_layer_index` are raft prefix layers, and the caller's layer schedule has no slab for them, so the silhouette renderer has no honest vertical extent to project and skips them under warning W1, which names the skipped entry count and the dropped index range. Drawing them against a neighbouring layer's slab would misstate raft height, so the omission is warned rather than guessed (Affected section: `docs/19_visual_debug.md` "Silhouette Side Views (schema 1.2.0)" warnings inventory; the silhouette renderer's slab derivation; Mitigation owner: visual-debug maintainer; Target close: derive honest raft slabs from raft height/layer-count config so raft side-view rendering is exact). | Open — 2026-08-28: filed by packet 247. |
| DEV-165 | 2026-09-02 | Medium | **Per-region support territory: a modifier-minted sub-region whose `support_type` family differs from its parent's confines each family to its own side of the modifier boundary; Orca has no per-region support family and therefore no such rule.** Canonical `support_type` is print-global, so `TreeSupport` and `PrintObjectSupportMaterial` never coexist on one object and never need territory. PnP routes per region (ticket 18/19), so both planners emitted bodies under the same overhang and the host cross-family guard rejected both sides on every overlapping layer (measured on `resources/support_test_modifier_normal_in_tree.3mf`: tree stopped at z = 18.0, no traditional support). The host now publishes `SupportAnalysisIR::support_territory` (schema 1.3.0), the tree planner bars foreign territory in its collision ladder and carves it from roles, the traditional planner clips its carry, and aggregation clips instead of rejecting (Info 1205). Clearance = support line width on the base side only. (Affected section: `docs/02_ir_schemas.md` IR 9a′; `SupportAnalysisView::territory_partition`; `slicer_wasm_host::support_territory`; Mitigation owner: support-family maintainer; Target close: none — intentional, PnP-only behaviour.) | Open — 2026-09-02: filed by SchemaBridgeMap ticket 19 (reopened). Renumbered from the originally-filed DEV-159 during the 2026-09-03 merge of `origin/parity/support-features` into `master`: that ID was independently allocated on the branch by packet 239c's seam-completion closure (now DEV-159..163). |
| DEV-164 | 2026-08-28 | Medium | **`cargo xtask build-guests --check` reports stale on a freshly built tree; the code-input fingerprint is the defect, not the artifacts.** Measured 2026-08-28 on an unmodified tree, before any packet edit: `--check` returned exit **1** with **33** stale guests, all reported as "fingerprint mismatch". A full `cargo xtask build-guests` then succeeded ("built 44 guest(s)", exit 0) and `--check` **still** returned exit 1 with the same 33; the count oscillated 33 -> 14 -> 33 across consecutive runs with no intervening edit. Exit was 1 and not 3, so `wasm-tools` is present and the WIT decode path works — the intended artifact-verification contract is distinct from the observed defect: the failure is confined to the per-guest code-input fingerprint derived from the Cargo path-dependency closure, not to WIT staleness or missing tooling. Corroborating evidence that the artifacts are in fact fresh: the `contract` binary's own `guest_fixture_freshness_tdd::guest_components_are_not_stale` **passes** on the same tree in the same session, so the two freshness oracles disagree and the artifact-decoding one says clean. **Consequence while open:** the gate `CLAUDE.md` §"Guest WASM Staleness" makes mandatory before attributing any guest/component/module-dispatch failure to one's own changes cannot be satisfied, so its exit code carries no signal and the rule it enforces is unenforceable. This must NOT be read as licence for "unrelated to my changes" deflection — the correct reading is that the gate is broken, not that staleness is harmless. Independently reproduced during the packet 239a closure review (host-only edit list; exit 1, 33 stale, all "fingerprint mismatch") (Affected section: `cargo xtask build-guests --check` fingerprint derivation in `xtask`; Mitigation owner: build-tooling maintainer; Target close: TBD — needs its own packet). | Open — 2026-08-28: filed from packet `239a-anchored-host-seams` deviation D4, which measured it but had no durable home for it; root cause narrowed to the code-input fingerprint, not diagnosed further. Renumbered from the originally-filed DEV-158 during the 2026-09-03 merge of `origin/parity/support-features` into `master`: that ID was independently allocated on mainline by packet 247 (raft silhouette rendering). |
| DEV-195 | 2026-08-28 | Low | **`precise_outer_wall` stays off by default where canonical defaults it on, because turning it on reorders classic-perimeters' wall emission.** OrcaSlicer declares `precise_outer_wall` as `coBool` defaulting to `1` (`docs/ORCA_CONFIG_REFERENCE.md`, the upstream snapshot; canonical read site is `PerimeterGenerator::process_classic` in `PerimeterGenerator.cpp`, which uses the flag to adjust outer-wall spacing only). Both PnP perimeter modules declare it `false` (`precise_outer_wall` in `modules/core-modules/classic-perimeters/classic-perimeters.toml` and `modules/core-modules/arachne-perimeters/arachne-perimeters.toml`, matched by the `unwrap_or` fallbacks in each module's `from_config`). Ticket 100 flipped both to `true` for parity and measured the result: `classic-perimeters` then emits the inner loop first and shifts every perimeter index by one — `concave_region_emits_outer_wall_without_panic` fails with "first wall must be the outer loop" (left `Inner`, right `Outer`) and `inner_walls_correct_type` fails with "Wall 1 should have perimeter_index 1" (left `2`, right `1`). Reverting only this key returns all 24 `classic-perimeters` tests to green, which isolates it as the sole cause. So the divergence is real but is **not** a default-alignment: PnP's precise-outer-wall path changes wall *ordering*, where canonical's changes only spacing. Aligning the default requires fixing that ordering defect first, which is packet work. (Affected section: `docs/15_config_keys_reference.md` §Deviations from OrcaSlicer; Mitigation owner: classic-perimeters / arachne-perimeters maintainer; Target close: the quality/walls packet that owns `precise_outer_wall`) | Open — 2026-08-28: default held at `false` in both modules; ordering defect measured and isolated by ticket 100, not yet fixed. Filed as DEV-158 during the 2026-09-03 master-into-wayfinder merge; that ID was independently allocated on mainline by packet 247 (raft silhouette rendering), and DEV-159..165 were taken by the support-features merge. Renumbered to DEV-195 in the merge resolution: DEV-166 was independently allocated on mainline by packet 241 (agg rasterizer), and upstream drafts claim up to DEV-192. |
| DEV-196 | 2026-09-03 | Low | **`printable_height` defaults to 250.0 mm where canonical defaults to 100.0 mm.** OrcaSlicer declares it `coFloat` with `set_default_value(new ConfigOptionFloat(100.0))` in `PrintConfigDef::init_fff_params` (`PrintConfig.cpp`) and rejects a print whose top layer exceeds it in `Print::validate` (`Print.cpp`). This port adopts the key and the rejection (`validate_printable_height`, `crates/slicer-model-io/src/loader.rs`, called from `run_slice` in `crates/slicer-runtime/src/run.rs`) but defaults it to `250.0`, matching two values this tree already ships: the 250 x 250 mm `printable_area` default in the `declare_resolved_config!` invocation (`crates/slicer-ir/src/resolved_config.rs`) and the `("printable_height", "250")` literal the emitter's `ORCA_CONFIG_PADDING` (`crates/slicer-gcode/src/serialize.rs`) has always advertised for this key. Canonical's 100.0 is a profile fallback that every real printer preset overrides; adopting it together with a new hard rejection would newly fail every model over 100 mm tall under default config, in a tree that had no build-height gate at all before this change. Also divergent in subject: canonical compares the last sliced layer's Z, this check compares the object's world-space mesh extent before slicing, so the two agree except within one layer height of the limit, where this form is stricter. User ruling, 2026-09-03, wayfinder map ticket 26. (Affected section: `docs/15_config_keys_reference.md` §Deviations from OrcaSlicer; Mitigation owner: slicer-gcode / slicer-runtime maintainer; Target close: none — intended) | Open — 2026-09-03: intended deviation, default held at `250.0`; rejection verified by `cargo test -p slicer-model-io --test printable_height_tdd`(8 passed, 0 failed). Renumbered to DEV-196 in the merge resolution: DEV-167 was independently allocated on mainline by packet 241b (producer invariant), and upstream drafts claim up to DEV-192. |
| DEV-168 | 2026-09-03 | Low | **`printer_structure` gates time-lapse injection, but three clauses of canonical's gate diverge.** Canonical `GCode::process_layer` (`GCode.cpp`) computes `need_insert_timelapse_gcode_for_traditional = (is_i3_printer && !m_spiral_vase) &#124;&#124; is_multi_extruder`, where `is_i3_printer` is `printer_structure == PrinterStructure::psI3`. This port declares the key on `machine-gcode-emit` (the module that owns the `time_lapse_gcode` injection site, `modules/core-modules/machine-gcode-emit/machine-gcode-emit.toml`) and suppresses the TimeLapse template in `run_gcode_postprocess` (`modules/core-modules/machine-gcode-emit/src/lib.rs`). Three divergences: (a) **`undefine` — the default — does not suppress**, where canonical injects nothing on an undefined single-extruder machine; this tree had no structure gate at all and setting `time_lapse_gcode` has always been sufficient, so suppressing at the default would silently drop output for every existing user. (b) the **`!m_spiral_vase` clause is unwired** — this port has no spiral mode (`spiral_mode` is an unimplemented queue key, pinned by `arachne_parity_gaps.rs`); when spiral lands it must extend this gate. (c) canonical's **`is_multi_extruder` is `nozzle_diameter.size() > 1`**, a printer property; no extruder-count key reaches a `PostPass` module here, so the stand-in is "this print performs a toolchange" (a `GCodeCommand::ToolChange` in the command stream), which differs on a multi-extruder printer running a single-tool print. (d) canonical's **outer clause `(!m_wipe_tower &#124;&#124; !m_wipe_tower->enable_timelapse_print())` is not wired**: canonical computes the traditional gate only when no prime tower is present *or* the tower does not have smooth timelapse enabled, because in `timelapse_type = smooth` the tower's outer wall is itself the snapshot target (`WipeTower::only_generate_out_wall`, prepended per layer in `WipeTower::generate`). `timelapse_type` does not exist in this tree, and neither does the smooth-mode tower body it selects — `WipeTowerModule::process` and `run_finalization` (`modules/core-modules/wipe-tower/src/lib.rs`) both skip any layer whose `tool_changes` is empty, so there is no wall to wipe on and nothing to suppress. Wayfinder ticket 31 folded `timelapse_type` into ticket 122 (prime tower body parity), which owns wiring this clause. Filed by wayfinder map ticket 27 (P20). (Affected section: `docs/15_config_keys_reference.md` §Deviations from OrcaSlicer; Mitigation owner: machine-gcode-emit maintainer; Target close: (b) with the spiral-mode packet, (c) if an extruder-count key reaches the postpass seam; (a) is intended) | Open — 2026-09-03: gate live and verified by `cargo test -p machine-gcode-emit --test machine_gcode_emit_tdd` (4 new tests) and `printer_structure_gates_time_lapse_injection_end_to_end` (`crates/slicer-runtime/tests/integration/machine_start_end_gcode_emission_tdd.rs`). 2026-09-03: clause (d) added by wayfinder ticket 31 (no code change; owned by map ticket 122). |
| DEV-169 | 2026-09-03 | Low | **`flush_volumes_matrix` / `flush_multiplier` are live per-filament-pair purge volumes, but four clauses of canonical's derivation diverge.** Canonical `WipeTower2::extract_wipe_volumes` (`WipeTower2.cpp`) builds `wipe_volumes[from][to]` from `flush_volumes_matrix`, scales it by `flush_multiplier`, and feeds it to `plan_toolchange`, where it sets that toolchange's purge-box depth. This port declares both keys on `wipe-tower` (`modules/core-modules/wipe-tower/wipe-tower.toml`) and routes the selected volume through `WipeTower::purge_volume_for` into the scan-line depth and the prime entity's extruded length (`modules/core-modules/wipe-tower/src/lib.rs`). Four divergences: (a) **no matrix means the flat `prime_volume`, not zero** — canonical always has a preset-supplied matrix and zeroes it unless `purge_in_prime_tower && single_extruder_multi_material`, neither of which exists in this tree (both sit in P02), so falling back keeps the tower working instead of silently emitting an empty one; (b) **`flush_multiplier` scales matrix entries only**, never the `prime_volume` fallback, which would otherwise cut every unconfigured print's purge to 30% of its declared volume; (c) **one scalar multiplier, not one per extruder** — canonical's `flush_multiplier` is a `coFloats` that `ToolOrdering::prepare_flush_matrices` indexes per nozzle, while `WipeTower2::extract_wipe_volumes` reads `get_at(0)`; this port matches the `WipeTower2` reading because per-tool config scoping is unavailable (inventoried by wayfinder ticket 118); (d) **`filament_minimal_purge_on_wipe_tower`**, canonical's per-filament lower clamp on each entry, is Tier D per-filament config not present in this tree, so no clamp is applied. Also: a non-square matrix is **rejected** at construction rather than truncated as canonical's `sqrt(size)` slicing would, because a mis-sized matrix mis-indexes every pair. Filed by wayfinder map ticket 30 (P23). (Affected section: `docs/15_config_keys_reference.md` §Deviations from OrcaSlicer; Mitigation owner: wipe-tower maintainer; Target close: (a)+(b) when P02 lands `purge_in_prime_tower` / `single_extruder_multi_material`, (c) when ticket 118's per-tool mechanism lands, (d) with the per-filament config model) | Open — 2026-09-03: keys live and verified by `cargo test -p wipe-tower` (7 new tests, incl. `flush_multiplier_scales_emitted_purge_geometry` and `flush_matrix_changes_emitted_purge_geometry_against_prime_volume`). |
| DEV-170 | 2026-09-05 | Low | **`grab_length` is live but declared scalar where canonical is a per-extruder `coFloats`.** Canonical reads `grab_length.get_at(new_extruder_id)` at both read sites — the `GCode.cpp` toolchange path (`grab_purge_volume = m_config.grab_length.get_at(new_extruder_id) * 2.4; wipe_volume = std::max(0.f, wipe_volume - grab_purge_volume)`) and the `Print.cpp` wipe-tower planning (`grab_extruder_id = filament_maps[filament_id] - 1`, same formula) — so one value per extruder. This port declares the key on `wipe-tower` (`modules/core-modules/wipe-tower/wipe-tower.toml`) as a scalar float (default 0, min 0) and applies the reduction inside `WipeTower::purge_volume_for` (`modules/core-modules/wipe-tower/src/lib.rs`), the single decision point feeding both the purge box's scan-line depth and the prime entity's extruded length. Two divergences: (a) **one scalar, not one per extruder** — per-tool config scoping is unavailable (inventoried by wayfinder ticket 118), the `flush_multiplier` precedent (DEV-169 (c)); (b) **the reduction applies to the `prime_volume` fallback too** — canonical always has a preset-supplied matrix, so it has no opinion on the fallback; the grab volume is extruded during the change regardless of the purge's source, and the default 0 keeps every existing print unchanged. The `2.4` cross-section constant (`(diameter/2)^2*PI` for 1.75 mm filament) is kept verbatim from both canonical read sites rather than derived from the port's per-filament `filament_diameter` (Tier D fog). Filed by wayfinder map ticket 40 (P33). (Affected section: `docs/15_config_keys_reference.md` §Deviations from OrcaSlicer; Mitigation owner: wipe-tower maintainer; Target close: (a) when ticket 118's per-tool mechanism lands, (b) none — intended) | Open — 2026-09-05: key live and verified by `cargo test -p wipe-tower` (4 new tests: `grab_length_absent_is_identity`, `grab_length_reduces_purge_volume`, `grab_length_clamps_purge_volume_at_zero`, `grab_length_reduces_emitted_purge_geometry`); `cube_4color_gcode_output_tdd` 9/9 green; guests rebuilt; clippy + check-literals + gen-config-docs clean. |
| DEV-171 | 2026-09-06 | Low | **`filament_flush_temp` / `filament_flush_volumetric_speed` live as scalar placeholder keys where canonical holds per-filament vectors with Tier D fallbacks.** Canonical declares `filament_flush_temp` (`coInts` nullable, default 0, max `max_temp` = 1500) + `filament_flush_volumetric_speed` (`coFloats` nullable, default 0, max 200) and reads them per filament at every `GCode.cpp` toolchange/placeholder site (`update_placeholder_parser_with_variant_params`, `set_extruder`, toolchange config builder via `get_at`), publishing the derived `flush_temperatures` / `flush_volumetric_speeds` placeholders for custom G-code; a 0 entry falls back to `nozzle_temperature_range_high` / `filament_max_volumetric_speed` (both Tier D deferred). This port declares both keys on `machine-gcode-emit` (`modules/core-modules/machine-gcode-emit/machine-gcode-emit.toml`) as scalars (temp int default 0 max 1500, speed float default 0.0 max 200.0) with matching `ResolvedConfig` fields (`crates/slicer-ir/src/resolved_config.rs`), published as-is through the module's generic `[key]` substitution (`format_placeholder_value`, `modules/core-modules/machine-gcode-emit/src/lib.rs`). Three divergences: (a) **one scalar, not one per filament** — per-tool values ride `tool_config:<idx>:` overrides, Orca vector ingest rides ticket 125 (the `flush_multiplier` DEV-169 (c) / `grab_length` DEV-170 (a) precedent); (b) **0 publishes as-is with no Tier D fallback** — at defaults a custom template sees `0` where canonical would see the range-high / max-volumetric value; default templates are empty so no shipped print changes; (c) **raw key names, not canonical's plural derived names** — a template spelling `[flush_temperatures]` stays unresolved (warned, passed through verbatim) rather than resolving. Filed by wayfinder map ticket 47 (P40). (Affected section: `docs/15_config_keys_reference.md` §Deviations from OrcaSlicer; Mitigation owner: machine-gcode-emit maintainer; Target close: (a) with the per-filament config model (ticket 125); (b)+(c) with it or never — intended) | Open — 2026-09-06: keys live and verified by `cargo test -p machine-gcode-emit` (2 schema + 2 placeholder-render + 35 harness green), `cargo test -p slicer-ir` green, `cargo test -p slicer-gcode` 17 binaries green; 46 guests rebuilt; workspace clippy + check-literals clean. |

| DEV-094 | 2026-09-08 | Medium | **Packet 200: seven wasm32 SDK host-service wrappers were bridged, including mesh queries, polygon operations, and `now_us`; classic-perimeters migrated to the wrappers and support-planner's collision-cache batch path was verified. ADR-0055 fuel and wall-clock before/after evidence answers the in-guest versus host-native question.** (Affected section: SDK host bridge, classic-perimeters, support-planner; Mitigation owner: slicer-runtime maintainer; Target close: —) | Closed — packet 200: seven-wrapper bridge, classic-perimeters migration, support-planner verification, and ADR-0055 evidence complete. |
| DEV-123 | 2026-09-08 | Low | Packet 206 seam-paint delivery: seam paint no longer partitions regions and is delivered as aligned segment annotations. | Closed — packet 206: seam-paint delivery closure. |
| DEV-133 | 2026-09-08 | Low | Packet 206 seam-paint delivery: exact `seam_enforcer`/`seam_blocker` semantic classification replaces substring matching. | Closed — packet 206: seam-paint delivery closure. |
| DEV-134 | 2026-09-08 | Low | Packet 206 seam-paint delivery: seam paint reaches both classic and arachne perimeter bias/planning paths. | Closed — packet 206: seam-paint delivery closure. |
| DEV-122 | 2026-09-08 | Medium | Packet 207 resolved shell and width configuration per painted `variant_chain` and object, re-keyed `painted_subsets`, repaired `RoleWidthContext`, removed the dead fallback arm, and retained the painted-chain → BASE-chain → default ladder. | Closed — packet 207: per-variant_chain/per-object resolution and fallback repair. |
| D-209-SUPPORT-FILL-BEHAVIOUR-CHANGE | 2026-09-08 | Medium | Packet 209 intentionally changes support-fill output: rotated-bbox scan start, half-open vertex handling, zero-length-span removal, bbox-centre rotation, and removal of the centroid fallback; support-specific rotation/fallback semantics are affected. | Recorded — packet 209: intentional support-fill behaviour change; affected support modules remain duplicated pending pattern services. |
| D-209-IRONING-FILL-BEHAVIOUR-CHANGE | 2026-09-08 | Low | Packet 209 intentionally changes ironing output: bbox-min scan start, half-open vertex handling, and zero-length-span removal; ironing remains unrotated by design. | Recorded — packet 209: intentional ironing-fill behaviour change. |
| DEV-132 | 2026-09-08 | Medium | Packet 212 closes half (a): the `extra_perimeters` read/config path is generator-symmetric; the surviving per-`Surface` canonical modelling distinction is carried by DEV-157. | Closed — packet 212: half (a) closed; half (b) retained in DEV-157. |
| DEV-182 | 2026-09-08 | Medium | Canonical tree support represents the top gap with a virtual gap node; PnP lowers the contact layer itself. Both produce the observable one-layer gap, but canonical roof-gap behaviour is not representable until the owning tree-planner packet resolves the model. | Open — packet 224 closure evidence: virtual-gap-node versus contact-layer lowering remains unresolved. |
| DEV-175 | 2026-09-08 | Low | Packet 226 authored-coloring carrier has no Orca precedent for the per-path carrier/grant seam; the purge/strip volume and associated implementation cost are accepted. | Closed — packet 226: authored-coloring no-Orca-precedent/purge-volume-cost deviation. |
| DEV-176 | 2026-09-08 | Medium | Packet 238b resolves the tree-planner smoothing decision separately from DEV-141: production smoothing is retained before emission gates; DEV-141's `smooth_outward` divergence is unrelated to tree smoothing. | Open — dated cross-reference 2026-09-08: packet 238b resolved the tree-planner smoothing question separately. |
| DEV-177 | 2026-09-08 | Low | Packet 238b retains the deliberate tree-planner smoothing arithmetic deviation: f64 scaled relaxation and round-on-commit rather than canonical integer truncation on every pass. | Closed — packet 238b: remaining DEV-142 arithmetic/smoothing disposition resolved. |
| DEV-178 | 2026-09-08 | Medium | Packet 238b records that the `need_extra_wall` carrier remains a coverage gap: `wall_counts` transport is represented in the IR evidence, but no renderer consumes the per-node requirement. | Open — packet 238b: coverage gap remains after `wall_counts` carrier evidence. |
| DEV-179 | 2026-09-08 | Low | The support smoothing precision deviation applies to both legacy `support-planner` (`f32` mm node math) and `tree-support-planner` (f64 scaled relaxation/round-on-commit); separate canonical waivers cover their distinct arithmetic paths. | Open — packet 238b: legacy and tree-support-planner paths remain explicitly waived. |
| DEV-180 | 2026-09-08 | Medium | Canonical `generate_support_layers` groups candidate Z values by minimum-height semantics, but `SupportPlanEntry` has no height field; affected support IR/planner sections cannot represent that per-entry rule. | Open — packet 239d: owner support-IR/planner track; mitigation is to add a height representation or document the closure decision. |
| D-240B-ADR-0009-AMENDED | 2026-09-08 | Low | Packet 240b amends ADR-0009: Decision-5 claim ownership is reassigned to `com.core.raft-default`, and AD-240B-1 absorbs the transport/emitter responsibility into the raft module. | Accepted — packet 240b: ADR-0009 amendment and AD-240B-1 absorption recorded. |
| DEV-167 | 2026-09-08 | Medium | Packet 241b retains `merge_region_identity_entries` as the producer invariant: one declared identity owns each merged support-plan entry. | Closed — packet 241b: merge_region_identity_entries retained as producer invariant. |
| DEV-181 | 2026-09-08 | Medium | Packet 241b amends ADR-0059's routing-cell decision: declared identity, not routing-cell ownership, is the support-plan ownership contract. | Closed — packet 241b: ADR-0059 routing-cell decision amended to declared-identity ownership. |
| D-233-INTERNAL-BRIDGE-QUALIFICATION-COVERAGE | 2026-09-08 | Medium | Packet 233 supersedes the earlier acceptance criteria for internal-bridge qualification. Unresolved coverage includes qualification, low-Z classification, role-labeling, and extra-layer-angle behaviour; these remain cross-referenced to DEV-148..153 and are not claims of canonical parity. | Open — packet 233: superseded acceptance criteria explicitly retained as unresolved coverage. |
| D-246-WAVE-OVERHANG-ANCHOR-DEPTH-DEFAULT | 2026-09-08 | Medium | Wave-overhangs deliberately use the PnP anchor-depth default `min(3mm, spacing × (wall_count + 1)) + base_spacing`, rather than canonical's auto reference; the default ensures waves engage instead of silently falling back to rectilinear. | Accepted — packet 246: deliberate PnP anchor-depth default deviation. |

| DEV-183 | 2026-09-08 | Medium | Packet 238c implements the bottom-interface support path (`SupportPlanRole::BaseInterface` / `ExtrusionRole::SupportBaseInterface`); this was previously absent from the renderer contract (former DEV-129). | Closed — packet 238c: bottom-interface support implemented. |
| DEV-184 | 2026-09-08 | Low | Packet 238c adopts canonical default support-interface flow value `0.5` while retaining the legacy negative sentinel for compatibility (former DEV-145). | Closed — packet 238c: canonical 0.5 default and legacy negative sentinel documented. |
| DEV-185 | 2026-09-08 | Medium | Packet 238c uses the interface-flow-over-line-width mechanism to derive support interface flow rather than treating line width as the complete flow contract (former DEV-146). | Closed — packet 238c: interface-flow-over-line-width mechanism recorded. |
| DEV-186 | 2026-09-08 | Medium | Packet 238c retires `support_density`; support density is derived from spacing/configuration instead of the legacy density key (former DEV-155). | Closed — packet 238c: spacing-derived density replaces support_density. |
