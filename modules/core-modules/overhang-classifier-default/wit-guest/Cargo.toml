[package]
name = "overhang_classifier_default_guest"
version = "0.1.0"
edition = "2021"

[workspace]

[dependencies]
overhang-classifier-default = { path = ".." }

[target.'cfg(target_arch = "wasm32")'.dependencies]
wit-bindgen = "0.57.1"

[lib]
crate-type = ["cdylib"]
