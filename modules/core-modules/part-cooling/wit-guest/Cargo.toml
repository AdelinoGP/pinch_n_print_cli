[package]
name = "part_cooling_guest"
version = "0.1.0"
edition = "2021"

[workspace]

[dependencies]
part-cooling = { path = ".." }

[target.'cfg(target_arch = "wasm32")'.dependencies]
wit-bindgen = "0.60.0"

[lib]
crate-type = ["cdylib"]
