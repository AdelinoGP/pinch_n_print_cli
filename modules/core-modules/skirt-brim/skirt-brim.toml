[module]
id           = "com.core.skirt-brim"
version      = "0.1.0"
display-name = "Skirt Brim"
description  = "Core skirt brim module"
author       = "modular-slicer"
license      = "MIT"
wit-world    = "slicer:world-finalization@1.0.0"

[stage]
id = "PostPass::LayerFinalization"

[ir-access]
reads  = ["LayerCollectionIR"]
writes = ["LayerCollectionIR"]

[claims]
holds    = []
requires = []

[compatibility]
incompatible-with = []
requires          = []
min-host-version  = "0.1.0"
min-ir-schema     = "1.0.0"
max-ir-schema     = "2.0.0"

[config.schema]
skirt_brim_enabled = "bool"
skirt_loops         = "int"
skirt_distance      = "float"
skirt_height        = "int"
brim_width          = "float"
line_width          = "float"

[config.overridable-per-region]
keys = []

[config.overridable-per-layer]
keys = []

[hints]
estimated-ms-per-layer = 10
layer-parallel-safe    = false
