//! Library surface for `pnp_cli` — exposed so integration tests can call
//! scaffolding functions directly without going through the binary.

pub mod io;
pub mod module_new;
pub mod profile_cmd;
pub mod visual_debug;
