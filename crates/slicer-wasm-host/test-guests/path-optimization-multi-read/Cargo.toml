[package]
name = "path-optimization-multi-read"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]
name = "path_optimization_multi_read_guest"

[dependencies]
wit-bindgen = "0.57.1"
slicer-sdk = { path = "../../../slicer-sdk" }
slicer-ir  = { path = "../../../slicer-ir" }
slicer-schema = { path = "../../../slicer-schema" }

[workspace]
