[package]
name = "slicer-host"
version = "0.1.0"
edition = "2021"
authors = ["ModularSlicer Team"]

[dependencies]
slicer-ir = { path = "../slicer-ir" }

[dev-dependencies]
tempfile = "3"

[lints.rust]
missing_docs = "warn"
unused_imports = "warn"
unused_must_use = "warn"
