paint_only = true

[module]
id           = "com.test.paint-only"
version      = "0.1.0"
display-name = "Paint Only Community"
description  = "Fixture: paint_only = true with a community region_split semantic"
author       = "test"
license      = "MIT"

[stage]
id = "Layer::Infill"

[ir-access]
reads  = []
writes = []

[claims]
holds    = []
requires = []

[compatibility]
incompatible-with = []
requires          = []
min-host-version  = "0.1.0"
min-ir-schema     = "1.0.0"
max-ir-schema     = "5.0.0"

[hints]
layer-parallel-safe = true

[[region_split]]
semantic   = "com.example.paint-only"
priority   = 1700
value_type = "custom_string"
