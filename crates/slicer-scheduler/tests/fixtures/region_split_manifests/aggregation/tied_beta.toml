[module]
id           = "com.test.agg-tied-beta"
version      = "0.1.0"
display-name = "Agg Tied Beta"
description  = "Aggregation fixture: com.example.beta@1500 (tied priority)"
author       = "test"
license      = "MIT"
wit-world    = "slicer:world-layer@1.0.0"

[stage]
id = "Layer::Infill"

[ir-access]
reads  = []
writes = []

[claims]
holds    = []
requires = []

[compatibility]
incompatible-with = []
requires          = []
min-host-version  = "0.1.0"
min-ir-schema     = "1.0.0"
max-ir-schema     = "5.0.0"

[config.overridable-per-region]
keys = []

[config.overridable-per-layer]
keys = []

[hints]
layer-parallel-safe = true

[[region_split]]
semantic   = "com.example.beta"
priority   = 1500
value_type = "flag"
