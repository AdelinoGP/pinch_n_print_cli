[module]
id           = "com.test.agg-b"
version      = "0.1.0"
display-name = "Agg Module B"
description  = "Aggregation fixture B: fuzzy_skin@200"
author       = "test"
license      = "MIT"
wit-world    = "slicer:world-layer"

[stage]
id = "Layer::Infill"

[ir-access]
reads  = []
writes = []

[claims]
holds    = []
requires = []

[compatibility]
incompatible-with = []
requires          = []
min-host-version  = "0.1.0"
min-ir-schema     = "1.0.0"
max-ir-schema     = "5.0.0"

[config.overridable-per-region]
keys = []

[config.overridable-per-layer]
keys = []

[hints]
layer-parallel-safe = true

[[region_split]]
semantic   = "fuzzy_skin"
priority   = 200
value_type = "flag"
