paint_only = true

[module]
id           = "com.test.agg-paint-only"
version      = "0.1.0"
display-name = "Aggregation Paint Only"
description  = "Aggregation fixture: paint_only = true with a community semantic at 1700"
author       = "test"
license      = "MIT"

[stage]
id = "Layer::Infill"

[ir-access]
reads  = []
writes = []

[claims]
holds    = []
requires = []

[compatibility]
incompatible-with = []
requires          = []
min-host-version  = "0.1.0"
min-ir-schema     = "1.0.0"
max-ir-schema     = "5.0.0"

[hints]
layer-parallel-safe = true

[[region_split]]
semantic   = "com.example.paint-only"
priority   = 1700
value_type = "custom_string"
